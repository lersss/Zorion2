# Контекст проекта для субагентов (сводка)

> Живая сводка, разносит **`@dispatcher`** по дельтам менеджера (раздел «Разнос
> по докам» в идеях, применяется в паузу). Субагенты читают ЕГО вместо
> `STATUS.md`/`docs/INDEX.md` целиком. Источник истины — `STATUS.md`, `docs/` —
> их читай, только если задача требует глубины. Один факт — одно место: здесь
> только ссылки и текущее состояние, не история.

**Обновлено:** 2026-09-22

## Фокус

Активной задачи нет — команда простаивает.
Очередь — у создателя и в 99_roadmap.md.
Долги: 🔴 B24, B27 (Restore полёта: позиция «в полёте» при битой цели), открытые решения и P1–P5 — STATUS.md §Открытые решения.

## Баги (активные)

Активные баги — реестр в `STATUS.md` §Баги (включая B24). Закрытые баги и фичи — `CHANGELOG.md` (по датам); детали фич — в спеках `docs/specs/`.

## Указатели для будущих задач

- NPC (26a, сдано 2026-09-15): работая с NPC, читай спеку `26a.1` и идею `26a`;
  следующие шаги — бенчмарк 26b (10-50-100к), wildcard-экранирование в поиске (🟢);
  оптимизация тика 26c (пакетные UPDATE + кэш dirty) — замер выигрыша за создателем.
- Двойные системы (35b/99.2.18): API планет отдаёт orbit_center/orbit_radius_au/circumbinary;
  вода/поверхности и генератор редких звёзд — будущие фичи.
- Расы (99.2.21/99.2.22): сданы 2026-09-17; перенос старого генератора поселений
  в расовую модель (65a) выполнен — см. STATUS.md §1 и CHANGELOG 2026-09-17.
- Пресеты генерации (72a): при ручном вводе minDist ≥ 200 с радиусом 2500 и
  200 кластерами возможен недобор — компенсировать радиусом ≥ 3000 / кластерами ≥ 300.
- Экзотика звёзд (41a): старые миры — возраст «—», бэкфилл запрещён; спеки
  `41a-exotic-star-card.md` и `99.2.20-planet-physics-cascade.md`.
- Корабль/оборудование (91a): сдано 2026-09-18 — раздел «Корабль» на дашборде
  (силуэт со слотами), двигатель-1 настоящий (миграция 000043), полёт требует
  двигателя (player; админ — исключение), селектор внешнего вида 61b — легаси;
  справочник энциклопедии — далёкий бэклог; спека `91a-ship-section-dashboard.md`.
- PITFALLS читается по разделам (БД/шелл, Go и конкурентность, Дизайн и числа,
  Фронт и UI, Прочее); `-race` доступен локально (gcc WinLibs) — DoD с детектором
  гонок (идея 93a).
- DoD: два уровня — быстрый цикл
  `go test -race -short (go list ./... | ? { $_ -notmatch 'generator/(planet|galaxy)$' })`
  всегда; `go test ./internal/generator/planet/ ./internal/generator/galaxy/` без
  `-race` при правке генератора (идея 2026-09-21, два уровня прогонов).
- Видимость игрока (77a, 2026-09-18): «открытая звезда, закрытая система» — вид
  звезды (имя/спектр/цвет/размер/координаты) виден везде, полёт к любой звезде,
  радиус радара (800/200 px) решает только для деталей системы (модалка 403 →
  карточка звезды с заглушкой), сканер + знание 7 дней; сущности ship_models +
  equipment, миграции 000040-000042; сырые идеи 77b (транспондер) и 77c (карта
  региона) ждут рынков.
- Каталог ресурсов + потребление рас (94a, 2026-09-18): универсальный слой 20
  ресурсов + 13 шаблонов хемотипов в `internal/resource/` (layer/chemotypes/diet/
  checks), вкладка админки «Ресурсы» (GET /admin/resources); имена — заглушки,
  финальные @writer; схема БД substances/resources — следующий релиз; термины
  создателя: «потребляет», «потребительская корзина» = «корзина тира», «ресурс»
  (не «вещество»).
- Ресурсы (2026-09-18): базовый слой 94a (20 ресурсов, `internal/resource/layer.go`)
  + витрина 111 реальных веществ (`real.go` — НЕ контент, граница в 99.2.5) во
  вкладке «Ресурсы» админки (подвкладки, 6 форматов, окна по одному, °C);
  ёмкость модели 86/107/43 (`distance.go` — проверка мин. расстояния для
  будущего генератора полей).
- Дашборд игрока (2026-09-18): шапка всегда видима + 4 вкладки (Корабль/Внешний
  вид/Статистика/Энциклопедия), активная вкладка в localStorage `dashboardActiveTab`.
- Полёты игрока (97a, сдано 2026-09-18): активный полёт персистится в
  player_flights (миграция 000044); Restore при старте сервера — прибывшие
  засчитываются сразу, летящие продолжаются с остатка; работая с travel — читай
  идею 97a, travel.Manager (FlightStore, Restore, TOCTOU-гвард), репозиторий
  player_flight_repository.go
- Студия товаров — часть игрового сервера (перенос A/B/C завершён 2026-09-20):
  UI `/studio` (web/studio.html), каталог в БД (categories/goods/goods_slots,
  миграция 000045, сид 131, БД = источник правды), `/studio/api/*` (JWT
  admin/skycomposer), ресурсы без привилегий, ИИ fill двухфазный «предложи →
  подтверди» (попап с вариантами, полный отчёт), витрина/слой админки
  «Ресурсы» читают БД (props ? 'family'/'closes'); общий модуль авторизации
  auth.js (ядро + адаптеры admin/studio); cmd/goods-studio + goods_data/
  config/goods удалены (импорт top-каталога/деревьев Т8 отменён решением
  создателя 2026-09-20, каталог наполняется через студию).
- Студия «Производство» (2026-09-21): категории строений конфигурируются
  слотами родителя — producer_slots (миграция 000052, расово-зависимо:
  универсальная база + переопределения семейств/рас), скрытая категория =
  hidden на слоте (не показывается, галка «показать скрытые» в шапке),
  инвариант «подтип kind=goods только при применяемом слоте»; согласование
  снято (статусов нет), скрытие — только у фабрик (обратимый флаг), канон
  «самая конкретная побеждает», дефолт веса/объёма 1/1, миграция 000054;
  следующий шаг — влияние конфигурации на игру (релиз 2 фабрик).
- Люди → F0 (2026-09-21): семейства F0–F9 (F0 «Люди» — отдельное семейство,
  F1 — чисто водный); данные/валидация/студия (переключатель расовости — 11
  семейств, «F0 Люди» первый); следующее — расовые фабрики и скрытые категории
  (идеи записаны).
- Арт-студия (98a/98b, 2026-09-20): фильтрация словарей по blocked (appearance/blocked в
  families.json), доп. теги tags и override итогового промпта в UI; внешность рас 59/59
  (appearance/blocked в families.json, разделы в races/*.md); выбор расы в эталонах +
  пересборка промпта (кнопка, единичная/массовая, reload без рестарта); ловушка PITFALLS
  «гонка status.json читательская»
- Карточка расы — игровое восприятие (99.2.26, готово 2026-09-19): новые поля
  race_lore.json (kind, niche, size_individual, size_group, home_words, lore,
  attributes_words 60×6 слов), словари/шкалы в internal/races/lore.go,
  валидация §9; спека `docs/specs/99.2.26-race-card-player-facing.md`
- Переходы админка ↔ карта (идея 2026-09-19, файлы: web/admin.html, web/map.html,
  admin/main.js, map/data.js, main.js).
- Корабли рас — готово (2026-09-20): каталог docs/gamedesign/races/ships/ (60
  файлов, кабина — место аватара); кандидаты: связь-с-кораблём, генератор
  внешности кораблей.
- Пакман-вайп (2026-09-20): админ-кнопка «Запустить Пакмана» рядом с «Удалить
  все миры» — джоб ест миры по траектории nearest (скорость float64 >0..10000,
  адаптивный батч=1), событие видно всем игрокам на карте (WS-пакман, баннер
  N/M, «Следить», поздние подключения видят), звёзды лопаются при прибытии;
  файлы: internal/handlers/admin_pacman.go, internal/handlers/pacman_notifier.go,
  web/static/js/map/pacman.js, web/map.html, web/static/js/map/data.js,
  web/static/js/admin/generation.js, web/admin.html, migrations/000047
  (npc_agents world indexes); спека `docs/specs/2026-09-20-pacman-galaxy-wipe.md`.
- Биомы (99.2.28, сдано 2026-09-20): справочник config/biome_catalog.json (57
  биомов/17 недр/9 правил типов, сид в go:embed, админка „Основное“ — правка
  admin/чтение admin+skycomposer, атомарная запись); слой 10 биомов после недр,
  классификация — правила справочника (пороги 15/20/25 убраны из кода); условия
  недр — данные; прототип — оверрайды каскада; иконки биомов (пачка 1, 23 шт) в
  ai_drafts/biomes/processed_p1B/ — встраивание в идее „визуализация атмосферы
  и поверхности“.
- Внутрисистемный полёт (99.2.27, сдан 2026-09-20): позиция игрока
  users.current_position (orbit/in_flight, уровни surface/biome — задел под
  высадку), player_intrasystem_flights (000046), IntrasystemManager
  (internal/travel), POST /api/intrasystem-flight, авто-знание presence,
  видимость стоящих (90a-изменение), компаньоны-цели (синтетические id); TODO
  следующей сессии — идея 2026-09-20 §10 (параллакс, «Найти меня» при
  межзвёздном, полёт к планете любой системы в радиусе, битая позиция).
- Композитный маршрут (99.2.30, сдано 2026-09-21, коммит 910c067):
  `users.pending_destination` JSONB (миграция 000049, цели
  planet/satellite/companion) — намерение долететь до объекта чужой системы;
  автостарт внутрисистемного сегмента по прибытии (общий
  `TravelHandlers.ArrivalHandler`, работает и в Restore фазе 1); валидация цели
  `/travel`: `companion` через `stellar_mods` (трансляция в intra
  `star`+синтетический id); ПКМ-меню «Лететь» (планета/компаньон), кнопка
  спутника; автооткрытие модалки по прибытии (маркер `compositeRoute`); пакман
  чистит намерения съеденных миров (шаг 4.5).
- Картинка планеты v4 (2026-09-21): форма поверхности — поле высот + регионы
  (нет кругов) + рваные границы материков (согласованный доменный варп +
  взвешенный Вороной); световой вектор L от seed, приглушённый блик,
  освещённые кольца; реальная атмосфера в режиме full (своя система + админ),
  honest — производная, stub — нейтральный силуэт без блика; кэш-ключ
  `(planet|mode|size|v4)`, диск `{v4}_{sha256}.png` + чистка легаси; бюджет
  малых биомов ослаблен (тест: ≥25% строго ±30%, остальные «не исчезают»);
  иконки биомов 57/57.
- Высадка/прогулка (2026-09-21, сдано, коммит fcf0626): позиция status=surface в
  users.current_position (hp серверный, пересчёт от landed_at), POST
  /api/surface/land|leave, жребий биома ∝ share, страница web/surface.html +
  web/static/js/surface/* (Canvas 2D, вид сбоку, процедурный мир биома, жизнь/погода),
  вход — ПКМ по планете «Высадиться», поверхность скрыта от других; урон
  падения/жидкости убран решением создателя (только 4 среды + события); добыча/
  скафандр — позже; спека docs/specs/2026-09-21-высадка-прогулка-по-планете.md.
- Звук (2026-09-21): `web/static/js/ui/sound.js` (Web Audio, файлы `web/static/audio/*.ogg`
  CC0 Kenney) — активируется только картой (`activateSound`); роли `interaction/confirm/error/flight_hum`;
  закрытия и админка молчат; гул — кроссфейд-луп `flight_hum.ogg`.
- Рецепт как сущность (000055, 2026-09-21): `recipes`/`recipe_components`/`producer_recipes`,
  фабрика держит набор рецептов; студия — селектор фабрики и граф замыкания,
  фильтр по семейству рас, «Состав рецепта»/«Сложность рецепта»/привязки, drag&drop
  и копирование набора; следующий шаг — релиз 2 фабрик (строения/снабжение).
- Строения (2026-09-21, коммит dfba218): таблица `buildings` (миграция 000056) —
  первая итерация релиза 2 фабрик; столица фракции — одна на фракцию, на
  `factions.homeworld_id`, тип `capital` («другое», не производит), создаётся
  идемпотентно (`EnsureCapitals`) в джобе генерации фракций; владелец-фракция
  (`owner_type`/`owner_id`, три класса сразу); показ во вкладке «Фракции» карточки
  планеты; `buildings` внесена в `truncateTables`; гейт Пакмана/`universeMutationMu`
  в `GenerateFactions` перенесён до ветки `total == 0`; следующие шаги релиза 2 —
  население строений, снабжение, разблокировка предмет-рецептом.
- Масса планет (2026-09-21, коммит `a8a8304`): формула от **нормированного
  расстояния** (номер орбиты) + per-системный бюджет диска `B`; reestr mass
  0.02–8. Работая с генерацией планет — читай спеку
  `docs/specs/2026-09-21-масса-каменистых-и-ледяных-планет.md`. Следующая
  обязательная задача — перелив в гигантов (идея 2026-09-21).
- Корабли рас — рецепт txt2img (2026-09-21): вкладка «Корабли рас» арт-студии —
  чистый txt2img (Juggernaut XL v9, 3/4 сверху, нос вправо), расовость материалом/
  текстурой расы (морские/авиационные слова — в негатив), frame-check (край <8 px /
  вытянутость <1.3), вырез hyst(12/40)+fill_holes, нормализация 200×200, Hi-Res,
  обязательная ручная приёмка; реестр игры растёт расовыми кораблями только в конец
  (проба «люди» 21→24, `spriteForAgent` — модуль пула). Спека
  `docs/specs/2026-09-21-ships-quality-rework.md`, пайплайн —
  `docs/gamedesign/art/art_ships.md` §2.0, скрипты `tools/spike_ship_sprite_run.py` /
  `_cut.py` (в студии — `tools/ship_sprite_cut.py`).
- Описание каталога (000057, 2026-09-21): `goods.description` — одно поле для
  товаров и ресурсов (игровое + рабочее различение записей), ручной ввод в
  студии, ИИ-описания (fill-составляющие / кнопка / пакетный прогон порциями по
  10), третья колонка массовой подгрузки; показ описания игроку — будущая
  задача. Клиент opencode: `OPENCODE_AGENT` (дефолт `build`), терпимый разбор
  JSON.
- Протопылевое облако (2026-09-21, этап 1, коммит `dd37a8c`): бюджет облака
  `M_диск` (прежний `B`) + обрезка роста гигантом `f_обр` (Марс-паттерн), `σ_ζ` 0.6;
  каскад и поток RNG не изменены. Работая с массой/составом планет — читай
  `docs/specs/2026-09-21-протопылевое-облако-архитектура-и-масса.md`.
- Пояса малых тел (2026-09-22): этап 1 (коммит `b24e358`) — бюджет облака `M_диск`
  как общая масса (нормировка профиля `w_i = c_i/S₀`, приор `median = S₀`), пояса —
  объект мира (астероидный `g−1`, Койпера за `x_out = 35`, debris у WD), таблица
  `system_belts` (миграция `000059`). Этап 2 (коммиты `d36f5ce` + `d9531c8`) —
  секция поясов в модалке, знание состава по скану/присутствию, полёт
  `object_type='belt'` (миграция `000065`), Restore переживает рестарт
  (`handlers.IntraRestoreTargetValid`). Спеки `docs/specs/2026-09-21-пояса-малых-тел-объект-системы.md`
  и `2026-09-22-пояса-малых-тел-этап-2-показ-знание-полёт.md`.
- Облако, этап 2 (2026-09-22, коммит `fb8db80`): состав планеты от
  `x_form`/`x_ice(t_form)` (резервуары + конденсация, миграция, потеря мантии —
  только состав, масса заморожена), снеговая линия внутрь (`5.5 → 2.7`), маркер
  `formation_history` в `planet.data` + значок в карточке; `compositionByZone`
  сохранена для поясов. Спека `docs/specs/2026-09-22-облако-этап-2-состав-и-история-формирования.md`.
- Контракты (2026-09-22, этапы A/B1/B2a/B2b/B3): контракт как сущность (миграция
  `000062`), деньги/эскроу (`000061`), тип «перелёт» (публикация с двигателем,
  взятие с перебазированием срока, закрытие по прибытии — игрок/NPC-агент), доска
  контрактов во фронте — вкладка «Задания/Контракты» карточки планеты. Спеки
  `docs/specs/2026-09-22-контракт-*.md`, `деньги-и-эскроу.md`.
- Экономика поселения (2026-09-22, итерации 1–3): залежи поверхности — таблица
  `deposits` (`000058`, генерация из трёх пилотов каталога, видимость со знанием);
  ветка поселения + входной/выходной буфер — `settlement_branches`/`settlement_branch_buffers`
  (`000064`, ленивая переработка вход → выход, линейная скорость от населения,
  админ-ручки); добыча ветки из залежей своей планеты (порядок локов ветка → залежи,
  выработанная залежь скрыта от игрока); потребление населением — итерация 4.
- Модалка системы — единая секция «Объекты» (2026-09-22, коммит `2004966`): планеты
  и пояса вперемешку по удалённости от звезды. Спека размеров — этап 1 (коммит
  `1fe5b72`): `planetRadius(size) = clamp(10·size^0.6, 5, 43)` — один источник для
  рендера и хит-теста, пол звезды; «упихать в кадр» снято. Спека
  `docs/specs/2026-09-22-визуальная-система-размеров-и-сцена-подлёта.md`.
- Графика карты (2026-09-22, коммит `62bcd9e`): вид звёзд — `web/static/js/map/star_render.js`
  (наборы Глаз/Фото/Спрайт/Корона, дефолт «Спрайт»), дефолты — `map/star_presets.js`;
  настройки игрока — дашборд «⚙️ Графика» (`dashboard/graphics.js`), localStorage,
  карта только читает; «Для слабых ПК» = `starVisualLite` (гасит эффекты и
  непрерывный кадр); ядро звёзд без «мишени» (коммит `fa94d3a`). e2e
  `tools/e2e/star-visuals-shot.js`, `star-perf.js`, `dashboard-graphics-check.js`.
- Погода поверхности (2026-09-22, коммит `0115e25`, только клиент): слой суток,
  11 явлений (+гроза/иглы/град/пепел/свечение), 6 вариантов, эмиссивный проход,
  метель-снежинки и туман-гряды; файлы `web/static/js/surface/*`; чеки
  `tools/surface-weather-check.mjs`, `tools/surface-env-check.mjs`; ловушка K vs °C.
- Студия товаров (2026-09-22, коммит `faf2f0e`): разделы — вкладками выше
  инструментов, формы добавления — в попапах, кнопки вызова — в шапке; вход
  студии — форма (username/current-password), поля форм — нейтральные name +
  autocomplete=off (Chrome считал имя логином); файл `web/studio.html`.
- Корабли рас (2026-09-22, коммит `75fe9fb`): фон magenta + вырез `edge` (tol 40
  чёрный / 100 magenta), накопительный `keep`, авто-детект фона
  (`tools/ship_recut_pool.py`), регресс `tools/test_ship_sprite_cut.py`; идея
  `docs/gamedesign/ideas/2026-09-22_корабли-рас-генерация-всех-и-выпил-легаси.md`.
- SQL-разведка dev-БД — обёртка `tools/db.ps1` (кодировка/многострочность/пароль
  внутри); не переизобретать вызов psql (идея «устойчивость агентов», 2026-09-22).
- Погода прогулки (2026-09-22): спека `docs/specs/2026-09-22-погода-поверхности-вид-и-физика.md`
  ✅ реализовано (коммит `daafb02`): выбор явления из физики планеты (гейты по
  давлению/радиации из suit-пакета + веса), 6 явлений (дымка/частицы/3 пояса
  параллакса/ветер с порывами), 3 прохода, модуль `surface_weather.js`, сияние —
  мягкие занавесы. Визуал прогулки (зум 1.8×, покрытие экрана, текстуры тел неба) —
  коммит `0583de8`; фикс невидимых камней — `cf76e06`
  (`tools/e2e/surface-float-coverage-check.js`).
- «Лететь» с поверхности (2026-09-22): взлёт + сегмент (`from=планета`), пункт «Вы на
  поверхности» открывает прогулку заново — коммиты `58e0df7`/`c97435e` (идея
  `2026-09-21_залипание-на-поверхности-взлёт-и-маршрут.md`).
- Ориентация спрайтов кораблей (2026-09-22, коммит `25de447`): угол/зеркало —
  метаданные (пара A/F), пиксели не поворачиваются нигде; вырез с `--no-orient`;
  реестр `ShipSprite.Angle/Flip`; отрисовка в игре — `rotate(H + V·A) + scale(F ? −1 : 1, V)`,
  `V = cos H < 0 ? −1 : 1` (антипереворот при курсе влево); импорт принятых —
  `tools/import_ship_sprites.ps1` (sha256 + гвард `orient_meta`). Спека
  `docs/specs/2026-09-21-угол-корабля-в-метаданных.md`.
- Витрина репозитория: `readme.md` — портфолио-страница (EN + RU, кадры `docs/media/`); инструкций по установке/запуску в ней нет намеренно — не возвращать; съёмка кадров — `tools/e2e/portfolio-shots.js`.

## FAQ-счётчик эталонов

`docs/gamedesign/faq_counter.md` — 5 (2026-09-22: залежи поверхности +2, ветка поселения +2, добыча из залежи +1; правило: в счётчик только эталоны физики/мира, вопросы роадмапа — в роадмап/идеи).

## Принятые решения создателя (свежие)

- 2026-09-21: сторож считает результат, а не объём; лимиты агента можно продлить кнопкой в сервисе учёта — только на срок, цикл по содержимому и порог памяти продлением не снимаются.
- 2026-09-21: столица фракции — постройка типа «другое», одна на фракцию, на её
  родной планете; владелец — реальная сгенерированная фракция (единая
  фракция-заглушка не вводится); поселения владельца пока не получают.
- 2026-09-21: сторож зацикливания агентов (повторы, правило по памяти ≥ 700k/900k, машинные признаки итераций, журнал) и живой учёт агентов — отдельный сервис `tools/agent-dash` (цена по фичам и субагентам, пик памяти, повторы, срабатывания сторожа); потолки шагов отклонены.
- 2026-09-20: итерация C переноса студии товаров сдана — перенос завершён (A/B/C ✅): студия — `/studio` на игровом сервере, fill двухфазный «предложи → подтверди», читатели Go-каталога на БД (слой 20/покрытие/gaps, С5 снят), общий auth.js; cmd/goods-studio удалён.
- 2026-09-20: итерация B переноса студии товаров сдана (UI на /studio + витрина 94a на БД; переходы админка ↔ студия).
- 2026-09-19: студия товаров сдана (99a.1/99a.2/99a.3); открыт хвост «контекстное меню ПКМ в списке справочника» (идея 99a.3).
- 2026-09-19: роль @uidesigner (100a) — анализ и проектирование интерфейсов (игра, админка, обе студии: арт-студия, студия товаров), спеки для разработчика; интерфейсы первичнее стиля; при фиче с большим UI — после @designer, до @developer.
- 2026-09-18: роль @critic (95a) — новые идеи = нагрузочный тест на старое; критик
  бьёт идею/спеку о дерево связности impact map; триаж: мелкие — правим сами,
  средние — звоночек создателю, крупные — жёсткий стоп (разблокировка только
  создателем).
- 2026-09-16: ретроспектива (62a) — при превышении бюджета итераций агент пишет
  корень зависания + дельту в инструкции; на гейте отдельный блок с отдельным
  вопросом, «ок» на гейте его не покрывает; область — коммуникация/контекст.
- 2026-09-16: QA-процесс (63a) — журнал прогонов `docs/QA/checklists/` (файл на
  прогон), единый реестр критичных точек `QA_CHECKLIST.md` ведёт @manager.
- 2026-09-16: 7 векторов разработки (Генерация, Новые механики, Поселения, Агенты,
  Визуал, Баги, Игрок) — подсказка `docs/VECTORS.md`.