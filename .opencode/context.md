# Контекст проекта для субагентов (сводка)

> Живая сводка, разносит **`@dispatcher`** по дельтам менеджера (раздел «Разнос
> по докам» в идеях, применяется в паузу). Субагенты читают ЕГО вместо
> `STATUS.md`/`docs/INDEX.md` целиком. Источник истины — `STATUS.md`, `docs/` —
> их читай, только если задача требует глубины. Один факт — одно место: здесь
> только ссылки и текущее состояние, не история.

**Обновлено:** 2026-09-18

## Фокус

Активной задачи нет — команда простаивает. Подробности сессии: STATUS.md §1.
Очередь по «риск/стоимость» (STATUS.md §3): нагрузочный тест (100 синтетических
клиентов) → генератор веществ (блок 🔴) → прототип поселения.
Долги: FAQ 11 открытых вопросов (порог достигнут → сессия закрытия FAQ), 🔴 B24, открытые решения STATUS.md §4, расхождения P1–P5.

## Баги (активные)

| № | Приоритет | Суть | Где |
|---|-----------|------|-----|
| B24 | 🔴 | В «Проверке гипотез» нельзя создать >1 поселения на планету — нужна настройка «поселений на планету» (срочно) | `internal/handlers/admin_hypothesis.go`, `internal/generator/planet/twin.go`, `web/static/js/admin/hypothesis.js` |
| B14 | 🟡 | Вкладка «Тесты» в админке не работает на проде (cwd ≠ корень, `os.Stat("go.mod")` падает) | `internal/handlers/admin_tests.go:53` |
| B16 | 🟡 | Перебор с декластеризацией: кластеры не видны, звёзды почти равномерно (`sparse`, mapSize 50000) | `web/static/js/admin/generation.js` |

Закрытые баги и фичи — CHANGELOG.md (по датам); детали фич — в спеках `docs/specs/`.

## Указатели для будущих задач

- NPC (26a, сдано 2026-09-15): работая с NPC, читай спеку `26a.1` и идею `26a`;
  следующие шаги — бенчмарк 26b (10-50-100к), wildcard-экранирование в поиске (🟢);
  оптимизация тика 26c (пакетные UPDATE + кэш dirty) — замер выигрыша за создателем.
- Двойные системы (35b/99.2.18): API планет отдаёт orbit_center/orbit_radius_au/circumbinary;
  вода/поверхности и генератор редких звёзд — будущие фичи.
- Расы (99.2.21/99.2.22): сданы 2026-09-17; перенос старого генератора поселений
  в расовую модель (65a) выполнен — см. STATUS.md §1 и CHANGELOG 2026-09-17.
- Пресеты генерации (72a): при ручном вводе minDist ≥ 200 с радиусом 2500 и
  200 кластерами возможен недобор — компенсировать радиусом ≥ 3000 / кластерами ≥ 300.
- Экзотика звёзд (41a): старые миры — возраст «—», бэкфилл запрещён; спеки
  `41a-exotic-star-card.md` и `99.2.20-planet-physics-cascade.md`.
- Корабль/оборудование (91a): сдано 2026-09-18 — раздел «Корабль» на дашборде
  (силуэт со слотами), двигатель-1 настоящий (миграция 000043), полёт требует
  двигателя (player; админ — исключение), селектор внешнего вида 61b — легаси;
  справочник энциклопедии — далёкий бэклог; спека `91a-ship-section-dashboard.md`.
- PITFALLS читается по разделам (БД/шелл, Go и конкурентность, Дизайн и числа,
  Фронт и UI, Прочее); `-race` доступен локально (gcc WinLibs) — DoD с детектором
  гонок (идея 93a).

## FAQ-счётчик эталонов

`docs/gamedesign/faq_counter.md` — 11 открытых вопросов (порог достигнут → сессия закрытия FAQ).

## Принятые решения создателя (свежие)

- 2026-09-18: роль @critic (95a) — новые идеи = нагрузочный тест на старое; критик
  бьёт идею/спеку о дерево связности impact map; триаж: мелкие — правим сами,
  средние — звоночек создателю, крупные — жёсткий стоп (разблокировка только
  создателем).
- 2026-09-16: ретроспектива (62a) — при превышении бюджета итераций агент пишет
  корень зависания + дельту в инструкции; на гейте отдельный блок с отдельным
  вопросом, «ок» на гейте его не покрывает; область — коммуникация/контекст.
- 2026-09-16: QA-процесс (63a) — журнал прогонов `docs/QA/checklists/` (файл на
  прогон), единый реестр критичных точек `QA_CHECKLIST.md` ведёт @manager.
- 2026-09-16: 7 векторов разработки (Генерация, Новые механики, Поселения, Агенты,
  Визуал, Баги, Игрок) — подсказка `docs/VECTORS.md`.