# Контекст проекта для субагентов (сводка)

> Живая сводка, разносит **`@dispatcher`** по дельтам менеджера (раздел «Разнос
> по докам» в идеях, применяется в паузу). Субагенты читают ЕГО вместо
> `STATUS.md`/`docs/INDEX.md` целиком. Источник истины — `STATUS.md`, `docs/` —
> их читай, только если задача требует глубины. Один факт — одно место: здесь
> только ссылки и текущее состояние, не история.

**Обновлено:** 2026-09-24

## Фокус

Активной задачи нет — команда простаивает.
Очередь — у создателя и в 99_roadmap.md.
Долги: 🔴 B24, B27 (Restore полёта: позиция «в полёте» при битой цели), открытые решения и P1–P5 — STATUS.md §Открытые решения.

## Баги (активные)

Активные баги — реестр в `STATUS.md` §Баги (включая B24). Закрытые баги и фичи — `CHANGELOG.md` (по датам); детали фич — в спеках `docs/specs/`.

## Указатели для будущих задач

> Здесь только то, что помогает будущей задаче: „работая с X — читай Y“ и „следующий шаг Z“. Описания сданных фич с датами и хешами коммитов сюда не входят — их место в `CHANGELOG.md`; если фича закрыта и указателя нет — строку удалить.

- NPC (26a, сдано 2026-09-15): работая с NPC, читай спеку `26a.1` и идею `26a`;
  следующие шаги — бенчмарк 26b (10-50-100к), wildcard-экранирование в поиске (🟢);
  оптимизация тика 26c (пакетные UPDATE + кэш dirty) — замер выигрыша за создателем.
- Пресеты генерации (72a): при ручном вводе minDist ≥ 200 с радиусом 2500 и
  200 кластерами возможен недобор — компенсировать радиусом ≥ 3000 / кластерами ≥ 300.
- Экзотика звёзд (41a): старые миры — возраст «—», бэкфилл запрещён; спеки
  `41a-exotic-star-card.md` и `99.2.20-planet-physics-cascade.md`.
- PITFALLS читается по разделам (БД/шелл, Go и конкурентность, Дизайн и числа,
  Фронт и UI, Прочее); `-race` доступен локально (gcc WinLibs) — DoD с детектором
  гонок (идея 93a).
- **DoD** — канон в `AGENTS.md` §2 (два уровня прогонов); здесь не дублируем.
- Масса планет (2026-09-21, коммит `a8a8304`): формула от **нормированного
  расстояния** (номер орбиты) + per-системный бюджет диска `B`; reestr mass
  0.02–8. Работая с генерацией планет — читай спеку
  `docs/specs/2026-09-21-масса-каменистых-и-ледяных-планет.md`.
- Перелив массы (2026-09-23, коммит `eff69b8`): гигант рождается переливом —
  двухпроходный пред-слой (`overflow.go`), масса гиганта из **отдельного** газового
  резервуара `M_gas`; класс мини-нептун `(M_crit,16]` (`TypeMiniNeptune`,
  `is_mini_neptune`, `2.61·(M/8.6)^0.577`); кривая `M→R` гигантов переанкерена
  (16→3.7 / 95→9.4 / 317.8→11.2, `GasGiantMassMin=16`); регион → доля газового
  режима `f_rich_eff`; P-ветка двойных сохранена. Спека
  `docs/specs/2026-09-23-перелив-массы-в-гигантов-и-мини-нептуны.md`.
- Пояса — этап 3 закрыт (3a–3e; спрайты `5fd01e3`, реестр спрайтов и сцена): сервер `137b306`, клиент-мини-игра `292bbd3`, кольцо на схеме + ПКМ `e937688`, управление «два стика» + бесконечный пояс `fbe2b84`; ассеты `web/static/sprites/belt/`, клиент `web/static/js/belt/*`. Хвосты: добывающий лазер (темп как предмет корабля), слабые формы спрайтов, impact_map (`belt_mining`/`ui_layers`).
- Добыча в поясе — **два ресурса** (2026-09-24): железо и лёд → **«Вода неочищенная»** (сырьё T0, рецепт → «Очищенная вода»). Доля ледяных камней задаётся составом пояса (`P(лёд) = ice/(ice+iron)`), тип жилы выбирается наведением носа, **трюм общий**. Работая с поясом/трюмом — держи в уме оба ресурса. Каталог: ресурс `source='manual'` — на проде завести вручную (иначе ледяных жил нет, железо работает). Спека `docs/specs/2026-09-24-ледяные-астероиды-вода.md`.
- Мир прогулки (эпик, 2026-09-23, в работе): ЧК0 ✅ (направление вида + рецепты биомов), ЧК1 ✅ (вид 12 биомов литосферы, коммит `a122558`; кэш картинки `v6`), далее ЧК2 (биосфера). Спека — `docs/specs/2026-09-23-мир-прогулки-рецепты-биомов.md`; направление — `docs/specs/2026-09-23-мир-прогулки-вид-направление.md`.
- Студия/производство (2026-09-24, И1, коммит `23f5367`): темп и потребление задаются полями (число «ед/сутки/млрд» на паре «постройка × рецепт», блок «Потребляет»), мир читает число вместо формулы `k·population/complexity`; спека `docs/specs/2026-09-23-стадии-поселения-и-скорость-производства.md`. Следующее — И2 (стадии поселения).
- Потребление поселения — по ТОВАРАМ (не категориям): позиция = `goods.name_norm`, нужду закрывает только свой товар; у воды свой эффект «Жажда». Работая со студией (селект «Потребляет») или карточкой поселения (строка нужды: покрытие/дефицит) — держи в уме: категория как ключ не подставляется. Спека `docs/specs/2026-09-24-потребление-по-товарам-и-альтернативы.md`.
- SQL-разведка dev-БД — обёртка `tools/db.ps1` (кодировка/многострочность/пароль
  внутри); не переизобретать вызов psql (идея «устойчивость агентов», 2026-09-22).
- Корабли рас: расовый реестр **117** + нейтральный; агент рисуется кораблём своей
  расы (раса агента — `npc_agents.race_id`, старт в мире расы); игрок выбирает
  только корабль своей расы; легаси-21 выпилен. Работая с кораблями/агентами —
  читай спеку `docs/specs/2026-09-23-корабли-рас-раса-агентов-и-игрока.md`.
- Трюм и ёмкость корабля (2026-09-24, миграция `000080`): стартовая ёмкость **50 т**
  (20 врождённых + 30 модуля «Грузовой модуль-1»); три универсальных слота
  (`universal`/`universal2`/`universal3`), видны в разделе «Корабль». Работая с добычей
  в поясе, перевозками/контрактами или UI корабля — держи в уме 50 т; хвосты —
  интерфейс установки/снятия модулей и прогрессия `cargo_2`/`cargo_3`.
- Площадка ВК Mini Apps (идея `2026-09-24_площадка-вк-mini-apps.md`): отложена до MVP с игровым циклом; две двери входа (ВК ↔ сайт, без мостика), полноценный мобильный вид игрока, монетизация голосами ВК, запас под другие площадки. При запуске — прогон `@critic` + дизайн. Работая с входом/аккаунтом или мобильным UI — читай идею.
- ИИ-помощник студии (2026-09-24, коммит `0a5464f`): индикатор и кнопки запуска/остановки в шапке студии, ручки `/studio/api/ai/{status,start,stop}`, пакет `internal/goodsstudio/aiserve`, сервис [7/7] в `start-all.cmd` (порт 3456 = `OPENCODE_URL`); управление только Windows + loopback + npx (прод — честное «недоступно»); спека `docs/specs/2026-09-24-студия-управление-локальным-ии.md`.
- Модель и стык ИИ-помощника студии (2026-09-24): модель задаётся `OPENCODE_MODEL` (личный `.env`, dev; у создателя — Gonka/DeepSeek `gonka/deepseek-ai/DeepSeek-V4-Flash-0731`); клиент разбирает ответ помощника по **валидному** JSON (модель рассуждает в `text` и цитирует шаблон промпта) и закрывает сессии (`DELETE /session/{id}`); таймаут одной порции короткий (45 с), прогон не обрывается. Работая с `internal/goodsstudio/ai/` — читай идеи `2026-09-24_студия-ии-прогон-не-висит-молча`, `2026-09-24_студия-стык-ии-помощник-разбор-и-сессии`; провайдер — `2026-09-24_gonka-провайдер-моделей`.

## FAQ-счётчик эталонов

`docs/gamedesign/faq_counter.md` — 6 (2026-09-23: +1 — почему сила голода растёт со временем и почему при долгом покрытии обнуляется (порог ≈ сутки, скорость восстановления 0.25); ранее 2026-09-22: залежи поверхности +2, ветка поселения +2, добыча из залежи +1; правило: в счётчик только эталоны физики/мира, вопросы роадмапа — в роадмап/идеи).

## Принятые решения создателя (свежие)

- 2026-09-23: на орбите планеты игрок видит полные данные о поверхности и
  поселениях; посещённую планету — снимком на момент отлёта; эталон рас уточнён:
  „игрок ничего не видит, пока не купит или не получит информацию, присутствуя сам
  на орбите планеты“; общеигровой календарь — год +1000 (UTC), включая админку.
- 2026-09-21: столица фракции — постройка типа «другое», одна на фракцию, на её
  родной планете; владелец — реальная сгенерированная фракция (единая
  фракция-заглушка не вводится); поселения владельца пока не получают.
- Правила сторожа/учёта агентов — `AGENTS.md` §4.18; ретроспектива — `manager.md`; QA-процесс и векторы — `manager.md` + `docs/VECTORS.md`.
