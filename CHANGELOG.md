# Changelog

Все значимые изменения проекта Zorion. Компактный формат: раздел по дате →
`Реализовано:` / `Исправлено:` / `Дополнительно:`. Детали — в идеях, спеках
и GDD-доках.

---

## 2026-09-21 — фабрики: релиз 2, итерация 1 — строения и столицы фракций

**Реализовано:**
- фабрики, релиз 2, итерация 1 — сущность «строение» + столицы фракций (одна на фракцию, на её родной планете), владелец-фракция, вкладка «Фракции» в карточке планеты, видимость «админ / знание планеты» (миграция 000056, коммит dfba218)

---

## 2026-09-21 — рецепт как сущность

**Реализовано:**
- рецепт как сущность (000055) — таблицы recipes/recipe_components/producer_recipes, фабрика держит набор рецептов, инвариант «выход рецепта = категория фабрики» (400), копирование набора из универсальных фабрик категории; студия: селектор фабрики и граф её замыкания, фильтр справочника по семейству рас, «Состав рецепта»/«Сложность рецепта»/«Доступен фабрикам», привязка перетаскиванием карточки на граф, пункт контекст-меню «привязать к фабрике», счётчик «в наборе N»; Снято: goods_slots и goods.tier_override, роуты /studio/api/goods/{id}/slots* и /studio/api/goods/{id}/tier

---

## 2026-09-21 — звук отклика интерфейса и полёта

**Реализовано:**
- звук отклика интерфейса и полёта — клик/открытие/выбор (interaction.ogg), подтверждение/старт/прибытие (confirm.ogg), ошибка (glitch_004), зацикленный гул полёта; звуки Kenney CC0 (web/static/audio/), Web Audio gapless; кнопка «🔊 Звук» на карте; админка и surface молчат; закрытия молчат

**Исправлено:**
- гул не возвращался после F5; гул рвался паузой <audio loop>

---

## 2026-09-21 — высадка на планету: прогулка по поверхности

**Реализовано:**
- высадка на планету — мини-игра «прогулка по поверхности»: POST /api/surface/land|leave, позиция status=surface (hp серверный, пересчёт от landed_at), жребий биома ∝ share, скрытие поверхности от других; страница web/surface.html + web/static/js/surface/* (Canvas 2D, вид сбоку, процедурный мир одного биома — ландшафт/пещеры/жизнь/погода), вход ПКМ по планете «Высадиться», опасность от 4 средовых осей + события (урон падения/жидкости убран решением создателя); QA 14/14 PASS

---

## 2026-09-21 — студия: снятие согласования каталога

**Реализовано:**
- снятие согласования каталога (статусы убраны у товаров/ресурсов/предметов/фабрик; «убрать» — удалением), скрытие — только у фабрик (обратимый флаг «скрыто»), лестница «самая конкретная фабрика» (раса > семейство > универсальная), дефолт веса/объёма товара 1/1 (миграция 000054); сняты валидаторы `non_approved_ref`/`incomplete_chain`; смоук студии 14/14; тестер — ПРОЙДЕНО 10/10

---

## 2026-09-21 — фабрики: релиз 1 «Фабрики» (каталог производителей/предметов)

**Реализовано:**
- релиз 1 «Фабрики» — каталог типов производителей/предметов (8 типов/4 предмета, сид), ветки студии «Производители»/«Предметы», объём/вес товаров, терминология «завод»→«фабрика» (код+ГДД), фикс пустой вкладки студии

**Исправлено:**
- пустая вкладка «Производители» (id="filters"), снос легаси production_units (000050)

**Дополнительно:**
- релиз 2 — отдельная задача: buildings/владельцы/население/снабжение

---

## 2026-09-21 — люди → F0 (отдельное семейство)

**Реализовано:**
- люди → F0 (отдельное семейство) во всей системе — лор (race_lore.json), корабли рас (ships.json + races/ships/humans.md), валидация семейств (lore.go), студия товаров (переключатель расовости дерева построек — 11 семейств, «F0 Люди» первый), карточка расы игрока («F0 · Люди»), канон 22_races (10 биосемейств F0–F9, F1 — чисто водный, 3 расы), доки humans/art_studio_spec/DB.md/000048; тестер — ПРОЙДЕНО 8/8

---

## 2026-09-21 — переименование товарных категорий

**Реализовано:**
- переименование товарных категорий — «комплектующие»→«Электроника», «детали»→«Механика» (код + миграция 000053); снят кандидат пересмотра «детали против комплектующих»

---

## 2026-09-21 — карточка спутника в модалке не сбрасывается на карточку планеты

**Исправлено:**
- карточка спутника в модалке больше не сбрасывается на карточку планеты при фоновом обновлении данных

---

## 2026-09-21 — картинка планеты: агрессивная рваность материков

**Реализовано:**
- картинка планеты — агрессивная рваность материков (согласованный доменный варп границ, заливка взвешенным Вороным без прямых стыков, кэш v4)

**Исправлено:**
- малые биомы больше не съедаются доминантами (передняя полусфера + отбрасывание перекрытий)

---

## 2026-09-21 — студия «Производство»: слоты родителя (скрытые категории строений)

**Реализовано:**
- студия «Производство» — слоты родителя (конфигурация категорий строений, расово-зависимо): таблица producer_slots (миграция 000052), наследование универсальной базы, скрытость на слоте (перекрывает наследуемое), инвариант «завод только при слоте», карточка родителя — редактор слотов, галка «показать скрытые», заглушка скрытой пустой категории; автоматика категорий по имени заменена конфигурацией слотов; тестер — ПРОЙДЕНО (прогоны 1–3)

---

## 2026-09-21 — сторож зацикливания и живой учёт агентов (инструменты)

**Дополнительно:**
- сторож зацикливания агентов (повторный вызов режется, напоминание про бюджет, гашение сессии, журнал срабатываний) и живой учёт агентов — локальный сервис `tools/agent-dash` (порт 8790).

---

## 2026-09-21 — композитный маршрут (99.2.30)

**Реализовано:**
- композитный маршрут (99.2.30) — полёт к планете/спутнику/компаньону чужой системы одним действием: межзвёздный сегмент + автостарт внутрисистемного по прибытии (серверное намерение `users.pending_destination`, миграция `000049`), ПКМ-меню «Лететь», автооткрытие модалки по прибытии, слежение карты; единая формулировка «Лететь» в меню полёта.

**Дополнительно:**
- дельта спеки 99.2.27 §5.1/§5.5 — чужие системы летят «Лететь» через ПКМ (в т.ч. к компаньонам).

---

## 2026-09-21 — доработка картинки планеты (форма, блик, реальная атмосфера)

**Реализовано:**
- картинка планеты — форма поверхности без кругов (поле высот + регионы вместо пятен-кругов), единый световой вектор и приглушённый блик по типам, освещённые кольца, реальная атмосфера в режиме full (своя система + админ; состав → дымка, давление → лимб, τ_IR → свечение, облачность → облака), режимы stub/honest/full с версией кэша v3 и чисткой легаси.

**Дополнительно:**
- 34 иконки биомов (пачка 2) — 57/57; заодно закрыт read-only поля имени биома у роли без прав (уже был исправлен — проверено).

---

## 2026-09-21 — дерево построек в студии (канвас «Производители»)

**Реализовано:**
- дерево построек в студии — канвас ветки «Производители» (Строения→Типы→Подтипы), автоматика категорий у Фабрики/Автофабрики (13 товарных) и Платформы (6 ресурсных), переключатель расовости (семейство F1–F9+robotic + раса, 60 рас), переименование типов без разрыва связей, миграция 000051 (parent_id/race), валидации (категория только у подтипов, уникальность кортежа, RESTRICT)

---

## 2026-09-21 — реализм распределения планет (гиганты по классам звёзд, горячие юпитеры, число планет)

**Реализовано:**
- реализм распределения планет — газовые гиганты по классам звёзд (G/K/F ~10%, M ~3%, O/B/A 0.5–2%), горячие юпитеры ~0.66% (миграция на орбиты 1–2), число планет у звезды 4–8 (mean F/G 6.5, K/M 5.5), металличность ×10^(0.5·[Fe/H]) на шанс гиганта, рескейл gas_giant_shift профилей регионов

**Исправлено:**
- мёртвый код шанса 0.8 у O/B/A, потолок 8 планет стал достижим, сверхгиганты больше не дают гигантов

---

## 2026-09-20 — визуализация атмосферы и поверхности (картинка планеты из биомов)

**Реализовано:**
- честная картинка планеты из биомов и атмосферы (генератор v2, стиль А; авторизация + режимы честная/заглушка; заслонение по видимым параметрам; кэш in-memory + диск через PLANET_IMAGE_CACHE_DIR; «Вид с орбиты» в модалке при прилёте; иконки биомов 23 шт; админка — поле цвета биома; починена утечка состава биомов игроку без знания)

**Исправлено:**
- strip биомов в видимости (77a И1)

---

## 2026-09-20 — админка: красная кнопка-стоп джоба

**Исправлено:**
- админка — красная кнопка-стоп у заголовка «Генерация»: остановка любого идущего джоба (в т.ч. пакмана) с подтверждением; после перезагрузки страницы идущие джобы восстанавливаются (прогресс + кнопки остановки), пакман снова останавливаем; заодно починена очистка интервалов опроса джобов после завершения (раньше поллили вечно)

---

## 2026-09-20 — рефакторинг структуры под токены (Батч 3)

Реализовано: стартовый контекст агента 76→34 КБ — STATUS 36→2.9 КБ (история реализаций убрана, только баги/решения/операционка; правило ведения в промтах dispatcher/manager), AGENTS 21→13 КБ (дубли описаний агентов → .opencode/agents/*.md), индекс реестра влияний docs/impact_map.md (13 КБ вместо чтения 90 КБ json целиком, дельта промта @critic).

---

## 2026-09-20 — рефакторинг структуры под токены (Батч 2)

Реализовано: порезка справочника рас (118→73 КБ — удалены 60 дублей лор-карточек, остались сводные таблицы + модель); второй проход переноса: 3 закрытые спеки в _archive/ (звёздное небо 30c.1, статичные спрайты кораблей 61b, перенос студии товаров iterC).

---

## 2026-09-20 — рефакторинг структуры под токены (второй проход переноса)

**Реализовано:**
- рефакторинг структуры под токены (второй проход переноса) — 3 закрытые спеки в `docs/specs/_archive/` (звёздное небо 30c.1, статичные спрайты кораблей 61b, перенос студии товаров iterC)

---

## 2026-09-20 — рефакторинг структуры под токены (Батч 1)

**Реализовано:**
- рефакторинг структуры под токены (Батч 1) — перенос 27 готовых спек в `docs/specs/_archive/`, обновление ссылок в 40 доках, исключение `_archive/` в валидаторе impact map

---

## 2026-09-20 — внутрисистемный полёт (99.2.27)

**Реализовано:**
- внутрисистемный полёт 99.2.27 — позиция игрока в системе (current_position, уровни orbit/surface), таблица player_intrasystem_flights (000046), IntrasystemManager (паттерн 97a, Restore-порядок), POST /api/intrasystem-flight, авто-знание при прибытии (source=presence), стоящие игроки видны в радиусе (изменение 90a), компаньоны — цели (синтетические id), UI модалки: вау-эффект полёта, слежение камеры + «Найти меня», ПКМ «Лететь» как на карте, бейдж скопления N

**Дополнительно:**
- известные недоработки (доделать в след. сессии): параллакс фона, корабль у кромки, «Найти меня» при межзвёздном полёте, ИП-4-фолбэк в старте

---

## 2026-09-20 — генерация биомов планеты (99.2.28)

**Реализовано:**
- генерация биомов планеты (99.2.28) — справочник биомов/недр/правил типов данными + админка «Справочник биомов» на вкладке «Основное», слой биомов по физике (пороги типов убраны из кода), условия недр из данных, прототип поселения через оверрайды каскада

**Исправлено:**
- условия недр читались не из данных (switch в коде), двойной источник

**Дополнительно:**
- иконки биомов пачка 1 (23 шт, стиль мини-пейзаж 48×48) приняты — встраивание следующим релизом

---

## 2026-09-20 — пакман-вайп галактики

**Реализовано:**
- Пакман-вайп галактики (админ-кнопка «Запустить Пакмана» рядом с «Удалить все миры»): джоб ест миры по траектории nearest (жадный ближайший сосед; спираль/змейка опции), скорость float64 >0..10000 с адаптивным батчом=1 (поедание по одной), событие видно всем игрокам на карте (WS-пакман: жёлтый 34 px с жующим ртом, нимб, след, баннер N/M, кнопка «Следить», поздние подключения видят), звёзды лопаются при прибытии пакмана, отмена/прогресс/отчёт, миграция 000047 (индексы npc_agents), гварды конфликтов с генерацией/очисткой, дефенсив onArrival (битая цель → NULL)

**Исправлено:**
- кнопка „Запустить Пакмана" в админке слала спираль — теперь шлёт „ближайшие" (nearest); пакман снова ест ближайших, а не носится по галактике.

---

## 2026-09-20 — студия товаров — итерация C (перенос в игровой сервер завершён A→B→C)

**Реализовано:**
- Студия товаров полностью перенесена в игровой сервер (итерация C, завершение A→B→C): ИИ «заполнить комплектующие» двухфазный «предложи → подтверди» (попап с вариантами по пунктам: Создать/Пропустить/Изменить категорию, Заполнить/Пропустить; дефолт — категория родителя при невалидной; применяется только принятое, полный отчёт; дропы исчезнувших ссылок/категорий/слотов под lock), перевод вкладки «Ресурсы» админки (слой 20/покрытие рас/gaps) на чтение из БД (С5 снят), общий модуль авторизации web/static/js/auth.js (ядро + адаптеры админки/студии), конфиг opencode env

**Исправлено:**
- e2e-смоук студии (fill: addSlot до fill, серверный опрос state, очистка proposals в preflight)

**Удалено:**
- Старая студия cmd/goods-studio + goods_data/config/goods (импорт top-каталога/деревьев Т8 отменён решением создателя 2026-09-20, каталог наполняется через студию)

---

## 2026-09-20 — арт-студия: раса в генераторе эталонов + пересборка промпта

**Реализовано:**
- селект расы в генераторе эталонов арт-студии (/genref race, мета RaceID) + кнопка «Пересобрать промт» (лор-файл → appearance/blocked в families.json, единичная раса и массовый режим «все расы семейства», reload без рестарта) + разделы «Внешний вид» во всех 59 расах (сессия 1)

**Исправлено:**
- гонка в тест-хелпере waitJobDone (ReadStatus проглатывал транзитную ошибку чтения status.json → ранний возврат, флак TestHandleGenRefRace; фикс — прямое чтение + валидный JSON)

**Дополнительно:**
- ретрай кандидата в genRefJob (защита прода от Windows Defender-гонок на свежих файлах), PITFALLS-ловушка про читательскую гонку status.json, QA-прогоны 2 и 3

---

## 2026-09-20 — корабли рас: внешний вид + кабина

**Реализовано:**
- 60 описаний кораблей рас (внешний вид + кабина, промпт-задел под генератор texture/silhouette/blocked); раздел «Внешний вид» людям; kind кшарров → «аморфный».

**Дополнительно:**
- Идеи-кандидаты «связь с кораблём» и «генератор внешности кораблей».

---

## 2026-09-20 — студия товаров — итерация B (UI на /studio)

**Реализовано:**
- Студия товаров — итерация B (UI на /studio): web/studio.html подключён к /studio/api/* (JWT admin/skycomposer, общий adminToken с админкой), «+ ресурс» (kind=resource, без слотов, тир-поле, статусы, удаление — ресурсы без привилегий), второе подтверждение удаления при cleared_links > 0, OR-фильтры справочника перенесены, витрина «Реальные вещества» (94a) читает каталог из БД (real/real_summary/families из goods с props), e2e-смоук /studio (12/12).

**Исправлено:**
- «Добавить родителя» под контракт iterA (слот из state, TypeError ушёл).

**Дополнительно:**
- Переходы админка ↔ студия (кнопки 🧰 Студия товаров / ⚙️ Админка, общий токен), пилюли тиров справочника 0..8 всегда.

---

## 2026-09-20 — сессия закрытия FAQ

**Реализовано:**
- Сессия закрытия FAQ: счётчик эталонов 18 → 0 (11 ответов создателя), правило «FAQ = только эталоны физики/мира; роадмап-вопросы не в счётчике»; актуализирован 21_ships.md под 61b (В1–В5 закрыты); заделы в роадмап §99.3: топливо, транспондер, звёздный атлас, техчертежи.

---

## 2026-09-19 — студия товаров: фильтры справочника «ничего не выбрано → список пуст»

**Исправлено:**
- фильтры справочника студии товаров — показ включается только при выбранном тире И хотя бы одной галке ("используемые"/"неиспользуемые"/"забаненные"), каждый фильтр — AND, категория/поиск только сужают; пустые состояния с подсказками; e2e-смоук студии обновлён (стабильные шаги 12/13b)

---

## 2026-09-19 — переходы админка ↔ карта

**Реализовано:**
- Переходы админка ↔ карта для админских ролей — клик по 🌍 в шапке админки открывает карту, на карте кнопка «⚙️ Админка» (admin/skycomposer); токены token/adminToken синхронизируются (тот же JWT, без повторного логина).

---

## 2026-09-19 — студия товаров — перенос в игровой сервер (итерация A)

**Реализовано:**
- Студия товаров перенесена в игровой сервер (итерация A): каталог в PostgreSQL (categories/goods/goods_slots, миграция 000045), сидер 131 ресурса + 19 категорий (БД = источник правды, Go-каталог больше не перезаписывает), роуты /studio + /studio/api/* (JWT admin/skycomposer), ресурсы без привилегий (создание/удаление/бан/тир; без рецептов), DELETE с очисткой ссылок + cleared_links, тир ресурса назначаемый (редкость), сериализация мутаций каталога

**Исправлено:**
- Кириллическая нормализация имён (name_norm из Go, 409 вместо 500)

---

## 2026-09-19 — студия товаров (99a/99a.2/99a.3)

**Реализовано:**
- Студия товаров (99a/99a.2/99a.3) — отдельный локальный инструмент (бинарь zorion-goods-studio.exe, порт 8799): фокус-подграф на канвасе + «всё дерево», тир-оверрайд, справочник с фильтрами и подгрузкой списка, попап-деталь и попап категорий, импорт top-каталога и деревьев Т8.

**Исправлено:**
- Регрессии кликов фокус-режима, семантика фильтра тиров, сохранение фильтров.

**Дополнительно:**
- Контекстное меню в списке справочника — следующая сессия.

---

## 2026-09-19 — новая карточка расы — игровое восприятие (99.2.26)

**Реализовано:**
- Новая карточка расы — игровое восприятие (лор, вид, ниша, размер, атрибуты-слова, дом) в config/race_lore.json + валидация §9 + энциклопедия.

**Исправлено:**
- Инверсия шкалы любопытства, дубль вида cryo_swarms, лог-шкала размножения.

**Дополнительно:**
- Тест согласованности 60x6 слов (DoD §9.3), e2e-смоук карточки.

---

## 2026-09-19 — арт-студия: фильтрация «шума» в генераторе картинок (98a) + доп. теги и панель итогового промпта (98b)

**Реализовано:**
- Арт-студия — фильтрация «шума» (98a, пилот 6 рас) + поле доп. тегов и панель итогового промпта (98b): раздел «Внешний вид» в races/*.md, поля appearance/blocked в families.json, фильтрация словарей forms.json по blocked (substring, фолбек при пустом пуле), структурное правило «раса-не-существо» (морфы недоступны → «alien» не попадает в промпт), акценты палитры фильтруются по blocked; UI студии — доп. теги (tags) и редактируемая панель «Итоговый промпт» (override-режим, кнопка «↺ Вернуть авто»)

---

## 2026-09-19 — Топовые товары: каталог 100 + 2 дерева Т8 (затравка студии товаров)

**Реализовано:**
- Каталог 100 топовых товаров и 2 полных дерева производства Т8 («Гелиос», «Кремний-9») — 465 узлов, 183 имени, 70 ресурсов из каталогов.

**Исправлено:**
- —

**Дополнительно:**
- Деревья — контент-затравка для студии товаров 99a (ввод как одна сеть), JSON вне git (goods_data/), схемы в идее 2026-09-19.

---

## 2026-09-18 — Терминология: товар ⊃ ресурс

**Реализовано:**
- Терминология: товар ⊃ ресурс (GLOSSARY: Товар/Ресурс, 05_economy §5.2.1, 06_factions, 11_markets); игровая сущность переименована «вещество» → «ресурс» в 26 доках (09_resources, 10_exploration, спеки, промты); витрина реальных веществ не тронута

---

## 2026-09-18 — полёт игрока переживает рестарт сервера (97a)

**Реализовано:**
- Полёт игрока переживает рестарт сервера (97a): таблица player_flights, Restore при старте — прибывшие засчитываются сразу, летящие продолжаются с остатка, битые миры отбрасываются.

**Исправлено:**
- Потеря прогресса полёта при рестарте сервера.

**Дополнительно:**
- TOCTOU-гвард удаления БД-строки полёта, e2e-смоук восстановления полёта после рестарта.

---

## 2026-09-18 — витрина 111 реальных веществ (оценка ёмкости модели осей)

**Реализовано:**
- Витрина 111 реальных веществ в админке (6 форматов визуализации, окна требований/хемотипов, сортировка, зум радара, температура в °C).

**Исправлено:**
- Температура в интерфейсе — только °C (кельвины — внутренняя единица модели).

**Дополнительно:**
- Правило UI «раздел ресурсов — вкладки, без скролла», админка на всю ширину экрана.

---

## 2026-09-18 — вкладки дашборда игрока

**Реализовано:**
- Вкладки в дашборде игрока (шапка всегда видима + 4 вкладки — Корабль/Внешний вид/Статистика/Энциклопедия; активная вкладка в localStorage dashboardActiveTab).

---

## 2026-09-18 — дозакрытие 77a «открытые звёзды» (видимость игрока)

**Реализовано:**
- Дозакрытие 77a «открытые звёзды» — вид звезды (имя/спектр/цвет/размер/координаты) виден везде, полёт к любой звезде (403 убран, топливо — задел), точка-огонёк удалена, радиус радара (800/200 px) решает только для деталей системы (модалка 403 → карточка звезды с заглушкой «долетите или купите отчёт»).

**Исправлено:**
- 403 при клике на за-радарную звезду разлогинивал игрока (теперь карточка звезды без ошибки), клик по дальней звезде показывал тост-ошибку.

**Дополнительно:**
- Поиск отдаёт за-радарную звезду полностью (планеты неизвестных систем — без имени/координат).

---

## 2026-09-18 — каталог базового слоя ресурсов + потребление рас (94a)

**Реализовано:**
- Каталог базового слоя 20 ресурсов + таблица потребления рас (94a, блок 99.2.5): 13 шаблонов хемотипов, мост consumption→окна, проверки "все накормлены"/посредственность/корреляции; вкладка "Ресурсы" в админке с фильтрами; решения: сублимация разрешена, порог покрытия ≥ 10, состав теневого товара T1 (4 позиции); имена ресурсов — рабочие заглушки (финальные @writer); схема БД substances/resources — следующий релиз.

---

## 2026-09-18 — мета-самопроверка роли @critic (95b)

**Дополнительно:**
- Мета-самопроверка роли @critic (95b) — роль подтвердила жизнеспособность и необходимость; impact_map.json перекодирован из mojibake; порядок вызова критика в manager.md уточнён (до гейта); границы с @balancetester разведены.

---

## 2026-09-18 — тултип NPC-агента — названия миров вместо айди

**Реализовано:**
- Тултип NPC-агента — названия текущего/целевого мира вместо айди.

---

## 2026-09-18 — стрелка-маркер над звездой игрока

**Реализовано:**
- Стрелка-маркер «ты здесь» над звездой игрока на карте (анимированная, покачивание; контрастный белый корпус с тёмной обводкой — видна на звёздах любых классов; скрыта в полёте и на обзоре галактики).

---

## 2026-09-18 — роль @critic (95a)

**Дополнительно:**
- Новая роль @critic — критик геймдизайна (проверка идей и спек на логические несостыковки по дереву связности impact map, триаж мелкое/среднее/крупное, жёсткий гейт на средние/крупные); @designer обязан дорабатывать дерево связности в спеке; impact map расширен (type mechanic, kind affects).

---

## 2026-09-18 — раздел «Корабль» в дашборде (91a), PITFALLS по разделам и -race локально (93a)

**Реализовано:**
- Раздел «Корабль» в дашборде (91a) — силуэт со слотами (радар/сканер/двигатель + пустые будущие), только личное; двигатель-1 как настоящий модуль (миграция 000043), скорость полёта из установленного двигателя, полёт без двигателя запрещён для player (админ — исключение); селектор внешнего вида 61b — легаси; справочник «Корабли/оборудование» в энциклопедии — далёкий бэклог; спека `docs/specs/91a-ship-section-dashboard.md`.

**Дополнительно:**
- Идея 93a — PITFALLS по разделам (БД/Go/Дизайн/Фронт/Прочее) + целевое чтение в промтах designer/balancetester + сверка диффа с PITFALLS в DoD developer; `-race` возвращён в локальный DoD (WinLibs gcc); FormsFor ускорен 98 с → 1.6 с (кеш сегментов шаблона вместо NewReplacer); performance-тест galaxy честно скипается под `-race`.
- Кандидат ГД-сущности «топливный бак/топливо» (+1 FAQ, эталон не задан); справочник «Корабли/оборудование» в энциклопедии — далёкий бэклог.

---

## 2026-09-18 — порезка структуры ГДД (92a)

**Дополнительно:**
- Порезка структуры ГДД (идея 92a): «Лог поселения» вынесен из `18a_population_death.md` в новый `18b_settlement_log.md`; из `22_races.md` §8 убраны 12 дословных голосов (остались 5 голосов семейств + ссылки на `races/`); INDEX-карта разделов для тяжёлых доков; промт `@designer`: спеки читать выборочно + маршрутизация лора рас (модель §1–2/§5/§7, лор — `races/<id>.md`); impact map: +3 сущности (18b, 18a, settlement_log).

---

## 2026-09-18 — агент @optimizer (89a)

**Дополнительно:**
- Новый агент @optimizer — аналитик-оптимизатор промтов и структуры; применён его первый отчёт (6 правок промтов: реальные пути gdesigner, -race/44a в start.md, регистр context.md, COORDINATION-локейт, контекстный бюджет спек/глоссария).

---

## 2026-09-17 — расы (99.2.21/99.2.22), роботы (99.2.24), арт-студия (76a/78a/88a), дашборд+энциклопедия (86a), голоса рас (К-1), расовый генератор (65a), тултип звезды (70a), полёт-разворот (66a), пресеты (72a), impact map (83a), баги

**Реализовано:**
- Расы (55a/56a, 99.2.21): каталог 50 рас config/races.json, территории (regions.race_id), генератор поселений рас (доминанта кластера + раса-сосед на выбросах, крутилка), миграции 000038/000039, админка «Генерация поселений рас» + отчёт по незаселившимся, раса в UI (карточка поселения, подпись региона).
- 99.2.22 — подкрутка генератора планет под расу-дома (синергия звёзд+планет: home-классы ×1.6, кламп орбиты [0.35,3.0], обогащение need-газов, тепло, двухслойная мягкость s; крутилки «Подкрутка под расу-дома» и «Число планет в кластерах рас» в админке); копилка 20 незаселившихся рас закрыта (смоук 20/20 при s=0.5); очень горячие лимитируются радиацией — ядро 45a.
- 65a — перенос фишек старого генератора поселений в расовый (Chance/Population/NeighborChance — config/race_settlement.json; Settleable/inhabited через RaceSuitable(humans), вода ≥ 10, гиганты исключены); старый генератор /admin/generate-settlements скрыт (код не удалён), мёртвый JS удалён.
- Информация о звезде вынесена из карточки в тултип при наведении на звезду на схеме модалки (главная + компаньоны, у компаньонов — расстояние до доминантной звезды); карточка звезды в правой панели убрана — остался список планет.
- Населённые планеты в списке звезды модалки помечаются домиком 🏠 (68a).
- Семейство «Роботы и AI» (73a/99.2.24) — 10 карточек (51–60) в каталоге (60 рас): слой robotic (энергия+сырьё), territory по условиям, флаги механик; справочник рас 22_races.md + папка races/ (60 файлов); сбор замысла — @scientist/@writer/@interviewer; Дополнительно: кандидаты ГД-сущностей «Энциклопедия рас», «Голоса рас», «Спящие колонии».
- Арт-студия — морф-наборы F2–F9 и семейство F10 «Роботы и AI» (76a): 56 рас в config/art/families.json, зверо-образы/одежды/негативы морфов у всех девяти семейств (было — только F4/F5), новое семейство F10 из слоя robotic (10 рас 51–60), категории форм F10, пункт F10 в списке семейств студии, тесты 46→56; Дополнительно: кандидат ГД-сущности «Бестиарий рас» отклонён создателем (в FAQ не вносится).
- Арт-студия — словарь рас 30–50 форм (78a): 56 рас × 40 форм из описаний, кандидаты эталона берут форму из расы (вариант «б»), инвариант forms 2–60; тултип лора расы на строке «Эталоны» (79b); персистентность полей UI (85a); эксперимент «персонаж» принят — дефолт вкл (82a); Дополнительно: —.
- Кшарры (23) и Маргулы (49) — переработка соль-рас-близнецов (80a): смысловое разведение внутри F5. 23: имя + forage «глубокие бассейны расплавов солей», числа без изменений, фикс лора «репр 100 — не максимум (у Термо-роёв 200)». 49: новая карточка — озёра жидких металлов (олово/свинец/цинк, электрохимия), РАС55 ГРД20 СЕР15 CO2 10 (СКФ 0), T 550–650/500–730, P 1–10/0.5–100, яд O₂ >0%; ось РАС расширена до «расплавы (соли и низкоплавкие металлы)»; 49 перестала быть мостовой (мосты 10/43/48); R-кривые 49 перегенерированы; races_test.go T 450→520; art-конфиги families.json — имена 23/49 + металлические формы 49 (коммит при мерже, файл общий с арт-командой).
- Название региона на карте — в геометрический центр ячейки Вороного (81a).
- Дашборд игрока + энциклопедия (86a): 6 разделов (расы, звёзды, планеты + 3 заготовки), силуэты рас до встречи, клиентский журнал открытий, лог аут из аккаунта; GET /api/encyclopedia/races (JWT, whitelist, race_lore.json); фильтр типов планет по реестру; Исправлено: счётчик «Типы планет X из 9» (внереестровые типы отфильтрованы).
- С карты регионов убрана отладочная строка с именем доминантной расы территории (осталась только строка профиля 59a) (87a).
- Арт-студия — семейство F1 «Водные» (88a): 3 расы (2 Океаниды, 3 Глубинники, 4 Прибрежные) в config/art/families.json по канону 99.2.21 §5 (forms=40, морфы beast 50 / clothes 32, category_keys liquid/organic/sphere/gas/crystal), пункт F1 в списке семейств студии; люди перенесены с F1 на F0 (humans.json family, пути race_f0_humans, надпись «портреты F0»); тесты 56→59.
- С карты регионов у игроков убрана отладочная строка «тип профиля региона» под названием (осталась у admin/skycomposer, 89a).
- На карте отображаются только корабли в полёте — чужие игроки и NPC-агенты, стоящие в системе (idle), скрыты (для всех ролей, 90a).
- «Голоса рас» (К-1) — 17 миниатюр-ощущений в 22_races.md §8 (раздел «Голоса рас»: семейства F2–F6, расы 1/3/22/41/46, роботы 51–60) + секция «Голос» в 12 человекочитаемых файлах рас (races/*.md); тексты @writer, внесение @designer.
- Территория роботов по условиям (84a, 99.2.24 §5: мягкое рассеивание, best-effort, один робот на планету, приоритет расе-дому, поселения по Race.Suitable вне кластера-дома; 9 тестов); Дополнительно: спеку 99.2.24 §5 уточнил @designer под решения создателя 2026-09-17.
- Реестр каскадных влияний docs/impact_map.json (83a): 7 сущностей, влияния рас/баланса/БД/доков; автосверка после каждого коммита — валидатор scripts/check_impact_map.ps1 и post-commit hook (.githooks/, core.hooksPath), только предупреждение.

**Исправлено:**
- Выбор мира отправления в полёте — разворот из текущей точки маршрута с честной длительностью (dist × 0.3 сек, мин 3) вместо мгновенного телепорта (66a); «Already in this world» — только без полёта.
- Пресеты генерации вселенной (72a): во всех 14 пресетах minDist +50 (звёзды дальше друг от друга), компенсация радиусом кластера ×1.1–1.2 (dense — mapSize 45000), чтобы каждый пресет давал полный добор 100% миров при любом seed (проверено на 4 seed); дефолт формы не менялся.
- UI-связка админки рас (ReferenceError generateRaceSettlements, отчёт в poll.js) — фикс после теста.
- 33c — lastFlightReloadAt сбрасывается между полётами (первая полётная подгрузка кластеров не пропускается гардом 600 мс); 16f — время полёта честно зависит от расстояния (потолок длительности снят фичами 61a/66a; calcTravelDuration: dist × 0.3 сек, мин 3).
- В карточке планеты и спутника убрана кнопка «показать все» в блоках Поверхность/Недра — список форм показывается полностью; удалён мёртвый код кнопки (tabs.js) (69a).
- Подвкладки раздела «Генерация» в админке переключены с дропдауна на кнопки-подвкладки (выбор сохраняется между сессиями) (71a).
- Баг 75a — пересчёт поселения по пути «Событие» терял race_id, из-за чего в модалке планеты первым показом стояла раса «Люди» вместо реальной (микротрещинные и др.); race_id добавлен в SELECT ... FOR UPDATE + регрессионные тесты.

**Дополнительно:**
- Прогон: 30/50 рас заселились на честном каскаде, 20 в копилке (конфигуратор §7.2, доделки 45a).
- Ретроспектива — жёсткий чек-лист для @tester (правка инструкций 2026-09-17).
- Все 60 рас (docs/gamedesign/races/) дополнены: секция «Живость» (писатель, формат Сцена+Суть) + научная картина в «Кто это/Как живут/Сосуществование» (учёный: биохимия, физика среды, оценки по времени); устранено расхождение в deep_sky.

---

## 2026-09-16 — полёт (рефреш, смена цели), модалка системы, карта (ПКМ, сетка, пресеты), профили регионов, спрайты кораблей, чистка, баги

**Реализовано:**
- Полёт переживает рефреш страницы (42a): кортеж полёта восстанавливается после F5, фокус сохраняется.
- Смена конечной точки полёта в пути (61a): повторный /travel во время полёта перенаправляет корабль из текущей точки маршрута (P) к новой цели, возврат в мир отправления отменяет полёт; честная длительность полёта (потолок 20 с убран, dist×0.3, мин 3 с); шапка карты показывает «В полёте: From → To» вместо тостов полёта.
- В карточке планеты (блок «Физика») показывается расстояние до звезды в а.е. (16d) — из orbit_radius_au, до 1 знака («0.7 а.е.»), нет поля → «—».
- ПКМ по пустому месту карты — тултип с координатами точки (47a): целые, показ на нажатии, мин. 3 с; драг карты теперь только на левой кнопке (правая отдана тултипу координат и меню мира).
- Пресеты генерации галактики до 100k звёзд (47a) — 10 новых пресетов (300–100 000 миров, кластеры 6–300, выбросы 10–35%, форма кластеров blob/circle из пресета; всего 14 с прежними); параметры согласованы с генератором (баланс-проверка В4+В1, итерация 2).
- Навигация модалки системы (51a): wide-компаньоны и внешние кратных в честной позиции за кадром; динамический минимум зума до всех звёзд; миникарта линейная, звёзды за краем — маркеры с подписями расстояний («N а.е.»), клик по миникарте = фокус, квадратная рамка viewport; звёзды статичны; планеты замедлены ×10; время анимации глобальное (переоткрытие модалки не сбрасывает фазы); яркие звёзды на отдалении (≥4px).
- Новые формы кластера (57a) — ring, bar, spiral, dumbbell, stream, core_halo + режим random (случайная форма на каждый кластер); добивка генерирует точки в форме кластера (форма читается при изучении); дефолт остаётся blob, пресеты не тронуты.
- Профиль региона влияет на генерацию звёзд и планет (59a, 99.2.10): 12 классов, мягкий сдвиг весов звёзд (все типы возможны), 4 хука планет без модификации каскада 99.2.20, миграция 000036; отладочный показ типа кластера под названием региона на малом зуме; компаньоны двойных/кратных следуют весам региона; сверка глоссария по итогам сессии (процесс).
- Физический каскад генератора планет (45a/99.2.20): T⁴-энергобаланс, атмосфера-объект, флаг жидкой воды, √L-орбиты, аккреция массы, плотность от состава, исправлены светимости O/B, металличность, реестр +6, аудит 44; поглощена 99.2.11.
- Статичные спрайты кораблей (61b): 21 PNG вместо параметрического генератора (99.2.15 удалён); выбор спрайта+цвета (users.ship_color, палитра 9+Оригинал), перекраска hue+destination-in, агенты разноцветные {спрайт,цвет} от id с прелоадером 189 вариантов, маппинг legacy ship_icon→PNG.
- Карточка звезды экзотики (41a) — честные значения вместо прочерков: ЧД «нет фотосферы (не излучает)»/«чёрный (тень)»/горизонт событий ≈2.95×M км, ЧД с диском «диск: 10⁵–10⁷ K»/«голубовато-белый», нейтронные/WD/протозвёзды — температура в K + цвет/радиус/светимость по типу; worlds.age (миграция 000033: остатки 2–13 млрд лет, протозвезда 1–10 млн «молодая»); планеты экзотики наследуют возраст системы (system_age), фолбэк старых миров 2–13; подсказка «🔄 Кликните по планете…» убрана из карточки.

**Исправлено:**
- followShip вне полёта (42a): след больше не залипает после «Найти меня» вне полёта.
- Баги 39a/39c — админка «Генерация»: recalcGenWeights и saveGenConfig падали "Cannot read properties of null (reading 'value')" (сумма весов не считалась, вес чёрной дыры/белого карлика не сохранялся); stId конвертирует snake_case→camelCase; блоки весов/средних/масс объединены в «Конфиг генерации» с кнопками внизу.
- B25 (46b): 401/403 и отсутствие токена на assignments/contracts/index редиректят на /login-page (вместо тоста/текста, защита от цикла); закрыты по сверке с кодом B17, B18, B20, B23.
- Координатная сетка убрана с карты (50a): вертикальные/горизонтальные полосы при зуме > 2.
- Кнопка «Удалить все миры» перенесена в блок генерации вселенной (57b, сверху).
- «Сессия истекла» при перезапуске сервера (58a): стабилизирован JWT_SECRET — AGENTS.md §1 / DEPLOY.md §2 больше не предлагают задавать секрет вручную (он перекрывал .env и убивал токены), .env.example — валидный плейсхолдер.
- Баг 55a: запросы к таблице assignments падали с "column type does not exist" (колонки type/reward, добавленные миграцией 000010, были удалены вручную); восстановлены идемпотентной миграцией 000035.
- Гигант 13 MJ → 20 K (T⁴+F_KH) (45a); «жара → радиоактивность» — перенос на ядро-редизайн (новая сессия).
- «Already in this world» при возврате во время полёта (61a); стартовая точка каскадных редиректов — от фактического старта сегмента, не от мира отправления.
- Координаты мира — в шапке модалки рядом с названием («Nemurzan (99, -1775)»): спектральный класс из шапки убран, из карточки звезды координаты убраны; 0 не показывается как «—» (twin-миры в (0,0)) — идея 49b.

**Удалено:**
- Прототипный слой заводов (48a): factories/goods_batches — генерация, модели, мёртвый репозиторий, DROP-миграция 000034; заводы по GDD — отдельная будущая фича (постройки игрока).

**Дополнительно:**
- Арт-этап 29a закрыт (27→21 принятых) (61b).
- Карточка звезды — подпись «Срок жизни» для обычных звёзд (по классу), «Возраст» остался для экзотики (49a).
- Арт-доки (art_principles.md, art_ships.md), агент @artist, скрипты AI-генерации спрайтов (tools/) — 27 принятых кораблей.
- В бэклог роадмапа (§99.3.3) добавлен пояс астероидов — новый объект в мире (идея 48a, вектор «Генерация», в долгую).

---

## 2026-09-15 — звёзды (экзотика, двойные системы), рождаемость, балансировщик, оптимизация NPC, баги фронта

**Реализовано:**
- Расширение генерации звёзд (29a): типы систем (одиночная/двойная/кратная), экзотические объекты (ЧД «тёмная», нейтронные с пульсарами/магнетарами, белые карлики, протозвёзды с протопланетным диском и классом K/M, сверхгиганты lbv/wr), модификаторы (фазы V/III/I, переменность, аккреция), масса звёзд в M☉, mean-модель числа планет (физические дефолты ≈1.6/систему); генераторы в админке — дропдаун-подвкладки (Звёзды/Планеты/Поселения/Гипотезы/NPC/Фракции), веса спектров и типов с автопроцентом, средние планет и массы по типам, generation_config.
- Полная модель двойных систем (99.2.18/35b): планеты вокруг барицентра у тесных пар (P-тип) и вокруг главной у широких (S-тип), честная температура от L₁+L₂, параметры компаньонов (масса/температура/разделение), кратные → широкие + внешний компаньон; модалка с двумя звёздами и подписью «компаньон: N а.е.», цвет точки на карте по спектру компаньона.
- Холод, гравитация и радиация переведены в рекурсивные R-компоненты модели изменения населения (99.2.13, идея 17a): ColdChangeRate — двухрежимная сшивка (0 при ≥288 K, 73 K → минуты по эталону), GravityChangeRate — двусторонняя (комфорт 0.8–1.2 g), RadiationChangeRate — степенная (0 при фоне ≤ 20); λ-механизм и полюса-обнуления убраны.
- Естественная рождаемость R_рожд = −k·R_ест (крутилки СПЖ/коэффициента в админке «Основное», /admin/settlement-settings; дефолт k=2 → рост +2%/год в комфорте); потолок 120 лет удалён (равновесие и рост живут вечно; запись «Вымерло» у всех вымерших с честной датой через Unix-секунды); FAQ «+30 °C» удалён как неактуальный.
- 99.2.17 UI-инструмент балансировки компонент изменения населения (16e): сегментные кривые (узлы + bend) вместо формул, вкладка «Балансировка» (drag, bend-ромбы, %-шкала Y, автофокус, зум X+Y, «Сброс вида»), пресеты кривых per-component (config/balancer_presets.json, env BALANCER_PRESETS_FILE, «сохранил = применил», активные переживают рестарт).
- Фон «звёздное небо» на близких зумах карты (30c): два слоя параллакса (ближний 0.06, дальний 0.006, тусклый), точки 1–2 px, тайл генерируется один раз, фон стоит при зуме и едет только при панорамировании.
- Модалка системы поделена на равные колонки (30a): канвас и правая панель flex:1 — закрыт баг «овал вместо круга» у аватар-звезды.
- Зум карты до 20× (43a) — вблизи видно корабли в полёте.
- Оптимизация тика планировщика NPC при 100k агентов (26c): пакетные UPDATE вместо 4000 одиночных, кэш позиций в памяти с dirty-инвалидацией; тик 691→160 мс на dev.

**Исправлено:**
- Паника генератора при малом world_count (Intn(0), идея 20b); оптимизирован хот-пат пересчёта населения (GetCurve→getCurveRef, 0 аллокаций на вызов вместо 8; идея 36a).
- Планеты больше не «спавнятся» посреди экрана при полёте (33a): при прибытии данные грузятся до центрирования камеры, в полёте кластеры запрашиваются объединением вьюпорта с полосой впереди по курсу (~1.5 с).
- Гонка loadingData на кадре прибытия (33b): ветка прибытия ждёт освобождения loadingData перед загрузкой зоны, полётный перезапрос не съедает загрузку через pendingReload.
- Дёргание корабля при полёте (37a): двойная перерисовка кадра при слежении (FPS падал вдвое), лишняя перерисовка из сетевого колбэка и скачок зума при полётном перезапросе; регрессия прилёта — порядок «центр → загрузка» восстановлен.
- Корабль игрока в полёте уменьшен до размеров иконок NPC-агентов (30b): shipSize 12 → 2.8, спрайт ≈9 px вровень с агентами.
- B30 (31b): вкладка «Корабли» — при пустом каталоге ship_parts категории с кнопками ＋1/＋10/↻ теперь видны всегда, генерацию можно начать с нуля.
- Поиск экзотики 500 (COALESCE), падение генерации на stellar_mods (nil→{}), regenerate-planets (pq.Array), взаимные блокировки джобов, fallback «ЧД как Солнце», «дв./нейтр.» в подписях карты, «Жёлтый карлик» у экзотики, «— тёмная» (29a).
- Алиасинг кортежей полёта в кэше (go 1.21, агенты летели по чужим маршрутам на карте) (26c).
- API отдаёт orbit_center/orbit_radius_au/circumbinary планет (whitelist-модель Planet) (35b).
- Холод переведён в °C на фронте/API (16e).
- Визуал модалки системы (40a): ЧД/нейтронная/WD — компактная точка без свечения во всех трёх местах (канвас, миникарта, правая панель); цвет главной звезды на миникарте — по спектру/типу вместо жёлтого хардкода.
- Названия в модалке системы (40b) — единый читаемый цвет #ececec вместо цвета звезды (у ЧД тёмный #2a1a4a текст не читался).

**Дополнительно:**
- VACUUM (ANALYZE) planets выполнен на проде (100k миров / 209k планет, статистика освежена, фильтры ~11–71 мс); docs/DEPLOY.md §5 — правило частоты (событийно после массовых операций) и доступ к БД на Amvera (публичный POSTGRES-домен / pgAdmin) (38a).
- Документ docs/DEPLOY.md — пошаговое развертывание (env, локальный запуск, деплой Amvera, генерация кораблей, VACUUM); правило: правка кода, затрагивающая развёртывание, обновляет DEPLOY.md (32a).
- Поглощён черновик ideas/start types.md, аудит 40→42 правила (док «43» устарел), протозвезда получает класс K/M (29a); спека 99.2.18, PITFALLS «planets.data не едет в API сам», QA_CHECKLIST B-MAP-7 (35b); регрессионные тесты кэша, новые ловушки в PITFALLS.md (26c).
- Установлен ComfyUI (SDXL Juggernaut, ControlNet Canny, API :8188), скрипты генерации кораблей в tools/, AI-генерация кораблей NPC (29a, в работе).

---

## 2026-09-15 — NPC-агенты, смертность населения, гипотезы-близнецы, модель поселения, лог «Вымерло»

**Реализовано:**
- Модель поселения целиком (`13_tiers.md`): население — объём труда, тир — качество труда; тир растёт от подушевого излишка; два потолка населения (подножный корм — не хранится, привозная еда); нагрузка — вектор товаров, покрытие рваное; прогресс тира в товарах; природный потолок тира; естественный запас «сливки»; деньги — три потока (`14_money.md` §14.3.0); словарь `docs/GLOSSARY.md` (~130 терминов, расхождения P1–P5); дыра «у робота нет квалификации» закрыта снабжением деталями (`13_tiers.md` §13.9.6).
- NPC-агенты v1 (`20a`): автономные наблюдатели (рандом в радиусе 1000), метят мир посещённым, пуш `npc_arrivals_batch`, ромбы на карте; архитектура под сотни тысяч (batch 2000/тик, сетка 1000, `PositionCache`); таблица `npc_agents` (`000026`), `/admin/npc*` + `/api/npc/positions`, вкладка «NPC». FAQ NPC: 9/9 закрыты.
- Минимальная модель смерти населения (`18a_population_death.md`): `TwoSidedSeverity`/`OneSidedSeverity`, формула `p0·exp(−λ·Δt)`, миграция `000021` (`settlements.population_exact`), экстраполяция на фронте, `GET /admin/mortality-preview`.
- Вкладка «Проверка гипотез» (близнецы): канонический шаблон клонируется N раз, одна варьируемая ось (`twin.go`), джоб `hypothesis`, `/admin/hypothesis/run`, тег `_experiment.{id,group}`, вкладка «🔬 Гипотезы».
- Калибровка температурной смертности (`99.2.12`): квадратичная тяжесть, `SatHot=200`/`SatCold=350`, жёсткий ноль T ≥ 700 K, `MaxRatePerHour` 0.01 → 0.1/час (500 K → t50% ≈ 12 ч).
- Перекалибровка температуры (`14b`): модульная формула `R_total = R_ест + R_темп(T) + λ-факторы`, `R_ест ≈ 6.34·10⁻¹⁰/сек` (2%/год), двухрежимная сшивка по контрольным точкам, возрастной потолок 120 лет.
- Лог поселения «Вымерло» (`16a`): таблица `settlement_log` (`000024`), запись той же транзакцией синка, дата — пересечение порога `NDead` по R-модели, причина — доминирующая компонента среды, UI «💀 Вымерло · дата · причина», до 3 записей.
- Плавное движение NPC-агентов между сэмплами (`25a`): клиентская линейная интерполяция позиций летящих между опросами 5с (лаг в один опрос — в сэмпле нет временных меток сервера), лёгкий rAF-цикл только при летящем агенте в вьюпорте, клик и поворот спрайта от интерполированной позиции. Реализация спеки 20a.1 §7 («при полёте иконка движется между сэмплами»).
- Массовая генерация NPC-агентов (26a): слоговый генератор имён «Имя Фамилия» (~7 млн пар, уникальность между пачками), POST /admin/npc/generate (фоновый джоб, COPY-вставка, без лимита, стартовые миры случайные), пагинация списка (keyset, миграция 000029), поиск агента на карте (имя/UUID, центрирование, подсветка), глобальный рубильник пушей (по умолчанию выкл), метрики (очередь на тик, лаги, время генерации), POST /admin/npc/clear «удалить всех агентов», тултип агента по наведению, имена агентов убраны с карты.

**Исправлено:**
- B27 (кнопка «Найти меня» молчала) — `clearUniverseTx` обнулял `current_world_id`; хелпер `assignCurrentWorldsTx` назначает ближайший к центру мир роли `skycomposer`.
- B28 (браузерный WS-тост NPC не работал) — `AuthMiddleware` читает JWT из query `?token=` только для пути `/ws`; реконнект ≤5 попыток.
- B29 (вкладка «Корабли» в админке пустая) — `tab-ships` был вложен в `tab-users` (незакрытый `</div>`), контент скрыт родителем `display:none`; фикс — закрывающий тег (25b).

**Дополнительно:**
- FPS-счётчик в нижнем левом углу карты для ролей admin/skycomposer (инструмент создателя) (28a).
- AGENTS.md §1/§5 устарели — админка на JWT-ролях (admin/skycomposer), а не X-Admin-Password; `zorion-server.exe` добавлен в `.gitignore` (26a).

---

## 2026-09-14 — карта (подписи, плотность, попап звезды), деплой Amvera, снапшот карты

**Реализовано:**
- Подписи масштабируются с зумом (звёзды 11–40px, регионы 13–48px); двойные названия регионов сняты.
- Плотность звёзд к краям (`galaxyEdgeAccept`, 1 → 0.3); регрессия генерации устранена — `spatialGrid` вместо O(n²) «добивки» (100k миров ~17s).
- Попап звезды: карточка звезды + компактный список планет второй колонкой; градации цвета звёзд по температуре (`stemp`, `getStarShade`); форма кластеров `blob`/`circle` (`clusterEdgeAccept`).
- Деплой на Amvera (`zorion-game-skycomposer.amvera.io`), `CGO_ENABLED=0`; миграция `000018` создаёт экономические таблицы (была пустой стабом).
- Карта из снапшота в памяти (`internal/mapcache`): кластеризация `/api/worlds/filter` в Go, immutable `Snapshot` + свап через `atomic.Pointer`; query стабильно 2–4 мс в любой зоне.
- Убрано перемещение из дашборда; кэш статистики инвалидируется в начале генерации планет.

**Исправлено:**
- 21a (разлогин при ребуте сервера) — `godotenv.Load()` в начале `main()` до `config.Load()`.
- B26 (у поселения с 0 населения стабильность показывалась «N%») — вывод «—».
- Стрелка тренда населения — из живого сигнала (`r_per_sec`/`lambda_per_hour`), дельта снапшотов — запасной вариант; закрыл B23.
- Мелкие баги: B3 (кэш текстур читается), B1 (пагинатор «Миров»), B11 (`DELETE FROM planets`), B9 (фильтр ресурсов работает).

---

## 2026-09-13 — разворот экономики: поселения, номенклатура, деньги, монетизация

**Реализовано (геймдизайн):**
- Разворот экономики: спрос проектируется раньше предложения; сущность «поселение» как NPC-рабочая сила и конечный потребитель (`13_settlements.md`).
- Перевывод номенклатуры (`05_economy.md` §5.2.1): товар существует, если есть потребитель и решение игрока; стартовый состав 13 товаров; генератор веществ заблокирован обратно.
- Деньги (`14_money.md`): эмиссия от занятого населения, приватные балансы, контракт = условия + эскроу.
- Монетизация (`15_monetization.md`): продаются деньги, кредиты из пула NPC, чеканки нет.
- NPC-агенты как полноценные клиенты (`06_factions.md` §6.7); упадок как посылка и кризисы (`17_decay.md` §17.1/§17.5); операция — синхронный тип контракта (`11_contracts.md` §11.3.3); промт для ИИ-геймдизайнера `docs/GD_PROMPT.md`.

**Исправлено:**
- B19 (ёмкость — «мёртвое» число) — колонка `settlements.capacity` удалена (`000019`), нижний потолок выводится формулой прироста из среды планеты.
- B21 (население «с потолка» 100k–1B) — генератор переведён на модель (`fixed`/`random`), колонка `settlements.level` удалена (`000020`).
- B22 (население превышало integer-колонку) — верхняя граница 2^31−1 в валидации, пресеты урезаны до `math.MaxInt32`.

---

## 2026-09-12 — регионы галактики, зум, кластеризация карты, биосфера

**Реализовано:**
- Регионы галактики: Вороного по кластерным центрам, имена, цвета, `GET /api/regions` (миграция `000015`).
- Зум карты (привязан к размеру галактики), кластеризация только от 5 звёзд, имя звезды при ховере («пилюля»).
- Баланс биосферы (леса 0.20, ниши джунгли/болота/луга); выбросы при генерации гауссовым смещением; кэш статистики планет + кнопка «Обновить».

**Исправлено:**
- B15 (границы регионов исчезали раньше названий миров) — затухание до `regionDisplayThreshold: 0.6`.
- B13 (границы Вороного у края карты) — запас обрезки ×1.15 → ×1.02.
- B10 (таблица `planets` не создавалась миграциями) — `CREATE TABLE IF NOT EXISTS planets` в `001_init.sql`.
- B5 (опечатка «разряженная» → «разреженная»), B12 (рассинхрон опций фильтра типов), B9 (фильтр ресурсов — артефакт данных), B11 (`DELETE FROM planets`), B1 (пагинатор), B3 (кэш текстур).

---

## 2026-09-11 — автотесты + поиск по названию

**Реализовано:**
- Автотесты на существующий код: `physics_test.go` + `composition_test.go` (coverage 39,4%), `galaxy_test.go` (спектры, 100k семплов, детерминизм), `planet_repo_test.go` (JSON-хелперы), хендлеры + парсер go-test JSON; dev-зависимость `go-sqlmock`.
- Поиск объектов на карте: `GET /api/entities/search` (JWT, точное имя без учёта регистра, спутники через `CROSS JOIN LATERAL`), миграция `000014` (индексы `LOWER(name)`), поле в фильтре-баре (debounce 250 мс), фокус + зум 6× + зелёное кольцо.

**Исправлено:**
- Флак `TestVerifyTamperedSignature` — замена последнего символа base64url меняла только padding; теперь портится символ из середины сегмента (`sig[10]`); 0/30 прогонов.

---

## 2026-09-11 — баги БД, миграции, автоучёт миграций

**Исправлено:**
- B6 `ADMIN_PASSWORD` — обязательный из env, `log.Fatal` при пустом.
- B2 `Register` — `unique_violation` (23505) → 409 вместо 500.
- B7 индекс `worlds(coord_x, coord_y)` — миграция `000012` (фильтр карты: 10 мс → 0,31 мс).
- B8 `FilterWorlds` 1–2 с — миграция `000013` (частичные индексы планет; 1845 → 63 мс и ниже) + `VACUUM (ANALYZE) planets`.
- `planets.forEach is not a function` на мирах без планет; убран отладочный `[PCM-DEBUG]`.

**Реализовано:**
- Формы поверхности спутников (реголит, ледяная кора, криовулканы, разломы, гейзеры; ~1,5% планетарных); тосты в админке (`ui/toast.js`); блок корабли/полёты/UI (`web/static/sprites/`, 22 SVG, `noise.go`, миграция `000011` `users.ship_icon`).
- Автоматический учёт миграций: `migrations.Apply(db)` при старте, версии в `schema_migrations`, baseline для уже существующей схемы; команда opencode `/start`.

**Дополнительно:**
- Перегенерация вселенной: 100 000 миров, 319 573 планеты; доки сжаты (`AGENTS.md` 392 → 158, `STATUS.md` 211 → 64); `origin` → `github.com/lersss/Zorion2`.

---

## 2026-09-10 — 0.4.0: аудит планет, карточка планеты, фиксы гонок, балансировка биосферы в холоде

**Реализовано:**
- Аудит планет: пакет `internal/audit` (дженерик `Run[T]`) + `internal/audit/planet` (43 правила: физика, композиция vs T/вода, атмосфера, ядро, спутники, жизнь), `GET /admin/audit`, вкладка «🔍 Аудит» в админке.
- Карточка планеты: модалка разбита (`tabs.js` + `panel.js` + `index.js`), новые поля (масса, плотность, ядро, композиции, спутники газовых гигантов), цветные полоски, обрезка топ-7 форм.
- Автоматический редирект на `/login-page` при 401/403, удаление токена из `localStorage`.

**Исправлено:**
- `concurrent map writes` в `PlanetGenerator` — `sync.Mutex` на `cache`/`cacheOrder`/`rand`; concurrent write в `WebSocketHub`; race в `TravelManager` (старый полёт удалял новый) и `StatusManager` (`TryStart`).
- Джунгли/леса/луга/болота при T 220–250 K — ×0.05 (2% планет); приливный нагрев спутников 400 → 100; `satellite_hotter_than_giant` 584 → ~0; `biosphere_without_water` 1089 → ~0.

**Дополнительно:**
- `PlanetGenerator` защищён мьютексом; биосферные формы при T < 250 ×0.05, абсолютные запреты (лава при T < 500, лёд при T > 320); `models.Planet` расширена (Density, Core, SurfaceComposition, Satellites, Climate, SystemAge, Hydrosphere, Biosphere, Radioactive).

---

## 2026-09-10 — 0.3.0: ядро планеты, плотность и размер, формула температуры, спектры

**Реализовано:**
- Ядро планеты (`core.go`): тип, доля массы, активность, радиоактивность, возраст.
- Физика: `T_eq = 278.7 × L^0.25 / sqrt(r)` (альбедо, парниковый эффект, вклад ядра); плотность и размер `R = (M/ρ)^(1/3)`.
- Спектральные классы: взвешенное распределение (O 0.5% → M 32%); классификация планет по композиции (`classify.go`); статистика с распределениями.

**Дополнительно:**
- Архетипы `size_min/max` → `mass_min/max`; JSON планеты + `density`/`core`/`system_age`; атмосферы расширены до 14 типов.

---

## 2026-09-10 — 0.2.0: композиция поверхности и недр, ресурсы, спутники, имена

**Реализовано:**
- 16 форм поверхности, 17 типов недр; матрица совместимости (backend + кеш); спутники газовых гигантов (3–10); 6 категорий ресурсов с маппингом «форма → категория»; тип `LocalizedName` (кириллица + латиница).

---

## 2026-09-09 — 0.1.0 (MVP)

**Реализовано:**
- Первый MVP-релиз: генерация вселенной, планеты, модальное окно, текстуры, админка.

---

## Ранняя сессия (без даты) — описания планет, кластеризация карты, безопасность

**Реализовано:**
- Описания планет: фаза 1 — 8 типов × (30 openings + 30 closings), ранее `gas_giant`/`icy`/`oceanic` (150+150); фаза 2 — Go-код (`descriptions_types/tags/load/pick/manager/mapping.go`), автопоиск JSON при старте, детерминированный выбор по хэшу `planet.id`, fallback 7 вариантов.
- Серверная кластеризация карты: `filter_worlds_handler.go` на SQL `GROUP BY` по ячейкам, gzip, `minZoom 0.02 → 0.001`; карта на 100k миров отвечает за < 100 мс.
- Миграция `000010` (`assignments.type/reward`); JWT-секрет из env (`JWT_SECRET`, ≥ 32 байта, `log.Fatal` при пустом).

**Исправлено:**
- `concurrent map writes` в `PlanetGenerator` (паника сервера) — `sync.Mutex` на кэш/`rand`; concurrent write в `WebSocketHub` — per-connection мьютекс; race в `TravelManager`/`StatusManager` (`TryStart`); паника от `r.(string)` в `recover`.
- Классификатор планет: ледяная требует `ShareOf(Glaciers) >= 30` (было 28% перекоса), органик — биосфера ≥ 25% вместо доминанты (тип был почти недостижим), стекло/металл — пороги 15 → 10.
- `ClearUniverse` падала с `upstream request timeout` — `clearUniverseTx`: `TRUNCATE` без CASCADE + явный список таблиц + временное снятие FK у `users`.
- `GetWorld` 500 при несуществующих locations/assignments (→ 404); `column "type" does not exist` → миграция `000010`; `superfluous WriteHeader`.
- `.env` удалён из индекса (был локальный дев-конфиг, не прод-секреты), добавлен в `.gitignore`.

**Дополнительно:**
- Удалены: `docs/CONTEXT.md` (→ `STATUS.md` + `docs/ARCHITECTURE.md`), `docs/readme.gamedev`, `faq_gamedesign.md`, `docs/PROMPT.md` (→ корень); `planet_data_description.go` восстановлен (нужная `clamp`, `generateDescription` — мёртвый код).
