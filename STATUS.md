# Статус проекта Zorion

> Только текущее состояние: фокус, баги, что дальше. **Истории здесь нет** — она в `CHANGELOG.md` и в git.
> Стек, структура, команды, миграции, ловушки — в `AGENTS.md`.

**Обновлено:** 2026-09-11
**Ветка:** master · **Remote:** `github.com/lersss/Zorion2`
**Состояние:** сервер поднят локально, `/status` → `All systems ready`, `go build ./...` проходит, в логах ни `panic`, ни `ERROR`.

---

## 1. Фокус

Явной задачи «в работе» нет. Незакоммиченное рабочее дерево (49+ файлов) снято четырьмя snapshot-коммитами и запушено в новый репозиторий.

**В дереве лежит недокументированный блок «корабли / полёты / UI»:** `web/static/sprites/` (22 SVG + `ships.json`), `web/static/js/map/flight.js`, `ui/toast.js`, `loader.js`, `modal/layout.js`, `internal/generator/planet/noise.go`, миграция `000011` (`users.ship_icon`). Нужно описать в GDD или закрыть как есть.

**Над проектом работают несколько агентов параллельно.** Git ведёт один.

---

## 2. Баги

| # | Что | Где | Приоритет |
|---|-----|-----|-----------|
| B6 | `ADMIN_PASSWORD` — дефолт `admin123`. **Проверено: админка открывается, `/admin/stats` отдаёт 200.** За этим паролем кнопка «Очистка вселенной» | `internal/config/config.go` | 🔴 |
| B1 | Пагинатор на вкладке «Миры» не работает | `web/static/js/admin/worlds.js` | 🟡 |
| B2 | `Register` возвращает 500 вместо 409 при гонке (нужен разбор `unique_violation`, код 23505) | `internal/handlers/auth_handlers.go` | 🟡 |
| B7 | Нет индекса на `worlds(coord_x, coord_y)`. Единственный индекс — `worlds_pkey`. Фильтр карты: `Seq Scan` по 100k строк, `Rows Removed by Filter: 95496`. Держится только на page cache | `migrations/` | 🟡 |
| B8 | `FilterWorlds` до 1,19 с при отдалённой камере (`cell >= 10167`) по логам. В изоляции тот же SQL — 14 мс без фильтров, 65–110 мс с фильтрами. **Причина не найдена**, гипотезы: несколько фильтров сразу либо параллельные запросы фронта (debounce 180 мс) | `internal/handlers/filter_worlds_handler.go` | 🟡 |
| B3 | Кэш текстур пишется, но не читается. При добавлении чтения — обязательно под мьютексом | `internal/generator/planet/planet_image.go` | 🟢 |
| B5 | Опечатка `"разряженная"` → `"разрежённая"` (синхронно с `descriptions_tags.go`) | `config/planet_archetypes.json` | 🟢 |

**Закрыто 2026-09-11:** B4 (`compatibility_matrix` не создана) — таблица в БД **есть**, миграция 009 применена. Запись была ошибочной.

---

## 3. Что дальше

Порядок по соотношению «риск / стоимость»:

1. **`ADMIN_PASSWORD` из env** — по аналогии с `JWT_SECRET`, `log.Fatal` при пустом. ~15 мин. Закрывает B6.
2. **Бэкапы БД** — `pg_dump` по расписанию. Таблица `users` с паролями уже терялась однажды (см. `AGENTS.md` §8). До любого роста нагрузки.
3. **CI** (`.github/workflows/ci.yml`): `go build` + `go vet` + `go test -race ./...`. Сейчас CI нет вообще, а `-race` закрывает главный риск проекта.
4. **Индекс на `worlds(coord_x, coord_y)`** — миграция 012. Закрывает B7.
5. **Нагрузочный тест** — 100 синтетических клиентов на `/ws`, `/travel`, `/api/contracts`. Рекорд по реальным игрокам — 7, почти наверняка не одновременно. Пока теста нет, «многопользовательская игра» ничем не проверена.
6. **`Register` → 409** (B2). ~20 мин.

Долгосрочный бэклог — `docs/gamedesign/08_roadmap.md`.

---

## 4. Открытые решения

- **`migrations/migrations.go`** — раннер с `Apply(db)` написан, но `main.go` его не вызывает. Подключать (и менять правило «миграции только вручную») или убрать?
- **Стратегия скоупа.** Обсуждалось, решения нет: резать ли roadmap (LLM-описания, аномалии как контент, расширение описаний, константы в админку) в пользу вертикального срезa игровой петли `контракт → полёт → добыча → сдача → мир изменился`. Аргумент: вселенная уже готова (100k миров), а контракты существуют как `/api/contracts/complete-test`.
- **`.env` в истории git** (`5253b8c` → `44e3cfc`). Чистить историю или просто ротировать креды? Практично второе, и это совпадает с B6.
- **Блок «корабли/полёты»** — фиксировать в GDD или оставить как есть?

---

## 5. Как обновлять

Держать файл коротким. После сессии: обновить дату, §1, §2, §3. **Историю не накапливать** — она в `CHANGELOG.md`. Правила и стек — не сюда, они в `AGENTS.md`.
