# Статус проекта Zorion

> Только текущее состояние: фокус, баги, что дальше. **Истории здесь нет** — она в `CHANGELOG.md` и в git.
> Стек, структура, команды, миграции, ловушки — в `AGENTS.md`.

**Обновлено:** 2026-09-11
**Ветка:** master · **Remote:** `github.com/lersss/Zorion2`
**Состояние:** сервер поднят локально, `/status` → `All systems ready`, `go build ./...` проходит, в логах ни `panic`, ни `ERROR`.

---

## 1. Фокус

Явной задачи «в работе» нет. Ближайшая очередь — в разделе 3.
Что сделано в прошлых сессиях — в `CHANGELOG.md`.

Решено не делать: балансировку статистики классификатора.

**В дереве лежит недокументированный блок «корабли / полёты / UI»:** `web/static/sprites/` (22 SVG + `ships.json`), `web/static/js/map/flight.js`, `ui/toast.js`, `loader.js`, `modal/layout.js`, `internal/generator/planet/noise.go`, миграция `000011` (`users.ship_icon`). Нужно описать в GDD или закрыть как есть.

**Над проектом работают несколько агентов параллельно.** Git ведёт один.

---

## 2. Баги

| # | Что | Где | Приоритет |
|---|-----|-----|-----------|
| B1 | Пагинатор на вкладке «Миры» не работает | `web/static/js/admin/worlds.js` | 🟡 |
| B9 | Фильтр по ресурсам на карте **не находит ничего никогда**. Фронт шлёт `mineral/organic/rare/fuel/water/gas`, в данных только `газы/редкие/энергия` и только у газовых гигантов (63 322 из 319 573 планет). Плюс порог `> 0.3` в хендлере мёртв: минимумы в данных 0.5/0.7/0.9. Запрос при этом стоит ~1 с | `web/map.html:62`, `internal/handlers/filter_worlds_handler.go:132`, `internal/generator/planet/planet_data_gas.go:71` | 🟡 |
| B10 | Ни одна миграция не создаёт таблицу `planets`, хотя на неё ссылаются `004`, `007`, `000008`, `000013`. На чистой БД накат падает на `004`: `relation "planets" does not exist`. Схему с нуля собрать нельзя — в живой БД `planets` появилась мимо миграций | `migrations/001_init.sql` | 🟡 |
| B3 | Кэш текстур пишется, но не читается. При добавлении чтения — обязательно под мьютексом | `internal/generator/planet/planet_image.go` | 🟢 |
| B5 | Опечатка `"разряженная"` → `"разрежённая"` (синхронно с `descriptions_tags.go`) | `config/planet_archetypes.json` | 🟢 |

Закрытые баги (B2, B4, B6, B7, B8) — в `CHANGELOG.md`, сессия 2026-09-11.

---

## 3. Что дальше

Порядок по соотношению «риск / стоимость»:

1. **Бэкапы БД** — `pg_dump` по расписанию. Таблица `users` с паролями уже терялась однажды (см. `AGENTS.md` §8). До любого роста нагрузки.
2. **CI** (`.github/workflows/ci.yml`): `go build` + `go vet`. Сейчас CI нет вообще. `go test -race ./...` из DoD локально не запускается — нужен cgo, gcc в PATH нет. И тестировать почти нечего: на весь репозиторий один `_test.go`, в `pkg/simulation/batch` — черновик, к сборке не подключён. См. §4.
3. **Нагрузочный тест** — 100 синтетических клиентов на `/ws`, `/travel`, `/api/contracts`. Рекорд по реальным игрокам — 7, почти наверняка не одновременно. Пока теста нет, «многопользовательская игра» ничем не проверена.

Долгосрочный бэклог — `docs/gamedesign/08_roadmap.md`.

---

## 4. Открытые решения

- **`-race` в definition of done.** `AGENTS.md` §2 называет его основным инструментом против риска из §0, но локально он не запускается (нет gcc), а покрывать нечем. Ставить mingw, оставить `-race` только в CI или сперва написать тесты на то, что §0 перечисляет как незащищённое?
- **Стратегия скоупа.** Обсуждалось, решения нет: резать ли roadmap (LLM-описания, аномалии как контент, расширение описаний, константы в админку) в пользу вертикального срезa игровой петли `контракт → полёт → добыча → сдача → мир изменился`. Аргумент: вселенная уже готова (100k миров), а контракты существуют как `/api/contracts/complete-test`.
- **`.env` в истории git** (`5253b8c` → `44e3cfc`). Чистить историю или просто ротировать креды? Практично второе, и это совпадает с B6.
- **Блок «корабли/полёты»** — фиксировать в GDD или оставить как есть?

---

## 5. Как обновлять

Держать файл коротким. После сессии: обновить дату, §1, §2, §3. **Историю не накапливать** — она в `CHANGELOG.md`. Правила и стек — не сюда, они в `AGENTS.md`.
