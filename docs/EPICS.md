# Эпики: где стоим и что дальше

> Доска прогресса по эпик-идеям. Владелец — `@manager`. Одна строка на эпик:
> **идея → текущий чекпоинт → что дальше**. Сами чекпоинты и детали живут в идее
> (`docs/gamedesign/ideas/`), здесь — только сводка. Состояние проекта целиком —
> `STATUS.md`; долгосрочный план — `docs/gamedesign/99_roadmap.md`.

**Обновлено:** 2026-09-26

## Эпики в работе

| Эпик (идея) | Где стоим | Дальше |
|---|---|---|
| **Контракт как состояние: ленивая доска + «пакет контрактов» + снабжение построек** — идея `docs/gamedesign/ideas/2026-09-23_контракт-состояние-ленивое-появление-заказов-и-снабжение-построек.md`; спека `docs/specs/2026-09-23-контракт-ленивая-доска-пакет-и-снабжение.md` | Поставка 1 (Б1+Б2) — ✅ сдана 2026-09-23 (ЧК1–ЧК4, коммит `132332a`) | Поставка 2 — снабжение постройки (буферы `buildings` + сенсор нужд + точка сдачи); ждёт релиза 2 фабрик |
| **Постройка структур на планете (инструмент песочницы)** — идея `docs/gamedesign/ideas/2026-09-24_постройка-структур-на-планете-из-игрового-интерфейса.md`; спека `docs/specs/2026-09-24-постройка-структур-на-планете.md` | **ЧК1 ✅** (`a217171`, принят 2026-09-24): кнопка «Построить» в карточке планеты (админ) — любая структура дерева производителей; вкладка «Строения»; владелец у поселений (только новые — при постройке и генерации; фракция расы). **ЧК2 — конспект интервью принят** 2026-09-25 (идея `2026-09-25_контракты-сдача-товаров-поселениям-интервью.md`): контракт = закрытие **текущей** потребности постройки (сколько не хватает во внутреннем хранилище); заказы рождает постройка; владелец платит (эскроу заранее); **частичная сдача разрешена**, **перекоммит запрещён**; новая сущность «внутреннее хранилище» | **ЧК2а ✅** принят 2026-09-25 (коммиты `a35e701` + `35dd106`; независимый прогон тестера 11/11 PASS): внутреннее хранилище (ячейки), рождение заказов из складского дефицита, свежесть доски, блок «Внутреннее хранилище» в карточке. **ЧК2б ✅** принят 2026-09-25 (коммит `854cdf6`; ревью без блокеров, прогон тестера 11 PASS / 1 N-A): точка сдачи груза на орбите планеты заказа, частичная сдача с пропорциональной выплатой и уменьшением остатка требования, приход в ячейку хранилища, блок «Мои заказы (в работе)» с кнопкой «Сдать». **ЧК2в ✅ дизайн зафиксирован 2026-09-25** (спека `docs/specs/2026-09-25-цена-долей-и-конкуренция-ЧК2в.md`): фиксированная ручная цена за единицу в настройке типа (`base_prices`), множитель конкретного эффекта; объёмные бонусы/штрафы и рост во времени отменены; впереди **калибровка `@balancetester`** (шкала эффекта, ~2000/час) и два средних вопроса `V`/free-mode, затем разработка и тест. **ЧК2г** разгрузка интерфейса/текст. **Сателлит эпика:** идея «Собственность игрока в дашборде» — `docs/gamedesign/ideas/2026-09-25_собственность-игрока-в-дашборде.md`, спека `docs/specs/2026-09-26-собственность-игрока-в-дашборде.md`. **ЧК1 ✅** принят создателем 2026-09-26: вкладка «Собственность» в дашборде (список своей собственности + «Посмотреть» → карта), ручка `GET /me/property`, индекс `idx_buildings_owner` (`000090`); ревью без блокеров, прогон тестера 10/10 PASS. **ЧК2 ✅** принят создателем 2026-09-26: кнопка «Перелететь» (переход на карту с `&fly=1`, доводит до планеты существующей цепочкой полётов; общий хелпер `flyToPlanet`, флаг готовности `meLoaded`); ревью без блокирующих, прогон тестера 9/9 PASS; код — коммит `65d6e8c`. **ЧК3 ✅** принят создателем 2026-09-26 (коммит `be48759`): разгрузка вкладки (компактная двухстрочная строка, отдельный `dashboard.css`), подписи «На карте»/«🚀 Лететь», тексты и статус маршрута в строке («в пути к…»/«прибыли»); ревью без блокирующих, прогон тестера 8 PASS / 1 N-A (живого полёта в dev-БД нет) — фича завершена. Трекер — идея `2026-09-25_контракты-сдача-товаров-поселениям-интервью.md` §8–§10 |
| **Мир прогулки: рельеф, декор и вода по конкретному биому** — идея `docs/gamedesign/ideas/2026-09-23_мир-прогулки-рельеф-и-декор-по-биому.md` | ✅ **ЭПИК ЗАКРЫТ** (ЧК6.3 ✅ 2026-09-26, коммит `3f2020c`). **ЧК-ship ✅** (`e6342c5`). **ЧК0 ✅ закрыт** (подход «всё брать, что украсит»): **Э0+Э1 ✅** (`c3e5602`, тестер 11/11) — цвет/палитра/декор 4 образцов; **Э2 ✅** (`cd3a474`, тестер ПРОЙДЕНО) — ярусный горизонт + фикс тёмных полос на стыках чанков земли; **Э3 ✅** (`d258437`, тестер ПРОЙДЕНО, ревью «блокирующих нет») — форма рельефа по биому (дюны/гребни/пласты/осыпи/русла) + снег шапками, рамка чанка расширена; **ЧК1 ✅** (`a122558`, тестер 10/10 PASS, ревью «блокирующих нет») — вид 12 биомов литосферы: 10 рецептов (3 песчаных, 6 скальных, 1 травяной) + горизонт семействам `скальные пустоши`/`травяные`, цвет биома = цвет с орбиты (кэш картинки `v6`). **ЧК2 ✅** (`9d659a9`, тестер 12/12 PASS, ревью «блокирующих нет») — вид 11 биомов биосферы (луга и степи, болота, коралловые рифы, багровые степи, светящиеся чащи, тихие рощи низкой гравитации, блуждающие рощи, металлические рощи, кислотные дебри, хемосинтетические сады, радиационные ковры), болотам дано семейство; кэш картинки `v7`. **ЧК3 ✅** (`6c5e7e3`, тестер 11/11 PASS, ревью «блокирующих нет») — вид 7 биомов вулканизма + горизонт пресета `лавовые` + клиентский движок читает отложенные параметры (`flow.lobes/slope/levees`, `spike.cluster`, `fan.w/roughness`, `crystal.glow`); кэш картинки `v8`. **ЧК4 ✅** (`f1292b9`, тестер 12/12 PASS, ревью «блокирующих нет») — вид 5 биомов крио (ледники, мёрзлые газы, азотно-ледяная тундра, сухой лёд, инеевые рощи) + горизонт пресета `ледяные`; новых семейств/примитивов нет; кэш картинки `v9`. **ЧК5 ✅** (`465a271`, тестер 14/14 PASS, ревью «блокирующих нет») — вид 10 биомов экзотики + горизонт пресета `экзотика` + новый декор «кристальное дерево», кэш картинки `v10`. **Э5.1 ✅** (`5a4a505`) — фундамент скульптурного объёма: единое поле твёрдости (физика и растр от одного источника), угловая коллизия игрока, консервативная пещерная маска, вытеснение кэша кусков земли. **Э5.2 ✅** (`4fe80d1`, тестер все PASS, ревью «блокирующих нет») — формы: на `горах` арки/нависания/грибные башни, на `каменных_пустошах` нависания и тёмные косметические расселины; `crater` зарегистрирован. **Э5.3 ✅** (`22ee20f`, тестер ПРОЙДЕНО, ревью «блокирующих нет») — гроты и своды: сплошной потолок + объёмные своды/неглубокие гроты у `пещерного_мира_с_потолком`, лавовые трубки у `лавовых_полей`/`магмового_океана`, кольцевые чаши у `кратеров`; корабль/фауна/спавн — на полу (`floorY`), приземная погода — на «поверхности неба» (`skyTop`); любая вырезанная форма проверяется на выходимость по полному полю и откатывается, если запирает | **ЧК6** вода — последний чекпоинт эпика; отдельными мелкими задачами — витрина трубок/чаши, перенос декора/`where` на `floorY`, стабилизация флейка детектора | **Э5** скульптурный объём (сдан: `5a4a505`/`4fe80d1`/`22ee20f`) → **ЧК6** вода (**ЧК6.1 отрисовка ✅ 2026-09-26** (коммит `6acb753`); **ЧК6.2 плавание/погружение ✅ 2026-09-26** (коммит `508896f`; ревью «блокирующих нет», тестер ПРОЙДЕНО: вход/плавание, нырок `down`/всплытие, обзор под водой, спавн на берегу; попутно закрыты запирание под ледовой коркой и порядко-зависимость уровня колонок); вид-косметика отложена в идею `2026-09-26_вода-косметика-вида.md`; **ЧК6.3 лодка ✅ 2026-09-26** (коммит `3f2020c`, тестер 14/14; спрайт 204×120, посадка `KeyE`, запрет лавы и подлёдных) — **ЭПИК ЗАКРЫТ**); отдельными мелкими задачами — вытеснение кэша кусков земли, серверное зеркало бюджета рецепта, стабилизация флейка детектора прыжка в e2e; отдельная идея — «крупные спрайты кораблей» `2026-09-25_крупные-спрайты-кораблей-для-натуральной-величины` |
| **Студия: рефакторинг монолита по блокам** — идея `docs/gamedesign/ideas/2026-09-25_студия-рефакторинг-по-блокам.md` | 🔄 в работе. Нарезка укрупнена до **8 шагов** (решение создателя 2026-09-25; карта 14 файлов — §2 идеи). **ЧК1 ✅** (`8ed5e4e`, смоук 5/5, ревью пройдено): пилот — блок «экспорт/импорт контента» вынесен в `web/static/js/studio/content_transfer.js`, тест-помощник `studioHTML()` читает вынесенные классические скрипты; студия 5120 → 4986 строк. **ЧК2 ✅** (`342b08c`, ревью пройдено, браузерный смоук пройден): ядро вынесено в `web/static/js/studio/core.js`, взаимодействия канваса — в `web/static/js/studio/interactions.js`; студия 4986 → 4462 строки. **ЧК3 ✅** (`48ec089`, ревью «блокирующих нет», браузерный смоук 9/9): граф товаров вынесен в `web/static/js/studio/graph.js`; студия 4462 → 3835 строк. **ЧК4 ✅** (`6fd7915`, ревью «блокирующих нет», браузерный смоук 9/9): попап товара → `web/static/js/studio/goods_popup.js`, ИИ-блок → `web/static/js/studio/fill_ai.js`; студия 3835 → 3286 строк. **ЧК5 ✅** (`6f8e873`, ревью «блокирующих нет», браузерный смоук 9/9): модалки/каркас справочника → `web/static/js/studio/dialogs.js`, справочник производителей → `web/static/js/studio/producers_ref.js`; студия 3286 → 2732 строки. **ЧК6 ✅** (`ecedcba`, ревью «блокирующих нет», браузерный смоук 9/9): масштаб единицы и редакторы попапа производителя → `web/static/js/studio/unit_scale_calc.js`, справочник предметов → `web/static/js/studio/items_ref.js`; студия 2732 → 2035 строк. **ЧК7 ✅** (`16ed85e`, ревью «блокирующих нет», браузерный смоук 9/9): дерево и разделы построек → `web/static/js/studio/buildings_tree.js`; студия 2035 → 1297 строк. **ЧК8 ✅** (`5309a55`, ревью «блокирующих нет», браузерный смоук 9/9): селекторы → `web/static/js/studio/selectors.js`, попапы каталога → `catalog_popups.js`, инициализация/проводка → `init.js`; пустой инлайновый `<script>` удалён, студия 1297 → **513** строк. **🏁 Эпик завершён (8/8)**, монолит разнесён на 14 классических модулей + 2 модульных | Финал: карта студии в `docs/ARCHITECTURE.md` §2 (модули `web/static/js/studio/*.js`) |
| **Синхронизация контента каталога dev → прод** — идея `docs/gamedesign/ideas/2026-09-22_каталог-стабильный-код-экспорт-на-прод.md`; спека `docs/specs/2026-09-24-каталог-экспорт-импорт-контента-на-прод.md` | **И1 ✅** (`09e9f41`, `7e32fa3`): номерная метка записи каталога (`code`) + ренейм-миграция канон-имён («Колония»/«Форпост»/«Поселение») + показ метки в студии. **И2 ✅** (`eb8f801`): экспорт-снимок `content/catalog.json`. **И3 ✅** (`0bc7ad2`): импорт с полной заменой («Проверить» → «Применить», защита живых связей). Выгрузка несёт раздел постройки (`11a6802`). **Первый перенос на боевой прод выполнен 2026-09-25**: режим накатки по именам, блокировок нет, повторный прогон — ровный ноль | Дальше каталог переносится кнопкой в студии (после первой накатки импорт идёт по меткам — без переназначений). Остаток — хвост 55 (разбить большой файл импорта) при следующем касании |

| **Ускоритель — модуль корабля и мини-игра «Прокладка маршрута»** — идея `docs/gamedesign/ideas/2026-09-25_ускоритель-модуль-корабля-и-мини-игра-прокладка-маршрута.md`; спеки `docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md`, `docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md` | ✅ **ЭПИК СДАН** (2026-09-26) — **ЧК1 ✅** (`d71d932`) — тип `accelerator`, выделенный слот, стартовый модуль, состояние отката, серверное ускорение; **ЧК2 ✅** — паспорт перелёта; **ЧК3-сервер ✅** (`425a910`) — поле от seed, оценка пути, ручки `offer`/`boost`; **ЧК3-клиент S1 ✅** (`970d304`, `9b0018e`, `a127e69`) — экран игры + пул спрайтов; **S2 ✅** (`62a28d9`) — вход из интерфейса; **S3 ✅** — смоук + прогон тестера (2026-09-25); админ-сброс таймера ✅ (`0c74e7d`); **модель v9 «Планшет» принята — решение создателя B «внедряем» (2026-09-26); серверная половина v9 готова: Э1-A ✅** (`32073ae`), **Э1-B ✅** (`46453f3`) — `offer` отдаёт доску, `scan` вскрывает сектор, `boost` оценивает путь (бонус включая отрицательный) | **Внедрение v9:** Э3 клиент-доска: **C1 ✅** (`bfba4eb`), **C2 + легенда-попап ✅** (`fe0adcc`), **R (космический словарь + визуал) ✅** (`0f16f62`); **Э4 — вход/спрайты ✅** (`39fb31a`); **Э6 — разбор по факторам ✅** (`a8983ff`, `1468f26`; тестер 12/12) — игра играбельна в сеттинге, результат объясняется. **Э5 — сдача ✅** (2026-09-26): реестр влияний обновлён (`dfa94d4`), спеки закрыты, дельты доков/хвостов переданы; пуш — по слову создателя |

## Отложенные эпики

| Эпик (идея) | Где стоим | Дальше |
|---|---|---|
| **Живая (реактивная) модалка** — идея-заготовка не создана | ⬜ ждёт | следующий шаг: завести идею (подписка модалки на изменения + точечное обновление вместо полной перерисовки; серверные события о редких изменениях структур по сокету + медленный «пульс» для чисел) |

## Как ведётся

- Эпик-идея держит **внутри себя** список чекпоинтов (✅ пройден / 🔄 идём / ⬜ ждёт) и раздел «Дальше».
- Здесь обновляется **одна строка на эпик** — при прохождении чекпоинта (`@manager`).
- Роадмап держит только «блок в работе / сделан», без чекпоинтов.
