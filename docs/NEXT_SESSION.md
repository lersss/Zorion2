# Промт следующей сессии

**Написан:** 2026-09-13 · **Тема:** модель поселения, продолжение

Читать вместе с `AGENTS.md` и `STATUS.md`. Здесь — **повестка обсуждения**:
на чём встали и что дообсуждать. Что сделано и что писать в коде — в `STATUS.md`
§1 и §3, сюда не дублируется.

Для чистой дизайн-сессии без доступа к репозиторию — `docs/GD_PROMPT.md`.
**Внимание: он устарел** — написан до модели поселения и её не содержит.

---

## 1. Где остановились

Выведена модель поселения целиком и внесена в доки. Кода не написано ни строки.

Костяк в одном абзаце: поселение описывается **двумя ортогональными осями** —
население (объём труда) и тир (качество труда). Население зажато двумя потолками:
нижним из подножного корма и верхним из привозной еды. Тир запирает сложность
завода и поднимает пол зарплаты; растёт от **подушевого** излишка, прогресс
набирается **поставленными товарами**, а не деньгами. Нагрузка — вектор товаров
за период, уменьшаемый **рваным** местным покрытием. Кошелёк поселения — лимит на
период, а не счёт; зарплата это переток от владельца, эмиссия — только чеканка
`m` при потреблении. У подножного корма есть видимый осадок, который торговец
скупает: предложение есть везде, спрос — только там, где игрок построил завод.

Документы: `13_tiers.md` (дизайн), `13_tiers_impl.md` (спека),
`docs/GLOSSARY.md` (словарь проекта).

---

## 2. Что решено и не пересматривается

Переоткрывать это — терять время. Если всё же возражаешь, возражай против
конкретного следствия, а не против формулировки.

| Решение | Где |
|---|---|
| Две оси: население — объём труда, тир — качество труда | `13_tiers.md` §13.7 |
| Тир растёт от подушевого излишка, не от суммарного | §13.7.1 |
| Два потолка населения; население выводится, а не назначается | §13.8 |
| Тир запирает сложность завода и поднимает пол зарплаты | §13.9.1 |
| Прогресс тира — в поставленных товарах, прошедших порог качества | §13.9.2 |
| Природный потолок тира: почти всегда 1, тир 3 всегда рукотворный | §13.9.4 |
| Нагрузка — вектор, не число и не деньги | §13.10 |
| Местное покрытие — рваный вектор, а не процент | §13.10.2 |
| Кошелёк — лимит на период; баланса у поселения нет | §13.11.2 |
| Зарплата — переток владельца; эмиссия — чеканка при потреблении | `14_money.md` §14.3.0 |
| Естественный запас: ленивый, с потолком, низкого качества | `13_tiers.md` §13.12 |
| Сложность авто-производства запирается снабжением: завод сложности N ест детали сложности ≥ N | `13_tiers.md` §13.9.6 |
| Построек поселения нет — тир это виртуальное описание | §13.7 |

---

## 3. Очередь на дообсуждение

По убыванию опасности для уже принятого.

### Q1. Чем ограничена сложность автоматического производства — **закрыто 2026-09-13**

Сложность авто-производства запирается снабжением: автозавод сложности N ест
**детали сложности ≥ N**, деталь такой сложности производят только поселения
тира ≥ N (`13_tiers.md` §13.9.6). Гейт потоковый (не стоковый), робот работает
где угодно, география живёт в снабжении. «Запчасти» переименованы в «детали».

### Q2. Распределение природных потолков тира

Сколько планет вытягивает своих до второго тира. Щедрое распределение делает
сложное производство доступным даром и схлопывает географию; скупое превращает
все поселения в одинаковые хижины. Ловится телеметрией «распределение тиров»
(`05_economy.md` §5.3). *Ведётся:* `13_tiers.md` §13.15 п.4.

### Q3. Потолки и скорости естественного запаса

Одна ручка против двух рисков сразу, и они тянут в разные стороны:

- **Первая волна** — 320 тысяч планет с насыщенными складами дают разовый вал
  товара, способный обрушить цены.
- **Игра в сбор урожая** — если сливки выгоднее производства, заводы никто не
  строит.

Ограничитель дальности — топливо, он работает сам. *Ведётся:* `13_tiers.md`
§13.12.5, §13.15 п.5.

### Q4. Состав корзины по тирам

Открытый класс, наполняется контентом. **Заблокирован номенклатурой:** товаров в
коде нет, генератор веществ в блоке (`STATUS.md` §3 п.3). Обсуждать имеет смысл
только вместе с разблокировкой. *Ведётся:* `18_needs.md` §18.1.6,
`13_tiers.md` §13.15 п.3.

### Q5. Норма чеканки и как объяснить эмиссию игроку

`m` — величина для симуляции, вопрос не дизайнерский. А вот вторая половина —
дизайнерская и старая: эмиссия невидима, игрок видит только цены. С разведением
потоков стало острее — зарплату он видит, чеканку нет. *Ведётся:* `14_money.md`
§14.3.3, §14.7 п.2.

### Q6. Гистерезис падения тира

Без него поселение мигает между тирами на каждом перебое. Балансное, но
конструкция нужна до кода. *Ведётся:* `13_tiers.md` §13.9.3, §13.15 п.6.

### Q7. Пять расхождений между доками

P1–P5 в `STATUS.md` §4. Самое содержательное — **P1 (робот и присутствие)**: в
одном документе робот назван формой присутствия, в двух других прямо
противопоставлен ему. Оно смежно с Q1 и, возможно, решается тем же ходом.

Второе по важности — **P2**: слово «фронтир» не определено ни в одном документе,
хотя несёт половину дизайна. Разведение предложено в `GLOSSARY.md` §1, но в доки
не внесено.

---

## 4. Чего не делать

- **Не возвращать постройки поселения.** Они дублируют заводы и ломают
  «сложность добровольна».
- **Не давать поселению баланс.** Кошелёк — лимит. Счёт вернёт сущность «деньги
  поселения» с её блокировками и гонками.
- **Не давать поселению производить товар на продажу.** Осадок подножного корма
  это не производство: никто не нанят, зарплат нет. Производство сделало бы
  экономически живыми все пригодные планеты и завело второй механизм предложения
  на ту же биржу.
- **Не сглаживать покрытие и потолки запаса.** Крайности здесь и есть содержание.
- **Не закрывать открытые списки** — состав корзины, число тиров, типы
  контрактов и кризисов наполняются контентом.

---

## 5. Что можно писать в коде прямо сейчас

Не требует ни одного из открытых вопросов выше:

1. **Формула прироста населения** — население сходится к пределу из среды планеты
   (ёмкость не хранится; B19 закрыт удалением колонки `capacity`, миграция
   `000019`). Закрывает B21.
2. **Естественный запас** — даёт цикл «прилетел, снял, увёз», не зависит ни от
   тира, ни от денег.

Спека: `13_tiers_impl.md` §13.13. Порядок и обоснование: §13.13.7.
Самое опасное место реализации — **гонка при снятии запаса** (§13.13.4).

---

**Сделано 2026-09-13 (код, продолжение темы поселений):** генератор поселений
переведён на **модель генерации** (`internal/generator/settlement/model.go` +
`fields.go`) — вертикальный срез для проверки подхода.

- Модель управляет и выбором планет, и расчётом населения. Режимы: `simple`
  (обход без условий, только шанс + население) и `complex` (правила по полям
  `planet.data`, AND). Население — стратегия `fixed`/`random`.
- Реестр полей (`fields.go`) с типами, экстремумами выпускаемых значений и
  допустимыми значениями; отдаётся админке через `GET /admin/settlement-fields`.
  Это и подсказки в форме, и источник валидации правил.
- В админке генерация поселений — отдельная вкладка «Поселения», форма
  «добавить поле» строит правило по типу поля (number → min/max,
  string → выбор значений, bool → true/false).
- Колонка `settlements.level` удалена (миграция `000020`): тир — расчётная
  величина, при генерации не задаётся. `ApplyOverrides`, `generateSettlementPopulation`
  и хелперы пресета удалены (осиротели). B18 пп.1/3/4 и B21 закрыты.

Оставшееся из B18: **повторный запуск генерации не чистит старые
`settlements`/`factories`/`goods_batches`** — дубли.

Открыто для следующего этапа: **шаблоны генерации полей для complex-модели**
(«линейно и т.п.» — как шаблон строки влияет на отбор и/или население) —
сейчас правила только фильтруют (AND) и не влияют на население.

---

*Написан 2026-09-13.*
