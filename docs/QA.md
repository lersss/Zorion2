# QA — реестр ручных проверок

> Актуальный чек-лист ручных проверок ведётся в **`docs/QA_CHECKLIST.md`**:
> уровень **Ядро** (гонится всегда) + уровень **Зоны** (таргетированно по диффу
> задачи). Формат — реестр «проблема → время последней проверки»: при каждом
> прогоне `@tester` перезаписывает только колонку «Последняя проверка», файл не растёт.
>
> **История прогонов** — в git-истории: до 2026-09-14 здесь вёлся журнал отчётов
> append-блоками (архив — в `git log` по этому файлу).

## Ссылки

- Чек-лист: `docs/QA_CHECKLIST.md`
- Пожелание и обоснование формата: `docs/gamedesign/ideas/qa-checklist.md`