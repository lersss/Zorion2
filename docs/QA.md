# QA — журнал ручного тестирования

> Сюда агент `@tester` (`.opencode/agents/tester.md`) дописывает отчёты о проверках.
> Один отчёт — один блок с датой. Файл ведётся агентом-тестировщиком; правки руками — по ситуации.

## Отчёты

## 2026-09-14 — 99.2.12 «Калибровка температурной смертности» (независимая проверка @tester)

- **Задача:** проверить реализацию спеки `docs/specs/99.2.12-temperature-mortality-calibration.md`
  (H1+H2+H3+H4): квадратичная тяжесть, асимметрия SatCold=350/SatHot=200, жёсткий ноль
  T≥700 K, MaxRatePerHour 0.01→0.1, кламп λ на JSON-границе, флаг Uninhabitable.
- **Прогнал:** `go build ./...`, `go vet ./...`, `go test -count=1 ./...`;
  выборочно `-count=3/-count=5` по флакийным тестам; численные эталоны — временным
  тестом в `internal/economy/settlement/` (`TestTmpSpecReferencePoints` и др.;
  после прогона удалён, мусора не оставил).
- **Сценарии:**
  - Эталонные t50% из спеки: 400 K→110.9 ч, 450→27.7, 500→12.3, 600→4.4,
    550→6.9, 650→3.1; 150→339.6, 100→84.9, 50→37.7, 0→21.2 — все сходятся
    (допуск ±0.6–1.0 ч, λ = ln2/t50).
  - Границы жёсткого нуля: T=699 K — конечная λ>0 (t50≈2.3 ч), T=700 K и T=5000 K —
    λ=+Inf; T=-100 K — конечная (HardZeroCold не задан, по спеке).
  - Комфорт: 200 K и 350 K ровно на границе → severity=0; 199/351 K → >0.
  - Популяция/чек-точки: `Population(0, +Inf, 24)=0`, `Population(p0, +Inf, 0)=p0`
    (Δt=0 не двигает чек-точку, guard от NaN), отрицательный Δt не двигает,
    `Recompute(Δt=0)` не меняет.
  - Эквивалентность гравитации/радиоактивности: линейная форма с капом на 1
    (0g → [0,1], 1.2+0.4g → 0.1, насыщение → 1, кап дальше; rad порог 20,
    насыщение 60, кап 1, радиация 100 → λ конечна, HardZeroAt=0).
  - Флаг `Uninhabitable`: true при T≥700 K, false при 699/0 K/100g/комфорте.
  - Кламп: `ClampLambda(+Inf)=100`, конечные значения не трогаются; оба пути
    `RecomputeSettlementPopulation` (визит и событие) клампят `DecayLambda`;
    `MortalityPreview` клампит `lambda_per_hour`, `Projection` при +Inf → 0 на
    всех чекпоинтах (exp(−Inf·h)=0). `extrapolate.js` уже защищён (`isFinite`).
  - Ревью на хирургичность: правки ограничены смертностью; гравитация/радиация
    численно не изменились; поля `Uninhabitable` в `models/economy.go` не
    добавлено (по спеке §Изменения в коде — флаг только в предпросмотре).
- **Вердикт:** С ОГОВОРКАМИ. Код корректен, все численные эталоны и краевые
  случаи спеки сходятся, JSON-границы от +Inf защищены. Но: (1) существующий
  тест `TestGetPlanetByID` сломан регрессией от жёсткого нуля и теперь флаки —
  `go test ./...` не гарантированно зелёный; (2) `18a_population_death.md` не
  обновлён по §Что меняется в 18a; (3) статусы в roadmap/README не обновлены.
- **Найденное:**
  1. **`internal/repository/planet_repo_by_id_test.go:20,47` — флакийный тест,
     регрессия от жёсткого нуля (приоритет: 🔴 до мержа).** Мок планеты
     `{"temperature":900,"gravity":1.2}`, тест ждёт `population=1_000_000`.
     Т=900 K теперь за жёстким нулём (T≥700 K): `TotalLambda`=+Inf,
     `Population(p0, +Inf, Δt)`=0 при любом Δt>0. Результат зависит от
     микросекунд между двумя `time.Now()` (мок `computed_at` и `now` в
     `attachSettlements`): при Δt=0 — проходит (1000000), при Δt>0 — падает
     (actual 0). Воспроизведено: `go test -count=5 -run TestGetPlanetByID`
     → FAIL 1 из 5; `-count=1 ./...` → FAIL. До калибровки 900 K давал
     конечную λ≈0.01 и тест был детерминированно зелёным — сломал именно этот
     блок. В списке тестов спеки (§Критерии готовности) теста нет — пропущен.
     Шаги: запустить `go test -count=5 -run 'TestGetPlanetByID$' ./internal/repository/`.
     Ожидаемо: стабильно зелёный. Фактически: зелёный или красный в зависимости
     от тиков таймера.
  2. **`docs/gamedesign/18a_population_death.md` — не обновлён по спеке §Что
     меняется в 18a (🟡).** Строки 13 «Численные константы … — черновые,
     не откалиброваны», 128–132 открытый вопрос №1 «масштаб времени … не
     откалиброван» — обе устарели для температуры (масштаб 0.1, SatCold/Hot,
     HardZeroHot=350 приняты и реализованы). Спека прямо предписывала правки
     разделов «Механизм», «Профиль устойчивости» и закрытие вопроса №1 для
     температуры; раздел «Открытые вопросы» спеки требует оставить в 18a только
     указатель («один факт — одно место»).
  3. **`docs/gamedesign/99_roadmap.md:64` и `docs/specs/README.md:24` — статус
     99.2.12 «📅 дизайн-бриф (ждёт одобрения)» не обновлён (🟡).** Код
     реализован и протестирован — статус строки не отражает состояние.
  4. **Наблюдение (не блок): `internal/generator/planet/composition_test.go:352`
     `TestBiosphereNicheDominance` — однократный флаки в полном прогоне
     (`-count=1 ./...`), «луга_степи» доминируют вне ниш.** Не воспроизвёлся
     далее: `-count=3` в изоляции и полный пакет — зелёные. К mortality
     отношения не имеет (композиция поверхности), вероятно, пересечение с
     глобальным `globalDescriptions`. Нужен отдельный разбор, не в этом блоке.
- **Рекомендации:**
  1. Поменять в `planet_repo_by_id_test.go:20` температуру мока с 900 K на
     450 K (конечная λ, тест проверяет «изменения нет при Δt≈0» — остаётся
     честным) либо явно ждать 0 с комментарием «планета за жёстким нулём».
     Прогнать `go test -count=5 ./internal/repository/` до зелёного.
  2. Обновить `18a_population_death.md` по §Что меняется в 18a (константы
     температуры из спеки, асимметрия, жёсткий ноль 700 K, закрытие вопроса №1
     для температуры).
  3. Обновить статус 99.2.12 в `99_roadmap.md` и `docs/specs/README.md`.
  4. Разобрать флаки `TestBiosphereNicheDominance` отдельным блоком (подозрение
     на порядок тестов/глобальное состояние).

### Исправления проверены (2026-09-14, повторный прогон)

- **Задача:** перепроверить фиксы замечаний 1–3 предыдущего отчёта (той же
  сессии). Самостоятельно, без доверия словам разработчика.
- **Прогнал:** `go build ./...`, `go vet ./...`, `go test -count=1 ./...`,
  `go test -count=10 ./internal/repository/...`,
  `go test -count=5 -run 'TestBiosphereNicheDominance' ./internal/generator/planet/`,
  `go test -count=3 ./internal/generator/planet/ -run 'TestGenerateSurfaceComposition|TestBiosphere|TestIsCompatible|TestLoadCompatibility'`.
- **Сценарии:**
  - Фикс 🔴: `internal/repository/planet_repo_by_id_test.go:20` — мок T=900 K →
    500 K, комментарий поясняет причину (жёсткий ноль ≥700 K обнулял население
    при микросекундном Δt). Проверена честность правки: при 500 K
    severity=(150/200)²=0.5625, λ=0.05625/ч, за Δt≈1e-9 ч убыль ~5.6e-11,
    население 1_000_000 сохраняется независимо от тиков таймера; тест
    по-прежнему проверяет «простой визит» (Δt < MinPersistInterval) — пересчёт
    в памяти без записей в БД. Не бессмысленный.
  - Фикс 🟡 доки: `docs/gamedesign/18a_population_death.md` — шапка
    («Температурный профиль откалиброван (99.2.12)»), §Механизм (квадратичные
    профили без капа, жёсткий ноль → λ=+Inf, p=0), §Профиль устойчивости
    (квадратичная форма, комфорт 200–350 K, SatHot=200/SatCold=350, жёсткий
    ноль ≥700 K, холодный не задан, grav/rad черновые), открытый вопрос №1
    (закрыт для температуры: MaxRatePerHour=0.1, SatCold/Hot, HardZeroHot;
    для grav/rad остался; NDead черновой). Сверено с кодом mortality.go:47-68 /
    projection.go:18-27 — совпадает.
  - Фикс 🟡 статусы: `docs/gamedesign/99_roadmap.md:64` и
    `docs/specs/README.md:24` — «✅ Сделано» (README с датой 2026-09-14).
- **Вердикт:** ОК. Все три замечания исправлены честно и согласованно,
  DoD (`build`/`vet`/`test`) зелёный, флаки-тест стабилен на `-count=10`.
- **Найденное:**
  - Повторный флаки не связан с фиксами: `TestBiosphereNicheDominance`
    (`composition_test.go:352`) упал ещё раз в полном `-count=1 ./...`
    (2-й случай за две сессии), но стабильно зелёный в изоляции
    (`-count=5`/`-count=3`) и в полном пакете planet. Причина определена:
    `applyRandomJitter` (`composition_modifiers.go:10`) итерирует map с общим
    `rng` — порядок итерации map в Go случаен, поэтому при фиксированном seed
    результат недетерминирован. Предсуществующая проблема, к 99.2.12 отношения
    не имеет; рекомендация п.4 остаётся в силе.
  - Мелочь: в `18a_population_death.md` подвал «*Обновлено 2026-09-13.*»
    не синхронизирован с шапкой (2026-09-14). Косметика.
- **Рекомендации:**
  - Фиксы можно принимать. Отдельным блоком: сделать `TestBiosphereNicheDominance`
    детерминированным (упорядочить итерацию в `applyRandomJitter` или
    пересчитывать множители по сортированным ключам) — текущий код флакает
    полный прогон `go test ./...`.
