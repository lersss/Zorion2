# Прогон 2026-09-18 — 77a «открытые звёзды» (дозакрытие, новая спека)

**Задача:** независимый прогон дозакрытия фичи 77a «открытые звёзды» — код приведён к новой спеке
`docs/specs/77a-ship-equipment-radar.md` (§0, §5.5, §7.3, §13 И6/И7/И11): вид звезды открыт везде,
полёт к любой звезде, радиус радара решает только для деталей системы.
**Предыдущие прогоны (старая спека, «точка-огонёк»):** `2026-09-17_77a-ship-equipment-radar.md`,
`2026-09-17_77a-spawn-world-fix.md` — старое поведение отменено.

**Сервер:** запущен через `go run` (temp build `C:\Users\admin\AppData\Local\Temp\go-build...\server.exe`,
создан 18.09 3:48:06 — ПОСЛЕ правок Go 3:43–3:45, B-DB-5 соблюдён). `/health` → "OK", `/status` →
"All systems ready" (до и после прогона). Бинарь `zorion-server.exe` (17.09 21:04) — старый, не используется.

**Игроки:** `tester77a_041958` (id 39d598dd-ba70-4bb0-95b7-13be8bdc063c, спавн Benyolob
(-27422.57, -58.14), radar_radius 800, equipment radar_1+scanner_1), `tester77b_042140`
(id 39e529a8-c681-418d-a5e8-1a1925a171fd, спавн Benyolob).

## Чек-лист (12 пунктов)

| # | Проверка | Результат | Факт |
|---|----------|-----------|------|
| 1 | `/api/worlds/filter` для свежего player: все кластеры с именами, поля звезды у далёких | ✅ | 20000 кластеров, все с `sname`/`sspec`/`x`/`y`/`sid`; далёкая Rengal (dist≈16292) отдана полностью (имя/спектр/координаты) — анонимных точек нет |
| 2 | Модалка `/api/worlds/{id}/planets` далёкой системы → 403 | ✅ | Rengal (dist≈16292) → 403 `{"error":"вне зоны видимости"}` |
| 3 | Модалка системы в радиусе → 200 с планетами | ✅ | Extenis (dist≈253) → 200, planets: [Husit, ...] |
| 4 | `/travel` к далёкой неизвестной звезде → 202 | ✅ | Rengal → 202, duration 4887 с (0.3×dist, 66a), полёт стартовал; 403 «вне зоны видимости» убран |
| 5 | `/travel` к несуществующему id → 404 | ✅ | `00000000-0000-0000-0000-000000000000` → 404 "World not found" |
| 6 | `/api/entities/search` звезды за радаром → полный результат | ✅ | Rengal → kind=world, name/spectral=M/coord_x/coord_y полные |
| 7 | `/api/entities/search` планеты неизвестной системы → без имени звезды/координат | ✅ | Lanubto (система Rengal) → world_id="", world_name="", spectral="", coord_x=0, coord_y=0 |
| 8 | Клик по дальней звезде → тост, НЕ разлогин (живой сценарий) | ⚠️ статически | `modal/index.js:50-55`: 403 → `notifyError('Вне зоны видимости: детали системы недоступны')`, return без `handleUnauthorized`; `map/data.js:426-431`: 403 → console.warn + null. Токен не чистится, редиректа нет. Живого браузера в окружении нет |
| 9 | Полёт к дальней звезде, прибытие, модалка после прибытия → 200 | ✅ | Полёт к Vanek (за радиусом 800, dist≈853, duration 250 с) → прибытие → модалка Vanek → 200, planets: 2 |
| 10 | DoD: build/vet/test | ✅ | `go build ./...` OK, `go vet ./...` OK, `go test ./...` — все пакеты ok (вкл. handlers/ship/travel) |
| 11 | Чужие игроки за радаром скрыты (`/api/players/positions`) | ✅ | Игрок 2 (Benyolob) видит игрока 1 в полёте (dist≈131<800); после прибытия игрока 1 в Vanek (853>800) — players: [] (скрыт). Не-летящие не отображаются (90a, не баг) |
| 12 | Фильтры `has_planets`/`has_life` по галактике | ✅ | has_planets=true → 20000 кластеров (9075 далёких |x|>20000), has_life=true → 3390 (1859 далёких) — радиус не ограничивает |

## Дополнительно проверено (вне чек-листа)

- **Редирект 61a:** `/travel` к Vanek во время полёта к Rengal → 202, `start_x/start_y` = точка P
  (-27434.69, -144.06) — корабль продолжает из текущей точки, не отскакивает к Benyolob.
- **`/me`:** `radar_radius: 800`, `equipment: {radar: radar_1, scanner: scanner_1, engine: null}`,
  `ship_model_id: starter` — стартовая комплектация 77a на месте.
- **B-MAP-6:** `node --check` по 8 свежим JS-файлам (map/data.js, map/events.js, map/map_render.js,
  modal/index.js, modal/events.js, modal/tabs.js, search.js, map/npc_agents.js) — синтаксис OK.
- **B-DB-5:** сервер (go run temp build 3:48) собран после правок Go (3:43–3:45) — новый код активен.

## Найденное

Блокеров нет. Замечаний к поведению 77a нет.

## Ре-прогон клиентского флоу (2026-09-18, после фикса разработчика)

Серверная часть не менялась — вердикт по ней валиден. Разработчик исправил клиентский флоу п.8
(баг подтверждён живым браузером создателя): при 403 вместо `notifyError` модалка открывается
с карточкой звезды и заглушкой. Точечный ре-прогон (только затронутая область):

| # | Проверка | Результат | Факт |
|---|----------|-----------|------|
| 1 | Код-флоу: клик по одиночной за-радарной звезде → модалка с карточкой, НЕ notifyError/разлогин/редирект | ✅ | `map/events.js:166-174` (cnt===1 → openSystemModal с starInfo {stype,stemp,systype,smods,x,y}) → `modal/index.js:54-70` (403 → data {planets:[], restricted:true, star_type, system_type, stellar_mods, temperature, coord_x, coord_y} из starInfo → renderModal) → заголовок с координатами (`index.js:249-251`), чипы типа (`285-303`) → `panel.js:155-165` (restricted → заглушка «Система вне зоны видимости…»). НЕ notifyError, НЕ handleUnauthorized |
| 2 | Пустой planets безопасен | ✅ | renderModal: planets=[] (`index.js:201`); drawSystem — guard'ы `length>0` (`modal_render.js:38,73`), звезда рисуется (`54-70`); minimap рисует главную звезду, планеты не рисуются (`minimap.js:44-89`); computeLayout с [] — ок (`layout.js:47-62`); вкладки карточки планеты не появляются (renderRightPanel(planets,null) → renderPlanetsList → restricted-заглушка) |
| 3 | Другие вызывающие openSystemModal не сломаны | ✅ | `search.js:120,122` — 5 аргументов (starInfo undefined), известные системы → 200, focusOpts работает; `admin/worlds.js:77` — 5 аргументов, админ → 200. Сигнатура расширена опциональным 6-м параметром — старые вызовы валидны |
| 4 | node --check изменённых JS | ✅ | modal/index.js, map/events.js, modal/panel.js, modal/state.js — синтаксис OK |
| 5 | 403-ветка в refreshPlanets | ⚠️ косметика | `modal/index.js:117-121` → throw → catch (`148-151`) → `notifyError('Ошибка обновления: …')`. Не разлогин. В restricted-модалке кнопки «Обновить» нет (заглушка без кнопки), автообновление (admin) в restricted не бывает — ветка практически недостижима штатно |
| 6 | Живой сценарий | ⚠️ статически | Браузера в окружении нет — проверено по коду + node --check. Создателю: клик по за-радарной звезде → модалка с карточкой звезды (имя/спектр/тип/координаты) и заглушкой «Система вне зоны видимости…», без тоста-ошибки, без разлогина |

**Вердикт ре-прогона: ОК.** Блокеров нет. Подозрительное (вне скоупа фикса, не регрессия):
`search.js:115-126` — планета НЕИЗВЕСТНОЙ системы (world_id="") → `openSystemModal("", …)` →
fetch `/api/worlds//planets` → 404 → notifyError «Ошибка загрузки планет». Существовало до фикса
(серверная часть и search.js не менялись), но UX-край: клик по планете неизвестной системы в
поиске показывает ошибку вместо заглушки. Предложение: в `focusOnResult` при пустом `world_id`
не открывать модалку (или открывать restricted-карточку звезды по имени).

## Не проверено (граница покрытия)

- **Пункт 8 (живой браузер):** клик по дальней звезде в реальном браузере не воспроизводил —
  окружение без браузера. Проверено статически по коду (modal/index.js, map/data.js) + node --check.
  Создателю: кликнуть по за-радарной звезде на карте → ожидать тост «Вне зоны видимости: детали
  системы недоступны», остаться на карте, токен в localStorage не должен исчезнуть.
- **Пункт 9 (полёт к очень далёкой звезде):** полёт к Rengal (81 мин) не дожидался — редирект на
  Vanek (250 с) и проверка прибытия на нём. Логика «игрок в системе → модалка 200» идентична
  (CanSeeWorld: current_world_id == worldID → true, `visibility.go:154`).
- **Знание (player_planet_knowledge) и протухание 7 дней:** не проверял — не входит в чек-лист
  дозакрытия (проверялось в предыдущих прогонах 77a).
- **Админ/skycomposer видят всё (И7):** не проверял живым админ-токеном — не входит в чек-лист;
  код: фильтры применяются только к `role=player` (`visibility.go`, `players_positions.go:25`).

## Предложения в чек-лист

- Кейс на «клик по за-радарной звезде → тост, не разлогин» (пункт 8) — кандидат в B-MAP-зону
  (регрессионный, бывший критичный баг).
- Кейс на `/api/players/positions` (77a §5.3): игрок в радиусе виден, за радиусом скрыт,
  не-летящие не отображаются (90a) — кандидат в зону карта/фронт.