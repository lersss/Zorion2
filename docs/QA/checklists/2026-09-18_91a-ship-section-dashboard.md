# Прогон 2026-09-18 — 91a «Раздел "Корабль" в дашборде игрока»

**Задача:** независимый прогон фичи 91a (спека `docs/specs/91a-ship-section-dashboard.md`):
раздел «Корабль» на дашборде `/`, двигатель `engine_1` как настоящий модуль (каталог +
стартовая комплектация + источник скорости), полёт без двигателя запрещён для `role=player`,
админ/skycomposer — исключение.

**Сервер:** `zorion-server.exe` (temp build `C:\Users\admin\AppData\Local\Temp\opencode\zorion-server.exe`,
собран 18.09 6:22:38, процесс стартовал 6:22:41 — после правок Go 6:18, B-DB-5 соблюдён).
`/health` → "OK" (до и после прогона). Миграция `000043` применена (последняя в
`schema_migrations`). Замечание: `internal/ship/catalog.go` и `internal/models/ship.go`
изменены 6:27:00 — ПОСЛЕ сборки бинаря 6:22:38; но живой сервер отдаёт все новые поля
(`ship_catalog` с `engine_1`, `ship_speed_factor` 0.3, валидация двигателя работает) —
функциональный код в бинаре есть, правки 6:27 косметические (комментарии).

**Игроки:**
- `qa91a_1789688254801` (id `6dfa8add-ef70-4dc8-aacc-99ea6eb10563`, спавн Glaan) — создан для
  прогона; на время пункта 3 `equipment.engine` обнулялся SQL, после — восстановлен `engine_1`.
- `qa91a_admin_1789688329610` (id `37fe17fc-8e5e-4772-9123-e9f5f15197f4`) — создан для пункта 4:
  роль `admin` + обнулённый `engine` (SQL), после — восстановлен (`player`, `engine_1`).

## Чек-лист (10 пунктов)

| # | Проверка | Результат | Факт |
|---|----------|-----------|------|
| 1 | `/me` (player): `ship_model` {id,name,slots}, `ship_catalog` (вкл. `engine_1` {type:engine, params.speed_factor:0.3}), `ship_speed_factor`=0.3, `equipment.engine`="engine_1"; существующие поля на месте | ✅ | Сырой ответ (curl.exe, UTF-8): `ship_model` {id:"starter", name:"Стартовый разведчик", slots:{radar:1,scanner:1,engine:1}}; `ship_catalog`: engine_1 «Двигатель-1» {type:"engine", params:{speed_factor:0.3}}, radar_1 {radius:800}, scanner_1 {depth:"surface",settlements:true}; `ship_speed_factor`: 0.3; `equipment.engine`: "engine_1"; `ship_icon` crescent.png, `ship_color` null, `ship_options` 21, `radar_radius` 800, `current_world_id` на месте |
| 2 | Полёт с двигателем: `POST /travel` → 202 | ✅ | Glaan → Appel → 202, duration 126 с (0.3×dist, 66a), полёт стартовал |
| 3 | Полёт без двигателя (player): `POST /travel` → 400 «Двигатель не установлен — полёт невозможен» | ✅ | engine обнулён SQL → 400 `{"error":"Двигатель не установлен — полёт невозможен"}` (точный текст в UTF-8) |
| 4 | Админ без двигателя: `POST /travel` → 202 | ✅ | role=admin + engine=null → 202, duration 126 с (исключение работает) |
| 5 | Миграция 000043: `equipment` содержит `engine_1`; у существующих игроков `users.equipment->engine` = `engine_1` | ✅ | `schema_migrations` — 000043 последняя; `equipment`: engine_1 {type:engine, params:{"speed_factor":0.3}}; бэкфилл: у всех существующих игроков (выборка 15) `equipment->>'engine'` = engine_1 |
| 6 | Дашборд `/`: карточка «Корабль» — превью (спрайт+цвет), имя модели, силуэт с ячейками (радар 800, сканер поверхность+поселения, двигатель 0.3 сек/px), 4 пустые ячейки без подписей; каталога «что бывает» НЕТ | ✅ статически | `web/index.html:66` карточка «🛸 Корабль»; превью `shipSectionPreview` (спрайт+цвет, :168-172); имя модели (:174); SVG-силуэт (:77-84) + слоты радар/сканер/двигатель (:85-87) + 4 пустые ячейки `ship-slot-future` без текста (:88-91); характеристики из каталога (`slotDetail` :145-159: «Радиус: 800 px», «Глубина: поверхность · Поселения: да», «Скорость: 0.3 сек/px»); рисуется только установленное из `users.equipment` (:186-209), каталог — справочник имён (И1) |
| 7 | Карта: полёт работает (клик по звезде → «Лететь» → летит) | ✅ | Серверно: пункт 2 (202). Клиентский флоу не менялся (`startFlight` → `/travel`), добавлена только блокировка без двигателя (`map/events.js:204-207`) |
| 8 | Блокировка без двигателя на карте: кнопка «Лететь»/«Перелететь» заблокирована, тост «Двигатель не установлен — полёт невозможен» | ✅ статически | `map/data.js:351-355` — `state.hasEngine` из /me (equipment.engine + ship_catalog, тип engine); `map/events.js:204-207` — «Лететь» (тултип) → `notifyError('Двигатель не установлен — полёт невозможен')`, return; `map/events.js:388-391` — «Перелететь» (контекстное меню) — то же; `modal/events.js:427-430` — «Перелететь» в модалке — то же; дефолт `hasEngine: true` до загрузки (`map/config.js:39`, `modal/state.js:39-40`) — UI не блокирует до данных |
| 9 | Не-регрессия: селектор 61b работает (смена сохраняется); модалка системы открывается; полёт переживает F5 | ✅ | Селектор: PUT `/me/ship-icon` anchor.png → 200 (восстановлен crescent.png); PUT `/me/ship-color` #ef4444 → 200, null → 200 (#ff0000 вне палитры → 400 — корректная валидация 61b, не баг). Модалка: `openSystemModal` расширен опциональным `starInfo.hasEngine` (`map/events.js:175`, `modal/index.js:48` — старые вызовы валидны), node --check OK. F5: `/me.flight` отдаётся (проверено: flight {duration:126, from, to, start_time, start_x, start_y}) |
| 10 | DoD: `go build ./...`, `go vet ./...`, `go test ./...` | ✅ | build OK, vet OK, test — все пакеты ok (вкл. handlers/ship/travel) |

## Дополнительно проверено (вне чек-листа)

- **`/me` без двигателя:** `ship_speed_factor` отсутствует (спека 91a §7.3: «клиент получает
  ship_speed_factor только при установленном двигателе») — ✅, `equipment.engine` пуст.
- **B-MAP-6:** `node --check` по 6 изменённым JS (map/events.js, map/data.js, map/config.js,
  modal/events.js, modal/index.js, modal/state.js) — синтаксис OK.
- **B-DB-5:** бинарь 6:22:38 собран после правок Go 6:18 (auth_handlers.go, travel_handlers.go);
  расхождение: catalog.go/ship.go изменены 6:27 — после сборки, но функционально сервер отдаёт
  всё по спеке (правки косметические).
- **Кракозябры в консоли** (cp866-артефакт PowerShell): сырые ответы сервера проверены через
  curl.exe в UTF-8 — имена «Стартовый разведчик», «Двигатель-1», «Радар-1», «Сканер-1» и текст
  ошибки корректны.

## Найденное

Блокеров нет. Замечание (не баг): `internal/ship/catalog.go` и `internal/models/ship.go`
изменены 6:27:00, бинарь собран 6:22:38 — исходники новее бинаря; функционально сервер
отдаёт всё по спеке (проверено живыми запросами), расхождение уйдёт при следующей сборке.

## Не проверено (граница покрытия)

- **Пункты 6–9 (живой браузер):** визуальный рендер карточки «Корабль» (силуэт, ячейки),
  клик по звезде → «Лететь», тост при блокировке без двигателя, F5 в полёте — окружение без
  браузера; проверено статически по коду + node --check + серверные эндпоинты. Создателю:
  открыть дашборд `/` (карточка «Корабль» с силуэтом и 3 модулями), на карте кликнуть по
  звезде → «Лететь», F5 во время полёта.
- **Пункт 8 (живой сценарий без двигателя):** тост при клике «Лететь» без двигателя —
  статически (`notifyError`), живого браузера нет.
- **Экстремальный случай «игрок без двигателя после миграции»:** теоретический (бэкфилл
  000043 выдал `engine_1` всем); проверен на обнулённом юзере (пункт 3).

## Предложения в чек-лист

- Кейс «полёт без двигателя → 400 + блокировка кнопки на карте» (91a §6.1) — кандидат в зону
  полёты (B-TRV) или карта/фронт (регрессионный, новое правило).
- Кейс «/me отдаёт ship_model/ship_catalog/ship_speed_factor» (91a §7.3) — кандидат в Ядро A2
  (расширение профиля) или зону auth.