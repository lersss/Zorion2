# Прогон 2026-09-18 — «Вкладки в дашборде игрока» (идея 2026-09-18)

**Задача:** независимый прогон фичи «Вкладки в дашборде игрока» (идея
`docs/gamedesign/ideas/_archive/2026-09-18_вкладки-дашборда-игрока.md`, правка только
`web/index.html`): вместо простыни из 5 карточек — шапка «Ваш агент» (всегда
видима) + таб-бар с 4 вкладками (Корабль / Внешний вид / Статистика / Энциклопедия),
активная вкладка в localStorage `dashboardActiveTab`, дефолт/fallback «Корабль».

**Сервер:** dev-сервер уже был поднят на `http://localhost:8080` (go run, статика
с диска noCache). `/health` → "OK", `/status` → "All systems ready" (A1 ✅).
Сервер отдаёт актуальный `web/index.html` с вкладками (проверено HTTP-фетчем:
`dashboard-tabs`/`activateDashboardTab`/`tab-look` присутствуют) — B-MAP-4 ✅.

**Игроки (созданы для прогона):**
- `tester_tabs_1789732464093` (id `b6d0df06-6bc5-4472-8ca2-98c41fcd4ed9`, спавн Glaan) —
  для API-проверки /me (A2).
- `dash_1789732554795_0` — для браузерного прогона дашборда (регистрация внутри
  скрипта, как в map-check.js).

## Чек-лист (12 пунктов)

| # | Проверка | Результат | Факт |
|---|----------|-----------|------|
| 1 | Шапка «Ваш агент» (имя, корабль, мир, статус полёта, кнопки Контракты/Карта/Выйти) видна всегда, не скрывается переключением вкладок | ✅ | Шапка — отдельная `.card` вне `.tab-pane` (`web/index.html:85-96`), структурно не скрывается; в браузере проверено после переключения на «Внешний вид»: `headerVisible=true`, все элементы на месте (`agentName` заполнен, `shipPreview`/`currentWorld`/`flightStatus` есть, кнопки «Контракты»/«Карта миров»/«Выйти» присутствуют) |
| 2 | Таб-бар под шапкой: 4 кнопки в порядке «🛸 Корабль / 🎨 Внешний вид / 📊 Статистика / 📖 Энциклопедия», активная подсвечена | ✅ | `web/index.html:98-103` — 4 `.tab-btn` в нужном порядке (проверено в браузере: `tabs=[🛸 Корабль|🎨 Внешний вид|📊 Статистика|📖 Энциклопедия]`); активная получает класс `active` (`activateDashboardTab`, :561-562), CSS-подсветка `.dashboard-tabs button.active` (:74-78) |
| 3 | Клик «Внешний вид» → панель с сеткой 21 спрайта + палитрой (включая «Оригинал») + статус сохранения; остальные панели скрыты | ✅ | Браузер: `tiles=21`, `palette=10` (Оригинал + 9 цветов), `original=true`, `#ship-status` на месте; панели: `tab-look` активна, `tab-ship`/`tab-stats`/`tab-encyclopedia` скрыты |
| 4 | Клик «Статистика» → статистика + заметка про журнал открытий | ✅ | Браузер: `#stats` содержит «Изучено систем»/«Полётов» (renderStats, `encyclopedia.js:27-47`), `.journal-note` «Журнал открытий хранится в этом браузере (localStorage)» на месте, активна `tab-stats` |
| 5 | Клик «Энциклопедия» → энциклопедия рендерится (разделы рас/звёзд/планет/ресурсов/кораблей/лор-терминов) | ✅ | Браузер: все 6 разделов (`enc-races`, `enc-stars`, `enc-planets`, `enc-resources`, `enc-ships`, `enc-lore`) присутствуют, текст содержит «Расы»/«Звёзды»/«Планеты»/«Ресурсы»/«Корабли»/«Лор»; `/api/encyclopedia/races` ответил (разделы отрисованы, не заглушка «Не удалось загрузить») |
| 6 | Клик «Корабль» → силуэт корабля со слотами (радар/сканер/двигатель) + сводные характеристики | ✅ | Браузер: `#ship-silhouette` есть, слоты `slot-radar`/`slot-scanner`/`slot-engine` на месте, `#ship-section-stats` содержит «Радиус радара 800 px», «Сканер поверхность + поселения», «Скорость 0.3 сек/px» |
| 7 | Перезагрузка страницы → активная вкладка восстанавливается (localStorage dashboardActiveTab) | ✅ | Браузер: клик «Внешний вид» → `stored=tab-look` → reload → активны `tab-look` (pane + btn + localStorage) |
| 8 | Первый заход (очищен localStorage) → дефолт «Корабль» | ✅ | Браузер: `localStorage.removeItem('dashboardActiveTab')` → reload → `stored=tab-ship`, активна `tab-ship` |
| 9 | При переключении вкладок и загрузке — нет ошибок в консоли браузера | ✅ | `page.on('pageerror')` за весь прогон (загрузка + 8 переключений + 2 reload + сохранения) — 0 событий |
| 10 | Функциональность не потеряна: клик по спрайту корабля и по цвету палитры во вкладке «Внешний вид» работает (статус «⏳ Сохранение...» → «✅ ... сохранена») | ✅ | Браузер: клик по 2-му спрайту (`book.png`) → статус «✅ Иконка сохранена»; клик по свотчу `#ef4444` → «✅ Цвет сохранён» (PUT `/me/ship-icon` и `/me/ship-color` отработали, 200) |
| 11 | e2e-смоук `tools/e2e/map-check.js` | ✅ | Прогнан: **ALL PASS** (1/5 register/login, 2/5 map loads, 3/5 click star, 4/5 restricted card, 5/5 screenshots). Смоук про карту, но закрывает общий клиентский флоу (регистрация/логин, загрузка страницы без JS-ошибок, клики по UI) — для дашборда дополнительно написан кастомный браузерный чек (см. ниже) |
| 12 | Резюме | 🟢 | Блокеров и замечаний нет (см. «Найденное») |

## Дополнительно проверено (вне чек-листа)

- **A2 (Ядро):** регистрация → логин → `/me` с JWT: `username`, `current_world_name`
  (Glaan), `ship_icon` crescent.png, `ship_color` null, `ship_options` 21,
  `ship_catalog` 3 (engine_1/radar_1/scanner_1), `equipment` {engine_1, radar_1,
  scanner_1}, `radar_radius` 800, `ship_speed_factor` 0.3, `flight` null — ✅.
- **Fallback на несуществующий id панели (идея §«Паттерн»):** `dashboardActiveTab='tab-bogus'`
  → reload → активна `tab-ship`, localStorage перезаписан на `tab-ship` — ✅
  (`activateDashboardTab` :557-560, `document.getElementById(tabId)` guard).
- **B-MAP-6 (синтаксис фронта):** правка — инлайн JS в `web/index.html`; проверено
  живым браузером (модуль загрузился, энциклопедия/селектор отрисовались, 0 pageerror) —
  сильнее `node --check`. ✅
- **B-MAP-4 (noCache статика):** сервер отдаёт актуальный `index.html` с вкладками
  без рестарта — ✅.
- **Кастомный браузерный чек дашборда** (`dashboard-check.js`, playwright-core +
  системный Chrome, скрипт во временной папке, в репо не добавлялся): 12 шагов
  (шапка/таб-бар/дефолт, 4 вкладки по очереди, reload-восстановление, очистка LS,
  bogus-fallback, сохранение спрайта и цвета, pageerror) — **ALL PASS**. Скриншот
  `dashboard-look.png` (вкладка «Внешний вид») сохранён в temp.

## Найденное

Блокеров и багов нет. Замечаний нет.

## Не проверено (граница покрытия)

- **Визуальный рендер скриншотов** (`dashboard-look.png`, `map.png`,
  `restricted-card.png`): модель тестера не поддерживает чтение изображений —
  скриншоты сняты, но глазами не просмотрены. Структура подтверждена DOM-проверками
  (21 тайл, 10 свотчей, активные классы). Создателю: глянуть дашборд `/` глазами —
  вкладки, подсветка активной, сетка спрайтов, палитра.
- **UX глазами игрока** (удобство таб-бара, порядок вкладок, hover-состояния) —
  вкусовое, вне покрытия тестера.
- **Край: очень длинные имена/миры в шапке** (перенос строк в шапке при узком окне) —
  не проверялось, экстремальный вход недостижим штатно.

## Предложения в чек-лист

- Кейс «дашборд `/`: вкладки (Корабль/Внешний вид/Статистика/Энциклопедия), активная
  в localStorage `dashboardActiveTab`, дефолт/fallback «Корабль», шапка всегда видима,
  сохранение спрайта/цвета работает» — кандидат в зону карта/фронт (B-MAP) как
  регрессионный для дашборда (клиентский флоу, Go-тестами не покрыт).