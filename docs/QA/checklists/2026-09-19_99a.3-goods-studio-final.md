# QA-прогон: Студия товаров 99a.3 — финальный регресс перед приёмкой

**Дата:** 2026-09-19
**Тестировщик:** @tester
**Вектор:** Студия товаров (финальный контрольный регресс после правок 99a.3: инверсия T0, перенос фильтров в топбар, авто-центрирование)
**Студия:** запущена на 127.0.0.1:8799 (394 товара: 263 good + 131 resource, 13 категорий)

## Что менялось (глазами, до прогона)

- **Инверсия T0:** `cmd/goods-studio/web/index.html:1005` — `if (!fT0 && g.kind === "resource") return false;` — T0 выкл = ресурсы скрыты из справочника, вкл = показаны.
- **Фильтры в топбар:** `index.html:123` — `#fT0` в топбаре; `#spravSearch/#fUsed/#fUnused/#fBanned` перенесены в `#topbar`; sticky-шапка `.sprav-head` в панели убрана.
- **Авто-центрирование:** клик по карточке на канвасе → pan к центру вьюпорта, зум сохранён (только фокус-режим).
- **Ранее:** клик по дочкам не сбрасывает выбор; dblclick = выбор+попап.

## Прогнал

- `node tools/e2e/goods-studio-check.js` — 21 шаг, **1 FAIL** (шаг 12, см. замечания), 0 SKIP
- `node tools/e2e/qa99a3-smoke.js` — 14 шагов, 0 FAIL
- `node tools/e2e/qa99a3-topbar-filters.js` — 6 шагов, 0 FAIL
- `node tools/e2e/qa99a3-center-check.js` — 5 шагов, 0 FAIL (F3)
- Диагностика: `diag-t0-drag.js` (drag ресурса в слот при T0 вкл), `diag-subgraph-rebuild.js` (шаг 13 goods-studio-check, не выполнился из-за обрыва на шаге 12)

DoD (`go build/vet/test`) не гонял: правки чисто фронтовые (index.html), Go-код не затронут.

## Сценарии (жёсткий чек-лист, каждый пункт — один раз)

| # | Что проверено | Результат |
|---|---|---|
| F1 | T0 выкл → в справочнике НЕТ ресурсов (263 карточки, 0 `.b-resource`); T0 вкл → ресурсы появились (394 карточки, 131 `.b-resource`) — smoke шаги 8a/8b + diag-t0-drag (264/0 → 395/131) | ✅ ПРОЙДЕН |
| F2 | Фильтры в топбаре: `#spravSearch/#fUsed/#fUnused/#fT0/#fBanned` внутри `#topbar`, панель `#sprav` чистая (нет `.filters`/`.sprav-head`); поиск фильтрует (QA_zzz_nomatch → 0, очистка → 263); панель скроллится свободно (scrollTop=2000, карточки видны, sticky-шапки нет) — topbar-filters шаги 1–3 | ✅ ПРОЙДЕН |
| F3 | Клик по дочке h_0002 на канвасе → карточка в центре вьюпорта (700/511 = центр), зум 1.5 сохранён; повторный клик по родителю h_0001 → снова в центре, зум сохранён; pan-драг по пустому месту → panX/panY изменились, центрирование НЕ сработало, выбор сохранён; выбор из справочника центрирует (зум 1.2 сохранён) — center-check шаги 1–4 | ✅ ПРОЙДЕН |
| F4 | Регресс полного цикла: goods-studio-check.js — **1 FAIL** (шаг 12, устаревший тест-скрипт, см. замечания; шаги 13–15 не выполнились из-за обрыва — закрыты отдельно: 13a/13b diag-subgraph-rebuild PASS, 14 = smoke шаг 7 PASS, 15 = все прогоны чисты); qa99a3-smoke.js — 14/14 PASS; qa99a3-topbar-filters.js — 6/6 PASS | ⚠️ С ОГОВОРКАМИ |
| F5 | JS-ошибок в консоли — 0 (все прогоны: goods-studio-check шаг 15 не выполнился, но smoke шаг 10, topbar-filters шаг 6, center-check шаг 5, обе диагностики — clean) | ✅ ПРОЙДЕН |
| F6 | Состояние чистое: GET /api/state — QA-товаров 0, оверрайдов 0 (проверено до и после всех прогонов; cleanup всех скриптов PASS) | ✅ ПРОЙДЕН |

## Вердикт

**С ОГОВОРКАМИ.** Приложение работает по ТЗ (F1–F3, F5, F6 — ПРОЙДЕН). Единственный FAIL — устаревший шаг 12 тест-скрипта `goods-studio-check.js`, не баг приложения (подтверждено диагностикой: drag ресурса в слот работает при включённом T0).

## Найденное

- **Тест-скрипт устарел (не баг приложения):** `tools/e2e/goods-studio-check.js:244-257` (шаг 12) тащит карточку РЕСУРСА из справочника в слот попапа, не включив `#fT0`. После инверсии T0 (правка №1) ресурсы по умолчанию скрыты из справочника (`index.html:1005`), карточка не находится → `dragTo` падает, fallback падает с `Cannot read properties of undefined (reading 'dispatchEvent')` (card = undefined). **Фикс теста:** перед шагом 12 поставить `await p.check('#fT0')` (и снять после, чтобы не влиять на шаги 13–15). Приложение подтверждено рабочим: `diag-t0-drag.js` — T0 вкл → drag ресурса в слот → `slotFilled: true` (recipe[0].good_id = res:water), 0 ошибок.
- **Побочный эффект обрыва:** из-за падения шага 12 try-блок прервался, шаги 13a/13b/14/15 goods-studio-check.js не выполнились. Закрыты отдельно: 13a/13b — `diag-subgraph-rebuild.js` PASS (клик по родителю на канвасе → selected=родитель, подграф содержит родителя и ребёнка, попап не открыт); 14 — smoke шаг 7 PASS (всё дерево: бейдж, поиск/ресурсы/btnTop видны, 263 карточки); 15 — все прогоны без pageerror.

## Чеклист непроверяемого

- **Не проверено:** поведение goods-studio-check.js шагов 13–15 в самом скрипте (обрыв на шаге 12) — закрыто эквивалентными проверками (см. выше); после фикса теста прогнать скрипт целиком.
- **Не проверено:** центрирование при очень большом подграфе (сотни карточек) — в текущем реестре подграфы до ~4 карточек; экстремальный размер недостижим штатно.
- **Не проверено:** визуально-UX (плавность pan-анимации, «приятность» центрирования) — headless-браузер; нужен живой браузер создателя.

## Предложение в чек-лист

- Обновить `tools/e2e/goods-studio-check.js` шаг 12: включить `#fT0` перед drag ресурса (иначе шаг всегда FAIL после инверсии T0). Применит @manager.