# QA-прогон: Студия товаров 99a.3 — регресс двух фиксов (клик по дочкам, sticky-шапка)

**Дата:** 2026-09-19
**Тестировщик:** @tester
**Вектор:** Студия товаров (регресс после основного прогона 99a.3; фиксы в `cmd/goods-studio/web/index.html`)
**Студия:** запущена на 127.0.0.1:8799 (394 товара: 263 good + 131 resource, 13 категорий)

## Что менялось (глазами, до прогона)

- **Фикс 1 (клик по дочкам в фокус-подграфе):** `cmd/goods-studio/web/index.html:581-627` —
  `downOnCard` запоминает карточку под mousedown (строка 590); mouseup сбрасывает выбор
  только при `!downOnCard && ev.detail === 1 && !moved` (строка 616); dblclick имеет
  fallback на `downOnCard` (строка 624).
- **Фикс 2 (sticky фильтры):** `cmd/goods-studio/web/index.html:113` —
  `.sprav-head{position:sticky;top:0;background:#181818;z-index:2}`.

## Прогнал

- `node tools/e2e/qa99a3-fix-smoke.js` — 5 шагов, 0 FAIL (F1–F3, F5)
- `node tools/e2e/qa99a3-sticky-check.js` — 5 шагов, 0 FAIL (F4, F5)
- `node tools/e2e/goods-studio-check.js` — 21 шаг, 0 FAIL, 0 SKIP (F6, регресс)

DoD (`go build/vet/test`) не гонял: фиксы чисто фронтовые (index.html), Go-код не затронут.

## Сценарии (жёсткий чек-лист, каждый пункт — один раз)

| # | Что проверено | Результат |
|---|---|---|
| F1 | Выбор h_0001 (корень дерева, 3 детей) → клик по дочке h_0002 на канвасе: подграф перестроился вокруг h_0002 (subset: res:plutonij, h_0001, h_0002, h_0004, h_0005), оверлей «Кликните товар в справочнике» НЕ появился, выбор не сброшен (selected=h_0002) | ✅ ПРОЙДЕН |
| F2 | Клик по пустому месту канваса (точка вне карточек/слотов): выбор сброшен (selected=null), оверлей «Кликните товар в справочнике» появился | ✅ ПРОЙДЕН |
| F3 | Двойной клик по дочке h_0002: выбор = h_0002, попап-деталь открыт (popupGoodId=h_0002) | ✅ ПРОЙДЕН |
| F4 | Прокрутка панели справочника в самый низ (scrollTop=22319): шапка `.sprav-head` видима целиком (position:sticky, top:0, z-index:2, фон непрозрачный), список ушёл под шапку; поиск `#spravSearch` в пределах панели (кликабелен); фильтр T0 при прокрученной панели работает (131 карточка, все — ресурсы) | ✅ ПРОЙДЕН |
| F5 | JS-ошибок в консоли — 0 (все три прогона: fix-smoke шаг 5, sticky-check шаг 5, goods-studio-check шаг 15) | ✅ ПРОЙДЕН |
| F6 | Регресс `goods-studio-check.js` (переписан под 99a.3): попап ⓘ без смены выбора (шаг 7), Esc закрывает с сохранением выбора (шаг 8), dblclick справочника = выбор+попап (шаг 9), тир-поле (ввод 7 → override + «⚠ отличается от вычисленного (0)», очистка → сброс, шаги 10a/10b), подгрузка списка (тост «Создано 1, пропущено 1, ошибок 1» + строки 3/4, textarea очищена, шаги 11/11b), drag&drop справочник→слот попапа (шаг 12), фокус-подграф: клик по родителю на канвасе = перестроение без попапа (шаги 13a/13b), «всё дерево» (шаг 14), оверлеи загрузки/пустоты (шаги 1–3) | ✅ 21/21 PASS |

## Вердикт

**ПРОЙДЕНО.** Оба фикса работают, регресса нет.

## Замечания

- Фикс 1 подтверждён и на «живом» сценарии (клик по родителю на канвасе, goods-studio-check шаг 13b: selected=g3, попап не открыт) — ветка сброса не задевает перестроение подграфа.
- Sticky-шапка: фон `#181818` непрозрачный, карточки уходят под неё без просвечивания (проверено визуально по скриншоту `tools/e2e/artifacts/goods-studio-99a3.png`).
- QA_-товары, созданные прогоном, удалены (cleanup PASS, 3 шт.).

## Чеклист непроверяемого

- **Не проверено:** поведение при очень длинном списке (>1000 карточек) — в текущем реестре 394 товара, прокрутка 22k px уже покрывает «длинный список»; экстремальные размеры недостижимы штатно.
- **Не проверено:** dblclick с реальным движением мыши между кликами (в тесте — программный `mouse.dblclick` без движения). Ветка `downOnCard` fallback покрывает случай «карточка уехала после перестройки»; движение >4px между кликами двойного — редкий ручной сценарий, требует живого браузера создателя.