# QA-прогон: Переходы админка ↔ карта (2026-09-19)

**Дата:** 2026-09-19
**Задача:** идея `docs/gamedesign/ideas/_archive/2026-09-19_админка-карта-переходы.md` (пожелание создателя 2026-09-19)
**Тестировщик:** @tester
**Окружение:** dev-сервер `http://localhost:8080` (health 200), Chrome (headless, playwright-core), Windows/PowerShell 5.1
**Учётные записи:**
- админ: `qa_nav_admin_235248` / `qa_pass_123` (создана через `/register`, роль `admin` повышена SQL на dev-БД)
- player: `e2e_nav_<ts>` (свежая регистрация на каждый прогон)

## Что проверялось

Правки менеджера (5 файлов, только клиент): `web/admin.html`, `web/static/js/admin/main.js`,
`web/map.html`, `web/static/js/map/data.js`, `web/static/js/main.js` — переходы
админка ↔ карта с копированием JWT между ключами localStorage (`adminToken` ↔ `token`).

## Чек-лист прогона

| # | Кейс | Результат | Улика |
|---|------|-----------|-------|
| 1 | Админка → карта: кнопка «🗺️ На карту» в шапке `/admin`; клик → `/map` без повторного логина (adminToken скопирован в token) | ✅ | `1a admin header` PASS (overlay=none, кнопка есть, token=false до клика); `1b admin->map` PASS (path=/map, tokenCopied=true, canvas=true, toastError=false) |
| 2 | Карта → админка (админ-роль): кнопка «⚙️ Админка» видна; клик → `/admin` без повторного логина (token скопирован) | ✅ | `2a admin btn visible` PASS (display=inline-block, visible=true); `2b map->admin` PASS (path=/admin, overlay=none, adminTokenCopied=true, toastError=false) |
| 3 | Карта → админка (роль player): кнопка «⚙️ Админка» скрыта (в DOM есть, display:none) | ✅ | `3 player btn hidden` PASS (exists=true, display=none, visible=false) |
| 4 | Соседние кнопки шапки карты: «🏠 Дашборд» → `/`, «📋 Контракты» → `/assignments` | ✅ | `4a dashboard btn` PASS (path=/); `4b contracts btn` PASS (path=/assignments) |
| 5 | 0 JS-ошибок (pageerror), нет тост-ошибок, нет разлогина/редиректа на `/login-page` на всех шагах | ✅ | `5 zero pageerrors` PASS (adminSession=0, playerSession=0); toastError=false на каждом шаге; редиректов на /login-page не было |
| 6 | e2e-смоук карты `tools/e2e/map-check.js` — ALL PASS | ✅ | 5/5 PASS: register/login, map loads (filter=200, canvasPx=5205, pageErrors=0), click star (modal=true), restricted card (dist=6988 > radar=800), screenshots |

## Дополнительно проверено

- **Синтаксис JS** (B-MAP-6): `node --check` по `web/static/js/admin/main.js`, `web/static/js/main.js`, `web/static/js/map/data.js`, `web/static/js/admin/auth.js` — без ошибок.
- **JWT админа валиден для `/me`** (curl): `role=admin` — карта не чистит токен при переходе (иначе был бы разлогин).
- **Порядок инициализации**: listener `#adminNavBtn` вешается в `init()` на верхнем уровне модуля (`web/static/js/main.js:236`) — работает независимо от момента показа кнопки в `loadUserData()`.
- **Скриншоты** (улики, `tools/e2e/artifacts/`): `nav-admin-header.png` (шапка админки с кнопкой «На карту»), `nav-map-admin.png` (карта с видимой кнопкой «Админка»), `nav-map-player.png` (карта игрока без кнопки), `map.png`, `restricted-card.png`. Визуально не просмотрены (модель не читает изображения) — проверка видимости программная (`display`/`offsetParent`/`textContent`).

## Найденное

Багов не найдено. Замечания (не баги):

1. **Первый прогон дал ложный FAIL** — баг тестового скрипта, не реализации: `addInitScript` с `localStorage.clear()` выполняется на каждой навигации и стирал скопированный токен. Исправлено (сид токена только при отсутствии), повторный прогон — ALL PASS.
2. **Поведение «админка без токена»** (идея §Поведение): оверлей логина (`inset:0, z-index:9999`) перекрывает кнопку «На карту» — нажать нельзя, это норма, не проверялось отдельно (задокументировано в идее).

## Вердикт

🟢 **ПРОЙДЕНО** — все 6 пунктов чек-листа ✅, багов не найдено.

## Предложения в чек-лист

- Новый кейс в зону «карта/фронт» (B-MAP-15): переходы админка ↔ карта — кнопка «На карту» в `/admin` (копирует adminToken→token), кнопка «Админка» на `/map` видна только admin/skycomposer (скрыта у player), переходы без повторного логина, 0 pageerror. Прогон: браузерный скрипт по паттерну `map-check.js` (временный `nav-check.js`, удалён после прогона; при необходимости восстановить из журнала).

## Повторный прогон: клик по 🌍 (2026-09-20)

**Причина:** уточнение создателя — отдельная кнопка «🗺️ На карту» убрана, переход
на карту теперь по клику на планету 🌍 в шапке админки (`web/admin.html:17`,
`<span onclick="goToMap()">` внутри `<h1>`; `goToMap()` в `web/static/js/admin/main.js:80-85`
не менялась: adminToken → token, редирект `/map`). Остальное (кнопка «⚙️ Админка»
на карте, скрытие у player) — без изменений, прогнано 6/6 ранее.

**Окружение:** dev-сервер `http://localhost:8080` (health 200), Chrome headless
(playwright-core), учётка `qa_nav_admin_235248` (role=admin, вход через `POST /login`).

| # | Кейс | Результат | Улика |
|---|------|-----------|-------|
| 1 | `/admin` под админом: планета 🌍 в шапке кликабельна (cursor:pointer), кнопки «На карту» нет, оверлей логина скрыт | ✅ | `1/2 admin header planet` PASS — span text=🌍, cursor=pointer, title=«Открыть карту миров под текущей учёткой», mapBtn=[] (ни одного элемента с текстом «На карту»), overlay=false |
| 2 | Клик по 🌍 → `/map` без повторного логина (token скопирован из adminToken), 0 pageerror, 0 тост-ошибок, 0 редиректов на /login-page | ✅ | `2/2 click planet -> map` PASS — path=/map, tokenCopied=true, canvas=true, toastError=null, pageErrors=0, loginRedirects=0 |

**Дополнительно:** `node --check` по `web/static/js/admin/main.js`, `web/static/js/main.js`,
`web/static/js/map/data.js` — SYNTAX OK. Скриншот `tools/e2e/artifacts/nav-admin-planet-map.png`
(карта после клика по 🌍; визуально не просмотрен — проверка программная).
Временный скрипт `nav-admin-map-check.js` удалён после прогона (паттерн — `map-check.js`).

**Вердикт повторного прогона:** 🟢 **ПРОЙДЕНО** — 2/2, багов не найдено.
Замечание (не баг): `goToMap()` при пустом `adminToken` всё равно редиректит на
`/map` (карта покажет логин) — но в админке без токена оверлей логина перекрывает
иконку (inset:0, z-index:9999), кликнуть нельзя — норма, задокументировано в идее.