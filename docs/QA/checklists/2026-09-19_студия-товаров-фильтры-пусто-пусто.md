# QA-прогон: Студия товаров — ФИНАЛЬНАЯ проверка семантики фильтров справочника

**Дата:** 2026-09-19
**Тестировщик:** @tester
**Вектор:** Студия товаров — финальная семантика фильтров справочника (создатель 2026-09-19):
**(используемые OR неиспользуемые OR забаненные) AND (тиры)**. Показ ТОЛЬКО парой «тир + галка»;
галки — объединение-расширение (OR), тиры — AND; категория и поиск только сужают (AND), показ не включают;
пустой результат при активных фильтрах → «ничего не найдено».
**Студия:** 127.0.0.1:8799 (бинарь `zorion-goods-studio.exe` собран 21:37:13, старт 21:37:14 — после правки index.html 21:37:01).
**Данные:** 394 товара (263 good + 131 resource), 13 категорий, тиры 0–8 (0=131, 1=115, 2=58, 3=29, 4=32, 5=15, 6=8, 7=4, 8=2);
used=331, unused=63, banned=0 (забаненных в данных НЕТ — создан тестовый QA-товар через API и забанен).

## Что менялось (глазами, до прогона)

- `cmd/goods-studio/web/index.html` `renderSprav` (1107–1157): `hasTier = tierSel.size > 0` (1120),
  `hasFlag = fUsed || fUnused || fBanned` (1121); `if (!hasTier || !hasFlag) return false` (1135) — тир + галка обязательны;
  галки — РАСШИРЕНИЕ (OR): `isBanned` → только при fBanned (1142), `isUsed` → только при fUsed (1143),
  иначе (неиспользуемые) → только при fUnused (1144); тиры — AND (1136); категория (1145) и поиск (1146) — AND, сужают;
  подсказка различает «ничего не выбрано — выберите тир и хотя бы одну галку» (нет тира/галки) и «ничего не найдено»
  (тиры+галки есть, пересечение пусто) (1155).
- `tools/e2e/goods-studio-check.js`: шаги 5a/5b под новую семантику (дефолт пуст, показ по «тир+галка»),
  шаг 12 через галки fUsed/fUnused вместо fT0, шаги 12/13b переписаны устойчиво (программный drag, центрирование перед кликом).
- Go-код не менялся.

## Прогнал

- `node tools/e2e/goods-studio-check.js` — 22 шага, **0 FAIL**, 0 SKIP — **ДВАЖДЫ** (стабильность 12/13b)
- Диагностика `tools/e2e/diag-sprav-final.js` (playwright-core, fresh context без localStorage, сброс фильтров между пунктами) — 20 проверок (пункты 2–13 чек-листа), **0 FAIL**; скрипт удалён после прогона
- DoD: `go build ./...` OK, `go vet ./...` OK, `go test ./...` OK (все пакеты зелёные)

## Сценарии (жёсткий чек-лист, каждый пункт — один раз)

| # | Что проверено | Результат |
|---|---|---|
| 1 | e2e-смоук `goods-studio-check.js` — 22 шага, 0 FAIL, два прогона подряд | ✅ ПРОЙДЕН |
| 2 | Старт (fresh context, без localStorage): 0 карточек, «ничего не выбрано — выберите тир и хотя бы одну галку», «Справочник · 0» | ✅ ПРОЙДЕН |
| 3 | Тир «все» (`toggleAllTiers()`) без галок: 0 карточек, «ничего не выбрано» — тир есть, галки нет → пусто | ✅ ПРОЙДЕН |
| 4 | Галка «используемые» без тиров (`tierSel` пуст): 0 карточек, «ничего не выбрано» — галка есть, тира нет → пусто | ✅ ПРОЙДЕН |
| 5 | Тир «все» + «используемые»: 331 карточка, каждая реально используется (сверка used[g.id] по state: для каждого id списка существует p с p.recipe[].good_id === id), забаненных 0, счётчик «· 331» | ✅ ПРОЙДЕН |
| 6 | Тир «все» + «неиспользуемые»: 63 карточки, ни одна не используется ни в одном рецепте, забаненных 0 | ✅ ПРОЙДЕН |
| 7 | Тир «все» + обе галки: **ОБЪЕДИНЕНИЕ** — 394 карточки = все не-забаненные товары (used ∪ unused; missing=0, extra=0). Ключевая проверка OR | ✅ ПРОЙДЕН |
| 8 | Забаненный товар (ключевая проверка): создан QA_Бан (тир 0), забанен через POST /api/goods/{id}/status. Тирами «все»: только «используемые» → QA НЕ в списке (331); только «неиспользуемые» → QA НЕ в списке (63); только «забаненные» → QA В списке, незабаненных 0 (1 карточка); «забаненные»+«неиспользуемые» → QA + неиспользуемые (64 = 1+63, missing=0, extra=0) | ✅ ПРОЙДЕН |
| 9 | Категория (c3, 79 товаров) без галок + тир «все»: 0 карточек, «ничего не выбрано» — категория показ НЕ включает | ✅ ПРОЙДЕН |
| 10 | Категория (c3) + тир «все» + «используемые»: 79 карточек, все категории c3 И используемые (пересечение) | ✅ ПРОЙДЕН |
| 11 | Поиск («вода») без тира/галки: 0 карточек, «ничего не выбрано»; поиск + тир «все» + «неиспользуемые»: 1 карточка, совпадает по имени И неиспользуемая | ✅ ПРОЙДЕН |
| 12 | Пилюли: «3» → 29 карточек, все тир 3; «0» → 70 карточек, все тир 0; «0»+«3» → 99 карточек, оба тира; «все»+«неиспользуемые» → 63 карточки | ✅ ПРОЙДЕН |
| 13 | JS-ошибки за весь прогон (e2e ×2 + диагностика): 0 pageerror | ✅ ПРОЙДЕН |
| 14 | DoD: `go build ./...` + `go vet ./...` + `go test ./...` — зелёные | ✅ ПРОЙДЕН |

## Вердикт

**ПРОЙДЕНО** (14/14 пунктов; e2e-смоук 0 FAIL ×2 прогона, диагностика 0 FAIL).

## Найденное

- Багов не найдено. Семантика «(используемые OR неиспользуемые OR забаненные) AND (тиры)» работает точно по формуле создателя:
  галки объединяются (п.7: 394 = все не-banned; п.8d: QA + неиспользуемые), тиры сужают (п.12), категория/поиск показ не включают (п.9/11).
- Замечание (не баг, артефакт теста): `categories[0]` в state — категория c1 с **0 товаров** (категории в state идут не по наполненности;
  товары распределены по c3=79, c6=63, c5=51, mineral=43, gas=31...). Первый прогон диагностики выбрал c1 → пункт 10 дал 0 карточек (ложный FAIL).
  После выбора категории с максимумом товаров (c3) — зелёный. Продукт не виноват: фильтр по пустой категории честно даёт пусто.
- Прежний журнал этого файла описывал СТАРУЮ AND-семантику («обе галки = пусто „ничего не найдено“ — ожидаемо») — устарел,
  переписан под финальную формулу создателя (OR-объединение галок).

## Чеклист непроверяемого

- **Не проверено:** тир-0 товары (kind=good, tier=0) — в данных их нет (тир 0 = 131 ресурс); проверка «ресурсы + тир-0 товары» покрыта только ресурсами.
- **Не проверено:** визуально-UX (внешний вид подсказок, «приятность» пустого состояния) — headless-браузер; нужен живой браузер создателя.
- **Не проверено:** поведение при нескольких забаненных товарах в разных тирах (в данных 0 banned; тест создавал 1 QA-товар) — покрыто логикой 8c/8d, массовый сценарий недостижим штатно.

## Предложение в чек-лист

- Зафиксировать как регрессионный кейс (финальная семантика): «тир не выбран ИЛИ галка не выбрана → список пуст всегда, даже при активных категории/поиске»;
  «галки используемые+неиспользуемые = ВСЕ не-забаненные (объединение, OR)»; «забаненные — отдельная группа, смешивается с неиспользуемыми/используемыми только при включённых галках»;
  «категория и поиск показ не включают, только сужают». Применит @manager.