# QA-прогон: кнопка пакмана шлёт nearest (идея «пакман снова носит по галактике»)

- **Дата:** 2026-09-20
- **Фича:** фикс бага «кнопка Запустить Пакмана всегда слала `trajectory: 'spiral'`» — тело запроса теперь `{worlds_per_second, batch_size: 200, trajectory: 'nearest'}` (+ поправлен комментарий)
- **Файлы фикса:** `web/static/js/admin/generation.js` (строка ~396; только клиент админки, JS статический — рестарт сервера не нужен)
- **Прогонщик:** @tester
- **Окружение:** Windows, сервер `localhost:8080` (живой, `/health` → OK), БД: миры ~29.9 тыс. на старте, админ-токен из `tools/e2e/pacman-shot.js` (ADMIN_TOKEN, роль admin)
- **Инструмент:** `node --check`, grep по `generation.js`, `Invoke-RestMethod` (POST `/admin/pacman/start`, POST `/admin/generate-cancel?job=pacman`), `node tools/e2e/ws-listen.js` (LISTEN_MS=15000, сам регистрирует игрока)

## Прогнал

- `node --check web/static/js/admin/generation.js` — синтаксис OK
- grep `trajectory|spiral|nearest` по `web/static/js/admin/generation.js` — 2 совпадения: комментарий `:385` (nearest) и тело запроса `:396` (`trajectory: 'nearest'`); `spiral` в файле отсутствует
- Live-прогон: POST `/admin/pacman/start` с телом `{"worlds_per_second":1,"batch_size":200,"trajectory":"nearest"}` → `{"status":"started"}`; WS-наблюдение 15 с (`ws-listen.js`); POST `/admin/generate-cancel?job=pacman` → `{"status":"canceled"}`

## Таблица результатов

| Пункт | Результат | Доказательство |
|---|---|---|
| P1. Синтаксис JS | **PASS** | `node --check web/static/js/admin/generation.js` — без ошибок |
| P2. Кодовая проверка кнопки | **PASS** | `generation.js:396` — `body: JSON.stringify({ worlds_per_second: worldsPerSecond, batch_size: 200, trajectory: 'nearest' })`; grep по файлу: `spiral` не встречается нигде (только комментарий `:385` про nearest) |
| P3. Live-прогон API | **PASS (360.7 единиц)** | 15 последовательных позиций `pacman_position` (seq 4–18), все шаги < 1500; макс. шаг 360.7 (seq15→seq16). Пакман остановлен сразу после наблюдения (`canceled`). |
| P4. Другие места с `trajectory` | **PASS** | grep `trajectory` по `generation.js` — единственное место отправки: `:396` (комментарий `:385` — текст, не запрос) |

### P3 — позиции и шаги (WS-фреймы `pacman_position`, скорость 1 → батч 1 → позиция = координата съедаемой звезды)

| seq | x | y | шаг от предыдущей |
|---|---|---|---|
| 4 | 610.17 | 193.12 | — |
| 5 | 768.45 | 115.34 | 176.4 |
| 6 | 992.79 | 262.33 | 268.2 |
| 7 | 1033.33 | 51.88 | 214.3 |
| 8 | 1095.38 | −110.85 | 174.2 |
| 9 | 1292.06 | −97.80 | 197.1 |
| 10 | 1376.43 | 124.75 | 238.0 |
| 11 | 1249.83 | 341.64 | 251.1 |
| 12 | 1092.77 | 490.86 | 216.6 |
| 13 | 769.25 | 425.94 | 330.0 |
| 14 | 482.98 | 449.43 | 287.2 |
| 15 | 297.05 | 262.41 | 263.7 |
| 16 | 98.98 | 563.83 | **360.7 (макс)** |
| 17 | 191.31 | 760.50 | 217.3 |
| 18 | −41.75 | 783.74 | 234.2 |

Все шаги в диапазоне 174–361 единиц — «поедание ползёт по ближайшим», прыжков через галактику (≥5000) нет. Съедено за наблюдение: 18 миров (eaten_total 4→18).

## Найденные проблемы

1. **[среда, не код] Пакман уже был running при старте прогона** (429/29967, остаток от предыдущего джоба — вероятно, запущенного старым кодом со spiral). По AGENTS.md §16 (не запускать поверх чужого джоба) сначала выполнен `generate-cancel?job=pacman` → `canceled`, затем стартован свой с `nearest`. Не баг фикса.

## Не проверено (чеклист непроверяемого)

- **Клик по реальной кнопке в браузере** — проверка сделана эмуляцией запроса (то же тело, что шлёт кнопка, `generation.js:396`); сам клик по UI-кнопке не выполнялся. Код кнопки (`startPacman`) прочитан статически — тело запроса совпадает с эмулированным.
- **Поведение при `worlds_per_second` > 1** (батч > 1, позиция ≠ координата звезды) — ТЗ проверяет только скорость 1; критерий PASS/FAIL задан для неё.
- **Долгосрочный тренд** (пакман на всю галактику 30 тыс. миров) — не проверялся, пакман остановлен через ~18 с как предписано.

## Итоговый вердикт

**ПРОЙДЕНО.** P1–P4 все PASS: синтаксис OK, кнопка шлёт `trajectory: 'nearest'` (без `spiral`), live-прогон подтвердил поедание по ближайшим (макс. шаг 360.7 единиц, все < 1500, прыжков ≥5000 нет), других мест отправки `trajectory` в файле нет. Пакман остановлен после наблюдения, галактика не пострадала (съедено 18 миров).

**Предложение в чек-лист (для @manager):** кейс B-ADM-7 «Кнопка Запустить Пакмана шлёт `trajectory: 'nearest'` (не spiral): старт со скоростью 1 → WS-фреймы `pacman_position` идут последовательными шагами < 1500 единиц (прыжков ≥5000 нет) → остановка через `generate-cancel?job=pacman`» — регрессионный кейс на клиент админки + серверный джоб пакмана.