# Прогон 2: Арт-студия — «Пересобрать промт: все расы → все, а не первая» (сессия 2)

**Дата:** 2026-09-20
**Тестировщик:** @tester (независимый прогон)
**Идея:** docs/gamedesign/ideas/2026-09-20_арт-студия-раса-в-генераторе-эталонов-и-пересборка-промта.md (сессия 2)
**Код:** НЕ закоммичен (working tree), зона cmd/art-studio
**Сервер:** 127.0.0.1:8798 запущен с новой версией (пересобран 20.09.2026) — живой HTTP доступен.

## Прогнал

- `go test -run TestHandleRebuildPromptAllRaces -v ./cmd/art-studio/handlers/` — PASS
- `go test -run TestHandleRebuildPromptSkipped -v ./cmd/art-studio/handlers/` — PASS
- `go test -run TestHandleRebuildPromptAllSkipped -v ./cmd/art-studio/handlers/` — PASS
- `go test -run TestUpdateFamiliesRace -v ./cmd/art-studio/handlers/` — PASS
- `go test -run TestHandleRebuildPrompt -v ./cmd/art-studio/handlers/` — PASS
- `go test ./cmd/art-studio/config/ -run TestLoadFamiliesAppearanceBlocked -v` — PASS
- `go build ./...` — exit 0
- `go vet ./cmd/art-studio/...` — exit 0
- `go test ./cmd/art-studio/...` — **FAIL**: `TestHandleGenRefRace` (race_handlers_test.go:235) «кандидатов 1, want 3»
- Код-проверки: rebuildPromptAll reload после цикла (rebuild_prompt.go:124,128), «нет расы» до записи (:47-50), «нет семейства» (:29-33)

## Сценарии (жёсткий чек-лист)

| Пункт | Результат |
|---|---|
| A1 Массовый режим, все расы | ПРОЙДЕН: TestHandleRebuildPromptAllRaces PASS (ok=true, updated=6 рас 5,6,7,8,9,48, skipped пуст) |
| A2 Раса без лор-файла → skipped | ПРОЙДЕН: TestHandleRebuildPromptSkipped PASS (updated=[5], skipped=5 с причиной «нет лор-файла», ok=true) |
| A3 Все skipped → ok=false | ПРОЙДЕН: TestHandleRebuildPromptAllSkipped PASS (ok=false, updated пуст, skipped=6, error-сводка) |
| A4 reload один раз после цикла | ПРОЙДЕН: rebuildPromptAll — reloadFamilies + runner.ReloadFamilies после цикла в любом случае (rebuild_prompt.go:124,128), внутри цикла reload нет |
| A5 Диск: валидность/comment/чужие семьи | ПРОЙДЕН: TestUpdateFamiliesRace PASS (json.Valid + ParseFamilies перед записью, comment «Конфиг семейств рас» сохранён, раса 6 не тронута, вставка полей в защитном пути) |
| B6 Единичный режим | ПРОЙДЕН: TestHandleRebuildPrompt PASS (ok=true, race_name, appearance/blocked, диск+память+/prompt) |
| B7 Несуществующая раса | ПРОЙДЕН: код — поиск по fam.Races до rebuildRace, error «нет расы … в …» (rebuild_prompt.go:47-50) |
| B8 Несуществующее семейство | ПРОЙДЕН: код — error «нет семейства …» (rebuild_prompt.go:29-33) |
| C9 go build ./... | ПРОЙДЕН: exit 0 |
| C10 go vet ./cmd/art-studio/... | ПРОЙДЕН: exit 0 |
| C11 go test ./cmd/art-studio/... | **ПРОВАЛ**: TestHandleGenRefRace (race_handlers_test.go:235) «кандидатов 1, want 3» — джоб /genref с race=5 дал 1 кандидата вместо 3 |
| C12 go test -race | Н/П: остановлен по критерию остановки (C11 ПРОВАЛ) |
| C13 Кросс-конфиг | ПРОЙДЕН: TestLoadFamiliesAppearanceBlocked PASS |
| D14 GET / | Н/П: остановлен по критерию остановки (C11 ПРОВАЛ) |
| D15 GET /races?fam=F2 | Н/П: остановлен по критерию остановки (C11 ПРОВАЛ) |
| D16 Живой /rebuild-prompt | Н/П: остановлен по критерию остановки (C11 ПРОВАЛ); живые данные не трогал |

## Замечания

1. Расхождение с прогоном 1 (тот же working tree, тот же день): прогон 1 — `go test ./cmd/art-studio/...` все пакеты ok; прогон 2 — TestHandleGenRefRace упал «кандидатов 1, want 3». Похоже на флаки теста (тайминг джоба/фейкового python), но по правилам повторный прогон не делал — нужна проверка разработчика (детерминизм или реальный баг).
2. Живой сервер не трогал: /rebuild-prompt против config/art/families.json не вызывался (запись на диск проверена только через unit-тесты на temp-копиях).

## Не проверено (граница покрытия)

- C12 (-race), D14–D16 (живой сервер) — не дорабатывал: критерий остановки сработал на C11.
- Детерминизм TestHandleGenRefRace — нужен повторный прогон разработчиком.

## Вердикт

**НЕ ПРОЙДЕН.** C11 (go test ./cmd/art-studio/...) упал на TestHandleGenRefRace — критерий остановки. Фича «все расы → все» сама по себе зелёная (A1–A5, B6–B8 PASS), но DoD арт-студии не чистый.