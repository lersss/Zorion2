# Прогон 3 (финальный независимый): Арт-студия — раса в генераторе эталонов + Пересобрать промт (массовый режим)

**Дата:** 2026-09-20
**Тестер:** @tester (независимый финальный прогон, сессия 2)
**Зона:** cmd/art-studio (working tree, НЕ закоммичено)
**Сервер:** 127.0.0.1:8798 (пересобран с правками, запущен менеджером)
**Предыдущий прогон:** флак TestHandleGenRefRace (гонка waitJobDone — ReadStatus проглатывал транзитную ошибку чтения status.json) — заявлен устранённым (фикс: прямое чтение + валидный JSON + Running=false; 40/40 у менеджера).

## Результаты по чек-листу

| # | Пункт | Результат |
|---|---|---|
| A1 | go test -count=20 -run TestHandleGenRefRace (прогон 1) | ПРОЙДЕН — ok, 4.359s |
| A1 | go test -count=20 -run TestHandleGenRefRace (прогон 2) | ПРОЙДЕН — ok, 4.787s |
| A2 | go test -count=3 ./cmd/art-studio/... | ПРОЙДЕН — 3/3, все пакеты ok (art-studio, config, generator, handlers, postproc) |
| B3 | TestHandleRebuildPromptAllRaces | ПРОЙДЕН — PASS (0.20s) |
| B4 | TestHandleRebuildPromptSkipped | ПРОЙДЕН — PASS (0.07s) |
| B5 | TestHandleRebuildPromptAllSkipped | ПРОЙДЕН — PASS (0.03s) |
| B6 | TestUpdateFamiliesRace | ПРОЙДЕН — PASS (0.05s) |
| C7 | TestHandleRebuildPrompt | ПРОЙДЕН — PASS (0.14s) |
| C8 | TestParseAppearanceSection | ПРОЙДЕН — PASS (0.00s) |
| C9 | Валидация validateAppearanceBlocked (по коду) | ПРОЙДЕН — rebuild_prompt.go:184-208: пустой после trim / >200 / \n в appearance, пустой токен / пробел-таб в blocked — все отклоняются до записи |
| C10 | Несуществующая раса/семейство → error до записи (по коду) | ПРОЙДЕН — rebuild_prompt.go:29-33 (нет семейства), 47-50 (нет расы) — ответ error до rebuildRace/updateFamiliesRace |
| D11 | TestHandleGenRefRace | ПРОЙДЕН — PASS (0.19s) |
| D12 | TestBuildPromptWideFormFromRace | ПРОЙДЕН — PASS (0.01s) |
| E13 | go build ./... | ПРОЙДЕН — exit 0 |
| E14 | go vet ./cmd/art-studio/... | ПРОЙДЕН — exit 0 |
| E15 | go test ./cmd/art-studio/... | ПРОЙДЕН — все пакеты ok, exit 0 |
| E16 | go test -race -count=1 ./cmd/art-studio/... | ПРОЙДЕН — все пакеты ok (handlers 4.270s, generator 9.741s), exit 0 |
| E17 | TestLoadFamiliesAppearanceBlocked -v | ПРОЙДЕН — PASS (59 рас) |
| F18 | GET / → 200, HTML: rebuildPrompt + «Пересобрать промт» + raceRefPool | ПРОЙДЕН — 200, все три маркера найдены |
| F19 | GET /races?fam=F2 → 200, JSON 6 рас | ПРОЙДЕН — 200, {"races":[...]} id 5,6,7,8,9,48 |
| F20 | Единичный /rebuild-prompt?fam=F2&race=5 | Н/П — живые данные не трогал; идемпотентность покрыта C7 (TestHandleRebuildPrompt) |

## Команды прогона

```
go test -count=20 -run TestHandleGenRefRace ./cmd/art-studio/handlers/   (×2, оба ok)
go test -count=3 ./cmd/art-studio/...
go test -count=1 -v -run "TestHandleRebuildPromptAllRaces|TestHandleRebuildPromptSkipped|TestHandleRebuildPromptAllSkipped|TestUpdateFamiliesRace" ./cmd/art-studio/handlers/
go test -count=1 -v -run "TestHandleRebuildPrompt$|TestParseAppearanceSection|TestHandleGenRefRace" ./cmd/art-studio/handlers/
go test -count=1 -v -run TestBuildPromptWideFormFromRace ./cmd/art-studio/generator/
go build ./...
go vet ./cmd/art-studio/...
go test ./cmd/art-studio/...
go test -race -count=1 ./cmd/art-studio/...
go test ./cmd/art-studio/config/ -run TestLoadFamiliesAppearanceBlocked -v
Invoke-WebRequest http://127.0.0.1:8798/  (200; rebuildPrompt/«Пересобрать промт»/raceRefPool)
Invoke-WebRequest http://127.0.0.1:8798/races?fam=F2  (200; 6 рас)
```

## Проверено по коду (C9/C10)

- `validateAppearanceBlocked` (cmd/art-studio/handlers/rebuild_prompt.go:184-208): appearance пуст после trim → ошибка; >200 символов → ошибка; содержит \n → ошибка; blocked: пустой токен → ошибка; токен с пробелом/табом → ошибка. Все проверки до записи на диск.
- `handleRebuildPrompt` (rebuild_prompt.go:25-65): нет семейства → error (29-33); нет расы в семействе → error (47-50); обе — до rebuildRace/updateFamiliesRace, диск не тронут.
- Массовый режим `rebuildPromptAll` (103-141): раса без слага/лор-файла/раздела → skipped с причиной (не ошибка операции); reload Server+Runner один раз после цикла; ok=false только при 0 пересобранных (error — сводка причин).
- `updateFamiliesRace` (families_patch.go:19-41): байтовая замена полей, json.Valid + ParseFamilies перед записью, невалидное не пишется.

## Замечания

1. F19: вывод кириллицы в консоли сломан (cp866/cp1251 — известная ловушка PITFALLS.md, среда, не баг); структура и количество рас (6) подтверждены по JSON.
2. F20 не выполнялся против живого сервера: единичный вызов идемпотентен, но покрыт юнит-тестом C7; живые данные config/art/families.json не изменялись.
3. Массовый /rebuild-prompt (без race) против живого сервера НЕ вызывался (запрет задания — переписал бы appearance/blocked всех рас F2).

## Вердикт

ПРОЙДЕН. Флак TestHandleGenRefRace устранён (20/20 ×2, -race ok). DoD зелёный. Готово к приёмке создателем.