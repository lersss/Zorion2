# QA-прогон: студия товаров — autocomplete="off" в полях добавления имён

**Дата:** 2026-09-21
**Что проверялось:** мелкая правка `web/studio.html` — добавлен `autocomplete="off"`
в 5 полей ввода имени (prodNewName, spravNewName, spravNewResName, itemNewName,
catNewName), чтобы Chrome не предлагал «Сохранить имя пользователя» (ассоциация
с формой логина на странице).
**Окружение:** сервер `cmd/server` на 127.0.0.1:8080 (живой, `/health` 200),
учётка skycomposer (`skycomposer`/`qa77a_admin_pass`), Chrome headless
(playwright-core).
**Проверял:** @tester (deepseek-v4-flash)

---

## Таблица «пункт → PASS/FAIL → примечание»

| # | Пункт | Результат | Примечание |
|---|---|---|---|
| 1 | E2E-смоук студии | **PASS** | `goods-studio-server-check.js`: **14/14 PASS, 0 FAIL**. Логин skycomposer, preflight auth (200/401), +ресурс, кириллица-ренейм + дубликат 409, OR-фильтры, бан/разбан, удаление, тир-оверрайд, «добавить родителя», JS-ошибок нет, fill 202 → «Ошибка ИИ» (opencode на 3456 недоступен — детерминированная negative-ветка), cleanup 3 QA_-товара. |
| 2 | Поля с autocomplete | **PASS** | Все 5 полей получили `autocomplete="off"`: `spravNewName` (studio.html:211), `spravNewResName` (:216), `prodNewName` (:227), `itemNewName` (:234), `catNewName` (:264). Других «полей добавления имени» на странице нет: остальные text-инпуты — поиск (`search` :179, `spravSearch` :202), ренейм с предзаполненным value (`renameInput` ×3, `catRenameInput`), поле семейства рас (`prodRaceFamily`), логин-форма (`studioLoginUsername` :286 — ей autocomplete нужен по назначению). |
| 3 | Разметка addrow | **PASS** | Блоки целы: `goodsAddRow` (:210–214), `resAddRow` (:215–219), `prodAddRow` (:226–231), `itemAddRow` (:233–237), `cat-add` (:263–266) — input+select+button на месте, id не тронуты. Обработчики кнопок живы: `catAddBtn`→addCategory (:1991), `btnSpravAdd`→spravAddGood (:1998), `btnSpravAddRes`→spravAddResource (:1999), `btnProdAdd`→prodAdd (:2007), `btnItemAdd`→itemAdd (:2008). |
| 4 | Форма «+ тип» на /studio | **PASS** | Браузер (Chrome headless): /studio с adminToken → клик `#branchProducers` → `prodAddRow` виден (display=flex), `prodNewName` с `autocomplete="off"` на месте, кнопка `+ тип` на месте, заголовок «Производители · 9», ввод текста в поле работает (`QA_ТипПроверка`), JS-ошибок нет. Скриншот: `tools/e2e/artifacts/studio-producers-branch.png`. |

---

## Найденные баги

**Не найдено.** Правка соответствует заявленному объёму (5 полей, только
атрибут), соседняя разметка и обработчики не задеты.

Замечания (не баги):
1. **Ренейм-инпуты** (`renameInput` :1068/:1477/:1611, `catRenameInput` :1782)
   autocomplete не получили — но они предзаполнены существующим именем
   (`value="..."`), Chrome не предлагает «Сохранить имя пользователя» на
   предзаполненных полях. Если на практике всплывёт — отдельная правка.
2. **Поле семейства рас** (`prodRaceFamily` :1447) — тоже предзаполнено/опционально,
   вне скоупа жалобы.

---

## Чеклист непроверяемого

| Что не проверено | Почему | Что нужно для закрытия |
|---|---|---|
| Само исчезновение предложения «Сохранить имя пользователя» в живом Chrome | Headless-режим не показывает нативные autofill-предложения браузера; проверена только статика (атрибут на месте) | Создатель: открыть /studio в обычном Chrome, ветка «Производители», клик в «имя типа» — предложения быть не должно |
| Ветки «Товары»/«Предметы»/попап «Категории» браузерно | E2E-смоук покрывает ресурсную ветку (шаг 2 — `#spravNewResName`), ветка «Производители» проверена отдельным скриптом; товар/предмет/категория — идентичная разметка (input+autocomplete), статика подтверждена | При желании — ручной клик по всем 5 полям |

---

## Вердикт: **ГОТОВО**

Правка минимальна и корректна: 5 полей получили `autocomplete="off"`, разметка
addrow-блоков и обработчики не задеты, e2e-смоук студии 14/14 PASS, ветка
«Производители» рендерится и работает. DoD (Go) не гонял — правка чисто
фронтовая (HTML-атрибуты), Go-код не затронут.

**Предложение в чек-лист (для @manager):** кейс «поля добавления имён в студии
имеют autocomplete="off" (Chrome не предлагает сохранить логин)» — как
регрессионный UI-кейс зоны B-ST (проверка статикой + браузером при правках
studio.html). Сейчас в QA_CHECKLIST такого пункта нет.