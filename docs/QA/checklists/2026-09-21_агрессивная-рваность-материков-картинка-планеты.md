# 2026-09-21 — Агрессивная рваность материков на картинке планеты (прогон QA)

**Дата:** 2026-09-21
**Тестировщик:** @tester (независимый прогон)
**Идея:** `docs/gamedesign/ideas/2026-09-21_агрессивная-рваность-материков-картинка-планеты.md`
**Сервер:** http://127.0.0.1:8080 (уже запущен, /health OK)
**Вердикт:** ПРОЙДЕНО (с оговоркой по визуальной оценке — см. «Непроверяемое»)

---

## Прогнал

| Команда | Результат |
|---|---|
| `go build ./...` | OK |
| `go vet ./...` | OK |
| `go test -race -count=1 ./internal/generator/planet/...` | ok (189.9s, свежий прогон) |
| `go test -race -count=1 ./internal/handlers/...` | ok (18.2s; диск-кэш картинок в этом пакете) |
| `tools/gen_planet_previews.ps1` | 16/16 OK, PNG в `C:\Users\admin\AppData\Local\Temp\opencode\previews\` |
| Детерминизм: 2 запроса Virno big | SHA256 идентичны, 2-й запрос 71ms |
| Диск-кэш `data/planet_images` | 32 файла, все с префиксом `v4_`, v3 нет |

Примечание: `C:\WinLibs\mingw64\bin` не существует; gcc найден через WinGet:
`C:\Users\admin\AppData\Local\Microsoft\WinGet\Packages\BrechtSanders.WinLibs.POSIX.UCRT_Microsoft.Winget.Source_8wekyb3d8bbwe\mingw64\bin` (gcc 16.1.0).

## Сценарии (по чек-листу задачи)

### 1. DoD — ПРОЙДЕНО
build/vet/test -race зелёные (свежий прогон `-count=1`, не кэш).

### 2. Визуальная рваность — ПРОЙДЕНО (количественно; глазами — см. «Непроверяемое»)
- **Renyenmus в БД НЕТ** (запрос по имени пуст) → эталоном взята **Virno** (землеподобная, id `6d1c8b43-3a32-414b-8758-19e027a57945`), плюс Odvilif (ледяная) и Sununjar (океаническая).
- Режим full подтверждён: `resolveMode` (planet_image_handler.go:143-144) даёт `ImageModeFull` для admin-JWT; скрипт запрашивал `size=big` без mode → full.
- Модель тестировщика не умеет читать изображения → визуальную оценку заменил количественной: воспроизвёл метрику `boundaryRaggedness` из `planet_image_v3_test.go` (граничные пиксели биома → центроид → (max−min)/mean) на реальных PNG 512px, классификация пикселя по ближайшему цвету категории (`categoryBase`):

| Планета | вода | литосфера | биосфера | крио | вулканизм | экзотика |
|---|---|---|---|---|---|---|
| virno_earth | 1.518 | 1.597 | 1.257 | — | 1.224 | 1.234 |
| odvilif_ice | 1.555 | 1.208 | 0.556 | 1.555 | 0.838 | 2.232 |
| sununjar_ocean | 1.847 | 1.629 | 1.282 | 1.180 | 1.180 | 1.183 |

- Все границы >> порога теста 0.35 и >> «до» (~0.25–0.3 по комментарию разработчика в тесте). Абсолютные значения завышены лимбом диска и атмосферой (классификация по цвету), но направление однозначно: круги и прямые стыки не читаются.
- Авторитетная метрика без атмосферы — `TestSurfaceNoCircles` (порог поднят 0.2→0.35, зелёный в DoD).

### 3. Детерминизм — ПРОЙДЕНО
Virno big дважды: SHA256 `89D0D45FC3166B5F1EDEFEDB3ED557C32E71AAB87AB73CCC17F4D02539691240` == `89D0D45F...` (идентичны). Плюс `TestSurfaceDeterministic` (3 режима) зелёный.

### 4. Кэш v4 — ПРОЙДЕНО
- `ImageGenVersion = "v4"` — `internal/generator/planet/planet_image_v2.go:44`.
- Версия входит в in-memory кэш-ключ (planet_image_v2.go:92) и в префикс диск-кэша (planet_image_disk_cache.go:65).
- Стартовый клин удаляет файлы не с текущим префиксом (planet_image_disk_cache.go:45,54-56) — в `data/planet_images` 32 файла, все `v4_`, v3 не осталось.
- Повторный запрос 71ms — из кэша.
- `TestPlanetImageVersionKey` (v4 в ключе) зелёный.

### 5. Красных тестов вне зоны — НЕ НАЙДЕНО
Полный `go test ./...` не гонял (в дереве параллельная студийная задача). Прогнал `internal/handlers` (диск-кэш картинок — в зоне влияния версии): ok. Студийные файлы не трогал.

## Найденное

Багов не найдено. Замечания (не баги):
- `C:\WinLibs\mingw64\bin` из инструкции не существует — gcc живёт в WinGet-каталоге; путь в отчёте выше (для будущих прогонов).
- Дифф `internal/handlers/planet_handler.go` (2 строки) — студийная правка сигнатуры `GetByIDWithPosition`, к задаче не относится, не влияет на картинку.

## Непроверяемое (граница покрытия)

- **Визуальная оценка PNG глазами** — модель тестировщика не принимает изображения. Заменено количественной метрикой рваности (см. п.2). **Нужно создателю:** открыть `C:\Users\admin\AppData\Local\Temp\opencode\previews\virno_earth.png` (эталон), `odvilif_ice.png`, `sununjar_ocean.png` и подтвердить «круги/прямые не читаются» — метрика это поддерживает, но финальное слово за глазом.
- **Эталон Renyenmus** — отсутствует в БД; проверено на Virno (землеподобная). Если Renyenmus появится — прогнать метрику на ней.
- **UX/вкус** («агрессивно» — достаточно ли агрессивно для создателя) — вкусовое решение, вне компетенции тестировщика.

## Предложения в чек-лист (для @manager)

- В `docs/QA_CHECKLIST.md` (зона planet_image): кейс «рваность границ на реальном PNG» — метрика (max−min)/mean по граничным пикселям биома > 0.35 на 512px full-картинке (скрипт `raggedness_check.py` в temp — можно перенести в `tools/`).
- Обновить путь gcc в инструкции DoD (WinLibs → WinGet-каталог).