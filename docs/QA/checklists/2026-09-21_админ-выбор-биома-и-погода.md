# QA-прогон: админский выбор биома + принудительная погода (идея 2026-09-21)

**Дата:** 2026-09-21 · **Роль:** @tester · **Статус:** рабочая копия, НЕ закоммичено
**Сервер:** `zorion-server-dev` на :8080 (PID 15348), `/health` OK
**Планета прогонов:** world `59795dac-e22c-4520-b7b0-e0e7960f285e` / planet `ee2d7564-ca17-4327-b0b6-e688a4122550` (Tenjin, 15 биомов); для п.3 — `62692dad-2dda-4e40-9437-9fa0c7930f7a` (25 биомов)

## Прогнал

```
git status / git diff -- <файлы задачи>
go build ./... ; go vet ./...                         → 0 / 0
go test ./internal/handlers/ -run TestSurfaceLand -v   → 13 PASS
go test ./internal/handlers/ -run TestPickWalkBiome    → PASS
node tools/e2e/surface-admin-biome-check.js            → 13 PASS / 0 FAIL
node tools/e2e/surface-walk-check.js                   → 14 PASS / 0 FAIL
node artifacts/api-probe.mjs                           → 15 PASS / 0 FAIL (API land)
node artifacts/probe-overflow.mjs                      → 3 PASS / 0 FAIL (меню vs окно)
node artifacts/probe-weather.mjs                       → 9 PASS / 0 FAIL (погода)
node artifacts/probe-admin-flow.mjs                    → сеть/консоль/релогин чистые (только favicon 404)
```

## Результаты (жёсткий чек-лист)

1. **PASS** — админ-меню: `🚶 Высадиться ⚙ выбор биома ▸`, чип `⚙ админ`, `Случайно — как у игрока` + 15 биомов с одной цифрой после запятой и по убыванию доли (пески пустыни 14.9 % … вулканические поля 1.0 %).
2. **PASS** — клик по `пески пустыни 14.9 %` → URL `biome=пески_пустыни`, брифинг «Пески пустыни литосфера · 14.9 %» (тот же биом, та же доля).
3. **PASS** — планета с 25 биомами, окна 1280x800 / 1280x480 / 900x700: rect меню целиком в окне (right/bottom ≤ inner), список `overflow-y:auto` 240/830 px.
4. **PASS** — player: меню `🚶 Высадиться биом ?` (нет «выбор биома»), `#hud-weather-admin` отсутствует.
5. **PASS** — API: player+biome → 400 «Выбор биома доступен только администратору»; admin+чужой биом (`кремниевые_рощи`) → 400 «Нет такого биома на планете»; admin+`вулканические_поля` → 200, позиция в БД с этим биомом; пустой/отсутствующий biome → 200, жребий из биомов планеты.
6. **PASS** — повторный land с `пески_пустыни` на поверхности с `вулканические_поля` → 200 и сохранённый `вулканические_поля`; в коде шаг 2 (идемпотентность) отдаёт сохранённый биом до валидации присланного (surface_handlers.go:507-522, :566-575).
7. **PASS** — 7 чипов (`авто` + 6 явлений); «туман» поднял среднюю яркость 26.03→33.50, «метель» светлых пикселей 185→304; HUD `метель · вручную`, держится ≥30 с; `авто` → `пыльная буря · авто`; `полярное сияние · вручную` тоже работает.
8. **PASS** — `surface-walk-check.js` (хардкод UUID убран, планета резолвится SQL: `59795dac…/ee2d7564…`) 14/14; `surface-admin-biome-check.js` 13/13.
9. **PASS** — JS-ошибок нет (pageerror=0), тостов-ошибок нет, релогина нет; единственные 4xx — `favicon.ico` (2× 404).
10. **PASS** — файлы фикса «невидимые стены» (`surface_config.js`/`surface_world.js`/`surface_render.js`/`surface_player.js`) в дифе содержат только `FLOAT_SPAN`/`FLOAT_GAP`/парящую породу, ни одного изменения по weather/biome/role; обычная высадка случайна: `pickWalkBiome` без изменений, `TestPickWalkBiomeDistribution` PASS, 12 проб дали 8 разных биомов.

## Найденное

- **Блокеров и воспроизводимых багов нет.**
- Мелочь/латентно (не воспроизводится на текущих данных, не блокер): `appendLandItem`/`landBiomeRow` (`web/static/js/modal/events.js:756-830`) вставляют `biomeIconHtml` с `<img data-biome-fallback>`, но обработчик `error` → фолбэк привязан только внутри вкладок (`web/static/js/modal/tabs.js:702-706`, `container.querySelectorAll`). Если у биома когда-то не окажется спрайта, в меню будет битая картинка вместо эмодзи/«•». Сейчас все 57 форм планет имеют `web/static/sprites/biomes/<form>.png` — дефект не проявляется.
- Наблюдение (унаследованное, не этой задачи): `clampMenuToScreen` вызывается только при раскрытии; свёрнутое меню, открытое у нижней кромки, не прижимается.

## Непроверяемое / границы покрытия

- Реальный 2–4-минутный таймер погоды не ждали: «явление держится» подтверждено кодом `weatherUntil = Infinity` (surface_main.js:52-63) + наблюдением ≥30 с.
- UX ощущения игрока (удобно ли, что случайная высадка у админа спрятана в «Случайно», а не в клике по «Высадиться») — вкусовое.
- Тач/мобильная вёрстка меню и погодных чипов не проверялась (только desktop-окна).
- Ветка «у планеты нет биомов» / газовый гигант в браузере не прогонялась (в БД под резолв не попала; код-путь не менялся).

## Заметка для @manager

Отчёт @developer в задание не приложен — построчная сверка с ним невозможна; сверял код с текстом задачи, расхождений нет.
