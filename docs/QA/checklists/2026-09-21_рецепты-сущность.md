# QA-прогон: «Рецепт как сущность» (студия товаров)

**Дата:** 2026-09-21
**Задача:** независимый прогон фичи «Рецепт как сущность» (студия товаров).
Реализация в рабочем дереве, не закоммичена. Гейт создателя пройден.
**Окружение:** dev-сервер `http://localhost:8080` (`/health` OK), PostgreSQL 127.0.0.1:5432.
**Спек:** `docs/specs/2026-09-21-рецепт-сущность-и-граф-фабрики.md` (§2.4, §5),
UI-ТЗ `docs/specs/2026-09-21-студия-товары-рецепты-ui.md`.
**Учётка:** существующая `skycomposer` (role=skycomposer; в `.env` bootstrap-переменных нет).
**Инструменты:** psql (временные `.sql`), Invoke-RestMethod, `tools/e2e/goods-studio-server-check.js`
(playwright-core + системный Chrome headless), собственный одноразовый браузерный шаг (ветки/селекторы).
Код не чинился, коммиты не делались.

---

## Результаты (по строке на пункт)

1. **PASS** — SQL: таблицы `recipes`/`recipe_components`/`producer_recipes` есть; `goods_slots` (`to_regclass`=f) и `goods.tier_override` (нет в `information_schema`) отсутствуют; счётчики `2 / 0 / 2`; миграция `000055` в `schema_migrations` = t.
2. **PASS** — `/health` = `"OK"`; `/status` = 200.
3. **PASS** — `node goods-studio-server-check.js`: **TOTAL: 19 steps, 0 FAIL, 0 SKIP**; независимо перепроверил пункты 5 и 7 своим способом (ниже) — совпало.
4. **PASS** — свой браузерный шаг: ветка «Товары» грузится, 0 pageerror; `#goodsFactorySel` (24 фабрики) и `#goodsFamilySel` (11 семейств) видны; выбор фабрики 64 меняет бейдж `#modeBadge` → «фабрика: Фабрика стройматериалов · всё дерево · в наборе 0»; «Производители» открываются (`segProdRace` виден, справочник рисуется), «Предметы» открываются (`itemAddRow` виден), 0 pageerror. Скриншоты: `.../Temp/opencode/art4/studio-branches.png`, `studio-prod.png`.
5. **PASS** — `POST /studio/api/producers/62/recipes {recipe_id:1}` (рецепт кат.10 → фабрика кат.7) → **400**.
6. **PASS** — повтор `POST /producers/65/recipes {recipe_id:1}` → **409**; цикл: `PUT /recipes/1/components/0 {good_id:227}` 200, затем `PUT /recipes/2/components/0 {good_id:222}` → **409** (транзитивно 222→227→222).
7. **PASS** — товар 222 (кат.10, привязан) `PUT /goods/222 {category_id:9}` → **409**; фабрика 65 (кат.10, привязана) `PUT /producers/65 {category_id:8}` → **409**; фабрика 63 (кат.8, без привязок) → кат.17 **200**, возврат → кат.8 **200**; новый товар без привязок → кат.9 **200**.
8. **PASS** — `copy-universal`: targetA (кат.7/F1) → `added 1 / skipped 0`, повтор → `added 0 / skipped 1`; targetB (кат.8, пустой источник) → `{0,0}` 200; не-конкретная фабрика (id 2) → **400**; фабрика ресурсной категории (id 17) → **400**.
9. **PASS** — `PUT /studio/api/goods/222/tier` → **404**; `POST /studio/api/goods/222/slots` → **404**.
10. **PASS** — `GET /admin/resources` (skycomposer) → **200**, ключи `resources,templates,races,gaps,real,real_summary,families,chemotypes` (ресурсы непусты). ИИ-fill: шаг 8 e2e дал понятную ошибку `report="Ошибка ИИ: ... 127.0.0.1:3456 ..."` (opencode не поднят) — регресса нет.
11. **PASS** — 3 параллельных `POST /producers/99/recipes {recipe_id:1}` (Start-Job): коды `[200, 409, 409]` — 500/паники нет, данные целы (PK + advisory lock).
12. **PASS** — после уборки SQL: `recipes=2, recipe_components=0, producer_recipes=2`, `goods=134`, `QA_*` goods=0, `QA_*` producers=0, «осиротевших» рецептов=0; фабрики 62/63/65 на исходных категориях (7/8/10), привязки = `62←recipe2, 65←recipe1`.

**DoD (Go затронут):** `go build ./...` → OK; `go vet ./internal/goodsstudio/... ./internal/repository/... ./internal/handlers/...` → OK; `go test ./internal/goodsstudio/...` → ok (goodsstudio, ai, graph, validate).

---

ИТОГ: ПРОЙДЕНО

Найденное: багов не найдено.

Не проверено:
- **ИИ-fill с живым opencode** — opencode на :3456 не поднят, ветка «предложи → подтверди» с непустыми предложениями и apply не прогонялась вживую (только детерминированная «Ошибка ИИ»). Нужно: поднять opencode и повторить шаг 8 e2e.
- **Drag&drop привязки (перетаскивание карточки справочника на канвас)** — проверен только API-путь привязки (пп. 5, 6); сам жест drop и тост «выберите фабрику» без выбранной фабрики — клиентская механика, требует отдельного сценария с pointer-эмуляцией (в прогоне не поднимался).
- **Миграция на «старых» данных** — БД свежая: `goods kind='good'` до миграции не было, data-миграция `tier_override`/`goods_slots` прошла холостой; перенос реальных составов/тиров не воспроизведён (нет подходящей БД).
- **Конкурентность глубже 3 запросов** — гонял 3 параллельные привязки, сервер не под `-race` и не под нагрузкой; более широкая конкуренция (крупные пачки, параллельно разные мутации каталога) не проверялась.
- **Вкусовой/UX вид** — раскладка селекторов, группировка optgroup, бейдж «не привязан», читаемость «в наборе N» — визуальная оценка за создателем (мной подтверждено только наличие/переключение).

Предложения в чек-лист (@manager):
- B-ST-8: «Рецепт-сущность: инвариант выход=категория» — привязка рецепта чужой категории → 400; повтор привязки → 409; транзитивный цикл через компонент → 409; смена категории привязанного товара/фабрики → 409, без привязок → 200.
- B-ST-9: «Рецепт-сущность: маршруты/копирование» — `copy-universal` (added>0; повтор {0,>0}; пустой источник {0,0}; не-конкретная/ресурсная фабрика → 400); старые `/goods/{id}/tier` и `/goods/{id}/slots` → 404; `/studio/api/state` аддитивен (`recipe_id`/`complexity`/`bound_factories`/`producer_recipes`).
- B-ST-10: «Рецепт-сущность: UI-ветка Товары» — селекторы фабрики/семейства, смена графа по фабрике («в наборе N»), ветки Производители/Предметы без JS-ошибок.
- Обновить B-ST-2: из описания убрать «тир-оверрайды»/«слоты товара» (сняты) — заменить на «сложность рецепта/состав рецепта».
