# Отчёт @tester — 2026-09-21 — Фабрики релиз 2, итерация 1: строения + столицы фракций

**Задача:** независимый прогон первой итерации релиза 2 фабрик — сущность
`buildings`, столицы фракций, владелец-фракция, вкладка «Фракции» в карточке
планеты.
**Спека:** `docs/specs/2026-09-21-фабрики-релиз-2-столицы-фракций.md` §2–§8.
**Среда:** dev-сервер `http://localhost:8080` (`/health` → 200), локальная БД
`zorion`, Chrome (playwright-core). Дата прогона 2026-09-21.

**Прогнал:**

- `go build ./...` → 0, `go vet ./...` → 0.
- `go test -count=1 ./internal/generator/faction/ ./internal/repository/ ./internal/handlers/` → ok.
- `psql` — схема/данные/каскад (§ниже).
- Живые API: `POST /admin/generate-factions`, `GET /admin/generate-status?job=generate_factions`,
  `GET /api/worlds/{id}/planets` (admin + player токены).
- `cd tools/e2e; node factions-capital-check.js` (ADMIN_TOKEN), Chrome headless.
- Браузерная проба пустых состояний (admin — планета без фракций; player — без знания).

**Сценарии (жёсткий чек-лист 1–8):**

1. **PASS** — миграция `000056` в `schema_migrations`; таблица `buildings`
   совпадает со §2.1: `id uuid PK gen_random_uuid()`, `planet_id uuid NOT NULL`
   FK → `planets(id) ON DELETE CASCADE`, `building_type text NOT NULL`,
   `owner_type text NOT NULL CHECK (owner_type IN ('player','faction','agent'))`,
   `owner_id uuid NOT NULL`, `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`;
   индексы `idx_buildings_planet_id` + частичный UNIQUE
   `uq_buildings_capital_owner (owner_type, owner_id) WHERE building_type='capital'`.
2. **PASS** — генерация: dev-БД не имела обитаемых планет (0 поселений), поэтому
   посажен **временный** settlement на планету Janog (мир Jorik) → `POST
   /admin/generate-factions` → `{"status":"started"}` (асинхронная ветка), статус
   джоба `total:1, processed:0, done`. В БД: **1 фракция, 1 столица**; запись
   столицы `building_type='capital'`, `owner_type='faction'`,
   `planet_id = factions.homeworld_id = 14337027…` (Janog), `owner_id = id`
   фракции; расхождений `planet_id <> homeworld_id` — 0, висячих столиц — 0.
3. **PASS** — идемпотентность: временный settlement удалён → повторный `POST
   /admin/generate-factions` → `{"capitals":0,"status":"factions_generated","total":0}`
   (ветка `total == 0`, синхронный догон `EnsureCapitals` под гейтом);
   `factions` и `buildings` остались 1/1, дублей по `(owner_type, owner_id)` — 0.
4. **PASS** — `GET /api/worlds/5e4d7d4b…/planets` (admin) → 200; у Janog есть
   `factions[]` `{id,name,type,color,description}` (1 шт.) и `buildings[]`
   `{id,building_type,owner_type,owner_id}` (1 шт.); у остальных 7 планет ключи
   `factions`/`buildings` **отсутствуют** (omitempty).
5. **PASS** — видимость:
   - player без знания (снял сканер, удалил knowledge) → `factions`/`buildings`
     отсутствуют у всех планет, `knowledge:null`;
   - player со знанием (вручную посажена строка `player_planet_knowledge` на
     Janog) → у Janog `factions`/`buildings` есть, у прочих — нет;
   - admin — видит всегда.
   Код: `stripPlanetDetails` (planet_visibility.go:74-77) — `view == nil` →
   `Factions`/`Buildings = nil`; юнит-тест `TestStripFactionsAndBuildings`
   (planet_visibility_test.go) зелёный.
6. **PASS** — e2e реальный Chrome (`factions-capital-check.js`): `1/4 map open`
   PASS, `2/4 system modal` PASS, `3/4 factions tab` PASS (`factions=1
   capitalRow=true`), `4/4 expand details` PASS (`before=none after=block
   owner=true`), `5/5 no toast errors` PASS (`toast-error count=0`), `5/5 no
   login redirect` PASS (`url=…/map`), `no page errors` PASS (0). `RESULT: ALL
   PASS`, exit 0. Скриншоты: `tools/e2e/artifacts/factions-capital.png`,
   `factions-capital-details.png`.
7. **PASS** — каскад транзакционно (без вайпа): в `BEGIN` создана временная
   планета + столица (`INSERT … RETURNING`) → `DELETE FROM planets` → столица
   исчезла (1 → 0), планета 0 → `ROLLBACK` (после откаты: планета 0, строение 0).
   Данные создателя не тронуты.
8. **PASS** — пустые состояния вкладки (живой браузер):
   - admin, планета без фракций (Isziazur) → «В отчёте сканера фракции не
     значились» (не «Фракций нет»);
   - player без знания (Pallekra, `knowledge=null`) → «Нет данных — купить
     отчёт»; 0 JS-ошибок.

**Вердикт: ПРОЙДЕНО** (все пункты 1–8 PASS; пункт 2 выполнен с оговоркой —
dev-БД без обитаемых планет, данные посажены временно и удалены после прогона).

**Найденное:** багов и расхождений со спекой не выявлено.

**Очистка после прогона:** удалены созданные тестером строки — фракция, столица,
временный settlement, строка `player_planet_knowledge`, два тест-пользователя.
Итог: `factions=0`, `buildings=0`, `settlements=0`, тест-пользователей 0
(состояние БД как до прогона). Временный browser-пробник удалён.

**Изменения тест-артефактов (не код фичи):** в `tools/e2e/factions-capital-check.js`
добавлены два шага `5/5 no toast errors` и `5/5 no login redirect` — пункт 6
спеки требует явных проверок `.toast-error` и редиректа на `/login-page`, в
исходном скрипте их не было. На feature-код правок не делалось.

**НЕ проверено (SKIP) и почему:**

- **Живой гейт C3 с работающим Пакманом** (`POST /admin/generate-factions` → 409
  ДО ветки `total == 0`). Запуск Пакмана на текущей dev-БД запрещён задачей
  (данные создателя). Покрыто только юнит-тестами
  `TestGenerateFactionsEmptyUniversePacmanGate` и `…MutexGate`
  (admin_factions_test.go) и чтением кода (`admin_universe.go`: гейт
  `IsRunning(JobPacman)` + `universeMutationMu.TryLock()` до ветки `total == 0`).
  Чтобы закрыть живьём: прогон на одноразовой галактике с запущенным Пакманом.
- **«Удалить все миры» / очистка (TRUNCATE `buildings`)** — деструктивно, не
  запускалось; покрыто тестом `TestTruncateTablesIncludesBuildings` и составом
  `truncateTables`.
- **Лог джоба `N factions, M capitals`** в асинхронной ветке — HTTP отдаёт
  `{"status":"started"}` до конца работы; чтение stdout dev-сервера не
  выполнялось (проверено только по коду `admin_universe.go`).
- **Массовые объёмы** (много фракций/столиц, инвариант 2 «два запроса на
  систему») — не нагрузочно; логика `= ANY($1)` подтверждена чтением
  `attachFactionsAndBuildings` и тестом `planet_faction`-мока.

**Предложения в чек-лист (применяет @manager):**

- Новый кейс зоны «БД/репозиторий» / «карта-фронт»: **строения-столицы фракций
  (релиз 2, итерация 1)** — миграция `000056`; генерация создаёт столиц ==
  фракций (`planet_id = homeworld_id`, `owner_type='faction'`); повторный прогон
  без дублей; API отдаёт `factions`/`buildings` (omitempty) при знании/админе и
  скрывает для player без знания; вкладка «Фракции» показывает фракцию + строку
  «🏛 Столица» с раскрытием владельца; пустые состояния «Нет данных — купить
  отчёт» (без знания) и «В отчёте сканера фракции не значились» (со знанием/админ,
  фракций нет); каскад `planets → buildings` (ON DELETE CASCADE).
- Дополнить e2e-скрипт `tools/e2e/factions-capital-check.js` проверками
  `.toast-error` и редиректа на `/login-page` (сделано тестером в этом прогоне —
  закрепить как постоянные шаги).
