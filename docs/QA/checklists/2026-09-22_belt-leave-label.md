# QA-прогон: подпись кнопки выхода в мини-игре добычи в поясе (этап 3)

**Дата:** 2026-09-22
**Тестировщик:** @tester
**Задача:** смена подписей кнопок выхода «Вернуться на корабль» → **«Вернуться на карту»** (только текст; поведение/сеть/API не менялись)
**Файлы правки:** `web/belt.html`, `web/static/js/belt/belt_ui.js`, `web/static/js/belt/belt_main.js` (+ комментарий в `belt_net.js`)
**Окружение:** Windows, dev-сервер `localhost:8080` (`/health` 200), PostgreSQL `127.0.0.1:5432`;
игрок `qa_belt_3a` (`edb113d4-…`) в `orbit/belt`, пояс `2da0035b-14d1-4868-b7cd-e904179d1d15` («Telyolhal»); JWT собран из `.env`-секрета (без правок БД).
**Вердикт:** ✅ **ПРОЙДЕНО** — 4/4 PASS, дефектов нет.

---

## Сценарии (жёсткий чек-лист, каждый пункт — один раз)

| № | Итог | Факт |
|---|------|------|
| 1. Статика | **PASS** | `grep "Вернуться на корабль\|Вернуться на карту"` по `web/`: строки «Вернуться на корабль» **нет ни одной**. Все точки выхода — «Вернуться на карту»: `web/belt.html:33` (`#belt-leave-btn`, подпись + `title`), `web/belt.html:59` (`#pause-leave`), `web/static/js/belt/belt_ui.js:195` (`setLeaveBusy` сброс), `web/static/js/belt/belt_main.js:92` (кнопка в подсказке «трюм полон»). Плюс комментарии `belt_main.js:4` и `belt_net.js:45`. |
| 2. Синтаксис JS | **PASS** | `node --check web/static/js/belt/belt_main.js` → exit 0; `node --check web/static/js/belt/belt_ui.js` → exit 0. |
| 3. Браузерный смоук | **PASS** | Playwright (системный Chrome), под `qa_belt_3a`, `/belt.html?belt=2da0035b-…`: `#belt-leave-btn` найдена, текст ровно **«Вернуться на карту»**; клик → `window.location.href = '/map'` (URL стал `http://localhost:8080/map`), **без `pageerror`**, без редиректа на `/login-page`, ответов HTTP ≥ 400 нет. Скриншот: `tools/e2e/artifacts/belt-leave-label.png`. |
| 4. DoD | **PASS** | Go-код не тронут (правка — только HTML/JS-подписи) → `go build`/`go vet`/`-race` не требовались. |

Прогон смоука: `[belt-open] PASS`, `[leave-label] PASS`, `[leave-click-map] PASS` (→ `/map`), `[no-pageerror] PASS`, `[no-login-redirect] PASS`. После прогона позиция игрока восстановлена самой игрой: `orbit/belt` (как до захода).

---

## Найденное

Дефектов нет. Текст подписи совпадает с требуемым во всех точках выхода; переход на `/map` работает как и был.

Примечание (не дефект этой задачи): `web/static/js/belt/belt_net.js` тоже изменён в рабочем дереве — это правка комментария (`«вернуться на корабль» → «вернуться на карту»`), поведения не касается. В список файлов задачи не входил, но согласуется с ней.

---

## Предложения в чек-лист

- **Новый кейс (клиент, регресс):** «кнопка выхода в мини-игре пояса подписана „Вернуться на карту“ (в т.ч. после `setLeaveBusy` и в подсказке „трюм полон“); клик ведёт на `/map` без разлогина/`pageerror`» — проверено в п.1/п.3.
- **Устаревших кейсов не выявлено.**

---

## Чеклист непроверяемого

- **Подпись в состоянии «Выходим…» (`setLeaveBusy(true)`)** — гонка ~мгновенная, глазами в headless не зафиксировал: `belt_ui.js:195` проверен по коду (`busy ? 'Выходим…' : 'Вернуться на карту'`), в живом кадре кнопка сразу уходит на `/map`. Нужно: медленная сеть/шаг-бай-шаг, если важно глазами.
- **Экран «трюм полон» (`hintHtml` → `#hint-leave`, `belt_main.js:92`)** — текст этой кнопки подтверждён грепом, но кликом не воспроизводил: пояс не выработан, трюм `qa_belt_3a` не заполнен до предела (не хотел портить тестовые данные заливкой трюма). Нужно: игрок с полным трюмом либо согласованное окно тестовых данных.
- **Пункт 4 (DoD)** — не гонял, т.к. Go не тронут (обосновано в таблице).
- **Визуальный вид кнопки** (шрифт/отступы/контраст) — вкусовое/визуальное, зона `@gdesigner`/`@uidesigner`; изменился только текст, разметка и стили не тронуты.
