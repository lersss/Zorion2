# QA-прогон: «Контракты + деньги» — этап A (деньги/счёт)

**Дата:** 2026-09-22
**Задача:** независимый прогон этапа A эпика «Контракты + деньги» (счёт, без эскроу/контрактов)
**Спека:** `docs/specs/2026-09-22-деньги-и-эскроу.md` §3, §3.4, §7
**Идея:** `docs/gamedesign/ideas/2026-09-22_контракты-сущность-деньги-эскроу.md`
**Проверяемые файлы:** `migrations/000061_money.sql`, `internal/models/account.go`,
`internal/repository/account_repository.go` (+test), `internal/handlers/money_handlers.go` (+test),
`internal/repository/user_repository.go`, `internal/handlers/auth_handlers.go`, блок денег в `cmd/server/main.go`
**Вердикт:** **С ОГОВОРКАМИ** (8 PASS / 0 FAIL / 2 не проверено — ограничение среды)

---

## Прогнал

| Команда | Результат |
|---|---|
| `go build ./...` | exit 0 |
| `go vet ./...` | exit 0 |
| `go test -race -short (go list ./... \| ? { $_ -notmatch 'generator[/\\](planet\|galaxy)$' })` | все пакеты `ok`, FAIL нет |
| `go test -race -count=1 -run "Account\|Money\|MyMoney" ./internal/repository/ ./internal/handlers/ -v` | 13 тестов PASS (см. п.10) |
| psql: `schema_migrations`, `to_regclass('accounts')`, `information_schema.columns` | см. п.8 |

**Браузерный смоук (`tools/e2e`) не запускался — основание:** этап A не содержит клиентских
флоу (только API `/me/money` и серверная логика счёта), UI-потребителя у денег ещё нет.

---

## Чек-лист (по строке на пункт)

1. **PASS** — `ensureAccount` идемпотентен: `ensureAccountSQL` = `INSERT ... ON CONFLICT (owner_type, owner_id) DO NOTHING`
   (`internal/repository/account_repository.go:29-33`); повторный вызов не переписывает баланс.
   Тест `TestEnsureAccountIdempotent` (второй вызов → 0 строк, без ошибки) — PASS.
2. **PASS** — регистрация создаёт счёт в одной транзакции: `CreateWithAccount` (`user_repository.go:41-56`)
   — `Begin` → `createUser(tx)` → `ensureAccount(tx, 'player', user.ID, seed)` → `Commit`, `defer tx.Rollback()`.
   `auth_handlers.go:142` вызывает `CreateWithAccount`. Тесты `TestCreateUserWithAccountSameTx`,
   `TestCreateUserWithAccountRollsBackOnAccountFailure` — PASS (откат при сбое начисления).
3. **PASS** — списание атомарно: `debitAccountSQL` (`account_repository.go:48-54`) — один `UPDATE` с
   `WHERE ... AND balance >= $3`; `RowsAffected == 0` → `applied=false`, ошибки нет (`Debit`, :98-113).
   Тест `TestDebitAccountInsufficientFunds` — PASS.
4. **PASS** — неположительная сумма отвергается до БД: `if amount <= 0 { return false, nil }`
   (`account_repository.go:101-103`), запрос не выполняется. Тест `TestDebitAccountRejectsNonPositive`
   (0 и −100, `ExpectationsWereMet` без ожиданий) — PASS.
5. **PASS** — инвариант `withdrawable <= balance` на списании: `withdrawable = LEAST(withdrawable, balance - $3)`
   (`account_repository.go:51`), плюс CHECK в миграции (`000061_money.sql:34`). Тест
   `TestDebitAccountSQLClampsWithdrawable` — PASS. Баланс в минус не уходит (`balance >= $3` + CHECK `balance >= 0`).
6. **PASS** — `GET /me/money` отдаёт `balance`, `withdrawable`, `operations`; пустая история → `[]`:
   `money_handlers.go:59-61` (`if ops == nil { ops = []models.MoneyOperation{} }`).
   Тесты `TestGetMyMoney`, `TestGetMyMoneyEmptyHistory` (проверка `"[]"`) — PASS.
7. **PASS** — без JWT → 401 (`money_handlers.go:34-38`), owner берётся из `auth.UserIDKey` (JWT), не из запроса;
   чужой счёт недостижим. Тест `TestGetMyMoneyUnauthorized` (БД не трогается) — PASS.
8. **НЕ ПРОВЕРЕНО (ограничение среды)** — миграция `000061` на локальной БД **не накатана**:
   `schema_migrations` max = `000059`, `to_regclass('public.accounts')` = NULL,
   `to_regclass('public.money_operations')` = NULL, колонки `npc_agents.owner_faction_id` нет.
   Фактический накат/бэкфилл не воспроизведён. **Статически** миграция корректна: `CREATE TABLE IF NOT EXISTS`
   + `ON CONFLICT DO NOTHING` (идемпотентно), сиды `10000` (player) и `10^15` (faction),
   `ALTER ... ADD COLUMN IF NOT EXISTS` — `000061_money.sql:17-65`. См. «Не проверено».
9. **PASS** — DoD зелёный: `go build ./...` exit 0, `go vet ./...` exit 0, `go test -race -short` (без
   `generator/planet|galaxy`) — все пакеты `ok`, FAIL нет.
10. **PASS** — тесты не пустые, покрывают заявленное: идемпотентность (`TestEnsureAccountIdempotent`),
    нехватка (`TestDebitAccountInsufficientFunds`), кламп (`TestDebitAccountSQLClampsWithdrawable`),
    откат tx (`TestCreateUserWithAccountRollsBackOnAccountFailure`), 401 (`TestGetMyMoneyUnauthorized`),
    пустая история (`TestGetMyMoneyEmptyHistory`), ленивая страховка (`TestGetMeEnsuresPlayerAccount`).

---

## Найденное

Багов не найдено. Замечаний уровня «баг» нет.

Наблюдения (не баги, на усмотрение @manager):
- `GetMyMoney` при ошибке `EnsureAccount` только логирует и продолжает (`money_handlers.go:40-42`);
  если счёта реально нет, `GetBalance` вернёт `nil` → 500 «Счёт не найден». Поведение осознанное
  (best-effort), но при сбое БД игрок получит 500 — приемлемо.
- `Credit` не проверяет `amount <= 0` (в отличие от `Debit`). Для этапа A не критично (Credit вызывается
  из внутренних путей этапа B), но при появлении внешнего входа стоит закрыть симметрично.

---

## Не проверено (граница покрытия)

| Что | Почему | Что нужно, чтобы закрыть |
|---|---|---|
| Фактический накат `000061` и бэкфилл на реальной БД (п.8) | Локальная БД на `000059`; `accounts`/`money_operations`/`npc_agents.owner_faction_id` отсутствуют. Миграции применяет сервер при старте — поднимать dev-сервер и писать в БД в рамках этого прогона не стал | Поднять dev-сервер (`.env`, `run.ps1`) или прогнать миграции, затем `SELECT count(*) FROM accounts WHERE owner_type='player'` = число `users` (390) и `owner_type='faction'` = число `factions`; повторный накат — без дублей |
| `GET /me/money` живым curl'ом с JWT и без (п.6–7) | Требует поднятого сервера + валидного игрового токена; в прогоне проверено только юнит-тестами через `httptest` | Поднять сервер, зарегистрировать игрока, `curl -H "Authorization: Bearer <JWT>" /me/money` → 200 с балансом 10000; без заголовка → 401 |
| Поведение при сбое начисления в реальной транзакции (п.2) | Проверено sqlmock-тестом, не живой БД | Интеграционный прогон с намеренным сбоем `ensureAccount` |
| UX/приватность глазами игрока | UI-потребителя у денег в этапе A нет | Появится на этапе B/C (доска контрактов) |

---

## Предложения в чек-лист (применит @manager)

- Добавить кейс **B-MONEY-1**: «накат `000061` на чистой БД → бэкфилл: `accounts` player = число `users`,
  faction = число `factions`; повторный накат не создаёт дублей» — закрывает п.8 интеграционно.
- Добавить кейс **B-MONEY-2**: «`GET /me/money` живым сервером: с JWT → 200/баланс, без JWT → 401,
  чужой `owner_id` недостижим» — закрывает п.6–7 end-to-end.
- Кейс «`Credit` с `amount <= 0`» — если этап B начнёт вызывать `Credit` из внешних путей.
