# QA-прогон — «С поверхности можно улететь» (идея 2026-09-21)

**Дата:** 2026-09-22
**Идея:** `docs/gamedesign/ideas/2026-09-21_залипание-на-поверхности-взлёт-и-маршрут.md`
**Роль:** @tester (независимый прогон, жёсткий чек-лист из 6 пунктов, каждый пункт ровно один раз)
**Вердикт:** **ПРОЙДЕНО** — 6 PASS / 0 FAIL
**Код (рабочая копия, не закоммичено):** `internal/handlers/intrasystem_handlers.go`,
`internal/handlers/intrasystem_handlers_test.go`, `web/static/js/modal/events.js`.
Чужие правки в рабочей копии (`internal/generator/planet/*`, `web/studio.html`,
`config/balancer_presets.json`, `.gitignore`, `tools/e2e/*`) не трогались и не стейджились.

---

## Окружение и методика

- dev-сервер уже был поднят на `http://localhost:8080` (`/health` → 200 OK). Перезапуск не
  потребовался: сервер **отдал 202 с новым `from_type=planet`** (см. п.4) — старый код на этом
  входе давал 400, значит живой процесс содержит фикс (ловушка «стухший сервер» снята фактом, B-DB-5).
- PostgreSQL 127.0.0.1:5432 (`zorion/zorion123`), psql-скрипты через `.sql`-файлы (PITFALLS).
- Тест-игрок: `qa_surf_010308` (свежая регистрация, id `d5d0ca69…`), мир `778c8a40…` (Naebostel).
  Позиция на орбите `0ee01acc…` (Oxlor) выставлена SQL-ом, высадка — штатным `POST /api/surface/land`
  (200, `status=surface`, `biome` задан). Способ размещения в системе на проверяемую логику не влияет.
- e2e: `tools/e2e/map-check.js`, реальный Chrome (playwright-core).

---

## Результаты по пунктам

1. **PASS** — `go build ./...` → exit 0; `go vet ./...` → exit 0. Ошибок/предупреждений нет.
2. **PASS** — `go test -race -short -count=1 ./internal/handlers/` → `ok zorion/internal/handlers 18.736s` (exit 0).
   Целевой `-run 'TestStartIntraFlight' -v`: новый `TestStartIntraFlightFromSurface` **PASS** (плюс 12 соседних PASS),
   старый `TestStartIntraFlightFromSurfaceRejected` в наборе отсутствует.
3. **PASS (код чист)** — тест красный на старом коде: удалённая ветка (`git diff`, `intrasystem_handlers.go`,
   бывш. ~412–418) возвращала 400 «Сначала вернитесь на орбиту (вызов корабля)» ещё до `StartAtomic`,
   а новый тест ожидает 202, `from=planet,p1` и **`expectIntraStartAtomic`** — на старом коде не выполнится ни
   ассерт кода, ни `ExpectationsWereMet()`. Строки `«Сначала вернитесь на орбиту»` **в Go/JS-коде больше нет**
   (rg: 0 совпадений по `*.go`, `*.js`, `cmd/`, `internal/`); остались только упоминания в доках —
   спека `docs/specs/2026-09-21-высадка-прогулка-по-планете.md:343,467` (переписать — пункт 4.4 идеи, задача доков),
   исторический чеклист `docs/QA/checklists/2026-09-21_…:51` и сама идея. Это ожидаемая незакрытая правка доков, не код.
4. **PASS** — вживую: игрок `status=surface` на Oxlor `0ee01acc…` → `POST /api/intrasystem-flight {object_type:"planet",object_id:"7b784af5…"}` (другая планета системы Nelbosyel)
   → **HTTP 202**, `{"duration":3,"from_type":"planet","from_id":"0ee01acc…","to_type":"planet","to_id":"7b784af5…",…}`;
   `/me` после → `status=in_flight`, `from_type=planet`, `from_id=0ee01acc…`. Ранее здесь был 400 (см. п.3).
5. **PASS (по коду)** — `web/static/js/modal/events.js:721-730`: при `onThisSurface` (своя планета, `status=surface`,
   `object_id===planet.id`, стр.648) пункт «🚶 Вы на поверхности» получает обработчик → `window.location.href='/surface.html?planet='+encodeURIComponent(planet.id)`
   (мёртвой подписи больше нет). У **другой** планеты `!onThisOrbit && !onThisSurface` → видимый пункт «🚀 Лететь» (стр.697-714),
   и он для позиции `surface` уходит в `startIntraFlight` (стр.707-708) — теперь получает 202 из п.4.
6. **PASS** — `cd tools/e2e; npm.cmd i` (`up to date`, exit 0); `node map-check.js` → **RESULT: ALL PASS**,
   `[2/5 map loads] pageErrors=0`, `[3/5 click star] toastError=false path=/map`, `[4/5 restricted] path=/map`,
   `[5/5]` скриншоты `artifacts/map.png`, `artifacts/restricted-card.png`; редиректов/разлогина нет, exit 0.
   **Отметка о покрытии:** сам `map-check.js` **не** содержит сценария «поверхность → Лететь» (rg по `surface|Лететь|contextmenu` — 0 совпадений).
   Целевой e2e для этого сценария существует отдельно — `tools/e2e/surface-walk-check.js` (в нём есть `hasLetsPojt`, ПКМ по планете,
   `POST /api/intrasystem-flight`): в объявленный чек-лист он не входил, поэтому **не запускался** (не расширял рамки прогона).

---

## Найденные баги

Не найдено.

## Чеклист непроверяемого

- **Визуальная проверка контекстного меню в браузере (п.5)** — проверено по коду `showPlanetMenu`
  (`events.js:648,697-730`), а не кликом: шаг 5 чек-листа требует пункт «Вы на поверхности»/«Лететь»,
  но запрещает изобретать новые проверки сверх 6 пунктов, а `map-check.js` из п.6 меню не открывает.
  Доказать кликом может существующий `tools/e2e/surface-walk-check.js` (ПКМ + `hasLetsPojt`) — запустить его,
  если создатель расширит рамки прогона.
- **Тест «красный на старом коде»** — подтверждено разбором удалённой ветки и ожиданий `go-sqlmock`
  (не прогоном: откат `intrasystem_handlers.go` к HEAD в общей рабочей копии затронул бы чужие параллельные правки).
- **Регрессия «взлёт + межзвёздный сегмент» (чужая система, `/travel`)** — вне 6 пунктов: текущая правка
  трогает только `StartIntraFlight` (своя система) и клиентское меню; чужой системы сценарий не проверялся.
