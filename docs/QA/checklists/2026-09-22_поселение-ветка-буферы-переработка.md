# QA-прогон: ветка поселения — входной/выходной буфер + ленивая переработка (итерация 2 эпика «Экономика поселения»)

- **Дата:** 2026-09-22
- **Задача:** независимая проверка фичи (код и спека не закоммичены)
- **Спека:** `docs/specs/2026-09-22-поселение-ветка-буферы-переработка.md` (§3–§6, §8)
- **Миграция:** `000064_settlement_branches.sql` (применена: `schema_migrations` = 000065; обе таблицы на месте)
- **Окружение:** dev-сервер `http://localhost:8080` (собран из текущего рабочего дерева `go build -o …\zorion-server-qa.exe ./cmd/server`, запущен по `AGENTS.md` §1: `.env` + `DATABASE_URL`/`REDIS_URL`/`TICK_INTERVAL_SEC=3`, `JWT_SECRET` вручную не задавался; `/health`=OK). PostgreSQL 127.0.0.1:5432 (`zorion/zorion123`), psql через `.sql`-файлы (PITFALLS). Браузер — системный Chrome (playwright-core).
- **Данные прогона:** поселение `00000b8b-6a5e-4668-89b2-95d588683f51` (planet `5fa237ea…`, world `6f55ca4f…`, population ≈ 3.01·10⁸), рецепт `69` «Пища» = `359 Мясо + 1 вода-ресурс → 378 Пища`.

## Команды

```powershell
go build ./...            # exit 0
go vet ./...              # exit 0
go test -race -short (go list ./... | ? { $_ -notmatch 'generator[/\\](planet|galaxy)$' })   # exit 0 (все ok)
go build -o %TEMP%\opencode\zorion-server-qa.exe ./cmd/server   # перезапуск dev-сервера на текущем коде
node --check web/static/js/modal/branches.js ; node --check web/static/js/modal/tabs.js   # exit 0
cd tools/e2e; npm.cmd i; node map-check.js              # exit 0, RESULT: ALL PASS
```

Живые шаги — `Invoke-RestMethod` (admin/player JWT) + `psql -f`; админ-токен — регистрация `qa_branch_45061` + SQL `role='admin'` (учётка и тестовые данные удалены после прогона).

## Результаты чек-листа

| # | Итог | Наблюдение |
|---|---|---|
| T1 | **PASS** | `go build ./...`=0, `go vet ./...`=0, `go test -race -short` по всем пакетам (кроме `generator/planet|galaxy`) — все `ok` (exit 0) |
| T2 | **PASS** | Ветка создана (`POST /admin/settlements/{id}/branches {recipe_id:69}` → 200, посев input 1/359, output 378); подложено 1000+1000; `processed_at` отброшен на 2 ч; GET карточки (admin) вызвал ленивый синк: вход 1000→984.44, выход 0→15.56, `processed_at` продвинулась; значения персистентны в БД |
| T3 | **PASS** | Обе пишущие ручки берут `FOR UPDATE` на строке ветки (`LockBranchTx` — `FOR UPDATE`; `branchSelectByIDSQL` — `FOR UPDATE OF b`). Живой конкурентный прогон (6×`AddBranchInput` + 4×GET карточки одновременно): один инкремент +100 учтён ровно (984.44→1068.89), вход не ушёл в минус, ошибок/`CHECK`-нарушений в логах нет; параллельные админ-мутации получили документированный 409 (`universeMutationMu.TryLock`) |
| T4 | **PASS** | (а) `DELETE goods 359` в откатываемой tx → `recipe_components.component_id` = NULL, ветка жива (1); (б) `DELETE recipes 69` → ветка 0, буферы 0 (CASCADE); (в) `truncateTables` (`admin_universe.go:60`) содержит обе таблицы + тест `TestTruncateTablesIncludesSettlementBranches` (`admin_factions_test.go:146`) зелёный |
| T5 | **PASS** | Живые: `amount`=0/-5 → 422; `good_id=378` (не компонент) → 422; повторная ветка рец.69 → 409; рецепт без заполненных компонентов (временный рец.70) → 422; несуществующая ветка → 404; несуществующее поселение → 404. Гейты Пакмана/`universeMutationMu` → 409 (код + unit `TestAddBranchPacmanGate`/`TestAddBranchMutexGate`; живое 409 в T3) |
| T6 | **PASS** | `stripPlanetDetails` обнуляет `Input` каждой ветки (`planet_visibility.go:72,110`) и затем `p.Settlements=nil` (:73). Живой player (со знанием планеты) GET карточки: планета отдаётся, `settlements = null` — вход/ветки не утекают; admin видит `Input`. Обходных путей к игроку нет: единственная игровая отдача планет — `GetPlanetsByWorldID`→`applyPlanetVisibility` (`planet_handler.go:28,137`); `GetPlanetByID`-потребители (contracts/image/intra-restore/admin) планету игроку не сериализуют (доска контрактов отдаёт только `contracts`) |
| T7 | **PASS** | Добавлен новый заполненный компонент (good 2) в рецепт 69 при живой ветке → после персистентного синка в `input` появилась строка `good 2 = 0` (top-up `ON CONFLICT DO NOTHING`), выход не изменился (affordable=0). Фикстура откачена (компонент pos=2 и его строка буфера удалены) |
| T8 | **PASS** | `node map-check.js` — RESULT: ALL PASS (регистрация/логин, карта без JS-ошибок, клик по звезде без тост-ошибки/редиректа, restricted-карточка за радаром). Дополнительно целевой админ-смоук карточки (временный пробник, удалён): «Ветки (…)» + «Выход» + «Вход (виден только админу)» + форма создания ветки + форма «добавить во вход» отрендерены; 0 `pageerror`, 0 `.toast-error`, нет редиректа. Скриншоты: `tools/e2e/artifacts/map.png`, `restricted-card.png`, `qa-branch-card.png` |

## Найденное

Багов не найдено. Замечаний по реализации — нет.

## Что НЕ проверено (граница покрытия)

- **Каскад через студию (ручки)**: проверены FK-контракты (живой `DELETE` в откатываемой транзакции); реальный HTTP-поток удаления товара/рецепта в студии и предупреждение `deposits-count`-образца (`/studio/api/goods/{id}/branches-count`) не гонял — это зона студии, вне объёма итерации 2.
- **Долговременный тренд скорости** (`k`, калибровка) — вне компетенции @tester, за @balancetester.
- **UX глазами игрока**: сейчас детали поселений игроку не отдаются вовсе (`Settlements=nil`) — фактического показа выходного буфера игроку нет; проверено правило видимости, не достижимость (§6 спеки).
- **Гейт Пакмана «в бою»** (409 при работающем джобе) живым запуском не воспроизводил: запуск Пакмана ест галактику и запрещён поверх чужих джобов — проверено кодом + существующими unit-тестами.
- **Смена состава рецепта для работающей ветки** (§3.3 «выпавший компонент») — покрыта частично T7 (новый компонент); выпадение компонента проверено только контрактом FK (SET NULL), живого прогона «остаток input не потребляется» не делал.

## Замечания к среде

- Запущенный ранее сервер (`zorion-server-b1.exe`) содержал **старый** бинарь без роутов `/admin/settlements/` и `/admin/branches/` (404 на первом запросе). Перезапущен на бинаре из текущего рабочего дерева; оставлен работать дальше (несёт незакоммиченную фичу).
- Все тестовые данные (ветка, temp-рецепт 70, компонент pos=2, учётки `qa_branch_45061`/`qa_branch_pl_34060`, строка знания) удалены после прогона; `settlement_branches` = 0.

## Предложения в чек-лист (применяет @manager)

- **Новые сценарии:** (1) живой сценарий ветки с отбросом `processed_at` (вход убывает / выход растёт / чек-точка двигается); (2) top-up нового компонента рецепта при живой ветке; (3) границы ручек `amount≤0`/не-компонент/дубль/пустой рецепт/404; (4) видимость: у player `settlements=null`, у admin `Input` виден.
- **Регрессионные точки:** FK `settlement_branches.recipe_id`/`buffer.branch_id`/`buffer.good_id` CASCADE и `recipe_components.component_id` SET NULL; `truncateTables` содержит обе таблицы.
- **Уточнить существующий кейс:** «выход виден игроку» — сегодня недостижимо (поселения игроку не отдаются), до появления владельца поселения кейс остаётся правилом видимости.
- **Устаревших кейсов не выявлено.**
