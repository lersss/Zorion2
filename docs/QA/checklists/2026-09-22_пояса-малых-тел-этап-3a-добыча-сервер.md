# QA-прогон: Пояса малых тел — этап 3a «Добыча в поясе» (сервер)

**Дата:** 2026-09-22
**Тестировщик:** @tester
**Спека:** `docs/specs/2026-09-22-пояса-малых-тел-этап-3-добыча.md` (§4–§8, §12.1)
**Окружение:** Windows, Go 1.27, gcc WinLibs 16.1.0 (PATH, `CGO_ENABLED=1`),
PostgreSQL 15 (`127.0.0.1:5432`), Redis `6379`; dev-сервер `localhost:8080`
(запущен 22:44 из текущего дерева), БД `system_belts`: 9 711 записей,
`iron_remaining` — у всех `NULL` (ленивая инициализация ещё не срабатывала).
**Вердикт:** ✅ **ПРОЙДЕНО (с одной оговоркой)** — 11/12 PASS; п.12 FAIL
(в рабочем дереве есть посторонние правки, см. «Найденное»).

---

## Прогнал

```
go build ./...                                                              → exit 0
go vet ./...                                                                → exit 0
go test -race -short (без generator/planet|galaxy)                          → все пакеты ok, FAIL нет
go test -race -short -count=1 ./internal/handlers/ .../repository/ ...      → ok (handlers 18.5s)
go test -race -short -count=1 -run 'Belt|Mining' -v ./internal/handlers/    → 59 PASS / 0 FAIL / 0 SKIP
powershell -File tools/db.ps1 -File <миграция/скрипты>                      → колонка/CHECK/идемпотентность
POST /api/belt/mine/enter|collect|leave, POST /api/intrasystem-flight,      → статусы/тела (HttpClient, UTF-8)
GET /api/players/positions, GET /api/cargo, GET /me
```

Тестовые данные: пояс `2da0035b-14d1-4868-b7cd-e904179d1d15` («Telyolhal»,
`composition.iron = 0.4975` → `k_iron ≈ 1.658`, `belt_class = «богатый»`,
`rate_cap = 0.8292 т/с`, `reserve = 995.05 т`), мир `5e274519-…`, игрок
`qa_belt_3a` (`edb113d4-…`), второй игрок `qa_belt_3a_b` (`96e2363c-…`).
После прогона состояние восстановлено (позиция игрока — `orbit/belt`,
`iron_remaining` тестового пояса снова `NULL`).

---

## Сценарии (жёсткий чек-лист, каждый пункт — один раз)

| № | Итог | Факт |
|---|------|------|
| 1 | **PASS** | `go build ./...` — exit 0, ошибок нет. |
| 2 | **PASS** | `go vet ./...` — exit 0, предупреждений нет. |
| 3 | **PASS** | Полный `-race -short` (пакеты без `generator/planet|galaxy`) — ни одного FAIL (все `ok`, часть из кэша); свежий `-count=1` по затронутым пакетам `internal/handlers` (18.5s), `repository`, `models`, `cargo`, `travel` — `ok`. Целевой прогон `-run 'Belt|Mining'`: 59 PASS / 0 FAIL / 0 SKIP. |
| 4 | **PASS** | Миграция `000069_belt_mining.sql` на живой БД: колонка `system_belts.iron_remaining` — `double precision`, `is_nullable = YES`, `column_default = NULL`; CHECK `system_belts_iron_remaining_check CHECK (iron_remaining >= 0)`. В транзакции с откатом: `NULL`/`0`/`12.5` приняты; `-1` отвергнут (`ERROR: new row … violates check constraint`, psql exit 3). Повторный прогон файла миграции: `NOTICE: column … already exists, skipping`, exit 0 — идемпотентна (`migrations/000069_belt_mining.sql:19-21`). |
| 5 | **PASS** | `POST /api/belt/mine/enter` из `orbit/belt` → **200**, пакет: `belt_id/belt_name/belt_kind`, `seed`, `composition_iron`, `resource{good_id:21,name:"Железо Fe",weight:1}`, `belt_class:"богатый"`, `remaining_level`, `started_at`, `mined:0`, `cargo{used,total:100,free}`, `limits{rate_cap:0.8292,dt_max:2}`. Запас при первом входе лениво инициализирован (`NULL → 995.05`). Позиция → `{mining, belt, object_id, mined:0, last_collect_at}` (`belt_mining_handlers.go:300-470`). |
| 6 | **PASS** | `POST /api/belt/mine/collect {amount:1000}` после паузы 3 с → `granted = 1.658418 = rate_cap(0.8292)·Δt_max(2)` — ровно потолок, `amount` урезан; повтор **сразу** → `granted = 0.0092` (кламп скорости Δ≈0 работает). `mined` = сумма. (`belt_mining_handlers.go:90-108, 493-636`). |
| 7 | **PASS** | `POST /api/belt/mine/leave` → **200**, `position = {orbit, belt, object_id}`, ключа `mined` нет; буфер перелит в трюм: `GET /api/cargo` и БД `player_cargo` дали `good_id 21, quantity = 1.6676266475798618`; `system_belts.iron_remaining` уменьшился 995.05 → 993.38 (`belt_mining_handlers.go:649-718`). |
| 8 | **PASS** | `iron_remaining = 0` + `enter` → **400** `{"error":"Пояс выработан"}` (не 500, не успех); позиция осталась `orbit/belt` (`belt_mining_handlers.go:411-414`). |
| 9 | **PASS** | Трюм полон (`used = 100/100`, `free = 0`) + `enter` → **400** `{"error":"Трюм полон"}` (`belt_mining_handlers.go:423-426`). |
| 10 | **PASS** | Игрок B (тот же мир, радар 800) видит A в `orbit/belt` (`uidA_seen = True` — позитивный контроль), после `enter` A (`status="mining"`) — **не видит** (`uidA_seen = False`, `players:[]`). Guard `players_positions.go:150-153` + фолбэк `:221-223`. |
| 11 | **PASS** | Из `mining` (после `collect`, `mined = 1.6584`) `POST /api/intrasystem-flight {object_type:"star"}` → **202**, `from_type:"belt"`, `from_id` = пояс. Буфер перелит: `used` 1.6676 → **3.3260** (+1.6584, ровно `mined`); `current_position = {in_flight, from belt→star}`, ключа `mined` нет. Повторный `leave` (no-op, `added:0`) трюм **не** увеличил — двойного зачёта нет (`intrasystem_handlers.go:607-620, 640-654`; `belt_mining_handlers.go:740-763`). |
| 12 | **FAIL** | В рабочем дереве есть правки, **не относящиеся к 3a** (чужие зоны): `cmd/art-studio/*` (5 файлов, ship_prompt/ships_job/ship.go), `cmd/art-studio/postproc/ship_cut_tol_test.go` (untracked), `tools/test_ship_sprite_cut.py`, `docs/PITFALLS.md` (+75/−), `docs/gamedesign/ideas/*` (4 идеи), `docs/specs/README.md`. См. «Найденное». |

Покрытие серверных тестов M1–M21 — все на месте и зелёные:
`TestBeltMineEnter/…NotAtBelt/…Idempotent/…Depleted/…CargoFull/…NoDataReserve/
…NoGoodRollsBack/…IdempotentHidden`, `TestBeltCollectClampRate/…Reserve/…Cargo/
…DtMax`, `TestBeltCollectConcurrentRateClamp`, `TestBeltMineConcurrentReserve`,
`TestBeltMineLeave/…LeaveNoOp/…Restore/…SystemBeltCascade`,
`TestMigrationIronRemaining`, `TestNormalizeMyPositionMining`,
`TestStartIntraFlightFromMining`, `TestStartTravelFromMining`,
`TestMiningHiddenFromPlayersPositions`, `TestBeltKnowledgeGate`.

---

## Найденное

1. **[п.12, FAIL] Посторонние правки в рабочем дереве.** Помимо файлов 3a
   (миграция, `belt_mining_handlers*.go`, `current_position.go`, `planet.go`,
   `planet_repo.go`, `cargo.go`, `planet_visibility.go`,
   `intrasystem_handlers.go`, `travel_handlers.go`, `players_positions.go`,
   `main.go`, тесты, `COORDINATION.md`) изменены:
   `cmd/art-studio/generator/{ship_prompt.go,ship_prompt_test.go,ships_job.go}`,
   `cmd/art-studio/handlers/ships_handlers_test.go`,
   `cmd/art-studio/postproc/ship.go`, `tools/test_ship_sprite_cut.py`,
   `docs/PITFALLS.md`, `docs/gamedesign/10_exploration.md`,
   `docs/gamedesign/ideas/*` (4 шт.), `docs/specs/README.md`; untracked —
   `cmd/art-studio/postproc/ship_cut_tol_test.go`,
   `docs/specs/2026-09-22-эффекты-снабжения-задержка-голод.md`,
   `docs/gamedesign/ideas/2026-09-22_{деплой-красная-сборка-amvera,эффекты-снабжения-задержка-голод}.md`.
   **Не дефект 3a:** это чужие зоны (арт-студия, идеи) в общей рабочей копии;
   риск — при коммите «свалить» их в один коммит 3a. Лечится явным `git add`
   только файлов 3a (решение @manager).
2. **[два файла 3a вне списка задачи, кода не касается]** Изменены
   `internal/repository/player_intrasystem_flight_repository.go`
   (добавлены `StartAtomicTx`/`CancelAtomicWithDestinationTx` — нужны для перелива
   буфера в одной транзакции со стартом полёта, §6.5) и
   `internal/handlers/belt_visibility_test.go` (колонка `iron_remaining` в
   sqlmock). Оба **относятся к 3a**, просто не названы в списке задачи — не баг.
3. **[наблюдение, не баг]** Файла `internal/models/current_position_test.go`
   нет, хотя спека §12.1 его перечисляет (M16 `TestBeltMineRestore` фактически
   живёт в `belt_mining_handlers_test.go` и делает JSON round-trip). Покрытие не
   потеряно; расхождение «спека ↔ имя файла теста».
4. Клиент e2e-смоук (спека §12.2) не запускался: клиента 3b ещё нет.

---

## Предложения в чек-лист

- **Новый кейс (сервер, ядро 3a):** «`enter` при `iron_remaining IS NULL` лениво
  инициализирует запас `600·k_iron` и отдаёт `remaining_level`; повторный `enter`
  запас не пере-инициализирует» — проверено живым БД-прогоном (п.5, M3/M19).
- **Новый кейс:** «кламп скорости точен: `granted == rate_cap · min(Δt, 2)`;
  два `collect` подряд в одну секунду не дают двойного потолка» (п.6).
- **Новый кейс:** «взлёт из захода (`/api/intrasystem-flight`) переливает буфер
  ровно один раз; повторный `leave` после взлёта не доначисляет» (п.11).
- **Новый кейс:** «видимость захода: позитивный контроль `orbit/belt` виден,
  `mining` скрыт» (п.10; без позитивного контроля кейс ложно-зелёный — учтено).
- **Устаревших кейсов не выявлено.**

---

## Чеклист непроверяемого

- **Клиентский флоу 3b** (`web/belt.html`, `web/static/js/belt/*`, кнопка
  «⛏ Добывать», HUD, экраны, рефреш во время захода глазами игрока, экран
  «трюм полон») — **не проверял: клиента ещё нет** (подэтап 3b). e2e-смоук
  `tools/e2e/belt-mining-check.js` не создан. Нужно: разработка 3b + UI-спека,
  затем обязательный браузерный прогон.
- **Гонки под нагрузкой (реальные параллельные `collect` на живом сервере,
  M12/M13) ** — не воспроизводил многопоточный сценарий вживую; покрыто
  юнит-тестами `TestBeltMineConcurrentReserve` и `TestBeltCollectConcurrentRateClamp`
  (sqlmock), порядок локов «пояс → игрок» проверен по коду
  (`belt_mining_handlers.go:550-586`). Нужно: нагрузочный прогон, если создатель
  захочет независимую проверку (риск AGENTS §0).
- **Рестарт сервера с `mining`-позицией (M16) на живом сервере** — не
  перезапускал dev-сервер (общая БД/чужие агенты); покрыто юнит-тестом
  `TestBeltMineRestore`. Нужно: отдельное окно перезапуска.
- **Каскад/Пакман (M18, удаление мира чистит `system_belts`, не трогает
  `player_cargo`)** — живым удалением мира не проверял (риск задеть чужие
  данные); покрыто `TestBeltMineSystemBeltCascade` + юнит-сторож. Нужно: при
  согласованном окне очистки.
- **Числа баланса** (`R_base`, `reserve = 600·k_iron`, `iron_ref = 0.30`,
  пороги классов/уровней) — зона `@balancetester`, не моя; спека §8.
- **Гейт знания `belt_class`/`remaining_level` глазами игрока без знания пояса** —
  проверен юнит-тестом `TestBeltKnowledgeGate`; визуально-модального отображения
  нет (3b).
