# QA-прогон: Трюм — грузоподъёмность корабля

**Дата:** 2026-09-22
**Тестировщик:** @tester
**Спека:** `docs/specs/2026-09-22-трюм-грузоподъёмность-корабля.md` (§4–§9, §11, §14)
**Вердикт:** ✅ ПРОЙДЕНО (12/12 PASS)

---

## Прогнал

```
go build ./...                          → exit 0
go vet ./...                            → exit 0
go test -race -short (без generator/planet|galaxy) → exit 0 (все ok)
go test ./internal/ship/ -run Cargo -v  → PASS (8 подтестов)
go test ./internal/cargo/ -v            → PASS (14 тестов)
go test ./internal/handlers/ -run TestTruncateTablesCoverMigrationFK → PASS
powershell -File tools/db.ps1 -Sql ...  → миграция/объекты/сид/бэкфилл
Invoke-RestMethod /api/cargo, /api/cargo/jettison, /me
cd tools/e2e; node dashboard-cargo-check.js → RESULT: ALL PASS (exit 0)
cd tools/e2e; node map-check.js             → RESULT: ALL PASS (exit 0)
```

Временный интеграционный тест `internal/cargo/qa_integration_test.go` (кламп/инвариант/нулевые строки
на реальной БД) — прогнан, **удалён** после прогона (не коммитится).

---

## Сценарии (жёсткий чек-лист)

1. **PASS** — миграция `000068` применена: `schema_migrations` содержит `000068`; `player_cargo` (PK `(user_id,good_id)`, FK `users`/`goods` CASCADE, CHECK `quantity >= 0`), `equipment.type='cargo'` (`cargo_1`, `{"capacity":80}`), `ship_models.base_capacity=20` + `slots.universal=1`, бэкфилл `users.equipment.universal='cargo_1'` (535 игроков).
2. **PASS** — ёмкость: стартовый корабль `total=100` (20 врождённая + 80 модуль); без модуля `20`; битый/чужой/неизвестный модуль → только врождённая 20 (не бесконечность) — `TestCargoCapacityMass` (6 подтестов) + `TestCargoCapacityMassDefaults`.
3. **PASS** — `GET /api/cargo`: без токена 401; с токеном форма `limits.mass {used,total}` + `items[]`; у нового игрока `0 / 100`, `items: []`.
4. **PASS** — инвариант `used ≤ total`: наполнение до 100, попытка добавить ещё → 0 принято; `used` никогда не превышает `total` (интеграционный тест на реальной БД).
5. **PASS** — жёсткий кламп: при 95/100 запрос 20 → принято 5, остаток не списан (интеграционный тест `TryAddCargo`; юнит `TestTryAddCargoClamps`).
6. **PASS** — «груз за борт»: частичный сброс 40→25 уменьшает занятое; `all` товара 25→0; `all` трюма (2 товара) → 0; на пустом трюме корректный no-op (200, `0/100`); сброшенное нигде не всплывает (объекта в космосе нет — только DELETE строк).
7. **PASS** — валидация: неизвестный `good_id` → 404; `quantity=0`/`-5` → 400; битый JSON → 400; без `good_id` → 400; без токена → 401.
8. **PASS** — нулевые строки: `TakeCargo` до нуля удаляет строку (`COUNT=0`); `JettisonCargo`/`JettisonAll` тоже удаляют — в БД не остаётся `quantity=0`.
9. **PASS** — вайп вселенной: `player_cargo` отсутствует в `truncateTables` (`admin_universe.go:60`); сторож `TestTruncateTablesCoverMigrationFK` PASS (нет FK на усекаемые — `player_cargo` ссылается только на `users`/`goods`); очистка не падает, трюм — состояние игрока.
10. **PASS** — регресс: `/me` отдаёт `equipment` (radar/scanner/engine/universal=cargo_1), `ship_catalog` (radar_1/scanner_1/engine_1/engine_fast/cargo_1), `ship_model` (slots + base_capacity=20); `go test ./internal/ship/ ./internal/handlers/` ok; полный `-race -short` ok.
11. **PASS** — e2e-смоук (реальный Chrome): `dashboard-cargo-check.js` ALL PASS — вкладка «Корабль», блок «Трюм» (пустое состояние «Трюм пуст.», полоса `0/100`, список, кнопки сброса), сброс по строке (`{good_id,quantity}`) и «выбросить всё» (`{all:true}`), **0 JS-ошибок, 0 тостов-ошибок, без разлогина**; XSS-имя `<img src=x onerror=alert(1)>` рендерится текстом (`nameTagCount=0`). `map-check.js` ALL PASS. Скриншоты: `tools/e2e/artifacts/dashboard-cargo-empty.png`, `dashboard-cargo-populated.png`, `map.png`, `restricted-card.png`.
12. **PASS** — серверно-авторитетность: player-facing ручек «положить/взять» нет (только `GET /api/cargo` + `POST /api/cargo/jettison`, `main.go:381-382`); клиент не перемножает вес — `cargo.js` берёт `mass`/`used`/`total` из ответа сервера.

---

## Найденное

Дефектов нет.

---

## Предложения в чек-лист

- **Новый кейс (ядро трюма):** «кламп при переполнении — остаток не списывается у источника» (п.5) — проверяется только интеграционно/через сервис, player-facing ручки нет; закрепить как регрессионный кейс `internal/cargo`.
- **Новый кейс:** «нулевые строки удаляются» (п.8) — `TakeCargo`/`Jettison` до нуля → `COUNT=0`.
- **Новый кейс:** «вайп вселенной не трогает `player_cargo`» (п.9) — сторож `TestTruncateTablesCoverMigrationFK` + отсутствие в `truncateTables`.
- **Новый кейс (e2e):** `dashboard-cargo-check.js` — блок «Трюм» + XSS-имя текстом + отсутствие JS-ошибок/разлогина.
- **Устаревших кейсов не выявлено.**

---

## Чеклист непроверяемого

- **Визуальная эстетика блока «Трюм»** (иконки, сортировка, подтверждение сброса) — вкусовое решение; спека §9.3 явно оставляет это на будущий шаг `@uidesigner`. Нужно: решение создателя, если захочет богаче.
- **Реальные веса каталога** (`goods.weight` ≠ 1) — в dev-БД все товары `weight=1`; калибровка весов — отдельная задача (§6). Нужно: калибровка каталога студии.
- **`goods.weight = 0`** (риск «бесконечного» трюма, §6) — нет CHECK `> 0`, в данных такого товара нет; экстремальный вход недостижим штатно. Нужно: CHECK при следующей правке каталога.
- **CASCADE при удалении товара из студии** (решение №7) — не проверял живым удалением ресурса (риск задеть чужие данные); покрыто юнит-тестом `TestPlayerCargoCascadeFKs`. Нужно: отдельный прогон студии, если потребуется.
- **Числа баланса** (20/80/100, шаг ×2.5) — зона `@balancetester`, не моя.
