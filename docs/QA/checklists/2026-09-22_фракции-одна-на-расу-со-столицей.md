# QA-прогон 2026-09-22 — Фракции: одна на расу со столицей

**Дата:** 2026-09-22
**Задача:** независимая проверка незакоммиченного изменения «фракции: одна на расу со столицей».
**Идея-источник:** `docs/gamedesign/ideas/2026-09-22_фракции-одна-на-расу-со-столицей.md`.
**Затронуто (по задаче):** `internal/generator/faction/faction.go`, `internal/generator/faction/faction_capitals_test.go`, `internal/handlers/admin_universe.go` (`GenerateFactions`), `internal/handlers/admin_factions_test.go`, `migrations/000066_factions_race.sql`.
**Окружение:** Windows, Go 1.27.0, gcc WinLibs (в PATH), PostgreSQL 15/Redis локально (не трогал).
**Не коммитил. Файлы не правил. Рабочую БД не трогал.**

> **Важно (состояние дерева).** В рабочем дереве одновременно живёт крупная чужая незакоммиченная правка (`internal/repository/branch_repository*.go`, `internal/economy/settlement/branch.go`, `internal/goodsstudio/seed_producers.go`, `internal/handlers/admin_hypothesis.go`, `internal/models/economy.go`, `000067_settlement_type.sql` и др.). Она ломает `go vet` и часть тестов — **вне зоны проверяемого изменения** (см. пп.2–3).

## Чек-лист (10 пунктов — каждый ровно один раз)

| # | Пункт | Вердикт | Доказательство |
|---|---|---|---|
| 1 | `go build ./...` | **PASS** | exit 0 |
| 2 | `go vet ./...` | **FAIL** | exit 1. Единственная причина — `internal/repository/branch_repository_test.go:115` (и ещё 5 вызовов): `not enough arguments in call to NewBranchRepository(db).SyncBranches` — have `([]string, map[string]float64, map[string]string, time.Time)`, want `(..., map[string]map[string]float64, ...)`. Это чужая правка `branch_repository.go` (её тест не догнали). **Проверяемые пакеты (`internal/generator/faction`, `internal/handlers`) — vet-чистые** (ошибка только в `repository`) |
| 3 | `go test -race -short` (все пакеты, кроме `generator/planet\|galaxy`) | **FAIL** | exit 1. Красные пакеты: `internal/repository` **[build failed]** (тот же `branch_repository_test.go`), `internal/economy/settlement` (`TestProcessBranch*` 7 шт.), `internal/goodsstudio` (`TestSeedProducersContent`), `internal/handlers` (~60 кейсов: `TestContract*`, `TestBelt*`, `TestMortalityPreview*`, `TestRunHypothesis*`, `TestPlanetImage*`, `TestArrival*`, `TestGetPlanetsByWorld*` — все падают на несовпадении SQL-мока `settlements` с новым `settlement_type_id`, т.е. чужая правка типов поселений). **Зелёные: `zorion/internal/generator/faction` = ok; фракционные тесты `internal/handlers` — PASS** (см. п.9) |
| 4 | Ровно одна фракция на расу (`settlements.race_id`) | **PASS** | `faction.go:37-99` — группировка по `race_id` (map `homeworlds`), затем одна вставка на расу. `TestGenerateFactionsOneFactionPerRace` PASS: 2 расы (`humans` с 2 поселениями + `saltfolk`) → `factions=2`. `TestGenerateFactionsOnlySettledRaces` PASS |
| 5 | Идемпотентность: повторный прогон не создаёт дублей | **PASS** | `faction.go:77,90-92` — `existingRaceIDs` + пропуск известных `race_id`; `saveFaction` под частичным UNIQUE `uq_factions_race` (миграция). `TestGenerateFactionsIdempotent` PASS: второй прогон `factions=0`. `EnsureCapitals` идемпотентен (`NOT EXISTS` + `ON CONFLICT DO NOTHING`): `TestEnsureCapitalsIdempotent` PASS |
| 6 | Родная планета = крупнейшее поселение расы (tiebreak детерминирован) | **PASS** | `faction.go:119-124` `betterHomeworld`: больше население → лучше; при равенстве — меньший `planet_id` (лексикографически). `TestGenerateFactionsHomeworldTiebreak` PASS: `p2`/`p1` по 100 → выбран `p1`. `TestGenerateFactionsOneFactionPerRace` — `humans` p1=100/p2=500 → `p2` |
| 7 | Раса без поселений пропускается | **PASS** | `faction.go:42` — кандидаты только `race_id IS NOT NULL AND race_id <> '' AND population > 0`; расы без поселений в выборке нет → фракция не создаётся. `TestGenerateFactionsOnlySettledRaces` PASS. Ветка «кандидатов нет»: `TestGenerateFactionsNoRacesStillEnsuresCapitals` PASS (0 фракций, столицы добираются) |
| 8 | Миграция `000066_factions_race.sql` корректна и идемпотентна | **SKIP** | На БД **не применял** (см. стоп-условие / рабочую БД не трогаю, scratch-БД под рукой нет). По тексту — корректна: `ADD COLUMN IF NOT EXISTS race_id TEXT`; `CREATE UNIQUE INDEX IF NOT EXISTS uq_factions_race ON factions(race_id) WHERE race_id IS NOT NULL` — частичный UNIQUE допускает легаси-строки с `NULL`, повторный прогон безопасен |
| 9 | Регрессии: `ResolvePublicationPlanet` и `attachFactionsAndBuildings` | **PASS** (по коду) | `contract_repository.go` в диффе **не изменён**; `ResolvePublicationPlanet:686-697` по-прежнему `SELECT homeworld_id FROM factions WHERE id=$1`. `attachFactionsAndBuildings` (`planet_repo.go:~350`) — `FROM factions WHERE homeworld_id = ANY($1) ORDER BY name ASC`, не затронут (правка `planet_repo.go` — про ветки). ⚠️ `TestResolvePublicationPlanet` **не прогнан**: пакет `internal/repository` не собирается из-за чужого `branch_repository_test.go` |
| 10 | Живой прогон генерации ×2 на dev-БД | **SKIP** | Не запускал: сработало стоп-условие по пп.2–3; состояние сервера/чужих джобов не выяснял, БД не трогал. Нужно отдельное окно без чужих джобов от создателя |

**Итог: 6 PASS / 2 FAIL / 2 SKIP.** Функциональное ядро изменения (пп.4–7, 9) работает и покрыто проходящими тестами; **DoD (`vet`+полный `test -race -short`) красный из-за чужих параллельных правок (типы поселений + ветки), а не из-за фракций.**

## Стоп-условие

Пункты 2 и 3 — FAIL → по инструкции остановился: динамические проверки с БД (п.8 применение миграции, п.10 живой прогон) не выполнял. Пункты 4–7, 9 закрыты кодом и уже прогнанными тестами пакета `faction` (отдельного прогона требуют только 8 и 10).

## Найденное

1. **[блокер DoD, вне зоны фракций] `go vet ./...` и `go test` красные** из-за незакоммиченной чужой правки `internal/repository/branch_repository.go` — сигнатура `SyncBranches` изменена, тест `branch_repository_test.go` не обновлён (6 вызовов). Файл: `internal/repository/branch_repository_test.go:115,240,262,287,308`.
2. **[блокер DoD, вне зоны фракций] ~60 кейсов `internal/handlers`, `internal/economy/settlement`, `internal/goodsstudio`** падают на новом `settlement_type_id` / `producer_types` (чужая правка типов поселений, `000067_settlement_type.sql`, `seed_producers.go`). Пример: `belt_visibility_test.go:153` — реальный SQL `... s.settlement_type_id, pt.name ...` не матчит мок.
3. **[инфо] Фракционные тесты зелёные:** `TestGenerateFactionsOneFactionPerRace`, `…HomeworldTiebreak`, `…Idempotent`, `…OnlySettledRaces`, `…NoRacesStillEnsuresCapitals`, `TestEnsureCapitals*` — PASS (`go test -race -short -run Faction ./internal/generator/faction ./internal/handlers`).
4. **[инфо, не баг] `EnsureCapitals` вызывается даже при 0 рас** (`faction.go:101-105`) — намеренно, для догона легаси-фракций без столиц (комментарий).

## Не проверено / границы покрытия

- **Миграция `000066` на живой БД** (п.8) — не применял: нет scratch-БД, рабочую не трогаю, стоп-условие. Нужно: поднять временную БД либо дать окно и подтвердить применение без ошибок.
- **Живая генерация фракций ×2 (п.10)** — не гонял: стоп-условие + запрет поверх чужих джобов (AGENTS §16). Нужно окно без чужих джобов, чтобы зафиксировать «≈29 фракций/столиц, у второго прогона 0 новых».
- **`TestResolvePublicationPlanet`** — не исполнен (пакет не собирается). Нужно: сначала починить чужой `branch_repository_test.go` (задача @developer/@manager), затем прогнать.
- **Полный `go test -race -short` на чистом дереве** — недостижим, пока в дереве висит чужая красная правка; красный DoD приписан ей, не фракциям.
- **Вкусовое/UX** (имя фракции = название расы, цвет/тип случайны) — вне автоматической проверки, решение создателя.

## Предложения в чек-лист (применит @manager)

- Новый кейс (B-FACTION-ONE-PER-RACE): одна фракция на заселённую расу, родная планета — крупнейшее поселение расы, tiebreak — меньший `planet_id`. Прогон: `go test -run GenerateFactions ./internal/generator/faction`.
- Новый кейс (B-FACTION-IDEMPOTENT): повторный прогон — 0 новых фракций, столицы не дублируются (`NOT EXISTS` + `ON CONFLICT DO NOTHING`). Прогон: `go test -run 'GenerateFactionsIdempotent\|EnsureCapitalsIdempotent' ./internal/generator/faction`.
- Новый кейс (B-FACTION-SKIP-UNSETTLED): раса без поселений фракции не создаёт; при 0 рас — 0 фракций, столицы добираются. Прогон: `go test -run 'OnlySettledRaces\|NoRacesStillEnsuresCapitals' ./internal/generator/faction`.
- Регресс-кейс (B-FACTION-PUBLICATION-PLANET): фракция-автор → `factions.homeworld_id` (`ResolvePublicationPlanet`), фракции на карточке планеты (`attachFactionsAndBuildings`). Прогон: `go test -run ResolvePublicationPlanet ./internal/repository`.
- Устаревший кейс: старый «фракции = 1–3 на обитаемую планету» (карточка `admin_factions_test.go` про число планет) — заменён на «число заселённых рас».
