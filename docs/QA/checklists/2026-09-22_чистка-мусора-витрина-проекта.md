# QA-прогон 2026-09-22 — Чистка мёртвого/отладочного мусора («спрятать всё старое», витрина проекта на GitHub)

**Дата:** 2026-09-22
**Идея:** `docs/gamedesign/ideas/2026-09-22_витрина-проекта-на-github-портфолио.md` §3.1
**Статус разработки:** не закоммичено — проверялось текущее рабочее дерево
**Окружение:** Windows, Go 1.27.0, gcc MinGW-W64 16.1.0 (WinGet-каталог, `CGO_ENABLED=1`), PostgreSQL 15 локально, Redis `6379`

**Заявлено:** удалены `pkg/simulation/batch/*` (4), `pkg/worldgen/*` (3), `internal/core/location_batch.go`, `goods_msg{,2,3}.json`; из `scripts/check_impact_map.ps1` снято исключение `'goods_msg'`; 40 `data/planet_images/v4_*.png` выведены из индекса (`git rm --cached`) + `data/planet_images/` в `.gitignore`.

## Прогнал (жёсткий чек-лист, каждый пункт один раз)

| # | Проверка | Итог |
|---|----------|------|
| 1 | `git ls-files` — нет путей `pkg/`, `internal/core/`, `goods_msg` в индексе | **FAIL** |
| 2 | `go list ./...` — нет `pkg/...` и `internal/core` | PASS |
| 3 | `rg -n "pkg/simulation\|pkg/worldgen\|internal/core\|your-project" --glob '!docs/**' .` | PASS (0 хитов) |
| 4 | `Get-ChildItem data\planet_images` — 154 файла | PASS |
| 5 | `git status --short` — состав изменений | PASS |
| 6 | `go build ./...` | PASS (rc 0) |
| 7 | `go vet ./...` | PASS (rc 0) |
| 8 | `go test -race -short (go list ./... \| ? { $_ -notmatch 'generator[/\\](planet\|galaxy)$' })` | PASS (rc 0) |
| 9 | `scripts/check_impact_map.ps1` | PASS (`ERROR: 0, WARNING: 12`) |
| 10 | Сервер стартует, `/health` → `OK`, остановлен | PASS (с оговоркой) |
| 11 | `data/planet_images` существует и доступен на запись | PASS |

**ИТОГ: PASS 10/11, FAIL: [1]**

## Детали по пунктам

### 1. FAIL — индекс git всё ещё содержит удаляемые пути

`git ls-files` выполнить не удалось: правило прав сессии запрещает произвольные `git`-команды
(разрешены только `git status`, `git diff`, `git log`). Пункт оценён связкой
`git status --short` + `git diff --cached --name-only`.

- В индексе **застейджено только 40 png** (`git diff --cached --name-only` = 40 × `data/planet_images/v4_*.png`).
- `pkg/simulation/batch/*` (4), `pkg/worldgen/*` (3), `internal/core/location_batch.go`,
  `goods_msg{,2,3}.json` показаны как **` D`** — удаление только в рабочем дереве, в индексе
  пути остались. Значит `git ls-files` их перечислит → индекс не чист.
- Итог: из индекса убраны только картинки; исходники/JSON удалены с диска, но не из индекса
  (расстайджинг `git add -A` / `git rm` по ним ещё не сделан).

### 2. PASS

`go list ./...` — 36 пакетов, ни одного `pkg/...` / `internal/core`; rc 0.

### 3. PASS

`rg` по коду (кроме `docs/`) по шаблонам `pkg/simulation|pkg/worldgen|internal/core|your-project`
— 0 совпадений (exit 1).

### 4. PASS

`data\planet_images` — ровно **154** файла (диск-кэш с диска не удалялся, совпадает с заявленным).

### 5. PASS

- **40 png** — `D ` (удаления из индекса, `git rm --cached`) ✅.
- **M вне заявленного объёма:**
  - `scripts/check_impact_map.ps1` — M (из заявленного объёма);
  - `.gitignore` — M (из заявленного объёма; `+data/planet_images/`);
  - `docs/COORDINATION.md` — M (+1 строка, регистрация зоны @developer; вне заявленного,
    но это координационный док, **не** горячий).
- **Горячие доки:** `AGENTS.md`, `STATUS.md`, `CHANGELOG.md`, `docs/INDEX.md`,
  `docs/gamedesign/99_roadmap.md`, `.opencode/context.md` — **ни один не изменён** ✅.
- **Новые untracked файлы:** только `docs/gamedesign/ideas/2026-09-22_витрина-проекта-на-github-портфолио.md`
  — это исходная идея задачи (легитимный док), не мусор.

### 6/7/8. PASS

`go build ./...` rc 0; `go vet ./...` rc 0; `go test -race -short` (без `generator/planet|galaxy`)
rc 0 — все 36 пакетов `ok`/`[no test files]` (gcc WinLibs 16.1.0 в PATH, `CGO_ENABLED=1`).

### 9. PASS

`scripts/check_impact_map.ps1` → `ERROR: 0, WARNING: 12`. Жалоб на `goods_msg` или удалённые
пути нет (12 WARNING — «возможно, новая горячая точка» по несвязанным докам/конфигам).

### 10. PASS (с оговоркой)

На момент прогона порт 8080 уже слушал `zorion-server-qa.exe` (PID 24640, `/health` → 200).
Процесс остановлен, запущен `go run ./cmd/server` (env `DATABASE_URL`/`REDIS_URL` из AGENTS.md §1,
`JWT_SECRET` не задавался — из `.env`): `/health` → 200, тело `OK`; сервер остановлен,
порт 8080 — down. Админ-джобы/запись в БД не запускались.

Оговорка: буквальная команда пункта `go run cmd/server/main.go` **падает** на компиляции
(`cmd/server/main.go:129:2: undefined: bootstrapSkycomposer`) — это документированная ловушка
AGENTS.md §2 (в `cmd/server` несколько файлов, запускать надо пакет `./cmd/server`).
Не дефект кода; исправление — в чек-лист (см. ниже).

### 11. PASS

`data/planet_images` существует; создание и удаление временного файла `.qa_write_test.tmp`
внутри каталога прошло успешно — каталог доступен на запись; временный файл убран.

## Найденное

1. **[незастейдженные удаления, блокирует цель «спрятать старое»] DELETED-пути остались в индексе.**
   `pkg/simulation/batch/*`, `pkg/worldgen/*`, `internal/core/location_batch.go`,
   `goods_msg{,2,3}.json` удалены только из рабочего дерева (` D`), в индексе git они ещё есть.
   Пока не сделан `git add`/`git rm` по ним, в коммит войдут только PNG — цель §3.1 по исходникам
   не достигнута. Диагностику/починку не выполнял (зона @developer/@manager).

## Не проверено (граница покрытия)

- **Пункт 1 через буквальный `git ls-files`** — команда запрещена правами сессии; вывод сделан
  по `git status --short` + `git diff --cached`. Достоверность высокая (состояние индекса видно
  однозначно), но прямой листинг индекса не получен.
- **Содержимое удалённых файлов** (что именно было в `pkg/*`, `internal/core/location_batch.go`,
  `goods_msg*.json` и не тянул ли их кто-либо) — не смотрел: код удалён из дерева, история —
  зона @developer.
- **Полный `git ls-files` на прочие нежелательные пути** (мусор за рамками перечисленных префиксов) —
  вне объёма чек-листа.
- **Визуал/UX витрины на GitHub** (как выглядит репозиторий после скрытия) — вкусовое, за создателем.

## Предложения в чек-лист (применит @manager)

- Новый регресс-кейс: после «чистки мёртвого» `git ls-files` не содержит `pkg/`, `internal/core/`,
  `goods_msg*` — проверять **и индекс, и рабочее дерево** (в этом прогоне индекс не очищен).
- Исправить формулировку пункта «поднять dev-сервер»: команда `go run cmd/server/main.go`
  нерабочая (документировано AGENTS.md §2) — правильная `go run ./cmd/server` (пакет).
