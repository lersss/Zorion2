# QA-прогон 2026-09-23 — пояса малых тел, подэтап 3e: «два стика» + бесконечный вдоль кольца пояс

> Прогон выполнил `@tester`. Файл журнала записан менеджером дословно по отчёту
> тестера: сессия тестера была остановлена сторожем зацикливания (семейство команд
> `tools/db.ps1`, 17 прогонов) и запись файла была перехвачена.
> Правка **чисто клиентская**, сервер не менялся; Go DoD не гонялся (Go не трогался).

**Предмет:** `web/static/js/belt/*` (управление «два стика», физика, стриминг бесконечного
пояса, добыча по наведению, HUD), `web/belt.html`, `web/static/css/belt.css`, новый e2e
`tools/e2e/belt-mining-check.js`. Состояние: working tree, не закоммичено.

**ИТОГО: 8/8 PASS. Новых дефектов нет.**

## Пункты чек-листа (жёсткий, каждый ровно один раз)

1. **PASS** — e2e-смоук: `npm.cmd i` + `node belt-mining-check.js` = **12/12 PASS** без токена
   (live-сцена SKIP); с `QA_TOKEN`+`QA_BELT_ID` = **13/13 PASS** (live scene PASS, 0 ошибок);
   пункты 08–19 все PASS.
2. **PASS** — Живой заход: карта → модалка мира Gilovpis → ПКМ по **строке пояса** → меню
   «⛏ Продолжить добычу» → клик → открылся `/belt.html?belt=93da4bf2…`; сцена отрисована
   (canvas + HUD + непустой растр, «Пояс Innorir»); `pageerror`=0, в консоли только
   `favicon.ico` 404 (pre-existing, не код пояса). ПКМ по кольцу канваса не кликал (тот же
   обработчик `modal/events.js:315-316`).
3. **PASS** — Управление: e2e `10` W плавно (v1=0.17 vs v60=258.6, throttle 0.963→0.000);
   e2e `09` A/D стрейф без доворота (y=53.4, heading=0.0000); e2e `08` мышь плавно
   (h1=0.010 → за 0.5 с h2=1.589 ≈ target 1.571); Shift — живая подсветка чипа `brake`
   (численное затухание не мерил); Esc — `#pause` показан/скрыт (живой прогон); живая
   разводка W/D/мышь/ЛКМ/Space/Shift по классам легенды корректна.
4. **PASS** — Добыча по наведению: живой — нос на жилу + ЛКМ → «⛏ Добыча идёт…», «Добыто за
   заход» 0 → 0.04 т; e2e `11` (нос на жиле / в пустоту / за `EXTRACT_RADIUS`), `12`
   Space=ЛКМ, `13` ПКМ подавлена (`defaultPrevented=true`).
5. **PASS** — Бесконечность вдоль кольца: e2e `14` (x=20k/60k: 21/23 тела, память 25 ≤ cap
   120), `16` (`_trim` режет: 6 ≤ 6); доп. замер 0…120000 м.е.: max asteroids **31**
   (cap 120), debris **43**, dust **5** (cap 60), минимум тел у корабля **11** — не
   кончаются, память не растёт. `Math.random` в `belt/*` нет.
6. **PASS** — Край поперёк: e2e `15` (за краем 0 тел, `vy` сохранён — стенки нет, возврат
   23); e2e `18` (декор/пыль гаснут по мировой полосе, overDecor=0 / overDust=0); живой
   полёт поперёк ~9 с → «Пояс позади — развернитесь к полосе», возврат → норма, 0 pageerror.
7. **PASS** — Выход: «Вернуться на карту» → `/map`; повторный заход открывается;
   детерминизм — e2e `17` (тот же seed → те же тела, другой seed → другой).
8. **PASS** — Регрессии: буфер ушёл в трюм (`player_cargo` good 21 = 0.0356 т), запас пояса
   297.0608 → 297.0252 (−0.0356); шкала = груз + буфер (`belt_ui.js:84-99`); HUD/легенда/
   кнопка на месте (e2e `19` + живая); выработанный пояс: строка «Запас железа: выработан»,
   клик «Добывать» сцену не открывает, `enter` → 400 `{"error":"Пояс выработан"}`.

## Команды

```powershell
cd tools/e2e; npm.cmd i; node belt-mining-check.js                 # 12/12 PASS (live SKIP)
$env:QA_TOKEN="<JWT>"; $env:QA_BELT_ID="93da4bf2-…"; node belt-mining-check.js   # 13/13 PASS
# временные драйверы (вне репо): belt-qa-3e.js, belt-qa-keys.js, belt-qa-edge.js,
# belt-qa-depleted.js, belt-qa-mem.js  (C:\Users\admin\AppData\Local\Temp\opencode\)
# SQL: tools/db.ps1 — подготовка позиции тест-игрока, сверка player_cargo/iron_remaining
```

## Находки

- Новых дефектов нет.
- Наблюдение (не дефект 3e): `console.error` от `GET /favicon.ico` 404 на `belt.html` —
  pre-existing.
- Известный отдельный баг (полный трюм → экран ошибки) — **не встретился**.
- Отступлений от §5.1/§5.2/§8.2/§12.4 в проверенном объёме нет; `nearestVein`/`FIELD_RADIUS`/
  `EDGE_SOFT` из `belt/*` убраны.

## Не проверено (границы покрытия)

ПКМ по кольцу на канвасе (тот же обработчик, не кликал по пикселю); численное затухание
Shift; «плавный доворот»/бесконечность глазами (мерил `heading` и дистанцию модульно);
выработанный пояс во время захода (нет пояса, исчерпаемого за разумное время); полный
трюм; вкусовые решения.

## Уборка

Тест-игрок `qa_belt3e_…` оставлен (scratch); пояс Delsenti `150c1c48` возвращён к
`iron_remaining=156.80847814391842`; временные драйверы — в temp; ничего не коммитилось.

## Предложения в чек-лист (применяет `@manager`)

Новая зона поясов/добычи (сейчас есть только B-SURF): **B-BELT-1…8** — e2e-смоук;
вход/выработанный; «два стика»; добыча по наведению; бесконечность + кап; край; выход +
детерминизм; регресс трюма. Дополнить **B-MAP-6** (e2e импортирует `belt/*` — косвенная
проверка синтаксиса).

## Ограничения прогона

Сторож исчерпал лимит на семействе `tools/db.ps1` и заблокировал инструменты тестера;
непроверенные хвосты (численное затухание Shift, ПКМ по кольцу пикселем, «выработанный
пояс в процессе») требуют продления лимита `.opencode/loop-guard.override.json` или
пояса с малым запасом.
