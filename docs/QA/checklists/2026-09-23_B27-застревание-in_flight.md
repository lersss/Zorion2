# Прогон 2026-09-23 — B27: застревание `in_flight` при рестарте (битая цель внутрисистемного полёта)

**Тестировщик:** @tester
**Способ:** независимая проверка логики на юнит-уровне (фейковый store), dev-сервер и живая БД не трогались (правка — логика старта).
**Проверяемые файлы:** `internal/travel/intrasystem_manager.go` (`RestoreIntra`), `cmd/server/main.go` (колбэк resetPosition, ~L282–310), `internal/travel/intrasystem_manager_test.go`.

## Жёсткий чек-лист

1. **PASS** — `go vet ./...` — чисто (exit 0). Полный `-race -short` прогон: `internal/travel`, `internal/handlers` (кроме фляка ниже) и остальные пакеты — ok. `go test ./internal/travel/ -run Restore -v` — все 8 тестов PASS (IntraRestoreFutureArrival/PastArrival/BrokenTarget/InterstellarWins/MovedSystem + Restore*).
   - ⚠️ Побочно: в полном `-race` прогоне один раз упал `TestWSHubBroadcast` (`internal/handlers/ws_notifier_test.go:63`, `read tcp ... i/o timeout`). В изоляции `-run TestWSHubBroadcast -count=1` — PASS. Фляк websocket-таймаута, к B27 не относится (не трогает travel/Restore).
2. **PASS** — `rg -n resetPosition`: единственный call-site — `internal/travel/intrasystem_manager.go:281-283`, внутри ветки битой цели (`validTarget == false`). Ветки «межзвёздная побеждает» (L259–264) и «игрок уже в другой системе» (L266–273) завершаются `continue` до L277 — reset не достижим.
3. **PASS** — `cmd/server/main.go:297-309`: `GetCurrentPosition` → ошибка → лог `⚠️ ... reset get position` + `return` (без паники); `pos != nil && pos.Status != "in_flight"` → `return` (no-op); иначе `UpdatePosition(StarOrbitPosition(worldID))`, ошибка → лог `⚠️ ... reset position`. `StarOrbitPosition` (`internal/models/current_position.go:50`) даёт валидную позицию `{status:"orbit", star}`.
4. **PASS** — независимая репродукция: временный `internal/travel/zz_b27_qa_test.go` со **своим** store (`qaB27Store`, не `newFakeIntraStore`), 3 кейса:
   - битая цель → колбэк вызван ровно раз `(u1, w1)`, `onArrival` не вызван, строка удалена — PASS;
   - есть межзвёздная → колбэк НЕ вызван — PASS;
   - игрок в другой системе (`w2` vs строка `w1`) → колбэк НЕ вызван — PASS.
   - временный файл удалён, `git status` в `internal/travel/` — только штатные изменения разработчика.
5. **PASS** — контроль не-тавтологичности: закомментировал вызов `resetPosition` в `intrasystem_manager.go` → `TestQAB27BrokenTargetCall` покраснел (`want 1 call (u1,w1), got []`), два «отрицательных» кейса остались зелёными. Вернул код как было (`git diff` — только дельта разработчика B27, без остатка правок).
6. **PASS** — `go vet ./...` exit 0 (см. п.1).

**Вердикт: ПРОЙДЕНО (6/6)**

## Итог

Ловит ли прежнее (застревание) поведение: **да** — при отключённом `resetPosition` битая цель оставляет игрока в `in_flight` без строки полёта, и именно этот случай даёт красный тест; с колбэком позиция сбрасывается на «орбиту звезды».

## Не проверено (граница покрытия)

- **Реальный рестарт сервера с битой целью в БД** (end-to-end: строка `player_intrasystem_flights` указывает на удалённую перегенерацией планету → рестарт → `/me` показывает орбиту) — не воспроизводилось: запрет трогать живую БД и рестартить dev-сервер; штатно битая цель в БД редка. Юнит-уровень и чтение кода закрывают логику; для полного e2e нужна подсадка строки в dev-БД создателем.
- **Ветка колбэка в `main.go` с реальным репозиторием** (`GetCurrentPosition` вернул `nil` vs `in_flight` vs иную позицию) — проверена чтением; интеграционного теста main.go нет, а БД-фейк для него не поднят.
- **Гонка «рестарт vs живой полёт»** (позицию успел обновить другой поток между `GetCurrentPosition` и `UpdatePosition`) — TOCTOU-окно в колбэке теоретически есть; проверить нельзя без конкурентного сценария с БД.
