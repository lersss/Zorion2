# QA-прогон: Корабли рас — П4 (выпил 21 легаси-спрайта)

**Дата:** 2026-09-23
**Фича:** корабли рас: раса агентов и игрока (`docs/specs/2026-09-23-корабли-рас-раса-агентов-и-игрока.md` §8)
**Подэтап:** П4 — выпил легаси-блока (21 спрайт, `ShipSprites`/`LegacyShipIconMap`/`DefaultShipIcon`)
**Вердикт:** 10/10 PASS, дефектов нет.

> Замечание прогона: запись файла отчёта сторожем зацикливания была заблокирована у тестера —
> текст перенесён менеджером без изменений.

## Чек-лист (каждый пункт один раз)

1. Нет ссылок на `ShipSprites`/`LegacyShipIconMap`/`DefaultShipIcon` в коде — **PASS** — `rg` по `internal/ cmd/ web/ tools/` (go/js/ps1/html/md): 0 совпадений.
2. Нет рантайм-ссылок на имена 21 легаси-PNG — **PASS** — в рантайме 0; остались тест-фикстуры, арт-скрипты `batch_concepts*.ps1`, незакоммиченный черновик `tmp-ship-check.js`.
3. `web/static/sprites/`: `neutral.png` есть; `race_*.png` = 117; легаси-21 нет — **PASS** — 117 + neutral = 118 PNG.
4. Иконка игрока резолвится; легаси `ship_icon` → людской корабль — **PASS** — регистрация → `race_humans_cruiser_02.png`; SQL-подмена на `crescent.png` и `ship_strela.svg` → `/me` отдаёт `race_humans_starship.png`, без падений.
5. `GET /me.ship_options` = 118 — **PASS** — 118 строк (117 `race_*` + `neutral.png`), без поля `race` = 0.
6. Агент рисуется кораблём своей расы — **PASS** — 4 живых агента; `methane_fungi`→`race_methane_fungi_02.png`, `sulfur_nests`→`race_sulfur_nests_05.png`, `sulfur_wanderers`→`race_sulfur_wanderers_02.png`, `awakened`→`race_awakened_01.png`.
7. Живые URL — **PASS** — `neutral.png`/`race_humans_starship.png` → 200; `boomerang.png`/`crescent.png`/`volcano.png` → 404.
8. e2e `map-check.js` — **PASS** — `ALL PASS`, exit 0, `pageErrors=0`.
9. Смоук `import_ship_sprites_smoke.ps1` — **PASS** — 18/18, exit 0.
10. DoD — **PASS** — `go build` (0), `go vet` (0), `go test -race -short` без generator/planet|galaxy (0), `go test -count=1 ./web/` (ok).

## Дефекты

Нет. Наблюдения (не баги): комментарий-история в `internal/models/ship_sprites.go`; `tools/e2e/tmp-ship-check.js` (незакоммиченный черновик с `crescent.png`); `tools/batch_concepts*.ps1` (арт-скрипты, не рантайм).

## Не проверено (границы покрытия)

- Живой агент-человек (у `humans` нет фракции/родного мира) — только функцией.
- Пиксельная визуальная сверка агентов на канвасе — приёмка создателя.
- Массовость: в dev-БД на момент прогона 4 агента (после П3 было 5400 — БД пересоздана другой сессией).

## Предложения в чек-лист (@manager)

- **B-SHIP-3 (П4):** нет рантайм-ссылок на легаси-идентификаторы/имена; диск 117+neutral, легаси-21 нет; URL 200/404.
- **B-SHIP-4 (П4):** `ship_options`=118; легаси `ship_icon` → `race_humans_starship.png`, не падает.
