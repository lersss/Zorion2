# Прогон QA — эпик «Мир прогулки», этапы Э0+Э1 (рецепты биомов)

**Дата:** 2026-09-23
**Спека:** `docs/specs/2026-09-23-мир-прогулки-рецепты-биомов.md` (§0 Э0 — данные/резолвер, Э1 — цвет и жизнь)
**Состояние кода:** рабочее дерево, **не закоммичено** (`config/biome_catalog.json`, `biome_catalog_seed.json`, `biome_catalog.go`, `biome_view.go`, `surface_handlers.go`, `admin_biome_catalog.go`, `planet_image_v2.go`, `surface_world.js`, `surface_render.js`, `surface_decor.js`)
**Сервер:** перезапущен на этом дереве (PID `39848`, `go run ./cmd/server`, `/health` → 200). Лог старта: `logs/qa_view_stderr.log`
**Артефакты (скриншоты):** `tools/e2e/artifacts/qa-view-{peski,jungle,forest,mountains,mountains_dead}.png` (день, погода «Штиль»)

---

## Вердикт: **ПРОЙДЕНО** (1 продуктовый дефект не найден)

Все 11 пунктов жёсткого чек-листа — PASS. Единственный FAIL в моём вспомогательном скрипте — ошибка ожидания самого теста, не продукта (см. §FAIL).

---

## Прогнал (команды)

| Что | Команда | Результат |
|---|---|---|
| Go build | `go build ./...` | EXIT 0 |
| Go vet | `go vet ./...` | EXIT 0 |
| Быстрый цикл | `go test -race -short (go list ./... \| ? { $_ -notmatch 'generator[/\\](planet\|galaxy)$' })` | EXIT 0, все пакеты `ok` |
| Surface-тесты | `go test -race -count=1 ./internal/handlers/` | EXIT 0 (`ok 19.1s`) |
| Генератор планет | `go test ./internal/generator/planet/` (без `-race`) | EXIT 0 (`ok 93.8s`) |
| JS-синтаксис | `node --check` по `surface/*.js`, `admin/biomeCatalog.js` | OK |
| Логика вида | `node tools/surface-view-check.mjs` | **29/29 PASS** |
| Частоты декора | temp-скрипт (не в репо) | кактус 30.3% / куст 46.9% / камень 15.7% / кость 7.2% |
| Пакет по биому | temp-скрипт через `POST /api/surface/land` (admin) | 45/46 PASS (1 — ошибка ожидания теста) |
| Админ-справочник | temp-скрипт GET/PATCH `/admin/biome-catalog` | **14/14 PASS** |
| e2e прогулки | `cd tools/e2e; node surface-walk-check.js` | **15 PASS / 0 FAIL**, pageErrors=0 |
| e2e карты | `cd tools/e2e; node map-check.js` | **ALL PASS** |

---

## Построчно по чек-листу

1. **Пески — PASS.** Пакет: `biome_color=#cfa96a`, `palette.base=#cfa96a`; декор `cactus,bush,rock,bone` — **дерева нет** (ни `tree`/`palm`/`conifer`). Частоты декора: кактус 30.3%, куст 46.9%. Скриншот `qa-view-peski.png`: земля тёплого песочного цвета, светлый гребень `#ecd6a4`, мелкий декор, деревьев нет.
2. **Джунгли — PASS.** `life_density=0.62`, `placement clustered tile 220 gap 14`; декор `tree(buttress=true,crown[2,3]),palm,fern,bush,lichen`; `hang` = лиана от дерева. Частоты: fern 36.2%, tree 20.4%, лиана у 1694 деревьев, контрфорс у 4750. Скриншот `qa-view-jungle.png`: сомкнутые кроны, стволы с контрфорсами (расширение у основания), лианы, папоротники; видны вертикальные зазоры между кучками.
3. **Лес — PASS.** Другая плотность (`life_density 0.4` против 0.62) и светлее палитра (`light #3a6b33` против `#2c6330`, `accent #9fcf6a`); `tile 300 gap 26` против `220/14`. Скриншот `qa-view-forest.png` заметно светлее и просторнее джунглей.
4. **Горы — PASS (снеговая линия — ожидаемо).** `biome_color=#726c64`, палитра серая; декор `conifer(where.maxAlt=260),lichen,rock,crystal`. Скриншот `qa-view-mountains.png`: серый камень, хвоя на нижних склонах, **снеговой линии нет** — в данных `snowLine:140` есть, но в клиенте `snowLine`/`viewOnly`/`leeMaxDeg` не читаются (grep по `web/static/js` пуст) → это Э3, **НЕ дефект**.
5. **Безжизненная планета — PASS.** Live-скриншот гор с `life=false` (`qa-view-mountains_dead.png`): ни хвои, ни лишайника, только камень и кристаллы. Подтверждено unit-тестами V8a–V8c (`tools/surface-view-check.mjs`): живой декор гейтится `pkg.life` (`surface_world.js:120-123`, `LIVING_DECOR`).
6. **Фолбэк — PASS.** `углеродные_нагорья`: `view_source="fallback"`, `biome_view` отсутствует; `biome_color` по-прежнему выводится сервером (`#8d7d6c`) — прежнее поведение. e2e `surface-walk-check.js` садился именно на фолбэк-биом «Хемосинтетические сады» → 0 pageerror, 0 тостов.
7. **Картинка планеты — PASS.** `ImageGenVersion = "v5"` (`planet_image_v2.go:47`); `GET /api/planet-image?...&size=big` → 200 `image/png`; диск-кэш: файлы `v5_*` есть, легаси `v4_*` = 0; чтение идёт только по префиксу текущей версии (`planet_image_disk_cache.go:45`) → старая картинка не отдаётся. Цвет биома в пакете (`biome_color`) = `palette.base`.
8. **Админка справочника — PASS (14/14).** GET несёт `view_families` (9), `view_primitives` (31), `view_diagnostics` (`view_errors=[]`, `view_missing=53`). После PATCH с правкой **одного** `color` («горы» `#726c64`→`#123456`): `view_families`/`view_primitives` побайтово идентичны, блоки `view` всех биомов идентичны, `view_errors` пусто. Исходное состояние и файл `config/biome_catalog.json` восстановлены побайтово (проверено `git diff --stat` — те же 76 строк).
9. **Диагностика вида — PASS.** Лог старта чист: ни строк `⚠️ Вид биома`, ни `view_errors` (проверено по байтам UTF-8). Каталог грузится (57 биомов, 17 недр, 9 правил типов). Нефатальность и бюджет вертикали — `TestBiomeViewValidateNonFatal`, `TestBiomeViewVerticalBudget` в `internal/generator/planet` (EXIT 0).
10. **Регресс прогулки — PASS.** `surface-walk-check.js` 15/15, 0 pageerror, 0 консольных ошибок, 0 редиректов, тостов-ошибок нет; ходьба (`distance=5 м`), прыжок (`rise=22.5`), HUD (`100/100`), погода (админ-чипы), корабль на высадке — на месте; `map-check.js` ALL PASS (карта/звезда/restricted-карточка/скриншоты).
11. **DoD — PASS.** `go build`/`go vet` — 0; `-race -short` (без planet/galaxy) — все `ok`; `internal/handlers` c `-count=1` — `ok 19.1s`; `internal/generator/planet` (правка генератора, без `-race`) — `ok 93.8s`.

---

## FAIL (все — не продуктовые)

- **FAIL в моём скрипте:** `F:углеродные_нагорья color пуст` — я ожидал пустой `biome_color` у фолбэк-биома, но сервер всегда выводит цвет производной (`#8d7d6c`). Это **прежнее** поведение (`SurfaceBiomeColorHex`), а не регресс: у биома просто пуст `color` в справочнике. Продуктовых дефектов не выявлено.

---

## Не удалось проверить сам (граница покрытия)

- **Форма рельефа (Э3):** `relief.layers` (`wave/crest/spike/step/dome/carve/fan/flow`), `snowLine`, `leeMaxDeg`, `viewOnly` — в клиенте не реализованы на Э1 (это Э3). Проверил только **данные** рецептов и то, что они не ломают каталог/пакет. Асимметрия дюн, снеговые шапки, ступени-пласты, конусы осыпей — **не проверялись** (нет кода).
- **Визуальная читаемость мелкого декора (кактусы) на песках:** в скриншоте `qa-view-peski.png` мелкий декор (h 6–52 px) при ZOOM 1.8 читается как небольшие тёмные фигуры; индивидуально кактус vs куст vs камень глазом не разделил. Состав подтверждён объективно: пакет (`decor=cactus,bush,rock,bone`), частотный прогон (кактус 30.3%) и unit-тесты V4b/V6a. **Чтобы закрыть глазами:** создатель/игрок — подойти вплотную к кактусу (или принять текущий масштаб как решение).
- **Вкусовые решения:** «джунгли — рощами, а не частоколом» — зазоры `tile 220 / gap 14` есть (unit V3b, видны на скрине), но при `life_density 0.62` и сомкнутых кронах силуэт густой. Насколько это «частокол» — оценка вкусовая, на решение создателя.
- **Реальная смена цвета картинки с орбиты после бампа v5:** проверил версию кэша и что старый `v4_*` не читается/вычищен; попиксельного сравнения «орбита == земля» не делал (нужен анализ PNG).
- **`ResetBiomeCatalogToSeed` («Сбросить к заводским») вживую не вызывал** — он перезаписал бы рабочий незакоммиченный `config/biome_catalog.json`. Синхронность двух копий подтверждена тестом `TestBiomeViewSeedSynced` (EXIT 0) и наличием `view`-секций в обоих файлах.
- **Перф-бюджет кадра** (кап ~600 отрисовок) на рецептном декоре не замерял — вне Э0/Э1 (декор ещё на Э1 минимальный; отдельный `surface-fps-check.js` не гонял).

---

## Предложения в чек-лист (применит @manager)

- **Новый сценарий (B-SURFACE-VIEW):** «Пакет высадки по биому: `view_source=catalog` и `biome_view` есть у биома с рецептом; `view_source=fallback` и `biome_view` пусто у биома без рецепта; `palette.base == biome_color`». Воспроизведение — `POST /api/surface/land {planet_id, biome}` под admin (роль **в токене** — после смены роли в БД нужен релогин).
- **Новый сценарий (B-ADMIN-BIOME-VIEW):** «PATCH `/admin/biome-catalog` с правкой одного `color` не теряет `view_families`/`view_primitives`/блоки `view`; GET их отдаёт; `view_diagnostics.view_errors` пусто». Ловушка — полная замена справочника.
- **Новый сценарий (B-PLANET-IMAGE-V5):** «При заполнении `color`/палитры образцов `ImageGenVersion` = v5; диск-кэш содержит `v5_*`, легаси `v4_*` вычищен/не читается».
- **Новый сценарий (B-SURFACE-LIFE):** «На планете `life=false` рецептный живой декор (хвоя/лишайник/дерево/куст/папоротник) не рисуется; неживой (камень/кристалл/обломок/кость) — рисуется».
- **Устаревших кейсов не нашёл** (в текущем `QA_CHECKLIST.md` зоны surface-view нет — файл не читал целиком, это зона @manager).

---

## Замечания по коду (не дефекты, для @developer/@manager)

- `web/static/js/surface/surface_world.js:120` — `LIVING_DECOR` включает `lichen`; для «безжизненной планеты» это правильно (лишайник = жизнь), но по спеке §3.2 `lichen` — плоские пятна на камне. Если у создателя лишайник = «неживой», гейт надо менять. **Решение по вкусу** — вынести на гейт.
- `web/static/js/surface/surface_world.js` — на Э1 рецепт влияет только на цвет/декор/размещение; `relief` рецепта в `terrainHeight` не применяется (форма — легаси `FORMATIONS`). Это соответствует плану Э0/Э1, но при чтении кода легко принять за «рецепт формы не работает».
