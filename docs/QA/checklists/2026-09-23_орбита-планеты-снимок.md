# QA-прогон: орбита планеты — снимок присутствия (этапы 1–4) + живая приёмка

- **Дата:** 2026-09-23
- **Задача:** независимый прогон фичи «присутствие/снимок» (орбита планеты): календарь +1000, режимы `presence`/`snapshot`, фиксация снимка при отлёте, скан не стирает снимок, чтение не пишет, «Купить отчёт».
- **Коммит:** **нет** — код в рабочем дереве, не закоммичен.
- **Кто гонял:** @tester
- **Окружение:** Windows; Go 1.27.0 windows/amd64; gcc WinLibs в PATH (`-race` доступен); PostgreSQL 15 (`127.0.0.1:5432`, БД `zorion`); Redis; Node + системный Chrome.
- **Важно про инстанс:** живой прогон сделан на **отдельном инстансе `:8081`** (свежая сборка `go build ./cmd/server`), т.к. dev-сервер на `:8080` был устаревшим бинарём (сборка 06:23, исходники правились до 08:16) — фича на нём не активна. `:8080` не трогался. `:8081` — тот же `.env`/БД/Redis.

---

## Go-тесты (п. 1, выполнено отдельно на этом прогоне)

- **DoD-сборка:** `go build ./...` — exit 0 (без вывода). `go vet ./...` — exit 0 (без вывода). **PASS**
- **Быстрый прогон с детектором:** `go test -race -short -count=1 (go list ./... | Where-Object { $_ -notmatch 'generator[/\\](planet|galaxy)$' })` — **exit 0, все пакеты `ok`**, гонок не поймано, флака/повторов не было. **PASS**
- **Тесты фичи (по именам):** `go test -race -short -count=1 -v -run '...' ./web/ ./internal/repository/ ./internal/handlers/` — **18/18 PASS**:

  | Пакет | Тесты | Итог |
  |---|---|---|
  | `web` | `TestGameDateInNode`, `TestKnowledgeTabsInNode`, `TestBranchesRenderInNode` | 3/3 PASS (`ok zorion/web`) |
  | `internal/repository` | `TestUpsertKnowledgeMergesData`, `TestScanPlanetPayloadKeepsSnapshot`, `TestScanSystemPayloadKeepsSnapshot`, `TestFixatePresenceWritesSnapshot` | 4/4 PASS (`ok zorion/internal/repository`) |
  | `internal/handlers` | `TestSnapshotReadDoesNotUpdateSettlements`, `TestCanBuyReportOnOtherPlanet`, `TestCanBuyReportSatelliteParent`, `TestStripPresenceModeFullDetails`, `TestStripSnapshotModeFrozenPicture`, `TestPlayerEffectsDTO`, `TestPresencePlanetID` (+ подтесты nil/орбита планеты/поверхность/орбита звезды/орбита компаньона/пояс (mining)/в полёте/спутник без репозитория), `TestStartTravelDepartureFixatesPlanetPresence`, `TestStartIntraFlightDepartureFixatesPlanetPresence`, `TestIntraArrivalPlanetFixatesSnapshot`, `TestIntraArrivalSatelliteFixatesParent` | 11/11 PASS (`ok zorion/internal/handlers`) |

  Примечание: в прогоне дополнительно присутствовал `TestPresencePlanetID_SatelliteParent` (не входит в запрошенный список; тоже PASS). Сообщения в логе `race balancer: раса "spark" без записи в store — человеческая модель (гвард 99.2.23 §3.2)` — ожидаемые, тесты зелёные.

`ТЕСТЫ: PASS — build/vet exit 0; race-short все пакеты ok (гонок нет); 18/18 фича-тестов PASS (web 3, repository 4, handlers 11).`

---

## Отчёт прогона (дословно, предыдущая сессия)

Важное про окружение: сервер на `:8080` был устаревшим бинарём (сборка 06:23, исходники правились до 08:16) — фича в нём не активна. Поднял свежую сборку (`go build ./cmd/server`) на `:8081` (тот же `.env`/БД/Redis), все живые проверки — на нём. Сервер `:8080` не трогал.

`ПУНКТ 1: PASS — 23.09.3026, 01:20 / 01:23 / 01:24 (UTC, год +1000)`
`ПУНКТ 2: PASS — contracts/flight по Date.now(); gameDate только в показе дат`
`ПУНКТ 3: PASS (част.) — раса/население/стабильность/эффекты есть; ветки/лог не проверить (0 строк в БД)`
`ПУНКТ 4: PASS — 0 вхождений всех запрещённых ключей в presence/snapshot/scan JSON`
`ПУНКТ 5: PASS — snapshot_at 01:22:45Z, население 876763241 стабильно, без effects/log`
`ПУНКТ 6: PASS — scanned_at обновился, data.snapshot сохранился (merge живьём)`
`ПУНКТ 7: PASS — computed_at/population_exact не изменились после чтений`
`ПУНКТ 8: PASS — control только для другой планеты, клик → «Покупка пока недоступна», 0 запросов; e2e ALL PASS`

Детали:
1. Календарь. `web/static/js/game_date.js` — единственный helper (+1000, UTC, формат `ДД.ММ.ГГГГ, чч:мм`). Фактические строки (helper на реальных метках из БД): `users.created_at = 2026-09-23T08:20:03+07:00` → `23.09.3026, 01:20` (`admin/users.js:47`); `updated_at` → `23.09.3026, 01:23` (`:48,146–147`); `npc_agents.last_observed_at = …08:24:42+07` → `23.09.3026, 01:24` (`admin/npc.js:44`). Лог вымирания — `tabs.js:596` (`gameDate(e.occurred_at)`); пресет «Балансировки» — `balancer.js:445` (`gameDateUtc`, суффикс `UTC`). UTC доказан: локальное `08:20 +07:00` показано как `01:20`.
2. Арифметика. `gameDate`/`GAME_YEAR_OFFSET` только в `admin/{users,npc,balancer}.js`, `modal/tabs.js`, `dashboard/money.js`. `contracts.js` (`timeLeftText`), `map/flight.js`, `modal/index.js` — прогресс по `Date.now()`/реальным меткам, правок нет.
3. Присутствие. Игрок на орбите Laphaab: `population: 876757536`, поселение `{race_id:"spark", race_name:"Искры", population:876757536, stability:69, effects:[{name,impact:"population_rate",state:"inactive"}]}`, `knowledge.mode="presence"`. Скрыто: `subterrain_composition:null`, `atmosphere:""`, нет ядра/описания. Ветки/лог — в dev-БД `settlement_branches`/`settlement_log` пусты (0 строк), не подтверждены.
4. Утечки. Сырой JSON в 3 режимах: `population_exact` 0, `computed_at` 0, `r_per_sec` 0, `lambda_per_hour` 0, `n_dead` 0, `"input"` 0, `load_at` 0, `"load"` 0, `source_position` 0, `"threshold"` 0, `"force"` 0, `"curve"` 0, `"w"` 0, `"enabled"` 0, `owner_id` 0. DTO эффекта — только `{name, impact, state}`.
5. Снимок при отлёте. `POST /api/intrasystem-flight` (from=planet) → `data.snapshot` появился сразу (snap_at `2026-09-23T01:22:45Z`, source `presence`), т.е. до смены позиции. Карточка: `mode=snapshot`, дата, `effects`/`log` отсутствуют; повторное чтение через 3 с → `population` тот же `876763241`.
6. Скан не стирает. Открытие системы со сканером → `scanned_at` обновился, `source=scanner`, а `data.snapshot` остался (merge `data || EXCLUDED.data` работает живьём).
7. Чтение не пишет. `computed_at = 08:08:12.731601+07`, `population_exact = 876666262.3284351` — без изменений после нескольких открытий в режиме снимка.
8. «Купить отчёт». На планете Laesysmal: `can_buy_report=false`, у остальных 7 планет — `true`. Браузер: карточка другой планеты, вкладка «Поселения» → кнопка есть; клик → `Покупка пока недоступна`, 0 новых запросов. Карточка планеты присутствия → кнопки нет, плашка `● Свежие данные`. Смоук `map-check.js` (`BASE_URL=:8081`): 1/5…5/5 ALL PASS, `pageErrors=0`, `toastError=false`, exit 0.

Итог: С ОГОВОРКАМИ (все 8 пунктов PASS по проверенному; ветки/лог и живой UI админки не проверены из-за пустых таблиц dev-БД и отсутствия админ-учётки).

Продуктовых багов не найдено. Мелкое (косметика JSON): в снимке `settlements[].planet_id=""` и `branches[].id=""` — поля не заполняются. Замечание по процессу: dev-сервер `:8080` был собран до правок → фича на нём не работала; после Э1–Э4 нужен перезапуск (`AGENTS §13`).

Что не проверено: лог вымирания (`settlement_log` пуста), ветки производства (`settlement_branches` пуста), живой рендер админки (нет учётки admin/skycomposer), стрелка тренда в снимке (проверено кодом), спутник→родитель / высадка / взлёт / Retry / Restore (только код-проверка).

Артефакты (не коммичены): `tools/e2e/tmp-orbit-buy.js`, `tools/e2e/artifacts/orbit-buy.png`; тестовый игрок `qa_orbit_20260923082003` (позиция правилась UPDATE'ом).

---

## Итог (сводно)

**С ОГОВОРКАМИ** — все 8 живых пунктов PASS по проверенному, Go-тесты (build/vet/race-short) — зелёные, 18/18 фича-тестов PASS. Ветки/лог и живой UI админки не проверены (пустые таблицы dev-БД, нет админ-учётки). Продуктовых багов не найдено; единственная находка — косметика JSON (`settlements[].planet_id=""`, `branches[].id=""`). Процессное замечание: `:8080` был устаревшим бинарём → после Э1–Э4 нужен перезапуск (`AGENTS §13`).
