# QA — независимый прогон (второй взгляд): «перелив массы в гигантов + мини-нептуны»

**Дата:** 2026-09-23
**Роль:** @tester
**Спека:** `docs/specs/2026-09-23-перелив-массы-в-гигантов-и-мини-нептуны.md` (источник истины)
**Скоуп:** подэтапы 1–2 (перелив массы, класс мини-нептун). DoD заявлен зелёным, ревью пройдено — проверяю независимо.
**Сервер:** `/health` → OK (`http://localhost:8080`).

**Важно (рабочее дерево):** в дереве — чужие незакоммиченные правки (NPC/поселения/поверхность).
Во время прогона они **дважды** приводили к поломке сборки вне скоупа (см. «Найденное», D1).
Файлы фичи не трогал, ничего не коммитил.

---

## Вердикт: **С ОГОВОРКАМИ**

Фича-код (генератор перелива, мини-нептун, аудит, модели) — **PASS**: сборка/vet, тяжёлый
генераторный прогон, фича-тесты, независимый замер чисел, e2e-смоук — зелёные.
**DoD 2 (полный `-race`) — FAIL, но только на чужих пакетах вне скоупа** (студия + сборка
`internal/repository`), к фиче не относится. Плюс два дефекта интеграции/спеки (D2, D3).

---

## Жёсткий чек-лист (ответ по строке)

1. **PASS** — `go build ./...` = 0; `go vet ./...` = 0 (на момент первого прогона; позже чужие правки сломали сборку, см. D1).
2. **FAIL (вне скоупа)** — `go test -race -short -count=1 (<все пакеты, кроме generator/planet|galaxy>)`: FAIL по `internal/handlers` (13 студийных тестов) и `internal/repository` (`build failed`). Причина — чужие незакоммиченные правки, не фича. Детали и повтор — D1.
3. **PASS** — `go test -count=1 ./internal/generator/planet/ ./internal/generator/galaxy/`: `ok 298.4s` / `ok 42.3s`, EXIT 0.
4. **PASS** — `go test -count=1 -run "TestOverflow|TestGasGiantShift|TestMinineptune|TestMassTagsMiniNeptune" ./internal/generator/planet/ ./internal/audit/planet/`: `ok 70.8s` / `ok 0.39s`. Прогнанные O-имена: O1 `TestOverflowNoCycle`, O2 `TestOverflowNoPileupAtMcrit`, O3 `TestOverflowRateByPlanetCount`, O4 `TestOverflowBirthOrbitInnerBoundary`, O5 `TestOverflowSingleGiantPerSystem`, O6 `TestOverflowNoEntropyInLoop`, O7 `TestOverflowFObRAppliedAfterGiant`, O11 `TestMiniNeptuneRadiusCurve`, O12 `TestMiniNeptuneClassification`, O13 `TestMiniNeptuneNoBiomes`, O14 `TestGasGiantShiftShiftsGas`, O17 `TestMinineptuneAuditCurve` + `TestMinineptuneAuditNoBiomesExclusion` + `TestMinineptuneAuditHydrogenInHeatExclusion`, O20 `TestOverflowG0ExcludedFromPass5`, O23 `TestPrimaryMiniNeptuneNoBelt`, а также `TestMassTagsMiniNeptuneAndGiantTail` (теги §12.3).
5. **PASS** — независимый пробник (свой, удалён после прогона; seed 777001, 10 000 миров/класс):
   - `P_ov`: G **20.30%**, K 11.10%, M 10.98% (спека/замер: G/F 20.3%, K/M 11%).
   - `P_giant` (системы с первичным гигантом): G **11.35%**, K 5.90%, M 6.18% (замер спеки: G/F 10.1%, K/M 5.7% — в окне `[8,12]%`; у меня чуть выше по G, названо).
   - Горячие (g ≤ 2, без циркумбинарных — только обычные звёзды): G **2.00%**, K 1.18%, M 1.20% (спека G/F ≈2.0%); глобально по весам ≈0.9%.
   - Мини-нептуны (систем): G 9.28%, K 5.27%, M 4.90% (класс непуст).
   - Пояса: G 10.29%, K 5.43%, M 5.66%.
   - Медиана массы гиганта: **G 333.9 / K 304.9 / M 326.8 M⊕** (цель `GasGiantTargetMedian` = 317.8); эмпирическая σ = **0.685–0.704 дек** (окно спеки `[0.5, 0.75]`, цель ≈0.62).
   - Полки: **ровно 8.0 → 0 из 63 767** (0.0000%); **ровно 4131 → 0**. Обе полки отсутствуют.
6. **PASS (с замечанием)** — окна сверены: σ в O10 проверяется ровно по спеке `[0.5, 0.75]` (`overflow_giant_test.go:553-555`) — «под факт» не расширено; медиана — широкий допуск, как разрешает спека. Замечания: (а) `region_profile_test.go` `TestProfilePlanetCountPerWorld` — допуск расширен `0.15 → 0.5` (обосновано названным следствием «−1 планета», но это widening, D4); (б) `giant_smoke_test.go` K/M окно `[3,9]%` — шире прочих (замер 5.7%, D4).
7. **PASS** — тело `(M_crit,16]`: `planet_data_minineptune.go:95-107` ставит `is_mini_neptune: true`, `is_gas_giant: false`, `biomes: []`, `subterrain: []`; радиус — `MiniNeptuneRadius` (`cascade_minineptune.go:28-30`, `2.61·(M/8.6)^0.577`); аудит не сыплет: `checkEmptyBiomes` исключает мини-нептун (`checks_biomes.go:56`), `checkHydrogenInHeat` исключает (`checks_physics.go:316`), `checkMassSizeDensity` — ветка по кривой (`checks_physics.go:52-64`). Подтверждено тестами O13/O17.
8. **PASS** — `k_region(±0.2) = [1/3, 5/3]`, `f_rich_eff = clamp(0.5·k_region)` ∈ [≈1/6, ≈5/6] (`overflow.go:113-128`); поведенчески: `TestGasGiantShiftShiftsGas` (2000 миров/сдвиг) → **shift +0.2 → 17.55%** миров с гигантом, **−0.2 → 6.05%** (направление верное, порядок ≈3×, «не доминируют» < 50%). `M_crit`/граница `16` глобальны: тест сверяет `p.crit == massMax` при всех сдвигах (`overflow_giant_test.go:591-599`).
9. **PASS** — P-ветка жива: `circumbinary.go:54` по-прежнему роллит `gasGiantChanceShifted`, `generateCircumbinaryGiant` (`circumbinary.go:197`) даёт гигантов через `runCascadeGiant`/`gasGiantMass()`; `TestCircumbinaryGiantAllowed` зелёный в полном прогоне пакета.
10. **PASS** — регион двигает только `f_rich_eff`, `M_crit` не трогает (O14 сверяет `p.crit == massMax` при `shift ∈ {−0.2,0,0.2}`); тела `> M_crit` уходят в ветку оболочки и через кламп каменистой ветки (`rockyMass`, `overflow.go:292-297`) не проходят — прижатия к `8` нет. Независимый замер: полка `8.0` отсутствует (0 из 63 767).
11. **PASS по доставке / дефект по клиенту** — `is_mini_neptune` добавлен в `models.Planet` (`models/planet.go:78-80`) и `populatePlanetFromJSON` (`planet_repo.go:661`), читается всеми репозиторными путями → доезжает до API. Незнакомый тип клиент рендерит без поломки (fallback цвета/подписи: `modal_render.js:299-305`, `utils.js:24-37`; `planet_types.js`/`journal.js:67` — под guard'ом). **Но** клиент не обрабатывает новый класс: пункт «Высадиться» для мини-нептуна активен (ветка `is_gas_giant` в `modal/events.js:782`, мини-нептун падает в `appendLandItem` → `events.js:790/1023`), сервер ответит ошибкой «Нет данных о поверхности» (`surface_handlers.go:622-624`, `pickWalkBiome==""`). Спека §8.3: поверхностей/биомов нет — «как у гиганта». Детали — D2. e2e-смоук `tools/e2e/map-check.js` прогнан (клиентский флоу карты затронут косвенно) → **RESULT: ALL PASS**, скриншоты `tools/e2e/artifacts/map.png`, `restricted-card.png`; мини-нептун смоук поштучно не покрывает.
12. **Честность** — что не проверено/чем ограничено, см. «Непроверяемое» ниже.

---

## Найденное

### D1 — BLOCKER окружения (вне скоупа фичи): чужие правки роняют сборку
Во время прогона рабочее дерево дважды приходило в несобираемое состояние из-за чужих
незакоммиченных правок (`internal/economy/settlement/*`, `internal/repository/settlement_owner_pass.go`,
`internal/repository/supply_effects_integration_test.go`):
- `internal/repository/settlement_owner_pass.go`: `undefined r.loadProducerRates`, `unknown field NormPerHour`,
  `too many/not enough arguments` (в разное время — `syntax error`), `supply_effects_integration_test.go:482: undefined: settlement.BranchRate`.
- Как следствие `FAIL [build failed]` у `internal/repository`, `internal/audit/planet`, `cmd/server`,
  `internal/goodsstudio`; у `internal/handlers` — 13 падений студийных тестов
  (`TestStudio*`, `studio_handlers_test.go`, `studio_descriptions_test.go`).
- Это **не фича**: к переливу/мини-нептунам отношения нет. Приоритет: высокий для @manager —
  полный DoD сейчас красный по чужим зонам, фичу это маскирует.
- Примечание: через пару минут дерево снова собиралось (`go build ./internal/generator/planet/` = 0),
  т.е. состояние плавающее (агент продолжает править).

### D2 — MEDIUM (интеграция/игрок): мини-нептун можно «высадиться», хотя поверхностей нет
- `web/static/js/modal/events.js:782` — спец-обработка только `planet.is_gas_giant`; мини-нептун
  идёт в `appendLandItem` (`events.js:790`, `:1023`) → пункт «🚶 Высадиться» **активен** с подписью «биом ?».
- Сервер: `surface_handlers.go:622-624` → `pickWalkBiome(p)==""` → 400 «Нет данных о поверхности»
  (есть тест `TestSurfaceLandNoBiomes`).
- Спека §8.3: у мини-нептуна «поверхностей/биомов/недр нет — как у гиганта». Ожидаемо: пункт
  должен быть disabled, как у гиганта.
- Приоритет: средний (игрок кликает и получает ошибку; не краш).

### D3 — LOW/MEDIUM (спека ↔ код): `P_mig` = 0.15 вместо спеки ≈0.20
- `overflow.go:57` `giantMigrationChance = 0.15`; спека §6.3/§10.4: гипотеза `P_mig ≈ 0.20`.
- Комментарий в коде обосновывает 0.15 под вилку Wright при измеренном `P_giant`; но источник истины —
  спека. Тест O8 несёт устаревшую подпись «P_mig ≈ 0.20» (`overflow_giant_test.go:412`).
- Последствие малое (глобальные горячие ≈0.8–0.9% — в вилке), но спека и код расходятся.
- Решение — за @manager/@balancetester: либо привести код к 0.20, либо зафиксировать 0.15 в спеке.

### D4 — LOW (тесты, item 6): расширенные допуски
- `region_profile_test.go` `TestProfilePlanetCountPerWorld`: `0.15 → 0.5` (обосновано «−1 планета»:
  пояс `g−1` + покинутая `g₀`), но это widening «под факт».
- `giant_smoke_test.go`: K/M окно `[3, 9]%` (замер 5.7%) — шире, чем у G/F `[8,12]%` (±3 п.п.).

### D5 — LOW (план тестов): спека требовала переписать T16/T9/T20, они не тронуты
- Спека §12.2: `TestSystemBudgetNotAppliedToGiantsAndExotic` (T16) — «переписывается, не сохраняется».
  Фактически `accretion_mass_test.go:437/447` остался прежним и зелён (проверяет легаси-`runCascadeGiant`/P-ветку).
  Новое поведение покрыто O19. Функционально не баг — расхождение плана тестов и реализации.

### D6 — INFO: нижняя граница `gasGiantMass()` для P-ветки сдвинулась 15.9 → 16
- `gas_giant_physics.go:24` `GasGiantMassMin = 16`, а `gasGiantMass()` тянет из `[GasGiantMassMin, Max]`.
  Спека §9.2 говорит «P-ветка сохраняет прежнее поведение целиком», §7.4 — `GasGiantMassMin = 16`.
  Формально мелкое противоречие внутри спеки; на игру влияет незначительно (P-гигант ≥16 вместо ≥15.9).

---

## Что осталось непроверенным (граница покрытия)

- **`go test -race -short` целиком (DoD 2)** — не удалось получить зелёный из-за чужих правок (D1);
  повтор дал тот же класс ошибок в студии/repository. Фича-пакеты гонок не показали, но полного
  зелёного прогона по модулю нет. Нужно: дождаться завершения чужих правок и повторить DoD 2.
- **Поведение мини-нептуна глазами игрока в браузере** (D2) — прогонял только общий `map-check.js`
  (ALL PASS); чтобы воспроизвести именно мини-нептун, нужен мир с таким телом в радиусе радара игрока
  и клик «Высадиться». Нужно: сгенерировать мир с мини-нептуном (или админ-генерация), затем e2e-клик.
- **Магнитуда регионального отклика `P_giant` по регионам** (item 8) — проверены знак и порядок
  через O14 (+0.2 → 17.55%, −0.2 → 6.05%); формального окна «какой отклик правильный» спека не задаёт
  (замер §17.9 — за @balancetester).
- **Долгосрочные/краевые объекты**: планеты в зоне `(M_crit,16]` на экзотике/двойных — в БД/потоке
  штатно не встречаются (P-ветка переливом не покрыта), проверял по коду и тестам, не живым миром.
- **Энциклопедия/`planet_types.js`** — мини-нептун в реестре типов отсутствует (D2-смежное), но
  это скорее контент-гэп; спека §11.2 его не требует.

---

## Предложения в чек-лист (применяет @manager)

1. **Новый кейс (мини-нептун, клиент):** у планеты класса «мини-нептун» пункт «Высадиться» disabled,
   как у газового гиганта (закрывает D2).
2. **Новый кейс (API):** `is_mini_neptune` присутствует в выдаче `/api/worlds/{id}/planets` и карточке
   (whitelist `models.Planet`/`populatePlanetFromJSON`) — сейчас проверено только чтением.
3. **Кейс (регионы):** региональный сдвиг `gas_giant_shift ∈ {−0.2,0,+0.2}` не двигает `M_crit`/`16`
   и не создаёт полку на `8` — добавить ассерт по числам (сейчас O14 сверяет только `p.crit`).
4. **Уточнить в спеке/коде:** `P_mig` (0.15 vs 0.20) — синхронизировать спеку §6.3 и `overflow.go:57` (D3).
5. **Пометить в реестре тестов:** T16/T9/T20 не переписаны (D5) — либо внести как есть, либо привести к спеке.

---

## Артефакты / команды

- Сборка/vet: `go build ./...`, `go vet ./...` — EXIT 0.
- Генератор: `go test -count=1 ./internal/generator/planet/ ./internal/generator/galaxy/` — ok 298.4s / 42.3s.
- Фича-тесты: `go test -count=1 -run "TestOverflow|TestGasGiantShift|TestMinineptune|TestMassTagsMiniNeptune" ./internal/generator/planet/ ./internal/audit/planet/` — ok.
- DoD 2: `go test -race -short -count=1 (<все пакеты, кроме generator/planet|galaxy>)` — FAIL (D1).
- Независимый пробник: временный `internal/generator/planet/qa_probe_test.go` (создан, прогнан, **удалён**).
- e2e: `cd tools/e2e; node map-check.js` — ALL PASS; скриншоты `tools/e2e/artifacts/{map,restricted-card}.png`.
