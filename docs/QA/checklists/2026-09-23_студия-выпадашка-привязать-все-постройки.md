# Прогон 2026-09-23 — студия: выпадашка «+ привязать» показывает все постройки-подтипы

**Тестировщик:** @tester (deepseek-v4.1-flash)
**Проверяемое (uncommitted, рабочее дерево):** `web/studio.html` — функция
`renderBindSection` (стр. 1589–1637), три правки по решению создателя 2026-09-23:
1. `avail = state.producer_types.filter(p => p.parent_id && !bound.includes(p.id))` — фильтр по
   `kind === "goods" && category_id === g.category_id` снят (`studio.html:1615-1616`);
2. подпись опции — `p.name — factoryLevelLabel(p)` вместо `parent_name — factoryLevelLabel(p)` (`studio.html:1627`);
3. пустое состояние — «все постройки уже привязаны» / «Построек нет — раздел „Производители“» (`studio.html:1629-1633`).

**Способ:** живой браузер — `playwright-core` + системный Chrome (headless, 1500×950), `/studio`.
Вход `POST /login` под ролью `skycomposer`, токен в `localStorage.adminToken`. Карточка товара —
реальным `page.dblclick` по карточке справочника; привязка/отвязка — реальными
`selectOption` + кликами по кнопкам попапа; drop на канвас — `DragEvent` с настоящим
`DataTransfer` в точке узла (`prodTreeLayout().pos[id]` + `panX/panY/zoom`) и в центре канваса.
Сеть ловилась `page.on('response')`. Временный скрипт `tools/e2e/tmp-bindall-check.js` после
прогона **удалён** (вместе с temp-JSON). Скриншоты — `tools/e2e/artifacts/studio-bindall-*.png`.

**Окружение:** dev-сервер `http://127.0.0.1:8080`, `/health` → `OK`, рестарт не требовался
(статика с диска). Перед пишущими пунктами `/admin/generate-status?job=*` → все `idle`
(universe, planets, factions, settlements, race_settlements, regenerate_planets, pacman,
hypothesis, npc).

**Учётка:** аккаунт `qa_mgr_studio` (роль `skycomposer`) в БД есть, но пароль восстановить
не удалось; вход выполнен под `skycomposer` (роль `skycomposer`) — та же роль, доступ к студии
эквивалентен. Не влияет на результат.

## Исходные данные (state на старте)

- `producer_recipes` = **0**; `goods` = **135**; `producer_types` = **36**; подтипов (`parent_id`) = **30**.
- «Пища» `id=378`, `recipe_id=69`, `category_id=7`; «Городок» `id=148` (`kind=goods`, `parent_id=1`,
  `category_id=NULL`); items-подтипы без категории — `6`, `7`, `8` (`parent_id=18` «Лаборатория»).

## Результаты (T1–T8 + T-пустой)

| # | Вердикт | Факт |
|---|---|---|
| **T1** | **PASS** | `/studio` открылся (`location.pathname=/studio`); `state.goods=135`, `state.producer_types=36`, подтипов=30; `pageerror=0`. |
| **T2** | **PASS** | Карточка «Пища» (378) открыта реальным dblclick (`#popupTitle="Пища"`). В блоке «Доступен постройкам» найден `select#bindFactorySel`, опций **30**. Среди значений есть `148` («Городок») и `6` («Исследовательская лаборатория»), а также items-подтипы `7`, `8` — не только товарные фабрики. |
| **T3** | **PASS** | Собрано 30 видимых текстов опций; дублей **0**; каждая начинается с имени постройки вида `«имя» — «уровень»` (проверено для всех опций). Примеры: «Исследовательская лаборатория — универсальная», «Фабрика механических деталей — универсальная», «Городок — универсальная». |
| **T4** | **PASS** *(с оговоркой по тексту строки)* | Выбрано `148`, клик «+ привязать» → `POST /studio/api/producers/148/recipes` = **200**. Опций стало **29**, значение `148` из списка пропало. Новая строка в списке «Доступен постройкам» появилась (1 строка, «− отвязать» для 148). `#report` не показан, `.toast-error`=0. **Оговорка:** текст строки — `«Поселение · универсальная»`, а не «Городок»: строка строится из `parent_name`+категория+уровень (`studio.html:1604-1606`), имя самого подтипа не выводится (см. «Наблюдения» №1). |
| **T5** | **PASS** | Кросс-`kind`: `148` уже привязан; выбран `6` (`kind=items`, без категории), «+ привязать» → `POST /studio/api/producers/6/recipes` = **200**, опций `28`. Строка для 6 появилась (кнопка `unbindFactory(6)`=1). Клик «− отвязать» → `DELETE /studio/api/producers/6/recipes/69` = **200**, опций **29**, `6` вернулся в выпадашку, `148` остаётся привязанным. Привязка `kind`/категорией не ограничена. |
| **T6** | **PASS** | «− отвязать» строку `148` → `DELETE /studio/api/producers/148/recipes/69` = **200**; строк привязок 0, показано «не привязан ни к одной постройке»; опций снова **30**. |
| **T7** | **PASS** | Ветка «Производители» + справочник «Товары»: бросок «Пищи» на узел `sub:148` → `POST /studio/api/producers/148/recipes` = **200**, `prodProducesText(sub:148)="Пища · т1"`; отвязка кнопкой `unbindFactoryRecipe(148,69)` = 200. Регресс ветки «Товары»: фабрика `62`, бросок «Пищи» на канвас → `POST /studio/api/producers/62/recipes` = **200**, `#modeBadge="фабрика: Фабрика пищевых продуктов · всё дерево · в наборе 1"`; отвязка = 200, привязок 0. |
| **T8** | **PASS** | `pageerror=0` за весь прогон; `.toast-error`=0; `#report` ни разу не показан (в студии ошибки идут в `#report`, класса `.toast-error` в DOM студии нет — счётчик 0 тривиален); после прогона привязок 0. Единственная консольная 404 — `/favicon.ico` (браузерный запрос, к правке не относится, повторяется на чистой загрузке). |
| **T-пустой** | **SKIP** | Пустое состояние («все постройки уже привязаны») воспроизводится только при привязке **всех 30** подтипов к рецепту — дорого/мусорно. По коду: строка существует и показывается, когда `avail.length===0` при наличии подтипов (`studio.html:1629-1633`); иначе — «Построек нет — раздел „Производители“». |

**Вердикт базового прогона: ПРОЙДЕНО (8/8 PASS + 1 SKIP).** Итог с дельтой (T9–T12, ниже) —
**ПРОЙДЕНО (12/12 PASS + 1 SKIP)**. Правка работает по назначению: выпадашка показывает
все 30 подтипов (включая `kind=items` и записи без категории), подписи различимы, привязка/отвязка
через UI и перетаскиванием отвечают 200, регресс ветки «Товары» зелёный, консоль чистая.

## Сеть (из перехвата `page.on('response')`)

`POST /studio/api/producers/148/recipes` 200 · `POST /studio/api/producers/6/recipes` 200 ·
`DELETE /studio/api/producers/6/recipes/69` 200 · `DELETE /studio/api/producers/148/recipes/69` 200 ·
`POST /studio/api/producers/148/recipes` 200 (drop) · `DELETE /…/148/recipes/69` 200 ·
`POST /studio/api/producers/62/recipes` 200 · `DELETE /…/62/recipes/69` 200.

## Очистка БД

Контроль через `tools/db.ps1`:

- `producer_recipes` = **0** (на старте 0; все 4 привязки прогона отвязаны).
- `goods` = **135**, `producer_types` = **36**, подтипов = **30** — не менялись.
- Временных `QA_*`: `goods` = 0, `producer_types` = 0 — не создавались.
- Категория товара «Пища» (378) = **7** — не менялась.
- Временный скрипт `tools/e2e/tmp-bindall-check.js` и temp-JSON удалены; остались только
  PNG-скриншоты (`studio-bindall-t2.png`, `studio-bindall-t4-bound148.png`, `studio-bindall-t7-tree.png`).

## Дельта 2026-09-23 (вторая правка, потом) — имя постройки первым в строке привязки (T9–T12)

**Что доработано:** `renderBindSection`, строка привязки теперь начинается с `p.name`:
было `parent_name · category · level`, стало `name · parent_name · category · level`
(`web/studio.html:1606-1607`). Регресс к наблюдению №1 предыдущего прогона. Статика с диска —
рестарт не требовался. Учётка та же (`skycomposer`, роль `skycomposer`).
Временный скрипт `tools/e2e/tmp-bindrow-check.js` после прогона **удалён**.
Скриншоты — `tools/e2e/artifacts/studio-bindrow-t9.png`, `studio-bindrow-t10.png`.

| # | Вердикт | Факт |
|---|---|---|
| **T9** | **PASS** | «Пища» (378), `#bindFactorySel` → «Городок» (148), POST `/studio/api/producers/148/recipes` = 200. Единственная строка: **`Городок · Поселение · универсальная`** (имя первым, не начинается с «Поселение»). Опций 29. |
| **T10** | **PASS** | Привязаны 6/7/8 (каждый `POST /studio/api/producers/{6,7,8}/recipes` = 200). Четыре строки различимы (4 разных имени), фактические тексты: «Исследовательская лаборатория · Лаборатория · универсальная», «Лаборатория космических технологий · Лаборатория · универсальная», «Лаборатория экипировки · Лаборатория · универсальная», «Городок · Поселение · универсальная». Было бы три одинаковых «Лаборатория · универсальная» — теперь различаются. Опций 26. |
| **T11** | **PASS** | Отвязаны все 4 (кнопки «− отвязать»): `DELETE /…/6|7|8|148/recipes/69` = 200; строк 0, показано «не привязан ни к одной постройке», опций снова **30**; `.toast-error`=0, `pageerror`=0, привязок 0. |
| **T12** | **PASS** | `tools/db.ps1`: `producer_recipes` = **0**, `goods` = **135**, `producer_types` = **36**; `QA_*` (goods/ptypes) = 0. Temp-скрипт и temp-JSON удалены; PNG-скриншоты оставлены. |

**Вердикт дельты: ПРОЙДЕНО (4/4 PASS).**

## Наблюдения (не блокеры)

1. **Строка привязки — имя подтипа.** ~~Было `parent_name · категория · уровень`, поэтому у «Городка»
   (148) видно «Поселение · универсальная», а у `6/7/8` — три одинаковых «Лаборатория · универсальная».~~
   **Исправлено второй правкой (T9–T11):** строка начинается с `p.name`
   (`studio.html:1606-1607`), «Городок · Поселение · универсальная», три лаборатории различимы.
2. **`.toast-error` в студии отсутствует** как класс: ошибки выводятся в `#report`
   (`studio.html:572-578`), сам блок в прогоне ни разу не показался. Кейс «`.toast-error` = 0» в
   студии проверяется тривиально — стоит формулировать как «`#report` не показан».
3. **Чтение `window.popupGoodId`** даёт `undefined` (это `let` верхнего уровня, не свойство
   `window`); факт открытия попапа подтверждён `#popupTitle="Пища"`. Замечание для будущих кейсов.

## Что не проверено (границы покрытия)

- **Пустое состояние выпадашки** (T-пустой): SKIP — требует привязки всех 30 подтипов; проверено
  по коду (`studio.html:1629-1633`), живьём не воспроизводилось.
- **Визуальная читаемость/верстка** опций и строк привязки (пиксельно) — скриншоты приложены,
  но оценка «глазами игрока» — вкусовая, за создателем.
- **Одновременная привязка 6/7/8 к одному рецепту** — в базовом прогоне не прогонялась; во второй
  правке проверена живьём (T10), три строки различимы.
- **Гонки/повторные POST** в этом прогоне не проверялись (правка чисто клиентская, защита
  `bindingInFlight` — предмет прошлых прогонов).

## Предложения в чек-лист (применяет @manager)

- Новый сценарий: «Студия → карточка товара: выпадашка „+ привязать“ показывает все подтипы
  (T1–T8)» — 30 опций, наличие `kind=items`/без категории, различимость подписей, привязка
  кросс-`kind` и по категории, отвязка, регресс drop в обеих ветках.
- Ожидание T4 привести к формату `name · parent_name · категория · уровень` («Городок · Поселение ·
  универсальная») — после второй правки строка включает имя подтипа.
- В кейсах студии фиксировать «`#report` не показан» вместо «`.toast-error` = 0».
- Добавить кейс на различимость строк при нескольких подтипах одного родителя/уровня
  (T10: 6/7/8 под «Лабораторией»).
