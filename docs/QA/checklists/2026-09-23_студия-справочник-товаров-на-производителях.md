# Прогон 2026-09-23 — студия: переключатель справочника «Постройки | Товары» на вкладке «Производители»

**Тестировщик:** @tester
**Проверяемое изменение:** `web/studio.html` (в рабочем дереве, коммита нет; Go-код не тронут)
**Идея:** `docs/gamedesign/ideas/2026-09-23_студия-рецепты-постройкам-справочник-товаров.md` §2
**Способ:** живой браузер — `playwright-core` + системный Chrome (headless), страница `/studio`,
вход под ролью `skycomposer` (`qa_role_sky_062714`), токен в `localStorage.adminToken`.
Временный скрипт прогона (`tools/e2e/tmp-*.js`) после прогона **удалён**; в репозитории мусора нет.
**Dev-сервер:** 8080, `/health` → `OK`, рестарт не требовался (статика с диска).

## Результаты (K1–K9)

1) **K1 PASS** — вкладка «Производители», дефолт: `#segSpravMode` виден (`inline-flex`), активна «Постройки»
   (`onBuildings=true`, `onGoods=false`, `spravMode="buildings"`); заголовок `Производители · 20`,
   20 карточек типов построек (0 карточек с «· тир »); фильтры каталога скрыты:
   `#filters=none`, `#tierFilters=none`, `#spravCat=none`. Пример карточки: «Поселение» / meta «товары».
2) **K2 PASS** — переключение на «Товары»: заголовок `Справочник · 134`, 134 карточки (все с «· тир »),
   видны `#filters=flex`, `#tierFilters=flex`, `#spravCat=inline-block`.
3) **K3 PASS** — тождество справочников: фильтры «все тиры» + галка «неиспользуемые» →
   на «Производителях/Товары» 134, на вкладке «Товары» 134 (**совпало**).
   Второй набор — категория `7`: на «Товарах» 2, на «Производителях/Товары» 2 (**совпало**); сброс категории → 134/134.
4) **K4 PASS** — поиск сузил 134 → 1, все совпадения содержат подстроку; категория сужает (см. K3);
   гейт работает: снять все тиры (галка стоит) → 0 карточек и «ничего не выбрано — выберите тир и хотя бы одну галку»;
   снять все галки (тиры стоят) → то же; восстановление → 134. Поведение как на вкладке «Товары».
5) **K5 PASS** — ⓘ по **не-ресурсной** карточке (товар «Пища», `kind=good`, `recipe_id`) открывает **попап товара**:
   заголовок «Пища», тело содержит `Состав рецепта` и `объём`, НЕ содержит `вход (input)` (маркер попапа постройки);
   `#detailPopup=flex`. (Первый заход попал на карточку-ресурс — у ресурса секции «Состав рецепта» нет штатно;
   подтверждено отдельным прогоном на товаре.)
6) **K6 PASS** — канвас не меняется: `canvas#graph.toDataURL()` хеш `-932642347`, len 46890 —
   идентичен до переключения, после «Постройки» и после обратно «Товары». `zoom=1`, `panX=0`, `panY=0` не сбрасываются.
7) **K7 PASS** — возврат на «Постройки» → `Производители · 20` (список построек). Перезагрузка страницы:
   режим сохранён (`spravMode="goods"`, активна «Товары», `seg` виден, фильтры видимы, `Справочник · 134`), ветка — `producers`.
8) **K8 PASS** — вкладка «Товары» не сломана: `#segSpravMode=none` (переключатель скрыт), `Справочник · 134`,
   фильтры каталога видимы, поиск сужает, ⓘ открывает товарный попап («объём»).
9) **K9 PASS** — `pageerror` = 0, элементов `.toast-error` = 0, `#report` пуст.
   Оговорка: в первом прогоне пойман 1 console-404 `Failed to load resource` (URL не пойман), во втором прогоне
   (`response.status>=400`) — 0 таких ответов, не воспроизвёлся, на функциональность не влиял.

**Вердикт: ПРОЙДЕНО (9/9)**

## Наблюдения (не баги фичи)

- В dev-БД есть ресурс с «сломанным» именем — текст карточки буквально `??????-??????` (категория «Вода»).
  К переключателю отношения не имеет (данные каталога), но имя в студию приходит как `?`-строки.
- console-404 первого прогона не воспроизвёлся; отдельного влияния на фичу не выявлено.

## Что не проверено / границы покрытия

- **Drag&drop рецепта на узел постройки** — в объём шага 1 не входит (явно названо границей создателю);
  не проверял.
- **Визуальный стиль переключателя** (отступы/иконки, читаемость «Справочник» под заголовком) — вкусовое, глазами создателя.
- **Наполнение dev-БД экзотикой** (много категорий/тиров, кастомные имена) — проверено на текущем каталоге (134 товара/ресурса, 20 типов построек).
- **Причина console-404** — URL не удалось поймать; нужен повторный прогон/лог сервера, если создатель сочтёт важным.

## Предложения в чек-лист (применяет @manager)

- Новый сценарий: «Студия → Производители → переключатель справочника Постройки/Товары» (K1–K9 выше).
- В кейс проверки попапа из режима «Товары» **обязательно брать не-ресурсную карточку** (у ресурса нет секции
  «Состав рецепта» — это не баг, а штатное поведение попапа ресурса).
- Дополнить: переключатель должен быть скрыт и на ветке «Предметы» (`branch=items`) — в этом прогоне проверена
  только ветка «Товары»; стоит закрепить в кейсе.
