# QA-прогон 2026-09-24 — И1 спеки «стадии поселения и скорость производства»

> Независимый прогон **итерации И1** спеки
> `docs/specs/2026-09-23-стадии-поселения-и-скорость-производства.md`
> (§2, §3, §9.1, §10 п.1–3, §11.1–11.2, §15.1, §15.2 T-Е1/Е2/Р1/Р2/Р3/А1,
> §15.4 пп.1–2,7): число скорости на паре «постройка × рецепт», единица
> «ед/сутки/млрд», чтение числа миром, поля студии.
> Роль: @tester. Производственный код НЕ правился (только чтение). Каталог
> (params типа 148, `rate` пар) после API-проверок восстановлен; временная
> admin-учётка удалена. **Браузерный смоук студии — сделан** (playwright-core + Chrome).

**Итог: ПРОЙДЕНО** (8/8 пунктов; отклонений от спеки нет).

## Что проверялось

- `migrations/000076_producer_rate_and_eat_units.sql` (untracked) — колонка
  `producer_recipes.rate` + конверсия `params.eat` ×2.4·10¹⁰ с признаком `eat_units`.
- `internal/economy/settlement/units.go` — `PerSecond` (единая точка конверсии).
- `internal/economy/settlement/branch.go` — `ProcessBranch` от `RatePerDayPerBillion`,
  `Complexity` из скорости убран.
- `internal/economy/settlement/needs.go:252` — спрос через `PerSecond`.
- `internal/repository/settlement_owner_pass.go` — `loadProducerRates` / `rateForPair`,
  `OwnerSettlement.SettlementTypeID`, `settlementLockSQL` читает тип.
- `internal/handlers/studio_producer_params.go`, `studio_recipes_handlers.go`,
  `studio_handlers.go` (StateView), `internal/repository/producer_repository.go`.
- `web/studio.html` — блоки «Производит» (число) и «Потребляет» (позиция→эффект→норма).
- Тесты: `units_test.go`, `branch_test.go`, `branch_repository_test.go`,
  `producer_units_migration_test.go`, `settlement_type_consistency_test.go` (T18),
  `studio_producer_rate_test.go`.

## Окружение

- Windows, Go 1.27.0; gcc (WinLibs) в PATH → `-race` доступен.
- dev-сервер `http://127.0.0.1:8080` (новый код, миграция `000076` применена),
  `/health` → 200 «OK». dev-БД `zorion`; SQL — через `tools/db.ps1`
  (`-ExecutionPolicy Bypass`).
- Каталог до прогона: `producer_recipes` = 5 пар (148×{69,73}, 151×{69,73,91}),
  **все `rate` = NULL**; тип 148 = «Деревня», `params.eat` = {вода:600, пища:600,
  продовольствие:600}, `eat_units='per_day_per_billion'`, `effects={продовольствие:голод}`.

## Пункты жёсткого чек-листа

| # | Вердикт | Доказательство |
|---|---------|----------------|
| A1 | **ПРОЙДЕНО** | `schema_migrations` содержит `000076`; `producer_recipes.rate` — `double precision`, `is_nullable=YES`, `CHECK (rate IS NULL OR rate >= 0)`; у 148 `eat`=600 (вода/пища/продовольствие) и `eat_units='per_day_per_billion'`; литералы `000067_settlement_type.sql:33` и `000070_supply_effects.sql:46` по-прежнему `2.5e-08` (не переписаны); повторный прогон UPDATE из `000076` в `BEGIN…ROLLBACK` → `UPDATE 0`, значение не изменилось. |
| A2 | **ПРОЙДЕНО** | `TestProcessBranchZeroRateInert` (`branch_test.go:382`) — `rate=0` → output/ProducedLast=0, вход и залежь не тронуты, `processed_at` продвинулся; путь `NULL` — `loadProducerRates` (`settlement_owner_pass.go:72-74`) берёт только `rate IS NOT NULL`, отсутствие ключа = 0 (`rateForPair:228-235`). `go test -race` — ok. |
| A3 | **ПРОЙДЕНО** | `TestProcessBranchRateLinear` (`branch_test.go:356`): линейно по `rate`, населению и Δt (`rate×pop/1e9/86400×Δt`); `TestProcessBranchInputConsumptionPerComponent` (`:422`): вход = `batches × quantity` по каждому компоненту (3:1). `go test -race ./internal/economy/settlement/` — ok. |
| A4 | **ПРОЙДЕНО** | `TestSyncSettlementsRateMissInert` (`branch_repository_test.go:222`): тип 999 → числа нет → `Produced=0`, вход не тронут, ветка **жива** (не удалена), `processed_at` продвинут; `OwnerSettlement.SettlementTypeID` (`settlement_owner_pass.go:105-107`), `settlementLockSQL` читает `settlement_type_id`. `Complexity` из скорости убран (`branch.go` — поля нет; `toBranch(population, base, rate, deposits)` без complexity; `rg Complexity` в пакете — только комментарии). `go test -race ./internal/repository/` — ok. |
| A5 | **ПРОЙДЕНО** | `PerSecond` — единственное место `1e9`/`86400` (`units.go:12-13`); `TestPerSecondIsSingleConversionPoint` (`units_test.go:32`) сканирует не-тестовые файлы пакета; `needs.go:252` использует `PerSecond`; T-Е1: `PerSecond(600,1e9)*86400 = 600`. `go test -race` — ok. |
| A6 | **ПРОЙДЕНО** | Живой curl/API на dev-сервере (временный admin): `PUT /studio/api/producers/148/recipes/69 {rate:650}` → **200**; `{rate:-5}` → **422**; пара 71 (не привязана) → **409**; тип 999999 → **404**; рецепт 999999 → **404**; типизированный `PUT /producers/148 {stage:{enter:100,exit:200}}` → **422**, `{enter:200,exit:100}` → **200**; `{eat:{вода:700,продовольствие:600},effects:{продовольствие:голод}}` → **200** + warning `позиция «вода» без эффекта — не потребляется`; в `params` появился `eat_units`. Код: `producer_repository.go:1104-1151`, `studio_producer_params.go:90-104`. |
| A7 | **ПРОЙДЕНО** | `GET /studio/api/state` (живой): `producer_recipes` — у всех записей есть поле `rate` (свойство присутствует, `null` при NULL); пара 148×69 → `650`; `producer_types[148].params` отдаётся целиком: `eat`/`eat_units`/`effects`/`stage` не срезаны (`studio_handlers.go:118-125` `RecipeBindingView.Rate *float64`). |
| A8 | **ПРОЙДЕНО** | Реальный браузер (playwright-core + Chrome): логин админом → карточка поселения «Деревня» → видны блоки **ПРОИЗВОДИТ** и **ПОТРЕБЛЯЕТ**; поле числа у пары (пища=650, вода пусто → «не производит» + предупреждение «рецепт … в наборе без числа — не производит»); строка «Потребляет» позиция→эффект→норма, placeholder «по умолчанию 600», предупреждение «позиция «Вода» без эффекта — не потребляется»; сырой JSON — в `<details>` закрыт; JS-ошибок страницы 0. Скриншот — `tools/e2e/artifacts/qa-i1-studio.png`. |

## DoD (Go-код затронут)

```powershell
go build ./...        # exit 0
go vet ./...          # exit 0
go test -race -count=1 ./internal/economy/settlement/ ./internal/repository/ ./internal/goodsstudio/ ./internal/handlers/ ./web/
# ok  settlement 2.781s | repository 2.263s | goodsstudio 1.362s | handlers 18.230s | web 2.307s  (exit 0)
```

Полный цикл `go test -race -short` (без `generator/planet|galaxy`) — exit 0,
все пакеты `ok`/`[no test files]`. Тяжёлый генераторный прогон не нужен
(`generator/*` не трогался).

## Находки

Багов, введённых И1, **не найдено**. Наблюдения (не FAIL):

1. **`/favicon.ico` → 404** (сервер отдаёт 404; браузер автоматически запрашивает
   favicon при загрузке `/studio`). В консоли это единственная запись
   `console.error: Failed to load resource ... 404`. Это **не JS-ошибка** и не
   связано с И1 (страница не объявляет favicon); существует независимо от фичи.
   Замечание на будущее, не регрессия.
2. **Контент-дрейф имени типа 148.** В БД `producer_types.id=148` теперь
   «**Деревня**», а спека (§4.3, §7, §3.1) называет его «**Городок**». Это
   ровно тот случай переименования, который чинила `000075` (резолв по id);
   И1 резолвит стадии/тип по id и на имя не опирается — на корректность не влияет.
   Для @manager: спека/impact-map ссылаются на «Городок» — актуализировать имя.

## Что не проверено и почему

- **Живое производство на реальном поселении** (T-Р1/T-Р2/T-Р3 «живьём», §15.4
  пп.1–2: «у живого поселения выход растёт, залежь убывает»). Не запускалось:
  требуется поселение на орбите/карточка планеты и запись в мир (dev-мир общий),
  а число `rate` у существующих пар = NULL. Эквивалент закрыт юнит- и
  репозиторными тестами (A2–A4) + API (A6) + браузерным смоуком (A8).
  Чтобы закрыть: создателю задать число паре «148 × Пища» в студии и прочитать
  карточку живого поселения (сравнить выход/залежь до-после).
- **Стадии поселения** (§4–§7: ладдера, гистерезис, добор веток, сброс нагрузки,
  очистка сирот) — это **итерация И2**, в объём И1 не входит; не проверялось.
- **Витрина арифметики игроку** (§8.3, решение Г5) — И2; настройка
  `settlement_arithmetic_visible_to_player` и `PATCH /admin/settlement-settings`
  в И1 не реализованы и не проверялись.
- **Экстремальные планеты/населения** (вне диапазона `[1e5,1e9]`) — данных в БД
  под рукой нет; не воспроизводимо штатно.
- **Свежая БД** (путь сида `seed_producers.go` с `eat=600` + `eat_units`) — на
  dev-БД не воспроизводимо без чистой базы; покрыто `seed_producers_test.go` и
  `settlement_type_consistency_test.go` (T18, зелёные).
- **Прод/Amvera** — нет доступа.

## Артефакты прогона

- `tools/e2e/qa-i1-studio.js` — сценарий прогона (A6/A7 API + A8 браузер);
  временный admin-аккаунт по образцу `tools/e2e/qa-e3-profile.mjs`.
- `tools/e2e/artifacts/qa-i1-studio.png` — скриншот карточки «Деревня»
  (блоки «Производит»/«Потребляет», число 650, предупреждения, свёрнутый JSON).
- SQL конверсии `000076` в `BEGIN…ROLLBACK` — файл
  `%TEMP%\opencode\qa_a1_idem.sql`.
