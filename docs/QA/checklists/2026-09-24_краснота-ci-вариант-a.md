# Независимая проверка: краснота CI (вариант A) — `cmd/art-studio/generator`

**Дата:** 2026-09-24
**Проверяющий:** @tester
**Предмет:** `cmd/art-studio/generator/ships_pixel_test.go` — `findPython()` проверяет `import PIL`, вызывающие тесты при `""` делают `t.Skip`.

## Прогоны

- `go test -race -short -count=1 -v ./cmd/art-studio/generator/` → `ok ... 21.742s`, `PASS`
- `go test -race -short -count=1 ./cmd/art-studio/postproc/` → `ok ... 2.680s`
- `go build ./...` → exit 0
- `go vet ./...` → exit 0

## Результаты по пунктам

| # | Пункт | Результат | Факт |
|---|---|---|---|
| 1 | Пакет генератора зелёный | OK | `ok zorion/cmd/art-studio/generator 21.742s` |
| 2 | Силуэтные тесты выполнены, не SKIP | OK | PASS: `TestShipSilhouettePixel` (0.46s), `TestShipSilhouetteForms` (6.55s, 13/13 подтестов), `TestShipSilhouetteHumansWingRegression` (0.50s), `TestShipSilhouettePixelAmmonia` (0.45s). SKIP — нет. |
| 3 | Нет Pillow → skip, скрипт не запускается | OK | `findPython` проверяет `exec.Command(p,"-c","import PIL").Run()` (ships_pixel_test.go:26) до возврата; `exec.Command(python,...make_ship_silhouettes.py)` — только после `if python == "" { t.Skip }` (ships_pixel_test.go:38–40, 116–118; формы — renderShipSpec:41–44, exec:55). Симуляции «python без Pillow» не было (venv с Pillow стоит первым кандидатом) — закрыто чтением кода. |
| 4 | build/vet чисто | OK | `go build ./...` exit 0, `go vet ./...` exit 0 |
| 5 | Изменены только ships_pixel_test.go + COORDINATION.md | OK | Наш дифф: `findPython` + skip-комментарии (19 строк). В рабочем дереве есть чужие правки. |
| 6 | postproc зелёный | OK | `ok zorion/cmd/art-studio/postproc 2.680s` |
| 7 | Другие тесты, запускающие python без проверки зависимостей | OK | Таких нет. |

## Пункт 5 — деталь

Наш дифф (подтверждён `git diff --stat`): `cmd/art-studio/generator/ships_pixel_test.go` (+19/−7) и `docs/COORDINATION.md` (+2; в том же файле — строка параллельной сессии про `migrations/000081_market_offers.sql` и market-файлы, её писал не мы).

Файлы, которых наша правка не касалась (параллельная сессия — рынок):
- изменён: `cmd/server/main.go` — роутинг `GET /api/planets/{id}/market` (market_handlers), к силуэтам/пикселям отношения нет;
- новые .go: `internal/handlers/market_handlers.go` (+тест), `internal/models/market_offer.go`, `internal/repository/market_repository.go` (+тест);
- новые не-Go: `migrations/000081_market_offers.sql`, `tools/e2e/*market*`, `tmp-*.js`;
- изменённые доки: `docs/ARCHITECTURE.md`, `docs/DB.md`, `docs/specs/README.md`, ideas-файл.

## Пункт 7 — деталь

`exec.LookPath("python")` / `exec.Command(...python...)` в тестах:
- `cmd/art-studio/generator/ships_pixel_test.go` — `findPython`, проверка PIL ✔
- `cmd/art-studio/generator/ships_silhouette_forms_test.go` — через тот же `findPython` ✔
- `cmd/art-studio/postproc/ship_cut_orient_test.go` — `findStudioPython`, проверка `PIL, numpy, scipy` ✔

Прочие тесты (`ship_cut_method_test.go`, `handlers/race_handlers_test.go`, `generator/ships_job_test.go`, `handlers/ships_handlers_test.go`) используют **фейковый** python (`.cmd`), реальных зависимостей не требуют — вне зоны.

Production-код (не тесты, без проверки зависимостей, но и не падает тестами): `cmd/art-studio/postproc/ship.go` (ShipFrameCheck/ShipSpriteCut/ProcessShip), `rembg.go` (RemoveBG), `handlers/ships_handlers.go` (shipOrientHint) — получают `pythonCmd` из конфига.

## Вердикт

**ПРОЙДЕНО.** Правка безопасна и решает красноту CI: зелёный пакет, силуэтные тесты реально выполнены, путь «нет Pillow → skip» подтверждён кодом, build/vet/postproc чисты, затронуты только заявленные файлы.

## Не проверено

- Симуляция «python есть, Pillow нет» — на машине venv ComfyUI с Pillow стоит первым кандидатом, отключить/переименовать нельзя (стоп-условие). Закрыто чтением кода (п.3).
- Прогон на реальном ubuntu-latest CI — недоступен локально; проверено соответствие логики (все кандидаты без PIL → `""` → skip).
