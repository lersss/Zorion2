# QA-прогон: «Ледяные астероиды в поясе (второй ресурс — вода)»

**Дата:** 2026-09-24
**Задача:** независимая проверка фичи «ледяные астероиды (второй ресурс — вода)» — код **НЕ закоммичен** (рабочее дерево)
**Спека:** `docs/specs/2026-09-24-ледяные-астероиды-вода.md` (§4–§12)
**Идея:** `docs/gamedesign/ideas/2026-09-24_ледяные-астероиды-вода.md`
**База:** `docs/specs/2026-09-22-пояса-малых-тел-этап-3-добыча.md` (осн. §N)
**Вердикт:** **ПРОЙДЕНО** — 8/8 пунктов PASS. Дефектов фичи не найдено.
Оговорки: 1 живой шаг e2e (live-сцена) SKIP без токена; T3-смоук потребовал одной setup-записи в dev-БД (обезличенный временный юзер). См. «Что НЕ проверено».

---

## Прогнал (команды)

```powershell
go build ./...                       # OK (exit 0)
go vet ./...                         # OK (exit 0) — после транзиентного сбоя от чужого И1-WIP (см. Наблюдения)
go test -race -short (go list ./... | ? { $_ -notmatch 'generator[/\\](planet|galaxy)$' })  # все ok, exit 0
go test -race -short -run 'Belt|Mining|Ice|Migration|Water' ./internal/handlers/           # ok
go test -race -short -v -run 'TestMigrationIceRemaining' ./internal/repository/            # PASS (M22)
# живой dev-сервер localhost:8080 (/health = 200 OK), dev-БД zorion
powershell -ExecutionPolicy Bypass -File tools/db.ps1 -Sql "..."   # 2 сводных запроса: пояса со льдом, goods «вода неочищенная»
node tools/e2e/belt-mining-check.js   # 21/21 PASS, PAGEERROR нет (live scene SKIP без QA_TOKEN/QA_BELT_ID)
node tools/e2e/belt-ice-shot.js       # errors=0, belt-ice-sprites.png / belt-ice-compare.png
node <temp>\belt-ice-live-smoke.js    # T3: enter->collect iron->collect ice->leave, ALL PASS
node <temp>\rngdiff.js                # T8: дифференциал RNG (no-ice), RNG-INVARIANT PASS
```

---

## Жёсткий чек-лист (T1–T8)

1. **T1 Сборка/линт/тесты** — **PASS** — `go build ./...` exit 0; `go vet ./...` exit 0;
   `go test -race -short` (без `generator/planet|galaxy`) — все пакеты `ok`, exit 0
   (вкл. `internal/handlers`, `internal/repository`, `internal/models`, `internal/cargo`).
   Первый прогон `go vet ./...` дал ошибки в `internal/economy/settlement` и
   `internal/repository` (`undefined: AttachNeedArithmetic`, `a.Need undefined`) — это чужой
   незакоммиченный WIP задачи «потребление по товарам» (И1), не лёд; в ходе прогона
   прод-код `AttachNeedArithmetic`/`Need` появился в дереве, повторный `go vet ./...` — exit 0.
2. **T2 Серверные тесты льда** — **PASS** — целевые `internal/handlers` + `internal/repository`:
   - M22 `TestMigrationIceRemaining` (`repository/planet_repo_test.go:36`) — PASS;
   - M23 `TestBeltMineEnterTwoResources`, M24 `TestBeltMineEnterIceOnly`
     (`iron_depleted_ice_alive`/`both_depleted`/`empty_composition`), M25
     `TestBeltCollectIceClampRate`, M26 `TestBeltCollectIceClampReserve`, M27
     `TestBeltCollectBothBuffersCargo`, M28 `TestBeltCollectResourceDefault`, M29
     `TestBeltCollectIceNoData` (`composition_has_no_ice`/`ice_remaining_NULL`), M30
     `TestBeltMineLeaveTwoGoods`, M31 `TestMiningFlushTwoReserves`
     (`both_buffers_direct`/`iron_only_skips_water`/`start_intra…`/`start_travel…`), M32
     `TestResolveWaterGoodByName`, M33 `TestBeltWaterGoodAbsent`
     (`enter_by_iron_succeeds`/`iron_depleted_water_absent`/`collect_ice_400`), M34
     `TestBeltKnowledgeGateIce`, M35 `TestBeltIceRateSplit` — все **PASS**.
   `internal/models` — своих тестов на второй ресурс нет (`[no tests to run]`), модель покрыта
   тестами handlers/repository.
3. **T3 Живой API-смоук** — **PASS** — сервер поднят (`/health` 200). Временным скриптом
   (в `%TEMP%`, не в репо) на поясе `Nemvil` (`0004ab0c-…`, kuiper, `composition_ice≈0.525`,
   `ice_remaining=20000`, `resource_ice` → good 426 «Вода неочищенная»):
   `enter` 200 (пакет с обоими ресурсами, `rate_ice=0.6998`, `ice_class=богатый`, `mined_ice=0`)
   → `collect {amount:100, resource:"iron"}` 200 (`granted=0.1946`, `mined=0.1946`)
   → `collect {amount:100, resource:"ice"}` 200 (`granted=2.7994`, `mined_ice=2.7994`)
   → `leave` 200 (`added=0.1946`, `added_ice=2.7994`, `position={status:orbit, object_type:belt,
   object_id:Nemvil}`); в `player_cargo` обе строки: «Вода неочищенная» 2.7994, «Железо Fe» 0.1946.
   Setup: одна запись в dev-БД (временный `qa_ice_<ts>` переставлен в мир/орбиту пояса) — см. Наблюдения O1.
4. **T4 Обратная совместимость `collect`** — **PASS** — без поля `resource` зачитывается железо:
   код `belt_mining_handlers.go:673-676` (`resource == "" → "iron"`) + тест M28
   `TestBeltCollectResourceDefault` PASS. Живой прогон T3c тоже слал `resource:"iron"`, ответ
   `resource:"iron"`.
5. **T5 Клампы** — **PASS** —
   (а) суммарный трюм: `granted := beltGranted(req.Amount, rateCap, delta, reserve, free-(mined+minedIce))`
   (`belt_mining_handlers.go:805`), `full: free-(minedNew+minedIceNew) <= 1e-9` (:870) — M27 PASS;
   (б) «полный трюм» только по сумме — M27/`ice-21` PASS;
   (в) запас не в минус: `granted ≤ reserve` внутри `beltGranted` (:138-140), апдейт под
   `FOR UPDATE` (:744-751) — M26 PASS;
   (г) потолок льда = 2× реально накапливаемой: `beltIceRateCap = 2·beltIceRateBase·k_ice` (:86),
   `rate_ice = beltIceRate = 0.667·k_ice` (:82, :388) — M35 `TestBeltIceRateSplit` PASS, M25 PASS.
6. **T6 Вход** — **PASS** —
   (а) пояс с выработанным железом и живым льдом входим — `belt_mining_handlers.go:585-600`
   (`iceAvailable && IceRemaining>0 → allDepleted=false`), M24 `iron_depleted_ice_alive` PASS;
   (б) пустой пояс (оба запаса 0) → 400 «Пояс выработан» — :597-600, M24 `both_depleted` PASS;
   (в) фолбэк (нет ресурса воды): `resolveIceGood` → `nil,nil` (:320-329), `water=nil` →
   ледяной гейт не активен, железо работает, не 500 — M33 PASS; состав пуст → 400 «нет данных»
   (:536-539), M24 `empty_composition` PASS.
7. **T7 Клиент e2e** — **PASS** — `node tools/e2e/belt-mining-check.js`: **21/21 PASS**, `PAGEERROR`
   нет; новые шаги `ice-16 vein-type`, `ice-16b render-no-throw`, `ice-17 ice-share`
   (share=0.909 при expected 0.926), `ice-18 target-resource`, `ice-18b collect-resource`
   (`{amount,resource:"ice"}`), `ice-19 two-buffers-hud`, `ice-20 fallback`, `ice-21 common-cargo-full`,
   `ice-22 belt-html-ids` — PASS. `node tools/e2e/belt-ice-shot.js`: `errors=0`,
   `iceSprite/iceVeinSprite/iceDebrisSprite=true`, кадры `artifacts/belt-ice-sprites.png`,
   `artifacts/belt-ice-compare.png`. Живой шаг `live scene` — **SKIP** (не заданы QA_TOKEN/QA_BELT_ID).
8. **T8 Регресс железа** — **PASS** — старые шаги `belt-mining-check.js` 08–19 PASS; железные
   числа/классы не менялись (`beltRateBase=0.25`, `beltRateCap=2·0.25·k_iron`,
   `beltReserveBase=600` — `belt_mining_handlers.go:36-75`, тесты `TestBeltCollectClampRate/…` PASS).
   RNG поясов без льда не сдвинут: `belt_world.js:153,200` — ролл `b.res` только при
   `this.iceAvailable && ice>0` (иначе `rng()` не вызывается, короткое замыкание). Дифференциал
   (`%TEMP%\rngdiff.js`): мир рабочего кода с `iceAvailable=false` побитово совпал с версией, где
   обе строки ролла удалены (одинаковые сигнатуры тел), при `iceAvailable=true` мир отличается и
   даёт `ice`-жилы — **RNG-INVARIANT PASS**; `ice-16` подтверждает `noIceBelt=0`, все жилы `iron`.

**Глазами (кадр `belt-ice-sprites.png`):** **PASS** — ледяной камень светло-голубой/белый с
кристаллическими гранями и **холодным белым** блеском руды; каменный — тёмно-бурый с **янтарным**
блеском; луч/искры по льду белые (не янтарные). На `belt-ice-compare.png` верхний ряд (лёд) и
нижний (камень) читаемо различаются.

---

## Наблюдения (не баги)

| # | Важность | Файл | Что |
|---|---|---|---|
| O1 | инфо | dev-БД | T3-смоук потребовал одной setup-записи: временный юзер `qa_ice_<ts>` переставлен в мир/орбиту ледяного пояса `Nemvil` (`UPDATE users SET current_world_id/current_position`). Данные общие — запись обезличенная и одноразовая; других правок БД не делал. |
| O2 | инфо | `internal/economy/settlement/*`, `internal/repository/settlement_arithmetic_test.go` | В начале прогона `go vet ./...` падал на чужом незакоммиченном И1-WIP (отсутствовали `AttachNeedArithmetic`/`Need`); к концу прогона прод-код появился — повторный прогон зелёный. Дерево правится параллельно другим агентом. |
| O3 | инфо | `belt_mining_handlers.go:924-940, 1049-1064` | «Вода исчезла из каталога посреди сессии» (leave/FlushTx) — named edge: ледяной буфер теряется с логом, полёт не блокируется. Отдельного теста на этот путь нет (M33 покрывает отсутствие на входе/collect). |

---

## Предложения в чек-лист (применит @manager)

- Добавить регрессионный кейс «`leave`/`FlushTx` при отсутствии ресурса воды посреди сессии»
  (сейчас — только код + named edge O3).
- Зафиксировать как регресс «RNG-инвариант поясов без льда» (дифференциал `rngdiff`): полезен
  при любой будущей правке `belt_world.js` — проверяет, что новый ролл не тратит `rng()`.
- Кейс «живой двухфакторный заход через API» (T3) можно оформить скриптом в `tools/e2e/` —
  сейчас прогон делался временным скриптом вне репо.

---

## Что НЕ проверено (граница покрытия)

- **Живой шаг `live scene` в `belt-mining-check.js`** — SKIP, т.к. не заданы `QA_TOKEN`/`QA_BELT_ID`
  (нужен игрок в поясе). Живое поведение закрыто T3 (API-смоук на реальном ледяном поясе).
  Чтобы закрыть браузерно: передать токен игрока в поясе.
- **Тайминг «полный трюм льда за 2–3 мин»** — это числа/баланс (`@balancetester`), не проверял;
  серверные клампы/скорости сверены (T5).
- **Экстремальные пояса** (пустой состав, `iron=0`+`ice>0` живьём, пояс без льда в dev-БД) —
  покрыты юнит-тестами M24/M33; живьём достижимы только через setup-записи БД, не гонял.
- **Визуал/UX глазами игрока в движении** (тосты, HUD при долгой добыче, гашение жил) — проверен
  программно (e2e `ice-19/20/21`) и по кадрам; живой сценарий глазами создателя — за ним.
- **Пакеты `generator/planet|galaxy`** — не трогались фичей (новых роллов генератора нет),
  в `-race -short` исключены по инструкции, отдельно не гонял.
