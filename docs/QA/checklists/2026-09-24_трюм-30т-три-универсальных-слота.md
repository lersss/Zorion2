# QA-прогон: трюм — дельта §20 (модуль +30 т, три универсальных слота, видимость)

**Дата:** 2026-09-24
**Задача:** независимый прогон правки трюма (дельта §20 спеки `docs/specs/2026-09-22-трюм-грузоподъёмность-корабля.md`)
**Вердикт:** **ПРОЙДЕНО** (8/8 пунктов PASS). Найден 1 дефект тест-обвязки (e2e не обновлён под 50 т) — не баг приложения.

## Что проверялось

Дельта §20 (решения создателя 2026-09-24):
- модуль `cargo_1`: `params.capacity` 80 → **30 т** → стартовая ёмкость **50 т** (20 врождённых + 30);
- `starter.slots.universal` 1 → **3** (два новых пустые);
- универсальные слоты **видны** в разделе «Корабль» (`web/index.html`);
- перегруженные трюмы обрезаны миграцией `000080`.

Код правки (рабочее дерево, не закоммичено):
- `internal/ship/catalog.go:36` — `capacity: 30`;
- `internal/ship/models.go:27` — `"universal": 3`;
- `web/index.html:171–173, 401–452` — id-ячейки `slot-universal{,2,3}`, цикл по `slots.universal`, ветка `slotDetail('cargo')`, `cargoOnly`;
- `migrations/000080_cargo_hold_delta.sql` — значение модуля, форма модели, обрезка `player_cargo`.

## Прогнал (команды)

- `go build ./...` → exit 0; `go vet ./...` → exit 0.
- `go test -race -short (go list ./... | Where-Object { $_ -notmatch 'generator[/\\](planet|galaxy)$' })` → все пакеты ok (gcc WinLibs в PATH).
- `go test -race -count=1 ./internal/handlers/... ./internal/repository/... ./internal/integration/... ./internal/cargo/... ./internal/ship/... ./internal/models/...` → ok (свежий прогон, без кэша).
- `GET /health`, `GET /me`, `GET /api/cargo`, `POST /api/belt/mine/{enter,collect,leave}` — живой dev-сервер (`http://localhost:8080`).
- SQL к dev-БД через `tools/db.ps1`.
- Браузер: `node tools/e2e/map-check.js` (point 8) + временный playwright-скрипт на системном Chrome (раздел «Корабль»).
- `node tools/e2e/dashboard-cargo-check.js` (диагностика устаревшего e2e).

## Сценарии (жёсткий чек-лист, по одному прогону)

| # | Проверка | Результат | Факт |
|---|---|---|---|
| 1 | Ёмкость трюма стартового корабля = 50 т | **PASS** | `GET /api/cargo` свежего игрока: `{"used":0,"total":50}`; `/me.ship_model.base_capacity=20`, `equipment.universal=cargo_1`, каталог `cargo_1.params.capacity=30` |
| 2 | В «Корабле» 3 универсальных слота | **PASS** | В браузере видимы ровно `slot-universal`, `slot-universal2`, `slot-universal3` (итого 6 видимых слотов: radar/scanner/engine + 3 universal); `slots.universal=3` из `/me` |
| 3 | Слот 1 «Грузовой модуль-1» +30 т; слоты 2–3 «не установлен» | **PASS** | `slot-universal` = «📦Грузовой модуль-1+30 т»; `slot-universal2/3` = «📦не установлен» |
| 4 | Открытие «Корабля» — без JS/тост-ошибок (реальный браузер) | **PASS** | pageerror=0, toast-error=false; консоль чистая (1-й прогон дал resource-404 без JS-исключения, 2-й — 0 ошибок, `HTTP404=[]`; флейкость ресурса, не JS) |
| 5 | Блок «Трюм»: `занято ≤ всего`, полоса ≤ 100% | **PASS** | label «0 / 50 т», fill 0%, used=0 ≤ total=50 |
| 6 | `qa_cargo_124736` больше не перегружен | **PASS** | SQL: `SUM(quantity*weight)=50`, ёмкость 20+30=50 → `used ≤ total`; миграция `000080` обрезала 95 т → 50 т |
| 7 | Регресс добычи: добытое в трюм, упор в 50 т | **PASS** | `POST /api/belt/mine/enter` → `cargo={used:0,total:50,free:50}`; `collect(amount=100)` → `granted=0.0024`, буфер растёт; `leave` при буфере 80 → `added=50` (кламп по свободному 50, не 80); `GET /api/cargo` → `used=50,total=50` |
| 8 | e2e-смоук `map-check.js` | **PASS** | `ALL PASS` (1/5…5/5), без разлогина/тост-ошибок; скриншоты в `tools/e2e/artifacts/` |

## Найденное

### Наши дефекты (правка трюма)

- **Багов приложения не найдено.**
- **Дефект тест-обвязки (не рантайм):** `tools/e2e/dashboard-cargo-check.js:129` (и `:139,147,152–153,182,202,217`) жёстко ждёт `"0 / 100 т"`, `total: 100` — после дельты приложение отдаёт 50 т. Прогон: `[2/5 empty hold (real API)] FAIL - {"label":"0 / 50 т", ...}`. Функционально UI корректен (label «0 / 50 т»); сломан сам e2e. Требуется обновить ожидания 100 → 50 (и моки `total: 100` → 50). **Это e2e-долг нашей дельты, а не баг.**

### Чужие дефекты (WIP-линия «лёд»)

- На момент прогона **не проявились**: `go test ./internal/handlers/...` и `./internal/repository/...` — **ok** (свежий `-count=1`, в т.ч. с `-race`). Предупреждение о красных тестах из-за льда на текущем состоянии дерева **не подтвердилось** (тесты зелёные). Если это следствие незакоммиченных правок WIP — фиксирую факт, ответственность линии «лёд».

## Чеклист непроверяемого

- **Визуальная компоновка 3 универсальных ячеек на силуэте (наложение/эстетика)** — проверено программно (все 3 видимы, не скрыты, `.ship-slot-future`=0, дублей нет), но «без наложения и красиво» — вкусовое/визуальное решение создателя. Нужно: взглянуть на `tools/e2e/artifacts/…`/`/` вкладку «Корабль» глазами (скриншот раздела снят временным скриптом).
- **Идемпотентность миграции `000080` повторным прогоном на живой БД** — не перезапускал миграцию поверх dev-БД (риск задеть чужое состояние); логика `jsonb_set`/`||`/обрезки идемпотентна по построению, новый тест `internal/integration/cargo_capacity_migration_test.go` — ok. Нужно для полного закрытия: прогон миграции дважды на изолированной БД (если тест этого не делает — см. предложение).
- **Прогрессия `cargo_2`/`cargo_3` (200/500 т)** — вне скоупа §20.6, не трогалась; не проверял.
- **Интерфейс установки/снятия модулей в пустые слоты** — механики нет (§20.11), пустые слоты проверены как «не установлен».
- **Аккаунты, кроме `qa_cargo_124736`, с грузом сверх 50 т** — SQL показал максимальный used=50 (только он был перегружен); обрезка остальных не потребовалась.

## Предложения в чек-лист (@manager)

- **B-CARGO-1 (новая):** стартовая ёмкость трюма = **50 т** (`20 врождённая + cargo_1 30`); `GET /api/cargo` и блок «Трюм» дают `total=50`; в разделе «Корабль» **3** универсальных слота (1 занят «Грузовой модуль-1» +30 т, 2 «не установлен»), `.ship-slot-future` отсутствуют.
- **B-CARGO-2 (новая):** миграция `000080` — `cargo_1.params.capacity=30`, `starter.slots.universal=3`, перегруженный `player_cargo` обрезан до `used ≤ total` (кейс `qa_cargo_124736`: 95 → 50); идемпотентность повторного прогона.
- **B-BELT-7 (уточнить):** жёсткий предел трюма в добыче — **50 т** (было 100): `enter` отдаёт `cargo.total=50`, `leave` клампит буфер по 50 т.
- **Устаревший кейс:** e2e `dashboard-cargo-check.js` — перешить ожидания 100 → 50 (иначе ложный FAIL).
