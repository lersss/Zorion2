# QA-прогон: арт-студия — вкладка «В игре» (витрина кораблей игрового реестра)

**Дата:** 2026-09-25
**Задача:** независимая проверка новой вкладки «В игре» (`cmd/art-studio`) — код **НЕ закоммичен** (рабочее дерево)
**Идея:** `docs/gamedesign/ideas/2026-09-25_арт-студия-витрина-кораблей-игры.md`
**Студия:** `http://127.0.0.1:8798` — уже поднята менеджером, **не перезапускалась и не убивалась** (создатель смотрит на неё)
**Вердикт:** **ПРОЙДЕНО** — 6/6 пунктов жёсткого чек-листа PASS.
Единственная аномалия — 404 `/favicon.ico` (фоновая просьба браузера, в студии файла нет; к фиче не относится, JS-ошибок нет).

**Побочные данные не тронуты:** пул `ai_drafts/ships_pool`, принятые корабли, игровые спрайты — только чтение (GET). Операций приёмки/удаления/поворота не выполнялось.

---

## Прогнал (команды)

```powershell
# студия уже запущена менеджером — только читаю
Invoke-WebRequest GET http://127.0.0.1:8798/                                  # 200, HTML
Invoke-WebRequest GET http://127.0.0.1:8798/ships/ingame                     # 200, JSON 118
Invoke-WebRequest GET http://127.0.0.1:8798/ships/ingame/img/...            # 200 image/png
# обход пути: ..%5Cstudio.json, ..%2Fstudio.json, ../../config/art/studio.json, ..%2F..%2Fconfig%2Fart%2Fstudio.json
cd tools/e2e; node ships-ingame-check.js                                      # браузерный прогон, RESULT 6/7 (см. п.5)
```

> DoD Go не гонял: правка затрагивает `cmd/art-studio` (импорт `internal/models`), живой рантайм уже собран и отвечает новыми эндпоинтами; перезапускать студию запрещено условием задачи. Прогон `go build/vet/test` — по договорённости отдан способности @developer (см. «Что НЕ проверено»).

---

## Жёсткий чек-лист (6 пунктов)

1. **`GET /` → в HTML есть кнопка вкладки «В игре»** — **PASS** — `tabbtn-ingame` присутствует в отдаваемом HTML; `cmd/art-studio/web/index.html:73` (`<button class="tab" id="tabbtn-ingame" onclick="showTab('ingame')">В игре</button>`).
2. **`GET /ships/ingame` → 200, ровно 118 записей** — **PASS** —
   - `Status=200`, `Content-Type: application/json; charset=utf-8`;
   - `COUNT=118`;
   - первая: `{"file":"race_humans_starship.png","id":"race_humans_starship","name":"Дозорный корабль (люди)","race":"humans","race_name":"Люди","angle":21.1,"flip":false}`;
   - последняя: `{"file":"neutral.png","id":"neutral","name":"Нейтральный","race":"","race_name":"","angle":0,"flip":false}` — раса пустая;
   - у записи с нулевым углом (`race_humans_starship_02.png`) поля **присутствуют**: `"angle":0,"flip":false` — DTO не теряет нули (`handleShipsIngame`, `cmd/art-studio/handlers/ships_handlers.go:236-261`).
3. **`GET /ships/ingame/img/race_humans_starship.png` → 200 `image/png`; выход из папки отклонён** — **PASS** —
   - `race_humans_starship.png` → **200**, `image/png`, `len=31897`;
   - `..%5Cstudio.json` → **404**;
   - `..%2Fstudio.json` → **404**;
   - `../../config/art/studio.json` → **404**;
   - `..%2F..%2Fconfig%2Fart%2Fstudio.json` → **404**.
   Чужой файл не отдаётся: `filepath.Base` в `handleShipsIngameImg` (`ships_handlers.go:263-267`), каталог — `gameSpritesDir()` (`ships_handlers.go:447`).
4. **Браузер: вкладка «В игре»** — **PASS** по всем подпунктам (headless Chrome, `tools/e2e/ships-ingame-check.js`) —
   - (а) группы по расам отрисованы, **групп = 58**, первая — «**Люди · 12**» (людской блок 12, `cmd/art-studio/web/index.html:788-795`);
   - (б) у всех групп есть счётчики: заголовки вида «Имя · N» (58/58);
   - (в) картинки реально загрузились: `naturalWidth>0` у **118/118**;
   - (г) блок «**Без корабля: 3**», в нём **ровно 3** кнопки: `Крио-небесные` (cryo_sky), `Гейзерные` (geysers), `Ледяной планктон` (ice_plankton) — совпадает с идеей (`index.html:798-811`);
   - доп.: пара показа применена — у первой карточки класс `orient rot`, `transform` ≠ `none`.
5. **Браузер: клик по первой расе из «Без корабля» → вкладка «Корабли рас» + выбранная раса; консоль без JS-ошибок** — **PASS** —
   - клик по `Крио-небесные` → вкладка `#tab-ships` видима, `#shipRace.value = cryo_sky` (совпало с ожидаемым слагом);
   - `pageerror = []` — **JS-ошибок нет**;
   - в консоли один `404` на `/favicon.ico` (браузер сам просит иконку; в `cmd/art-studio/web/` только `index.html`, favicon отсутствует **до** этой фичи — не регрессия, на витрину не влияет).
6. **Скриншот вкладки «В игре»** — сохранён: **`tools/e2e/artifacts/ingame-tab.png`** (325 149 байт).

Скрипт браузерного прогона: `tools/e2e/ships-ingame-check.js` (X) + журнал `tools/e2e/artifacts/ships-ingame-check.log`.

---

## Найденное

- **Багов, ломающих фичу, не найдено.**
- **Наблюдение (не дефект фичи):** `GET /favicon.ico` → 404. Файла иконки в `cmd/art-studio/web/` нет, браузер запрашивает его на любой вкладке. Не связано с витриной; закрывать — по желанию (отдельная мелочь вне скоупа). JS-ошибок нет.

---

## Что НЕ проверено (границы покрытия)

- **DoD Go (`go build ./...`, `go vet ./...`, `go test -race -short`).** Причина: условие задачи прямо запрещает перезапускать/убивать студию, а живой рантайм уже собран менеджером. Поведенчески новый код подтверждён (эндпоинты отвечают), но компиляцию/vet/тесты пакета нужно прогнать @developer или при гейте с разрешением рестарта.
- **Попарная сверка `angle/flip` каждой из 118 записей с реестром `internal/models/race_ship_sprites.go`.** Причина: без сборки/импорта реестра извне — это контентная сверка; я проверил лишь применение трансформа к первой карточке и наличие полей в JSON у нулевой записи. Полная сверка «JSON ↔ реестр ↔ отображение» — на стороне @developer/@balancetester.
- **Визуальная оценка витрины глазами игрока/создателя** (читаемость подписей, порядок, цвета). Причина: вкусовое/UX; доступен скриншот `tools/e2e/artifacts/ingame-tab.png` — оценку даёт создатель.
- **Открытие картинки в новой вкладке по клику** (заявлено в идее §6) — в headless не проверял (открытие target'а). Причина: не входило в жёсткий чек-лист; onclick виден в разметке (`index.html:791`).

---

## Предложения в чек-лист (применит @manager)

- Новые сценарии (регресс витрины): «`/ships/ingame` = 118, порядок реестра, `angle/flip` присутствуют при нулях», «`/ships/ingame/img` не отдаёт вне папки (traversal 404)», «вкладка „В игре“: 58 групп, первая людская, счётчики, блок „Без корабля“ = 3 (cryo_sky, geysers, ice_plankton)», «клик по расе без корабля переключает в „Корабли рас“ с выбранной расой, консоль без `pageerror`».
- Добавить в будущий прогон проверку «открытие картинки в новой вкладке» (target) — сейчас не покрыто.
