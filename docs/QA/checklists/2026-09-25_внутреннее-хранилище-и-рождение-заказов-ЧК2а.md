# ЧК2а — Внутреннее хранилище поселений + рождение заказов + минимальный UI

**Прогон:** `@tester`, независимый, 2026-09-25.
**Норматив:** `docs/specs/2026-09-25-внутреннее-хранилище-и-рождение-заказов.md` (ревизия 6):
§1.4/§1.6/§1.7, §4.5, §5.3, §5.5, §7 (пп.1–7), §8 (видимость), §12 (T2a/T8/T9/T10/T15/T17/T19/T20);
концепт вида — `docs/specs/2026-09-25-внутреннее-хранилище-вид-и-иконки.md` §3–§5.
**Состояние кода:** НЕзакоммиченное рабочее дерево (подэтапы 1–3а в `a35e701`, 3б и UI — незакоммичены).
Код не правил, не коммитил.

## Жёсткий чек-лист

1. PASS — `go build ./...` (0), `go vet ./...` (0), `go test -race -short (go list ... -notmatch generator/(planet|galaxy))` — все пакеты ok, exit 0.
2. PASS — живьём под admin-JWT: `GET /api/worlds/dd898db4…/planets` → блок `storage` есть (`size=1000`, 4 ячейки: `position/good_id/code/amount/cap/share/need_kind/effect/deficit`); SQL `settlement_storage_cells` (36 строк) + `settlements.storage_size` подтверждают носитель.
3. PASS — presence+настройка: handler-тест `TestStorageVisibilityByMode` + `TestGetPlanetsByWorldReadsArithmeticVisibilityTrue` (storage с ячейкой/`code`/`need_kind`) + код `playerSettlements` (гейт на весь блок, per-cell фильтра нет → производственные ячейки входа рецепта игроку проходят). Живой presence-путь не воспроизведён (нет живого игрока на орбите, см. «непроверяемое»).
4. PASS — настройка выключена: handler-тест `TestGetPlanetsByWorldReadsArithmeticVisibilityFalse` (`Storage == nil`); живьём plain-player без присутствия → `settlements_visible=0`, `storage_visible=0` (утечки нет).
5. PASS — снимок/scan: `knowledge_repository.go:224–231` `snapshotBranch` без `Output`/`Storage`; `buildPresenceSnapshot` (`:262–299`) хранилище не замораживает; `planet_visibility.go:359+` `stripSnapshotSettlementSecrets` ставит `Storage = nil` всегда; тест `TestStorageVisibilityByMode` (snapshot → nil).
6. PASS — живьём: `GET /api/planets/f5d272f9…/contracts` → 8 открытых долей, все `author_type='settlement'`, `package_key=supply:<planet>:<settlement>:<good>`, `requirements.quantity` = порог ячейки (500/250), `reward>0`. `contracts WHERE author_type='settlement'` было 0 до чтения → рождение по дефициту (`amount < cap`) работает.
7. PASS — код `SyncPlanetStorageCommitIfChanged` (`planet_repo.go:591+`) вызывается в `GetPlanetBoard` (`contract_handlers.go:148–157`) ДО `MaterializeBoard`; IT на живой БД `TestSyncPlanetStorageCommitIfChangedNoopIT` (повтор без изменения → `computed_at` не двигается) и `TestBoardFreshnessNoDeadlockIT` (4 цикла sync×materialize — дедлока нет) — PASS.
8. PASS — IT `TestMaterializeBoardFreeModeIT`: нулевой счёт владельца → `reward=0`, `escrow_amount=0`, `money_operations` нет, CHECK не падает. Живьём free-mode не гонял (нужен владелец с нулём; тест T8 — эквивалент).
9. PASS — IT `TestMaterializeBoardTakenShareNoNewSharesIT` (taken → 0 новых открытых) + `TestMaterializeBoardCancelsDroppedPackageIT` (пакет выпал → open снят `cancelled`, залог возвращён, лог `superseded`). Смешанный случай (одна доля taken + другая open того же пакета) отдельным тестом не закрыт — см. предложения.
10. PASS — web-тест `frontend_branches_test.go` (Node): нет `остаток (осадок)`, нет `Вход (виден только админу)`, нет заголовка `Выход`; остались `сделано/съедено/скорость поедания/забираем/доля залежи/скорость (число пары)`. `web/static/js/modal/branches.js` — `branchBlockHtml` без буферов.
11. PASS — `tools/e2e/map-check.js`: ALL PASS, exit 0. `[1/5] register/login PASS · [2/5] map filter=200 canvasPx=15406 pageErrors=0 · [3/5] click star PASS modal=true toastError=false · [4/5] restricted card PASS dist=6764 radar=800 · [5/5] screenshots PASS`. Артефакты: `tools/e2e/artifacts/map.png`, `restricted-card.png`.

**Итог:** PASS 11 / FAIL 0 / SKIP 0.

## Найденное

- **Функциональных багов не найдено.**
- **Ловушка окружения (не баг кода, важное для прогонов):** живой dev-сервер был устаревшим
  (`zorion-server.exe` собран 7:45, исходники правлены 9:58–10:40; в public-БД `settlement_branch_buffers`
  уже переименована в `_retired`, а старый бинарь её ещё читал → `GET /api/worlds/…/planets` отдавал **500**).
  Пересобрал (`go build -o zorion-server.exe ./cmd/server`) и перезапустил (AGENTS §13) — после этого все
  живые проверки прошли. Логи: `logs/qa-chk2a-server.{out,err}.log`.
- **Дрейф дока (не код):** `docs/COORDINATION.md:60` утверждает «миграция 000086 ещё не написана», но
  `migrations/000086_settlement_storage_cells.sql` и `000087_retire_branch_buffers.sql`/`000088_contract_author_settlement.sql`
  уже существуют и применены. Владелец — `@manager`/`@dispatcher`.

## Пробелы покрытия (не FAIL)

- **T9 смешанный случай:** нет теста «в пакете одна доля `taken`, при этом ещё одна `open` — материализация
  снимает открытую (`superseded`) и новых не публикует». Код (`takenKeys` → `target=nil` → `reconcileShares`)
  это делает, но прямого assert нет.
- **Item 3 живьём:** production-ячейки под player-JWT с присутствием проверены только handler-тестом + кодом
  (у живого игрока нет присутствия на планете с владельческим поселением; менять глобальную настройку
  `settlement_arithmetic_visible_to_player` на общей dev-БД нельзя).

## Предложения в чек-лист (применит @manager)

- Добавить кейс `B-STORAGE-1`: доска планеты с владельческим поселением и `amount < cap` → `author_type='settlement'`,
  `package_key=supply:<planet>:<settlement>:<good>`, `quantity=⌊cap−amount⌋` (живой прогон пункта 6).
- Добавить кейс `B-STORAGE-2`: смешанный пакет (taken + open) при материализации — открытая снята `superseded`,
  новых нет (закрыть пробел T9).
- Дополнить «DoD/окружение»: перед живыми API-проверками сверять время сборки `zorion-server.exe` с правкой
  исходников (иначе 500 из-за ретайра таблиц) — сейчас это ловушка, ловится только вручную.

## Что НЕ смог проверить сам (граница покрытия)

| Что | Почему | Чем закрыто косвенно / что нужно |
|---|---|---|
| Item 3 живьём (player + presence + настройка вкл, производственные ячейки) | нужен живой игрок, физически присутствующий на орбите планеты с владельческим поселением; менять его присутствие/настройку на общей dev-БД нельзя | handler-тест `TestStorageVisibilityByMode`/`TestGetPlanetsByWorldReadsArithmeticVisibilityTrue` + код `playerSettlements`; создателю — при визуальной приёмке зайти на орбиту такой планеты и включить настройку |
| Item 8 живьём (free-mode на нулевом счёте) | нужен владелец с нулевым балансом; на живых данных владельцы платёжеспособны | IT `TestMaterializeBoardFreeModeIT`; создателю — при желании обнулить счёт тестового владельца |
| Item 9 живьём (taken → снятие open) | взятие доли меняет состояние общей доски | IT T9-тесты; отдельного живого прогона не делал |
| Визуальный рендер блока «Внутреннее хранилище» в браузере | нужен игрок с присутствием; смоук `map-check.js` карточку поселения не открывает | Node-тест `storageBlockHtml` (web) + чтение `branches.js`; создателю — глазами на орбите |
| Числовая калибровка размеров/долей (`params.storage.size`, `shares`) | баланс — зона `@balancetester` | не моя зона |
