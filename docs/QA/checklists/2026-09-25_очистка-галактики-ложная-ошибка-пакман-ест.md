# QA-прогон — баг-фикс «Очистка галактики — ложная ошибка про пакмана»

**Дата:** 2026-09-25
**Задача:** `docs/gamedesign/ideas/2026-09-25_очистка-галактики-ложная-ошибка-пакман-ест.md`
**Вердикт:** ✅ ПРОЙДЕНО (без оговорок)
**Режим:** неразрушающий (общая dev-БД; реальная очистка/пакман НЕ запускались)

Проверяемые правки (не закоммичены):
- `internal/handlers/admin_universe.go` — `ClearUniverse`
- `internal/handlers/admin_pacman.go` — `StartPacman`
- `internal/handlers/admin_pacman_test.go` — тесты
- `web/static/js/admin/generation.js` — `clearUniverse`

---

## 1. Сборка и vet — PASS

- `go build ./...` → `BUILD_OK` (exit 0)
- `go vet ./...` → `VET_OK` (exit 0)

## 2. Тесты пакета handlers с детектором гонок — PASS

- `go test -race -short -count=1 ./internal/handlers/` → `ok zorion/internal/handlers 21.403s` (exit 0)
- Wall-time ~23.4 с (первый прогон был cached — п.2 зафиксирован на `-count=1`).

## 3. Целевые тесты — PASS (5/5)

`go test -race -count=1 -run 'TestClearUniverseMutexHeldByPacman|TestStartPacmanMutexHeld|TestClearUniverseBlockedByPacman|TestClearUniverseBlockedByGenerateFactions|TestClearUniverseBlockedByRegenerate' ./internal/handlers/ -v`

- `TestStartPacmanMutexHeld` --- PASS
- `TestClearUniverseBlockedByPacman` --- PASS
- `TestClearUniverseMutexHeldByPacman` --- PASS
- `TestClearUniverseBlockedByGenerateFactions` --- PASS
- `TestClearUniverseBlockedByRegenerate` --- PASS
- `ok zorion/internal/handlers 1.240s`, exit 0

Подтверждение «в теле 409 нет `Pacman`»: оба теста занятого замка содержат
`require.NotContains(body, "Pacman")` — `admin_pacman_test.go:448` (`TestStartPacmanMutexHeld`)
и `admin_pacman_test.go:481` (`TestClearUniverseMutexHeldByPacman`).

## 4. Текст 409 и список джобов `ClearUniverse` — PASS

`internal/handlers/admin_universe.go`:
- занят `universeMutationMu` → `"Universe mutation is running, wait for it"` (`admin_universe.go:922`).
- любой из джобов — `"Generation is running, cancel it first"` (`admin_universe.go:913`).

Фактический список проверяемых джобов (9 условий = 8 джобов + пакман), строки 904–912:
1. `JobGenerateUniverse`
2. `JobGeneratePlanets`
3. `JobRegeneratePlanets`
4. `JobGenerateNPC`
5. `JobHypothesis`
6. `JobGenerateFactions`
7. `JobGenerateRaceSettlements`
8. `JobGenerateSettlements`
9. `JobPacman`

Набор совпадает с 8 джобами `StartPacman` (`admin_pacman.go:126–133`) + сам пакман.

## 5. Ложный текст в `internal/` — PASS

`rg "Pacman is eating" internal/ web/` → единственное вхождение — комментарий
теста `internal/handlers/admin_pacman_test.go:467`. Рабочего текста нет.
`StartPacman` при занятом замке отдаёт нейтральное сообщение
(`admin_pacman.go:120`); прочие ручки замка уже отвечали нейтрально
(`admin_deposits.go:163`, `admin_settlement_branches.go:88,215`,
`admin_settlement_effects.go:82`, `admin_structures_create.go:87`,
`admin_universe.go:782`).

## 6. Клиентский флоу кнопки — PASS (код)

`web/static/js/admin/generation.js`, `clearUniverse`:
- 371: `const btn = document.getElementById('clearUniverseBtn')`
- 372: запоминается `originalLabel` (`btn.textContent`)
- 373–376: `btn.disabled = true`; `btn.textContent = '🗑️ Очищаем…'`
- `try/fetch` 377–388: успех (`res.ok`, 380) и ошибка (`else`, 385) + `catch` (388)
- `finally` 389–394: `btn.disabled = false`; `btn.textContent = originalLabel` — восстанавливается и при успехе, и при ошибке/исключении.

Кнопка существует: `web/admin.html:187` (`id="clearUniverseBtn"`).
Смежное (не менялось): `poll.js:72–73` дизейблит кнопку, пока `jobType==='pacman'`
в статусе running, и снимает в 88.

## 7. e2e-смоук `tools/e2e/map-check.js` — не гонялся (не применим)

Не запускал. `map-check.js` покрывает **игровую карту** (регистрация/логин,
`/map`, клик по звезде, restricted-карточка, скриншоты) — `tools/e2e/README.md:28–46`;
упоминаний админки/`clearUniverse` в смоуке нет (`rg "admin|clearUniverse|Очистка"
tools/e2e/map-check.js` → пусто). Прогон не добавил бы доказательств по фиксу,
а реальная очистка в смоуке всё равно недопустима на общей dev-БД.
Флоу закрыт кодом п.6 + юнит-тестами п.3 (серверная ветка 409) + п.4.

## 8. Регресс соседних ручек мутаций — PASS

Полный прогон п.2 (`./internal/handlers/`) уже включал эти тесты; для явного
подтверждения прогнаны адресно:
`TestAddDepositPacmanGate`, `TestAddDepositMutexGate`, `TestAddBranchPacmanGate`,
`TestAddBranchMutexGate`, `TestSetSettlementEffectLoadGates`,
`TestPacmanBlocksOtherHandlers` (8 подтестов), `TestCreateStructureSettlement`,
`TestCreateStructureBuilding` → все PASS, `ok 1.311s`.

---

## Найденные баги

Нет.

## Предложения в чек-лист (@manager)

- Новый регрессионный кейс: «Занятый общий замок мутаций вселенной → 409 с нейтральным
  текстом, без упоминания пакмана» (тест `TestClearUniverseMutexHeldByPacman` /
  `TestStartPacmanMutexHeld`).
- Новый кейс: «`ClearUniverse` блокируется идущим смежным джобом (фракции/агенты/
  поселения) до записи в БД» (`TestClearUniverseBlockedByGenerateFactions`).
- Устаревших кейсов не выявлено.

## Что НЕ смог проверить (граница покрытия)

- **Живая очистка крупной БД (~28 с) и повторный клик в браузере** — запрещено на
  общей dev-БД (снесло бы чужие данные). Закрыто на уровне кода: клиентский
  `disabled`/`finally` (п.6) + серверный 409 (п.3–4). Нужен создатель на
  изолированной БД, если требуется подтверждение «вживую».
- **Визуал «🗑️ Очищаем…» и поведение кнопки глазами игрока** — вкусовое/визуальное,
  не проверяю статически; нужен запуск `start-all.cmd` + вход в админку с ролью
  `admin`/`skycomposer` (учётки у меня нет).
- **Реальный e2e-флоу админки** — в `tools/e2e` смоука админки нет (только игровая
  карта), нужно завести отдельный `admin-check.js` или прогнать вручную создателю.
- **Поведение под реальной конкуренцией джобов в рантайме** (flush фонового тика +
  TRUNCATE) — авто-повтор дедлока не входил в объём фикса и не проверялся.
