# ЧК2б — Сдача груза и зачёт (контракты поселений)

**Прогон:** `@tester`, независимый, 2026-09-25.
**Норматив:** `docs/specs/2026-09-25-сдача-груза-и-зачёт-ЧК2б.md` (принята создателем 2026-09-25);
смежное — `docs/specs/2026-09-25-внутреннее-хранилище-и-рождение-заказов.md` (ЧК2а).
**Состояние кода:** НЕзакоммиченное рабочее дерево (подэтапы 1–2). Код не правлен, не закоммичен.

> **Пометка `@manager`.** Сессия тестера была остановлена сторожем зацикливания (loop-guard) после
> серии однотипных SQL-вызовов: файл отчёта тестер не успел записать — отчёт воспроизведён здесь
> менеджером из ответа тестера дословно по фактам. QA-фикстуры в dev-БД после прогона **вычищены
> менеджером** (9 фиктивных контрактов и связанные строки удалены; ячейка воды поселения
> `95f85337…` возвращена к `amount = 0`; трюм и позиция QA-игрока очищены). Реального списания с
> чужого счёта не было — записей `escrow_lock` по фикстурам не оказалось (контракты вставлены как
> фикстуры, не через публикацию).

## Жёсткий чек-лист

1. PASS — `go build ./...`=0, `go vet ./...`=0, `go test -race -short` (без `generator/planet|galaxy`) — все пакеты `ok`, FAIL/DATA RACE нет.
2. PASS — новые тесты реальны (не пустышки), каждый что-то утверждает (перечень — ниже).
3. PASS — роут `POST /api/contracts/deliver` есть; без токена → 401 (после пересборки сервера).
4. PASS — живьём: нет контракта → 404; `travel` → 422; supply `completed` → 409; чужой исполнитель → 403.
5. PASS — живьём: `surface` → 422; орбита другой планеты → 422; правильная орбита → проходит; текст «Сдать можно только с орбиты планеты заказа».
6. PASS — живьём: частичная сдача 12/20 → выплачено 120, остаток 8, эскроу 200→80, ячейка 0→12; затем полная сдача 8 → выплачено 80, `completed`, ячейка 20; **Σ выплат 120+80=200 = исходному залогу**.
7. PASS — «не то» (нужен 378, в трюме только 426) → 422, трюм не тронут; «лишнее»: трюм 100 → взято 8, в трюме осталось 92.
8. PASS — ячейка = двойному порогу → 409, требование/эскроу/ячейка/трюм не изменились; при `amount=990` сдано ровно 10 (`2·cap − amount`), ячейка 1000.
9. PASS — ячейки владельца по товару нет → 409 (fail-closed), груз остался (426:10), авто-создания ячейки нет.
10. PASS — автор-строение → 422 «сдача от строений пока не поддерживается» (задел).
11. PASS — бесплатный supply: полная сдача → ровно одна `money_operations` с `delta=0`; частичная при `pay=0` → 0 записей `money_operations`.
12. PASS (с оговоркой) — `node map-check.js` ALL PASS, 0 JS-ошибок/тостов-ошибок/редиректов; блок «Мои заказы (в работе)» + кнопка «Сдать» проверены Node-тестом `web/frontend_contracts_test.go` (исполняет реальный `contracts.js` с фикстурами) и живой серверной точкой API, **но не живым кликом в браузере**.
13. N/A — живой критерий создателя не проверялся: подходящее живое поселение с активным эффектом и matching-заказом не найден до остановки инструментов сторожем. Нужно: id поселения с активным эффектом «жажда»/«голод» + id его ячейки/товара + соответствующий supply-заказ, затем прогон пересчёта слоя нужд с фиксацией роста ячейки и падения `R`.

**Итог:** PASS 11 / FAIL 0 / N-A 1 (+1 PASS с оговоркой).

## Новые тесты (пункт 2)

`internal/repository/contract_delivery_test.go`:
- `TestDeliverFullSupplyUsesCompleteTx` — полная сдача через существующий путь закрытия; лишнее не берётся; один `money_operations`; логи `completed`/`escrow_released`/`delivered`.
- `TestDeliverPartialProportionalPayment` — выплата 400, эскроу 1000→600, выводимая 200→120, статус `taken`.
- `TestDeliverClampToDoubleCap` — `delivered = 2·cap − amount` (150 при cap 100 / amount 50).
- `TestDeliverPartialThenFull` — Σ выплат 400+600 = 1000 = исходный эскроу.
- `TestDeliverFreeSupplyZeroEscrow` (2 подтеста) — полная пишет нулевую операцию; частичная при нулевой выплате не пишет.
- `TestDeliverNoCargo` / `TestDeliverStorageFull` / `TestDeliverMissingCell` — sentinel-ошибки + откат.
- `TestDeliverContractDamaged` (0 и 2 требования) — fail-closed, первое требование не выбирается молча.
- `TestDeliverNotAvailable` / `TestDeliverAuthorUnsupported` / `TestDeliverBuildingUnsupported`.
- `TestDeliverCargoMismatchFailsClosed` / `TestDeliverStorageCellFailureRollsBack` — откат без частичного учёта.

`internal/handlers/contract_deliver_handlers_test.go`:
- `TestDeliverContractRequiresAuth` — 401 без БД; `TestDeliverContractGates` — таблица 404/422/409/403/422;
- `TestDeliverContractRoleForbidden` — admin→403; `TestDeliverContractNotOnOrbit` — surface/чужая планета→422;
- `TestDeliverContractExpiredAfterDue` — 409; `TestDeliverContractBuildingUnsupported` — 422;
- `TestDeliverContractSuccess` — 200 `delivered`/`paid`/`remaining`.

`web/frontend_contracts_test.go` (+41) — словари `supply`/«Снабжение», `settlement`/«поселение»/🏘; `myWorksHtml` (только взятые supply, open/travel исключены, `data-contract-deliver`, «остаток N ед.», XSS-экранирование); `deliverResultText`/`deliverErrorText`/`remainingUnits`.
`internal/repository/storage_cell_repository_test.go` (+24) — тест нового чтения ячеек под блокировкой (`GetStorageCellsForUpdateTx`).

## Найденное

- **Дефектов кода не найдено.**
- **Оговорка (не дефект):** после полной сдачи `contracts.escrow_amount` остаётся 80 — существующее поведение пути закрытия (канон «`escrow_amount` не обнуляется»), не дефект ЧК2б.
- **Порядок гейтов в хендлере** (автор → статус → исполнитель) чуть отличается от §5.1 (статус → исполнитель → автор) — итоговые коды не меняет.
- **Ловушка окружения (повторяется):** живой dev-сервер был **устаревшим** бинарём (собран 11:46, правки 12:59–13:12) → `POST /api/contracts/deliver` отдавал ложный 404. Тестер пересобрал и перезапустил сервер (новый PID), после чего проверки прошли. （Второй раз после ЧК2а — кандидат в памятку прогонов.）

## Пробелы покрытия (не FAIL)

- **Пункт 12:** блока «Мои заказы (в работе)» в живом браузере кликом не проверяли — только рендер-тест + серверный API. Нужен e2e-скрипт: логин игроком со взятым заказом → открыть карточку планеты заказа → клик `[data-contract-deliver]`.
- **Пункт 13 (критерий создателя):** живой сценарий «форпост голодает → рейс → сдача → эффект снялся → `R`↓» не воспроизведён.
- Гонка «сдача vs истечение», «двойная сдача», ненулевая выводимая доля при частичной выплате — только юнит-тесты (живьём без доп. данных недостижимо).
- Роль не-player → 403 и «≠1 goods-требование» → 422 — юнит-тесты.

## Что НЕ смог проверить сам (граница покрытия)

| Что | Почему | Чем закрыто косвенно / что нужно |
|---|---|---|
| Живой клик «Сдать» | нужен игрок со взятым заказом и навигация к карточке планеты заказа; e2e-скрипта под флоу контрактов нет | Node-тест `frontend_contracts_test.go` + живой API; нужен e2e-скрипт |
| Критерий создателя (эффект/`R`) | нужны id поселения с активным эффектом, его ячейки/товара и matching-заказа; подготовку не завершил | нужен подготовленный живой сценарий (см. чек-лист, пункт 13) |
| Гонки сдачи | на общей dev-БД гонки не воспроизводимы безопасно | юнит/IT-тесты; репродукция — `@tester` при отдельной БД |

## Предложения в чек-лист (применил @manager)

- `B-CON-5`: живая частичная и полная сдача — пропорциональная выплата, Σ выплат = исходному залогу, рост ячейки, закрытие заказа.
- `B-CON-6`: гейты сдачи — орбита only, «не то»/«лишнее», переполнение ×2, отсутствие ячейки → 409, автор-строение → 422, нулевой `money_operations` у бесплатного заказа.
- В памятку прогонов: перед живыми API-проверками сверять время сборки `zorion-server.exe` с правкой исходников (устаревший бинарь даёт ложный 404/500).
- В памятку `@tester`: SQL-разведка — **одним сводным вызовом** `tools/db.ps1` (серия однотипных запросов = сторож зацикливания).
