# QA-прогон: студия, ЧК2 «вынос ядра» — 2026-09-25

> Независимый живой прогон @tester. Задача: ЧК2 эпика «Студия: рефакторинг монолита
> по блокам» (идея `docs/gamedesign/ideas/2026-09-25_студия-рефакторинг-по-блокам.md`).
> Из `web/studio.html` вынесены в отдельные классические скрипты:
> - `web/static/js/studio/core.js` — ядро (`state`, `$`, константы канваса, `fetchState`,
>   `showReport`, `esc`, `codeNote`, `fullTreeMode`, `focusSubset` и др.);
> - `web/static/js/studio/interactions.js` — контекстное меню карточки и drag&drop
>   карточки справочника на канвас (`showCtxMenu`, `hideCtxMenu`, `bindRecipeToFactory`,
>   `dndReset`, `dndReset2`, `prodMode` и т.п.).
>
> Порядок в `web/studio.html:492–495`: `core.js` → `content_transfer.js` → `interactions.js`
> → инлайн `<script>` (строка 495) — подтверждён. Файл студии 4986 → 4462 строки.

**Дата:** 2026-09-25
**Сервер dev:** `http://127.0.0.1:8080`, `/health` = `OK`. Статика с диска (`noCache`),
рестарт не требовался.
**Браузер:** системный Chrome через `playwright-core` (headless), viewport 1400×1000.
**Временная учётка:** `qa_cx_*` (роль `skycomposer`) — регистрация + `UPDATE users`,
удалена после прогона. В БД ничего не писалось (только чтение `/studio/api/state`);
экспорт снимка контента (клик) пишет `content/catalog*.json` — серверный роут, не БД-мутация.
**Коммита нет** — правки в рабочем дереве.

---

## Прогнал

```
node --check web/static/js/studio/core.js          → exit 0
node --check web/static/js/studio/interactions.js  → exit 0
cd tools/e2e; BASE_URL=http://127.0.0.1:8080 node qa-i1-studio.js
                                                  → 19 шагов, 1 FAIL (известный хвост A6), A8 PASS
BASE_URL=http://127.0.0.1:8080 node tmp-qa-studio-core-extract.js  → 9/9 PASS
```

---

## Жёсткий чек-лист

| # | Вердикт | Факт (одной строкой) |
|---|---|---|
| **1** | **PASS** | `node --check` обоих файлов: `core.js` exit 0 (246 строк), `interactions.js` exit 0 (281 строка); синтаксис чист. |
| **2a** | **PASS** | `qa-i1-studio.js` шаг **A8** (логин + смоук `/studio`, `page.on('pageerror')`): `errors=0 badResponses=[]`, скриншот `artifacts/qa-i1-studio.png`. |
| **2b** | **FAIL — известный хвост** | Шаг A6 «пара нет → 409» получил **404**: `RECIPE_FREE=71` нет в dev-БД (`qa-i1-studio.js:21`), сервер корректно отдаёт 404 «нет рецепта». Устаревшая предпосылка данных, к выносу отношения не имеет → прогон не провален. |
| **3a** | **PASS** | `/studio` под `skycomposer`: `pageErrors=0`, JS-`console.error=0`; единственный console-`error` — `Failed to load resource` (favicon.ico 404, вне правки). `loadingOverlay=none`, `studioLoginOverlay≠flex`. |
| **3b-core** | **PASS** | `typeof` по имени в странице = `function`: `fetchState`, `showReport`, `esc`, `codeNote`, `fullTreeMode`, `focusSubset`. |
| **3b-inter** | **PASS** | `typeof` по имени = `function`: `showCtxMenu`, `hideCtxMenu`, `bindRecipeToFactory`, `dndReset`, `dndReset2`, `prodMode`. |
| **3c** | **PASS** | **Живое контекстное меню** на нарисованной карточке: полное дерево (`#fullTree`), реальный правый клик (`page.mouse.click(..., {button:'right'})`) по карточке «Пища» → `#ctxMenu` `display=block`, **6 кнопок** (`+ слот`, `заполнить комплектующие`, `добавить родителя`, …). Путь: `right-click mouse` (фолбэк не понадобился). |
| **3d** | **PASS** | **Живой drag&drop**: ветка «Товары», выбрана фабрика id=23 (`factoryMode=true`), `dragenter` на `#canvasWrap` с `DataTransfer` (товар id=378) → `class="dnd-over"` появился, `dndActive=true`; `dragover` тоже отработал. Обработчик из `interactions.js` сработал. |
| **3e** | **PASS** | Скриншот студии: `tools/e2e/artifacts/qa-studio-core-extract.png` (56 139 Б). Финально JS-ошибок нет (`pageErrors=0`). |
| **4** | **PASS** | **ЧК1 не сломан**: клик `#btnExportContent` → `#report` = `Экспорт: сохранено 322 записей, файл catalog-20260925.json`, скачивание `catalog-20260925.json`. |

**Итог:** все пункты PASS, кроме известного хвоста A6 (не регрессия). **СМОУК ПРОЙДЕН.**

---

## Артефакты

- Скрипт целевого смоука: `tools/e2e/tmp-qa-studio-core-extract.js` (временный, не коммичен).
- Скриншоты: `tools/e2e/artifacts/qa-studio-core-extract.png`,
  `tools/e2e/artifacts/qa-i1-studio.png`.
- Журнал: этот файл.

## Найденное (не чинил)

1. **Устаревшая предпосылка `tools/e2e/qa-i1-studio.js:21`** — `RECIPE_FREE = 71`
   («рецепт есть, но к 148 не привязан → 409»). В текущей dev-БД рецепта 71 нет, поэтому
   `PUT /studio/api/producers/148/recipes/71` корректно отдаёт 404, а не 409; скрипт
   выходит с `exit 1`. Не код игры — правка тестовых данных/ожидания. Для @manager.
   (Тот же хвост зафиксирован в журнале `2026-09-25_браузерный-смоук-студии-вынос-content-transfer.md`.)
2. Багов самого выноса не найдено.

## Предложения в чек-лист (@manager)

- Считать сценарий «ЧК-вынос блока студии» регулярным: `node --check` каждого вынесенного
  файла + браузерный смоук с проверкой `typeof` имён блока (по имени, не `window.`) +
  живое действие вынесенного блока (контекстное меню / dnd / экспорт).
- Кейс A6 в `qa-i1-studio.js` устарел по данным — либо обновить `RECIPE_FREE`, либо
  перевести ожидание на «404/409 — пары нет».

## Чек-лист непроверяемого

- **Импорт контента «Проверить»/«Применить»** — не запускался (пишет в общую БД, запрещено
  задачей); проверен только экспорт. Закрыть на изолированной БД.
- **`showSpravCtxMenu` / `ctxBindToFactory` / `askDeleteGoodFor`** — вынесены в том же
  `interactions.js`, но требуют режима фабрики + удаления/привязки в БД; в этом прогоне не
  дёргались (минимальный чек-лист). Живое контекстное меню карточки канваса (`showCtxMenu`)
  покрыто.
- **Реальный drop с привязкой рецепта** (`drop` → `bindRecipeToFactory` → POST) — не
  выполнялся (мутация каталога); проверен только `dragenter`/`dragover` и подсветка.
- **Визуальное соответствие карточек/меню глазами игрока** — вкусовое/UX, вне тестера.
