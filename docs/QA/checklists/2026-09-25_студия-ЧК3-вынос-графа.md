# QA-прогон: студия, ЧК3 «вынос графа товаров» — 2026-09-25

> Независимый живой прогон @tester. Задача: ЧК3 эпика «Студия: рефакторинг монолита
> по блокам» (идея `docs/gamedesign/ideas/2026-09-25_студия-рефакторинг-по-блокам.md`
> §2/§3). Из `web/studio.html` (исходные строки 497–1124) вынесены в новый
> классический скрипт `web/static/js/studio/graph.js` секции:
> - «скоп ветки Товары» (`factoryType`, `factoryMode`, `concreteGoodsFactories`,
>   `factoryClosureGoodIds`, `factoryLevelLabel`, `factoryRecipeIds`, `universalSources`,
>   `mostSpecificFactories`/`mostSpecificSlot` …);
> - «отрисовка графа» (`computeLayout`, `renderGraph`, `showTip`, `hideTip`);
> - «клики и пан/зум» (`hitTest`, `centerTop`, `centerOn`, `saveView`, wheel/mouse listeners).
>
> Порядок подключения (`web/studio.html:492–496`): `core.js` → `content_transfer.js`
> → `interactions.js` → `graph.js` (новая строка 495) → инлайновый `<script>`. Файл
> студии 3835 строк; `graph.js` — 629 строк; `git diff web/studio.html` = +1/−628.

**Дата:** 2026-09-25
**Сервер dev:** `http://127.0.0.1:8080`, `/health` = `OK`. Статика с диска (`noCache`),
рестарт не требовался.
**Браузер:** системный Chrome через `playwright-core` (headless), viewport 1400×1000.
**Временная учётка:** `qa_gx_*` (роль `skycomposer`) — регистрация + `UPDATE users`,
удалена после прогона (проверено: `SELECT count(*) ... LIKE 'qa_gx_%'` = 0). В БД ничего
не писалось, кроме чтения `/studio/api/state`; мутаций каталога не выполнялось. Экспорт
снимка (п.8, по чек-листу) — серверный роут `GET /studio/api/content/export`, пишет
`content/catalog*.json` на диск, не БД-мутация.
**Коммита нет** — правки в рабочем дереве.

---

## Прогнал

```
node --check web/static/js/studio/graph.js                       → exit 0
cd tools/e2e; BASE_URL=http://127.0.0.1:8080 node tmp-qa-studio-graph-extract.js
                                                                 → 8 шагов (п.2–9), 8/8 PASS
```

---

## Жёсткий чек-лист

| # | Вердикт | Факт (одной строкой) |
|---|---|---|
| **1** | **PASS** | `node --check web/static/js/studio/graph.js` → exit 0 (629 строк); синтаксис чист. |
| **2** | **PASS** | `/studio` под `skycomposer`: `pageErrors=0`, JS-`console.error=0` (единственный console-`error` — `Failed to load resource` favicon.ico 404, вне правки); `#loadingOverlay` скрыт (`display=none`), `#studioLoginOverlay` ≠ `flex`. |
| **3** | **PASS** | `typeof` по имени = `function` для всех 12 обязательных: `factoryType`, `factoryMode`, `concreteGoodsFactories`, `factoryClosureGoodIds`, `computeLayout`, `renderGraph`, `showTip`, `hideTip`, `hitTest`, `saveView`, `centerTop`, `centerOn`. |
| **4** | **PASS** | **Граф рисуется:** ветка «Товары», реальная галка «всё дерево»; `state.goods=151`, `computeLayout` дал **19 размещённых** карточек (`pos`/`vis`), первая — «Пища»; канвас `#graph` 1054×757, **непустых (alpha>8) пикселей 3283/21565 (frac≈0.152)** — нарисованные карточки/рёбра, не фон. |
| **5** | **PASS** | **Правый клик по реально нарисованной карточке** («Пища»): `page.mouse.click(x,y,{button:'right'})` → `#ctxMenu` `display=block`, **6 кнопок** (`+ слот`, `заполнить комплектующие`, `добавить родителя`, …). Путь: `right-click mouse` (фолбэк не понадобился). `interactions.js` покрыт повторно. |
| **6** | **PASS** | **Живой dragenter:** ветка «Товары», выбрана конкретная фабрика id=23 (`factoryMode=true`), `dragenter` на `#canvasWrap` с `DataTransfer` (товар id=378) → появился класс `dnd-over`, `dndActive=true`; `dragover` тоже отработал. Без реального drop/POST. |
| **7** | **PASS** | **Пан/зум графа жив:** живое колесо мыши над канвасом — `zoom 1.000→1.150`, `panX=-79.05`, `panY=-56.85`; `saveView()` записал `localStorage`: `gs_zoom=1.15`, `gs_panX=-79.05`, `gs_panY=-56.85`. Путь: `live wheel`. |
| **8** | **PASS** | **ЧК1/ЧК2 не сломаны:** клик `#btnExportContent` → `#report` = `Экспорт: сохранено 322 записей, файл catalog-20260925.json`, скачивание `catalog-20260925.json` (322 записи, как в ЧК2). |
| **9** | **PASS** | Финально после всех действий: `pageErrors=0`, JS-`console.error=0`. Скриншот `tools/e2e/artifacts/qa-studio-chk3.png` (76 675 Б) — граф нарисован, выбранная карточка «Пища» подсвечена. |

**Итог:** **СМОУК ПРОЙДЕН** (9/9 PASS). Багов самого выноса не найдено.

---

## Артефакты

- Скрипт целевого смоука: `tools/e2e/tmp-qa-studio-graph-extract.js` (временный, не коммичен).
- Скриншоты: `tools/e2e/artifacts/qa-studio-chk3.png` (финальный, чистый граф),
  `tools/e2e/artifacts/qa-studio-chk3-graph.png` (сразу после отрисовки/правого клика).
- Журнал: этот файл.

## Хвост A6 (известный)

Общий `tools/e2e/qa-i1-studio.js` **не гонялся** в этом прогоне (фокус — вынос графа).
Известный хвост A6 (`RECIPE_FREE=71` — устаревшая предпосылка данных, рецепта 71 нет
в dev-БД → сервер отдаёт 404 вместо 409) **не проверялся и не является регрессией ЧК3**.
Статус — как зафиксировано в `2026-09-25_студия-ЧК2-вынос-ядра.md` и
`2026-09-25_браузерный-смоук-студии-вынос-content-transfer.md`. Задача — @manager
(обновить данные/ожидание).

## Найденное (не чинил)

1. Багов выноса блока «Граф товаров» не найдено: порядок скриптов корректен, все имена
   живы, отрисовка/тултип/правый клик/пан-зум/экспорт работают.
2. Наблюдение уровня тест-харнесса, **не баг продукта**: если галку `#fullTree` взвести
   программно (`el.checked = true`), не породив событие `change`, инлайновый
   `updateOverlays()` (`web/studio.html:3465–3481`) не пересчитывается и оверлей
   `#focusEmptyOverlay` (`z-index:5`, полупрозрачный) остаётся поверх канваса, перехватывая
   реальные мышь-события. В живом флоу пользователя галка кликается → `change` → оверлеи
   прячутся (в финальном прогоне `focusEmpty=none`, правый клик и живое колесо дошли до
   канваса). К выносу графа отношения не имеет — инлайновый код, не тронут.

## Предложения в чек-лист (@manager)

- Считать сценарий «ЧК-вынос блока студии» регулярным (как предложено в ЧК2): `node --check`
  каждого вынесенного файла + браузерный смоук с `typeof` имён блока (по имени) + живое
  действие вынесенного блока. Для блока «Граф товаров» живое действие = отрисовка
  (непустые пиксели канваса + размещённые карточки), правый клик по карточке, `dragenter`,
  живой пан/зум с проверкой `gs_*` в `localStorage`.
- Полезный приём харнесса: в тестах студии галку/селект канваса взводить **реальным**
  кликом (`page.check`), а не присвоением `.checked` — иначе инлайновые оверлеи остаются
  и перехватывают указатель (ложные FAIL по правому клику/колесу).
- Кейс A6 в `qa-i1-studio.js` устарел по данным — обновить `RECIPE_FREE` или перевести
  ожидание на «404/409 — пары нет».

## Чек-лист непроверяемого

- **`qa-i1-studio.js` целиком** — не запускался в этом прогоне (задача ЧК3); хвост A6
  только отмечен как известный. Для полного регресса студии — отдельный прогон.
- **Реальный drop карточки с привязкой рецепта** (`drop` → `bindRecipeToFactory` → POST) —
  не выполнялся (запрещена мутация каталога); проверены `dragenter`/`dragover` и подсветка.
- **Программные вызовы `centerTop`/`centerOn` на реальном клике** — проверены только `typeof`
  (имена живы) и косвенно через отрисовку; живой клик по «⟲ на верхний тир» не дёргался.
  Закрыть при регрессе центрирования.
- **Визуальное соответствие карточек/графа глазами игрока** (шрифты, цвета, обрезка имён) —
  вкусовое/UX, вне тестера; скриншоты приложены для эталона @gdesigner/@uidesigner.
- **Живой drop и импорт контента** — пишут в общую БД/каталог, запрещены задачей.
