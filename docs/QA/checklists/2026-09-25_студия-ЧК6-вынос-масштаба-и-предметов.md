# QA-прогон: Студия — рефакторинг монолита по блокам, ЧК6 (вынос `unit_scale_calc.js` + `items_ref.js`)

- **Дата:** 2026-09-25
- **Роль:** @tester (независимый живой прогон)
- **Объект:** рабочее дерево (без коммита). Вынесены `web/static/js/studio/unit_scale_calc.js` (549 стр.) и `web/static/js/studio/items_ref.js` (150 стр.); `web/studio.html:492–502` — порядок `core → content_transfer → interactions → graph → goods_popup → fill_ai → dialogs → producers_ref → unit_scale_calc → items_ref → инлайн`.
- **Среда:** dev-сервер `http://127.0.0.1:8080` (`/health` = OK), системный Chrome через `playwright-core` (headless), viewport 1400×1000.
- **Скрипт:** `tools/e2e/tmp-qa-studio-chk6.js` (временный, не коммитить). Артефакт: `tools/e2e/artifacts/qa-studio-chk6.png`.
- **Данные:** в БД — только временный `skycomposer` (register → UPDATE role → DELETE, `left=0`). Каталог не менялся (POST/PUT/DELETE не вызывались).

## Итог: **СМОУК ПРОЙДЕН** ✅

## Пункты

| # | Вердикт | Факт (одной строкой) |
|---|---|---|
| 1 | PASS | `node --check unit_scale_calc.js` exit 0; `node --check items_ref.js` exit 0 |
| 2 | PASS | `/studio` под temp skycomposer: overlay loading=`none`, login=`none`, `pageerror=0`, jsConsoleError=0 (один 404 — favicon) |
| 3 | PASS | Все 14 имён `unit_scale_calc.js` — `function` (`bad=[]`) |
| 4 | PASS | Все 11 имён `items_ref.js` — `function` (`bad=[]`) |
| 5 | PASS | Реальный клик по сегменту масштаба: `localStorage.gs_unitScale` `null → "person"`, попап перерисован (bodyLen=7215, сегмент на месте). Ключи сегмента: person/kilo/mega/billion |
| 6 | PASS | Реальный клик `#branchItems`: `branch=items`, `state.items=4`, `#spravList` — 4 карточки, title «Предметы · 4»; dblclick по карточке → попап предмета (`#detailPopup`=flex, title «Чертёж», bodyLen=821) → `#popupClose` → display=none |
| 7 | PASS | `#branchGoods` + «всё дерево»: граф нарисован (nonBgPixels=3283/21565, canvas 1054×757); ПКМ по карточке «Пища» → `#ctxMenu` display=block, 6 кнопок |
| 8 | PASS | Реальные клики по разделам производителей → открыт попап (первый: «Колония», bodyLen=7215). Проход по карточкам: блоки `eatBlock` («Потребляет»), `stageBlock` («Стадия»), `prodSlotEditor` («Слоты категорий») — все три отрисованы в видимом HTML, без ошибок |
| 9 | PASS | Клик `#btnExportContent` → в `#report` «Экспорт: сохранено 322 записей, файл catalog-20260925.json», download=catalog-20260925.json; финально `pageerror=0`, jsConsoleError=0; скриншот снят |

## Прогнал

```
node --check web/static/js/studio/unit_scale_calc.js   # exit 0
node --check web/static/js/studio/items_ref.js         # exit 0
cd tools/e2e; node tmp-qa-studio-chk6.js               # 9/9 PASS
go test -count=1 ./web/                                 # ok zorion/web 1.364s
```

`go test ./web/` — фронт-контрактные тесты (`frontend_studio_unit_scale_test.go`, `frontend_studio_sections_test.go` и др.) читают `studio.html` с диска, поэтому прогнан с `-count=1` (кэш недействителен: внешний JS-вход). Зелёный. Go-код не менялся — тяжёлый DoD (`-race`, генераторы) не запускался.

## Найденное

Багов нет.

Замечания (не баги):
- `unitScaleSegmentHtml` (`unit_scale_calc.js:19`) отрабатывает и без загруженного `globalThis.UnitScale` (fallback ×1), но клик по сегменту в этом случае перерисует лишь хранимую единицу — в прогоне модуль загружен, проверено живым кликом.
- Клик по экспорту штатно перезаписывает серверный снимок `content/catalog.json` (был и без того изменён в рабочем дереве до прогона) — не БД, как в ЧК4/ЧК5.

## Предложения в чек-лист

- Добавить в регресс постоянный сценарий: «переключатель масштаба единицы темпа зависит от попапа производителя» (`unitScaleSegmentHtml` рендерится только в `renderProdPopup`), т.к. проверка требует открытого попапа — иначе ложный SKIP.
- Считать `localStorage.gs_unitScale = null` до первого клика нормальным (значение по умолчанию `billion` живёт в `core.js:66`, в ключ не пишется) — в кейсе фиксировать переход `null → <key>`, а не «должно быть billion».

## Что НЕ проверено (границы покрытия)

- **Сохранение масштаба при перезагрузке страницы** — не проверял (кейс ЧК6 только про живой клик); перезагрузка с `gs_unitScale` потребовала бы ещё одной сессии.
- **Реальные мутации через новые функции** (`saveProdJSON`, `saveEatBlock`, `saveStageBlock`, `itemAdd`, `saveItemJSON`, `changeItemSlot`) — намеренно НЕ вызывались: запрет писать каталог (POST/PUT/DELETE). Проверена только отрисовка/наличие и popup-open/close.
- **`toggleTier`/`toggleAllTiers`/`spravClick`/`spravDblClick`/`dragStart`/`renderItemPopup`** — проверены только по `typeof` (п.4); их живое поведение (смена тиров, drag&drop) в этом чекпоинте не прогонялось.
- **Визуальная корректность раскладки сегмента масштаба и блоков попапа глазами игрока** — только косвенно (HTML непустой, ошибок нет); эстетика — вопрос к @gdesigner/создателю.
- **Экстремальные данные** (десятки предметов/производителей, длинные имена) — в dev-БД их нет; нужна либо генерация, либо отказ.
