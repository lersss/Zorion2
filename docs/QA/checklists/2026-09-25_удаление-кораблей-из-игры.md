# 2026-09-25 — @tester: арт-студия, удаление кораблей из игры

- **Предмет.** Спека `docs/specs/2026-09-25-арт-студия-удаление-кораблей-из-игры.md`;
  идея `docs/gamedesign/ideas/2026-09-25_арт-студия-удаление-кораблей-из-игры.md`.
- **Коммиты.** `74e6830` (реестр → данные), `d54afc8` (студия читает файл,
  `POST /ships/ingame/delete`, импортёр), `2e494b1` (UI-кнопка + e2e).
- **Окружение.** Windows 10, Go 1.27 (windows/amd64), Node v24.20.0,
  Postgres 15 (127.0.0.1:5432), Redis (127.0.0.1:6379).
- **Порт 8798 занят чужой устаревшей студией** (PID 41428, старт 2026-09-25
  02:56 — до коммитов фичи; `GET /ships/ingame/delete` отдаёт 404, UI без
  кнопки «Удалить»). Чужой процесс НЕ убит. Прогон витрины/контракта выполнен
  на свежесобранной студии под отдельным портом (8799), живое удаление — в
  песочнице (8800).
- **Безопасность.** Боевые `config/ships_registry.json`, `web/static/sprites/`,
  `ai_drafts/final_accepted/ships` не изменялись. Любое реальное удаление —
  только в песочнице `%TEMP%\opencode\{studio-qa-a,ships-sandbox}`.

## Таблица итогов

| # | Итог | Суть |
|---|---|---|
| 1 | PASS | `go build ./...` EXIT 0, `go vet ./...` EXIT 0 — чужая незакоммиченная линия сборку/вет не ломает |
| 2 | PASS | `go test -count=1 ./cmd/art-studio/...` ok (6 пакетов), `go test -count=1 ./internal/models/...` ok |
| 3 | PASS* | Витрина «В игре»: 118 карточек, у всех кнопка «Удалить» (118/118); confirm содержит имя+файл+«из игры»+«перезапуска»; отмена — no-op (0 POST, 118→118, src не изменился); JS-ошибок от фичи нет. *Единственный FAIL скрипта — 404 `/favicon.ico` (вне фичи), см. «Найденное» |
| 4 | PASS | `node race-ships-variant-check.js`: RESULT PASS (23), включая 6 новых кейсов деградации при удалении |
| 5 | PASS | `GET` → 405; `neutral.png` → 409; `race_humans_starship.png` → 409; `..%2Fevil.png` → 400; `нет_такого.png` → 404 (неразрушающе) |
| 6 | PASS | Лог старта игры: «Реестр кораблей загружен: 118 записей»; `GET /me.ship_options` = 118, первый `race_humans_starship.png`, последний `neutral.png` (race пустая) |
| 7 | PASS | Песочница: удаление `race_coastal_01.png` → `ok:true`, steps {registry,meta,accepted,sprite} = все true, `ships_left:117`, `race_ships_left:2`; исчезли запись реестра, PNG игры, PNG accepted и запись меты; последний — `neutral.png`; повторный вызов → 404 (не 500) |
| 8 | PASS | `git status --short` по боевым путям пуст; боевой реестр 118, `race_coastal_01.png` на месте и в игре, и в accepted |

## Детали прогона

### 1. Сборка/статика
`go build ./...` → EXIT 0; `go vet ./...` → EXIT 0. В дереве есть чужая
незакоммиченная линия (поселения/склады, миграции `000086`/`000087`) — на
нашу зону не влияет, падений нет.

### 2. Тесты
`go test -count=1 ./cmd/art-studio/...`: `zorion/cmd/art-studio`, `.../comfy`,
`.../config`, `.../generator`, `.../handlers`, `.../postproc` — все ok.
`go test -count=1 ./internal/models/...` — ok.

### 3. e2e витрины (порт 8799, свежая сборка)
`node ships-ingame-check.js` (копия с BASE=8799): 8/9 PASS.
- (a) 58 групп, первая «Люди · 12»; (b) у всех групп счётчик «· N»;
- (c) 118 картинок загружены; ориентация применена (matrix());
- (d) блок «Без корабля: 3» совпал с живым каталогом (3 кнопки);
- (delete) 118 карточек, у всех кнопка `.del`; confirm =
  `Удалить Звёздный корабль (люди) (race_humans_starship.png) из игры? Действие
  необратимо (возврат — только git) и вступит в силу в игре после её перезапуска`;
  отмена — 0 запросов на `/ships/ingame/delete`, DOM не изменился;
- (5) клик по расе без корабля открывает вкладку «Корабли рас» и выбирает расу
  (cryo_sky);
- **FAIL (5) no JS errors**: `console.error` = 404 `http://127.0.0.1:8799/favicon.ico`.

### 5. Контракт (порт 8799, песочная копия реестра; неразрушающе)

| Запрос | Ответ |
|---|---|
| `GET /ships/ingame/delete` | 405 |
| `POST /ships/ingame/delete?file=neutral.png` | 409 |
| `POST /ships/ingame/delete?file=race_humans_starship.png` | 409 |
| `POST /ships/ingame/delete?file=..%2Fevil.png` | 400 |
| `POST /ships/ingame/delete?file=нет_такого.png` | 404 |

### 6. Загрузка реестра игрой
Свежий бинарь `./cmd/server` поднят на `SERVER_PORT=8091`,
`TICK_INTERVAL_SEC=3600`. В логе старта:
`✅ Реестр кораблей загружен: 118 записей` (перед bootstrap, как в §3.2).
`/register` + `/me`: `ship_options` = 118, `[0]=race_humans_starship.png`,
`[-1]=neutral.png` (race `""`).

### 7. Песочница (живое удаление)
Каталог `%TEMP%\opencode\ships-sandbox`: копия `config/` (реестр 118 + `races.json`),
`web/static/sprites/` — 5 скопированных PNG, `ai_drafts/final_accepted/ships/` —
`ships_meta.json` + `race_coastal_01.png`. Студия запущена из каталога (порт 8800).

- До: `ship/ingame` 118, `race_coastal_01.png` есть в реестре, PNG игры, PNG accepted и мете.
- `POST ?file=race_coastal_01.png` → `{"ok":true,"file":"race_coastal_01.png","race":"coastal","ships_left":117,"race_ships_left":2,"steps":{"registry":true,"meta":true,"accepted":true,"sprite":true}}`.
- После: реестр 117 (без записи), PNG игры / PNG accepted / запись меты — удалены;
  `GET /ships/ingame` сразу отдаёт 117 без перезапуска студии; нейтральный последний.
- Повторный `POST` → 404 `{"ok":false,"error":"удалять нечего: …"}` — не 500, сервер жив.

### 8. Чистота
`git status --short -- config/ships_registry.json web/static/sprites
ai_drafts/final_accepted/ships` — пусто. Боевой реестр 118, спрайт и accepted
`race_coastal_01.png` на месте.

## Найденное

1. **важное (не из этой фичи, но блокирует зелёный e2e).**
   `tools/e2e/ships-ingame-check.js:162-164` — проверка «no JS errors» валится на
   `404 /favicon.ico`: браузер запрашивает favicon, у студии такого маршрута нет
   (`cmd/art-studio/handlers/http.go:132-183`, отдаётся `http.NotFound`).
   Воспроизводится и на боевой устаревшей студии 8798 (`/favicon.ico` → 404), т.е.
   дефект не внесён фичей — это упущение теста/ассета. Скрипт из-за него всегда
   exit 1, хотя все проверки фичи зелёные. Лечится либо favicon-маршрутом, либо
   игнором `favicon.ico` в сборе console-ошибок.
2. **информация (побочные эффекты прогона, не дефект).**
   - Шаг 6 (санкционированный старт сервера) применил к dev-БД миграцию
     `000087_retire_branch_buffers.sql` из чужой незакоммиченной линии поселений.
   - В dev-БД создан проверочный игрок `qa_del_ships_204964194` (для JWT `/me`).
     Если не нужен — удалить вручную.
3. Замечаний по коду фичи нет: защита `neutral.png`/`race_humans_starship.png`
   (`ships_handlers.go:308`), `validShipDeleteName` (обход пути, `:425`),
   `pathInsideDir` (`:436`), порядок «реестр → мета → accepted → PNG игры»
   (`:336-398`) и связывание меты/accepted по sha256 с фолбэком по имени
   (`:498-595`) отработали ровно по спеке.

## Предложения в чек-лист (применяет @manager)

- B-SHIP (зона @manager): заменить жёсткие «118»/«117» на «N из
  `config/ships_registry.json`» — 118 сейчас совпадает, но реестр теперь чистится.
- Новый кейс контракта удаления (неразрушающий): 405 / 409×2 / 400 обход / 404
  «нечего удалять» — как в п.5 этого прогона.
- Новый кейс e2e витрины: кнопка «Удалить» + подтверждение с именем/файлом/
  «перезапуск» + отмена = no-op, **без** реального удаления боевого корабля.
- Учесть известный шум `favicon.ico` 404 в e2e студии (см. «Найденное» п.1),
  иначе кейс непроходим.

## Что НЕ проверено (граница покрытия)

- **Реальное удаление боевого корабля** (`config/ships_registry.json` боевой
  ветки) и его эффект в игре — запрещено правилом безопасности; это ручной шаг
  создателя на dev-копии. Покрыто песочницей (п.7).
- **Сценарий §5 (пустой пул расы → подмена игрока людским дефолтом, агента
  нейтральным после перезапуска игры)** — требует удаления боевого корабля +
  перезапуска игрового сервера; недостижимо без боевого удаления.
- **Окно рассинхрона §7** (примитив на клиенте до перезапуска игры) — визуально
  в браузере, после реального удаления + живого игрока; не воспроизводил.
- **UX глазами игрока/создателя** — только скриншот
  `tools/e2e/artifacts/ingame-tab.png` (см. артефакты прогона); вкусовая оценка
  за создателем.
- **Импортёр `tools/import_ship_sprites.ps1` + его смоук** — вне моего чек-листа
  (пп.1–8); покрыт тестами @developer, отдельно не прогонял.
- **Долгосрочные тренды** (переиспользование освободившихся имён `race_<slug>_NN.png`
  из §5 п.3) — по определению не проверяются за один прогон.
