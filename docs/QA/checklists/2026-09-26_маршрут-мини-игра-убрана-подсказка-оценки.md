# QA-прогон: убрана живая подсказка-оценка пути в мини-игре «Прокладка маршрута»

- **Дата:** 2026-09-26
- **Тип:** независимая проверка мелкой правки (клиент, незакоммичено)
- **Изменённые файлы:** `web/route.html`, `web/static/js/route/route_game.js`, `web/static/js/route/route_config.js`
- **Объём правки:** удалён `<span id="quality-hint">`; убрана установка текста подсказки; удалены `pathHint`/`pathHintWord`/`greedyRouteLen`/`pathEffLen`/`REF_OVERSHOOT`.
- **Окружение:** dev-сервер `http://localhost:8080` (`/health` → OK), Chrome headless (playwright-core), мобильный вьюпорт 390×844.

## Что прогнал

| Команда | Результат |
|---|---|
| `git diff -- web/route.html web/static/js/route/{route_game,route_config}.js` | дифф соответствует описанию (только удаление, без побочных правок) |
| `node tools/e2e/route-check.js` | `RESULT: ALL PASS - PASS=8 SKIP=1 FAIL=0` (exit 0) |
| `node tools/e2e/tmp-qa-route-chip.js` (временный probe, создан и удалён в этом прогоне) | `RESULT: ALL PASS` |
| `grep` по `web/` на `quality-hint|pathHint|pathHintWord|greedyRouteLen|pathEffLen|REF_OVERSHOOT|эталон|короче|длиннее` | в `web/route*` совпадений нет; остальные — несвязанные админка/поверхность/модалка/студия |
| `tools/db.ps1` (фикстуры / каскады / чистка) | свои 2 юзера удалены, `/health` → OK |

## Сценарии (жёсткий чек-лист, 6 пунктов)

1. **`node tools/e2e/route-check.js` → `ALL PASS`.** Строка: `RESULT: ALL PASS - PASS=8 SKIP=1 FAIL=0` (exit 0). SKIP=STEP 7 (рестарт сервера mid-flight — осознанный SKIP, чтобы не уронить общий dev-сервер). — **PASS**
2. **`#quality-hint` в DOM нет.** В файле `web/route.html` элемент отсутствует; живая проверка браузера: `document.getElementById('quality-hint') === null`, в `#hud .hud-feedback .hud-chip` остался только `beacon-count`. Источник `route_config.pathHint` удалён, `route_game.js` больше не пишет подсказку. — **PASS**
3. **Нет утечки прогноза до отправки.** Живой HUD на экране игры:
   `"Паспорт ⏷ 🛰 близкий … Маяки 3 · Ложные 3 · Зоны 2 Пояс: … ← На карту Ведите путь от СТАРТА к звёзде-ФИНИШУ Маяки 0/3 ↶ Точка назад Проложить Заново"`.
   Нет «эталон», нет «короче/длиннее», нет секунд/`%`/ETA/«Осталось». В `route-check.js` STEP 2 аналогично `leak=none`. В HUD только паспорт, статус-строка и «Маяки N/M». — **PASS**
4. **Нет JS-ошибок (`pageerror`).** `route-check.js` STEP 2 `pageErrors=0`; финальный `pageerrors: PASS - none` (включая `/map` после возврата). Временный probe: `pageerrors: PASS - none` на карте, в игре и после возврата. — **PASS**
5. **Полный флоу работает.** Старт перелёта (202, `available=true`) → на `/map` кнопка «Ускорить» активна → клик ведёт на `/route.html` → отрисовка пути тапами → гейт «Проложить» активен → submit HTTP 200 → экран результата (`#result-class` = «Отличный маршрут») → «Вернуться на карту» ведёт на `/map` (`#flight-panel.flying` появился, JS-ошибок нет). Также `route-check.js` STEP 3: `remaining 274s → 192s, decrease=30%, active=true`. — **PASS**
6. **Фикстуры убраны, `/health` ок.** Созданные прогоном пользователи удалены: `e2e_route_1790369692182_0`, `e2e_routechip_1790369748609_0` (`DELETE 2`, осталось 0). Перелёты/ускоритель удаляются каскадом (`player_flights`/`player_accelerator` — `ON DELETE CASCADE`). Временный probe-скрипт удалён. `/health` → `OK`. Накопившийся исторический ворох из 678 чужих `e2e_*`-юзеров **не трогал** (не мои фикстуры; массовая чистка — отдельное решение). — **PASS**

## Найденные дефекты

Не найдено. Правка чистая, побочных эффектов нет.

## Вердикт

**ПРОЙДЕНО.** Все 6 пунктов — PASS. Живой браузерный смоук (и штатный `route-check.js`, и временный probe через кнопки карты) подтверждает: `#quality-hint` исчез, прогноз пути до отправки не утекает, JS-ошибок нет, полный флоу и возврат на карту работают. Правка соответствует заявленной (только удаление), её проверить безопасно и она проверена.

## Что НЕ смог проверить сам (граница покрытия)

- **Пункт 3 — «нет утечки прогноза»** проверен на одной сгенерированной карте (3 маяка, 2 зоны). Иных конфигураций поля (0/1/2 маяка, поле без зон, экстремальные коэффициенты зон) в этом прогоне не было. Закрыть: генераторный/юнит-уровень на `route_config` — теперь чистые функции оценки удалены, отдельные граничные поля можно воспроизвести только подготовкой данных.
- **Пункт 5, STEP 7 (`route-check.js`)** — рестарт сервера mid-flight не проверялся: это уронило бы общий dev-сервер (осознанный SKIP; 97a-строка покрывается Go-тестами).
- **Ожидание «после отката кнопка снова доступна»** — не достижимо живьём (cooldown 25 мин), покрыто только частично.
- **UX глазами игрока** («чище ли стал экран без chip-подсказки, не осталось ли визуальной пустоты в `hud-feedback`») — вкусовое/визуальное решение, оценивается создателем по скриншотам `tools/e2e/artifacts/route-field.png`, `route-result.png`.
- **Прочая клиентская интеграция подсказки** (если `pathHint` где-то ещё импортировался из динамического/несобранного кода) — проверено `grep` по `web/`; отрицательный результат.

## Предложения в чек-лист

- Добавить в `route-check.js` STEP 2 явную проверку отсутствия `#quality-hint` и слов-оценки («эталон»/«короче»/«длиннее») в HUD — сейчас эту правку скрипт покрывает лишь косвенно (только числа/ЕТА), без явного ассерта на удалённый chip.
- Новый регрессионный кейс: «на экране игры отсутствует live-подсказка качества пути» — применит @manager.
