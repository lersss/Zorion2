# ЧК6.1 «Мир прогулки: вода — отрисовка» — независимый прогон

**Дата:** 2026-09-26
**Задача:** ЧК6.1 — отрисовка жидкости (эпик «Мир прогулки», шаг «а» данные+сервер, шаг «б» клиентский слой)
**Спека:** `docs/specs/2026-09-25-мир-прогулки-вода.md` (§3 контракт, §5 отрисовка), приложение `…-вода-приложение.md` §A.2/§A.4
**Вердикт:** **С ОГОВОРКАМИ** (10/10 пунктов PASS; 1 флак-перф-тест, не относящийся к воде)
**Состояние:** правки НЕ закоммичены (`git status` — M по `internal/generator/planet/*`, `internal/handlers/surface_handlers*.go`, `config/biome_catalog.json`, `internal/generator/planet/biome_catalog_seed.json`, `web/static/js/surface/*`; новые `tools/surface-liquid-check.mjs`, `tools/e2e/surface-liquid-render-check.js`)

---

## Прогнал (команды + числа)

**Статическая проверка клиента:** `node --check web/static/js/surface/*.js` — 11/11 OK (нет SyntaxError, import-граф цел).

**Данные/справочник (свой скрипт поверх обеих копий):**
- 11 биомов с `liquid` в дельте (10 «вода» + `магмовый_океан`), `explicit`; 5 сухих — без `liquid`;
- пресет `водные/берег`: `horizon` есть (2 пояса), `liquid` нет;
- `config/biome_catalog.json` ↔ `internal/generator/planet/biome_catalog_seed.json` — `diffs=0` (view/color/семейства);
- `color == palette.base` у всех 10 водных; `магмовый_океан color=#7a2410` (не менялся).

**Инструменты (все зелёные):**
| Инструмент | Результат |
|---|---|
| `tools/surface-liquid-check.mjs` | **34/34 PASS** |
| `tools/surface-profile-check.mjs` | **75/75 PASS** |
| `tools/surface-field-check.mjs` | **22/22 PASS** |
| `tools/surface-view-check.mjs` | **44/44 PASS** |
| `tools/surface-env-check.mjs` | **17 PASS / 0 FAIL** |
| `tools/e2e/surface-liquid-render-check.js` | **6/6 PASS**, 0 pageerror |
| `tools/e2e/surface-solid-field-check.js` | 1-й прогон 6/8 (S4b/S4c перф-флак), 2-й прогон **8/8 PASS** |
| `tools/e2e/surface-walk-check.js` | **15/15 PASS** |

**Go — точечно (не полный набор, под сомнение «синхрон сида + резолв жидкости на сервере»):**
- `go test ./internal/generator/planet/ -run "TestBiomeViewSeedSynced|TestBiomeLiquidGateAutoMatrix|TestBiomeViewLiquidResolve|TestBiomeViewLiquidPriority|TestBiomeViewLiquidValidation|TestBiomeViewValidateNonFatal|TestBiomeViewFallbackNoRecipe"` → `ok` (0.519s)
- `go test ./internal/handlers/ -run "TestSurfaceLandCarriesLiquid|TestSurfaceLandCarriesBiomeView|TestSurfaceLandIdempotentIgnoresBiome"` → `ok` (0.104s)
- `go test ./internal/generator/planet/ -run TestCacheKeyVersion` → `ok`
- `go test ./internal/handlers/ -run Disk` → `TestDiskCacheBig`/`TestDiskCacheLimitLRU`/`TestDiskCacheVersionCleanup` PASS

**Живьём (curl/psql/браузер):** `/health` 200 «OK», `/status` 200 «All systems ready»; живая прогулка на планете `e7344e6d` (`океаны` 100 %) — серверный пакет `/api/surface/land` нёс `liquid={medium:"вода",mode:"global",color:"#2a7fd0"}`, `liquid_source="explicit"`.

---

## Отчёт по 10 пунктам жёсткого чек-листа

1. **PASS** — `liquid` (explicit) в дельте у 10 биомов «вода» + `магмовый_океан`; отсутствует у `коралловые_рифы`, `инеевые_рощи`, `пещерный_мир_с_потолком`, `струнные_рощи`, `пружинная_тундра` (обе копии справочника, `L6` ×4).
2. **PASS** — обе копии семантически синхронны (`diffs=0`, `TestBiomeViewSeedSynced`); горизонт пресета есть (2 пояса 0.50/0.70, `H1a`); `liquid`-дефолта на пресете нет (`config.liquid=false`, `seed.liquid=false`; `surface-view-check H1b`).
3. **PASS** — `planet_image_v2.go:50 ImageGenVersion = "v11"`; `planet_image_v3_test.go:468` обновлён на `v11`; диск-кэш `planet_image_disk_cache.go:65` пишет `v11_{…}.png`, стартовый клин `:45-57` удаляет всё не-`v11_*` (в коде `v10` остался только в комментариях); `TestCacheKeyVersion` + `TestDiskCacheVersionCleanup` PASS. Файлов `v10_*` в выдаче нет (диск-кэш для `size=small` не используется — отдача из памяти).
4. **PASS** — живой браузер, биом `океаны`: тело воды запечено в чанк (`R1` 77/77 непрозрачных), `drawLiquidFront` рисует зеркало/пелену/кромку (`R4` пикселей=38002), клиент видит `wet=5471`, `maxDepth=139.5`, среда «вода»; 0 `pageerror`.
5. **PASS** — `liquidAt ∩ solidAt' = ∅` (`L2a` точек=4026 нарушений=0); в живой прогулке на зеркале нет твёрдого (W4 3697 колонок, 0); «ходьба по воде» невозможна; `bedY` без корки (`L4e` bedY=341.2 > lv=330 > floorY=304).
6. **PASS** — `подлёдные_океаны`: корка запечена как земля (`R2` 90/90), полынья открыта над зеркалом (`R3` 3/3), живой прогон — `crustCols=7156` все твёрдые, `polyCols=370`, `iceH=26`; ширина полыньи ≥ `PLAYER_W+2=14` (L4i мин=16), детерминизм от seed (L4l/L4m).
7. **PASS** — `магмовый_океан`: `medium="лава"`, `glow=#ff9a20` (L5/L5b); `drawLiquidEmissive` рисует свечение в браузере (`R5` пикселей=1677, без броска).
8. **PASS (с оговоркой)** — диф справочника: только добавления + перезапись пресета + запятая у `магмовый_океан`; **удалённых строк в чужих биомах нет**. Регресс неводных: `surface-view-check` V4/V8/V9, `H1b/H5a`, `surface-profile-check R1` «сданные слои = прежний профиль», `surface-field-check T1a` (`solidAt == эталон`, 0 расхождений) и `T1b` (`terrainHeight` 0). `коралловые_рифы` — свой горизонт сохранён (2 пояса `crest`), `liquid` нет. Полноценный визуальный контакт-лист ЧК0–ЧК5 повторно не снимался.
9. **PASS** — `surface-walk-check.js`: 15/15, вход/ПКМ/высадка/брифинг/ходьба (4 м)/прыжок (rise=8.0)/возврат на орбиту, 0 `pageerror`, 0 JS-ошибок, без разлогина/редиректа. Дополнительно (живой браузер на `океаны`): персонаж реально вошёл в воду (`headLiquid="вода"`, depth=88.5), ходьба 6 м, 0 `pageerror`; на `подлёдные_океаны` — ходьба по корке, 0 `pageerror`.
10. **PASS** — уровень `basin` детерминирован от seed (L7); полыньи — тот же seed → те же окна, другой seed → другие (L4l/L4m); поле/рельеф детерминированы (`surface-field-check T5`, `surface-profile-check P6`). Волна `liquidWave(seed, x, t)` — чистая функция от seed/времени (код `surface_world.js:458`), отдельным ассертом не покрыта (см. «не проверено»).

**Сводка:** **PASS 10 / FAIL 0 / N-A 0.**

---

## Сценарии (привязка к чек-листу)

- **A1** `/health`→«OK», `/status`→«All systems ready» — PASS.
- **A2** регистрация/логин/`/me` с JWT (через e2e-скрипты) — PASS.
- **A4** карта + модалка без JS-ошибок (`surface-walk-check 10a`) — PASS.
- **B-SURF-4** высадка/прогулка/HUD/«Вызвать корабль»/0 pageerror — PASS (`surface-walk-check`; оговорка кейса про headless-прыжок не проявилась — `10g` PASS).
- **B-SURF-13/14** порядок слоёв, `view_source=catalog`, `palette.base==color` — PASS.
- **B-SURF-17** версия картинки — **обновить:** КЕЙС устарел (в чек-листе `v10`, фактически `v11`).
- **B-SURF-18/19** горизонт (2 пояса 0.50/0.70, дымка) — PASS (`H1a/H1d/H5b`), включая `коралловые_рифы` (свой горизонт, не залит).
- Новые сценарии ЧК6.1 — см. «Предложения в чек-лист».

---

## Найденное

**Дефектов в фиче воды не найдено.** Замечания:

- **LOW (инструмент/перф, адрес `tools/e2e/surface-solid-field-check.js:281-289`).** `S4b`/`S4c` — абсолютный порог `<60 ms` и относительный `≤2×` на живой машине **флакают**: 1-й прогон горы 63.5 ms (>60) и `магмовый_океан` 35.8 ms > 2×17.2 ms; 2-й прогон — 8/8 PASS (горы 55.5 ms). К воде не относится (замеряются `горы`/`пещерный_мир`/`лавовые_поля`/`кратеры`, `магмовый_океан` — тот же рецепт форм Э5.3, что и раньше; `liquid` генерацию твёрдого не трогает). Рекомендация: не жёсткий порог, а baseline прошлого прогона / несколько прогонов.
- **INFO (чек-лист, адрес `docs/QA_CHECKLIST.md` B-SURF-17).** Запись говорит `ImageGenVersion=v10` — по факту `v11` (ЧК6 §4.6). Обновляет @manager.

---

## Чеклист непроверяемого

- **Визуальное «офигеть» глазами игрока** (красота зеркала, блика, кромки, отличимость метана/аммиака/CO₂) — не автоматизируется. Скриншоты: `tools/e2e/artifacts/qa6-water-walk.png` (океаны), `surface-play.png`, `surface-liquid-*`. Нужен просмотр создателем.
- **Полноэкранная подкраска под водой §5.3 и дырокол-«мокрый взгляд»** — это ЧК6.2 (плавание/погружение); в объём ЧК6.1 не входит. Проверено лишь, что голова в воде (`headLiquid="вода"`) и 0 ошибок.
- **«Тёмная вода под коркой»** `underIce` — пиксельно подтверждено только «корка непрозрачна + полынья пуста над зеркалом» (`R2/R3`); сама тёмная тонировка тела — оценка по скриншоту/коду, не машинный ассерт.
- **Живой прогон метановых/аммиачных/CO₂-океанов и `термальные_террасы`/`озёра_реки` (basin)** — в БД есть планеты-водные, но живьём гонял `океаны` (global) и `подлёдные_океаны` (underIce); остальные среды — через headless-рендер и mjs-модель. Экстремальные биомы в БД искать не пришлось.
- **Замер бюджета кадра** (~600 отрисовок/кадр; LOD/кап в коде не реализованы) и **FPS/GPU** — не замерял (по B-SURF-12 headless FPS недостоверен; ориентир-требование спеки).
- **Детерминизм волны по времени `t`** — отдельного ассерта нет (код чистой функции `liquidWave`), покрыт косвенно.

---

## Предложения в чек-лист (применяет @manager)

Новые кейсы (вода, ЧК6.1):
- **B-SURF-36** Признак жидкости: у 10 биомов «вода» + `магмовый_океан` `liquid` в **дельте** (`liquid_source=explicit`), `liquid_medium`/`water_min` жидкость **не включают**; сухие `коралловые_рифы`/`инеевые_рощи`/`пещерный_мир_с_потолком`/`струнные_рощи`/`пружинная_тундра`; пресет `водные/берег` несёт только `horizon`, без `liquid` (`tools/surface-liquid-check.mjs`, L6).
- **B-SURF-37** Инвариант «жидкость ≠ твёрдость»: `liquidAt ∩ solidAt' = ∅` в теле, «по воде не ходим», `bedY` без ледяной корки (`tools/surface-liquid-check.mjs` L2/L4e/L4g; живой `W4`).
- **B-SURF-38** `underIce`: корка запечена в канвас земли (твёрдая, по ней ходят), вода под коркой, полыньи (окно ≥ `PLAYER_W+2`, детерминизм от seed) — `tools/e2e/surface-liquid-render-check.js` R2/R3 + живой прогон.
- **B-SURF-39** Лава: `магмовый_океан` — непрозрачное самосветящееся тело (`glow`), `drawLiquidEmissive` рисует — R5/L5b.
- **B-SURF-40** Живая прогулка на водной планете: серверный пакет несёт `liquid`+`liquid_source`, вода видна на канвасе, персонаж входит в воду, ходьба/прыжок, 0 pageerror/без разлогина (e2e на планете `океаны`).

Обновить:
- **B-SURF-17** — `ImageGenVersion` `v10` → **`v11`** (ЧК6 §4.6).
- **A6/`-race`** — дельта жидкости не гонялась с детектором гонок локально (её гоняет джоб CI); при следующем полном прогоне — включить.

---

## Уборка фикстур

Создано и удалено (dev-БД):
- пользователи `e2e_water_1790358929235_0`, `e2e_water_1790358896630_0`, `e2e_walk_1790358793851_0` → удалены (`DELETE 3`), вместе с ними `player_planet_knowledge` (8 строк, `DELETE 8`); `player_flights`/`player_cargo`/`player_intrasystem_flights`/`player_accelerator` — 0 строк.
- временный e2e-скрипт `tools/e2e/tmp-qa6-water-walk.js` и вспомогательный `qa-water-cat.mjs` — удалены.
- артефакты-скриншоты оставлены в `tools/e2e/artifacts/` (`qa6-water-walk.png`, `surface-play.png`, `surface-solid-field-results.json`).

---

## Дневной контактный лист (11 водных биомов, светлая фаза)

**Артефакт:** `tools/e2e/artifacts/qa6-water-contact-sheet.png` (11 подписанных панелей в сетке 4×3).
Отдельные кадры: `tools/e2e/artifacts/chk6-<биом>-day.png` (11 шт.).

**Чем добивался дня (явно).** Использован **штатный дебаг-рычаг фазы суток админки** —
вторая строка `.hud-center` (`web/static/js/surface/surface_ui.js:151 showEnvToggle`,
чип `hud-env-admin`, `button[data-env="день"]`). Клик по «День» взводит
`state.env.forced = 'день'` (`surface_main.js:82 setEnv`), а `SurfaceEnvironment.u()`
возвращает `PHASE_U['день'] = 0.70` (`surface_environment.js:28,82`) — полный дневной
вид с первого кадра, без подбора момента/seed. На **всех 11** кадрах HUD `#hud-env`
прочитан как **«День»**. Дополнительно у `подлёдные_океаны` принудительно взведён
**«Штиль»** (тот же админский ряд погоды): авто-погода выпала «Морозный туман» и
целиком выбеливала кадр — вода/лёд были неразличимы (пересъёмка один раз).

**Инжекция биома** — подход `tools/e2e/surface-liquid-render-check.js` /
живого прогула `chk1-shots.mjs`: админ-роль → `POST /api/surface/land`
(`?planet=<id>&biome=<form>`), планета резолвится из БД по владению `form` (не газ-гигант,
не мини-нептун). Живой браузер (Chrome headless), реальный рендер; **0 `pageerror`** на
всех прогонах. Скрипты: `%TEMP%\opencode\chk6\chk6-shots.mjs` + `contact-sheet.mjs`.

| # | Биом (панель) | medium / mode | Заметка по виду (1 строка) |
|---|---|---|---|
| 1 | `океаны` | вода / global | широкая стальная гладь с крупными бело-голубыми «пенами-облаками» по урезу — вода читается хорошо; пена визуально крупновата (похожа на кучевые облака, а не брызги) |
| 2 | `озёра_реки` | вода / basin | зелёный день, зеркало за полосой пены, тёмно-зелёный берег; basin (окно 400) — вода полосой, не «морем» |
| 3 | `мелководья_заливные` | вода / global | под «Дождём» ярко-циановая лента + пена; тело полупрозрачное — сквозь воду видна отмель, «заливные» читаются |
| 4 | `планктонные_моря` | вода / global | тёмно-бирюзовое небо и светящаяся (glow `#8fffe0`) бирюзовая кромка — самый «водный» кадр листа |
| 5 | `термальные_террасы` | вода / basin | песчаные террасы-ступени, вода только тонкой лентой на нижней террасе — террасы перекрывают воду |
| 6 | `метановые_моря` | метан / global | почти чёрно-зелёное зеркало с белыми точками (звёзды/блики); **вопрос:** на свету выглядит «пусто-чёрным», метановая жидкость почти не опознаётся |
| 7 | `углеводородные_равнины` | метан / basin | олива, узкая тёмная лента воды на горизонте; тело углеводородов в кадре мало |
| 8 | `аммиачные_крио-океаны` | аммиак / global | серо-сиреневый день, тонкая розово-фиолетовая лента; зеркало почти сливается с небом — **низкий контраст**, вода малозаметна |
| 9 | `co2_океаны` | co2 / global | янтарное небо, тонкая жёлто-зелёная лента жидкого CO₂ на плато; сам «океан» в кадре почти не виден (линия) |
| 10 | `подлёдные_океаны` | вода / underIce | ледяная равнина + тёмно-синяя вода под коркой; **на кадре подписаны** «лёд (корка)» и «полынья → вода» (см. ниже) |
| 11 | `магмовый_океан` | лава / global | тёмно-бордовое тело с чёрными «шлаками», яркая оранжевая кромка лавы (glow) с пузырями-вспышками; **плотность:** свечение — тонкая линия по урезу, «моря лавы» нет, кадр преимущественно тёмный |

**Отметки на `подлёдные_океаны`** (`chk6-подлёдные_океаны-day.png`, панель 10 листа).
Позиции посчитаны **по реальной геометрии** мира (те же `crustTop`/`polynyaAt`/
`liquidLevel` и трансформ, что у рендера), не на глаз:
- **«лёд (корка)»** — полоса `[crustTop, liquidLevel]` (светлая, запечена в землю);
- **«полынья → вода»** — окно `polynyaAt`, над зеркалом (тёмно-синяя вода).
**Вопрос к виду:** контраст «корка ↔ вода» умеренный, окно полыньи без подписи
неочевидно; авто-погода «Морозный туман» полностью белит кадр (в этом режиме воду
не видно совсем).

**Общий нюанс выбора планет (честно).** Биом инжектируется в *рецепт вида*, а рельеф/seed
берётся у резолвнутой планеты. 3 биома (океаны, планктонные_моря, термальные_террасы)
сняты на **одной** планете `84512026…`, 2 (озёра_реки, мелководья_заливные) — на
`a7627cf6…`; это даёт честное сравнение рецептов на одном рельефе, но «сколько воды
видно в кадре» зависит от рельефа seed — у `co2_океаны`/`углеводородные_равнины`/
`термальные_террасы` вода вышла тонкой лентой именно из-за высокой отмели в точке спавна.

**Вердикт раздела:** **ПРОЙДЕНО** — 11/11 биомов сняты днём, контактный лист собран,
0 `pageerror`; вопросов к виду (не багов) — 3: метан «чёрное на свету»,
низкий контраст аммиачной воды, тонкая (не «морская») кромка лавы и пены океана.
Решение «офигеть/нет» — за создателем (визуал глазами игрока не автоматизируется).

**Уборка.** dev-БД: созданы 3 служебных админа `chk6_*` (по одному на прогон) → удалены
(`DELETE 3`); `player_planet_knowledge`/`player_cargo`/`player_flights` по ним — 0 строк
(каскад не задет). Прочие таблицы не трогал, разрушительных операций не запускал.
Код/конфиги репо не менял (`git status` — только чужие незакоммиченные правки ЧК6;
артефакты `tools/e2e/artifacts/` вне git). Временные `.sql` в `%TEMP%\opencode\chk6\` удалены,
скрипты (`chk6-shots.mjs`, `contact-sheet.mjs`) оставлены в `%TEMP%` для воспроизведения.
