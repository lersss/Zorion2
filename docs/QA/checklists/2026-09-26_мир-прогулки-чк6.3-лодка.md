# QA — ЧК6.3 «Мир прогулки: лодка» (2026-09-26)

**Что:** независимый прогон ЧК6.3 — `surface_boat.js` (новый) + правки `surface_config.js`/`surface_main.js`/`surface_render.js`, спрайт `boat.png`, проверки `tools/surface-boat-check.mjs` и `tools/e2e/surface-boat-check.js`. Спека `docs/specs/2026-09-26-мир-прогулки-лодка.md`.
**Когда:** 2026-09-26, рабочичее дерево (не закоммичено). **Чем:** `node tools/surface-boat-check.mjs`; playwright-core e2e (`surface-boat-check.js`, `surface-walk-check.js`, `surface-swim-check.js`, `surface-liquid-render-check.js`, `map-check.js`); регресс `surface-swim-check.mjs`/`surface-liquid-check.mjs`; curl/`Invoke-WebRequest` спрайта; System.Drawing по пикселям; `node --check`. Сервер `127.0.0.1:8080` (`/health`=OK). Фикстур в БД не создавал.

## Результаты (14 пунктов)

1. **Спрайт** — ПОЙДЕНО: GET `/static/sprites/surface/boat/boat.png` → 200 `image/png`, 37780 Б; файл 204×120, на y=84 непрозрачных 178/204 px (борт x≈20..180, края прозрачны).
2. **Посадка вплавь** — ПОЙДЕНО: B1a `canBoard=0`, `board=true`, player.x=boat.x=10360.0; B2 второй `use` → `aboard=false`, тот же объект `Boat` (второй лодки нет).
3. **Посадка с берега** — ПОЙДЕНО: B13 `dx=-6`, центральная колонка x=1000 сухая (`dryCentral=true`).
4. **Скорость** — ПОЙДЕНО: vmax=256.0 = `WALK_SPEED(160)·BOAT_FACTOR(1.6)`; со спринтом 256.0 = без спринта (не ускоряет).
5. **Снос/инерция** — ПОЙДЕНО: `BOAT_DRAG=80 < BOAT_ACCEL=160`; после ввода v0=256.0 → 0 за 192 кадра (плавно, без рывка).
6. **Ныряние запрещено** — ПОЙДЕНО: B6 при `down+jump` 120 кадров Δ(y, lv−BOAT_HULL)=0.0000, y=310.0 < lv=330.0.
7. **Мягкий стоп у берега** — ПОЙДЕНО: B7a край зеркала x=1482.98 (x+17=1499.98 < 1500), `vx=0`; урона/отскока нет.
8. **Запрет лавы** — ПОЙДЕНО: B8a `medium='лава'` → `canBoard=null` (и `board=false`).
9. **Запрет `underIce`** — ПОЙДЕНО: B8b на `подлёдные_океаны`, полынья x=720 (все ±7 колонки — полынья) → `canBoard=null`; B8c синтетика `mode=underIce` → null.
10. **Полный силуэт vs твёрдое** — ПОЙДЕНО: B7b `penetrated=false`, стоп x=1482.98 (x+17<1500) перед сводом `solidAt(x≥1500, y≤LV−5)`; проба X `[x±17]`, Y от lv−34 до lv+6 по `footOffsets`.
11. **Выход** — ПОЙДЕНО: `disembark` → `aboard=false` (лодка складывается), выход в открытой воде; `player.distance` 0.0 → 309.3 за 120 кадров (HUD не замирает).
12. **Регресс пешком/вплавь** — ПОЙДЕНО: `surface-swim-check.mjs` 29/29, `surface-liquid-check.mjs` 34/34, e2e `surface-walk-check` 15/15, `surface-swim-check` 7/7, `surface-liquid-render-check` 6/6; B11a `surface_player.js` без ссылок на Boat/BOAT_, B11b/c скорости плавания/ходьбы совпадают.
13. **Детерминизм и ошибки** — ПОЙДЕНО: B12 два прогона дают x=509.333/vx=256/distance идентично; e2e E10 `0 pageerror`; `node --check` 4 изменённых файлов — OK.
14. **e2e-смоук** — ПОЙДЕНО: `surface-boat-check.js` 10/10 (скриншот `tools/e2e/artifacts/surface-boat-check.png`), `surface-walk-check.js` 15/15, `surface-swim-check.js` 7/7, `surface-liquid-render-check.js` 6/6, `map-check.js` ALL PASS.

## Блокирующие

Нет.

## Неблокирующие / границы покрытия

- **Живой `KeyE`** через реальный `keydown` в `surface_main.js` отдельным синтетическим событием не прогонял: проверено модульно (B1/B2) + код-ревью `surface_main.js:236-237` (краевое, `e.repeat` игнор) и `toggleBoat` (`surface_main.js:93-96`). Кто закроет: прогон с dispatchEvent `KeyE` в e2e-харнессе прогулки.
- **Пиксельная идентичность «того же кадра»** при повторном прогоне не сравнивалась — B12 подтверждает детерминизм `x/vx/distance`, но не равенство пикселей кадра.
- **Загрузка реального PNG в живом кадре**: e2e E4 получил код-фолбэк (`drawBoat` вызван до async `onload`); пиксельно, что в игре рисуется именно спрайт, не измерял.
- **Приёмка арта «глазами»** (силуэт/палитра/читаемость) — вкусовая, вне моей зоны (создатель/@gdesigner).
- **Экстремальные планеты** (`polynya.w > BOAT_W`, сложные своды) в природе не искал — B7b/B9 синтетические; живой мир с такой геометрией не подобран.
