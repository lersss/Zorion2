# QA-прогон: разбор по факторам мини-игры «Прокладка маршрута» (v9)

**Дата:** 2026-09-26
**Роль:** @tester (независимый прогон)
**Коммиты:** `a8983ff` (сервер+клиент), `1468f26` (значки; доска/словарь — ранее)
**Окружение:** dev-сервер `http://127.0.0.1:8080` (код задеплоен, `/health`=200), реальный браузер
Chrome (playwright-core, 390×844, headless), системный Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe`.
**Затронутые файлы:** `internal/routegame/grid_explain.go` (+test), `internal/handlers/accelerator_grid_boost.go` (+test),
`web/static/js/route/route_config.js`, `route_figures.js`, `route_ui.js`, `route_game.js`, `web/route.html`, `web/static/css/route.css`.

## Прогнал

- `node route-check.js` (13 шагов) — `BASE_URL=http://127.0.0.1:8080` (tools/e2e).
- `node tmp-qa-breakdown.js` — мой независимый probe (items 3,4,5,6,7,8,9,10 + реальный флоу item 2).
- `go build ./...` → EXIT 0; `go vet ./...` → EXIT 0.
- `go test -race -short -count=1 ./internal/routegame/... ./internal/handlers/...` → оба `ok` (85.7 s / 25.5 s).
- `GET /health` → 200.

## Сценарии (жёсткий чек-лист, каждый пункт ровно один раз)

| # | Итог | Факт |
|---|---|---|
| 1 | **PASS** | `route-check.js`: `RESULT: ALL PASS - PASS=13 SKIP=0 FAIL=0`. Включает 3b breakdown, 3c «Чистый курс», 5 реальный boost с непустым разбором. |
| 2 | **PASS** | Реальный флоу (без стабов) `travel→доска→scan→курс→«Проложить»`: `200`, `body.breakdown` — массив (3 фактора: `dead_end`/`find_used`/`wall_cost`), `domShown=true`, скриншот `artifacts/route-breakdown-real.png`. |
| 3 | **PASS** | Группы в DOM ровно `Ошибки>Находки>Не ошибка`; 8 строк, у каждой canvas 30×30 с ненулевыми пикселями + имя + `×N`. Скриншот: `ОШИБКИ/НАХОДКИ/НЕ ОШИБКА` (арт. `route-breakdown.png`, `route-breakdown-real.png`). |
| 3b | **PASS** | Полоса тяжести: у 4 error-строк severity 1..4 → 4 слота, заполнено = severity (Перелёт Цели 1/4, Зонд впустую 4/4, Петля 1/4, Сопротивление 1/4); цифр в полосе нет; у gain/neutral полосы нет вовсе. |
| 4 | **PASS** | `offer` `raw` — без `breakdown`; `scan` (HTTP 200) `raw` — без `breakdown`; до отправки `#result-breakdown.hidden=true`, `innerHTML=""`, оверлей результата не показан. |
| 5 | **PASS** | Стаб `breakdown:[]` → блок виден, `.bd-clean` = «Чистый курс», 0 строк. |
| 6 | **PASS** | Стаб `{bonus,remaining_s}` без поля → `#result-breakdown.hidden=true`, `innerHTML=""`, результат «Ускорение принято» показан, игра работает. |
| 7 | **PASS** | `unknown_future_code` в стабе: строка не отрисована (`names` без `null`, текст без кода), остальные 8 строк целы. |
| 8 | **PASS** | `mud_cost`=«Сопротивление», `wall_cost`=«Помехи» — обе строки присутствуют, дублей имён нет (`names=[Перелёт Цели,Зонд впустую,Петля,Сопротивление,Находка,Попутный поток,Помехи,Сбой — в стороне]`). |
| 9 | **PASS** | В тексте разбора нет `%`, десятичных чисел, «итог/сумм/оптимум/было бы/потер»; единственное число — `bonus` в шапке (`+50 %` в заголовке — допустимо). `newRem` считается из `bonus` до разбора, разбор — чистая функция `ExplainGridPath` (на итог не влияет). |
| 10 | **PASS** | Глифы «Петля» (`revisit`, 95 пикс.), «Перелёт Цели» (`overshoot`, 71), «Зонд впустую» (`ping_wasted`, 49) — непустые, подписи-сигнатуры различны (`distinct=true`). |
| 11 | **PASS** | Из прогона route-check: легенда 4 свёрнутые группы (`groups=4 collapsed=true swatch=0`), разведка `pings 2→1`, двухтапная отправка (`submitting="Прокладываю…"`), отрицательный результат (`−12 %` → «Перелёт стал длиннее»), `pageerrors: none` (в обоих прогонах). |
| 12 | **PASS** | `go build ./...`=0, `go vet ./...`=0, `go test -race -short -count=1` по `internal/routegame`/`internal/handlers` — `ok`, `/health`=200. |

## Вердикт: **ПРОЙДЕНО**

Все 12 пунктов PASS, SKIP/NA нет. Дефектов не найдено.

## Найденное

Дефектов нет.

## Предложения в чек-лист (применит @manager)

- Новый кейс: **непустой разбор, все `code` неизвестны → блок скрыт** (не «Чистый курс») — спека §4.7.1, в текущем чек-листе не покрыт.
- Новый кейс: **глиф строки нарисован и различим** (canvas 30 px, пиксели > 0, сигнатуры `revisit`/`overshoot`/`ping_wasted` различны) — текущий смоук проверяет наличие строк, но не заливку глифа.
- Уточнить формулировку п.3: «полоса без цифр» → «4 слота, заполнено = severity (только error); при `severity=0` полосы нет».

## След фикстур

- `tools/e2e/tmp-qa-breakdown.js` — новый untracked probe (воспроизводимый, оставлен как evidence).
- Браузерные артефакты: `tools/e2e/artifacts/route-breakdown-real.png`, `route-breakdown.png` (и прежние `route-*.png`).
- БД: прямых правок не было. Созданы одноразовые пользователи смоука (`e2e_route_*`, `e2e_bd_*`) и их полёты. Endpoint самоудаления отсутствует; пользователи остаются с осознанным префиксом (как в штатном `map-check.js`/`route-check.js`). Общие таблицы не менялись.

## Чеклист непроверяемого (что не смог проверить сам и почему)

- **Длительный тренд/калибровка severity 1–4** — числа полос `@balancetester`, мой прогон проверяет только соответствие «severity → заполнение», не адекватность значений.
- **Все 16 кодов у одного игрока за одну партию** — реальное поле даёт срез (в живом прогоне 3–6 факторов), полный набор воспроизводился только стабом; поведение детекции каждого кода — зона `ExplainGridPath` unit-тестов (в них есть).
- **`cells`-подсветка по тапу** (опциональная, §4.7.1) — реализация отложена (строки неинтерактивны); проверять нечего.
- **Вкусовое** название группы «Не ошибка» против альтернативы «Прочие события» (спека оставляет выбор создателю) — не техническое.
