# QA-прогон: ускоритель / мини-игра «Прокладка маршрута» — прицельный перепрогон после дробления файлов

**Дата:** 2026-09-26
**Задача:** независимый прицельный перепрогон линии «Ускоритель / мини-игра „Прокладка маршрута"» после рефакторинга-дробления крупных файлов с barrel-реэкспортом (коммит `eef2b93`, рабочая копия). Цель — убедиться, что дробление ничего не сломало.
**Вердикт:** **ПРОЙДЕНО** (все 7 пунктов; N-A нет — живой сервер поднят, e2e прошли целиком).

## Прогнал

```
git status --porcelain                                              # чисто по проверяемой линии (изменений нет)
node --check web/static/js/route/*.js                               # 20/20 OK, TOTAL_FAIL=0
# линковка модулей в Node (кроме браузерной точки входа route_game.js)
node %TEMP%\route_link.cjs                                          # 19/19 ok, route_game.js SKIP, TOTAL_FAIL=0
node route_game.js probe                                            # ReferenceError: document is not defined (ожидаемо)
go build ./...                                                      # EXIT=0
go vet ./...                                                        # EXIT=0
go test -race -short -count=1 ./internal/routegame/ ./internal/handlers/   # EXIT=0 (routegame 80.6s, handlers 42.5s)
node tools/e2e/route-check.js                                       # ALL PASS — PASS=13 SKIP=0 FAIL=0, EXIT=0
node tools/e2e/map-check.js                                         # ALL PASS (5/5), EXIT=0
git diff --name-status eef2b93^ eef2b93                             # состав дробления
```

## Пункты

1. **PASS** — `node --check` по всем 20 файлам `web/static/js/route/*.js`: без ошибок (`TOTAL_FAIL=0`).
2. **PASS** — все 19 модулей `web/static/js/route/*`, кроме `route_game.js`, линкуются в Node без ошибок — в т.ч. barrel-фасады `route_config.js` / `route_figures.js` / `route_render.js` (если бы реэкспорт указывал на отсутствующее имя, Node упал бы на линковке). `route_game.js` — ожидаемо `ReferenceError: document is not defined` (браузерная точка входа, не SyntaxError/ошибка импорта).
3. **PASS** — `tools/e2e/route-check.js`: `RESULT: ALL PASS — PASS=13 SKIP=0 FAIL=0`, exit 0. Все шаги прошли на живом сервере: STEP 1 (offer), STEP 2 доска/мобильная вёрстка/легенда, STEP 3 negative + breakdown + clean course, STEP 4 scan, STEP 5 submit, STEP 6 applied, STEP 7 already_active, pageerrors none. N-A ни по одному шагу не потребовалось.
4. **PASS** — `tools/e2e/map-check.js`: `RESULT: ALL PASS` (5/5), exit 0 — регресса карты нет.
5. **PASS** — `go build ./...` exit 0; `go vet ./...` exit 0.
6. **PASS** — `go test -race -short -count=1 ./internal/routegame/ ./internal/handlers/`: оба пакета `ok`, exit 0, детектор гонок чист (прогон свежий, не cached).
7. **PASS** — структура после дробления:
   - Новые файлы дробления (`accelerator_grid_{board,dto,scan}.go`, `grid_explain_{geometry,uncertainty,util}.go`, `route_{labels,geometry,render_background,render_core,render_geom,figures_field,figures_landmarks,figures_marks,figures_prims,figures_sector}.js`) — ни одного дублирующего имени (глобальная проверка совпадений имён по `web/static/js/**` и `internal/**` их не находит). Предсуществующие совпадения (`doc.go`, `config.js`, `events.js`, …) к этой правке отношения не имеют.
   - Barrel-фасады сохранили контракт импортов: `route_config.js:75-76` реэкспортит `route_labels.js`/`route_geometry.js`; `route_figures.js:11-15` реэкспортит 5 модулей фигур, сам держит диспетчер `drawLegendFigure` (`route_figures.js:26`); `route_render.js` — оркестратор, тянет `route_render_geom.js`/`route_render_background.js`/`route_render_core.js` (`route_render.js:19-21`); `grid_explain.go` — публичный API + оркестратор, делегирует `explainGeometry`/`explainUncertainty` (`grid_explain.go:140-141`), сортировка `gridSortFactors` вынесена в `grid_explain_util.go`.

## Найденное

Багов нет. Правок кода не делал.

## Не проверено (граница покрытия)

- **Визуальный паритет мини-игры «глазами игрока» (пиксель-в-пиксель до/после дробления):** e2e подтверждает отсутствие JS-ошибок и работоспособность доски/легенды/разбора, но не сравнивает кадр до и после рефакторинга. Чтобы закрыть — снять скриншоты доски до и после (артефакты `route-check.js`) и сверить; либо визуальный приём создателем.
- **`route_game.js`** как таковой в Node не линкуется (браузерная точка входа) — полноценно покрыт только живым браузером в `route-check.js`.

## Фикстуры

- `tools/e2e/route-check.js`: создан throwaway-пользователь `e2e_route_1790414494000_0` (перелёт `Ectorti`, dist=925, boost применён, остаток 333 s).
- `tools/e2e/map-check.js`: создан throwaway-пользователь `e2e_1790414583610_0`.
- Скрипты по замыслу оставляют пользователей с префиксом `e2e_` (эндпоинта самоудаления нет); след оставлен выше.

## Предложение в чек-лист

- Кейс зоны B-ACC: «после дробления файлов линии — `node --check` + линковка всех модулей `route/*` в Node (кроме браузерной точки входа), `route-check.js`/`map-check.js` ALL PASS, `go build`/`vet`/`go test -race` зелёные». Применит @manager.
