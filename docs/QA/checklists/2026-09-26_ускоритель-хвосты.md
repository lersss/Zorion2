# QA-прогон: ускоритель — хвосты (анти-бот, гонка сегмента, гейты, тап-подсветка, пул спрайтов, доки)

**Дата:** 2026-09-26
**Задача:** независимая проверка пакета правок трёх инстансов по линии «Ускоритель — модуль корабля и мини-игра „Прокладка маршрута"» (рабочая копия, не закоммичено).
**Вердикт:** **ПРОЙДЕНО** (все 11 пунктов; 2 из них частично N-A — см. пояснения).

## Прогнал

```
git status --porcelain
go build ./...                                              # EXIT=0
go vet ./...                                                # EXIT=0
go test -race -short ./internal/routegame/ ./internal/handlers/ ./internal/travel/ ./internal/repository/ ./internal/models/   # EXIT=0, все ok
go test ./internal/routegame/ -run TestGridEvaluate_PathValidity -count=1 -v      # PASS
go test ./internal/handlers/ -run 'TestAcceleratorOfferRateLimited|TestAcceleratorScanRateLimited|TestAcceleratorBoostRateLimited|TestAcceleratorTravelReason' -count=1 -v   # PASS
Get-ChildItem web/static/js/route/*.js | node --check      # все 10 файлов OK
cd tools/e2e; node route-check.js                           # ALL PASS (13/13), EXIT=0
cd tools/e2e; node map-check.js                             # ALL PASS (5/5), EXIT=0
```

## Пункты

1. **PASS** — `go build ./...` exit 0.
2. **PASS** — `go vet ./...` exit 0.
3. **PASS** — `go test -race -short` по 5 пакетам: все `ok` (handlers 23.4 s, остальные cached), exit 0; детектор гонок чист.
4. **PASS (живой API — N-A)** — минимальный шаг отсекается `step_too_small`: `internal/routegame/grid_model.go:714` + проверка на `grid_model.go:745-749` (до `not_adjacent`), тест `internal/routegame/grid_model_test.go:203-206` (`TestGridEvaluate_PathValidity`, PASS); `429 rate_limited`: per-user token-bucket `internal/handlers/accelerator_ratelimit.go` (карта под `sync.Mutex`, `allowAccelRequest`), вызывается в offer/scan/boost (`accelerator_game_handlers.go:88`, `accelerator_grid_handlers.go:309`, `accelerator_grid_boost.go:101`) через общий `writeAcceleratorRefusal` (та же форма `{available:false, reason, cooldown_remaining_s}`), тесты `TestAccelerator{Offer,Scan,Boost}RateLimited` — PASS. **Живой API — N-A:** dev-сервер (`zorion-server`, PID 2548) запущен 14:24:19, а правки файлов — 15:28–15:55 → крутится старый код; перезапуск не делал (чужая сессия создателя, см. «Не проверено»).
5. **PASS** — `Replace` удалён, вместо него `RoutePuzzleRepository.Ensure` (`internal/repository/route_puzzle_repository.go:55-73`): `INSERT … ON CONFLICT (user_id, kind) DO UPDATE … WHERE player_route_puzzle.segment_hash IS DISTINCT FROM EXCLUDED.segment_hash` + перечитывание `Get` канонической строки; в хендлере `accelerator_grid_handlers.go:173-192` — `Ensure` + гард `canonical.SegmentHash == hash`. При одинаковом новом хэше у конкурентных запросов второй upsert не проходит WHERE и читает строку первого → один `secret`/одна доска. SQL-форма закреплена `TestRoutePuzzleEnsureUpsertAndReread`.
6. **PASS** — единый источник причин `acceleratorTravelReason` (`internal/handlers/accelerator_handlers.go:67`), порядок `no_module → unknown_game → already_active → too_short → cooldown`; используют: offer (`accelerator_game_handlers.go:117`), boost (`accelerator_grid_boost.go:141,154`), scan (`accelerator_grid_handlers.go:341`), блок `/travel` (`accelerator_handlers.go:103`, оба пути — `travel_handlers.go:390` и `:504`); блок `/me` (`auth_handlers.go:348` → `acceleratorMeBlock`) отдаёт `active`/`ready` без `reason` (по контракту) и считает те же `models.BoostActive`/`models.CooldownRemaining`; отказы — общий `writeAcceleratorRefusal`. Порядок покрыт `TestAcceleratorTravelReason` (11 кейсов) — PASS.
7. **PASS (по коду; живой тап строки — в «Не проверено»)** — `factorRow` (`web/static/js/route/route_ui.js:159-217`): клетки дедуплицируются через `Set`, класс `interactive`/`role=button`/`tabIndex` только при непустых `cells`; `toggle` по повторному тапу снимает `on` и зовёт `onHighlight(null)`, по первому — `clearHighlight(null)` + `onHighlight(cells, factorColor(...))`; клик по фону оверлея снимает (`route_ui.js:131-133`); `state.highlight` рисуется `drawHighlight` (`route_render.js:338-353`); CSS `.bd-row.interactive`/`.bd-row.on` (`web/static/css/route.css:244-247`). Строки без `cells` не интерактивны.
8. **PASS** — в `web/static/sprites/route/` осталось 6 PNG (`*_01`), в `web/static/sprites/route/_pool/` — ровно 16 вынесенных; `POOLS`/`SPRITE_FREEZE` сведены к `*_01` (`route_config.js:13-28`); предзагрузка (`route_sprites.js:13-57`) берёт `SPRITE_FREEZE` и на `img.onerror` резолвится (фолбэк не сломан); доска рисуется — route-check STEP 2 `canvas=390x844 px=329160` PASS.
9. **PASS** — публичные поля сектора `sig_name`/`surround_name` заполняются из `Sig.String()`/`Surround.String()` (`internal/handlers/accelerator_grid_handlers.go:99-101,251-253`), String-методы — `grid_model.go:105,159`; значения Тихий/Ровный/Гулкий и Ничего/Кордон/Обрыв/Мгла/Течение закреплены `internal/routegame/grid_sector_names_test.go` (PASS).
10. **PASS** — `tools/e2e/route-check.js`: `RESULT: ALL PASS — PASS=13 SKIP=0 FAIL=0`, exit 0 (реальная доска, легенда, разбор, scan, boost, already_active, 0 pageErrors); `tools/e2e/map-check.js`: `ALL PASS` (5/5), exit 0 — регресса карты нет.
11. **PASS** — `docs/DB.md` описывает `player_accelerator` (миграция `000089`, файл `migrations/000089_accelerator.sql`) и `player_route_puzzle` (миграция `000091`, файл `migrations/000091_player_route_puzzle.sql`); `docs/GLOSSARY.md` — раздел «18. Ускоритель и мини-игра „Прокладка маршрута"» с терминами; `docs/QA_CHECKLIST.md` — зона B-ACC-1…B-ACC-5.

## Найденное

Багов нет. Наблюдения (не блокеры):
- `web/static/js/route/route_config.js:311-327` (`REASON_TOAST`) не содержит `step_too_small`: если сервер вернёт этот код (нештатный путь с дребезгом клеток), клиент покажет общий `fallback` (`route_game.js:143`), а не осмысленный текст. Штатно недостижимо — клиент дедуплицирует точки пути. Кандидат на бэклог, не баг.

## Не проверено (граница покрытия)

- **Живой API новых гейтов/анти-бота** (429 `rate_limited`, `step_too_small` от boost, `sig_name`/`surround_name` в offer): dev-сервер работает на старом коде (процесс с 14:24:19, правки 15:28–15:55). Нужен перезапуск сервера создателем на новом коде — сам не перезапускал, чужие сессии не трогал.
- **Живой тап-подсветки строки разбора** (п. 7): в `route-check.js` нет шага «тап по `.bd-row.interactive` → подсветка клеток → снятие тапом по фону»; проверял чтением кода. Чтобы закрыть — добавить такой шаг в `tools/e2e/route-check.js`.
- **Реальная конкурентность `Ensure`** (п. 5): подтверждена чтением SQL и разбором поведения row-lock PG; `sqlmock` блокировку строк не воспроизводит. Для полного закрытия — интеграционный тест на живой БД (2 горутины, один `secret`).
- **Вкусовое:** выбор `_01` как замороженных победителей пула (делегирован визуальному дизайнеру) и разборчивость каймы подсветки по цветам фактора в градациях серого — глазами игрока.

## Фикстуры

- `tools/e2e/route-check.js`: создан throwaway-пользователь `e2e_route_1790413202575_0` (+ перелёт `Ectorti`, dist=925, boost применён, остаток 182 s).
- `tools/e2e/map-check.js`: создан throwaway-пользователь `e2e_1790413219687_0`.
- Удаление через `tools/db.ps1` не прошло: FK `player_planet_knowledge_user_id_fkey` без CASCADE. Скрипты e2e по своему замыслу оставляют префиксованных пользователей («no self-delete endpoint»). Оставлены в dev-БД с префиксом `e2e_`.

## Предложение в чек-лист

- Дополнить B-ACC-4 шагом: у строки разбора с непустыми `cells` — тап подсвечивает связанные клетки (дедуп), повторный тап и тап по фону оверлея снимают; строка без `cells` не интерактивна (сейчас в чек-листе только содержание строки/групп).
- Дополнить B-ACC-2/B-ACC-3 проверкой `sig_name`/`surround_name` в ответе `offer` (player-facing словарь Тихий/Ровный/Гулкий; Ничего/Кордон/Обрыв/Мгла/Течение).
- Добавить кейс анти-бота: 11-й запрос offer/scan/boost за секунду (burst 10) → `429 rate_limited` в единой форме отказа; путь с повтором соседней клетки → `400 step_too_small`.
- Кейс на гонку сегмента: два параллельных offer при смене сегмента → один `secret`/одна доска (сейчас покрыто только SQL-формой на sqlmock).
