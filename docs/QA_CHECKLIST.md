# QA Check-List Zorion

> Реестр ручных проверок «проблема → время последней проверки» (критичные точки).
> Файл НЕ растёт: при каждом прогоне перезаписывается только колонка «Последняя проверка».
> Источник структуры: `docs/gamedesign/ideas/qa-checklist.md` (согласовано игроком 2026-09-14).
> Единственный писатель — @manager (63a): состав критичных точек, даты из отчётов инстансов.
>
> **Правило прогона:** полный прогон — после больших релизов / раз в 3–4 мелких релиза;
> при обычной задаче — только затронутые диффом кейсы, списком от @manager.
> Отчёты инстансов @tester — журнал `docs/QA/checklists/<дата>_<задача>.md` (сюда не пишут).

## Ядро (гонится всегда)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| A1   | Сервер стартует с env из AGENTS.md §1, `/health` → 200 «OK», `/status` → 200 «All systems ready» | 2026-09-18 |
| A2   | Регистрация нового пользователя → логин → `/me` с JWT возвращает профиль (включая ship_model/ship_catalog/ship_speed_factor, 91a) | 2026-09-18 |
| A3   | Через админку: `/admin/generate` → `/admin/generate-planets` → миры и планеты есть (`/admin/worlds`, `/admin/stats`) | 2026-09-16 |
| A4   | Карта открывается (`/map` — регионы/кластеры), клик по системе открывает модалку без ошибок JS | 2026-09-15 |
| A5   | Полёт туда-обратно между системами не падает: `/travel` → ожидание прибытия (3–60 с) → `/me` показывает новый мир → обратный полёт | 2026-09-16 |
| A6   | Конкурентные риски AGENTS.md §0: параллельные регистрации, полёты, просмотры карты и ws-соединения (`/ws`) не роняют процесс, `/health` жив | 2026-09-15 |
| A7   | `/admin/audit` на сгенерированном мире → 200, JSON с `TotalEntities > 0`, без паники | 2026-09-16 |

## Зоны

### auth (JWT, middleware, истёкший/битый токен, доступ к игровым маршрутам)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-AUTH-1 | Игровой маршрут (например `/me`, `/worlds`) без заголовка `Authorization`, с «Basic …» или «Bearer» без токена → 401, процесс жив (`middleware.go:17-28`) | 2026-09-16 |
| B-AUTH-2 | Токен, подписанный старым `JWT_SECRET` (рестарт с другим секретом), → 401 «Invalid token»; повторный логин выдаёт рабочий токен | 2026-09-14 |
| B-AUTH-3 | Нет утечки чужих данных: `/me`, `/api/contracts` под токеном A возвращают данные только A; токен B на этих маршрутах не проходит | — |
| B-AUTH-4 | `/register` с пустым username/password → 400; повторная регистрация того же username → 409; `/login` с неверным паролем → 401 | 2026-09-15 |
| B-AUTH-5 | Роль изменили после выдачи токена (игрок → admin, без пере-входа): `/admin` показывает окно входа, 0 запросов к `/admin/*`, 0 тостов-ошибок — роль для UI-гейта берётся из токена, а не из БД (идея 2026-09-23) | 2026-09-23 |
| B-AUTH-6 | Skycomposer: вкладка «Пользователи» видна, `GET /admin/users?page=1&limit=1` → 200, остальные вкладки без 403 (парный кейс к B-AUTH-5, идея 2026-09-23) | 2026-09-23 |

### генерация (галактика/планеты/фракции: краевые параметры, объёмы, пустой вход)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-GEN-1 | Повторный POST `/admin/generate` во время идущей генерации → 409 «Generation already running» (`TryStart`, `admin_universe.go:147-151`), двойной генерации нет | 2026-09-15 |
| B-GEN-2 | `/admin/generate` с пустым телом и краевыми значениями (`world_count=0/-1`, `outlier_percent=99`) → дефолты и кламп (≤50), генерация завершается без ошибок | — |
| B-GEN-3 | `world_count=100000`: генерация мира доходит до конца (прогресс в `/admin/generate-status`), процесс не падает, `/admin/stats` показывает сгенерированное число | — |
| B-GEN-4 | `/admin/generate-cancel?job=generate_universe` во время работы → статус «canceled», повторный запуск генерации возможен | — |
| B-GEN-5 | `/admin/generate-factions` без заселённых планет → 200 `{"status":"factions_generated","total":0}`, без ошибки | — |
| B-GEN-6 | Каждая из новых форм кластера (ring/bar/spiral/dumbbell/stream/core_halo/random) генерирует вселенную без паники; селект формы передаёт значение как есть; blob/circle не сломаны (идея 57a) | 2026-09-16 |
| B-GEN-7 | Профили регионов (59a): генерация вселенной → ~75% регионов с `profile`+`profile_intensity` (0/1/2, классы из каталога); звёзды в профильных регионах сдвинуты (young: O/B↑, M↓; metal_rich: [Fe/H]↑; dead: M/L/T/Y↑, экзотика↑), все типы возможны; планеты: `planet_count_mult` (dead 1.29 vs фон 1.65), `gas_giant_shift` (dead/quasar/old ↓, young/metal_rich ↑), surface/resource bias направленно; инварианты: масса в диапазоне класса, [Fe/H] ∈ [−0.8,+0.5], планеты 0–8, гиганты [0.05,0.95], каскад не тронут; `/api/regions` отдаёт `profile` (отладочно); на карте под названием региона вторая строка с типом — только у admin/skycomposer (у игроков скрыта, 59a) | 2026-09-17 |

### пакман/вайп (идея 2026-09-20: джоб, поедание, WS-событие всем, клиентский вау-эффект)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-PAC-1 | Джоб: POST `/admin/pacman/start` → 202; повторный → 409; при идущем любом джобе генерации/очистке → 409; отмена `/admin/generate-cancel?job=pacman` → `canceled` с отчётом «Съедено N из M» (не error); повторный запуск после отмены работает | 2026-09-20 (смоук; полный прогон @tester отложен) |
| B-PAC-2 | Поедание: после вайпа worlds/planets/settlements/factions/locations/production_units/assignments/settlement_log/regions/npc_agents/player_planet_knowledge = 0; users не тронуты; у игроков, чей мир съеден, current_world_id = NULL; опустевшие регионы удалены, частичные пересчитаны; events (лог) не тронут | 2026-09-20 (живой смоук 80к/44 с; полный прогон отложен) |
| B-PAC-3 | WS всем игрокам: `pacman_start` (1×), `pacman_position` (дроссель), `pacman_eaten` (≤200 миров, только открытые данные id/name/x/y/spectral — 77a), `pacman_end`; поздние подключения видят (активация от любого события) | 2026-09-20 (смоук; playwright-headless WS не ловит — артефакт среды, у реального браузера работает) |
| B-PAC-4 | Клиент: пакман рисуется (жёлтый 34 px, рот-сектор 0→69° из центра, без глаз/обводки, нимб, след), **звёзды лопаются при прибытии пакмана** (не заранее/сами), баннер «Галактику пожирает Пакман! N/M», кнопка «Следить за Пакманом», тосты, оверлей «Галактика пуста» | 2026-09-20 (приёмка создателя вживую; e2e-смоук ALL PASS) |
| B-PAC-5 | Скорость float64 >0..10000 (0.01 — «кинорежим»), адаптивный батч = 1 при `wps < batch_size` (поедание по одной), траектория `nearest` (жадный ближайший сосед; spiral/snake — опции) | 2026-09-20 (смоук 0.5/с и 1/с — по одной) |
| B-PAC-6 | Миграция 000047 (индексы npc_agents) применена; снапшот карты пересобран после джоба (фильтры/карта без «призраков» съеденных миров); во время джоба полный снапшот (съеденные скрывает клиент по eaten) | 2026-09-20 (смоук) |
| B-PAC-7 | Игроки под вайпом: полёт к съеденному миру прерывается с уведомлением; дефенсив onArrival — битая цель → NULL, НЕ FK-ошибка; внутрисистемные полёты съеденных миров удалены; пустая галактика — дружелюбное сообщение, не 500 | 2026-09-20 (частично; полный прогон отложен) |

### админка/джобы (красная кнопка-стоп, восстановление после F5; идея 2026-09-20)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-ADM-6 | Красная кнопка-стоп джоба у заголовка «Генерация»: скрыта без джобов → видна при запуске любого джоба (в т.ч. пакмана) → F5 восстанавливает прогресс и кнопку без задвоения джоба → клик с confirm → canceled + поллинг остановлен → повторный запуск работает | 2026-09-20 (два полных прогона @tester на чистой галактике 30k; консоль: только 404 favicon.ico, не связан с фиксом) |

### экономика/поселения (пересчёт населения, чек-точки, Δt=0, смертность)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-ECO-1 | Холодная планета (T ≤ 100 K; R-модель, R_холод — гладкая, обнуления нет, 99.2.13) и жаркая с витринным порогом «t_смерти(p₀) < 1 ч» (R-модель, применена 2026-09-14): модалка и `/admin/mortality-preview` показывают население по `r_per_sec` / «поселение не выживет» (не NaN/Inf), JSON валиден | 2026-09-15 |
| B-ECO-2 | Повторные просмотры планеты в пределах `MinPersistInterval` (30 мин) не двигают чек-точку: население стабильно, новые записи в БД не появляются (ленивый пересчёт) | 2026-09-14 |
| B-ECO-3 | Край Δt: просмотр планеты практически сразу после предыдущего (Δt≈0, отрицательное Δt) не ломает пересчёт — население не меняется и не NaN (`recompute.go:24-27`) | 2026-09-14 |
| B-ECO-4 | `/admin/mortality-preview` с экстремальными входами (T=5000 K, 23 K, 100g, rad 100) → валидный JSON без `+Inf`/NaN (гвард `r < 1` для экстремальной g, 99.2.13), страница админки жива | 2026-09-15 |
| B-ECO-7 | Равновесие без потолка (99.2.16): поселение в полном комфорте (293 K, g 1, rad ≤ 20) при r = 0 (нетто 0, k=1) → запись «Вымерло» НЕ создаётся (равновесие живёт вечно); при убыли (k<1) → запись с честной датой через Unix-секунды от computed_at; возраст ≥ 120 лет население НЕ обнуляет (потолок удалён) | 2026-09-15 |
| B-ECO-8 | `/admin/settlement-settings` (99.2.16): без роли admin/skycomposer → 401; GET → дефолты (СПЖ 50, k 2) и валидация значений; PATCH невалидных (СПЖ 0/600, k −1/11) → 400; PATCH валидных → 200 и ручки применяются (пересчёт населения виден) | — |
| B-ECO-9 | Ветка поселения (итерация 2, миграция 000064): живой сценарий — во вход подложены компоненты рецепта «Пища», `processed_at` отброшен → вход убывает, выход растёт, чек-точка двигается (персистентно); конкурентно админский ввод + чтение карточки → инкремент не теряется, минуса нет (параллельные админ-мутации → 409); границы ручек: `amount≤0`/не-компонент/рецепт без компонентов → 422, дубль ветки → 409, нет ветки/поселения → 404; новый заполненный компонент рецепта получает строку входа (top-up); player не видит вход, admin видит | 2026-09-22 |
| B-ECO-10 | Добыча из залежи (итерация 3, миграция не требуется): живой сценарий — залежи «Мясо» + «вода-ресурс» на планете поселения, ветка «Пища» → при отброшенном `processed_at` запасы залежей убывают, выход растёт, вход остаётся проходным (~0); запас упирается ровно в 0 (не ниже), ветка встаёт целиком при истощении одного компонента; несколько залежей одного ресурса — от крупной к мелкой; две ветки на одной залежи — суммарная убыль равна сумме выходов, без минуса и потерь; путь «в памяти» (<30 мин) не пишет в БД; игрок со знанием не видит залежь с запасом 0 (активные видит), без знания залежей нет, админ видит с пометкой «выработано» | 2026-09-22 |
| B-ECO-11 | Потребление населением и тип поселения (итерация 4, миграция `000067`): тип поселения — подтип справочника строений «Поселение» **без привязки к категории товаров** (класс строения есть всегда), все существующие поселения получают тип (бэкфилл), тип проставляется при создании; живой сценарий — две ветки («Вода» рецепт 73, «Пища» рецепт 69), при отброшенном `processed_at` выход растёт на произведённое и убывает на съеденное (порядок «производство → потребление»), остаток («осадок») копится ≈+10 % производства; норма берётся по товару-выходу ветки и изолирована (правка нормы одного товара не меняет другую ветку: вода — своя, еда — своя, не суммируются), явный `0` = «не ест этот товар», нет записи для товара / нет типа → `DefaultEatK`; кламп ≥ 0 и дренаж буфера; идемпотентность, путь «в памяти» не пишет, два параллельных синка едят один раз; игрок не получает вход/детали поселений, админ видит вход/выход/сделано/съедено/скорость; карточка показывает «Выход · остаток (осадок)», «за проход: сделано/съедено», «скорость поедания (ед/сек)»; студия — подтип виден в дереве, «+ подтип» без категории, удаление используемого типа → 409 | 2026-09-22 (@tester 15 PASS / 0 FAIL / 1 SKIP: живой вайп/Пакман не запускался — общая dev-вселенная) |
| B-ECO-12 | Админ-витрина R в карточке поселения (идея 2026-09-25): админам (admin/skycomposer) рядом со стрелкой населения — суммарный R в `%/с` (тысячные, замена «непонятных» `e-4`), в тултипе — состав (`r_breakdown`: среда natural/birth/heat/cold/gravity/radiation + ряды эффектов), сумма частей == `r_per_sec`; у player (presence) и в snapshot/scan/none поля `r_breakdown`/`r_per_sec` нет (нет утечки); реальный браузер: span `%/с` и `title` с «R суммарный»/«Состав», 0 JS-ошибок; синтаксис карточки — `node --check web/static/js/modal/tabs.js` + `TestKnowledgeTabsInNode` | 2026-09-25 |

### контракты (доска, эскроу, ленивая материализация; регресс ЧК3, 2026-09-23)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-CON-1 | Ленивая доска — кадэнс/идемпотентность (спека 2026-09-23): повторное чтение доски планеты в пределах кадэнса не пишет ничего (нет новых долей и отметки-строки); при истёкшем кадэнсе — ровно один комплект долей; пакет, **выпавший из источника нужд**, снимается с возвратом залога (`escrow_return`, `reason='superseded'`), а «ручной» открытый контракт без пакета на той же планете **остаётся** (F1) | 2026-09-23 (ЧК3) |
| B-CON-2 | Пакет контрактов: дубли нужд от датчика не откатывают проход (берётся одна, детерминированно, F2); второй `Take` доли того же пакета тем же игроком → 409 «уже есть взятая доля» (не «недоступно»); взятая доля при материализации не меняется (статус/награда/объём/срок) | 2026-09-23 (ЧК3) |
| B-CON-3 | Пакет в админ-публикации + DTO доски (ЧК4, 2026-09-23): `POST /admin/contracts` с `type=supply`, `author_type=building`, `package_key`/`share_index` (1 и 2) создаёт две **открытые** доли одного пакета; игровая публикация (`POST /api/contracts`) с любым из полей пакета → **400** (инструмент только админский); `share_index ≤ 0` → **400**; повторная публикация открытой доли того же `(package_key, share_index)` → **409**; доска планеты отдаёт `package_key`/`share_index` и **не** отдаёт `executor_*` и денежные поля залога (`escrow_*`) | 2026-09-23 (ЧК4, ПРОЙДЕНО) |
| B-CON-4 | Группировка доски (ЧК4, 2026-09-23): на вкладке «Контракты» доли одного пакета рисуются **одним блоком** «нужда» со списком долей (размер, награда, срок), а заказ без пакета — отдельной прежней строкой; живой второй `Take` доли того же пакета тем же игроком → тост с ошибкой и 409; 0 ошибок в консоли, смоук `tools/e2e/contracts-board-check.js` = ALL PASS | 2026-09-23 (ЧК4) |
| B-CON-5 | Сдача груза (ЧК2б, 2026-09-25): стоя на орбите планеты заказа, взятый supply-заказ: частичная сдача → пропорциональная выплата, остаток требования уменьшается, ячейка хранилища растёт; довоз до `completed`; **Σ выплат = исходному залогу**; «лишнее» не берётся (в трюме остаётся), «не то» → 422, трюм не тронут | 2026-09-25 (ЧК2б, API-уровень) |
| B-CON-6 | Гейты сдачи (ЧК2б, 2026-09-25): не с орбиты (surface/чужая планета) → 422; нет контракта → 404; не supply/не taken → 422/409; чужой исполнитель → 403; ячейка владельца по товару отсутствует → 409 (fail-closed, ячейка не создаётся); ячейка у двойного порога → 409 (`2·cap − amount ≤ 0`); автор-строение → 422 (задел); бесплатный supply — полная сдача пишет нулевой `money_operations`, частичная при `pay=0` — без движения | 2026-09-25 (ЧК2б, API-уровень) |

> Живой путь «нужда → доля» появляется с носителем нужд — Поставка 2; до неё движок покрыт интеграционными тестами `internal/repository` (гонка материализации × истечение, F1/F2), а пакет вручную создаётся админ-инструментом (ЧК4) — им же проверяются B-CON-3/B-CON-4.

### полёты (travel: несуществующая система, повторные вызовы)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-TRV-1 | POST `/travel` с несуществующим `world_id` → 404 «World not found», процесс жив (`travel_handlers.go:70-74`) | 2026-09-18 |
| B-TRV-2 | `/travel` в текущий мир (from == to) → 400 «Already in this world» | 2026-09-16 |
| B-TRV-3 | Повторный `/travel` во время полёта (61a): новая цель → редирект 202 (корабль продолжает из текущей точки P); та же цель → 202 идемпотентно (полёт не сброшен); мир отправления → 202 редирект (разворот из точки P, честная длительность, 66a) | 2026-09-18 |
| B-TRV-4 | Полёт с битым `current_world_id` (мир удалён перегенерацией) → не падает: сброс на первый доступный мир, полёт стартует | — |
| B-TRV-5 | Рефреш в полёте (42a): F5 во время активного полёта → полёт восстанавливается из `/me`, фокус сохранён; «Найти меня» вне полёта не включает слежение (followShip зачищается) | — |
| B-TRV-6 | Редирект (61a): в полёте выбор другой звезды → полёт перенаправлен, корабль продолжает из ТЕКУЩЕЙ точки маршрута P (не отскакивает к миру отправления), летит к новой цели, панель показывает новую цель | 2026-09-16 |
| B-TRV-7 | Каскадный редирект (61a): два редиректа подряд — стартовая точка второго берётся от фактического старта сегмента (P первого), а не от мира отправления (нет «перескока» на линию A→нов.цель) | 2026-09-16 |
| B-TRV-8 | Честная длительность (61a): dist > 66 → duration > 20 сек (потолок убран), минимум 3 сек сохраняется; полёт без смены цели — поведение не изменилось | 2026-09-16 |
| B-TRV-9 | Возврат в мир отправления во время полёта (66a): обычный редирект — корабль разворачивается из текущей точки P и летит обратно с честной длительностью (dist×0.3, мин 3), без телепорта; ответ — обычный TravelResponse (from == to == A, старт = P); без полёта — прежний 400 «Already in this world» | 2026-09-17 |
| B-TRV-10 | Полёт без двигателя (91a §6.1): player с пустым `equipment.engine` → `/travel` 400 «Двигатель не установлен — полёт невозможен»; кнопка «Лететь»/«Перелететь» на карте заблокирована с подсказкой; админ/skycomposer без двигателя летает (202) | 2026-09-18 |
| B-TRV-11 | Полёт переживает рестарт сервера (97a): активный полёт (будущий arrive_at) → рестарт → `/me` отдаёт flight с ОРИГИНАЛЬНЫМИ start_time/duration, остаток пути корректен (включая простой сервера), прибытие вовремя → current_world_id = целевой мир, строка в player_flights удалена | 2026-09-18 |
| B-TRV-12 | Рестарт дольше полёта (97a): полёт завершился в простой → при старте сразу засчитано прибытие (`/me`: current_world_id = целевой, flight = null, строка удалена) | 2026-09-18 |

### NPC (массовая генерация, тик планировщика, кэш позиций — 26c)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-NPC-1 | Метрики при 100k агентов: `last_tick_ms` 100–300 мс, overdue не лавинит, full_batches стабилен | — |
| B-NPC-2 | `/api/npc/positions` — 100k записей, поля, x/y числа, оба статуса | — |
| B-NPC-3 | Регрессия кэша: create/PATCH/DELETE/clear/generate → метрики и позиции сходятся без «призраков» | — |
| B-NPC-4 | После clear → регенерация → планировщик подхватывает | — |
| B-NPC-5 | Сверка позиций с БД на живом сервере: у выборки flying `positions.target/current` == БД `target/from`, в пачке ≥2 разных target (регрессия алиасинга 26c A2) | — |

### Высадка/поверхность (мини-игра «прогулка»; спека 2026-09-21)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-SURF-1 | Высадка/возврат: `POST /api/surface/land` с орбиты планеты → `status=surface`, `hp=100`, биом ∈ биомов планеты (жребий ∝ `share`); `POST /api/surface/leave` → `orbit`; повторные вызовы идемпотентны (тот же биом, hp пересчитан, не 500) | 2026-09-21 |
| B-SURF-2 | Скрытие: `/api/players/positions` НЕ отдаёт игрока со `status=surface` (другие не видят); на орбите — виден (контроль) | 2026-09-21 |
| B-SURF-3 | Серверный HP: `/me` и повторный `land` отдают **пересчитанный** от `landed_at` hp (не сохранённый); рефреш не восстанавливает здоровье; на мягкой планете урона нет | 2026-09-21 |
| B-SURF-4 | Вход/флоу (e2e, реальный браузер): ПКМ по планете на её орбите → «Высадиться» → прелоадер скрывается → брифинг кликабелен → прогулка открывается, мир рисуется, ходьба/прыжок, HUD; «вызвать корабль» → орбита; 0 pageerror / JS-ошибок / тостов-ошибок / разлогинов. **Оговорка (2026-09-24):** пиксельный детектор прыжка (пункт `10g` в `surface-walk-check.js`) нестабилен в headless (флейк: то `idleY=-1`, то `idleRange` при рабочем прыжке) — не связан с видом биома, провалом прогона не считать | 2026-09-21 |
| B-SURF-5 | Отказы: `land` не с орбиты / на поверхности другой планеты / при `in_flight` / планета без биомов → 400; **`POST /api/intrasystem-flight` с поверхности → 202, `from_type=planet`, `from_id` = планета поверхности** (взлёт + сегмент, не «молчаливый старт от звезды»; 2026-09-22) | 2026-09-22 |
| B-SURF-6 | Визуал прогулки: зум ~1.8× (мир/игрок крупнее, физика в метрах та же); земля покрывает всю ширину экрана на широком окне, деревья на земле, вертикального шва нет; тела неба — реальные текстуры планет (совпадают с модалкой системы), спутники диском, при недоступности (401) — фолбэк без падения; резкость (суперсэмплинг ×2) без блоков | 2026-09-22 |
| B-SURF-7 | Парящая порода: растр чанка ⊇ твёрдая область float-формаций — нет невидимых твёрдых объёмов (игрок не упирается в «пустоту»). Автотест `tools/e2e/surface-float-coverage-check.js` (цель 0; контроль старым алгоритмом >0 — тест не тавтологичен) | 2026-09-22 |
| B-SURF-8 | Погода — гейты выбора из физики (11 явлений): вакуум/тонкая (`pressure_atm` вне `suit.pressure_comfort_atm`) → только штиль+пыль (без осадков/туманов/сияния/свечения); комфорт → явления по осям; гроза только при T ≥ 273.16 K; крио-варианты (иглы/град) требуют присутствия вещества; пепельный дождь — вулканизм и p ≥ 0.5; `life=false` → без свечения; сияние только при метке `radioactive`; пороги — из `suit`/тройных точек/каталога (не «на глаз»); админ-форс любого из 11 работает | 2026-09-22 |
| B-SURF-9 | Погода — детерминизм/анти-повтор: одно `(seed, цикл)` → одно явление и вариант; явление не повторяется два цикла подряд; рефреш/повторная высадка не «перетасовывают» небо; `Math.random` в исходниках погоды нет; слой суток — чистая функция `f(seed, elapsed)` | 2026-09-22 |
| B-SURF-10 | Погода — вид и слои (11 явлений + варианты + сутки): явления читаются (штиль/туман/пыль/метель/дождь/сияние/гроза/ледяные иглы/ледяной град/пепельный дождь/свечение); метель — снежинки (не «сосульки»), туман на воде — гряды (не «блобы»); подписи HUD по `weatherLabel` (нетоксичный дождь «Дождь», варианты «Смог»/«Ливень» и т.п.); различимы рассвет/день/закат/ночь, ночью сияние/свечение ярче, звёзды гаснут под густым явлением; при ходьбе частицы не мерцают и привязаны к рельефу; HUD чистый | 2026-09-22 |
| B-SURF-11 | Погода — порядок кадра и эмиссивное: финальный эмиссивный слой (`drawEmissive`: разряд+вспышка грозы, glow свечения, aurora+reflection сияния) идёт после переднего тинта среды (`drawEnvironmentFront`) и ночью не гасится; три погодных прохода (Back/Mid/Front) и их порядок не изменены (админ-чипы `гроза`/`свечение`/`полярное сияние` × фаза `ночь`) | 2026-09-22 |
| B-SURF-12 | Погода/прогулка — перф при спринте: нет провалов кадра при стриминге чанков (`tools/e2e/surface-fps-check.js`; в headless значим факт провалов, не абсолютный FPS; окончательно — на GPU создателя) | 2026-09-22 |
| B-SURF-13 | Корабль на месте высадки (2026-09-23): в прогулке над точкой спавна **парит** корабль игрока (иконка/цвет из пакета), лёгкое покачивание; **не твёрдый** (сквозь проходишь), физику/ввод не трогает; порядок слоёв `decor → ship → creatures → player` (игрок под кораблём не перекрыт); на разном рельефе/биомах корабль над точкой спавна; при вызове корабля/смерти исчезает со страницей. **Перед проверкой — перезапустить dev-сервер** (стейл бинаря не отдаёт `ship_*`; AGENTS §13) | 2026-09-23 |
| B-SURF-14 | Вид биома — рецепт из пакета (2026-09-23): у биомов с рецептом `view_source=catalog`, `biome_view` непусто, земля/палитра/декор из рецепта (пески — песок+кактусы без деревьев; джунгли — сомкнутые кроны, контрфорсы, лианы, кучки с зазорами; лес — светлее/просторнее; горы — серый камень, хвоя на нижних склонах); `palette.base == biome_color`; биом **без** рецепта → `view_source=fallback`, прежний вид, 0 ошибок | 2026-09-23 |
| B-SURF-15 | Гейт жизни (2026-09-23): при `life=false` **живого** декора нет (деревья/хвоя/лишайник/трава), неживой (камень/кристаллы/обломки) может быть; фолбэк-биомы — прежнее поведение | 2026-09-23 |
| B-SURF-16 | Админка справочника биомов — секции вида не теряются (2026-09-23): GET несёт `view_families`/`view_primitives`/`view_diagnostics`; после PATCH (в т.ч. правки одного `color`) секции и блоки `view` побайтово идентичны, `view_errors` пусто; битый рецепт вида → фолбэк **одного** биома, каталог/генерация/сервер живы (нефатальная валидация) | 2026-09-23 |
| B-SURF-17 | Кэш картинки планеты — версия (обновлено 2026-09-26, ЧК6 вода): `ImageGenVersion=v11`; `/api/planet-image` отдаёт `v11_*`, легаси `v10_*` не читается; цвет планеты с орбиты соответствует новому цвету биома (10 водных биомов) | 2026-09-26 |
| B-SURF-18 | Ярусный горизонт — данные в пакете (2026-09-23): у биома с рецептом `biome_view.horizon.layers` — ровно 2 пояса, `prim` из `view_primitives` (kind `relief1d`), `parallax` 0.50/0.70, `fill` — ключ палитры (не литерал); у биома **без** рецепта горизонт пусто → прежний вид; клиент применяет рецепт только при `view_version=1` | 2026-09-23 |
| B-SURF-19 | Ярусный горизонт — вид (2026-09-23): 2 декоративных пояса дальнего плана между дальним силуэтом и землёй, силуэт свой у биома (пески — гряды дюн, лес/джунгли — линии крон, горы — планы хребтов); по ярусам **не ходишь** (не твёрдые, физика их не знает); **ночью ярус не светлее неба**; фолбэк-биом — без ярусов | 2026-09-23 |
| B-SURF-20 | Стыки чанков земли — нет тёмных полос (2026-09-23): на однородном светлом биоме (пустыня, день, погода «Штиль») нет тёмных вертикальных полос с периодом ~460 px (`CHUNK×ZOOM`) — скан по горизонтали; эталон «до» — `tools/e2e/artifacts/seam-ab-calm.png` | 2026-09-23 |
| B-SURF-21 | Форма рельефа по биому (2026-09-23): силуэт из рецепта — пески асимметричные дюны (пологий наветренный, крутой срыв ≤ угла откоса), джунгли/лес холмы + русла, горы гребни/пики/пласты/осыпи; размах профиля заметно больше прежнего; биом без рецепта — прежняя форма (фолбэк); рябь (`viewOnly`) видна, но физики не касается | 2026-09-23 |
| B-SURF-22 | Снеговая линия (2026-09-23): снег шапками у вершин (не заливает склон); на теневой стороне снега больше, чем на освещённой; линия локальная (от местного максимума), не абсолютная | 2026-09-23 |
| B-SURF-23 | Бюджет высоты и рамка (2026-09-23): `relief.scale`/амплитуды не выводят профиль за растр чанка (свип по seed); «что твёрдо — то видно» (нет непрокрашенного твёрдого у низа и по краям), полос на стыках нет; физика движения не изменена | 2026-09-23 |
| B-SURF-24 | ЧК1 — данные литосферы (2026-09-24): все 12 биомов литосферы `view_source=catalog` (0 в фолбэке), `view_errors=0`, `view_warnings=0`, `palette.base == color`; обе копии справочника синхронны (тест `TestBiomeViewSeedSynced`) | 2026-09-24 |
| B-SURF-25 | ЧК1 — различимость 12 биомов (2026-09-24): замороженные кадры админ-чипом — 3 песчаных разведены между собой, 6 скальных разведены между собой, `грозовая_степь` не выглядит как пески (контактный лист `tools/e2e/artifacts/chk1-contact-sheet.png`) | 2026-09-24 |
| B-SURF-26 | ЧК1 — горизонт скальных/травяных (2026-09-24): у семейств `скальные пустоши` и `травяные` по 2 пояса горизонта (`parallax` 0.50/0.70), на кадре не одна тёмная полоса; сданные образцы ЧК0 вид не изменили | 2026-09-24 |
| B-SURF-27 | ЧК2 — данные биосферы (2026-09-25): 11 биомов биосферы `view_source=catalog` (0 фолбэков), `palette.base == color`, у 10 из 11 горизонт задан в дельте биома (у `луга_степи` — семейный) | 2026-09-25 |
| B-SURF-28 | ЧК3 — данные вулканизма (2026-09-25): все 7 биомов вулканизма `view_source=catalog` (0 фолбэков), `view_errors=0`, `view_warnings=0`, `palette.base == color`; у пресета `лавовые` 2 пояса горизонта; обе копии справочника синхронны | 2026-09-25 |
| B-SURF-29 | ЧК3 — различимость 7 биомов (2026-09-25): замороженные кадры — 7 вулканических биомов разведены силуэтом/палитрой/декором (контактный лист `tools/e2e/artifacts/chk3-contact-sheet.png`); у `венерианских_плоскогорий` свой дельтовый горизонт, снега/хвои нет | 2026-09-25 |
| B-SURF-30 | Расширение движка формы — ЧК3 (2026-09-25): `flow.lobes/slope/levees`, `spike.cluster`, `fan.w/roughness`, `crystal.glow` реально влияют на силуэт/вид (контрастные пары); у `fan` угол ≤ `angleMax`; `roughness` включается только при явном `w`; `crystal.glow:false` — матовый, без флага — светится; **сданные ЧК0/ЧК1/ЧК2 вид не изменили** | 2026-09-25 |
| B-SURF-31 | Размер корабля на высадке (2026-09-25): пакет отдаёт `ship_scale` (реестр кораблей, дефолт 12 ростов); корабль рисуется `ship_scale × рост игрока`; нет поля → прежние 72 px; корабль-декорация (сквозь проходишь), слои `decor → ship → creatures → player`; известная оговорка — при размере 12 на окнах ниже ~1250 px верх корабля обрезается (принято создателем) | 2026-09-25 |
| B-SURF-32 | ЧК4 — данные крио (2026-09-25): все 5 биомов крио (`ледники`, `мёрзлые_газы`, `азотно-ледяная_тундра`, `сухой_лёд`, `инеевые_рощи`) `view_source=catalog` (0 фолбэков), `view_errors=0`, `view_warnings=0`, `palette.base == color`; у пресета `ледяные` 2 пояса горизонта; обе копии справочника синхронны; `ImageGenVersion=v9` | 2026-09-25 |
| B-SURF-33 | ЧК4 — различимость 5 биомов (2026-09-25): замороженные кадры — 5 крио-биомов разведены силуэтом/палитрой/декором, ни один не выглядит как категорийный фолбэк (контактный лист `tools/e2e/artifacts/chk4-contact-sheet.png`); сданные ЧК0–ЧК3 вид не изменили | 2026-09-25 |
| B-SURF-34 | ЧК5 — данные экзотики (2026-09-25): все 10 биомов экзотики `view_source=catalog` (0 фолбэков), `view_errors=0`, `view_warnings=0`, `palette.base == color`; у пресета `экзотика` 2 пояса горизонта; обе копии справочника синхронны (`TestBiomeViewSeedSynced`), `view_primitives=32`; `ImageGenVersion=v10` | 2026-09-25 |
| B-SURF-35 | ЧК5 — различимость 10 биомов и новый декор (2026-09-25): контактный лист `tools/e2e/artifacts/chk5-contact-sheet.png` — 10 кадров разведены; новый примитив `crystal_tree` виден в `кристальных_рощах` и `карбидно-алмазных_зарослях`, детерминирован, на `life=false` не отбрасывается (минерал); декор на экран ≤~79 при ориентире ~600 (LOD в коде не реализован — ориентир, мерить замером); сданные ЧК0–ЧК4 вид не изменили | 2026-09-25 |
| B-SURF-36 | ЧК6.1 — вода: данные и инвариант «жидкость ≠ твёрдость» (2026-09-26): жидкость есть (`view.liquid`, explicit) у 10 биомов категории «вода» + `магмовый_океан` (лава), **нет** у `коралловые_рифы`/`инеевые_рощи`/`пещерный_мир_с_потолком`/`струнные_рощи`/`пружинная_тундра` (признак по **дельте**, не по пресету; `liquid_medium` сам не включает); `liquidAt ∩ solidAt' = ∅` («по воде не ходим», вода не внутри камня); `bedY` не включает ледяную корку; `underIce` — корка в `solidAt'`, полыньи детерминированы от `seed`; обе копии справочника синхронны; `ImageGenVersion=v11` | 2026-09-26 |
| B-SURF-37 | ЧК6.1 — дневной контактный лист 11 водных биомов (2026-09-26): админ-чип «День» (+ «Штиль» для подлёдных), контактный лист `tools/e2e/artifacts/qa6-water-contact-sheet.png`, лёд/полынья подписаны по геометрии (`crustTop`/`polynyaAt`/`liquidLevel`); `pageerror=0`. Примечания: видимая вода зависит от seed-рельефа планеты (биом инжектится, рельеф — у планеты); авто-туман скрывает воду на `подлёдных_океанах`; **вид (пена/полноводность/среды/лава) — косметика, отложена** (идея `2026-09-26_вода-косметика-вида.md`) | 2026-09-26 |
| B-SURF-38 | ЧК6.2 — плавание и погружение (2026-09-26): вода включается по `liquidAt` (легаси `waterY`/`inWater` нет); плавание ×0.5 ходьбы, спринт в воде выкл; нырок `down` (`ArrowDown`/`KeyS`), всплытие `jump`, плавучесть держит голову выше зеркала (клипы 0.2 и 2.5); опора на дно; обзор под водой — подкраска по голове (отдельно от подводной пелены декора); `underIce` — ходьба по корке, вход через полыньи, запирания под коркой нет (регресс S13, seed 777001); лава — вход/плавание без урона; спавн на сухой/ледовой колонке без 2D-форм (или зеркало-фолбэк), корабль на `spawnX`; `tools/surface-swim-check.mjs` 29/29, `surface-liquid-check` 34/34, `surface-field-check` 22/22, `surface-profile-check` 75/75, `surface-view-check` 44/44; e2e `surface-swim-check` 7/7, `surface-walk-check` 15/15, `surface-liquid-render-check` 6/6, `map-check` ALL PASS; 0 `pageerror` | B-SURF-39 | ЧК6.3 — лодка (2026-09-26, коммит `3f2020c`): новый модуль `surface_boat.js` (класс `Boat` + `drawBoat`), константы `BOAT_*`, ввод `KeyE`; посадка/выход идемпотентны (второй лодки нет), посадка вплавь и с пологого берега (`dx` ≤ `BOARD_REACH`, центральная колонка может быть сухой); движение по тому же `liquidLevel` (`y = liquidLevel − BOAT_HULL`, ×`BOAT_FACTOR`=1.6, спринт не ускоряет, снос `BOAT_DRAG`, мягкий стоп `vx=0`); нырять нельзя; проба твёрдого по ПОЛНОМУ силуэту (`X [x±17]`, `Y lv−34…lv+6`, `solidAt'`, вкл. лёд `underIce`) — в твёрдое не входит; запрет лавы и `underIce` (включая полыньи) — по режиму; выход в любом месте, `player.distance` не замирает (HUD); в лодке пульс-кольцо игрока гаснет (F2); спрайт `boat.png` (204×120, вода y=84) с кодовым фолбэком; `surface_player.js`/`surface_world.js` не менялись; `tools/surface-boat-check.mjs` 22/22; e2e `surface-boat-check` 10/10, `surface-walk` 15/15, `surface-swim` 7/7, `surface-liquid-render` 6/6, `map-check` ALL PASS; регрессы `surface-swim-check` 29/29, `surface-liquid-check` 34/34; 0 `pageerror` | 2026-09-26 |

### карта/фронт (Canvas-кластеры, масштаб, модалка, статика noCache)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-MAP-1 | `/map` на малом зуме грузит регионы (`/api/regions`), зум до систем распускает кластеры; max/min зум не роняет рендер | 2026-09-17 |
| B-MAP-2 | Панорамирование за загруженный край во время полёта догружает кластеры (`maybeReloadClusters`, без дёрганий и зависаний) | 2026-09-15 |
| B-MAP-3 | Модалка системы: открытие, вкладки; экстремальная планета (T=900 K) рендерится без NaN/Inf в цифрах | 2026-09-16 |
| B-MAP-4 | Правка JS/CSS без рестарта сервера видна после F5 (`noCache` на `/static/`, `/map`, `/admin`) — нет залипшего кэша ES-модулей | 2026-09-17 |
| B-MAP-5 | `/api/entities/search` с пустым/невалидным запросом → 400 или пустой результат; фронт не падает и не вешает вкладку | 2026-09-15 |
| B-MAP-6 | JS-модули фронта без SyntaxError: проверка синтаксиса по `web/static/js/**` (node --check или глазами по свежим правкам — пропущенные запятые/скобки); один упавший модуль роняет весь import-граф и UI | 2026-09-18 |
| B-MAP-7 | Мини-карта при wide-компаньоне (35b §8 п.5, требование создателя №3б; 51a): wide/кратная система — компаньон рисуется у края круга мини-карты (`clampToMini`, minimap.js), цвет по спектру, не перекрывает планеты (~внутри 54 px от центра); подпись расстояния на масштабе 120px НЕ выводится (нечитаема — решение §6.5 принято); в основной схеме модалки wide-компаньон НЕ рисуется (только миникарта, 51a). Проверено статически по layout.js/minimap.js/modal_render.js; живого браузера нет | 2026-09-16 |
| B-MAP-8 | Полёт (33a): кластеры подгружаются ВПЕРЁД по курсу, а не под кораблём; прибытие — планета цели на месте сразу, без «спавна» в центре экрана; после — панорама/зум работают | — |
| B-MAP-9 | Полёт со слежением (37a): ровно один draw() за кадр (FPS не падает вдвое) | — |
| B-MAP-10 | Полётный перезапрос кластеров (37a): не меняет зум, без скачка | — |
| B-MAP-11 | FPS-плашка у admin/skycomposer (28a) в левом нижнем углу: видна в покое, обновляется ~2 раза/сек, стабильна при полёте; у player ничего не рисуется | — |
| B-MAP-12 | Модалка системы без планет (34a): на канвасе только звезда без текста, в панели — «Нет планет» есть | — |
| B-MAP-13 | Внешний вид двойных (35a): модалка — multiple два компаньона +60°, binary один, single без изменений; карта — multiple две точки 30°/150°, кластеры без изменений | — |
| B-MAP-14 | Дашборд `/` (вкладки, 2026-09-18; строка «Счёт» и 6-я вкладка «💳 Счёт» — 2026-09-23): шапка «Ваш агент» всегда видима при переключении (в т.ч. строка «Счёт: … Cr»); 6 вкладок (🛸 Корабль / 🎨 Внешний вид / ⚙️ Графика / 📊 Статистика / 📖 Энциклопедия / 💳 Счёт) — показывается ровно одна панель; активная вкладка восстанавливается из localStorage `dashboardActiveTab` (дефолт «Корабль», fallback при невалидном id); селектор спрайта/цвета во вкладке «Внешний вид» сохраняет («✅ Иконка/Цвет сохранена»), 0 ошибок консоли | 2026-09-23 |
| B-MAP-15 | Дашборд → «⚙️ Графика» + карта (2026-09-22): дефолты чистого localStorage (вид «Спрайт», граница «туман», «Обычный вид»); выбор сохраняется и переживает F5; «Для слабых ПК» гасит эффекты и непрерывный кадр мерцания, не теряя ключи игрока; на `/map` переключателей графики в углу НЕТ (зум/центр/поиск на месте), выбранный вид и граница применяются; 0 ошибок консоли | 2026-09-22 |
| B-MAP-16 | Вид звёзд — ядро без колец (2026-09-22, коммиты `fa94d3a`/`1fe5b72`): на карте (зум появления названий, виды «Спрайт»/«Глаз») и в модалке системы звезда — одна яркая точка с мягким свечением, без вложенных колец/тёмной перемычки/резкого края диска; сильный зум — одно ядро (не два); палитра O…Y без колец, L/T/Y тусклые, но цветные; компаньоны — светящиеся звёзды, не крупнее родителя; модалка: hover-кольцо, планеты/орбиты читаемы; «Для слабых ПК» гасит кадр; 0 ошибок консоли, e2e `map-check.js` зелёный. Авто-прогоны: `tools/e2e/star-visuals-shot.js`, `star-perf.js`, `qa-star-target-modal.js`, `qa-star-lite.js`, `qa-star-hybrid-perf.js` | 2026-09-22 |
| B-MAP-17 | Переходы админка ↔ карта (ранее ошибочно B-MAP-15, 2026-09-19): в `/admin` клик по планете 🌍 в шапке → `/map` открывается без повторного логина (adminToken скопирован в token); на `/map` кнопка «⚙️ Админка` видна только admin/skycomposer и ведёт в `/admin` без повторного логина (token скопирован в adminToken); у player кнопка скрыта; соседние кнопки шапки (Дашборд/Контракты) работают; 0 pageerror, 0 тост-ошибок, 0 разлогинов | 2026-09-19 |

### деньги игрока (счёт: «Счёт: … Cr» в шапках дашборда и карты, вкладка «💳 Счёт»; `GET /me/money`)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-MONEY-1 | Дашборд `/` и `/map`: строка «Счёт: 10 000 Cr» в шапке (дашборд — последней; карта — перед «Текущий мир»); вкладка «💳 Счёт» показывает баланс и историю; у нового игрока — «Операций пока нет»; даты — игровым календарём (год +1000, UTC), не часами браузера; e2e `tools/e2e/map-check.js` и чек дашборда — 0 JS-ошибок/разлогинов | 2026-09-23 |
| B-MONEY-2 | Приватность: второй игрок видит только свой баланс (две живые сессии на одном сервере); чужих балансов нигде нет; 401/403 → редирект на `/login-page` (статика чистая, живые 2 сессии — за создателем) | — |
| B-MONEY-3 | Отказ `/me/money` (перехват/сеть): шапка показывает `—`, карточка «❌ Не удалось загрузить счёт», страница и карта живут (не падают) | — |

### админка (пароль, generate, clear, stats, compatibility, tests)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-ADM-1 | `/admin/*` API без JWT с ролью admin/skycomposer или с неверным → 401/403; с верным → 200 (роли, спека 99.2.14; актуализировано 2026-09-15) | 2026-09-16 |
| B-ADM-2 | `/admin/clear` при идущей генерации → 409 «cancel it first»; после clear пользователи сохраняются, `current_world_id` обнулён; повторный clear идемпотентен | 2026-09-15 |
| B-ADM-3 | `/admin/stats`: после clear → 0/0; после генерации счётчики совпадают с реальностью (кэш инвалидируется, `admin_universe.go:272-273, 565-566`) | 2026-09-16 |
| B-ADM-4 | `/admin/compatibility`: правка пары сохраняется, `/admin/compatibility/reset` возвращает дефолты, повторный reset не ломает матрицу | — |
| B-ADM-5 | `/admin/tests`: первый вызов гоняет `go test`; `?refresh=1` — перезапуск; вызов во время прогона ждёт и не вешает сервер; без исходников — честный ответ «не развёрнуты» | — |
| B-ADM-6 | «Очистить вселенную» при активном фоновом NPC-тике (в БД есть агенты) → очистка проходит без ошибки дедлока (`40P01`); при исчерпании повторов — понятная ошибка, данные целы (полный откат) | 2026-09-23 |

### аудит (движок Run[T], правила на сгенерированном мире)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-AUD-1 | `/admin/audit` на сгенерированном мире: 200, `TotalEntities > 0`, `IssuesByCode`/`IssuesBySeverity` корректны, `SampleIssues ≤ SampleLimit`, флаг `Truncated` честный | 2026-09-16 |
| B-AUD-2 | `/admin/audit` на пустой вселенной (после clear): 200, `TotalEntities = 0`, без ошибок (пустые views не падают, `engine.go:42-45`) | — |
| B-AUD-3 | Аудит «невозможной» планеты (T ≥ 450 K или T ≤ 100 K с поселением; R-модель 99.2.12/99.2.13 — жёстких нулей нет, но высокая убыль): правило severity=high отрабатывает, аудит не падает (`safeCheck` ловит паники) | 2026-09-14 |

### БД/репозиторий (миграции, идемпотентность, большие объёмы)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-DB-1 | Рестарт сервера на существующей БД: `schema_migrations` учтена, неприменённых миграций нет, старт без ошибок (повторно не выполняются) | 2026-09-17 |
| B-DB-2 | БД, накатанная вручную (нет `schema_migrations`, таблицы есть): baseline отмечает миграции применёнными без выполнения, сервер стартует (`migrations.go:44-51, 87-113`) | — |
| B-DB-3 | Большие объёмы (100k миров / сотни тысяч планет): `/worlds`, `/admin/stats`, `/api/worlds/{id}/planets` отвечают за разумное время без таймаутов; `/health` зелёный сразу после старта (карта грузится в фоне) | — |
| B-DB-4 | Повторный `/admin/generate-planets` на существующих планетах не дублирует: количество планет не удваивается (B11 — старые удаляются перед генерацией, `admin_universe.go:298-307`) | 2026-09-15 |
| B-DB-5 | Развёрнутый код = исходники: `zorion-server.exe` собран ПОСЛЕ последних правок затронутых `.go`, процесс запущен после сборки (иначе сервер крутит старую логику при «зелёных» исходниках — регрессия 14b 2026-09-14: бинарь 02:49 при правках mortality.go 06:04) | 2026-09-18 |
| B-DB-6 | Битый мир в player_flights (перегенерация): Restore НЕ восстанавливает полёт с удалённым from/to, строка удаляется, `/health` жив (97a) | 2026-09-18 |

### Арт-студия (локальный инструмент художника: cmd/art-studio, порт 8798, конфиги config/art/*.json)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-ART-1 | Семейства F1–F10 (59 рас) отдаются студией: `/races?fam=F1` → 3 водные расы (id 2/3/4), `/races?fam=F10` → 10 рас (id 51–60), `/ref?fam=*` → опции рас, неизвестный fam → пустой список без паники; морф-наборы (beast_forms/anthro_clothes/anthro_neg) у всех семейств; люди — отдельная вкладка «Люди» (F0) | 2026-09-17 |

### Студия товаров (часть игрового сервера: /studio + /studio/api/*, JWT admin/skycomposer; перенос A/B/C завершён 2026-09-20, cmd/goods-studio удалён)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-ST-1 | /studio доступен admin/skycomposer с общим adminToken (переход из админки «🧰 Студия товаров» без повторного входа); player — оверлей/403; протухший JWT — 401-сброс | 2026-09-20 |
| B-ST-2 | CRUD каталога: товары/ресурсы/категории, ресурсы без привилегий, удаление с `cleared_links > 0` (второе подтверждение), OR-фильтры справочника, пилюли тиров 0..8 всегда; состав и сложность — у рецепта (слоты/тир-оверрайд сняты 000055) | 2026-09-21 |
| B-ST-3 | Fill двухфазный «предложи → подтверди»: кнопка «заполнить комплектующие» → «ИИ думает» → попап «Предложения ИИ» (варианты: Создать/Пропустить/Изменить категорию, Заполнить/Пропустить; невалидная категория — дефолт родитель + пометка «не найдена») → «Применить выбранное» применяет только принятое, «Отмена» ничего не меняет; недоступность opencode → честная ошибка «ИИ недоступен»; повторный fill при активной генерации → 409 | 2026-09-20 |
| B-ST-4 | Apply-дропы между попапом и применением: исчезла ссылка / удалена категория / слот занят или сдвинут → пункт не применяется, причина — в полном отчёте попапа | 2026-09-20 |
| B-ST-5 | Вкладка «Ресурсы» админки читает БД: слой 20/покрытие рас/gaps (дискриминатор `props ? 'closes'`); правка layer-ресурса в студии (тир) видна в подвкладке без рестарта (С5 снят) | 2026-09-20 |
| B-ST-6 | Лестница «самая конкретная побеждает»: на уровне расы — расовая фабрика, иначе семейная, иначе универсальная; на уровне семейства расовые записи/слоты/типы НЕ видны; скрытая своя фабрика скрывает категорию у уровня, снятие флага возвращает; список справочника не «повисает» | 2026-09-21 |
| B-ST-7 | Согласование снято: у товаров/ресурсов/предметов нет кнопок статуса и фильтров «забаненные» (только удаление); у фабрики — обратимая галочка «скрытая»; уникальность имён держится независимо от скрытости; вес/объём товара — 1/1 по умолчанию | 2026-09-21 |
| B-ST-8 | Рецепт как сущность — инварианты: привязка рецепта чужой категории → 400; повтор привязки → 409; транзитивный цикл компонента → 409; смена категории товара И фабрики при привязках → 409 (без привязок — 200); старые роуты `/goods/{id}/slots*`, `/goods/{id}/tier` → 404 (000055, идея §7) | 2026-09-21 |
| B-ST-9 | Рецепт как сущность — состав и привязки: `POST /studio/api/recipes` + компоненты (`pos=max+1`, количество, allow_resource, очистка/удаление), `copy-universal` (added>0; повтор added:0/skipped>0; пустой источник `{0,0}`; не-конкретная/ресурсная фабрика → 400), 3 параллельные привязки — 200/409/409 без 500 | 2026-09-21 |
| B-ST-10 | Студия «Товары» под рецепты (UI): селекторы «Фабрика»/«Семейство» в шапке, граф замыкания выбранной фабрики (несвязный допустим) + «в наборе N», маркер «др. фабрика», попап «Состав рецепта»/«Сложность» (у ресурса поля нет)/«Доступен фабрикам», drag&drop привязки на канвас (без фабрики — тост-подсказка), пункт контекст-меню «привязать к фабрике», кнопка копирования набора в «Постройках»; граф/карточки без JS-ошибок | 2026-09-21 |
| B-ST-11 | Разделы построек (бывш. ветка «Производители»): расовость из фильтра шапки при создании типа через «+ тип» (как у узла-приглашения пустого слота) — «Семейство» → `race_family` без расы, «Раса» → раса + её семейство, «Универсальный» → без расовости; созданный на уровне семейства тип виден на своём уровне и не виден на «Универсальном» | 2026-09-21 |
| B-ST-12 | Попап «Описания ИИ»: «Отмена» при идущем прогоне — остановка, предложения сохранены (попап остаётся); «Отмена» при неактивном прогоне — отказ от набора (предложения и счётчики пусты, попап закрывается, после F5 не всплывает, отчёт «Набор предложений отброшен»); повторная отмена при простое — без 500 | 2026-09-21 |
| B-ST-13 | Разбор ответа ИИ: модель может рассуждать в том же `text`-part и цитировать шаблон промпта (валидный JSON) до настоящего ответа — прогон даёт предложение, а не «мусор в ответе ИИ» (фикс `extractJSON`, идея 2026-09-24; касается и описаний, и комплектующих) | 2026-09-24 |
| B-ST-14 | Сессии ИИ-помощника не копятся: после серии `Ask` число сессий (`GET /session?limit=5000`) не растёт линейно, `DELETE /session/{id}` → 200 (фикс `Client.deleteSession`, идея 2026-09-24) | 2026-09-24 |
| B-ST-15 | Разделы построек по видам (поле `producer_types.section`): ряд «Постройки» из 5 разделов вместо «Производители»; дерево фильтруется по разделу, уровни «Строения»/классы сняты; «Фабрики» — два корня («Фабрика» + «Автофабрика»); селект «Раздел» в карточке типа-корня и в «+ тип» (предзаполнен активным); переименование корня не уводит из раздела; служебный «Прочее» достижим и переживает F5; при «Товарах/Предметах» кнопки раздела не подсвечены; расовость/скрытость/справочник/drag работают | 2026-09-25 |
| B-ST-16 | Студия после дробления монолита на модули (подсказки для прогонов, 2026-09-25): попап товара открывается **двойным** кликом по карточке (не одинарным); id вкладок разделов построек — `#branchProdFactory` (не `#prodSectionFactory`); попап производителя/узла — общий `#detailPopup` (`#prodPopup` в разметке нет); `gs_unitScale` до первого клика = `null` (дефолт `billion` живёт в `web/static/js/studio/core.js`) | 2026-09-25 |

### Сторож зацикливания (инструмент команды: `.opencode/plugins/loop-guard.js`, сервис учёта `tools/agent-dash` на 8790)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-GUARD-1 | Критерий по файлам считает результат: 30 правок одного файла с меняющимся содержимым — напоминания НЕТ; возврат к уже виденному состоянию (5 раз) → напоминание «возвращался»; 60 правок одного файла → грубая страховка «переписан 60 раз» | 2026-09-21 |
| B-GUARD-2 | Память: старая форма сообщения (`role` + `tokens.cache.read`) и новая (`type` + `cacheRead`) дают одно и то же; сообщение с нулевыми токенами НЕ стирает известную память; ≥ 700k → напоминание, ≥ 900k → отказ (продлением не снимается) | 2026-09-21 |
| B-GUARD-3 | Повторы и команды: 15 прогонов одного семейства команд → напоминание; семейство считается по **телу команды без служебного префикса установки кодировки** (`$OutputEncoding = …` / `[Console]::OutputEncoding = …` / `chcp 65001`) — разные команды с общим префиксом серией не считаются, а повторы одного только префикса по-прежнему ловятся; 5 одинаковых вызовов с тем же результатом → отказ; 15 действий после напоминания → отказ; серия отказов → гашение сессии | 2026-09-21 |
| B-GUARD-4 | Продление лимитов: активная заявка `.opencode/loop-guard.override.json` → строка `extend` в журнале, сброс счётчика «после напоминания», объёмный предел `60 + extra`; истёкшая заявка, заявка без срока и битый файл — игнорируются, сторожа не роняют | 2026-09-21 |
| B-GUARD-5 | Продление НЕ отменяет ловлю возврата файла к виденному состоянию (спираль всё равно напоминает) | 2026-09-21 |
| B-GUARD-6 | Сервис учёта: `POST /api/extend` → 200 и валидная заявка (атомарная запись, `.tmp` не остаётся); пустая сессия и не-JSON → 400; тело > 4 КБ → 413; активные заявки видны в `/api/stats.extensions`, истёкшие отфильтрованы | 2026-09-21 |
| B-GUARD-7 | Страница учёта: в карточке активной сессии есть кнопка «продлить +15 / 30 мин», нажатие шлёт POST на `/api/extend`, при активной заявке видна плашка «продлено ещё N мин» (проверено по коду и разметке; живой клик в браузере — за @manager) | 2026-09-21 |

### пояса малых тел / добыча в поясе (мини-игра: вход, управление «два стика», бесконечный пояс; сцена `web/static/js/belt/*`)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-BELT-1 | e2e-смоук сцены: `cd tools/e2e; npm.cmd i; node belt-mining-check.js` — все пункты PASS (доворот носа к курсору, стрейф без разворота, плавная тяга, бурение по наведению, `Space`=ЛКМ, `ПКМ` подавлена, бесконечность вдоль кольца, рассеивание за краем, кап тел, детерминизм от seed, легенда); live-пункты требуют `QA_TOKEN`+`QA_BELT_ID` | 2026-09-23 |
| B-BELT-2 | Вход: карта → ПКМ по кольцу/строке пояса → «⛏ Добывать» → сцена грузится без `pageerror`; выработанный пояс сцену не открывает (`enter` → 400 «Пояс выработан») | 2026-09-23 |
| B-BELT-3 | Управление: W разгоняет плавно, отпустил — сброс; A/D стрейф без доворота носа; мышь доворачивает нос с ограниченной скоростью; Shift тормозит; Esc — пауза/продолжить | 2026-09-23 |
| B-BELT-4 | Добыча: нос на жилу + ЛКМ → добыча идёт (буфер/шкала растут), нос в пустоту или за радиусом → бурения нет; пробел = ЛКМ; ПКМ в сцене не открывает меню | 2026-09-23 |
| B-BELT-5 | Бесконечность: полёт вдоль кольца — тела не кончаются, память не растёт (≤ кап 120 тел + 60 пыли); уход поперёк за полосу — тел/декора/пыли нет, стенки и отскока нет, возврат — пояс снова | 2026-09-23 |
| B-BELT-6 | Выход из добычи («Вернуться» — кнопка и Esc→пауза): карта **с открытым попапом системы пояса** (бейдж «● Вы в поясе»); без метки попап сам не всплывает, после F5 не переоткрывается (`tools/e2e/belt-return-check.js`); повторный заход — тот же мир (детерминизм от seed) | 2026-09-23 |
| B-BELT-7 | Регресс трюма/запаса: добытое уходит в трюм (`player_cargo`), запас пояса списывается на ту же величину, шкала трюма = груз + буфер; полный трюм — добыча недоступна. ⚠️ стартовая ёмкость **50 т** (дельта трюма 2026-09-24), не 100 | 2026-09-24 |
| B-BELT-8 | Полный трюм при входе: клик «⛏ Добывать» **не уводит** с карты, ошибка показывается попапом поверх модалки системы, модалка остаётся; `POST /api/belt/mine/enter` не вызывается, `GET /me` до/после идентичен (стек попапов) | 2026-09-23 |
| B-BELT-9 | Спрайты в сцене: все крупные тела и обломки рисуются спрайтами (**фолбэк 0**), формы разные; реестр 16 камней / 3 обломка / 4 паттерна руды | 2026-09-23 |
| B-BELT-10 | Свет (rim-light): постоянное направление, **не вращается** вместе с телом, **обрезан по альфе** — ореола на фоне нет (`tools/e2e/belt-halo-check.js`) | 2026-09-23 |
| B-BELT-11 | Руда: светится у жил, гаснет по мере бурения; у выработанного пояса руды нет, камень темнее/обесцвечен | 2026-09-23 |
| B-BELT-12 | Фолбэк-инвариант: недоступные файлы ассетов → сцена не падает, рисуются многоугольники, `pageerror`=0 | 2026-09-23 |
| B-BELT-13 | Производительность сцены: полёт среди камней без просадки (60 fps, упор в vsync-кап headless) | 2026-09-23 |
| B-BELT-14 | Кольцо пояса на схеме: пояс рисуется **только россыпью камней** — без подложки-`annulus` и без обводки (кромок); ховер = подсветка камней, `cursor: pointer`; хит-тест/ПКМ по кольцу цел | 2026-09-23 |
| B-BELT-15 | Полёт к поясу (ПКМ по кольцу) = **место клика** (проекция на осевую линию), отличается от канонического; маркер «я здесь» по прибытии = цель полёта (без прыжка); ПКМ по **строке пояса** в списке «Объекты» — фолбэк: ближайшая к кораблю точка; после F5 — канонический фолбэк, маркер не исчезает (`tools/e2e/belt-nearest-check.js`) | 2026-09-23 |
| B-BELT-16 | Камни россыпи: разные размеры («якоря» + мелочь), сгустки с пустыми промежутками, часть вытянутых, светлый край со стороны звезды; **число камней от протяжённости** пояса (`beltStoneCount`: `<0.5 а.е.` → 6–10, `0.5–2` → 10–16, `2–8` → 16–22, `>8` → 22–28; «выработан» ×0.5, пол 2) — `tools/e2e/belt-stones-check.js` | 2026-09-23 |
| B-BELT-17 | Подпись вида пояса: `kuiper` показывается как «**пояс астероидов**» (собственное имя «пояс Койпера» у чужих звёзд не используется; решение создателя 2026-09-23) | 2026-09-23 |
| B-BELT-18 | Заход в пояс при `iron_remaining IS NULL` (после перегенерации галактики): заход **открывается**, запас инициализируется лениво (NULL → reserve), добыча идёт; пустой запас ≠ запрет (`tools/e2e/qa-belt-null-enter.js`) | 2026-09-23 |
| B-BELT-19 | Ледяные камни (второй ресурс, 2026-09-24): доля ледяных = лёд среди руды (`ice/(ice+iron)`); в поясе без льда ледяных камней нет и мир **как до фичи** (поток RNG не сдвинут); `belt-mining-check.js` пункты `ice-16…ice-22` PASS | 2026-09-24 |
| B-BELT-20 | Добыча по ресурсу: нос на **ледяной** камень → копится «Вода неочищенная» (`collect{resource:"ice"}`), на **железный** → железо; **без** поля `resource` (старый клиент) → железо (обратная совместимость) | 2026-09-24 |
| B-BELT-21 | Общий трюм двух ресурсов: буферы железа и воды суммируются в занятом; «трюм полон» — по сумме (`mined + mined_ice + granted ≤ free`); выход кладёт **оба** ресурса (`player_cargo` — две строки), запас льда списывается на ту же величину; запас льда не уходит в минус | 2026-09-24 |
| B-BELT-22 | Вход/выработанность: пояс с выработанным железом, но живым льдом — **входим**; «Пояс выработан» — только когда выработаны **все доступные** ресурсы; при отсутствии ресурса воды вход не даёт пустой заход (железо работает, не 500) | 2026-09-24 |
| B-BELT-23 | HUD/список: два буфера (железо и вода) + **одна** шкала трюма; чипы «Запас железа/льда» и «Пояс/Лёд»; при отсутствии ресурса воды ледяной UI **скрыт**; в строке пояса при знании — чип/строка льда, без знания — молчание | 2026-09-24 |

> ⚠️ **Следить (пересечение фич):** дельта трюма 2026-09-24 снижает стартовую ёмкость **100 → 50 т**
> (B-BELT-7). Тогда полный трюм льда наберётся за **~1,3 мин**, а не 2–3 — `rate_ice` потребует
> **пере-калибровки** под новый трюм (отдельный шаг, когда трюм 50 т сядет; см. идею по льду, «хвосты»).

### трюм / грузоподъёмность корабля (модуль хранения, универсальные слоты; дельта спеки трюма 2026-09-24, миграция `000080`)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-CARGO-1 | Ёмкость и слоты: стартовая ёмкость = **50 т** (`base_capacity` 20 + модуль «Грузовой модуль-1» 30); в разделе «Корабль» видны **три** универсальных слота (первый занят модулем с «+30 т», два пустых — «не установлен»); `GET /api/cargo` → `total=50`; нет JS/тост-ошибок (`tools/e2e/dashboard-cargo-check.js`) | 2026-09-24 |
| B-CARGO-2 | Миграция `000080`: у `cargo_1` `capacity=30`, у `starter` `slots.universal=3`; перегруженные трюмы **обрезаны** до новой ёмкости (на dev — `qa_cargo_124736` 95 → 50 т), `used ≤ total` у всех; повторный прогон идемпотентен | 2026-09-24 |

### слои интерфейса / стек попапов (реестр `web/static/js/ui/layers.js`, диалоги `ui/alert.js`, перевод попапов)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-UI-1 | e2e `layers-check.js` — все пункты PASS: алерт поверх модалки, Esc и клик-вне закрывают только верхний, видимый тост не съедает Esc и не гаснет, «призрачных» слоёв нет, дропдаун поиска над модалкой | 2026-09-23 |
| B-UI-2 | Регресс попапов: меню карты/модалки (в т.ч. ПКМ по кольцу пояса), тултип координат, поиск NPC и сущностей, баннер пакмана и оверлей «Галактика пуста» — поведение сохранено, `pageerror`=0 | 2026-09-23 |
| B-UI-3 | Node-безопасность: `go test ./web/ -run TestAdminFrontendLoadsInNode` PASS (реестр не затягивает `map/*` в граф админки) | 2026-09-23 |
| B-UI-4 | `z-index` вне реестра — только внутренние элементы попапа и хром `map.css` (`rg z-index` по `ui/`, `modal/`, `map/`, `search.js`) | 2026-09-23 |

### корабли рас (раса агента и игрока, расовый реестр; спека 2026-09-23, П1–П4) + удаление кораблей из игры (арт-студия, спека 2026-09-25)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-SHIP-1 | `GET /me.ship_options` = **N строк из `config/ships_registry.json`** (все записи файла + нейтральный последним; N меняется после удалений), у каждой поле `race`; `PUT /me/ship-icon`: свой расовый файл → 200, чужой расовый и `neutral.png` → 400; селектор дашборда — только пул расы игрока | 2026-09-23 |
| B-SHIP-2 | Агент рисуется **кораблём своей расы**: `GET /api/npc/positions` отдаёт `race_id`; разные расы → разные файлы; 3 расы без корабля (`cryo_sky`, `geysers`, `ice_plankton`) и неизвестная → нейтральный; неполные наборы вариантов в нейтральный не уходят. ⚠️ у `humans`/`ice_plankton` живых агентов нет (нет фракции/родного мира) — проверять функцией | 2026-09-23 |
| B-SHIP-3 | Легаси-21 выпилен: нет рантайм-ссылок на `ShipSprites`/`LegacyShipIconMap`/`DefaultShipIcon` и имена 21 PNG; на диске `neutral.png` + PNG на каждую расовую запись реестра (N−1); `/static/sprites/neutral.png` и `race_humans_starship.png` → 200, `boomerang.png`/`crescent.png`/`volcano.png` → 404 | 2026-09-23 |
| B-SHIP-4 | Легаси-значения `users.ship_icon` (`ship_strela.svg`, `crescent.png`) после миграции `000074` → `race_humans_starship.png`, без падений; регистрация/админ-создание дают валидный расовый корабль | 2026-09-23 |
| B-SHIP-5 | Контракт удаления корабля из игры (`POST /ships/ingame/delete?file=`): `GET` → 405; защищённые `neutral.png`/`race_humans_starship.png` → 409; обход пути (`..`, `%5C`) → 400; несуществующий → 404; успешное удаление снимает запись реестра + PNG игры + PNG accepted + запись `ships_meta.json`, повтор не ломается. **Неразрушающе — на песочнице**, боевой реестр/спрайты не трогать | 2026-09-25 |
| B-SHIP-6 | e2e витрины «В игре» (`tools/e2e/ships-ingame-check.js`): у каждой карточки кнопка «Удалить», в подтверждении имя+файл+«перезапуск», **отмена — no-op** (боевой корабль не удаляется), 0 JS-ошибок; шум `favicon.ico` 404 ошибкой не считается (маршрута у студии нет) | 2026-09-25 |

### локальный рынок планеты / магазин модулей (витрина, покупка, трейд-ин; спека 2026-09-24)

| Кейс | Проблема / что проверяем | Последняя проверка |
|------|--------------------------|--------------------|
| B-MARKET-1 | Витрина `GET /api/planets/{id}/market` — гейт знанием: режим `none` → 404 «Нет данных» (без `offers`/`can_trade`); знание + `settlements_count==0` → 200 `offers:[]`; знание + `>0` → 4 оффера по 3000 Cr с развёрнутым `item`; `can_trade` true только у роли player на орбите планеты (спутник — по родителю), surface/вне системы/админ → false | 2026-09-24 |
| B-MARKET-2 | Покупка `POST …/market/buy` — пустой слот: списана полная цена 3000; занятый: выкуп `floor(price/2)` из `market_offers` (`cargo_1` → 1500); отказы 403/409(закрыт)/404(оффер)/400(слот)/409(нет средств, баланс не изменён); `withdrawable=LEAST(...)`, журнал `kind='purchase'`, сумма сгорает (никому не зачислена) | 2026-09-24 |
| B-MARKET-3 | Эффект модуля из любого слота — `RadarRadius`/`HasScanner`/`HasEngine` читают `universal*`-слоты; два радара — максимум (не сумма); `EngineSpeed` в v1 — только из легаси-слота `engine` (право полёта — из любого) | 2026-09-24 |
| B-MARKET-4 | Клиент (e2e `tools/e2e/market-check.js`): вкладка «Магазин» в карточке планеты есть при знании о поселениях и нет при `none`/`settlements_count==0`; 4 карточки, цены с сервера; пересчёт выкупа на занятом/пустом слоте; кнопка заблокирована + подпись при `can_trade==false`; покупка обновляет ячейку; 0 pageErrors | 2026-09-24 |

## Актуализация

- Кейсы в чек-лист — только то, что Go-автотесты НЕ покрывают (сквозное, конкурентность, UI). Автотесты не дублируем.
- Кейсы зон добавляются в основном из найденных багов (регрессионные).
- Перевод кейса в Ядро — только по решению игрока, критерий: «поломка = нельзя играть / краш / потеря прогресса». Удаление из Ядра — тоже решение игрока.
- Новая подсистема/пакет → новая зона (добавляет @manager при маршрутизации).
- Владелец файла — @manager (63a): решает состав критичных точек, переносит даты из отчётов инстансов (@tester сюда не пишет).
- Полный прогон — после больших релизов / раз в 3–4 мелких релиза; при обычной задаче — затронутые диффом кейсы, списком от @manager.
- Отчёт инстанса @tester — файл `docs/QA/checklists/<дата>_<задача>.md` (журнал прогонов, растёт); строка «Предложение в чек-лист: …» — в этом отчёте, применяет @manager.
- В отчётах: «прогнал: <список кейсов>».