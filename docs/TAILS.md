# Реестр хвостов Zorion

> **Определение (создатель, 2026-09-23):** хвост — это то, что **не сделано в рамках релиза**
> или **не может быть сделано сейчас, но надо сделать**. Содержание ведёт `@manager`;
> **файл — горячий док: единственный писатель и git-владелец — `@dispatcher`** (как у
> `STATUS.md`/`CHANGELOG.md`) — менеджер передаёт дельту, диспетчер вносит.
> Читается на старте сессии, обновляется при закрытии задачи.

**Обновлено:** 2026-09-24

## Правила

1. **У хвоста обязан быть адрес** — кто и где его закроет. Нет адреса → это не хвост, а
   пожелание: идёт в `docs/gamedesign/ideas/` или `99_roadmap.md`.
2. **Хвост живёт в реестре, пока открыт.** Закрыл — строку убрать; след остаётся в идее и
   коммите. Реестр историю не копит (иначе протухнет, как любой список «на потом»).
3. **Границы (один факт — одно место):** баг продукта → `STATUS.md` §Баги; большая фича →
   `docs/EPICS.md`; занятость файла → `docs/COORDINATION.md`; критичная точка прогона →
   `docs/QA_CHECKLIST.md`. Здесь — только незакрытые куски **сданной** работы.
4. **Срока и приоритета нет** (решение создателя 2026-09-23: «всё будем разбирать»). Условие,
   при котором хвост закрывается, если оно есть, — прямо в колонке «Адрес».

## Открытые хвосты

| № | Хвост | Откуда | Адрес: кто и где закроет | Появился |
|---|---|---|---|---|
| 1 | Ловушка «игровые даты — только через модуль календаря (UTC, +1000), не `toLocaleString`» не внесена в общий список ловушек (в коде — есть, `7de1ca0`) | фича «присутствие на орбите и снимок», коммит `7de1ca0` | `@developer` — в `docs/PITFALLS.md`, когда файл освободится от бэклога (хвост 2) | 2026-09-23 |
| 3 | Ловушка «`sqlmock` не падает на неожидаемый запрос, если хендлер глотает ошибку (best-effort) — негативные проверки делать умышленно невыполненным ожиданием + `require.Error(ExpectationsWereMet)`» не внесена в список ловушек | этап Э2/Э3 фичи снимка | `@developer` — в `docs/PITFALLS.md` вместе с хвостом 1 | 2026-09-23 |
| 4 | Ловушка «`\|\|` (merge знания) сливает только верхний уровень: вложенный объект заменяется целиком» не внесена в список ловушек (в коде — есть, `7de1ca0`) | фича снимка, коммит `7de1ca0` | `@developer` — в `docs/PITFALLS.md` вместе с хвостом 1 | 2026-09-23 |
| 5 | Нет клиентского теста «население снимка не пересчитывается» (замечание `@reviewer`, тест T14 спеки) | фича снимка | `@developer` — добавить при следующем касании карточки/снимка | 2026-09-23 |
| 6 | Не проверено живьём: ветки производства и лог вымирания в карточке планеты (в dev-БД таблицы пусты) | прогон `@tester` 2026-09-23 | `@tester` — на планете с ветками/логом (или сгенерировать данные) | 2026-09-23 |
| 7 | Не проверен живьём рендер дат (+1000, UTC) в админке — нет учётной записи `admin`/`skycomposer` | прогон `@tester` 2026-09-23 | `@tester` — под админ-учёткой | 2026-09-23 |
| 8 | `docs/COORDINATION.md` не чистится: метки завершённых линий (слои интерфейса, спрайты поясов) остались; файл содержит незакоммиченные правки | работа 2026-09-23 | `@manager` — снять устаревшие метки при следующей задаче (сам файл не коммитим) | 2026-09-23 |
| 9 | Энциклопедия планет не знает тип «мини-нептун» (`web/static/js/planet_types.js`) — контент-гэп, спекой не требуется | фича «перелив массы в гигантов + мини-нептуны», коммит `eff69b8` | `@developer`/`@uidesigner` — при следующем касании энциклопедии | 2026-09-23 |
| 10 | Живой клик «Высадиться» по мини-нептуну в браузере не проверен — нужен мир с таким телом в радиусе радара (серверная половина покрыта тестом `TestSurfaceLandSurfacelessClass`) | фича «перелив массы в гигантов + мини-нептуны», коммит `eff69b8` | `@tester` | 2026-09-23 |
| 11 | Прыжок попапа студии при перетаскивании: drag-математика берёт `offsetLeft/offsetTop` (позиция раскладки `50%`), а не визуальные координаты — попап «прыгает» под курсором и может улететь за экран (тот же симптом «за границами») | студия товаров, прогон `@tester` 2026-09-24 (идея попапа товара) | `@developer` — считать позицию из `getBoundingClientRect()` при следующем касании `web/studio.html` | 2026-09-24 |
| 12 | Попап детали студии центрируется по всей ширине `#main`, а не по канвасу `#canvasWrap` (панель 320 px справа) — заходит на правую панель на ~233 px | студия товаров, прогон `@tester` 2026-09-24 | `@developer`/`@uidesigner` — центрирование по канвасу (цель центрирования — решение создателя) | 2026-09-24 |
| 13 | Флавор причины «Жажда» (`18_needs.md` §18.3.1) — текст «Вымерло · <дата> · Жажда»; код `thirst` уже внесён в `18b_settlement_log.md` | эпик «Снабжение форпоста водой», спека `2026-09-24-потребление-по-товарам-и-альтернативы` §8.3 | `@writer` — текст флавора в `18_needs.md` §18.3.1 | 2026-09-24 |
| 14 | Дельта канона решения 11 в спеке `2026-09-22-эффекты-снабжения-задержка-голод.md` §3.3/§4.2 («позиция закрывается любым товаром категории») устарела → «только своим товаром» | эпик «Снабжение форпоста водой», спека §15 | `@designer` — помета в спеке (или перенос в `_archive` при закрытии линии) | 2026-09-24 |
| 15 | Запас льда в поясе пока **плоский** 20 000 т (временно, «на тест»); правильная форма — зависимость от состава пояса (как у железа `reserve = 600·k_iron`) | фича «ледяные астероиды», коммит `96d71fb` | `@designer`/`@balancetester` — отдельная итерация чисел (спека `2026-09-24-ледяные-астероиды-вода.md` §18 п.1) | 2026-09-24 |
| 16 | Скорость льда (`rate_ice`) потребует **пере-калибровки** под стартовый трюм **50 т** (дельта трюма 2026-09-24 снизила 100 → 50): сейчас полный трюм льда ≈ 1,3 мин, а не 2–3 | пересечение фич «ледяные астероиды» + «дельта трюма 2026-09-24» | `@designer`/`@balancetester` — когда трюм 50 т сядет | 2026-09-24 |
| 17 | Ресурс «Вода неочищенная» (`goods.id 426`, `kind='resource'`, `source='manual'`) не воспроизводится из миграций/сидера — **на проде завести вручную** (иначе добычи воды там не будет, сработает фолбэк) | фича «ледяные астероиды», коммит `96d71fb` | создатель/студия — данные каталога на проде | 2026-09-24 |
| 18 | Слабые арт-формы ледяных камней `ice_02/03/07` (плоские/«стеклянные») — кандидаты на перегенерацию | фича «ледяные астероиды», коммит `96d71fb` | `@artist`/`@gdesigner` — по слову создателя | 2026-09-24 |
| 19 | Живой браузерный прогон сцены добычи (live-шаг e2e) не делался — нужен `QA_TOKEN` + пояс в dev-БД; поведение закрыто прогоном по API | прогон `@tester` 2026-09-24 | `@tester` — с токеном и поясом | 2026-09-24 |
| 20 | Ловушка «тесты, зовущие внешний интерпретатор/утилиту, должны проверять ЗАВИСИМОСТИ, а не наличие команды: на ubuntu-latest python есть (python-is-python3), а Pillow/numpy нет → тест падал через t.Fatalf вместо skip» не внесена в список ловушек (в коде — есть, комментарий `findPython`) | фикс красноты CI, коммиты `9d0e501`/`ca8fdcb` | `@developer` — в `docs/PITFALLS.md` вместе с хвостами 1/3/4, когда файл освободится | 2026-09-24 |
| 21 | Ловушка «`tools/db.ps1 -Sql` теряет двойные кавычки в JSON-литералах» — в e2e использовать `jsonb_build_object(...)` | фича «магазин модулей», коммит `a333827` | `@developer` — перенос в `docs/PITFALLS.md`, когда файл освободится (хвост 2) | 2026-09-24 |
| 22 | Калибровка цены базового модуля витрины: гипотеза **3 000 Cr**, эталон не задан, рабочий интервал (2 500; 4 175] | фича «магазин модулей», коммит `a333827` (вердикт @balancetester В1) | `@balancetester` — прогон после накопления данных экономики | 2026-09-24 |
| 23 | Админка без карты: `scheduleInterstellarArrivalFallback` (обновление модалки системы по прибытии межзвёздного полёта) живьём не проверен — покрыт чтением кода | баг «модалка целевого мира после прилёта», коммит `bd07cd7` | `@tester` — при следующем касании админки | 2026-09-24 |
| 24 | Композитный маршрут: снятие устаревшего признака межзвёздного полёта по прибытии живьём не гонялся (юнит + заглушка) | тот же баг, коммит `bd07cd7` | `@tester` | 2026-09-24 |
| 25 | Ловушка «модалка, открытая во время межзвёздного полёта, не обновлялась по прибытии (`my_position = null` — авто-обновление видит только `in_flight`); «своя система» не выводить из `my_position != null`» не внесена в `docs/PITFALLS.md` (файл занят бэклогом — хвост 2) | тот же баг, коммит `bd07cd7` | `@developer` — в `docs/PITFALLS.md` вместе с хвостами 1/3/4/20/21, когда файл освободится | 2026-09-24 |
| 33 | ЧК2 — контракты: владелец-поселение как заказчик/плательщик + зачёт привезённого (цикл снабжения форпоста водой) | эпик «Постройка структур на планете», спека `2026-09-24-постройка-структур-на-планете.md` §17 | `@manager`/`@designer` — следующая сессия | 2026-09-24 |
| 34 | Дельты обычных доков по ЧК1: `docs/DB.md` (миграция 000082), `docs/ARCHITECTURE.md` §2 (карта `admin_structures*.go`, `modal/{build,structures}.js`), спека контрактов §5.2, `13_settlements.md` §13.2, `GLOSSARY.md` §1 (омоним `owner_type`) | эпик «Постройка структур на планете», коммит `a217171` | владельцы доков (`@designer`/`@dispatcher`) | 2026-09-24 |
