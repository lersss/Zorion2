# 03. Планеты

**Обновлено:** 2026-09-15

---

## 3.1. Количество и распределение

Количество планет зависит от типа звезды и задаётся **средним числом (mean)**
конфигом генерации звёзд (эталон менеджера §4и.3, вердикт астронома §4е идеи
29a; спека `99.2.4-exotic-stars.md` §5.2):

| Тип | mean (планет) | Комментарий |
|---|---|---|
| O, B | 0.3 | |
| A | 1 | |
| F, G | 1.75 | |
| K | 2 | |
| M | 2.5 | |
| L, T, Y | 1 | |
| ЧД | 0.1 | далёкие каменистые (орбиты 10–12); максимум 1 |
| Нейтронная | 0.05 | пульсарные планеты редки; максимум 1 |
| WD | 0.3 | выжившие далёкие (орбиты 5–8) + обломочный пояс; максимум 1 |
| Протозвезда | 0 | протопланетный диск вместо планет |
| Двойная широкая | ×0.9 к счёту главной | S-тип планет |
| Двойная тесная | mean 0.2 | P-тип |
| Кратная (3+) | ×0.9 к счёту главной | wide-семантика, отдельной ветки нет |
| Прочая экзотика (сверхгиганты O–B–A) | 0.1 | |

**Механика:** `n = floor(mean) + Бернулли(frac)`, потолок 8. Средние
редактируются в админке (генератор звёзд, спека `99.2.3-generation-tuning.md`
§4.3); здесь — дефолты физического реализма. Экзотические планеты (ЧД/НЗ/WD)
генерируются отдельной веткой без архетипной рулетки — спека `99.2.4` §5.3–6.
> Примечание: диапазоны «0–2»/«0–3» у остатков (вилка астронома, идея 29a §4е)
> — наблюдаемое, не mean: при mean < 1 генератор выдаёт максимум 1 планету
> («чаще 0» сохраняется).

---

## 3.2. Архетипы планет

Конструктор из 5 категорий черт:

- **Архетип:** жаркий, умеренный, холодный, экстремальный, изменчивый.
- **Поверхность:** базис для композиции (см. `04_composition.md`).
- **Гидросфера:** океаны, озёра, ледяной покров, подлёдная, сухая, кислотная.
- **Атмосфера:** азотно-кислородная, углекислая, плотная, разреженная, ядовитая, облачная, туманная, электрическая, парниковая, водородно-гелиевая, гелиевая, азотная, водородная, метановая.
- **Биосфера:** стерильная, микробная, растительная, грибная, симбиотическая, светящаяся.

~500 комбинаций. Загружается из `config/planet_archetypes.json`.

**Баланс биосферы (2026-09-11):** в умеренном архетипе биосферные формы
подняты (леса 0.20, луга 0.16, джунгли 0.12, болота 0.07; океаны/скалы
снижены до 0.13), чтобы леса могли доминировать. Чтобы луга/джунгли/болота
тоже встречались, добавлены **биосферные ниши** (`composition_modifiers.go`):
джунгли — тёплые влажные (вода > 60%, T > 300), болота — обильная вода при
умеренном тепле (вода > 55%, T 270–330), луга — умеренная вода (20–45%).
Вне ниш леса — дефолтный биом. Итог: все 4 биосферные формы доминируют
(~4% планет).

**Поля архетипа:**
- `base_surface` — базовые веса форм поверхности.
- `base_subterrain` — базовые веса типов недр.
- `allowed_hydrospheres` / `allowed_atmospheres` / `allowed_biospheres`.
- `temperature_min/max` — диапазон температуры.
- `water_chance` / `life_chance` — вероятности.
- `mass_min/max` — диапазон массы в земных единицах.

---

## 3.3. Газовые гиганты

Для горячих звёзд (O, B, A) на дальних орбитах (index ≥ 3) с шансом 80%.

**Свойства:**
- H-He атмосфера (водородно-гелиевая, водородная, гелиевая).
- Масса **15.9–4131 M⊕ (0.05–13 MJ)**, распределение **логнормальное** (медиана 1 MJ = 317.8 M⊕, σ = 0.9 декады, усечение [15.9, 4131] пересэмплингом; фактические доли после усечения: ~22% > 1000 M⊕, ~26% в 15.9–100 M⊕ — лёгкие гиганты ~четверть популяции, остальные ~100–1000 M⊕). Верх = физический предел газового гиганта: 13 масс Юпитера (4131 = 13×317.8, округлено вниз) — выше коричневый карлик (13–80 MJ), звезда от ~80 MJ.
- Радиус по **честной кривой M→R с насыщением** (электронное вырождение держит радиус ~1 RJ от ~1 MJ до ~80 MJ); наивная формула `(M/ρ)^(1/3)` для гигантов **не используется** (при 13 MJ она дала бы ~27 R⊕ и ~6.4 g — физически абсурдно). Кривая — двухсегментная степенная через опорные точки создателя (0.05 MJ → 5.9 R⊕; 1 MJ → 11.2 R⊕; 13 MJ → 11.2 R⊕):

  ```
  M1=15.9, R1=5.9;  M2=317.8, R2=11.2
  α1 = ln(R1/R2)/ln(M1/M2) ≈ 0.214
  R(M) = 11.2 × (M/317.8)^0.214   для M1 ≤ M < M2
  R(M) = 11.2                     для M ≥ M2 (плато насыщения)
  ```

  Константы (единый источник для генератора и реестра полей): `GasGiantMassMin = 15.9`, `GasGiantMassMax = 4131`, `GasGiantMassRef = 317.8`, `GasGiantRadiusMin = 5.9`, `GasGiantRadiusMax = 11.2`.
- **Плотность и гравитация — производные**: `ρ = M/R³` (0.077–2.94 в единицах Земли; у 13 MJ = 2.94), `g = M/R²` (0.46–32.9 g; у 13 MJ = 32.9 g, у Юпитера 2.53 g).
- Множество спутников (3–10).
- Ресурсы: энергия, редкие элементы, газы.
- Нет композиции поверхности — только атмосфера.

**Спутники** — полноценные локации (см. `04_composition.md`).

---

## 3.4. Масса и размер

**Масса — первичный параметр.** Задаётся архетипом.

| Тип планеты | Масса (M⊕) |
|-------------|-----------|
| Малая (Марс, Луна) | 0.1–0.5 |
| Землеподобная | 0.5–3 |
| Суперземля | 3–8 |
| Газовый гигант | 15.9–4131 (0.05–13 MJ) |

**Размер вычисляется через плотность:**

```
R = (M / ρ)^(1/3)
```

где ρ — плотность в единицах Земли:

| Состав | Плотность |
|--------|-----------|
| Газовые гиганты | 0.077–2.94 (ρ = M/R³, производная от кривой §3.3) |
| Ледяные | 0.5–0.8 |
| Океанические | 0.8–1.0 |
| Скалистые | 0.9–1.3 |
| Металлические | 1.2–1.6 |

**Исключение:** для газовых гигантов `R = (M/ρ)^(1/3)` **не применяется** — радиус берётся по кривой насыщения §3.3, а плотность/гравитация выводятся из него.

**Проверка:** Земля (M=1, ρ=1) → R=1. Юпитер (M=318, ρ=0.24) → R≈11. Гигант 13 MJ (M=4131) → R=11.2, ρ=2.94, g=32.9.

---

## 3.5. Ядро планеты

**Ядро — первичная сущность**, определяющая температуру поверхности, магнитное поле, геологическую активность.

**Параметры:**

| Поле | Диапазон | Что значит |
|------|----------|-----------|
| Type | металлическое / силикатное / ледяное / экзотическое | Состав |
| MassPercent | 5–60% | Доля от массы планеты |
| Activity | 0–100 | Жидкое ядро, конвекция |
| Radioactivity | 0–100 | U, Th, K |
| Age | 0.1–13 млрд лет | Возраст (общий с системой) |

**Генерация:**
- **Type** — от массы: M>5 → металлическое, 0.5–5 → металлическое/силикатное, <0.5 → силикатное/ледяное/экзотика.
- **MassPercent** — от массы: 25–40% для землеподобных, 20–40% для газовых, 5–25% для малых.
- **Activity** — от архетипа + доли магматических камер в недрах.
- **Radioactivity** — от доли радиоактивных зон в недрах.
- **Age** — один на систему, зависит от спектра звезды.

**Вклад в температуру:**

```
base_heat  = Activity × 1.5 + Radioactivity × 2.5   (0–400 K)
age_factor = max(0.15, 1.0 - Age/15)
mass_factor = MassPercent / 30
HeatContribution = base_heat × age_factor × mass_factor
```

**Ожидаемое распределение типов:**
- металлическое ~55%
- силикатное ~30%
- ледяное ~12%
- экзотическое ~3%

---

## 3.6. Физическая температура

### 3.6.1. Равновесная температура

```
T_eq = 278.7 × L^0.25 / sqrt(r)
```

где:
- `L` — светимость звезды (в солнечных единицах).
- `r` — орбитальный радиус: `0.4 × 1.7^orbitIndex` (в а.е.).
- `278.7` — солнечная постоянная (равновесная T Земли без атмосферы).

### 3.6.2. Корректировки

```
T_albedo = T_eq × (1 - albedo)^0.25
T_green  = T_albedo × greenhouse_factor
T_final  = T_green + internal_heat + tidal_heat
```

где:
- **albedo** — от доминирующей формы поверхности (лёд 0.65, скалы 0.15, океаны 0.06).
- **greenhouse_factor** — от типа атмосферы (разреженная 1.0, азотно-кислородная 1.1, углекислая 1.3, парниковая 2.5).
- **internal_heat** — вклад ядра (см. 3.5).
- **tidal_heat** — приливный нагрев (только для спутников).

### 3.6.3. Ограничения

- Clamp по диапазону архетипа (`temperature_min/max`).
- Абсолютные границы: [20, 2500] K.

### 3.6.4. Ожидаемое распределение

- Медиана: 300 K (+27 °C).
- Обитаемая доля (для людей): ~6% (оценка 5–8%) — честный каскад 99.2.20 (идея 45a), балансируется числом звёзд; прежняя домодельная «жилая зона 200–400 K: 40–45%» неактуальна.

---

## 3.7. Вода и жизнь

**Важно (2026-09-17):** §3.7 описывает **пригодность для людей/водной биосферы** — человеческий базис (функция `races.HumansSuitable`). После введения рас (решение создателя 2026-09-16, спека 99.2.21) жизнь в игре **не сводится к воде**: 60 рас (включая роботов 51–60) существуют вне водного окна, свои окна пригодности — в карточках `config/races.json` (`Race.Suitable`). Полный справочник — `22_races.md` §1. «Обитаемость» здесь = «обитаемость для людей».

**Вода:**
- Зависит от температуры, гидросферы архетипа, случайного шанса.
- Конкретные гидросферы (`океаны`, `озёра`, `ледяной покров`, `подлёдная`) переопределяют значение.

**Жизнь** (человеческий базис):
- Только при наличии воды (>10%), T 200–400 K и неядовитой атмосфере.
- Вероятность определяется биосферой архетипа (`life_chance`).

**Обитаемость** (для людей):
- Жизнь + вода >10% + T 200–350 K + атмосфера ≠ ядовитая.

---

## 3.8. Геймдизайнерские типы планет

Классификация по **композиции**, а не по одному `type`. Порядок проверок — от специфичных к общим.

| Тип | Условия |
|-----|---------|
| Газовый гигант | is_gas_giant |
| Радиоактивная | radioactive = true |
| Землеподобная | habitable + life + есть скалы + есть океаны/озёра |
| Океаническая | dominant == океаны + water > 60 |
| Ледяная | dominant == ледники, или T < 200 |
| Вулканическая | лава + вулканические поля ≥ 20% |
| Пустынная | пески ≥ 25% + water < 25 |
| Стеклянная | стеклянные поля ≥ 15% |
| Металлическая | металлические поля ≥ 15% |
| Органик | доминируют биосферные формы |
| Скалистая (базовая) | fallback |

**Никакой «не определена»** — fallback есть всегда.

---

## 3.9. Композиция планеты (сводка)

Помимо архетипа, каждая планета получает две независимые композиции:

- **Поверхность** — 16 форм с процентами.
- **Недра** — 17 типов с процентами.

Подробности — в `04_composition.md`.

Газовые гиганты — исключение: у них нет композиции поверхности, но есть спутники.

---

## 3.10. Проверка классификации на реальных планетах

Сверка геймдизайнерских типов с известными планетами (при запущенной генерации):

| Планета | Тип в игре | Подобных в галактике |
|---|---|---|
| Меркурий | пустынная | 86 |
| Венера | вулканическая | 516 |
| Земля | землеподобная | 1 624 |
| Марс | пустынная | 1 431 |
| Юпитер | газовый гигант | 532 |
| Сатурн | газовый гигант | 292 |
| Уран | газовый гигант | 105 |
| Нептун | газовый гигант | 0 |

Открытые вопросы классификации:

- **Маленькие планеты (0.1–0.5 M⊕) редки.**
- **Холодных ледяных гигантов нет.**
- **Кольца — только визуальные.**
- **Горячий сухой мир классифицируется как «пустынная».**

---

## 3.11. Реестр полей `planet.data` (инвентарь)

Единый источник правды для правил генерации поселений (пресетов) —
реестр `internal/generator/settlement/fields.go`. Его отдаёт админке
`GET /admin/settlement-fields`. Этот раздел — инвентарь того, что уже
есть в `planet.data`, чтобы не изобретать велосипед. Один факт — одно место:
кто-нибудь может начать дизайн «полей не хватает» — сверяйся сюда.

**Легенда:** ✅ — открыто в реестре пресетов и можно фильтровать;
◽ — ключ есть в `planet.data`, но в реестре не открыт (могут использовать
пресеты с доработкой реестра); ➖ — в данных и реестре нет (новая механика).

### Числа (number)

| Поле | Диапазон в данных | Статус |
|------|-------------------|--------|
| `temperature` | 20–2500 K (−253…2227 °C) | ✅ |
| `water_percent` | 0–100 % | ✅ |
| `mass` | 0.1–4131 M⊕ | ✅ (обновлено 2026-09-15, было 0.1–300) |
| `size` | 0.4–11.2 R⊕ | ✅ (обновлено 2026-09-15, было 0.43–12.6) |
| `gravity` | 0.29–32.9 g (M/R²) | ✅ (обновлено 2026-09-15, было 0.29–3.18) |
| `density` | 0.077–2.94 (в ед. Земли, ρ⊕) | ✅ (обновлено 2026-09-15, было 0.15–1.8; единица `g/cm³` в реестре исправлена на ρ⊕) |
| `moons` | 0–10 (гиганты 3–10, стандартные ≤ 4) | ✅ |
| `development_level` | 0–1 (только у обитаемых) | ✅ |
| `system_age` | 0.1–13 млрд лет | ✅ |
| `orbit_radius_au` | фактический радиус орбиты (S: `orbitRadiusByIndex(i)` 0.4×1.7^idx; P: `3 × companion_sep_au`), а.е. | ◽ (35b/99.2.18) |

### Строки (string)

| Поле | Значения | Статус |
|------|----------|--------|
| `atmosphere` | 14 типов | ✅ |
| `biosphere` | 6 типов | ✅ |
| `hydrosphere` | 6 типов | ✅ |
| `type` | 11 геймдизайнерских типов | ✅ |
| `surface_dominant` | 16 форм + `газовый_гигант` | ✅ (открыто 2026-09-13) |
| `archetype` | жаркий/умеренный/холодный/экстремальный/изменчивый | ✅ |
| `political_system` | демократия/диктатура/…/нет | ◽ |
| `description` | текст описания | ◽ (в правилах не нужен) |
| `orbit_center` | `main` (вокруг главной, S) / `barycenter` (вокруг барицентра, P) | ◽ (35b/99.2.18) |

### Булевы (bool)

| Поле | Статус |
|------|--------|
| `life` | ✅ |
| `radioactive` | ✅ |
| `is_gas_giant` | ✅ (запасной ключ; выводится и из `surface_dominant`) |

### Объекты/вложенные (не фильтруются правилами напрямую)

| Поле | Что внутри | Статус |
|------|-----------|--------|
| `surface_composition` | веса 16 форм | ◽ |
| `subterrain_composition` | веса 17 типов недр | ◽ |
| `core` | type, mass_percent, activity, radioactivity, age, is_active, is_metallic | ◽ |
| `resources` | сводка ресурсов | ◽ |
| `satellites` | спутники газовых гигантов (у каждого тоже `gravity`) | ◽ (уровень спутников) |

### Уже есть в физике, но не в данных (пересчитать можно)

Эти величины вычисляются в `internal/generator/planet/`, но в `planet.data`
не записываются. Если понадобятся пресету — материализовать по образцу
`gravity` (2026-09-13), не пересоздавая с нуля:

- `albedo` — `computeAlbedo` (0.05–0.7 по доминирующей форме).
- `greenhouse` — `computeGreenhouse` (множитель парника по атмосфере, до 2.5).
- `t_eq` — равновесная температура от звезды (без альбедо/парника).
- `core_heat` — вклад ядра в K (`Core.HeatContribution()`).
- `orbit_index` / радиус орбиты — живут в `PlanetData.OrbitIndex`, в JSON нет.

### В модели нет вовсе (потребуют новых механик)

`orbital_period` (год), `rotation/day_length`, `axial_tilt`, `eccentricity`,
`atmospheric_pressure`, `escape_velocity`, `magnetic_field` (есть только
косвенно `core.is_active`).

---

## 3.12. Эталоны (FAQ)

> Эталоны создателя по планетам фиксируются здесь («один факт — одно место»;
> перенесено из идеи `23a` 2026-09-15). Открытые вопросы эталонов считаются в
> `docs/gamedesign/faq_counter.md`. Дизайн по эталонам — спека
> `docs/specs/99.2.15-honest-gas-giants.md`.

**Вопрос:** какая максимальная масса газового гиганта?
**Ответ (эталон создателя, 2026-09-15):** «в реальном мире Юпитер 317.8 M⊕; максимальный гипотетический газовый гигант (не маленькая звезда) ≈ 13 масс Юпитера ≈ ~4100 M⊕; дальше коричневый карлик (13–80 MJ), звезда от ~80 MJ».

**Вопрос:** какой радиус у тяжёлых газовых гигантов?
**Ответ (эталон создателя, 2026-09-15):** «радиус тяжёлых гигантов насыщается ~11–12 R⊕ (электронное вырождение держит радиус ~1 RJ от ~1 MJ до ~80 MJ); наивная формула (M/ρ)^(1/3) отклонена — при 13 MJ она дала бы ~27 R⊕ и ~6.4 g, физически абсурдно».

**Вопрос:** какая плотность у гиганта 13 MJ?
**Ответ (эталон создателя, 2026-09-15):** «плотность растёт с массой, у 13 MJ ~2.9 в единицах Земли».

**Вопрос:** какие опорные точки кривой масса→радиус у гигантов?
**Ответ (эталон создателя, 2026-09-15):** «M~1 MJ → R~11.2 R⊕; M~13 MJ → R~10–11 R⊕; M~0.05 MJ → R~5.9 R⊕».

**Вопрос:** какая гравитация у тяжёлого газового гиганта?
**Ответ (эталон создателя, 2026-09-15, исправлен):** «гравитация M/R² до ~2.9 g» — **эталон исправлен** по итогам конфликта с инвариантом `g = M/R²` (при M ≈ 4131 M⊕ и R ≈ 11 R⊕ формула даёт ~33 g; для 2.9 g нужен R ≈ 38 R⊕ — «абсурдный» размер, отклонённый ранее; «2.9» — смешение с плотностью 2.94 ρ⊕). Итог: **честная физика — g = M/R², у 13 MJ ≈ 32.9 g**; потолков/клампов нет.

**Вопрос:** какая нижняя граница массы газового гиганта?
**Ответ (эталон создателя, 2026-09-15):** 0.05 MJ ≈ 15.9 M⊕ — полный физический диапазон газовых/ледяных гигантов; диапазон генератора 15.9…4131 M⊕.

**Вопрос:** какое распределение масс внутри диапазона гигантов?
**Ответ (эталон создателя, 2026-09-15):** логнормальное — медиана ~1 MJ (317.8 M⊕), σ = 0.9 декады, усечение [15.9, 4131] пересэмплингом; горб у «юпитеров», хвост до 13 MJ; фактические доли после усечения: ~22% > 1000 M⊕, ~51% в 100–1000 M⊕, ~26% в 15.9–100 M⊕ (лёгкие гиганты — ~четверть популяции). Лог-равномерное не выбрано.

---

*Обновлено 2026-09-15.*