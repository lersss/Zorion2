# 99. Дорожная карта и бэклог

**Обновлено:** 2026-09-12

> **Роль документа:** долгосрочный бэклог и открытые вопросы дизайна.
> **Краткосрочный статус** (что сделано, что сейчас, что дальше) — в `STATUS.md`.
> **История изменений** — в `CHANGELOG.md`.

---

## 99.1. Дорожная карта (крупные вехи)

| # | Блок | Статус |
|---|------|--------|
| 1 | Генерация вселенной (Пуассон + кластеры, спектры) | ✅ Сделано |
| 2 | Композиция поверхности и недр (16 форм, 17 типов) | ✅ Сделано |
| 3 | Ресурсы (6 категорий, маппинг форм) | ✅ Сделано (по старой модели) |
| 4 | Ядро и физика температуры | ✅ Сделано |
| 5 | Карточка планеты (UI) | ✅ Сделано |
| 6 | Аудит планет (43 правила) | ✅ Сделано |
| 7 | Библиотека аномалий (22 файла) | ✅ Сделано |
| 8 | **Описания планет** (11 типов, ~700 текстов) | ✅ Сделано |
| 9 | **Серверная кластеризация карты** (100k миров) | ✅ Сделано |
| 10 | **Аномалии как контент** (Go + API + фронт) | 🔄 Следующий блок |
| 11 | **Переход на новую ресурсную модель** | 📅 Планируется (§99.2.5) |
| 12 | **Поселения и труд** | 📅 Планируется (§99.2.6) |
| 13 | **Контракты и перелёт** | 📅 Планируется (§99.2.7) |
| 14 | **Автоматические заводы и присутствие** | 📅 Планируется (§99.2.8) |
| 15 | Экономика (энергия, рынки, производство) | 📅 Планируется |
| 16 | Фракции (генерация, войны, контракты) | 📅 Планируется |
| 17 | LLM-генерация описаний | 📅 Планируется |
| 18 | Тонкая настройка генерации (константы в админку) | 📅 Планируется |
| 19 | **Экзотические типы звёзд** (нейтронные, квазары и др.) | 📅 Планируется |

**Порядок изменён 2026-09-12.** Блоки 11–14 выделены из общего «Экономика»,
потому что они **независимы** и каждый проверяем тестами. Экономика (энергия,
рынки) идёт после них: она опирается на поселения и контракты, а не наоборот.

---

## 99.2. Крупные блоки в разработке

### 99.2.1. Аномалии как контент

**Статус:** 🔄 следующий блок

Редкие «нарушения» физики — не баги, а фичи мира. Библиотека **готова** (22 JSON-файла
в `config/anomalies/`). Осталось превратить её в рабочий контент:

1. **Go-код:** чтение `config/anomalies/` при старте, кеш в памяти под `RWMutex`.
   Ошибка чтения JSON — `log.Fatal`, а не старт с полупустым кешем.
2. **API:** эндпоинт, отдающий тексты фронту.
3. **Детекция на фронте:** по кодам из аудита (см. `internal/audit/planet/`).
4. **Показ:** отдельный блок в карточке планеты, под описанием.
   Может быть несколько аномалий на одной планете.

Формат и список — в `config/anomalies/README.md`.

### 99.2.2. LLM-генерация описаний

**Идея:** при создании планеты (в `GeneratePlanets`) генерировать уникальные описания
через LLM и сохранять в БД. Детерминизм обеспечен — читается из БД, а не генерится
на каждый запрос.

**API на выбор:**
- **Groq** — быстрый, 1000 запросов/день бесплатно.
- **YandexGPT 5 Lite** — 10 млн токенов/месяц, работает из РФ.
- **Google Gemini** — 30 запросов/день.
- **OpenRouter** — 26+ бесплатных моделей.

**Что генерировать:**
- Обычные описания планет (вместо библиотеки `config/descriptions/`).
- Описания аномалий.

**Fallback:** если API недоступен — библиотека `config/descriptions/` + библиотека аномалий.

**Архитектура:**
- Пакет `internal/llm/` — обёртка над API.
- Вызов из `GeneratePlanetsForWorld` после генерации планеты.
- Батчи по 100 планет с параллельным запросом.
- Поле `description` уже есть в JSON планеты — не надо миграций.

### 99.2.3. Тонкая настройка генерации

Все хардкод-константы вынести в конфиг, редактируемый через админку:
- Спектральные классы (веса, диапазоны T).
- Планеты (количество, возраст, шансы газовых гигантов).
- Классификатор (пороги).
- Физика (альбедо, парниковые факторы).
- Ядро (пороги массы, диапазоны).
- Композиция (множители по T и воде).

**Хранение:** JSON-дефолты + override в БД (как у матрицы совместимости).

### 99.2.4. Экзотические типы звёзд

**Идея:** добавить в генерацию редкие «экзотические» звёзды и системы вокруг них.
Список типов систем с распределением уже заявлен в `02_worlds.md` §2.1
(нейтронная звезда 1%, чёрная дыра 1%, протозвезда 1%, экзотическая ~5%),
но **в коде не реализован** — `internal/generator/galaxy/galaxy.go` генерирует
только обычные спектральные классы O–Y.

**Продумать (главный открытый вопрос):**
1. **Список типов** — нейтронные звёзды, пульсары, магнетары, чёрные дыры,
   белые карлики, квазары, красные гиганты, протозвёзды. Что входит, что нет.
2. **Генерация объектов в таких системах** — как это отличается от обычной
   звезды: аккреционные диски, приливное разрушение, пульсарные ветра,
   отсутствие планет у чёрных дыр, «выжженные» внутренние орбиты,
   спутники вокруг экзотических тел.
3. **Влияние на физику** — `luminosityBySpectral` (светимость для формулы
   температуры), `determineSystemAge`, `determinePlanetCount`, шансы
   газовых гигантов, архетипы планет.
4. **Влияние на спектральные классы** — хранить тип звезды отдельным полем
   или расширять `spectral_class` (нужна миграция `worlds`).
5. **UI и карта** — как рисовать и отличать такие системы на карте.

**Ограничения:** не в MVP. Каждый тип — редкий (≤1%), не должен ломать
баланс аудита и статистики (см. `internal/audit/`, `admin_stats_anomalies.go`).

### 99.2.5. Переход на новую ресурсную модель

**Статус:** 📅 планируется, разблокировано 2026-09-12

Дизайн переработан и зафиксирован в `05_economy.md` и файлах семейства
(`09_resources.md`, `10_exploration.md`, `11_markets.md`, `12_society.md`,
`13_settlements.md`, `14_money.md`, `15_monetization.md`).
Текущий код генерации ресурсов построен по **старой** модели и подлежит замене —
таблица расхождений в `05_economy.md` §5.4.

> **РАЗБЛОКИРОВАНО 2026-09-12.** Перевывод выполнен: номенклатура выведена
> методом от потребителей (`05_economy.md` §5.2.1, стартовый состав — 13 товаров,
> список открыт), состав осей проверен снизу — все десять входят хотя бы в одно
> окно требований (`09_resources.md` §9.1.2). **Генератор веществ писать можно.**

**Порядок работ:**

1. **Разделить «вещество» и «месторождение».** Сейчас одна строка = ресурс на
   конкретной планете, то есть сущности слиты, и универсальному веществу негде
   жить: оно одно на галактику и ссылается из тысяч мест.

   | Таблица | Уровень | Содержимое |
   |---|---|---|
   | `substances` | Галактика | Вид (`universal` / `field`), имя (кир/лат), категория, свойства, домашняя точка и радиус (`NULL` у универсальных) |
   | `deposits` | Планета | Планета, вещество, запас, богатство, поверхность/недра, признак выработки |

   Побочная выгода: свойства перестают дублироваться в каждой строке ресурса
   (сейчас ~1.6M копий девяти чисел при 318k планет).

2. **Универсальный слой** — 20–30 веществ **вне генератора полей**
   (`09_resources.md` §9.2.1, §9.2.2). Ни домашней точки, ни радиуса.

3. **Генератор полей** — «дом + радиус», степенное распределение радиусов
   (`09_resources.md` §9.2.3, §9.2.4).

4. **Месторождения и истощение** (`10_exploration.md` §10.1): запас зависит от
   типа и доли источника, а не случайное число в фиксированном диапазоне.

5. **Сводка богатства** — только вещества полей, не универсальные
   (`05_economy.md` §5.4).

**Предпосылки — сделать до первой перегенерации вселенной:**

- **B11** (`STATUS.md` §2) — `GeneratePlanets` не очищает старые планеты.
- **Две таблицы ресурсов** в живой БД (`AGENTS.md` §8) — разрулить одной миграцией.
- **B9** (`STATUS.md` §2) — фильтр карты по ресурсам. Кормится сводкой
  `data["resources"]`, которую переписывать всё равно придётся.

**Чем проверять.** Гарантии генератора из `05_economy.md` §5.3 — готовый список
красных тестов: количество универсальных и отсутствие у них дома и радиуса;
фильтр категорий; число веществ в произвольной системе в коридоре; доля систем с
веществом редчайшего тира; минимальное расстояние между областями двух редчайших;
доля профилей товаров, не закрываемых локально, — больше нуля везде.

**Чего не делать:**

- **Не начинать тик симуляции.** Истощение и рынок энергии спроектированы под
  ленивый пересчёт (`11_markets.md` §11.1.2) и работают без глобального тика.
- **Не строить лаборатории и сертификаты анализа** (`10_exploration.md` §10.3.5).
  Генератор должен лишь записать точные значения в `substances`.

### 99.2.6. Поселения и труд

**Статус:** 📅 планируется · новый блок 2026-09-12

Опирается на `13_settlements.md`. **Не зависит от ресурсной модели** — можно
начинать параллельно.

**Порядок работ:**

1. **Таблица `settlements`** — уже существует, пишется, не читается. Добавить:
   `stability` (читается), `patience` (терпение, §13.5.1), `capacity` (аграрная
   ёмкость). На одной планете — несколько поселений.

2. **Привязка завода к поселению** (`13_settlements.md` §13.1.2). Смена —
   бинарная (§13.2.1): укомплектован → работает; нет → не работает.

3. **Подножный корм** (§13.2) — незанятое население кормится само. Работает не
   везде (§13.2.2): выводится из температуры, атмосферы, воды.

4. **Жёсткие потребности** (§13.4.2) — из среды планеты. Одна-две на поселение,
   известны до постройки.

5. **Стабильность** (§13.5) — сумма непокрытых потребностей. Формула прозрачна,
   раскрывается списком причин (§13.7.2).

6. **Гибель и восстановление** (§13.6): коллапс при стабильности 0; сквоттинг
   (§13.2.3) восстанавливает население на пригодных планетах.

7. **Накопленный ущерб** (§13.6.4) — счётчик на постройке. Копится от катастроф,
   заживает, пока постройка работает. Порог → обрушение.

**Чем проверять.** Симуляция (`06_factions.md` §6.7) на headless-прогоне:
темпы рождаемости против гибели, скорость сквоттинга, доля поселений,
восстановившихся без игрока.

**Чего не делать:**

- **Не включать градацию смены** (§13.2.1). Бинарная — заглушка, не окончательное
  решение. Возврат градации — после того, как заработает базовая версия.
- **Не включать второй ярус потребностей** (§13.4.1). Он схлопывается без
  градации смены.

### 99.2.7. Контракты и перелёт

**Статус:** 📅 планируется · новый блок 2026-09-12

Опирается на `11_markets.md` §11.2 и `14_money.md` §14.5–14.6.

**Порядок работ:**

1. **Эскроу** (`14_money.md` §14.5.2) — контракт как пакет «условия + запертые
   средства». Списание с собственного счёта, без удалённой информации.

2. **Простейший тип — перелёт** (`11_markets.md` §11.2.6, `14_money.md`
   §14.6.1). Проверка по прибытии, без частичного исполнения, без гонки.
   **Начинать с него.**

3. **Поставка сырья и товара** (`11_markets.md` §11.2.2) — окно свойств,
   частичная поставка, несколько исполнителей. Гонка при сдаче — блокировка
   строки в БД (`AGENTS.md` §0).

4. **Истечение и возврат** (`14_money.md` §14.6) — без этого деньги утекают из
   оборота.

5. **Отмена только до взятия** (`11_markets.md` §11.2.2).

6. **Бюджет с пополнением** (`14_money.md` §14.6.3) — для обслуживания роботов.
   Новая форма эскроу.

**Чем проверять.** Красный тест: заказчик публикует перелёт, исполнитель берёт,
летит, прибывает — эскроу раскрыт. Второй: не прибывает в срок — эскроу возвращён.

### 99.2.8. Автоматические заводы и присутствие

**Статус:** 📅 планируется · новый блок 2026-09-12

Опирается на `13_settlements.md` §13.7 и `11_markets.md` §11.2.3.

**Порядок работ:**

1. **Автоматический завод как вариант постройки** — не отдельная сущность.
   Два набора содержания (`11_markets.md` §11.1.3): человеческий ест еду,
   автоматический — энергию.

2. **Реакция на сбои** (`13_settlements.md` §13.7.6) — только через агента.
   Медленная реакция = ущерб копится.

3. **Присутствие как контракт** (`11_markets.md` §11.2.3) — оплата долей от
   выработки. Расчёт по факту (`14_money.md` §14.6.2).

4. **Переделка в обе стороны** (`11_markets.md` §11.1.1) — та же модернизация,
   что переключение категории.

5. **Градиент человек/робот** (`13_settlements.md` §13.7.3) — изолиния по цене
   еды. Настраивается симуляцией.

**Чего не делать:**

- **Не позволять роботу делать робота** (`13_settlements.md` §13.7.5). Инвариант 11.
- **Не запирать всю редкость за роботами** (`10_exploration.md` §10.2.5).

---

## 99.3. Долгосрочный бэклог

### 99.3.1. Безопасность

- **`ADMIN_PASSWORD` из env.** Сейчас дефолт `admin123`.
- **Проверка `unique_violation` в `Register`** — обработать код `23505`,
  вернуть 409 вместо 500.
- **Роль «администратор» для пользователя**.

### 99.3.2. Многопользовательская архитектура

- **Правило проекта:** любая map, доступная из двух горутин, — под мьютексом.
- **Проверить оставшиеся файлы:** `internal/generator/faction/faction.go`,
  `internal/generator/galaxy/galaxy.go` (при параллельной генерации).
- **CI с `go vet -race`.**
- **Кэш текстур в `planet_image.go`** — пишется, но не читается.

### 99.3.3. Контент и баланс

- **Проработка каждой формы поверхности** отдельным документом (начиная со «Скал»).
- **Биосферы — проработка интересных.**
- **Показ пустой породы в недрах** — решить, показывать или нет.
- **Расширение описаний** до 150+150 популярных типов.
- **Ревизия `gas_giant`/`icy`** (600 текстов).
- **Экзотические аномалии** — 100+ категорий, по 2–3 на галактику.

### 99.3.4. Механики

- **Мини-игра сбора ресурсов** — «три в ряд» (`10_exploration.md` §10.2.3).
- **Мини-игра разведки** — не выбрана (`10_exploration.md` §10.2.3).
- **Поиск по названию кластера/региона.**
- **Карточка планеты — компактность.**
- **Регионы на карте.** Визуал или механика?
- **Визуал звёзд «не просто кружки»** — спрайт-атлас в offscreen canvas: (спектральный класс × градация температуры × 2–3 размера) ≈ 240 спрайтов 32×32, внутри запечены ядро + мягкий ореол + 4-лучевые шипы. Радиальные градиенты считаются один раз при создании атласа, кадр рисуется `drawImage` вместо `arc`/`gradient` на каждую звезду. Цвета берём из уже пришедших в снапшот `sspec`/`stemp` — сервер не нагружаем. Размер спрайта пересчитываем только при смене зум-корзины.
- **Валюты фракций.**
- **Репутация игрока** с фракциями.
- **Дипломатия между фракциями.**
- **Управление поселениями игроком** («мэр») — `13_settlements.md` §13.1.3.

### 99.3.5. Экономика (расширение)

- **Рецепты товаров и качество** (`05_economy.md` §5.2.2, §5.2.3).
- **Цены и рынки, локальная биржа** (`11_markets.md` §11.2.1).
- **Торговые пути.**
- **Модель энергии** — агрегатная арифметика (`11_markets.md` §11.1.2).
- **Содержание построек** (`11_markets.md` §11.1.3).
- **Лаборатории и сертификаты** (`10_exploration.md` §10.3.5).
- **Мегапроекты** (`05_economy.md` §5.2.5).

### 99.3.6. Фракции (расширение)

- **60+ типов.** Конкретный список.
- **NPC-агенты** (`06_factions.md` §6.7) — бюджет поиска, детерминизм,
  headless-симуляция.

### 99.3.7. Инфраструктура

- **Рефакторинг остальных генераторов имён** на `LocalizedName`.
- **CI** (`.github/workflows/ci.yml`): `go vet` + `go test ./...` + `go vet -race`.
- **Инструменты анализа галактики** (см. 99.5).
- **Вкладка «Совместимость»** в админке.
- **`compatibility_matrix`** — таблица не создана, миграция `009` не применена.

### 99.3.8. Технические

- **`internal/planet_image.go`** — генерирует текстуры по старому `type`.
- **`pkg/simulation/batch/`** и **`pkg/worldgen/`** — черновики.
- **`internal/core/location_batch.go`** — нерабочий импорт.

### 99.3.9. Известные баги

| # | Что | Где | Приоритет |
|---|-----|-----|-----------|
| B1 | Пагинатор на вкладке «Миры» не работает | `web/static/js/admin/worlds.js` | 🟡 |
| B2 | Кэш текстур пишется, но не читается | `internal/generator/planet/planet_image.go` | 🟢 |
| B9 | Фильтр карты по ресурсам не находит ничего | — | 🟡 |
| B11 | `GeneratePlanets` не очищает старые планеты | — | 🔴 |

---

## 99.4. Открытые вопросы дизайна

### Геймплей

- **Мини-игра сбора** — как играется?
- **Мини-игра разведки** — не выбрана (`10_exploration.md` §10.2.3).
- **Регионы** — визуал или механика?
- **Фракции и сбор** — могут ли запретить игроку собирать на своей территории?
- **Пустая порода в UI** — показывать или нет?
- **Присутствие как отдельный контракт** — или часть охраны
  (`12_society.md` §12.1.1)?
- **Оплата присутствия** — доля от выработки подходит для охраны. Подходит ли
  для реакции на сбои, где выработки может не быть?

### Экономика

- **Сезонность** — постоянный мир, вайп или проворот секторов
  (`05_economy.md` §5.5 п.1)?
- **Сколько веществ доступно в одной системе** (`05_economy.md` §5.5 п.2)?
- **Как смешивать свойства в рецепте** — усреднение или штраф по худшему
  (`05_economy.md` §5.5 п.3)?
- **Соотношение скорости разведки и добычи** (`05_economy.md` §5.5 п.4).
- **Крутизна множителя дефицита энергии** (`05_economy.md` §5.5 п.5).
- **Темп истощения относительно темпа освоения** (`05_economy.md` §5.5 п.6).
- **Цена энергии: кто устанавливает** — рынок или владелец станции?
  Множитель и агрегатная арифметика решены (`11_markets.md` §11.1.2),
  но кто выбирает точку на кривой — не решено.
- **Спрос и предложение на товары: как моделировать равновесие.**
  Спрос выведен из поселений (`13_settlements.md`), номенклатура
  перевыведена (`05_economy.md` §5.2.1), но механика равновесия не описана.
- **Температура окружения ресурса после добычи.** Состояние
  (твёрдое/жидкое/газ) зависит от температуры локации (планета, хранилище,
  фабрика, трюм). Пока: состояние вычисляемое, только на планете; температура
  хранилищ и фабрик — когда придёт экономика. Хранение и транспортировка газов
  и жидкостей — отдельная механика.
- **Валюты фракций** — когда вводить раздельные?
- **Компаундирование капитала** (`12_society.md` §12.4.5) — роботы усилили
  проблему.
- **Лицо упадка** (`12_society.md` §12.4.4) — катастрофы или природный фронт?
- **Компенсация сжатия денежной массы** (`14_money.md` §14.3.5).
- **Бюджет как форма эскроу** (`14_money.md` §14.7 п.6).

### Монетизация

- **Доля исполнителя** — 50 % условны (`15_monetization.md` §15.8).
- **Потолок вывода** — не выбран.
- **Спред** — 5–15 % назван диапазоном.
- **Предварительная оценка для доли от выработки** (`15_monetization.md`
  §15.8 п.6).
- **Достаточно ли присутствия, чтобы удержать средний слой**
  (`15_monetization.md` §15.7 п.7).
- **Стартовые роботы для онбординга** (`15_monetization.md` §15.8 п.9).

### Контент и LLM

- **LLM-API** — Groq, YandexGPT, Gemini?
- **Что делать при отказе LLM.**
- **Фракции: 60+ типов** — какие?
- **Биосферы** — что даёт каждая?
- **Атмосферные ресурсы** — какие газы, с какой скоростью?

### Технические

- **Мини-карта в модалке** — несовпадение вьюпорта.
- **Производительность статистики** при 100k+ планет.
- **Кэш статистики** — когда добавить?

### Проверка классификации (реальные планеты)

| Планета | Тип в игре | Подобных в галактике |
|---|---|---|
| Меркурий | пустынная | 86 |
| Венера | вулканическая | 516 |
| Земля | землеподобная | 1 624 |
| Марс | пустынная | 1 431 |
| Юпитер | газовый гигант | 532 |
| Сатурн | газовый гигант | 292 |
| Уран | газовый гигант | 105 |
| Нептун | газовый гигант | 0 |

Открытые вопросы:
- **Маленькие планеты (0.1–0.5 M⊕) редки.**
- **Холодных ледяных гигантов нет.**
- **Кольца — только визуальные.**
- **Горячий сухой мир классифицируется как «пустынная».**

---

## 99.5. Инструменты анализа галактики (черновик)

1. **Карта плотности.** Тепловая карта — где какие типы планет, где жизнь, где ресурсы.
2. **Аналитика по ресурсам.**
3. **Экономическая аналитика.**
4. **Демографическая.**
5. **Фракционная.**
6. **Проверка реалистичности.** Автотест на инварианты (аудит).
7. **Динамика во времени.**
8. **Экспорт.** JSON / CSV / отчёты.
9. **Сравнение галактик.**
10. **Отладка генератора.** Показать seed → планеты, replay.

---

## 99.6. Как обновлять этот файл

- **Закрыл задачу из 99.2** — перенеси в 99.1, обнови `STATUS.md`, добавь строку в `CHANGELOG.md`.
- **Появилась новая задача** — добавь в 99.2 (если крупная) или 99.3 (если в бэклог).
- **Приоритеты изменились** — перепиши 99.1.
- **Ничего не удаляй** из 99.3 (бэклог) — только перемещай вверх, когда задача становится актуальной.
- **Известные баги** — краткая таблица в 99.3.9, полная — в `STATUS.md` (раздел 3).
- **Открытые вопросы** — в 99.4. Закрыл — убери.
- **Текущий статус, что сейчас делаем** — **не здесь**, а в `STATUS.md`.

---

*Обновлено 2026-09-12.*