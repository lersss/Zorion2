# 100c. Живой учёт агентов — отдельный сервис расходов, памяти и зацикливаний

**Создано:** 2026-09-21
**Статус:** ✅ реализовано 2026-09-21 — сервис написан и проверен (30 + 32
автоматические проверки, живой прогон на настоящем хранилище), сторож дополнен
правилом по памяти и машинными признаками итераций. Решение создателя: **и сторож
(100b), и учёт (контролёр токенов) живут в проекте как инструменты разработки**
**Приоритет:** операционная задача (видеть, куда уходит бюджет команды агентов)
**Вектор:** не игровой — обвязка команды (как 62a, 63a, 93a, 100b)

---

## Пожелание (слова создателя, 2026-09-21)

> «и я бы хотел знать цену каждой фичи (opencode дает мне в интерфейсах такую
> информацию, но руками собирать неохота)»

> «Нет, в сервер не надо. Живая таблица. Учет сессии, сгруппированный по
> субагентам. Отдельный автообновляемый веб-сервис»

До этого — наблюдение создателя: «циклить начинает, когда переполняется
контекстом. Вроде бы opencode отслеживает это. Мы можем сделать сервис на основе
этих данных?»

## Замеры, из которых вырос сервис (2026-09-21)

- Сессия, сгруппированная по дереву (корень-менеджер + подсессии) = фича.
  Всего 1044 сессии, ~$410; 309 деревьев-фич. Пример: «Расы» — $14.64 на 51
  сессию, «Композитный маршрут» — $9.86 на 6.
- Наблюдение про переполнение подтверждается: у зацикленных сессий (повтор одного
  вызова >20 раз) пик рабочей памяти — медиана 648k, у спокойных — 235k;
  аварийное сжатие памяти от переполнения — у 6 из 15 зацикленных против 6 из 185
  спокойных. Самая высокая память в проекте — 916–917k (порог переполнения
  моделей deepseek).
- Но правило не строгое: худший случай (774 повтора одной команды) шёл при
  памяти 476k без переполнения. Поэтому сторожа (100b) это не отменяет.
- Точные повторы вызовов (сверх трёх) — 3803 за всю историю.

## Что сделано

Отдельный локальный сервис `tools/agent-dash/` на Node без внешних зависимостей:

- `stats.mjs` — чтение хранилища сессий opencode (`~/.local/share/opencode/opencode.db`,
  **только на чтение**, WAL допускает чтение при работающем opencode) и агрегаты;
- `server.mjs` — HTTP-сервис на `127.0.0.1:8790`, отдаёт страницу и `/api/stats`;
- `index.html` — живая таблица (самообновление раз в 5 с, сортировка по столбцам);
- `start.ps1` — запуск; `README.md` — настройки и ограничения;
- `stats.test.mjs` — прогон на синтетическом хранилище.

Игровой сервер не затронут: сервис — отдельный процесс, ничего не пишет в
хранилище и в проект (кроме своего кэша).

## Что показывает

- **По субагентам** (главный экран): сессий, цена, ответов, пик памяти, вызовов,
  повторов, срабатываний сторожа, сжатий памяти, последняя активность.
- **По фичам**: то же по дереву сессий; цена фичи = сумма по дереву.
- **Последние сессии** — 40 свежих, с пометкой «работает» (< 5 минут).
- Периоды: сегодня / 7 дней / всё время.

## Как считается (важные детали)

- Цена и память **режутся по дням** через отметки сообщений — «сегодня» это
  настоящая активность за сегодня, а не цена всей сессии.
- **Повторы** — счётчик за всю сессию (по истории вызовов); по периоду не режется.
- **Срабатывания сторожа** берутся из журнала сторожа
  (`.opencode/loop-guard.log`, gitignore), а не из текста сессий: текст сторожа
  встречается и в самих файлах проекта, первая версия счётчика давала ложные
  попадания (28 «срабатываний» на моих же записях).
- Тяжёлые счётчики досчитываются в фоне порциями (сначала дорогие сессии), итог
  кладётся в `.cache.json` (gitignore) — повторный запуск поднимается за секунды.

## Правка сторожа (расширение идеи 100b)

Сторож теперь пишет журнал срабатываний: `{at, session, action, reason, ...}`
(`action: remind | blocked | aborted`), файл `.opencode/loop-guard.log`. Пишет
только если известен каталог проекта; каталог `.opencode` создаёт сам. Это
единственный надёжный источник для учёта — по частям сессии отличить отказ
сторожа от текста файла нельзя.

## Правило по памяти и машинные признаки итераций (2026-09-21)

Повод: разработчик в фиче «Дешёвая рваная генерация материков» ушёл в спираль —
**77 минут, 95 вызовов, $1.09** (90% денег фичи), пик памяти **774k**, и всё это
при том, что в его промте есть «лимит итераций: 5, остановись и доложи».
Разбор, почему правило не сработало:

1. итерации никто не считает — это текст в промте, а не механизм;
2. «итерация» не определена: у него были не повторные попытки, а всё новые замеры
   («замер → замер → замер»), правило про такой режим молчит;
3. у подсессии нет кнопки «остановись»: «доложи» для модели = «заверши ответ
   с нерешённой задачей», а его последние три сообщения начинались одинаково
   («Кнопки 1–4 не решают…») — он считал, что докладывается и продолжает;
4. про лимит никто не напоминает в моменте — к 77-й минуте вес инструкции размыт;
5. гейты для подсессий не работают: пока подсессия крутится, её никто не видит.

Решение (одобрено создателем, «давай да») — перенести правило из текста в
механизм. Сторож теперь:

- **напоминает** (один раз за сессию) и вкладывает напоминание прямо в системную
  память агента перед следующим шагом, если сработал любой признак спирали:
  память ≥ 700k; один и тот же файл переписан ≥ 25 раз; одно семейство команд
  (первые 48 символов команды) прогнано ≥ 15 раз; три своих вывода подряд
  начинаются одинаково. Текст напоминания прямо разрешает сдать незаконченную
  работу: «незаконченная задача с отчётом — нормальный результат»;
- **тормозит**: если после напоминания агент делает ещё 15 таких же действий —
  вызов обрезается, после серии отказов сессия гасится;
- **гасит по памяти**: ≥ 900k — отказ сразу, повторные попытки — гашение сессии.

Важно: признаки поймали бы тот самый случай (файл-щуп переписан 20 раз, десятки
`go test … -run "TestProbe…"` с одинаковым началом команды).

## Проверка

- `node tools/agent-dash/stats.test.mjs` — 30 проверок: агрегаты по ролям, дерево
  фич, период по дням, повторы, срабатывания сторожа из журнала (вмешательства и
  напоминания раздельно, разрез по периоду), кэш, битые строки журнала. Все зелёные.
- Прогон сторожа — 32 проверки: повторы, порядок ключей, «разные команды не
  трогаем», напоминание по памяти/файлу/семейству команд/повтору вывода, вложение
  напоминания в системную память, торможение после игнора, гашение, журнал.
  Все зелёные.
- Живой прогон на настоящем хранилище: сервис поднялся, страница HTTP 200,
  `/api/stats` отдаёт 1044 сессии / $409.98 / 60512 вызовов / 3803 повтора /
  309 фич; досчёт из кэша — 1 секунда.

## Границы и риски

- Счётчик сторожа наполнится только с новых сессий после перезапуска opencode.
- Хранилище — файл пользователя вне проекта (13 ГБ); чтение может подтормозить при
  активной записи.
- Сервис слушает только localhost и показывает расходы — не выставлять наружу.
- Повторы считаются по параметрам вызова; «слегка разные» циклы остаются в зоне
  наблюдения (та же граница, что у сторожа).

## Разнос по докам (дельта для @dispatcher, 2026-09-21)

Правило 18 в `AGENTS.md` оформляет оба пункта — сторожа (100b) и учёт (100c);
отдельная дельта 100b поглощается этой.

**Разнос по докам: ГОТОВО** — разнесено @dispatcher 2026-09-21 (AGENTS.md §4
правило 18, `.opencode/context.md`, `CHANGELOG.md`, `docs/INDEX.md`).

## Правка 2026-09-22: расход в час в шапке

**Пожелание (создатель, 2026-09-22):** «добавь в 8790 еще расход в час в шапку»;
уточнения создателя в той же сессии: «цифра/ч», «без подписи "расход в час"»,
окно — «за последний час». Правка мелкая → сделана самим менеджером.

**Сделано:**

- `stats.mjs` — в отчёт добавлено `rate: { perHour, windowMin }`: сумма цены
  ответов агентов проекта за последние 60 минут (окно — `DASH_RATE_MIN`,
  по умолчанию 60). Фильтр проекта один — общий `projectLike`.
- `index.html` — в шапке рядом с названием проекта цифра вида `$2.00/ч`, без
  подписи; подсказка при наведении — «цена за последние N мин». От кнопок
  периода не зависит: это живой темп трат.
- `stats.test.mjs` — +3 проверки (окно = час, старые траты не входят, поле есть
  в обычном отчёте); всего 44 проверки.
- `README.md` — описание цифры и переменной `DASH_RATE_MIN`.

**Проверено:** `node tools/agent-dash/stats.test.mjs` — PASS=44 FAIL=0; живой
сервис отдаёт `rate.perHour` и `rate.windowMin=60`; независимый прогон тестера —
пересчёт по хранилищу сходится с API (±0.01), на странице цифра `$2.18/ч`,
подписи «расход в час» у цифры нет, ошибок JS нет
(`docs/QA/checklists/2026-09-22_дашборд-расход-в-час.md`).

## Разнос по докам (дельта для @dispatcher, 2026-09-22)

**Разнос по докам (горячие доки): ГОТОВО** (@dispatcher 2026-09-22) — внесён `CHANGELOG.md`.

`CHANGELOG.md`: +2026-09-22, `Дополнительно: в шапке сервиса учёта агентов
(порт 8790) появилась цифра расхода в час — траты команды за последние 60 минут
(окно настраивается `DASH_RATE_MIN`).`

## Правка 2026-09-24: срок работы сессии (живой счётчик в карточках активных)

**Пожелание (создатель, 2026-09-24):** «добавим в учёт агентов срок их работы в
часах-минутах»; уточнения: «живой секундомер, но без секунд»; «подкрашивать, если
работает ненормальное время»; «выводить активное время там, где зелёный кружок,
вместо "n секунд назад"».

**Что сделано:** в карточке активной сессии (блок «Активные») вместо строки
«N мин назад» показываем живое активное время работы сессии рядом с зелёным
кружком. Формат «X ч Y мин» (без секунд), обновляется вживую.

**Метрика — активное время сессии:** от первого действия до последнего; паузы
между соседними сообщениями дольше 10 минут не считаются. У живой сессии к сумме
добавляется текущий промежуток (сейчас − последнее сообщение) — счётчик тикает.
Почему не «от сообщения человека»: opencode прячет под ролью `user` служебные
сообщения сжатия памяти (несколько на сессию) — замер по ним даёт мусор.

**Данные истории (1853 сессии проекта, активное время в минутах; медиана / p75 /
p90 / max):** разработчик 13/34/63/222, тестер 5/10/22/125, дизайнер 7/17/40/307,
критик 4/5/8/41, балансер 3/4/12/21, менеджер (вкладка = вся фича) 25/88/148/953.
Обычная задача субагента — 5–15 мин; дольше 30 мин — верхние 3–7% задач.

**Пороги (решение создателя):** больше 15 минут — жёлтым (мягкое внимание),
больше 30 — оранжевым, больше 60 — красным. Настраиваемые: `DASH_WORK_NOTE_MIN=15`,
`DASH_WORK_WARN_MIN=30`, `DASH_WORK_BAD_MIN=60`. Менеджерскую вкладку и диспетчера
не красим (по природе длинные), число показываем. Оговорка: медиана разработчика —
13 минут, поэтому на жёлтый попадёт примерно треть субагентских сессий — это
задумано (мягкий уровень «внимание»).

**Файлы:** `stats.mjs` (активное время в отчёте), `server.mjs` (пороги в
`report.limits`), `index.html` (вывод и подсветка), `stats.test.mjs` (проверки),
`README.md` (описание).

**Решение по менеджеру (создатель, 2026-09-24):** оставить без подсветки (выбрано
«оставить как есть» — менеджерская вкладка живёт всю фичу и по природе длинная).
Проверка на живом сервисе это подтвердила: карточка менеджера «2 ч 10 мин» без
цвета, дизайнер «6 мин» — ниже жёлтого, тоже без цвета.

**Статус:** ✅ реализовано 2026-09-24.

**Проверено:** автотесты сервиса 47/47; независимый прогон тестера 7/7 (пересчёт
активного времени по хранилищу сошёлся до миллисекунды, обрезка пауз работает,
ошибок JS на странице нет; отчёт
`docs/QA/checklists/2026-09-24_счётчик-времени-дашборд-агентов.md`); живой смоук
`/api/stats` — пороги и `workMs` на месте.

## Разнос по докам (дельта для @dispatcher, 2026-09-24)

`CHANGELOG.md`: +2026-09-24, `Дополнительно: в сервисе учёта агентов (порт 8790) в
карточках активных сессий появился живой счётчик времени работы (часы-минуты;
паузы дольше 10 минут не считаются) с подсветкой по порогам 15/30/60 минут —
настраивается DASH_WORK_NOTE_MIN/WARN_MIN/BAD_MIN.`

Опционально `docs/PITFALLS.md` (на усмотрение владельца файла): + ловушка «в
хранилище сессий opencode под ролью `user` лежат служебные сообщения (сжатие
памяти) — считать „сообщения человека“ по `role=user` нельзя» (используется в
`tools/agent-dash`).

## Маршрут

Приём → наблюдение создателя → замеры по хранилищу → уточнение (в сервер не надо,
живая таблица по субагентам) → гейт («да») → сервис + журнал сторожа → проверки →
**гейт приёмки** → коммит + разнос доков.
