# 16g. Вкладка «Генерация» в админке

**Создано:** 2026-09-14
**Статус:** Выполнено (2026-09-14, менеджер — мелочь, правило 2026-09-14).
Ожидает одобрения + коммит по «го».
**Приоритет:** фича (UI-рефакторинг админки, без изменения функционала)

---

## Суть

Из вкладки «Основное» вынесено всё, что касается генерации мира (выбор
создателя: объём 3 — включая «Создать мир»), в отдельную вкладку
«🌌 Генерация» (`tab-generation`). В «Основном» остались только краткая
статистика и блок пароля.

## Что перемещено

- ➕ Создать мир
- 🌌 Генерация вселенной
- 🌍 Генерация планет
- 🏛️ Генерация фракций
- 🗑️ Очистка вселенной

## Правка (менеджер, 2026-09-14, мелочь)

- Файл `web/admin.html` — чистый HTML: добавлена кнопка вкладки
  `data-tab="tab-generation"` в `.admin-tabs`, карточки вынесены в
  `<div id="tab-generation" class="tab-pane">`. ID — уникальны (проверено;
  79 id, дублей нет).
- JS (`main.js`, `tabs.js`, `generation.js`) — **не тронут** (обработчики
  onclick — глобальные `window.*`, не зависят от родительского div; ленивой
  инициализации для tab-generation не требуется). Go — не тронут.
- Статика отдаётся с диска, рестарт не нужен.

## Автотесты

- Go: не затронуты (DoD не требуется);
- JS: не тронут (node --check не нужен);
- Уникальность id: проверена, 79/79 (дублей нет).