# Визуализация атмосферы и поверхности — картинка планеты из биомов

**Дата:** 2026-09-20
**Вектор:** Визуал
**Приоритет:** высокий (следующая задача после релиза биомов)
**Статус:** 🚧 в работе — **пауза** (создатель: «ждем гейта соседней команды»);
релиз биомов 99.2.28 состоялся (коммит `07a5b12`); заготовка под следующую
сессию — §7б

**Связи:** идея 2026-09-20 «Генерация биомов планеты» (поставщик биомов —
поверхность как объекты со своими параметрами); 99.2.20 (атмосфера-объект:
состав, давление, τ_IR, `cloudFraction`); `planet_image.go` +
`postprocessing.go` (текущий визуал планеты); идея 2026-09-20 «внутрисистемный
полёт» (модалка системы — место для большой картинки планеты); вектор «Визуал»
(`docs/VECTORS.md`).

---

## 1. Пожелание (создатель, 2026-09-20)

Из уточнения направления по биомам:

> «Биомы — полезно в генерации картинки (надо будет доработать генератор после
> того, как сделаем атмосферу)… в генерации контента для высадки на планету».

Скоуп доработок по вектору (решение создателя 2026-09-20, после релиза биомов):

> «тщательно проработать биомы; Визуализация (атмосфера + поверхность); Большая
> сгенеренная картинка планеты в модалке (вид с орбиты), если мы на неё прилетели;
> высадка».

## 2. Рамки (решения создателя 2026-09-20)

1. **Порядок:** визуализация делается **после релиза биомов** (биомы поставляют
   куски поверхности с параметрами, из которых строится картинка).
2. **Скоуп вектора** (зафиксировать после релиза биомов, последовательность):
   1. тщательная проработка биомов;
   2. визуализация атмосферы + поверхности;
   3. большая сгенерированная картинка планеты в модалке (вид с орбиты, когда
      на планету прилетели);
   4. высадка.
3. **После сессии** — дать задание на картинки (промт), чтобы создатель увидел
   результат глазами.

## 3. Что уже есть (контекст, проверено менеджером 2026-09-20)

- **Атмосфера в каскаде — объект** (`atmosphere.go`, `AtmosphereData`): состав
  газов в %, давление, парниковый эффект, масса, высота, средняя молекулярная
  масса; тип классифицируется (`classifyAtmosphere`); есть `cloudFraction`
  (облачная доля по типу атмосферы).
- **Визуал планеты** (`planet_image.go`): картинка по `visualType`
  (rocky/earth/ice/lava/gas) + `applyPostProcessing` (тень, блик, «ободок»
  атмосферы по флагу `hasAtmosphere`). Атмосфера визуально **не связана** с её
  реальными параметрами; биомы как куски поверхности на картинке **не видны**.
- **Картинка планеты** отдаётся через `/api/planet-image`; большой вид планеты
  с орбиты в модалке системы — **отсутствует**.
- **Модалка системы** (`web/static/js/modal/tabs.js`) показывает состав строкой
  (`surface_composition` / `subterrain_composition` / `atmosphere`) — это то, что
  станет объектами биомов/недр.

## 4. Черновой скоуп (уточнится после релиза биомов)

1. **Визуализация поверхности из биомов:** 100% кусков поверхности → текстура
   (цвет/вид местности берём из параметров биома).
2. **Визуализация атмосферы по параметрам:** состав, давление, облачность —
   вместо косметического «ободка».
3. **Большая картинка планеты в модалке** (вид с орбиты), когда на планету
   прилетели.
4. **Высадка** (отдельно, после визуала; вектор «Игрок»/«Поселения»).

## 5. Маршрут (после релиза биомов)

@critic по идее → гейт → @gdesigner (стилистика/варианты внешнего вида планеты;
на старте визуальной задачи — 2–4 варианта на выбор создателя) → @uidesigner /
@designer (компоновка модалки, контракт картинки) → @developer → @tester →
гейт → приёмка.

**Пока не запускаем** — ждём релиза биомов.

## 6. Журнал хода работ

- 2026-09-20: идея заведена по решению создателя как ближайшая после релиза
  биомов; зафиксирован скоуп вектора (биомы → визуализация → картинка в модалке
  → высадка).
- 2026-09-20: менеджер проверил контекст: атмосфера в каскаде — уже объект,
  визуально — только «ободок»; биомы на картинке не отражаются; большого вида
  планеты с орбиты нет.
- 2026-09-20: **создатель запустил задачу («делаем»)**; релиз биомов 99.2.28
  состоялся (полная приёмка, коммит кода `07a5b12`) — разблокировка. Иконки
  биомов (пачка 1, 23 шт, `ai_drafts/biomes/processed_p1B/`) — встраивание в этой
  задаче. Старт маршрута: @critic по идее.
- 2026-09-20: **@critic по идее: 8 находок (средних 3, мелких 5), крупных нет.**
  Средние: С1 (точная картинка раскрывает скрытое — гейт 77a), С2 (источник
  «биом → цвет» не определён), С3 (контракт картинки: входы/кэш/фолбэки).
  Мелкие М1–М5 (фолбэк иконок для 34 биомов, терминология облачности, кэш-
  коллизии, триггер картинки, 41 новый биом в модалке без иконок). Противоречий
  с эталоном 99.2.20 нет.
- 2026-09-20: **гейт 1 — решения создателя** (см. §7а): С1 отложен до просмотра
  картинок (картинка — из уже доступных клиенту данных); С2 — контракт
  «биом → цвет» на дизайне, дрейф приемлем; С3 — производительность в контракт
  картинки. Запуск @gdesigner: стилистика + ТЗ на картинки.
- 2026-09-20: **@gdesigner технически недоступен** — модель `opencode/mimo-v2.5-free`
  отвечает Forbidden. Создатель: «переключи Gdesigner на deepseek» — модель
  в `.opencode/agents/gdesigner.md` переключена на `opencode/deepseek-v4-flash`
  (правка применена; подхватится после перезапуска opencode — определения
  агентов кэшируются на старте сессии). Места/размеры картинок проверены:
  канвас орбит (малая ~20–40px, сервер 256px) + иконки биомов (~16–32px) +
  большая «вид с орбиты» (~256–512px, новое место); один процедурный генератор
  на оба размера.
- 2026-09-20: **ожидание** — создатель: «ждем гейта соседней команды».
  Продолжение (стилистика @gdesigner / контракт «биом → цвет» @designer) —
  после гейта соседней команды.
- 2026-09-20: **impact map дополнен (подготовка в паузу):** +3 сущности —
  `planet_image` (генератор картинки: planet_image.go/postprocessing.go/handler,
  кэш по seed, 3 типоразмера), `atmosphere_data` (атмосфера-объект, 2+ потребителя,
  находка @critic), `biome_icons` (иконки биомов, пачка 1); связи
  `biomes → planet_image`, `biome_catalog.json → planet_image` (контракт «биом →
  цвет»). JSON валиден (52 сущности), валидатор по новым записям чист.
- 2026-09-20: **долг реестра impact map (15 протухов, накопленные, НЕ этой задачи):**
  пакман (worlds/npc_agents/regions/mapcache/WebSocketHub/StatusManager — id без
  сущностей/путей), биомы с виртуальными путями (planets.data.biomes/subterrain/
  surface_composition/subterrain_composition, PlanetKnowledgeView), каскад
  (classify.go, 99.2.20, 99.2.10, 99.2.22 — разрывы из задачи биомов). Отдельная
  чистка реестра — предложить создателю.
- 2026-09-20: **прогресс сессии зафиксирован; заготовка под следующую сессию —
  §7б** (готовые промты на стилистику/контракт, блокеры, маршрут дальше).

## 7. Открытые вопросы

1. Стилистика визуала (2–4 варианта от @gdesigner на выбор создателя) — на старте.
2. Источник картинки: генерация на сервере (как сейчас `/api/planet-image`) или
   отдельный генератор большой картинки — решается на @designer/@uidesigner.
3. Связь «атмосфера + поверхность» на большой картинке (облачность, дымка,
   свечение) — по реальным параметрам атмосферы.

## 7а. Решения гейта 1 (2026-09-20, создатель)

- **С1 (гейт видимости 77a) — ОТЛОЖЕН:** «давай сначала посмотрим на картинки,
  что можно по ним понять. Закрыть всегда успеем». Сначала визуал + просмотр
  картинок создателем, гейт видимости решаем после. **Ограничение на время
  разработки:** картинка строится из данных, которые клиент уже получает
  (не раскрывает больше, чем отдаёт API планеты/модалка) — инвариант 77a И1
  «клиент не получает скрытых данных» не нарушаем. Полный гейт — после показа.
- **С2 (источник «биом → цвет») — НА ДИЗАЙНЕ:** «надо придумать». Контракт
  «биом → цвет/вид» придумывает @designer/@gdesigner. **Визуальный дрейф
  приемлем** («вращается планета :)»; не понравится — пересмотрим): картинка
  генерируется на лету по текущему каталогу — правки каталога могут менять вид
  существующих планет, это допустимо.
- **С3 (контракт картинки / производительность):** картинка генерится на лету с
  серверным кэшем по seed (одинаковые планеты = один расчёт); нагрузка зависит
  от числа уникальных просматриваемых планет, не от числа пользователей. Для
  большой картинки (вид с орбиты) — диск-кэш PNG / ограничение размера, войдёт
  в контракт картинки (требование «выдержать ~1000 пользователей» — в спеку).
  Нагрузочный тест (STATUS §3 п.1) — кандидат на включение `/api/planet-image`.

## 7б. Заготовка под следующую сессию (фиксация 2026-09-20)

**Триггер старта:** создатель сказал «продолжай» / гейт соседней команды пройден.

### Блокеры
1. **Гейт соседней команды** — создатель: «ждем гейта соседней команды».
2. **@gdesigner:** модель в `.opencode/agents/gdesigner.md` переключена на
   `opencode/deepseek-v4-flash`, но **подхватится только после перезапуска
   opencode** (определения агентов кэшируются на старте сессии). До рестарта
   запуск @gdesigner даёт `Forbidden: mimo-v2.5-free` — не пытаться.

### Что сделано (2026-09-20)
- @critic по идее: 8 находок (С1–С3 средние — решения §7а; М1–М5 мелкие — на спеку),
  крупных нет, с эталоном 99.2.20 противоречий нет.
- Места/размеры картинок: канвас орбит модалки (малая текстура, запрос radius
  20–40 px, сервер 256px) + иконки биомов (23 PNG, ~24 px) + большая «вид с
  орбиты» (256–512 px, новое место). Один процедурный генератор на оба размера.
- impact map: +3 сущности (planet_image, atmosphere_data, biome_icons) + связи
  biomes→planet_image, biome_catalog.json→planet_image; JSON валиден (52).
- Долг реестра: 15 «протухов» (накопленные) — предложить отдельную чистку.

### Следующий шаг (после разблокировки): @gdesigner — стилистика
Промт-заготовка: «Стилистика внешнего вида планеты (вид с орбиты) для процедурного
Go-генератора. Прочитай: эту идею, `config/biome_catalog.json` (структура биома),
`internal/generator/planet/planet_image.go` + `postprocessing.go` (текущий визуал),
`internal/generator/planet/atmosphere.go` (параметры атмосферы). Выдай:
(1) 2–4 РАЗНЫХ варианта стилистики (палитры, характер, как показать биомы/атмосферу) —
реализуемо процедурно в Go, не «нарисовать вручную»; (2) контракт «биом → цвет»
(С2): поле color/visual в каталоге или функция от категории, с примером палитры
для 5–7 биомов; (3) ТЗ на картинки (промты) для @artist — показать создателю
«глазами»; (4) визуал атмосферы по параметрам (состав/давление/τ_IR/cloudFraction)».
Готовый полный промт — в истории сессии менеджера (2026-09-20).

### Дальше по маршруту
1. Гейт: создатель выбирает вариант стилистики.
2. @designer/@uidesigner: компоновка модалки (большая картинка при orbit {planet},
   99.2.27) + контракт картинки (входы биомы+атмосфера, расширение кэш-ключа,
   фолбэки старых миров/газовых гигантов/спутников, размеры 256/512, диск-кэш для
   большой, М1–М5) → спека `docs/specs/`.
3. @critic по спеке → @balancetester (если числа) → гейт.
4. @developer: TDD, DoD `go build` + `go vet` + `go test -race ./...` (gcc в PATH —
   docs/PITFALLS.md).
5. @tester: e2e-смоук клиентского флоу (модалка: малая текстура на орбитах +
   большая картинка с орбиты + иконки биомов в составе).
6. Гейт → приёмка → коммит + дельты (§8).

### Решения создателя (не переспрашивать)
- **С1:** гейт видимости 77a — после просмотра картинок создателем; на время
  разработки картинка строится из данных, доступных клиенту (77a И1 не нарушаем).
- **С2:** контракт «биом → цвет» — придумать на дизайне; визуальный дрейф приемлем.
- **С3:** картинка на лету с кэшем по seed; большая — диск-кэш/лимит; ~1000
  пользователей; нагрузочный тест — кандидат на включение /api/planet-image.

### Требования к дизайну/разработке (из прогона @critic)
- Старые миры без `biomes` — фолбэк на текущий генератор (не падать, «нет данных»).
- Газовые гиганты без биомов (99.2.28 §9.5) — картинка/атмосфера консистентно.
- Иконки: 23 из 57 — фолбэк для остальных (эмодзи/цвет), иначе смешанный вид (М1).
- Кэш-ключ/seed расширить под новые входы — коллизии дают чужую планету (М3).

## 8. Разнос по докам

(заполнится на сдаче через @dispatcher; заготовка — §7б. Ожидаемые дельты:
STATUS.md §1 — заметка о задаче; .opencode/context.md — «Указатели для будущих
задач»: визуализация планеты; 99_roadmap.md — строка новой фичи (номер на дизайне);
CHANGELOG.md — запись релиза; docs/specs/README.md — строка спеки. impact map уже
дополнен этой сессией.)
