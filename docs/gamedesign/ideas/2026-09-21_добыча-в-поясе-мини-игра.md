# Добыча в поясе малых тел — мини-игра

**Дата:** 2026-09-21 (уточнено 2026-09-22)
**Вектор:** Игрок · Новые механики
**Статус:** **3a (сервер) и 3b (клиент) реализованы и закоммичены 2026-09-22**
(коммиты `137b306`, `292bbd3`; дизайн-спека `docs/specs/2026-09-22-пояса-малых-тел-этап-3-добыча.md`
+ UI-спека `…-ui.md`). Играбельно (ассеты астероидов — заглушки). Осталось: **3c** (графика
астероидов + e2e) и **3d** (ПКМ по кольцу — когда появится кольцо), затем разнос по докам и пуш.
Предусловия сняты: «облако, этап 2» реализовано (коммит `fb8db80`), **трюм** реализован
(коммит `bdc76c7`, идея `2026-09-22_трюм-грузоподъёмность-корабля.md`).

## Пожелание (сырое, от создателя)

> «игрок будет долетать до пояса, и например добывать что-то в мини-игре. Отдельный плутон там
> не интересно и не нужно»

## Уточнения создателя (2026-09-22)

1. **Механика — полёт и сбор в невесомости (вариант Б), НЕ «три в ряд».** Добыча на
   планетах и в поясах **может быть разной** — это осознанное решение, а не расхождение.
2. **Добываем пока только железо.** Полный состав пояса не заводим — один ресурс-заглушка.
3. **Трюм — новый слот корабля.** У игрока появляется грузоподъёмность, она
   определяется **размером трюма**, базовое значение корабля — **100**. Это **новая
   механика** (см. «Зависимости»), не часть мини-игры.
4. **Место входа — модалка системы**, по образцу высадки на планету: **ПКМ по
   астероидному кольцу → контекстное меню «Добывать»**.
5. **Приоритет:** следующей линией идёт «облако, этап 2»; добыча в поясе — после неё
   (остаётся в очереди, записана).

## Открытые вопросы (не блокируют запись, решаются в дизайне)

- **Состав пояса** — правило ГДД `10_exploration.md` §10.2.3: место решает «что», навык —
  только «сколько и как быстро». Пояс пока даёт только железо; полноценные ресурсы —
  вместе с новой моделью ресурсов (`99.2.5`).
- **Истощение пояса** — конечный запас (как залежи на планетах) или бесконечный?
  Рекомендация менеджера: конечный (иначе пояс — «вечный станок», ГДД §10.1).
- **Риск/провал захода** — потеря груза, урон, проваленный заход — или без последствий,
  как прогулка по поверхности?
- **Случайная находка в поясе** (редкий сгусток, брошенный корабль/станция) как причина
  лететь именно сюда — сейчас или позже? (открытый вопрос базовой спеки поясов §12).
- **Форма захода** — один заход = одна партия? ограничение по времени/ходам, и от чего
  оно зависит (тип пояса)?

## Зависимости

1. **Пояс как объект системы** — ✅ реализовано (этап 1, коммит `b24e358`).
2. **Полёт к поясу** (`object_type='belt'`) — ✅ реализовано (этап 2, коммит `d36f5ce`,
   довесок Restore `d9531c8`).
3. **Трюм / грузоподъёмность** — ✅ **реализовано 2026-09-22 (коммит `bdc76c7`)**:
   врождённая ёмкость 20 т + грузовой модуль 80 т в универсальном слоте (старт 100 т),
   содержимое `player_cargo`, жёсткий предел, «груз за борт», `GET /api/cargo` +
   блок «Трюм» на дашборде. Добыча кладёт добытое через **внутренний сервис** трюма
   (`TryAddCargo`), не через HTTP. Идея/спека: `2026-09-22_трюм-грузоподъёмность-корабля.md`,
   `docs/specs/2026-09-22-трюм-грузоподъёмность-корабля.md`.
4. **Ресурсы** — «железо» как заглушка; связь с новой моделью ресурсов (`99.2.5`,
   `substances`/`deposits`), которая пока **не реализована**.
5. **Интерфейс** — шаг `@uidesigner` (`docs/VECTORS.md` §5: интерфейсы первичнее стиля).
6. **Образец механики** — `docs/specs/2026-09-21-высадка-прогулка-по-планете.md`
   (коммит `fcf0626`): серверная позиция уровня, клиентская Canvas-мини-игра, страница
   `web/surface.html`. Но у пояса — своя механика (полёт/сбор, не ходьба).

## Решение создателя 2026-09-22: графика поясов (вариант A)

Вопрос создателя «есть в планах графику астероидов прикрутить?». **Решение — вариант A:**

1. **Графика астероидов входит в дизайн добычи в поясе (этап 3)** — в мини-игре игрок летит в пояс и собирает, астероиды нужны прямо в сцене; ТЗ на ассеты — через `@gdesigner`/`@artist`.
2. **Кольцо/полоса пояса на карте системы** — отдельным **визуальным шагом `@gdesigner`**, по резерву спеки `docs/specs/2026-09-22-визуальная-система-размеров-и-сцена-подлёта.md` §4.3 (аннулус `radius_au ± width_au/2`). Напоминание создателя (правило 99a) — не потерять.

## Решение создателя 2026-09-22: развилки добычи (закрыты)

Создатель подтвердил рекомендации приёмной («да, все так»):

1. **Истощение пояса — конечный запас на весь пояс** (колонка `system_belts.iron_remaining`,
   миграция `000069`). Классификация: **игровое решение** (абстракция-метроном, не масса пояса),
   **не эталон мира** → FAQ-счётчик не меняется. Если создатель позже захочет — оформим как эталон.
2. **Риск захода — без последствий** (вариант A): столкновения только прерывают бурение, груз не теряется.
3. **Случайная находка — позже** (после модели ресурсов `99.2.5`/лута); сейчас только железо.
4. **Форма захода — только трюм + запас пояса, без таймера** (вариант A).
5. **Вход v1 — кнопка «⛏ Добывать»** в строке пояса; ПКМ по астероидному кольцу — задел,
   активируется вместе с кольцом (отдельный визуальный шаг `@gdesigner`).

**Мелочи (решение приёмной, не спрашивал):** тормоз — Shift; после выхода — на карту;
вступительного брифинга нет; уровень запаса пояса показывается только при знании пояса.
Номер миграции `000069` подтверждён (свободен; последняя занятая — `000068`).

## Дальше

3a и 3b закоммичены, играбельно (ассеты — заглушки). Осталось: **3c** — графика астероидов
(`@gdesigner`/`@artist` по §9 спеки): замена плейсхолдеров, слои/эффекты + **e2e-смоук**
`tools/e2e/belt-mining-check.js` (§12.2) + QA-чек-лист; **3d** — ПКМ по кольцу (после визуального
шага `@gdesigner`, когда кольцо появится на карте системы). Затем сдача: разнос по докам,
`impact_map`, пуш. Вектор **Игрок**.

## Решение создателя 2026-09-22 (по игре): шкала трюма в мини-игре

На прогоне создатель заметил: «добыто 4,39», а шкала трюма 0/100. **Решение:** шкала трюма
показывает **занятое место = груз в трюме + буфер захода** и растёт прямо во время добычи;
при входе с непустым трюмом сразу видно, что уже лежит. Признак «полон» согласован со шкалой.
Внесено в 3b (коммит `292bbd3`).

---

## Чекпоинт (конец сессии 2026-09-22) — история

**Где стоим.** Линия поясов: этапы 1–2 ✅ (`b24e358`, `d36f5ce`, довесок `d9531c8`), трюм ✅ (`bdc76c7`/`af2accd`). Этап 3 (добыча в поясе, мини-игра):
- **3a (сервер) ✅ реализован, принят, закоммичен** — `137b306`: миграция `000069` (`system_belts.iron_remaining`, nullable: NULL «нет данных» / 0 «выработан» / >0 запас), состояние захода `{status:"mining"}` в `users.current_position`, ручки `POST /api/belt/mine/{enter,collect,leave}`, клампы (скорость `R_cap = 2·R_base·k_iron`, запас под локом, трюм с буфером `mined + granted ≤ free`), запись в трюм через `TryAddCargo`, видимость (guard — скрыт от других), перелив буфера при взлёте в одной транзакции. Тестер: 11/12 PASS (единственный FAIL — чужие файлы в копии), API-смоук на живой БД прошёл; ревью — «ОК» после правок.
- **3b (клиент) ✅ реализован, закоммичен** — `292bbd3`: `web/belt.html` + `web/static/js/belt/*` (инерционный полёт, бурение, HUD, состояния, звук, тосты), кнопка «⛏ Добывать» в строке пояса; **ассеты астероидов — плейсхолдеры**. Ревью нашло блокер (кнопка входа блокировалась у свежего пояса) — исправлен; по игре создателя шкала трюма = груз + буфер.
- Артефакты дизайна/UI/QA — коммит `0dafbcc`.

**Следующий шаг линии — 3c.** Графика астероидов (`@gdesigner`/`@artist` по §9 спеки: 6–10 силуэтов, мелкие обломки, слои/эффекты) + замена плейсхолдеров + **e2e-смоук** `tools/e2e/belt-mining-check.js` (§12.2) + QA-чек-лист. Затем **3d** — ПКМ по кольцу (когда появится кольцо). Дальше сдача.

**Отдельный визуальный шаг.** Кольцо пояса на карте системы — `@gdesigner`, резерв спеки размеров §4.3 (создатель просил не потерять; тогда же активируется 3d — ПКМ по кольцу).

**Хвосты (не закрыты).**

1. **Разнос по докам по этапу 3 ещё НЕ применён** (дельты ниже, применяет `@dispatcher`). Ранее (2026-09-22) `@dispatcher` уже внёс помету про визуал поясов в `99_roadmap.md` §99.3.3 и указатель в `.opencode/context.md` — эти дельты применены; теперь нужны дельты по реализации.
2. `docs/PITFALLS.md` — кандидаты-ловушки этапа 3 (ниже); файл занят чужими незакоммиченными правками.
3. `docs/impact_map.json` — узел/рёбра `belt_mining` (дельта менеджера; пост-коммитный hook даёт WARNING о «новой горячей точке»).
4. 🟡 «позиция остаётся „в полёте“ при битой цели» — дельта `@dispatcher` в `STATUS.md` §Баги (старый хвост линии поясов, не этапа 3).

**Пуш.** Локальные коммиты линии (трюм `bdc76c7`/`af2accd`; этап 3: `0dafbcc`, `137b306`, `292bbd3`) **не запушены**. Пуш — через `@dispatcher` / с разрешения создателя.

**Разнос по докам (дельта для `@dispatcher` / `@manager`, при сдаче этапа 3).**

- **`docs/DB.md`**: строка миграции `000069_belt_mining.sql` + колонка `system_belts.iron_remaining` (**nullable**: NULL = «нет данных», 0 = «выработан», > 0 = запас; `CHECK >= 0`).
- **`docs/ARCHITECTURE.md`** §2/§4: `internal/handlers/belt_mining_handlers.go`, ручки `POST /api/belt/mine/{enter,collect,leave}`, `status="mining"`; клиент `web/belt.html` + `web/static/js/belt/*`.
- **`docs/GLOSSARY.md`**: термины «Заход в пояс», «Буфер захода», «Запас пояса (конечный, абстракция)», «Класс пояса» и «Уровень запаса» (разведены).
- **`docs/PITFALLS.md`** (кандидаты): «`TryAddCargo` и Tx-варианты — сохраняй ветку no-op→rollback, иначе коммитится пустая транзакция и падают тесты»; «перечислители `status`: `mining` обязан быть и в `planet_visibility.beltPresence`, и в обоих обработчиках полёта (`StartIntraFlight`, `StartTravel`)»; «столкновение в контакте: повтор каждый кадр продлевал `blockedUntil` и глушил бурение — прерывать только на реальный удар». **Файл занят** — передать владельцу метки.
- **`docs/impact_map.json`** (ведёт `@manager`): `belt_mining` (`type: "mechanic"`) — writes `system_belts` / `player_cargo` / `db:users.current_position`; reads `goods` / `cargo_capacity`; affects `belt`, `99.2.27`, `planet_visibility.go`, `players_positions.go`, `web/static/js/modal/*`, `map/data.js`, `travel` (оба обработчика), `10_exploration.md §10.2.3`.
- **`docs/gamedesign/99_roadmap.md`** §99.3.3: строка «Пояс астероидов» — этап 3 (3a/3b сделаны, 3c/3d в работе); §99.3.4: уточнить, что для поясов выбран класс «полёт/сбор», «три в ряд» — для планет.
- **`CHANGELOG.md`** (правило: реализованное — сюда, не в `STATUS.md`): `CHANGELOG.md`: +2026-09-22, `Реализовано: добыча в поясе малых тел — сервер (3a) и клиент-мини-игра (3b, ассеты-заглушки); запас пояса конечный; вход кнопкой «Добывать»`.
- **`.opencode/context.md`** §Указатели: строка о добыче в поясе (3a/3b сделаны, 3c/3d остались).

**Ретроспектива.** Бюджет итераций не превышен (дизайн — пакеты правок в пределах; разработка 3a/3b — по одной итерации правок на ревью). Ретроспектива не требуется.

---

## Пожелание создателя 2026-09-22 (по игре): пояс на карте системы — приоритет

На гейте 3c создатель сменил приоритет: «Меня в первую очередь интересуют астероиды в модалке.
Сейчас ты прилетаешь к звезде почему-то, а должны быть они на месте планеты» + «на какой-то орбите».

**Диагноз (проверено по коду и БД):** в модалке системы пояс показан только строкой в таблице
«Объекты»; на канвасе (схеме орбит) он **не рисуется**. `objectCanvasPos` для `object_type='belt'`
возвращает центр звезды (`layout.mainX/Y`) — поэтому полёт к поясу заканчивается у звезды; стоя в
поясе, маркер «я здесь» тоже не рисуется (`drawMyPosition` — ранний выход для `belt`). Данные для
кольца уже есть: `system_belts.radius_au`/`width_au` (в `BeltView`; напр. `asteroid` — `0.064 а.е.`
± `0.017`). Резерв дизайна — `docs/specs/2026-09-22-визуальная-система-размеров-и-сцена-подлёта.md`
§4.3: annulus `radius_au ± width_au/2` в той же орбитальной шкале, что планеты.

**Статус:** принято приоритетом 3c+ (активирует отдельный визуальный шаг «кольцо пояса на карте
системы» + задел 3d «ПКМ по кольцу → Добывать»). Графика астероидов добычной сцены (направления
`docs/gamedesign/art/art_belt_asteroids.md` §1) — **после** этого шага.

**Порядок и ComfyUI (2026-09-22):** пояс на схеме рисуется **кодом** (полоса-кольцо + мелкие
астероиды) — **ComfyUI не нужен**, шаг не блокируется занятым генератором. Пробная пачка астероидов
у `@artist` (направление 2, 3 тела) — как освободится ComfyUI; спрайты позже подменят процедурные
мелкие камни на схеме и пойдут в добычную сцену.

**Выбор создателя 2026-09-22:** вид пояса на схеме — **вариант C «Россыпь камней»** (лёгкая
подложка-`annulus` + 12–24 процедурных камня по осевой линии; точка подмены камней на спрайты —
одна функция `drawBeltStone`). Корабль в поясе — на середине кольца, угол детерминирован от
`belt.id` (не прыгает). Мелочи (блик по классу, ховер-подсветка, дальние пояса за кадром) — решение
приёмной: блик руды показывать при знании класса; ховер — лёгкая подсветка полосы; Койпера/Оорта
без `orbit_index` — v1 за кадром (отдельный шаг). Детали — `docs/gamedesign/art/art_belt_asteroids.md` §9.

**Реализовано и закоммичено 2026-09-23.** Клиент пояса на схеме — коммит `e937688`
(`web/static/js/modal/{utils,layout,modal_render,events,panel,index}.js`: точка пояса вместо центра
звезды, маркер `orbit`/`mining`, слой кольца `drawBelts`/`drawBeltStone`, `showBeltMenu`, кнопки
строки пояса убраны; e2e `tools/e2e/belt-check.js` переведён на ПКМ-меню + qa-пробы геометрии).
Выход из мини-игры «Вернуться на карту» — коммит `38151ed`. Генератор: обломочный пояс WD занимает
свободную орбиту — коммит `153e4b3` (тест `TestDebrisBeltFreeOrbit`, 2000 миров). @tester:
**6/6 PASS** (`docs/QA/checklists/2026-09-22_пояс-на-схеме.md`).
**Дельты доков (внести при закрытии спеки / `@dispatcher`):** UI-спека `…-ui.md` §2/§4.3 (кнопки
строки пояса → ПКМ-меню) и `art_belt_asteroids.md` §9.4 («panel.js: beltRow/wireBeltButtons — не
трогать») — устарели; `docs/QA/checklists/2026-09-22_пояса-малых-тел-этап-2-…md` — шаг e2e
`belt-fly-btn` переименован в `belt-menu`.

**Анти-бот (отдельная идея, 2026-09-22).** Обсудили с создателем: сервер клампит скорость, но
не проверяет, что игрок **играет** (мини-игра — клиентская). Подход «запись вводов + серверный
реплей меченого астероида» вынесен **отдельной незакрытой идеей** —
`docs/gamedesign/ideas/2026-09-22_анти-бот-проверка-добычи-реплей-вводов.md` (решение создателя:
завести, но пока не реализовывать).

**Пожелание создателя 2026-09-22 (по игре): убрать кнопку «Лететь» из списка объектов**
«лететь из списка планет системы надо будет убрать, но правый клик по этому списку с контекстным
меню оставить». Уточняется (в списке «Объекты» кнопка «Лететь» есть только у строки пояса; у планет
в списке кнопок нет — полёт к планете уже только через ПКМ по схеме; правого клика по строкам списка
сейчас нет).

## Пожелание создателя 2026-09-22 (по игре): кнопка выхода — «Вернуться на карту»

Создатель: из мини-игры надо выходить кнопкой «вернуться на карту», а не «на корабль». Проверено:
поведение уже вело на `/map` — поправлены **только подписи** (`web/belt.html` ×2 — кнопка выхода
и пауза; `belt_ui.js` — сброс подписи; `belt_main.js` — подсказка «трюм полон» + её кнопка, тост
выхода, комментарий; `belt_net.js` — комментарий). @tester: PASS
(`docs/QA/checklists/2026-09-22_belt-leave-label.md`). **Дельта UI-спеки** §7 (формулировки, строки
«Кнопка выхода» / подсказка трюма / тост выхода / пауза): «Вернуться на корабль» → «Вернуться на
карту» — внести при закрытии спеки.

---

## Чекпоинт на новую сессию (конец сессии 2026-09-23)

**Где стоим.** Линия поясов: этапы 1–2 ✅ (`b24e358`, `d36f5ce`, `d9531c8`), трюм ✅
(`bdc76c7`/`af2accd`). Этап 3 (добыча в поясе, мини-игра):
- **3a (сервер) ✅** — `137b306` (миграция `000069`, `status="mining"`, ручки
  `POST /api/belt/mine/{enter,collect,leave}`, клампы скорости/запаса/трюма, `TryAddCargo`).
- **3b (клиент) ✅** — `292bbd3` (`web/belt.html` + `web/static/js/belt/*`; **ассеты астероидов —
  плейсхолдеры**).
- **3c (графика + e2e) — НЕ закрыт.**
- **Сделано 2026-09-23 (по игре создателя):**
  - **Пояс на схеме системы** — `e937688`: кольцо на своей орбите, полёт ведёт к кольцу (не в
    звезду), маркер «я здесь» (`orbit` зелёный / `mining` янтарный), ПКМ по кольцу и по строке
    пояса → меню «Лететь»/«Добывать»; кнопки строки пояса убраны. Это «отдельный визуальный шаг» +
    задел 3d.
  - **Выход из мини-игры «Вернуться на карту»** — `38151ed`.
  - **Генератор: обломочный пояс WD — на свободную орбиту** — `153e4b3` (иначе планета оказывалась
    внутри пояса; старые 15 миров не мигрируем — **пересоздать галактику**).
  - **Артефакты** (идея, арт-док `art_belt_asteroids.md`, незакрытая идея анти-бота, QA) — `9de3be8`.
  - @tester: **6/6 PASS** (`docs/QA/checklists/2026-09-22_пояс-на-схеме.md`).

**Следующий шаг линии.** 3c: пробная пачка астероидов у `@artist` (**направление 2**, 3 тела) — как
освободится ComfyUI; затем подмена процедурных камней (`drawBeltStone` на схеме + заглушки в сцене
добычи) на спрайты; e2e `tools/e2e/belt-mining-check.js` (§12.2); закрытие спеки 3c + разнос по докам
+ пуш.

**Обновление 2026-09-23 (поздняя сессия): подэтап 3e сделан.** Управление кораблём в мини-игре
переделано на «два стика» (мышь ведёт нос, W/S — тяга с разгоном, A/D — стрейф, ЛКМ — добыча по
наведению), пояс стал бесконечным вдоль кольца (стенки/отскока нет). Коммит `fbe2b84`, идея
`2026-09-23_управление-кораблём-и-бесконечный-пояс.md`; `@tester` 8/8 PASS, e2e
`tools/e2e/belt-mining-check.js` создан здесь (13/13) — 3c расширяет его под спрайты.
Осталось: **3c** (графика, ждёт ComfyUI), разнос по докам, пуш.

**Обновление 2026-09-23 (закрытие): подэтап 3c сделан — этап 3 закрыт целиком.** Спрайты
астероидов в сцене: 16 форм × 2 варианта текстуры + 3 мелких обломка + 4 паттерна руды
(`web/static/sprites/belt/`), отдельный слой руды с гашением по бурению, rim-light постоянного
направления с обрезкой по альфе, тонировка истощения, прелоад с фолбэк-многоугольником.
Коммит `5fd01e3`. Независимый прогон `@tester`: **8/8 PASS** (`docs/QA/checklists/2026-09-23_3c-спрайты-астероидов.md`),
e2e сцены 12/12 (13/13 с живой сценой), фолбэков в кадре 0, ореола света нет (замер
`tools/e2e/belt-halo-check.js`), fps 60. Кадры из игры — `ai_drafts/belt_asteroids/ingame/`.

**Хвосты линии (не 3c):** (1) **добывающий лазер** — темп добычи как предмет корабля (решение
создателя 2026-09-23, идея `2026-09-23_управление-кораблём-и-бесконечный-пояс.md`), отдельная
фича; (2) **стек попапов** — вынесен в отдельную спеку (коммит `09afa39`), к поясам не относится;
(3) слабые формы пачки (`rock_02a`, `rock_06a`, `rock_05a`) — кандидаты на передел, если создатель
скажет; (4) разнос по докам и пуш — за `@dispatcher` по слову создателя.

**Разнос по докам (дельты для `@dispatcher`).**
- **`CHANGELOG.md`**: +2026-09-23 — `Реализовано: астероиды пояса — настоящие камни в сцене добычи
  (16 форм с текстурой и крáтерами, мелкие обломки), руда светится отдельным слоем и гаснет по мере
  бурения; свет падает со стороны звезды и не крутится вместе с камнем; выход из мини-игры —
  «Вернуться на карту»`; `Исправлено: обломочный пояс у белого карлика больше не садится на орбиту
  планеты (нужна пересозданная галактика)`.
- **`docs/ARCHITECTURE.md`** §2: ассеты сцены пояса `web/static/sprites/belt/` (16 камней, 3 обломка,
  4 паттерна руды); клиент — реестр спрайтов и прелоад в `belt_config.js`/`belt_render.js`, поля
  тела (`sprite`, `veinPattern`, `veinRot`, `veinScale`, `veinRich`) в `belt_world.js`;
  e2e `belt-mining-check.js`, `belt-sprites-shot.js`, `belt-halo-check.js`; инструменты пайплайна
  (`tools/process_belt_rock.py`, `make_belt_silhouettes.py`, `make_belt_veins.py`,
  `make_belt_preview.py`, `batch_belt_mass.ps1`).
- **`docs/gamedesign/art/art_belt_asteroids.md`** §3.2/§3.3 (дельта `@gdesigner`): крáтеры задаются
  **только тонкой тёмной дугой-кромкой без заливки** (заливка/кольцо → SDXL лепит выпуклые шары);
  вариант «a» при denoise 0.50 оставляет «плиту» — для плоских форм 0.55; rim-light — от
  **постоянного направления (сторона звезды)**, формулировка «от центра пятна» устарела (пятно
  заменено бесконечным поясом, 3e).
- **`docs/impact_map.json`** (ведёт `@manager`): узел `belt_mining` — рёбра на
  `web/static/sprites/belt/**` и инструменты пайплайна; узел `belt` — обновить причину ребра к
  `web/static/js/modal/events.js`.
- **`.opencode/context.md`** §Указатели: строка «пояса — этап 3 закрыт (3a–3e; спрайты `5fd01e3`)».

**Ретроспектива.** Бюджет итераций не превышен (3c: дизайн не требовался, разработка — 1 раунд
правок по ревью/тесту, тест — 2 прогона). Ретроспектива не требуется.

**Хвосты (не закрыты).**
1. **Пуш:** локальные коммиты (трюм, этап 3, пояс на схеме, генератор) **не запушены** — через
   `@dispatcher` / с разрешения создателя.
2. **Разнос по докам** — дельты ниже, применяет `@dispatcher`.
3. **Дельта UI-спеки** `…-ui.md` §2/§4.3 (кнопки строки пояса → ПКМ) и `art_belt_asteroids.md` §9.4
   («beltRow/wireBeltButtons — не трогать») — внести при закрытии; `docs/QA/checklists/2026-09-22_
   пояса-малых-тел-этап-2-…md` — шаг `belt-fly-btn` → `belt-menu`.
4. **`docs/impact_map.json`:** hook отметил `docs/gamedesign/art/art_belt_asteroids.md` как возможную
   новую горячую точку — решить.
5. **`docs/PITFALLS.md`** — кандидаты-ловушки этапа 3 (ниже); файл занят чужими правками.
6. 🟡 «позиция остаётся „в полёте“ при битой цели» — дельта `@dispatcher` в `STATUS.md` §Баги
   (старый хвост линии).

**Разнос по докам (дельта для `@dispatcher`/`@manager`).**
- **`CHANGELOG.md`**: +2026-09-23 — `Реализовано: пояс малых тел на схеме системы (кольцо на орбите,
  полёт к поясу, ПКМ-меню «Лететь»/«Добывать»); Исправлено: кнопка выхода из мини-игры — «Вернуться
  на карту», обломочный пояс WD больше не садится на орбиту планеты`.
- **`docs/ARCHITECTURE.md`** §2: клиент пояса на схеме — `web/static/js/modal/{layout.js
  (beltMidRadius/beltRing/beltAngle/beltPoint),modal_render.js (drawBelts/drawBeltStone)}`; e2e
  `tools/e2e/belt-check.js` (ПКМ-меню).
- **`docs/DB.md`**: строка миграции `000069_belt_mining.sql` + колонка `system_belts.iron_remaining`
  (nullable: NULL «нет данных» / 0 «выработан» / >0 запас).
- **`docs/GLOSSARY.md`**: «Заход в пояс», «Буфер захода», «Запас пояса», «Класс пояса», «Уровень
  запаса».
- **`docs/gamedesign/99_roadmap.md`** §99.3.3: этап 3 (3a/3b сделаны; пояс на схеме — сделано;
  3c/3d в работе).
- **`.opencode/context.md`** §Указатели: строка про пояс на схеме + добычу в поясе.
- **`docs/PITFALLS.md`** (кандидаты): «`TryAddCargo` Tx-варианты — ветка no-op→rollback»; «`mining` —
  во всех перечислителях (`beltPresence`, `StartIntraFlight`, `StartTravel`)»; «столкновение в
  контакте — прерывать только на реальный удар».

**Ретроспектива.** Бюджет итераций не превышен (дизайн/разработка — в пределах; правки по игре — по
одной итерации). Ретроспектива не требуется.

## Промт на следующую сессию

```text
@manager Продолжаем линию поясов, подэтап 3c (графика астероидов + e2e).
Чекпоинт — docs/gamedesign/ideas/2026-09-21_добыча-в-поясе-мини-игра.md,
раздел «Чекпоинт на новую сессию (конец сессии 2026-09-23)».
Коротко: 3a/3b закоммичены; пояс на схеме системы сделан (e937688);
генератор обломочных поясов починен (153e4b3); выход из мини-игры
«Вернуться на карту» (38151ed); @tester 6/6 PASS.
Осталось: пробная пачка астероидов у @artist (направление 2, 3 тела —
ComfyUI был занят), подмена процедурных камней на спрайты (схема + сцена
добычи), e2e-смоук tools/e2e/belt-mining-check.js, закрытие спеки 3c,
разнос по докам и пуш.
Первым делом скажи, свободен ли ComfyUI, — и запускаем @artist на пробную пачку.
```

---

## Чекпоинт на новую сессию (конец сессии 2026-09-23, вечер)

**Где стоим.** Линия поясов **закрыта целиком**: этапы 1–2, трюм, этап 3 — 3a (сервер),
3b (клиент), **3c (спрайты астероидов)**, 3d (кольцо на схеме + ПКМ), **3e (управление «два
стика» + бесконечный пояс)** — плюс правки по игре: кольцо без подложки/обводки, полёт к
**ближайшей** точке кольца, камни россыпи переделаны (сгустки/«якоря»/свет от звезды) и их число
— от **протяжённости** пояса, подпись `kuiper` → «**пояс астероидов**». Открыта и сделана
(этапы 0–1) новая линия **«слои интерфейса / стек попапов»**: реестр слоёв, диалоги, перевод
игровых попапов, ошибка поверх модалки; этапы 2–3 (админка/студии) впереди.

**Коммиты сессии (все локальные, не запушены).** `fbe2b84` 3e · `5fd01e3` 3c спрайты ·
`09afa39` слои 0–1 · `1bebcb7` спека слоёв · `e0ccb5f` кольцо без обводки + полёт к ближайшей ·
`01f72d2` камни + подпись · `765128a` фикс захода при NULL-запасе · артефакты: `cccd632`,
`410ae53`, `954418a`, `ce3fad0`.

**Проверки (независимые прогоны).** `@tester`: 6/6 · 8/8 · 7/7 · 9/9 · 5/5; зонды:
`belt-mining-check.js` 13/13, `belt-nearest-check.js` 9/9, `belt-stones-check.js` 11/11,
`belt-halo-check.js` (ореола нет), `layers-check.js` 31/31, `qa-belt-null-enter.js` 5/5,
`map-check.js`/`belt-check.js` ALL PASS. Кадры — `ai_drafts/belt_asteroids/ingame/` и
`tools/e2e/artifacts/` (обе папки в `.gitignore`).

**Хвосты (не закрыто).**
1. **Пуш** — 20+ локальных коммитов; **разнос по докам** — дельты написаны в идеях
   (`2026-09-23_управление-кораблём-и-бесконечный-пояс`, `2026-09-23_стек-попапов-и-ошибки-поверх-модалки`,
   `2026-09-23_пояс-на-схеме-правки-создателя`), применяет `@dispatcher` по слову создателя.
2. **Дельта арт-дока** `art_belt_asteroids.md` §9 (`@gdesigner`): подложка/кромки кольца не
   рисуются; точка пояса — ближайшая к кораблю (фолбэк канонический); вид камней и число по
   протяжённости; **спека этапа 2 §7.4** — подпись `kuiper` → «пояс астероидов».
3. **Спека слоёв §4.2/§10** — строка «запаса нет данных → алерт» устарела после фикса
   `765128a` (пустой запас заход не запрещает) — дельта владельцу.
4. **«Облако Оорта»** — та же правка метки (собственное имя Солнца), когда создатель скажет.
5. **Композитный маршрут из чужой системы** — цель полёта к поясу там остаётся канонической
   (клиент момента старта не видит; это серверная часть).
6. **Числа вида камней** (плотность сгустков, доля вытянутых) — по вкусу создателя.
7. **Слабые формы спрайтов** (`rock_02a`, `rock_05a`, `rock_06a`) — кандидаты на передел.
8. **Этапы 2–3 слоёв** (админка/студии) и **добывающий лазер** (темп добычи как предмет корабля)
   — ждут приоритета создателя.
9. **Живой «выработанный» пояс** — в БД нет ни одного (0/9674), кейс закрыт кодом/интерцептором.
10. **`impact_map`** — завести узлы `belt_mining` и `ui_layers` (hook даёт WARNING на горячие точки).

**Ретроспектива.** Бюджет итераций не превышен (дизайн слоёв — 3 прохода, ровно по бюджету;
разработка и тесты — по одной итерации на задачу). Ретроспектива не требуется.
**Замеченное (не ретро, но в копилку):** клиентский пред-чек был **строже сервера** и блокировал
заход при пустом запасе — правило «клиент не запрещает то, что сервер разрешает» (пусто ≠ запрет).

## Промт на следующую сессию

```text
@manager Хвосты после сессии 2026-09-23 (пояса + слои интерфейса).
Чекпоинт — docs/gamedesign/ideas/2026-09-21_добыча-в-поясе-мини-игра.md,
раздел «Чекпоинт на новую сессию (конец сессии 2026-09-23, вечер)».
Сделано: линия поясов закрыта (3a–3e, спрайты 5fd01e3, управление fbe2b84) + правки по игре
(кольцо без подложки и полёт к ближайшей e0ccb5f, камни и число по протяжённости 01f72d2,
подпись «пояс астероидов»); слои интерфейса этапы 0–1 (09afa39); фикс захода при NULL-запасе
(765128a).
Осталось: пуш и разнос по докам (@dispatcher), дельта арт-дока §9 и спеки этапа 2 §7.4,
метка «облако Оорта», этапы 2–3 слоёв, добывающий лазер, слабые формы спрайтов, числа камней
по вкусу создателя, impact_map (belt_mining/ui_layers).
Первым делом — пуш и разнос по докам, если создатель дал слово.
```
