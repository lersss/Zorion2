# История формирования планет: свободный состав, миграция, видимые следы

**Дата:** 2026-09-21
**Вектор:** Генерация
**Статус:** уточнение завершено, маршрут — дизайн (часть 2 темы «все варианты планетообразования»)

## Решение создателя (дословно)

> «надо привести нашу модель к тому виду, когда все варианты планетообразования
> возможны. Можно даже в описание прокидывать, что планета мигрировала, например,
> или потеряла ледяную часть.»

## Что это значит

- **Состав перестаёт быть жёсткой функцией номера орбиты**: каменистая планета может
  встать за снеговой линией, ледяная — внутри.
- Механизмы — **физические, не шум**: миграция (родилась ближе, уехала дальше);
  снеговая линия, двигавшаяся со временем (в молодой системе она ближе к звезде);
  потеря ледяной мантии (сдуло звездой, ободранная оболочка ледяного гиганта);
  позднее формирование.
- История формирования **видна**: в описание/маркер планеты прокидывается «мигрировала»,
  «потеряла лёд», «сорванный эмбрион» (кандидат §11 спеки массы).

## Что в модели жёстко сегодня (факты, 2026-09-21)

- `compositionByZone` (`internal/generator/planet/cascade.go`): три зоны по снеговой
  линии `r_ice = 2.7·√L`; доли породы/железа/льда заданы диапазонами **зоны**
  (внутри лёд 0–10%, переход 10–30%, снаружи 40–60%). **Каменистой за линией не бывает
  по построению**, ледяной внутри — тоже (кроме подкрутки расы).
- Физически снеговая линия — **предрасположенность** («льда в диске больше»), а не приговор:
  она менялась со временем, а тела мигрируют.

## Смежные (impact) — свобода состава это вход обитаемости

- Состав → плотность → радиус → **гравитация** → окно гравитации людей и веса биомов;
- Состав → **альбедо** → T₀ → **температура** → биомы/пригодность.
- Значит требуется замер: доля обитаемых, окно гравитации `[0.5, 2]`, температуры.

## Развилки (за создателем)

1. **Насколько свободен состав:** (а) небольшой хвост — редкая каменистая за линией;
   (б) полноценно независимые от расстояния вероятности («всё возможно»), при сохранении
   **частотного** тренда (большинство далёких тел всё-таки ледяные).
2. **Как прокидывать историю:** маркер/тип планеты (читаемый факт, есть потребитель)
   vs только текст описания (без нового поля).

## Связи

- `2026-09-21_разброс-массы-планет-марс-рядом-с-землёй.md` — часть 1 темы (масса).
- `2026-09-21_плутон-и-дальние-малые-тела.md` — часть 3 (пояса) того же замысла
  «все варианты», но другой архитектуры (объект системы).

## Решение создателя (2026-09-21) — корень темы

> «может, мы прямо сделаем еще один слой каскада — протопылевое облако?»
> «…которое потом будет раскладываться на планеты»
> «давай облако, а из облака уже какими-то моделями планеты и пояса нарезать»

**Принято:** вводится **протопылевое облако (диск)** — родитель, из которого планеты и
пояса **нарезаются**. Оно заменяет собой три заплаты сразу: per-системный множитель
«богатство системы», жёсткую снеговую линию и константу обрезки гигантом. Части 1–3
темы — следствия этого объекта, а не отдельные правки.

**Проверено:** этот путь в каскаде **уже держали в уме** — спека `99.2.20` §9, развилка
«Масса»: (а) степенная аккреция (принято), (б) **изоляционная масса — «нужна модель диска»**
(отложено). Создатель предлагает сделать того самого отсутствующего родителя.

**Названная цена (принята к обсуждению):** каскад нормирует орбиты по `√L` («номер орбиты =
одни условия у любой звезды», `PITFALLS` «Дизайн и числа»); настоящее облако живёт в
физических расстояниях → этот принцип пересматривается (крупная развилка за создателем).

**Следствия для плана:** спека `2026-09-21-разброс-массы-планет-обрезка-гигантом.md`
(часть 1, черновик) **не выпускается отдельно** — становится первым шагом модели облака
(одна перегенерация и один проход по тестам вместо двух). Часть 3 (пояса) — нарезается из
облака, природа объекта подтверждена (объект системы).

## Правило создателя (2026-09-21): доля обитаемых — не инвариант

> «Доля обитаемых — не инвариант, может быть больше, может быть меньше, не нужно сейчас
> к ней прицепляться. Посмотрим как правильно, потом подкрутим если надо будет.»

- **Снимается** цель `~2.3–4%` (`03_planets.md` §3.6.4) как жёсткий инвариант и окно
  смоук-теста той же вилки.
- Доля обитаемых — **наблюдаемая величина**, а не критерий: замер остаётся (информационный),
  подгонки под окно нет; подкрутка — позже и только по решению создателя.
- **Развилка спеки облака `Р10` снята** (риск выхода из окна больше не блокирует).
- **Дельта доков** (передаёт `@dispatcher`, дословно):
  `03_planets.md` §3.6.4: «Цель обитаемой доли ~2.3–4%» → «Доля обитаемых — **наблюдаемая
  величина, не инвариант** (решение создателя 2026-09-21): может быть больше или меньше;
  измеряется на прогонах, подгонки под окно нет».
  Проверки/смоук: окно `2.3–4%` — **не обязательный порог**; при выходе — зафиксировать
  фактом, а не подгонять генератор (объявленная правка теста — на разработчике).

## Дальше

**Гейт 2026-09-21:** создатель утвердил архитектуру и этап 1 «по рекомендациям» дизайнера.

**Прогоны по спеке облака (`2026-09-21-протопылевое-облако-архитектура-и-масса.md`):**
- `@critic` — крупных 0, средних 3, мелких 4 (висячая сущность `giant_truncation`; отсутствие
  дельты о ревизии реализованной спеки; двойственность статуса `race_tuning.go`; «заменяет три
  заплаты» vs скоуп этапа 1; место ролла `p`; битая внутренняя ссылка);
- `@balancetester` — этап 1 **идентичен** проверенному черновику (формула, σ, `f_обр`, роллы);
  ярдстик 0.764/0.995/0.104/0.136 и тренд `7/1` = 4.84 подтверждены; **завышены**: пол `0.02`
  заявлен 0.12% → факт **0.066%**, `M < 0.125` заявлен 2.81% → факт **2.20%**; инверсии 50% → **38%**;
  `Settleable` 2.47% не воспроизводился; наследование `B` компаньонами не подтверждено в коде.
- Правки (7 пунктов) отправлены `@designer` (итерация 2).

Маршрут: после правок — гейт «в разработку» (этап 1), затем этапы 2–3.

---

## Сдача (2026-09-21)

**Этап 1 реализован** — код-коммит `dd37a8c` (`internal/generator/planet/{cascade.go, planet_data.go, planet_data_generate.go, accretion_mass_test.go, giant_smoke_test.go}`). Приёмка создателя: «ок».
- `@reviewer`: «одобрить с замечаниями» — каскад не сломан (слои 2 и 4–10 не тронуты, поток RNG не сдвинут), замечания закрыты.
- `@tester`: «принять с замечаниями» — отчёт `docs/QA/checklists/2026-09-22_протопылевое-облако-этап-1.md`; независимо: ярдстик 0.777/0.998/0.107/0.137, тренд `7/1` = 4.8, пол `0.02` 0.08%, `ровно 8.00` 2.6–2.8%, обитаемая доля 2.38%.
- Dev-сервер перезапущен, `/health` 200.

## Разнос по докам (дельта для @dispatcher)

**`CHANGELOG.md`:** `+2026-09-21, Реализовано: протопылевое облако — этап 1 (коммит dd37a8c): бюджет облака M_диск (прежний B, тот же ролл — поток RNG не сдвинут), обрезка роста гигантом f_обр = 0.08 при |orbitIndex − giantOrbit| ≤ 2 (Марс-паттерн; ярдстик Венера 0.78 / Земля 0.99 / Марс 0.105), σ_ζ 0.4 → 0.6; каскад (слои 2, 4–10) не изменён. Дополнительно: спека docs/specs/2026-09-21-протопылевое-облако-архитектура-и-масса.md (этапы 2–3 — дизайн).`

**`docs/gamedesign/03_planets.md` §3.6.4:** «Цель обитаемой доли ~2.3–4%» → «Доля обитаемых — **наблюдаемая величина, не инвариант** (решение создателя 2026-09-21): может быть больше или меньше; измеряется на прогонах, подгонки под окно нет. Замер этапа 1: 2.38%.»

**`docs/gamedesign/03_planets.md` §3.4 / §3.10 / §3.12:** дельты — по §9 спеки `2026-09-21-протопылевое-облако-архитектура-и-масса.md` (тренд массы медианный + обрезка у гиганта; доля малых планет; эталон FAQ про Солнечную последовательность).

**`docs/gamedesign/99_roadmap.md` §99.2:** + строка «Протопылевое облако — этап 1 (масса: бюджет `M_диск` + обрезка гигантом) **реализован** 2026-09-21, коммит `dd37a8c`; этапы 2 (состав/история формирования) и 3 (пояса/Плутон) — дизайн; идея `docs/gamedesign/ideas/2026-09-21_история-формирования-планет-состав-миграция-описания.md`».

**`docs/PITFALLS.md` «Дизайн и числа»:** + «**Поток RNG генератора недетерминирован между прогонами одного seed** (не только `planet.data`): порядок итерации map в композиции сдвигает расход случайных чисел, поэтому массы и доли гуляют. Пороги тестов на доли делать устойчивыми к вилке (запас), а не по одному прогону. Обнаружено при ревизии 2026-09-21 (этап 1 облака).»

**`.opencode/context.md`:** указатель «Протопылевое облако (2026-09-21, этап 1, коммит `dd37a8c`): бюджет облака `M_диск` (прежний `B`) + обрезка роста гигантом `f_обр` (Марс-паттерн), `σ_ζ` 0.6; каскад и поток RNG не изменены. Работая с массой/составом планет — читай `docs/specs/2026-09-21-протопылевое-облако-архитектура-и-масса.md`. Следующие этапы — состав/история и пояса/Плутон.»

**`docs/specs/99.2.20-planet-physics-cascade.md` §3.2/§3.3/§9/§12 и `docs/specs/99.2.22-race-generator-tuning.md` §3.3:** дельты — по §9 спеки облака (формула; `B` → `M_диск`; терминологическая правка §3.3 без пересчёта).

**`docs/gamedesign/faq_counter.md`:** дельты нет (новых вопросов-эталонов не появилось; направление снеговой линии — вопрос физики, проверяется отдельно на этапе 2).

**Моя зона (сделано, не дельта):** `docs/impact_map.json` (узел `planet_mass` обновлён; добавлены `disk_cloud`, `giant_truncation`), `docs/specs/README.md` (статус), спеки облака и впитанного черновика, идеи темы, QA-отчёт.

**Не моя зона:** снята устаревшая метка в `docs/COORDINATION.md`.

## Открытое / следующий шаг

- **Этап 2** (состав и история: каменистая за снеговой линией, миграция, потеря льда, маркеры в описании) — дизайн.
- **Этап 3** (пояса и Плутон — объект системы) — дизайн после этапа 2.
- **Отдельная тема (звоночек):** сделать поток RNG генератора воспроизводимым (порядок итерации map) — предсуществующее, не блокер.
