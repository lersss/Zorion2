# 2026-09-21. Переименование товарных категорий: «комплектующие» → «Электроника», «детали» → «Механика»

**Создано:** 2026-09-21
**Статус:** реализовано 2026-09-21 (код + миграция 000053); доки разнесены @dispatcher 2026-09-21
**Приоритет:** средний (устраняет шаткость номенклатуры — старая пометка «кандидаты на пересмотр»)
**Вектор:** Генерация · Новые механики

**Связи:** `docs/gamedesign/05_economy.md` §5.2.1 (список 13 категорий, пометка
«кандидаты на пересмотр: детали против комплектующих»); `internal/goodsstudio/model/state.go`
(`DefaultCategories`); `internal/goodsstudio/seed_producers.go` (корзина Автофабрики);
миграция `000053_rename_goods_categories.sql`; `docs/impact_map.json` (categories/goods/producer_types).

---

## 0. Пожелание создателя (2026-09-21)

> «Лучше переименовать категории тогда: Механика, Электроника.»

Итог (подтверждено создателем «без разницы» по порядку):
- **«комплектующие» → «Электроника»** — проводящая начинка (проводимость ↑, `T_melt` ↑, химактивность ↓);
- **«детали» → «Механика»** — несущее/подвижное (твёрдость, плотность, `T_melt`).

Состав 13 категорий не меняется, меняются только два имени. Старая пометка
«детали против комплектующих — возможно, одно» снимается: категории оставлены
двумя, имена сделаны описательными по природе (электрика/механика), а не по
собирательному слову.

## 1. Факты (сверка с кодом, 2026-09-21)

- **Список категорий** — `internal/goodsstudio/model/state.go` `DefaultCategories`
  (13 позиций, сид `goods_catalog_seed`); в БД — таблица `categories` (kind='good').
- **Корзина Автофабрики** — `seed_producers.go`: `input.consumables = ["детали","комплектующие"]`
  (корзина роботов, `19_robots.md`); на живой БД правится миграцией.
- **Товары** — `goods.category_id` (составной FK `(kind, category_id)`), при импорте
  топ-каталога товары стоят в категориях «детали»/«комплектующие».
- **«Детали» как обычное слово** (детали корабля/арта/расы) в доках — НЕ трогаем
  (см. §3, дельта точечная).

## 2. Код — сделано (DoD зелёный)

| Файл | Правка |
|---|---|
| `internal/goodsstudio/model/state.go` | «комплектующие»→«электроника», «детали»→«механика» |
| `internal/goodsstudio/seed_producers.go` | корзина Автофабрики → `["механика","электроника"]` (+комментарий) |
| `internal/goodsstudio/seed_producers_test.go` | ожидаемые строки обновлены (+комментарий) |
| `migrations/000053_rename_goods_categories.sql` | новый файл (см. §2a) |
Проверено: миграция применена вручную на живой БД → категории переименованы,
тестовый товар переехал на «механика», корзина обновилась; откатано до исходного
состояния, чтобы применилось штатно при старте сервера. `go build`/`go vet` чисто,
`go test ./internal/goodsstudio/... ./internal/repository/... ./internal/handlers/...`
зелёные.

## 2a. Миграция 000053 (data-миграция)

Три блока (образец — 000051: name_norm литералами нижнего регистра, не через
`lower()` — коллация C, `docs/PITFALLS.md` «БД и шелл»):

1. `UPDATE categories SET name/name_norm` — «комплектующие»→«электроника»,
   «детали»→«механика» (только `kind='good'`; ресурсные не трогаются).
2. `UPDATE goods SET category_id` — товары спорных категорий переезжают на новые id.
3. `UPDATE producer_types ... jsonb_set(input, '{consumables}', ...)` — корзина
   Автофабрики: строки «детали»→«механика», «комплектующие»→«электроника».

Порядок относительно сида: миграции применяются ДО `goodsstudio.Seed` — на свежей
БД категорий ещё нет, UPDATE холостые, сид создаёт новые имена; на существующей —
UPDATE переименовывает, маркерный сид пропускается. Оба пути дают один результат.

## 3. Разнос по докам: ГОТОВО (дельта разнесена @dispatcher 2026-09-21)

Разнесено по GDD-докам: `05_economy.md`, `09_resources.md`, `10_exploration.md`,
`11_production.md`, `13_tiers.md`, `13_settlements.md`, `14_money.md`,
`15_monetization.md`, `16_territory.md`, `17_decay.md`, `18_needs.md`,
`19_robots.md`, `07_ui.md`, `GLOSSARY.md`, `NEXT_SESSION.md`, спека «Фабрики —
сущность производства (2026-09-20)», `DB.md` (миграция `000053`), идея «студия —
дерево построек», `CHANGELOG.md`.

Правило соблюдено: «детали» в смысле кораблей/арта/рас/схемы/дипломатии
(`21_ships.md`, `art/*`, `races/*`, `18b_settlement_log.md`, `07_ui.md` §про
корабли, `06_factions.md`, `22_races.md`) не трогались.

## 4. Развилки

1. **Порядок строк в списке** — создатель: «без разницы». Взято: на месте
   «комплектующих» — «электроника» (id 12), на месте «деталей» — «механика» (id 13).
2. **Переименовывать ли вариант «Механика»/«Электроника» вообще** — создатель
   сначала отклонил, затем вернул («переименуй как попросил»). Итог: делаем оба.

## 5. Открыто

- Импортированный топ-каталог на продовой БД (если есть товары в спорных
  категориях) — миграция перекладывает их автоматически (проверено на тестовом товаре).
