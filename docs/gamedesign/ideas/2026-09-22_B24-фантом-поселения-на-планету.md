# B24 — фантом: «поселений на планету» в Проверке гипотез (уже реализовано)

**Дата:** 2026-09-22
**Вектор:** Баги (админка / Проверка гипотез)
**Приоритет:** 🔴 в статусе → снят (баг не существует; строка устарела)
**Сессия:** «Сессия фикса багов» — первый пункт списка открытых багов.

## Пожелание/баг создателя

> «Какие баги открыты? давай закрывать по одному» — начали со срочного B24 из
> `STATUS.md` §Баги: «нельзя создать >1 поселения на планету — нужна настройка
> „поселений на планету"».

## Что происходит (факт из кода, приёмная 2026-09-22)

Настройка **уже полностью реализована**; баг закрыт фактом ещё 2026-09-15
(архивная идея `_archive/24a. B24 — … (закрыт).md`), строка тогда снята из
`STATUS.md`. **Строка вернулась** при недавней порезке `STATUS.md` (батч 3,
`e736c45`/`ad0828f`) — то есть это устаревший фантом, а не регресс.

Доказательства:

- **Бэкенд** `internal/handlers/admin_hypothesis.go` (строки 185–228): читает
  `group.Settlement.SettlementsPerPlanet`, `if perPlanet <= 0 { perPlanet = 1 }`,
  цикл `for n := 0; n < perPlanet; n++` создаёт N поселений на заселённую планету.
- **Валидация** `internal/generator/planet/twin.go` (строки 119–121):
  `settlements_per_planet` не может быть отрицательным.
- **Фронт** `web/static/js/admin/hypothesis.js`: поле «Поселений на планету»
  (`hypSPP_${i}`, строки 86–90, `min="1"`), отправка `settlements_per_planet` в
  `buildTwinSpec` (строка 522).
- **БД:** уникального ограничения на `settlements.planet_id` нет
  (`migrations/000018_create_economy_tables.sql` — только индекс), N строк на
  планету разрешены.
- **Тест:** `internal/handlers/admin_hypothesis_test.go` — `SettlementsPerPlanet: 2`,
  ожидание 4 поселения (2 планеты × 2). Прогон 2026-09-22:
  `go test ./internal/handlers/ -run TestRunHypothesisJobPipeline` → **PASS**.

## Маршрут

Код не трогаем — чинить нечего. Быстрый путь не нужен (нет правок, нет
разработки/тестирования: фича в коде и покрыта тестом). Действие — только
снятие устаревшей строки из `STATUS.md` §Баги (дельта `@dispatcher`).

## Разнос по докам

**Разнос по докам: ГОТОВО** (разнесено @dispatcher 2026-09-22) — строка `B24` снята из `STATUS.md` §Баги; `CHANGELOG.md` и `docs/impact_map.json` — без изменений (по дельте).

## Связи

- Архивная идея `docs/gamedesign/ideas/_archive/24a. B24 — поселения на планету в Проверке гипотез (закрыт).md`.
- Спека `docs/specs/99.2.23-race-r-curves-twins.md` §5 (`settlements_per_planet`).
- `STATUS.md` §Баги — источник устаревшей строки.
