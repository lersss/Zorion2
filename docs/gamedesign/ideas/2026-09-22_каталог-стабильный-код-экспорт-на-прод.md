# 2026-09-22. Каталог: стабильный код записи + экспорт/перенос контента на прод

**Создано:** 2026-09-22
**Статус:** ✅ линия сдана — И1 (`09e9f41`, `7e32fa3`), И2 (`eb8f801`), И3 (`0bc7ad2`); выгрузка несёт `section` (`11a6802`); идёт подготовка первого переноса на боевой прод (шаг D).
**Приоритет:** высокий (нужен создателю для переноса рабочей базы на прод)
**Вектор:** Генерация · Поселения (каталог — база экономики) · операционка (перенос на прод)
**Эталон:** не применим

**Связи:** каталог `goods`/`categories` (`migrations/000045_create_goods_catalog.sql`, `internal/goodsstudio/`, студия `/studio`); сид `goods_catalog_seed` (`internal/goodsstudio/seed.go`, разовый, 131 ресурс); итерация 1 эпика «Экономика поселения» (`docs/gamedesign/ideas/2026-09-22_экономика-поселения-залежи-ветка-буферы.md`) — ссылается на пилотные ресурсы каталога; `docs/DEPLOY.md` (деплой Amvera).

---

## 0. Пожелание создателя (2026-09-22, дословно)

> «надо сделать, чтоб я работал в базе.»

> «надо сделать какой-то экспорт хотя бы. Мне нужно же это как-то всё на прод
> переносить будет. Скорее б.»

(«б» — вариант «завести у записи каталога стабильный короткий код и ссылаться на
код, а не на имя»; альтернатива «а» была — оставить ссылку по имени + предупреждение
при переименовании.)

Контекст ответа: обсуждалась итерация 1 (залежи). Пилотные ресурсы («Растения»,
«Мясо», «Вода-ресурс») заведены создателем через студию прямо в БД и **в код-сид не
попали**; предложение «занести в сид» создатель отклонил в пользу работы в базе +
экспорта.

---

## 1. Суть

1. **Рабочий процесс — «в базе».** Создатель правит каталог через студию, студия
   пишет прямо в БД; код-сид остаётся только стартовым наполнением пустой базы.
2. **Нужен экспорт/перенос** контента из рабочей базы на прод (и вообще между
   средами).
3. **Нужна стабильная идентичность записи каталога** — короткий `code`, не зависящий
   ни от `id` (автоинкремент, в каждой базе свой), ни от `name_norm` (меняется при
   переименовании). Без неё экспорт/перенос и ссылки из кода/генератора не
   переносимы.

---

## 2. Факты (сверка с кодом, 2026-09-22)

- `goods.id` — `BIGSERIAL` (автоинкремент): значения **не переносимы** между базами
  (локальные «Растения» получили 358, на другой базе будет иное число).
- `categories` уже имеет колонку **`code`** (`000045`; `is_system`) — стабильный
  идентификатор категории. У `goods` колонки `code` **нет**; идентичность —
  `name_norm` (`UNIQUE`).
- Сид `goods_catalog_seed` (`internal/goodsstudio/seed.go`) — **разовый** (маркер в
  `generation_config`), наполняет 131 ресурс при первом старте. Записи, добавленные
  через студию позже, в сид не попадают.
- Миграции каталога уже используют паттерн «по `name_norm` + `ON CONFLICT DO
  NOTHING`» (`000051`, `000053`) — устойчиво к переименованиям, но зависит от имени.

---

## 3. Открытые вопросы (для уточнения/дизайна)

1. **Что выгружаем:** только `goods` + `categories` или ещё `recipes` /
   `recipe_components` / `producer_types` / `producer_recipes` / `producer_slots`?
   («всё» — уточнить.)
2. **Формат:** SQL-дамп, JSON, файл-миграция? Куда кладём (репозиторий, ручная
   выгрузка)?
3. **Как заливать на прод:** миграция, кнопка в админке/студии, CLI?
4. **Идемпотентность:** повторный импорт не плодит дубли (сопоставление по `code`).
5. **`code`:** как генерируем для существующих записей; редактируется ли в студии;
   правила уникальности (в пределах `kind`?); как связан с `name_norm`.
6. **Порядок с итерацией 1:** вводим `code` до/вместе с залежами (тогда генератор
   ссылается на код сразу) или залежи идут по имени, а код — позже (переключение
   ссылки)?
7. **Объём `code`:** только ресурсы или все товары?

---

## 4. Маршрут

(заполняется)

---

## 5. Уточнение создателя (2026-09-24)

**Пожелание дословно:** «нам нужна какая-то синхронизация базы с продом».
Ответы на 4 вопроса:

1. **Что переносим — контент** (справочники студии), не игровую вселенную
   (миры/планеты/поселения не синхронизируем).
2. **Направление — только dev → прод.**
3. **Режим — только по требованию, кнопкой** (автоматики и фоновой синхронизации нет).
4. **Источник истины — пока dev** (прод игроков не ждёт). Дополнительно:
   **«конфиг вгрузить было бы здорово»** — то есть кроме выгрузки из студии приятно
   иметь загрузку конфигурации из файла (воспроизводимость fresh-базы/прода).

**Следствие:** к пунктам 1–7 §3 добавляются ответы: №1 — весь контент (справочники),
№2/№3 — выгрузка из студии кнопкой + перенос на прод; №6 — решается дизайном
(стабильный `code` — предпосылка переносимости ссылок).

---

## 6. Сидер — что это и как связан с идеей

**Сидер** — программное стартовое наполнение пустой базы: Go-код, который при **первом**
старте сервера заливает в БД базовый контент. Работает **один раз** — защищён маркером
в `generation_config`:

- `goods_catalog_seed` (`internal/goodsstudio/seed.go`) — базовый каталог (131 ресурс +
  категории);
- `producer_catalog_seed` (`internal/goodsstudio/seed_producers.go`) — типы построек
  (класс «Поселение» + 7 ступеней, фабрика, автофабрика, добывающая платформа,
  энергостанция, 3 лаборатории) + предметы + связи.

Есть маркер → повторные старты сид **пропускают**. На **существующей** (dev) базе маркер
уже стоит, поэтому сидер молчит, а всё, что создатель правит в студии, живёт только в
этой БД. На **свежей** (прод-подобной) базе маркера нет → сидер заливает базовый контент.

**Почему всплыло:** создатель переименовал в студии класс и ступени, а сидер и доки знают
**старые** имена:

| | в dev-БД (студия) | в сидере (свежая база/прод) |
|---|---|---|
| класс (тип-родитель) | **Колония** | `Поселение` |
| ступень-пол | **Форпост** | `Аутпост` |
| следующая ступень | **Поселение** | `Посёлок` |

Причём локальный ключ ступени в студии — тоже `поселение` (id 155), то есть имя класса в
сидере и имя ступени в dev-БД **совпадают**. Отсюда два риска на свежей базе/проде:

1. Прогон сидера вставил бы класс `Поселение` и ступень `Посёлок` (`ON CONFLICT
   (name_norm) DO NOTHING`) — то есть **старые имена**, и вернулась бы путаница
   «класс = ступень»;
2. `default_settlement_type_id` в сидере цепляется к `Аутпост` **по имени** → на
   переименованной базе ключ уедет не на «Форпост».

**Вывод:** сидер и переименование должны быть согласованы. Два пути (решение — создатель):

- **(1) Переименование — часть канона.** Сидер (и доки, и тесты-фикстуры) приводим к
  именам класса «Колония», ступеней «Форпост»/«Поселение»/… ; `default_settlement_type_id`
  цепляем к коду ступени-пола (`code`, не имя) — тогда это и есть «конфиг вгрузить» для
  свежей базы.
- **(2) Оставить канон-имена.** Тогда переименование в студии — локальная косметика dev-БД;
  для свежей базы она не даёт эффекта.

Это часть той же задачи «синхронизация dev → прод»: экспорт контента + загрузка конфига,
и обе опираются на **стабильный `code` записи** (п.1.3 этой идеи). Адрес: сидер/тесты —
`@developer`, доки — `@dispatcher`, дизайн — `@designer`.

---

## 7. Разведка объёма контента (exact, dev-БД 2026-09-24)

Переносить (справочники-контент, ~полтысячи строк, маленький экспорт):

| Таблица | Строк | Что |
|---|---|---|
| `categories` | ~19 | категории товаров и ресурсов |
| `goods` | ~131+ | ресурсы + товары, правки студии |
| `recipes` / `recipe_components` | ? | рецепты и состав |
| `producer_types` | 42 | дерево построек (класс «Колония» + ступени, фабрики, лаборатории) |
| `producer_recipes`, `producer_slots`, `producer_items`, `items` | ? | привязки |
| `effect_types` | ? | типы эффектов (голод/жажда) |

Не переносим: `worlds`, `planets`, `settlements`, `regions`, `factions`, `npc_agents`,
`active_effects`, `deposits`, `buildings` и прочие объекты мира.

---

## 8. Решение создателя — принято

**2026-09-24 · новые имена — канон** (дословно: «новые канон»). То есть:

- сидер и доки приводим к именам: класс (тип-родитель) — **«Колония»**, ступени —
  **«Форпост» → «Поселение» → «Городок» → «Город» → «Мегаполис» → «Метрополия» →
  «Экуменополис»**;
- «базовый тип поселения» (`generation_config`) цепляем к **стабильному `code`
  ступени-пола**, не по имени (это же снимает риск при будущих переименованиях);
- локальный ключ ступени «Поселение» в dev-БД — уже `поселение` (id 155); класс после
  переименования — `колония` (id 1), коллизии имён нет.

По §6 это путь **(1)**. Затрагивает: `internal/goodsstudio/seed_producers.go` (имена +
резолв `default_settlement_type_id`), тесты-фикстуры (`seed_producers_test.go`,
`settlement_type_consistency_test.go`, `admin_structures_test.go`, `producer_repository_test.go`
и др.), комментарии и доки (`DB.md`, спека стадий §4.3, `GLOSSARY.md` §7, `ARCHITECTURE.md`).

**Порядок работ:** (1) сидер/код/тесты → «Колония»/«Форпост»/«Поселение» + резолв по
коду; (2) доки; (3) дизайн экспорта контента dev → прод (кнопкой) + загрузка конфига.
Пункты 1–2 — правка без дизайна (`@developer` + `@dispatcher`); пункт 3 — `@designer`.

### Ход работ (2026-09-24)

- **Сидер приведён к канону** (`@developer`, в рабочем дереве, не закоммичено):
  `internal/goodsstudio/seed_producers.go` — класс «Колония», ступень-пол «Форпост»,
  следующая «Поселение»; резолв `default_settlement_type_id` — через константу
  `settlementFloorStageName`, при отсутствии ступени **ошибка вслух** (не молчаливый
  пропуск); новый тест `TestSeedSettlementFloorStageConstant`. `@reviewer`:
  «принять с замечаниями», риск для свежей БД низкий.
- **Ключевое замечание ревьюера (учтено в дизайне):** на **существующей** БД (dev/prod
  с маркером сидера) сид не запускается, а data-миграции `000067`/`000077` знают
  **старые** имена → канон-имена и dev-настройки **не доедут** на прод без отдельной
  data-миграции переименования. Она вошла в спеку итерацией И1 (номер `000083` —
  согласовать).
- **Спека синхронизации готова:**
  `docs/specs/2026-09-24-каталог-экспорт-импорт-контента-на-прод.md` (`@designer`).
  Итерации: **И1** экспорт-снимок (`content/catalog.json`, натуральные ключи) +
  ренейм-миграция; **И2** импорт (`POST /studio/api/content/import`, идемпотентный,
  атомарный, `id` не меняются); **И3** стабильный `code` записи каталога.
  `@critic`: крупных нет, 4 средних + 8 мелких — средние внесены (порядок секций по FK,
  доставка `content/` в образ через `Dockerfile`, коллизия имени класса-корня, no-op
  привязок эффектов); M8 (валидация инвариантов при импорте) вынесен вопросом.
  Открытые вопросы к создателю — О1–О5 (§12 спеки). Статус: **на гейте создателя**.

### Решения создателя по спеке (2026-09-24, гейт дизайна)

1. **Настройки генерации не переносим** — только контент (+ ссылка-якорь
   `default_settlement_type_id`). *«1. нет»*.
2. **Импорт — полная замена:** контент, которого нет в файле, **удаляется**
   (прод = точная копия dev). *«2. да»*. Осознанно опаснее дефолта: потерянная при
   выгрузке запись пропадёт и на проде — риск зафиксировать в спеке.
3. **Правки прода импорт перезаписывает** (dev — источник истины, last-write-wins).
   *«3. да»*.
4. **Проверка целостности — дефолт:** минимальные проверки + атомарность, полную
   валидацию студии не дублируем. *«4. дефолт»*.
5. **Метка записи — цифровая/номерная** (`p_0001`, `g_0142`, бессмысленная, не из
   имени): не «врёт» при переименованиях, в отличие от описательной. Решение создателя
   *«дак пусть метка будет цифровой»* после обсуждения, что `id` для переноса **не
   годится** (номера не совпадают между базами; по `id` ссылаются объекты мира).
   Тайминг (сразу/итерацией) — **уточняется**.

**Замечание по п.2 (полная замена) и п.5 (метка):** «удалять то, чего нет в файле»
безопасно только при надёжном сопоставлении записей — то есть по метке, а не по имени
(при переименовании запись «исчезла бы» из файла и была бы удалена на проде вместе со
связями). Связка «полная замена ↔ метка» — довод в пользу того, чтобы метка была
**сразу**, а не третьей итерацией; вынесено создателю.

### Решения создателя по спеке (2026-09-24, гейт дизайна — 2-й раунд)

- **В1 — первичная накатка на непустую цель: «да, просто накатывать».** Прод не
  блокируем: сопоставление по имени + переназначение метки по dev. Уточнение принято
  дефолтом: перед «Применить» — экран **«Проверить»** (дифф), не блокер. Риск только у
  первой накатки; после совпадения меток полная замена безопасна.
- **В2 — разовая миграция имён (И1) остаётся.** «Ок».
- **В3 — у `categories` метки нет.** «Ок».
- **В4 — резервная копия перед первым «Применить» обязательна.** «Ок».

**Порядок итераций (после 2-го раунда):** И1 — **номерная метка** (`code`, номерная) +
ренейм-миграция `000083`; И2 — экспорт-снимок с метками; И3 — импорт с полной заменой
(только после меток). Статус: **решения закрыты, Шаг 1 → разработка**.

### Ход работ: И1 и И2 сданы (2026-09-24/25)

- **И1 ✅** — метки `code` (миграция `000084`: SEQUENCE + DEFAULT + бэкфилл по id,
  частичный UNIQUE; `g_`/`p_`/`i_`/`e_`), ренейм-миграция канон-имён `000083`
  («Поселение»→«Колония», «Аутпост»→«Форпост», «Посёлок»→«Поселение»; data-driven,
  идемпотентно, явный отказ при коллизии), показ метки в студии (read-only).
  Коммиты `09e9f41` (код+тесты), `7e32fa3` (спека, идея, `DB.md`, ловушки).
  Ревью: «принять с замечаниями», критичного нет; интеграционные тесты на живой PG
  (4/4) прогнаны менеджером.
- **И2 ✅** — экспорт-снимок `content/catalog.json` (секции топологически по FK,
  ссылки по метке/натуральному ключу, `items.unlocks` — `producer`/`category` метками,
  якорь меткой), ручка `GET /studio/api/content/export` (одна tx, запись файла +
  скачивание, путь env `CONTENT_CATALOG_PATH`), кнопка «Экспорт» в студии, `Dockerfile`
  `COPY content/`. Коммит `eb8f801`. Ревью: код корректен; критические замечания при
  коммите закрыты (разделение хунков от параллельной линии, `content/.gitkeep`).
- **Дельта канона по `unlocks`** (находка `@developer`, реализована): в снимке `unlocks`
  несётся **по метке/натуральному ключу**, не по внутренним id; нерезолвимая ссылка →
  отказ. Внесено в спеку (§4/§5/T15) `@designer`.
- **И3 (импорт с полной заменой) — не начат:** ждёт паузы в рабочем дереве
  (параллельно пишет линия «разделы построек»). Безопасность: сначала «Проверить»
  (дифф), потом «Применить»; удаление только «чего нет в файле», живой мир защищён;
  бэкап перед первым применением обязателен (В4).

---

## ЧЕКПОИНТ (2026-09-25): линия сдана целиком

**Все три итерации реализованы и закоммичены:** И1 (`09e9f41`, `7e32fa3`), И2 (`eb8f801`), И3 (`0bc7ad2`). Линия каталога dev→прод **готова к применению создателем**.

**Что теперь умеет:**
- хранить контент в dev-базе и переносить его на прод по кнопке (метка `code` переносима, id не меняются, живой мир защищён);
- «Экспорт контента» — файл-снимок `content/catalog.json`; «Импорт контента» — двумя шагами («Проверить» → дифф → «Применить»), полная замена с отказом при мировых ссылках;
- разовая накатка при расхождении меток между базами (решение создателя В1 «просто накатывать»).

**Проверено:** 14 интеграционных тестов на живой PG (T3–T16), независимый браузерный прогон `@tester` (ПРОЙДЕНО), ревью диффов, реестр влияний чист (0 ERROR / 0 WARNING).

**Что осталось (хвост, не блокирует применение):**
1. **Живой прогон на реальном прод-подобном окружении** (`@tester`, с бэкапом `pg_dump`): на свежей базе полный цикл dev→прод не гонялся — покрыто моделью/тестами. Адрес: `@tester` при первом реальном переносе.
2. **Пуш** — по слову создателя (`@dispatcher`); ветка локально впереди origin.
3. **Доковые дельты И3** — `CHANGELOG`/`GLOSSARY`/`TAILS` (частично внесены И1/И2; досвер `@dispatcher`).
4. **Бюджет:** `content_import.go` ~1930 строк (AGENTS §4.8) — кандидат на разбиение при следующем касании.
5. **Ловушка при коммите:** `internal/handlers/studio_content.go` держала **чужую** правку (`p.Section` из линии «раздел построек») — вычищена из коммита; чужая линия незакоммичена в других файлах, при её коммите `Section` вернётся.

**Для следующей сессии:** линия самостоятельна и сдана; продолжение — только по новому пожеланию создателя (напр. «применить на проде» → прогон `@tester` с бэкапом, затем пуш).

---

## Вызов на новую сессию (И3) — ИСТОРИЧЕСКОЕ (И3 сдан; оставлено для контекста)

**Где стоим.** И1 (метки `code` + канон-имена) и И2 (экспорт-снимок) сданы и в origin
(`09e9f41`, `7e32fa3`, `eb8f801`). Спека готова: `docs/specs/2026-09-24-каталог-экспорт-импорт-контента-на-прод.md`
(§5 алгоритм, §7 UI, §9 тесты T3–T15). Решения создателя закрыты (§12). Открыт хвост 39
(реестр — узел заведён, досверить автосверку) и И3 (в `EPICS.md`).

**Спека и ориентир.** Импорт `POST /studio/api/content/import` (тело `{}` — файл с диска,
или `{"snapshot":{...}}`; параметр `dry_run=true`), одна транзакция + `pg_advisory_xact_lock`
(паттерн `beginMutation`), атомарный отказ при любой ошибке.

**Задача следующего чекпоинта (сжато):**
1. Режим сопоставления (§3.5): все без метки → первичная накатка по `(kind,name_norm)`;
   все с меткой → штатный по метке; **смесь → отказ**.
2. Порядок секций топологически: `categories → goods → recipes/recipe_components →
   producer_types` (родители раньше детей) `→ items → producer_slots/producer_recipes/
   producer_items → effect_types`.
3. Upsert по метке; **`id` не меняются** (мировые FK целы); дочерние наборы — `DELETE`+`INSERT`
   целиком (листья).
4. Ссылки — по картам (метка→id); `items.unlocks` → в БД id-форма.
5. **Полная замена:** удаление «чего нет в файле» — по метке; **предсчёт мировых ссылок**
   (независимо от `ON DELETE`; каскад — ловушка) → любая ссылка = `blocked` + отказ 409.
6. `default_settlement_type_id` — резолв по метке якоря; не найдено → отказ.
7. `dry_run` — тот же алгоритм с `ROLLBACK`, возвращает дифф (`create/update/delete/blocked/unmatched`).
8. UI: кнопка «Импорт» двумя шагами — «Проверить» (дифф) → «Применить» (2-е подтверждение);
   `blocked/unmatched` непусты → «Применить» неактивна.

**Рамки/безопасность:** бэкап перед первым применением (В4); dev — источник истины (перезапись
правок прода); настройки генерации не переносим; валидация — минимальная (дефолт O5). Импорт —
самая «трогающая много файлов» часть: запускать на **чистом дереве** (без чужих незакоммиченных
линий), коммит — с разделением хунков при переплетении.

**Тесты:** T3–T13 + T15 (полный список — спека §9). Обязательно живой PG (`internal/integration/`)
на: экспорт→импорт в пустую БД; идемпотентность 0/0/0; блок удаления живой записи; `dry_run` без
записи; bootstrap на цели без меток. Плюс контракт студии на две кнопки.

**Первый шаг новой сессии:** проверить, что дерево чисто (нет чужих незакоммиченных линий) →
спавн `@developer` на И3 по этому разделу → ревью → тестер → гейт.

## 9. Разнос по докам

**Разнос по докам: ГОТОВО** (разнесено `@dispatcher` 2026-09-25). Внесены:
`CHANGELOG.md` (2026-09-25 — метка `code`, канон-имена класса поселения, экспорт-снимок),
`docs/GLOSSARY.md` §5 (термины «метка переноса (`code`)» и «снимок контента»),
`docs/ARCHITECTURE.md` §2 (строка метки `code` у контентных таблиц, миграция `000084`),
`docs/TAILS.md` (хвост 39 переформулирован — закрыть вместе с И3),
`docs/gamedesign/99_roadmap.md` §99.2 (строка линии каталога). `docs/EPICS.md` — строка
эпика уже внесена `@manager`; `docs/DB.md` — bullets `000083`/`000084` уже внесены
`@developer`; `docs/DEPLOY.md`/`docs/specs/README.md` — чужие зоны. Чужие
незакоммиченные правки (линия «разделы построек», NPC, art-studio) не тронуты.

---

## ЧЕКПОИНТ (2026-09-25, продолжение): подготовка первого переноса на боевой прод

**Задача сессии:** применить перенос контента на боевом сервере (Amvera) по слову создателя,
предварительно закрыв пробелы проверки.

### Сделано

- **Шаг A — выгрузка несёт `section`.** В `buildContentSnapshot` (`internal/handlers/studio_content.go`)
  не переносился `producer_types.section`: импорт его читал, а выгрузка — нет, поэтому на проде
  разделы построек обнулялись бы. Одна строка + тест `TestBuildContentSnapshotSection`; сверены
  **все 11 секций** формата — `section` был единственным пропуском. Коммит `11a6802`.
- **Шаг B — живой полный цикл на свежих прод-подобных БД — ПРОЙДЕНО.** Две одноразовые БД
  (`zorion_qa_src`/`zorion_qa_tgt`), серверы на 8081/8082: экспорт → бэкап `pg_dump` → сухой
  прогон → применение → проверка (разделы, канон-имена, якорь «Форпост», сохранение `id`) →
  идемпотентность → восстановление из бэкапа. Отчёт:
  `docs/QA/checklists/2026-09-25_перенос-контента-цикл-на-свежей-бд.md`.
- **B1 — рецепт бэкапа исправлен.** Прежний «`pg_dump` контентных таблиц» **не восстановим в
  чистую БД** (дамп несёт `DEFAULT catalog_next_code(...)`, но не тянет ни функцию, ни счётчики
  `*_code_seq`). `DEPLOY.md` §3 → полный логический дамп `pg_dump -Fc "<url>" -f <файл>.dump`;
  ловушка в `docs/pitfalls/db-shell.md`; проверено фактическим восстановлением (числа строк и
  `last_value` счётчиков совпали). Коммит `11a6802`.
- **B2 — рискованный путь `bootstrap` — ПРОЙДЕНО.** Расхождение меток вызвано вручную: режим
  включился, метка переназначена (`g_9901 → g_0001`), локальный `id` сохранён, потерь/дублей нет.
  Отчёт: `docs/QA/checklists/2026-09-25_перенос-контента-накатка-по-именам.md`.
- **Решения создателя:** стратегия меток — **A** (разовая накатка при первом переносе; B отклонён);
  **прод — отдельная база** (не клон dev). Записаны в спеку §12.2 (итерация 6).
- **Мусор в dev:** удалена тестовая запись `qa_i3_dl_good` (`goods.id 380`, ресурс) — остаток
  разработки И3, случайно заведённый в рабочую БД; снимок перевыгружен (151 товар).
- **Снимок `content/catalog.json`** (dev: 151 товар, 42 постройки, 6 с разделом, якорь
  `p_0036`/«Форпост») — в коммите артефактов.

### Осталось (шаг D)

Бэкап прод-БД полным логическим дампом + импорт кнопкой («Проверить» → «Применить»); создатель
даёт доступ к прод-БД и админ-сессии. После D — закрыть хвост 54.

### Ретроспектива

Нет — бюджет итераций на этапах не превышен; правок инструкций не предлагается.

### Хвосты (обновление)

- **Хвост 54** (живой прогон) — цикл на свежих прод-подобных БД **прогнан** (2026-09-25);
  остаётся **первый реальный перенос на боевой прод** (шаг D).
- **Хвост 55** (`content_import.go` ~1930 строк) — открыт, разбить при следующем касании.
- **Хвост 39** (автосверка реестра влияний) — прогнана: `ERROR: 0, WARNING: 0`; закрыть.

### Разнос по докам (дельта `@dispatcher`) — **ГОТОВО**

**Разнесено `@dispatcher` 2026-09-25:** `CHANGELOG.md` (2026-09-25 — импорт, `section` в выгрузке, полный дамп), `docs/TAILS.md` (хвост 39 снят; хвост 54 переформулирован), `.opencode/context.md` (указатель линии каталога). `docs/EPICS.md` — не трогался (файл занят параллельной линией, строку эпика обновит `@manager`).

- `CHANGELOG.md`: запись за 2026-09-25 про экспорт контента говорит «Загрузка (импорт) — впереди» —
  устарела. Добавить/дополнить: `+2026-09-25, Реализовано: импорт контента каталога (полная замена,
  «Проверить» → «Применить», защита живых связей); выгрузка несёт раздел постройки (section);
  рецепт бэкапа перед импортом — полный логический дамп.`
- `docs/TAILS.md`: хвост **39** — убрать строку (автосверка чиста); хвост **54** — переформулировать
  («цикл на свежих прод-подобных БД прогнан 2026-09-25; остаётся первый реальный перенос на боевой
  прод, адрес `@tester`/`@manager`»); хвост **55** — оставить.
- `.opencode/context.md`: свежий контекст — линия каталога сдана (И1/И2/И3); выгрузка несёт
  `section`(`11a6802`); импорт на прод — кнопкой, бэкап перед ним — полным логическим дампом.
- `docs/EPICS.md`: строка эпика — **И3 ✅** (`0bc7ad2`), выгрузка несёт `section` (`11a6802`);
  «Дальше» — первый перенос на боевой прод. **Внести после** того, как уедет чужая
  недокоммиченная правка «мир прогулки» (сейчас файл занят другой линией).

---

## ШАГ D (2026-09-25): первый перенос на боевой прод — ВЫПОЛНЕН

**Прод:** Amvera, студия `https://zorion-game-skycomposer.amvera.io`. Применение сделал менеджер
сам по учётке `skycomposer` (создатель дал доступ; секреты не сохраняются).

**Порядок и результат:**

- Деплой Amvera после пуша `4083b86`: `/health` OK, `/status` «All systems ready».
- Сухой прогон (`POST /studio/api/content/import?dry_run=true`): режим **`bootstrap`** (метки прода
  и dev разошлись — ожидаемо), `blocked=0`, `unmatched=0`; переназначение меток (127 товаров +
  6 построек); к удалению — 4 записи.
- **Решение создателя (разовое, 2026-09-25):** страховочный бэкап `pg_dump` **не снимался** —
  «это тестовая база на проде». Отступление от решения В4 только для этого прогона; для боевых
  баз рецепт (полный логический дамп) остаётся обязательным, как в `DEPLOY.md` §3.
- Применение (`POST /studio/api/content/import`): `created` — 24 товара, 28 построек, 16 слотов,
  5 связей «постройка×рецепт», 19 рецептов, 28 позиций состава; `updated` — 127 товаров,
  6 построек; `deleted` — 4 товара; `warnings` нет.
- Идемпотентность: повторный сухой прогон → режим **`managed`**, create/update/delete = 0.
- Проверка на проде (`GET /studio/api/state`): 151 товар; разделы построек совпадают с dev
  (`colony`×1, `energy`×1, `factory`×2, `lab`×1, `mining`×1; без раздела ×36); канон-имена
  (Колония / Форпост / Поселение / Городок / Город / Мегаполис / Метрополия / Экуменополис);
  dev-уникальные ресурсы («Очищенная вода» `g_0135`, «Вода неочищенная» `g_0136`, «Кремний Si»,
  «Аммиак NH₃») на месте; старые «-ресурс»-записи («вода-ресурс», «аммиак-ресурс»,
  «кремний-ресурс», «Вода H₂O») удалены.

**Побочно:** удаление 4 старых записей вынесено создателю отдельным вопросом перед применением
(по живому миру блокировок не было) — ответ «ок».

### Хвосты после шага D

- **Хвост 54 — закрыть:** первый реальный перенос на боевой прод выполнен 2026-09-25.
- **Хвост 55** (`content_import.go` ~1930 строк) — открыт.
- **Новый:** строка эпика каталога в `docs/EPICS.md` не обновлена (И3 `0bc7ad2`, `section`
  `11a6802`, первый перенос на прод выполнен) — файл занят чужой незакоммиченной правкой;
  `@manager` обновит после её коммита.

### Разнос по докам (дельта `@dispatcher`, шаг D)

- `docs/TAILS.md`: убрать строку хвоста **54** (перенос на боевой прод выполнен 2026-09-25);
  добавить строку нового хвоста про строку эпика в `docs/EPICS.md` (адрес — `@manager`).
- `CHANGELOG.md` / `.opencode/context.md` — без изменений (импорт внесён ранее).
