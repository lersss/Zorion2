# 2026-09-22. Один батник поднимает все локальные сервера (start-all.cmd)

**Создано:** 2026-09-22
**Статус:** ✅ реализовано 2026-09-22 (коммит `c3d65c8`): `start-all.cmd` — один двойной
клик поднимает все локальные сервера (база, игровой сервер, генерация картинок, учёт
агентов, арт-студия), живоe пропускается, в конце сводка портов. Приёмка создателя — «ок»
на гейте 2026-09-22
**Приоритет:** 🟡 операционка разработки (не игровая фича)
**Вектор:** операционка/инфраструктура разработки (вне игровых векторов)
**Эталон:** не задан

**Связи:** `AGENTS.md` §1 (запуск локально, `JWT_SECRET` из `.env`), §2 (DoD);
`run.ps1` (старый скрипт запуска — прописывал `JWT_SECRET` вручную, **удалён 2026-09-22
по решению создателя**, см. §5); `docs/DEPLOY.md` §2 (локальный запуск);
`.opencode/agents/manager.md` (источник правил о моём процессе).

---

## 1. Пожелание создателя (2026-09-22)

> «подними все сервера наши» → «можно один экзешник, который все сервера поднимает?»
> → «или батник» → «батник пока».

Потребность за этим: одно действие вместо «вспомнить, что и в каком порядке запускать».
Сейчас локальный запуск — ручная последовательность: база → игровой сервер → генерация
картинок → учёт агентов → арт-студия, и каждое поднятие заново изобретает «как отвязать
процесс, чтобы он не умер вместе с консолью».

## 2. Решение (батник, не экзешник)

**Файл:** `start-all.cmd` в корне проекта (двойной клик). Причина выбора батника —
создатель: «батник пока»; аргумент менеджера: нулевая сборка, правится за секунду,
не устаревает; экзешник отложен (собирать из нашего кода, если понадобится тихий запуск
без окна).

**Поведение:**
- Порядок: база (с ожиданием готовности) → Redis (служба Windows) → игровой сервер →
  генерация картинок → учёт агентов → арт-студия.
- **Повторный запуск безопасен:** живоe пропускается (проверка порта), дублей слушателей
  не появляется.
- Каждый сервер поднимается в отдельном свёрнутом окне, независимо от окна батника.
- В конце — таблица «что получилось» по портам, адреса игры/студии/учёта и пути логов.
- Переключатели в начале файла: генерация картинок, учёт агентов, арт-студия (`1`/`0`);
  по умолчанию поднимается всё.
- Флаг `--no-pause` — не ждать нажатия клавиши (для запуска из скриптов).
- Погашения серверов в этом заходе нет (кнопка «погасить всё» — отдельно, если попросит).

**Что поднимается и где:** база — `C:\pgsql` (`pg_ctl`), Redis — служба Windows,
игровой сервер — `go run ./cmd/server` (ключи сессий берутся из `.env` сам),
картинки — `C:\ComfyUI` (venv, порт 8188), учёт агентов — `tools/agent-dash`, арт-студия —
`go run ./cmd/art-studio`.

## 3. Ловушка формата файла (найдено в работе, 2026-09-22)

Батник в репозитории **обязан** быть **CRLF + cp866 (OEM)**. Первый вариант был записан
UTF-8 с LF: `cmd.exe` рвёт парсинг (исполняет обрывки строк и русских слов как команды),
причём симптом неочевиден — вывод обрезан, ошибки вида `'порт' is not recognized`.
Кодировка консоли на машине — 866; в начале файла стоит `chcp 866` для страховки.

## 4. Что проверено вживую (менеджер, 2026-09-22)

- Холодный старт: погашены база и игровой сервер → батник поднял базу, дождался её,
  затем собрал и поднял игровой сервер; итог — шесть «ок».
- Повторный запуск при живых серверах: шесть «уже работает», повторных слушателей нет.
- Отдельно: погашен «учёт агентов» → батник поднял только его.
- После холодного старта `/health` → OK, `/status` → «All systems ready»; генерация
  картинок, учёт агентов и арт-студия отвечают 200.

**Независимый прогон @tester (2026-09-22, отчёт `docs/QA/checklists/2026-09-22_start-all-batnik.md`) — 5/5 PASS:**
повторный запуск при живых серверах ничего не поднимает и не плодит дублей слушателей;
погашенный «учёт агентов» поднимается один; погашенный игровой сервер поднимается за
≤75 с, `/health` → OK; вывод в cp866 читается, ошибок парсинга нет; после всех прогонов
все шесть портов живы, состав процессов не изменился.

**Побочный эффект (найден тестером):** при скриптовом запуске с редиректом вывода в файл
(`--no-pause > файл`) файл-приёмник остаётся заблокирован после завершения батника —
дескриптор наследуют отвязанные дочерние сервисы. На двойной клик (вывод в консоль)
не влияет; учитывать при автоматических прогонах.

## 5. Открытые вопросы к создателю

1. **Старый `run.ps1`** прописывал `JWT_SECRET` вручную — это ровно то, что запрещено
   правилом `AGENTS.md` §1 (каждый перезапуск с новым секретом = «сессия истекла» у всех
   залогиненных). Батник этой ошибки не имеет. **Решение создателя 2026-09-22: «убирай» →
   `run.ps1` удалён из репозитория** (заменён `start-all.cmd`, коммит `45d770e`). Ссылки на
   удалённый файл закрыты у их владельцев: `docs/DEPLOY.md` (§2 и §1 про секрет) —
   `@developer`, коммит `3824dee`; `AGENTS.md` §1 — `@dispatcher`, коммит `a8bcda8`;
   исключения в `scripts/check_impact_map.ps1` (моя зона) — вычищены. Исторические
   упоминания (`docs/gamedesign/ideas/_archive/…`, идеи 21b/58a, журналы QA, в т.ч.
   `docs/QA/checklists/2026-09-22_деньги-счёт-этап-A.md`) не трогаем — это записи о прошлом.
2. **Погашение серверов** одной командой — не делалось (создатель не просил).

## 6. Разнос по докам

**Обладатели и адресаты:** `docs/DEPLOY.md` и `docs/PITFALLS.md` → `@developer`
(единственный писатель по `docs/INDEX.md`); горячие доки (`AGENTS.md`, `CHANGELOG.md`) →
только `@dispatcher` (по прямому указанию создателя).

- `docs/DEPLOY.md` §2 (локальный запуск) → **ГОТОВО (2026-09-22, коммит `f29178b`)**,
  внесён абзац: «Локально удобнее всего поднимать всё батником `start-all.cmd` в корне проекта
  (двойной клик): база → игровой сервер → генерация картинок → учёт агентов → арт-студия;
  живоe пропускается, в конце печатается сводка портов; состав (картинки/учёт агентов/
  арт-студия) переключается в начале файла; прочие способы (`run.ps1`, ручной `go run`)
  остаются, но требуют поднять базу и Redis самому».
- `AGENTS.md` §1 (стек и запуск) → **ГОТОВО (2026-09-22, коммит `da53756`)**, строка:
  «Поднять все локальные сервера разом — `start-all.cmd` в корне проекта (двойной клик);
  подробности и переключатели состава — `docs/DEPLOY.md` §2».
- `CHANGELOG.md` → **ГОТОВО (2026-09-22, коммит `da53756`)**, запись за 2026-09-22,
  `Дополнительно: один батник start-all.cmd …`.
- `docs/PITFALLS.md` → **ГОТОВО (2026-09-22, коммит `20cee49`)** — ловушка про батники
  внесена в конец раздела «БД и шелл». Уточнение к разведке: метка 97a в `docs/COORDINATION.md`
  оказалась **исторической** (97a — «полёт игрока переживает рестарт», её правки давно
  закоммичены); файл де-факто держала незавершённая задача «корабли рас» — её правки лежали
  в рабочем дереве незакоммиченными (46+/29−, гейт задачи ещё не пройден). Внесено чистым
  частичным стейджем: в коммит попал ровно один хунк с ловушкой, корабельные правки
  остались в рабочем дереве нетронутыми (проверено `git diff --numstat` до/после — 46+/29−).
  Ловушка: «Батник в репозитории — только CRLF + cp866 (OEM) …» (текст — в `docs/PITFALLS.md`,
  раздел «БД и шелл»).
- `scripts/check_impact_map.ps1` (исключения реестра, зона `@manager`) — внесено этим же
  заходом: `start-all.cmd` добавлен в список исключений, чтобы хук
  post-commit не давал ложное «возможно, новая горячая точка: start-all.cmd».
