# Пояса малых тел — этап 2: показ, знание, полёт

**Дата:** 2026-09-22
**Вектор:** Игрок (показ/полёт) · Генерация (пояса)
**Статус:** ✅ реализовано 2026-09-22 (коммит `d36f5ce`), принято создателем. **Открыт довесок H3** (Restore полёта к поясу после рестарта — `cmd/server/main.go`): ждёт коммита линии «контракты» (держит файл) — вместе с ним применятся дельты в занятые доки.
**Спека:** `docs/specs/2026-09-22-пояса-малых-тел-этап-2-показ-знание-полёт.md`
**Предыстория:** продолжение линии «протопылевое облако». Этап 1 поясов сдан 2026-09-22 (коммит `b24e358`); базовая спека — `docs/specs/2026-09-21-пояса-малых-тел-объект-системы.md`.

**Порядок линии (выбран менеджером, создатель: «в том порядке, как удобнее»):**
пояса этап 2 (показ/знание/полёт) → пояса этап 3 (добыча-мини-игра) → облако этап 2 (состав/история).

## Пожелание создателя (дословно)

> «Продолжаем протопылевое облако разрабатывать»
> «в том порядке, как удобнее»
> «Не забыть про визуал только надо в будущих релизах. А так - го»

## Решения создателя 2026-09-22 (по развилкам спеки)

1. **Что открыто сразу:** тип, имя, геометрия, типичное тело и масса видны как объект системы; **состав — за сканом в радиусе/присутствием** (как у планет).
2. **Память о составе:** без отдельной таблицы — состав вычисляется на чтении (нет даты/TTL).
3. **Пояс — цель композитного маршрута (чужая система):** да, «лететь везде».
4. **Кольцо пояса на канвасе системы:** в этапе 2 не рисуется.

## Правило создателя (не забыть)

- **Визуал поясов — в будущих релизах.** Кольцо/полоса пояса на карте системы создатель просил не потерять. Отложено под спеку `2026-09-22-визуальная-система-размеров-и-сцена-подлёта.md` §4.3. При закрытии задачи передать напоминание дельтой (роадмап / спека размеров) — легаси-формулировок не оставлять (правило 99a).

## Решения менеджера

- **Миграция — `000065`** (перенумеровано 2026-09-22). Причина: `000062` занят контрактами, `000060` — компаньонами; `000063` и `000064` **фактически заняла линия «ветки поселений»** (её спека `2026-09-22-поселение-ветка-буферы-переработка.md` §1/§11: контракты `000062`, поселения `000063`→`000064`). Файл — `000065_belt_flight.sql`; свободных номеров ниже не осталось.
- **Замок координации (2026-09-22):** линия «контракты B1» держала `cmd/server/main.go` и `internal/handlers/visibility_handlers_test.go`, при этом сама ждала коммитов остальных. Разорвано так: показ поясов сделали **best-effort** (ошибка выборки поясов не роняет ответ модалки, только warn) — правки в чужом тест-файле не нужны, DoD зелёный; правку Restore в `main.go` (полёт к поясу переживает рестарт, H3) вынесли в **быстрый довесок** после коммита контрактов.

## Разнос по докам

**Применяет @dispatcher в паузу (горячие доки — дельтами). Применено мной в этом коммите — только `impact_map`.**

- **`CHANGELOG.md`** — `+2026-09-22, Реализовано: пояса малых тел — этап 2 (раздел «Пояса» в модалке системы, знание состава по скану в радиусе/присутствию, полёт к поясу `object_type='belt'`, миграция `000065`); Дополнительно: выборка поясов best-effort (сбой не роняет карточку системы).`
- **`STATUS.md`** — не менять (§1 — только текущее состояние; реализованная фича идёт в `CHANGELOG`).
- **`docs/impact_map.md` / `docs/impact_map.json`** — **применено этим коммитом**: рёбра узла `belt` → `77a` (знание состава), `99.2.30` (композитный `destination`), `planet_handler` (`belts` в ответе модалки), `planet_visibility` (`stripBeltDetails`), `intrasystem_flight`/`99.2.27`, `players_positions`, `modal` (`web/static/js/modal/*`), `map/data.js`; `system_belts` → `planet_handler` (читающая ручка `GetBeltsByWorldID`); `path` у `belt` дополнен потребителями этапа 2.
- **Заблокировано чужим локейтом (применить вместе с довеском H3):**
  - **`docs/DB.md`** — строка таблицы `player_intrasystem_flights`: «CHECK `from_type`/`to_type` включает `belt` (миграция `000065`, спека поясов этап 2 §5.3)»; в список миграций: «`000065_belt_flight.sql` — пояс — цель внутрисистемного полёта: CHECK `player_intrasystem_flights.from_type/to_type` расширен значением `belt` (спека поясов этап 2 §5.3).»
  - **`docs/ARCHITECTURE.md` §2 (карта файлов)** — «`internal/handlers/planet_visibility.go` — `stripBeltDetails`/`applyBeltVisibility` (BeltView пояса для игрока, спека поясов этап 2 §4.2/§4.3); `internal/repository/planet_repo.go` — `GetBeltsByWorldID` (читающая ручка поясов).»
  - **`docs/PITFALLS.md`** — новая ловушка «`object_type='belt'` — перечислители»: «Значение добавлено в `object_type` внутрисистемного полёта (спека поясов этап 2 §5.4). Точки, где значение перечисляется поимённо и обязано знать `belt`: `objectRadiusAU`, `targetInSystem`, `positionObjectValid`, `normalizeMyPosition`, `arrivalTargetValid`, `StartIntraFlight` (`intrasystem_handlers.go`), `resolveIntraStatus` (`players_positions.go`), `destinationInSystem`/`validateTravelDestination`/`autostartIntra` (`travel_handlers.go`), `switch objType` в Restore (`cmd/server/main.go`). Пропуск любой → пояс молча уходит в фолбэк «орбита звезды» или 400. Функции принимают `belts []models.Belt` рядом с `planets` — источник поясов обязателен в каждой точке.»
- **`docs/gamedesign/03_planets.md` §3.13** — дельта: пояс виден игроку в модалке системы (секция «Пояса»), состав — за сканом/присутствием; пояс — цель внутрисистемного и композитного полёта.
- **`.opencode/context.md`** — указатель: «Пояса малых тел — этап 2 (2026-09-22, коммит `d36f5ce`): секция «Пояса» в модалке, знание состава по скану/присутствию, полёт `object_type='belt'` (миграция `000065`); довесок Restore после рестарта — ждёт коммита контрактов; спека `docs/specs/2026-09-22-пояса-малых-тел-этап-2-показ-знание-полёт.md`».
- **`docs/COORDINATION.md`** — наша метка занятости снята (не коммитим — снята только в рабочем файле).
- **НЕ забыть (правило создателя):** визуал поясов (кольцо на карте системы) — в будущих релизах; отложено под `docs/specs/2026-09-22-визуальная-система-размеров-и-сцена-подлёта.md` §4.3 — напоминание передать дельтой в роадмап/спеку размеров.
