# 2026-09-22. Экономика поселения: залежи на поверхности + производственная ветка (входной/выходной буфер)

**Создано:** 2026-09-22
**Статус:** 🔄 эпик, план зафиксирован 2026-09-22. **Итерация 1 (залежи на поверхности) — ✅ реализовано 2026-09-22 (коммит `3208cad`).** **Итерация 2 (ветка + буферы + ленивая переработка) — ✅ реализовано 2026-09-22 (коммит `85f6539`), приёмка создателя получена, прогон @tester T1–T8 PASS** (миграция `000064`; чекпоинт — §7). Следующие итерации — §2. Отдельная задача «стабильный код + экспорт каталога на прод» — идея `2026-09-22_каталог-стабильный-код-экспорт-на-прод.md`.
**Приоритет:** высокий (ядро будущей экономики поселения)
**Вектор:** Поселения (ядро) · Генерация (ресурсы поверхности)
**Эталон:** не задан

**Связи:** спека `docs/specs/2026-09-20-фабрики-сущность-производства.md` (§11.4 — постройка «склад» не вводится, хранение внутренними буферами; §11.5 — `settlements` своя таблица); `docs/specs/2026-09-21-высадка-прогулка-по-планете.md` (добычи нет); биомы `99.2.28` + `config/biome_catalog.json` (у биома ресурсов нет); рецепты — миграция `000055` (`recipes`/`recipe_components`/`producer_recipes`); `producer_types.input/output`; `internal/economy/settlement/` (ленивый пересчёт); `internal/repository/economy_repository.go`; `99.2.5-resource-model.md` (planned `deposits`/`substances`); `internal/resource/generator.go`/`summary.go` (легаси-сводка `planets.data.resources`); `docs/gamedesign/13_tiers.md` §13.12, `13_settlements.md` §13.3/§13.3.0, `18_needs.md` §18.3.2, `_archive/94a-consumption-catalog.md` §7.2.

---

## 0. Пожелание создателя (2026-09-22, дословно)

Первичное:
> «Давай сделаем большой релиз: поселение людей, которое добывает пищу с
> поверхности, перерабатывает её, что-то идёт в рост и удовлетворение
> потребностей, что-то копится на их внутреннем складе.»

Уточнения в приёмной:
> «1 — просто сделать какую-то ветку в поселении, сгенерить под неё руду на
> поверхности, посадить туда поселение и убедиться, что оно начало его потреблять.
> 2 — песочница. 3 — NPC. 4 — фоновая, причём ленивая. 5 — органический ресурс
> биома, надо будет законфигать. 6 — встроена в поселение, как мы и планировали.
> Свой рецепт у поселения. 7 — пока просто буфер, добыча минус потребление.
> Дальше будем дорабатывать.»

Второе уточнение (разворот модели, дословно):
> «у типа поверхности («биома») появляется **таблица, какие ресурсы могут
> генерироваться** в этом биоме.»
> «на планете заводится залежь этого ресурса — каким образом? нужны подробности.»
> «у поселения — ветка со своим рецептом: она **уменьшает залежи ресурса и
> перерабатывает его** (что-то просто уничтожается, что-то идёт в буфер).»
> «Рецепт у ветки пока — ссылка-задел: состав в петлю не входит — **можно делать
> сразу, чтобы входил**.»
> «может вообще сделать сразу правильно — **входной буфер и выходной буфер**? На
> вход поселение будет получать ресурсы поверхности (видит только админ), на
> выход — то, что написано в рецепте (если есть входные ресурсы), а **добыча — это
> отдельная штука**? можем даже сейчас просто сделать в интерфейсе кнопку "добавить
> ресурсы" во входной буфер, не заморачиваться с добычей, **декомпозируем это**?»
> «**а. Залежи — конечно только если знаем.**»
> «Залежи — это тоже **объект**, генерируется на поверхности. Можем кстати с него и
> начать, и двигаться итеративно. Предлагаю **записать в идею весь список** того,
> что нам надо сделать, и потихоньку несколькими сессиями двигаться по нему.»

Третье уточнение (фиксация плана, дословно):
> «1 — да, причём я думаю давай это зафиксируем, и **с чистой сессии начнём**.
> 2 — да, **с конечным**. И **дайте мне инструмент добавить залежь на планету**.
> 3 — только пилотный **один ресурс — пища**.»

**План зафиксирован 2026-09-22.** Итерация 1 стартует **с чистой сессии**.
Пилотный ресурс — **пища** (один).

## 1. Направление (что изменилось от первой редакции)

| Было (первая редакция) | Стало (решение создателя) |
|---|---|
| У биома — один ресурс (`surface_resource`) | У биома — **таблица** ресурсов, которые могут генерироваться (ресурс + шанс + богатство) |
| Один «припас ветки»: добыча − потребление | **Входной и выходной буфер** у ветки; рецепт реально превращает вход в выход |
| Рецепт — ссылка-задел | Рецепт **входит в петлю** (состав) — делать сразу |
| Добыча — часть ветки | **Добыча — отдельная механика**; декомпозируем: сначала кнопка «добавить ресурсы» во вход, без добычи |
| Залежь появляется в админ-действии | Залежь — **объект, генерируется при генерации планеты** из биомов; с этого и начинаем |
| Утечка залежи игроку (жёсткий стоп критика) | **Вариант «а»:** залежь видна только если планету знаем |

## 2. ЭПИК — список работ по итерациям (трекер)

### Итерация 1 — Залежи на поверхности — ✅ реализовано 2026-09-22 (коммит `3208cad`)
- **1.1.** Конфиг биома: **таблица ресурсов**, которые могут здесь генерироваться. Поля записи: ресурс каталога + шанс/вес появления + диапазон богатства. Один биом — много ресурсов. **Для пилота — один ресурс: пища.**
- **1.2.** Генерация залежей при генерации планеты: из биомов поверхности (доля/площадь биома → сколько залежей и насколько жирных).
- **1.3.** Залежь — **объект**: планета + ресурс + слой (поверхность) + богатство + **запас** (конечный, потом расходуется добычей).
- **1.4.** Видимость: залежь показываем **только при знании планеты** (как прочие детали).
- **1.5.** Просмотр: в админке (для песочницы) и/или на карточке планеты при знании.
- **1.6.** **Инструмент админа: добавить залежь на планету вручную** (требование создателя — чтобы можно было поставить залежь без генерации).

### Итерация 2 — Ветка поселения: входной и выходной буфер + рецепт — 🚧 реализовано 2026-09-22 (не закоммичено; тест и приёмка — следующая сессия)
- **2.1.** У поселения — ветка со ссылкой на **рецепт каталога** (веток может быть несколько; пилот — одна).
- **2.2.** **Входной буфер** (ресурсы, видит только админ) и **выходной буфер** (то, что даёт рецепт).
- **2.3.** Рецепт реально превращает вход в выход (состав `recipe_components` входит в петлю).
- **2.4.** Админ-действие **«добавить ресурсы»** во входной буфер — без добычи.
- **2.5.** **(уточнение 2026-09-22)** Переработка вход → выход идёт **фоном по времени** (ленивый пересчёт, свой чек-поинт) — фон переехал из итерации 4 в итерацию 2 по решению создателя.

### Итерация 3 — Добыча
- **3.1.** Ветка черпает из залежи во входной буфер и **уменьшает запас залежи**.
- **3.2.** Ленивый пересчёт добычи по времени (тот же приём, что население/полёты — без тика).

### Итерация 4 — Потребление населением
- **4.1.** **(уточнение 2026-09-22)** Ленивая переработка вход → выход уже сделана в итерации 2; здесь остаётся **потребление**: часть **уничтожается** (на людей), часть копится в **выходном буфере**.
- **4.2.** Видимая петля в карточке: вход / выход / что уничтожено / запас.

### Позже (за пределами итераций 1–4)
- Рост населения и удовлетворение потребностей (корзина/стабильность/терпение) — канон `18_needs`/`13_tiers`.
- Истощение залежей, видимое на поверхности (в «прогулке»).
- Постройка «склад»/автономия (канон-конфликт — см. §6).
- Рынки; расовые ветки; компонентная переработка во всей глубине.
- Лестница/связь с фабриками (`producer_types`/`producer_recipes`) — при надобности.

## 3. Решения создателя (свод)

1. **Песочница**, NPC-поселение людей (F0), не игрок.
2. **Биом → таблица** возможных ресурсов (не один).
3. **Залежь — объект**, генерируется на поверхности вместе с планетой; начинаем с неё.
4. **Ветка поселения** ссылается на рецепт; у ветки **входной и выходной буфер**.
5. Ветка **уменьшает залежь** и **перерабатывает**: часть уничтожается, часть в буфер.
6. **Рецепт входит в петлю** сразу (не задел).
7. **Добыча — отдельная механика**; на ближайшей итерации — ручная кнопка «добавить ресурсы» во вход.
8. **Залежи видимы только при знании планеты** (вариант «а», снят жёсткий стоп).
9. Идём **несколькими сессиями** по списку §2 (этот документ — трекер).
10. Рост/потребности — позже.
11. Залежь — **с конечным запасом** (расходуется добычей).
12. Нужен **инструмент админа: добавить залежь на планету** вручную.
13. **Пилотный ресурс — один: пища.**
14. **План зафиксирован 2026-09-22; итерация 1 стартует с чистой сессии.**

### Уточнения после гейта итерации 1 (2026-09-22, дословно)

15. **Правило «площадь биома → число/жирность залежей» отменено** — «*некорректное, это сразу легаси*». Генерация залежей — **случайное число**, биомы не участвуют. (Отменяет п.1.2 и часть п.2.)
16. **Таблица ресурсов биома в итерации 1 не вводится** — «*нам пока нужны только сущности, конфигать что где потом будем*». Биом-конфиг (`biome_catalog.json`) не трогаем; задача конфигурации «что где» — позже.
17. **Пилотных ресурсов — три: `мясо`, `вода`, `растения`** (создатель уже завёл: «Растения» 358, «Мясо» 359, «Вода-ресурс» 1). Пункт 13 (один ресурс «пища») — **заменён**; «пища»/«Еда» — товар переработки.
18. **Удаление ресурса каталога удаляет его залежи** (ответ «б»): FK `deposits.good_id ON DELETE CASCADE`.
19. **«Осадок» к залежам отношения не имеет** — «*результат сверхпереработки ресурса (напр. мясо) в товар "Еда"*» (итерации 2–4). Противопоставление «запас залежи vs осадок» снимается.
20. **Канон «свой ресурс не перерабатывается в товар» (94a §7.2) устарел** (решение создателя) — переработка ресурса в товар нормальна.

**Прежний открытый вопрос к дизайнеру («пища») — закрыт:** пилот — три существующих ресурса (п.17), «пища»/«Еда» — товар переработки (п.19).

### Уточнения приёмной 2026-09-22 (итерация 2, старт)

21. **Каталог: вода в рецепте.** `recipe_components` рецепта «Пища» (id 69) переведён с ресурса «Вода H₂O» (id 94) на «вода-ресурс» (id 1), лишний ресурс «Вода H₂O» **удалён** из каталога (вариант «а» создателя: «*проще поправить рецепт, лишний удалить*»). Выполнено менеджером в БД 2026-09-22; «Вода H₂O» больше нигде не использовалась. Рецепт «Пища» = **Мясо + вода-ресурс → Пища** (сложность 1).
22. **Превращение вход → выход — по времени** (лениво), а не по кнопке: ответ создателя на вопрос о моменте переработки — «*по времени*». Фон переезжает из итерации 4 в итерацию 2 (§2).
23. **Веток у поселения — несколько** может быть; пилотный прогон — **на одной** («*веток несколько может быть, но пока прогоним на одной*»).

## 4. Факты (сверка с кодом, 2026-09-22)

- **Поселение** — `settlements` (`planet_id, population, population_exact, stability, computed_at, race_id`; миграции `000018/19/20/21/24/39`). Модель `internal/models/economy.go`, доступ `internal/repository/economy_repository.go` (ленивый синк под `FOR UPDATE`). Полей владельца/тира/склада нет.
- **Пересчёт населения** — `internal/economy/settlement/` (R-модель от среды; `ChangeComponents` — точка сборки; `MinPersistInterval` = 30 мин). С едой не связан.
- **Ресурсы** — `internal/resource/` (слой 20, категории, хемотипы, диета) + каталог БД `goods.kind='resource'` (131). Продовольствие намеренно исключено из слоя. Органические ресурсы в каталоге уже есть (`органика-ресурс`, `морская биомасса`, `органические аэрозоли`).
- **Поверхность** — `internal/handlers/surface_handlers.go` (прогулка), `web/surface.*`; добычи/инвентаря нет. У `BiomeDef`/`config/biome_catalog.json` полей ресурсов нет.
- **Производство** — `producer_types` (`input/output/params`), `items`, `producer_items`, `recipes`/`recipe_components`/`producer_recipes` (000055), `producer_slots` (000052), `buildings` (000056 — только столицы). Игровой симуляции нет.
- **Легаси-сводка ресурсов планеты** — `internal/resource/generator.go`/`summary.go` → `planets.data.resources` (категория → богатство); читается картой (`mapcache/snapshot.go`) и силой фракций (`generator/faction/faction.go`). Новая залежь не должна её молча дублировать (см. §6).
- **Две ленивые петли** — на `settlements.computed_at` уже живёт население; новая петля должна иметь свою чек-точку.

## 5. Модель (предложение; финализирует @designer на итерацию)

- **Ресурсы биома:** таблица (в конфиге биома или отдельная сущность) «биом × ресурс каталога × шанс × диапазон богатства».
- **Залежь (`deposits`)** — объект на планете: `planet_id, good_id, stratum('surface'), wealth, amount` (запас). Состыковать с planned `99.2.5`.
- **Ветка поселения (`settlement_branches`)** — ссылка на рецепт + **буфер входа** и **буфер выхода** (форма — JSONB или таблица, за дизайнером) + скорость переработки.
- **Петля** — ленивая по Δt: добыча (итерация 3) → вход → рецепт → выход; часть уничтожается (население).

## 6. Находки @critic (перенесены; статус после решения создателя)

- 🔴 **Утечка залежи игроку** (жёсткий стоп) — **снято создателем:** вариант «а», видимость только при знании. Учесть фильтр деталей планеты.
- 🟡 **Склад/буфер канон-конфликт** (`18_needs` §18.3.2 «запас расширяется складами» vs §11.4 «склад не вводится») — **отложен** (позже).
- 🟡 **Две сводки ресурсов поверхности** (легаси `planets.data.resources` vs новая залежь) — не дублировать; свести позже. Для итерации 1 — решить, читает ли залежь что-то из легаси.
- 🟡 **Удаление ресурса/рецепта в студии** под ссылкой из залежи/ветки — запрет удаления (предложение).
- 🟡 **«Корм есть не везде»** (`13_settlements` §13.3.2) — не проверяется; отложить для песочницы.
- 🟡 **География** («биом → фиксированный ресурс»): в новой модели ослабляется уже меньше — биом даёт *набор* ресурсов; распределение биомов даёт географию.
- 🟡 **`goods` vs planned `substances`** (`99.2.5`) и **место легаси-сводки** — решить при состыковке модели.
- **Мелкие** (термин «буфер»/«припас», ярлык «руда» у органики, `kind='resource'` CHECK, уникальность веток, валидация биом-конфига) — правим сами.

## 7. Маршрут

- Идея обновлена под эпик (этот документ).
- **Итерация 1:** переработать спеку `2026-09-22-...` под «залежи» (таблица ресурсов биома + генерация + объект залежи + видимость) → @critic по спеке → гейт → @developer (миграция **000058**) → @tester → сдача. ✅ (коммит `3208cad`)
- **Итерация 2 (текущая):** каталожная правка (п.21) ✅ → спека @designer (ветка + входной/выходной буфер + рецепт + фоновая переработка) → @critic по спеке + @balancetester (числа) → **возврат @designer** (балансировщик дал В1: число скорости не бьётся с реальным масштабом поселений 1e5–1e9) → гейт (создатель: вариант A, «самохозяйство» — задумка, «го») → @developer (миграция **000064**) → @tester → сдача.
  - **Номер миграции — `000064`** (менеджер, 2026-09-22). `000061` — деньги, `000062` — контракты, **`000063` уже занят задачей «пояса малых тел, этап 2»** (`000063_belt_flight.sql`, файл создан раньше и залочен в `docs/COORDINATION.md`) — уступаем, берём `000064`. Первоначальная бронь `000063` за этой задачей отменена.
- Далее итерации 3–4 по §2.

### Чекпоинт 2026-09-22 (продолжение — следующая сессия)

**Где стоим.** Итерация 2 **реализована** (код на диске, **НЕ закоммичен**). Спека `docs/specs/2026-09-22-поселение-ветка-буферы-переработка.md` (статус: дизайн согласован создателем, к разработке). Решения создателя на гейте: скорость — **вариант A** (линейная от населения), «самохозяйство» — задумка песочницы, «го» на разработку дано. @critic — блокеров нет; @reviewer — 2 важных + 3 мелких, **все исправлены**; DoD (`build`/`vet`/быстрый `-race` по нашей зоне) — зелёный.

**Реализовано:** таблицы `settlement_branches` / `settlement_branch_buffers` (миграция **`000064`**, применена к dev-БД, поясной `000063` не заблокирован), чистая функция переработки (`internal/economy/settlement/branch.go`), репозиторий с `FOR UPDATE OF b`, админ-ручки `POST /admin/settlements/{id}/branches` и `POST /admin/branches/{id}/input`, видимость (вход — только админ), предупреждение студии числом веток, фронт-блок в карточке (`modal/branches.js`).

**Файлы (наши).** *Новые:* `migrations/000064_settlement_branches.sql`, `internal/economy/settlement/branch.go` (+тест), `internal/repository/branch_repository.go` (+тест/хелпер), `internal/handlers/admin_settlement_branches.go` (+тест), `web/static/js/modal/branches.js`, `web/frontend_branches_test.go`, спека. *Изменённые:* `internal/models/economy.go`, `internal/repository/planet_repo.go`/`goods_repository.go` (+тесты), `internal/handlers/planet_visibility.go`/`admin_universe.go`/`studio_handlers.go` (+тесты), `cmd/server/main.go` (блок роутов), `web/static/js/modal/tabs.js`, `docs/COORDINATION.md`.

**Не сделано (следующая сессия):**
1. `@tester` — независимый прогон, в т.ч. живой сценарий (ветка «Пища»: добавить Мясо + Воду → вход убыл, выход вырос), конкурентность (`FOR UPDATE`), каскады T12, top-up T14.
2. Гейт теста → приёмка создателя.
3. **Коммит** (после приёмки): код — developer явными путями, идея/спека — менеджер; разнос доков (§8) + @dispatcher.
4. ⚠️ **Общие файлы с параллельной задачей «пояса этап 2»** — `cmd/server/main.go` и `internal/handlers/planet_visibility.go` правят обе задачи: коммитить аккуратно, явными путями, не захватывая чужое.

**Почему без коммита:** правило «коммит — после полной приёмки» + в рабочем дереве недокоммиченные правки ещё нескольких параллельных задач (пояса, деньги/контракты, корабли). Работа не потеряется — она на диске; продолжим в следующей сессии с этого места.

### Чекпоинт (обновление 2026-09-22, сессия продолжения)

**Прогон @tester — зелёный, приёмка получена.** Отчёт: `docs/QA/checklists/2026-09-22_поселение-ветка-буферы-переработка.md`. Контрольные точки T1–T8 — **PASS**, ни одного FAIL/SKIP. Живой сценарий ветки «Пища»: вход 1000 → 984.44, выход 0 → 15.56, `processed_at` продвинулся; конкурентность (админский инкремент vs синк) — без потерь и минуса; каскады (component_id → NULL; удаление рецепта сносит ветку) — верны; границы ручек 404/409/422 — верны; видимость (player `Input` обнулён, обхода нет) — верна; top-up нового компонента — работает; e2e-смоук `tools/e2e/map-check.js` + целевой админ-смоук — PASS (0 JS-ошибок, 0 тостов, без редиректа). Тестовые данные удалены, dev-сервер перезапущен на текущем коде.

**Вердикт:** принимаем, багов не найдено.

**Порядок коммитов (решение создателя 2026-09-22):** сначала коммит задачи «пояса малых тел, этап 2», затем «контракты/деньги», **только потом — наш**. Причина: четыре общих файла содержат чужие недокоммиченные хунки, и при нашей сдаче первыми их нельзя отделить без риска:
- `cmd/server/main.go` — наши 2 роута веток + правки контрактов/денег (снос `assignmentRepo`/`testHandlers`/роутов страниц);
- `internal/handlers/planet_visibility.go` — `stripBranchInputs` (наши) + `applyBeltVisibility`/`stripBeltDetails`/`beltPresence` (пояса);
- `internal/handlers/admin_universe.go` — 2 таблицы веток в `truncateTables` (наши) + `system_belts` (пояса) + эскроу/`money_operations` (деньги);
- `internal/repository/planet_repo.go` — `attachBranches` (наши) + `GetBeltsByWorldID` (пояса).

**Коммит выполнен 2026-09-22 — `85f6539`.** Соседи закоммитились первыми («пояса малых тел, этап 2» — `d36f5ce`; «контракты/деньги» — `6fb1fe2` + `58133a3`); проверка показала, что в четырёх общих файлах после них остались **только наши** хунки. Код закоммитил @developer явными путями (24 файла: 10 новых + 14 изменённых), чужого не захвачено; `go build`/`go vet` зелёные (полный DoD с `-race` прогнан @tester на том же дереве). Post-commit hook impact-map: ERROR 0, WARNING 8 (чужие доки: пояса/погода/деньги/контракты).

**Не сделано:**
1. Разнос доков (§8, дельта итерации 2 ниже) + `@dispatcher` (горячие доки) — запускает создатель.

## 8. Разнос по докам

**Итерация 1 (2026-09-22, коммит `3208cad`).** Дельты для @dispatcher (горячие доки) и владельцев доков; дословно.

- `CHANGELOG.md`: +2026-09-22, `Реализовано: залежи поверхности (итерация 1 эпика «экономика поселения») — таблица deposits (миграция 000058: планета + ресурс каталога + слой surface + богатство + конечный запас, FK CASCADE), случайная генерация залежей из трёх пилотов каталога (растения/мясо/вода-ресурс) на планетах с поверхностью, видимость только при знании планеты, админ-инструмент «добавить залежь», предупреждение о каскаде залежей при удалении ресурса в студии; биомы в генерации не участвуют.`
- `99_roadmap.md` §99.2: + строка «Экономика поселения: залежи на поверхности (итерация 1) — ✅ реализовано 2026-09-22 (коммит `3208cad`); далее итерации 2–4 (ветка/буферы/петля)».
- `STATUS.md` §Открытые решения: строку про флейкость `TestBiosphereNicheDominance` заменить на «тест сделан устойчивым (распределение по seed'ам, 2026-09-22); генераторная причина (порядок итерации map) остаётся — `PITFALLS` «Дизайн и числа»».
- `.opencode/context.md` §Фокус: «активная задача — залежи поверхности (итерация 1) сдана, коммит `3208cad`; следующая — стабильный код каталога + экспорт на прод, затем итерация 2 экономики». §Указатели для будущих задач: + «Залежи поверхности (итерация 1): таблица `deposits` (000058), генерация из трёх пилотов каталога (резолв по `name_norm`), видимость со знанием, админ-ручка `POST /admin/planets/{id}/deposits`; ветка/буферы/петля — итерации 2–4».
- `docs/gamedesign/faq_counter.md`: +2 (сколько залежей рождает планета; диапазоны богатства/запаса залежи) — счётчик 0 → 2.
- `docs/GLOSSARY.md`: + термины «залежь», «пилотный ресурс», «богатство залежи», «запас залежи»; строку «Осадок» переопределить — «результат сверхпереработки ресурса в товар (напр. „Еда“), к залежам не относится» (решение создателя 2026-09-22).
- `docs/DB.md`: + запись миграции `000058` и таблицы `deposits` (задержано — файл занят незакоммиченной дельтой «денег»; добавить, когда освободится).
- `docs/ARCHITECTURE.md` §2: + строка «Залежи поверхности» (`deposit_generation.go`, `deposit_repository.go`, `admin_deposits.go`, `models/deposit.go`, `web/static/js/modal/deposits.js`) — задержано (файл занят «деньгами»).
- `docs/specs/99.2.5-resource-model.md`: + пометка «`deposits` уже заведена итерацией 1 экономики (минимальный набор, миграция 000058); `99.2.5` расширяет (выработка/сводка)».
- `docs/gamedesign/09_resources.md` §9.2.6/§9.2.7: + пометка «залежь на поверхности (итерация 1): планета → конкретные ресурсы-залежи; биом→ресурсы — отдельная задача конфигурации».
- Канон «осадок»/переработка: `docs/gamedesign/13_tiers.md` §13.12/§13.12.2/§13.12.3, `13_settlements.md` §13.3.0, `_archive/94a-consumption-catalog.md` §7.2 — пометить устаревшими/переопределёнными решением создателя 2026-09-22 (осадок — сверхпереработка ресурса в товар; «свой ресурс не перерабатывается в товар» — устарело).
- `docs/specs/README.md`: строка манифеста нашей спеки в HEAD **отсутствует** (проверено 2026-09-22) — строка нужна, см. дельту итерации 2 ниже.
- `docs/impact_map.json`: ✅ внесено менеджером (сущности `deposits` (table), `surface_deposits` (mechanic); ребро `goods → deposits`).

**Итерация 2 (2026-09-22, коммит `85f6539`).** Дельты для @dispatcher (горячие доки) и владельцев доков; дословно.

- `CHANGELOG.md`: +2026-09-22, `Реализовано: ветка поселения (итерация 2 эпика «экономика поселения») — таблицы settlement_branches / settlement_branch_buffers (миграция 000064), входной и выходной буфер ветки, ленивая переработка вход → выход по прошедшему времени (скорость линейна от населения, вариант A; числа не калиброваны), админ-ручки «создать ветку» и «добавить ресурсы во вход», вход видит только админ, блок веток в карточке поселения, предупреждение студии числом веток; пилот-рецепт «Пища» (Мясо + вода → Пища); добыча из залежи и потребление — итерации 3–4.`
- `99_roadmap.md` §99.2: строку «Экономика поселения: залежи на поверхности (итерация 1) — ✅ реализовано 2026-09-22 (коммит `3208cad`); далее итерации 2–4 (ветка/буферы/петля)» дополнить: «итерация 2 (ветка + входной/выходной буфер + ленивая переработка) — ✅ реализовано 2026-09-22 (коммит `85f6539`); далее итерации 3–4 (добыча из залежи, потребление населением)».
- `.opencode/context.md` §Фокус: «активная задача — ветка поселения (итерация 2 эпика «экономика поселения») сдана, коммит `85f6539`; следующие — итерации 3–4 (добыча, потребление)». §Указатели: + «Ветка поселения: таблицы `settlement_branches` / `settlement_branch_buffers` (000064), переработка — `internal/economy/settlement/branch.go` (линейная, k = 2.78·10⁻⁸, не калибровано, `MinPersistInterval` 30 мин), админ-ручки `POST /admin/settlements/{id}/branches` и `POST /admin/branches/{id}/input`, вход обнуляется `stripBranchInputs`».
- `docs/GLOSSARY.md`: + термины «ветка поселения», «входной буфер», «выходной буфер», «переработка ветки», «чек-точка ветки»; уточнение к «буферу» — при употреблении всегда с пометкой «входной/выходной буфер ветки».
- `docs/DB.md`: + запись миграции `000064_settlement_branches.sql` и двух таблиц (`settlement_branches`, `settlement_branch_buffers`, каскады settlement/recipe/good).
- `docs/ARCHITECTURE.md` §2: + строка «Ветка поселения» (`internal/economy/settlement/branch.go`, `internal/repository/branch_repository.go`, `internal/handlers/admin_settlement_branches.go`, `internal/models/economy.go` (`SettlementBranch`/`BranchBufferEntry`), `web/static/js/modal/branches.js`).
- `docs/gamedesign/faq_counter.md`: +2 (вопросы: «почему скорость переработки ветки зависит от населения и сложности рецепта»; «есть ли потолок у выходного буфера» — потолка нет, отложен в итерацию 4). Файл сейчас = `0`: дельта итерации 1 (+2) **не применена** — при применении обеих счётчик `0 → 4`.
- `docs/gamedesign/13_tiers.md` §13.12.3: + пометка «выход NPC-ветки — „пол“ (низкое качество, не берёт заказы „не меньше N“); поле качества не заводится до итерации 4».
- `docs/specs/README.md`: + строка манифеста спеки итерации 2 — «`2026-09-22-поселение-ветка-буферы-переработка.md` | Ветка поселения — итерация 2 эпика «Экономика поселения»: таблицы `settlement_branches`/`settlement_branch_buffers`, ленивая переработка вход → выход (линейная скорость, вариант A), админ-ручки, вход виден только админу, выход — «пол»; миграция `000064` | ✅ реализовано 2026-09-22 (коммит `85f6539`) | 2026-09-22 | высокий |» (задержано — файл занят незакоммиченной дельтой «второй пакет погоды»; наша строка сейчас лежит в её рабочем дереве).
- `docs/impact_map.json`: ✅ внесено менеджером (сущности `settlement_branches`, `settlement_branch_buffers` (table), `settlement_branch_processing` (mechanic); рёбра к `settlements`, `recipes`, `goods`, `internal/goodsstudio/graph`).
- `docs/QA_CHECKLIST.md`: ✅ внесено менеджером (кейс `B-ECO-9` — ветка поселения; проверка 2026-09-22, зона «экономика/поселения»).

**Открытый вопрос «стабильный код + экспорт каталога на прод»** — отдельная идея `docs/gamedesign/ideas/2026-09-22_каталог-стабильный-код-экспорт-на-прод.md` (следующая задача; залежи ссылаются на пилоты по `name_norm` до её появления).

## 9. Ретроспектива

(при превышении бюджета итераций)
