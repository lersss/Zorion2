# 2026-09-23 — Мир прогулки: рельеф, декор и вода по конкретному биому (эпик)

**Создано:** 2026-09-23
**Статус:** 💡 сырая идея → эпик; поставлен в очередь (решение создателя: «сначала хвосты»)
**Приоритет:** большой заход; **после текущих хвостов** (решение создателя 2026-09-23)
**Вектор:** Генерация + Визуал (мир прогулки — клиент `web/static/js/surface/*` + справочник биомов)
**Эталон:** не задан; критерий приёмки — «офигеть как вы придумали» (создатель), см. §4

**Связи:**
- спека `docs/specs/2026-09-21-высадка-прогулка-по-планете.md` §7.2 (ландшафт/формации),
  §7.3 (жизнь), §7.4 (физика/плавание), §5.3 («высокий разброс — формациями внутри биома,
  не сменой биома»), В9 (формации — **клиентская константа по `biome_category`**; per-biome —
  будущий кандидат каталога — **эта идея его реализует**);
- спека `99.2.28-biomes-planet-surface.md` §3.1 (`planets.data.biomes` `{form, share}`),
  справочник `config/biome_catalog.json` (57 биомов, 6 категорий, `params`), админка
  «Основное» (правка справочника), hot-reload store `internal/generator/planet/biome_catalog.go`;
- идеи: `2026-09-21_поверхность-баги-и-вид.md` (графика/погода прогулки),
  `2026-09-21_поверхность-полная-поверхность-и-вращение-планеты.md` (карта поверхности);
  `2026-09-22_второй-пакет-погоды-поверхности.md`;
- код: `web/static/js/surface/surface_world.js` (`terrainHeight`/`isSolid`/`decorAt`/
  `creaturesFor`), `surface_config.js` (`FORMATIONS`, `LIFE_DENSITY`), `surface_player.js`
  (`waterY`), `surface_render.js` (`getChunkCanvas`/`drawTerrain`/`drawDecor`),
  `web/static/js/surface/surface_net.js`, `internal/handlers/surface_handlers.go` (пакет прогулки);
- impact map: сущность `surface_weather`, ребро `surface_landing → surface_weather`.

---

## 0. Решение создателя (дословно, 2026-09-23)

> «у нас в прогулке одинаковый рельеф, не зависит от типа поверхности. Это некрасиво,
> когда в биоме "океан" ты ходишь по поверхности, а "пустыня" вообще не пустыня.
> В общем, нужна переработка.»

Ответы на уточнения:

1. **«в эпике — большой заход, но побей на чекпоинты»**
2. **«нужно чтобы было Красиво и достоверно»**
3. **«океан и вообще водные — последним, подумаем. может лодку выдадим надувную»**
4. **«нужно сделать прям качественно, пока не знаю как. Будем переделывать пока я не скажу
   "офигеть как вы придумали". И лучше рано, чем поздно :)»**
5. **«сначала хвосты»**
6. **«На месте посадки ставь корабль игрока.»**

---

## 1. Пожелание

Мир прогулки должен читаться как **конкретный биом**, в который игрок высадился, —
красиво и достоверно. Сейчас рельеф одинаков и не зависит от типа поверхности:
в «океане» ходишь по суше, «пустыня» не пустыня. Нужна переработка.

---

## 2. Диагноз (факт из кода, приёмная 2026-09-23)

1. **Рельеф задан категорией, а не биомом.** `SurfaceWorld` берёт ключ
   `pkg.biome_category`, `FORMATIONS` — 6 наборов на 6 категорий
   (литосфера / вода / биосфера / вулканизм / крио / экзотика). В справочнике
   **57 биомов**, разложенных по этим 6 категориям:
   литосфера — 12, биосфера — 13, вода — 10, вулканизм — 7, крио — 5, экзотика — 10.
   Следствие: все 12 «литосферных» биомов (горы, пески пустыни, кратеры, каменные
   пустоши, соляные чаши, раковинная пустыня, танцующие пески, грозовая степь,
   углеродные нагорья, металлорудные возвышенности, стеклянные поля, металлические
   поля) получают **одну и ту же** «болванку» рельефа (хребет/каньон/плато/висячие
   скалы/арки/осыпи). Отсюда «пустыня — не пустыня».
2. **Декор тоже по категории.** `decorAt` даёт лишь 4 вида (tree/plant/lichen/rock),
   плотность — `LIFE_DENSITY` по категории. В пустыне/на «металлических полях» при
   `life=true` могут расти деревья.
3. **Воды на экране нет вообще.** `surface_player.js` считает `waterY = baseY + 170`
   для всей категории «вода» (плавание — замедление), но **нигде не рисуется жидкость**;
   по факту игрок ходит по суше под видом океана (grep по `water|liquid|океан` в
   `web/static/js/surface/*` — только физика/погода, отрисовки жидкости нет).
4. **Корень (тот же класс «один факт — одно место»):** форма рельефа, декор и вода
   описаны **шестью общими наборами**; конкретный биом в них не участвует.

---

## 3. Модель (предложение менеджера; гипотеза к проверке @designer)

**Не 57 отдельными генераторами** (дублирование, дорого, расходится) — **грамматика
форм + рецепт на биом**:

- **Библиотека примитивов рельефа** (переиспользуемые модуляторы): дюны/барханы,
  шпили, террасы, каньоны, хребты, арки, висячие скалы, осыпи, лавовые потоки,
  гейзерные конусы, ледниковые трещины, торосы, болотные кочки, кристаллические жилы,
  грибные башни, воронки, плато… Каждый примитив — уже существующие оси
  (`ridge`/`flatten`/`offset`/`caves`/`float`) плюс, при нужде, свои параметры.
- **Библиотека декора** (что стоит/растёт на земле): деревья, кактусы, грибы,
  кристаллы, лишайники, оазисы, трубы/гейзеры, наросты, обломки, окаменелости…
- **Рецепт биома** = набор форм + набор декора + материал/цвет поверхности +
  плотность жизни + (позже) жидкость. Хранится **в справочнике биомов** (данные;
  правится в админке, как остальной справочник) — **не второй константой в коде**
  (иначе два источника правды разъедутся — прямое следствие В9 спеки прогулки).
  Клиент получает рецепт в **пакете прогулки** (аддитивно).

Проверить с @designer: достаточно ли грамматики на 57 биомов, сколько примитивов
реально нужно, не проще ли часть биомов свести к «семействам вида».

## 4. Эпик и чекпоинты

**Критерий приёмки каждого чекпоинта — «офигеть» (создатель).** Число итераций
заранее не ограничиваем (решение создателя 2026-09-23: «будем переделывать, пока
не скажу офигеть»); при превышении бюджета 3 — вынос создателю по правилу
ретроспективы (решает он).

| ЧК | Что | Биомы | Команды |
|---|---|---|---|
| **ЧК0** | **Направление и образцы.** 2–3 контрастных сухопутных биома (напр. горы, пески пустыни, лес/джунгли) — варианты вида: рельеф + декор + материал/цвет. Цель — утвердить **подход/грамматику** до раскатки на все 57 | 2–3 | @gdesigner (арт-направление) + @designer (структура/данные) + @scientist (достоверность форм) |
| **ЧК1** | Литосфера | 12 | @designer → @developer → @tester |
| **ЧК2** | Биосфера | 13 | @designer → @developer → @tester |
| **ЧК3** | Вулканизм | 7 | @designer → @developer → @tester |
| **ЧК4** | Крио | 5 | @designer → @developer → @tester |
| **ЧК5** | Экзотика | 10 | @designer → @developer → @tester |
| **ЧК6** | Вода/океан | 10 | **последним** — решение о плавании/лодке отложено (§6) |
| **ЧК-ship** | **Корабль игрока на месте посадки** (§5) | — | @developer (+ @uidesigner?) — мелкий, можно раньше |

**Порядок:** ЧК0 обязателен первым (создатель не знает, «как» — подход ищем на
образцах). Категории раскатываются по одной, каждая — свой гейт.
**ЧК-ship** независим от рельефа; может быть сделан раньше как быстрый видимый
результат (ждать решения создателя).

---

## 5. Корабль на месте посадки (пожелание создателя 2026-09-23)

На месте высадки (точка спавна прогулки) показывать **корабль игрока** — визуальный
якорь «я только что сел здесь», а не «я возник из ниоткуда».

Технические вопросы для @designer/@developer:
- откуда брать спрайт: у игрока есть `ship_model`/`ship_sprite` (реестр
  `web/static/js/map/ship_sprites.js`) — либо добавить поле в пакет прогулки
  (аддитивно; сейчас в пакете корабля нет), либо клиент тянет из уже доступных данных;
- корабль — декорация в мире (не интерактивен) или точка, к которой «вызов корабля»
  визуально привязан;
- поведение при взлёте/смене биома, при бесконечной прогулке (остаётся в 0?).

---

## 6. Открытые вопросы

- **Вода/океан — отложено (решение создателя 2026-09-23):** «последним, подумаем.
  может лодку выдадим надувную». Развилка «плавание/погружение vs берег + лодка» —
  решается на ЧК6, не раньше.
- **«Красиво и достоверно» — как именно, создатель пока не знает** («пока не знаю
  как»). Это содержание ЧК0: искать подход на образцах и итерировать.
- Нужен ли `@scientist` для достоверности форм (как реально выглядят дюны, ледники,
  лавовые поля, гейзерит, криовулканы) — предлагаю подключить на ЧК0/ЧК1.

---

## 7. Маршрут

**Сейчас:** в очередь — «сначала хвосты» (решение создателя 2026-09-23). Эпик записан,
в работу не запускается до прохождения хвостов.

**Затем:** ЧК0 (@gdesigner + @designer + @scientist) → гейт «офигеть» → @designer
(спека: рецепты биомов, данные справочника, пакет) → @critic → @developer → @tester →
гейт → ЧК1…ЧК5 (по категориям) → ЧК6 (вода).

**Роадмап:** нужна строка (блок §99.2) — дельта `@dispatcher` (менеджер сам не пишет).

---

## 8. Журнал

- 2026-09-23: пожелание принято. Разведка кода §2. Решения создателя §0. Эпик и
  чекпоинты §4. Отложено: вода/океан (§6). В очередь — «сначала хвосты».
