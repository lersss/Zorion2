# 2026-09-23. Отмена правила «один вопрос за раз» (легаси)

**Создано:** 2026-09-23
**Статус:** 🔄 записано; дельты ждут «го» создателя на применение
**Приоритет:** средний (правка инструкций)
**Вектор:** — (процесс/инструкции, вне векторов разработки)
**Эталон:** не задан

**Связи:** `AGENTS.md` §4; `docs/GD_PROMPT.md`; `docs/GO_DEV_PROMPT.md`; `.opencode/agents/manager.md`; `docs/INDEX.md` §«Владельцы доков».

---

## 0. Решение создателя (дословно)

> «и давай уже уберем правило "только одного вопроса", это легаси»

## 1. Где правило записано (найдено 2026-09-23, `rg -i "вопрос за раз"`)

| Файл | Место | Текст |
|---|---|---|
| `AGENTS.md` | §4.3, п. 3 | «**Один вопрос за раз.**» |
| `docs/GD_PROMPT.md` | стр. 26 | «**Один вопрос за раз.** Не задавай списками.» |
| `docs/GO_DEV_PROMPT.md` | стр. 28 | «**Один вопрос за раз.**» |
| `.opencode/agents/manager.md` | стр. 52 | оговорка «(штатный механизм, один вопрос за раз)» в разделе «РЕТРОСПЕКТИВА» |

Историческая запись `docs/gamedesign/ideas/_archive/62a. Ретроспектива — агенты учатся на ошибках.md` — архив, **не трогаем**.

## 2. Предлагаемые дельты (дословно)

1. **`AGENTS.md` §4.3 п.3** — заменить строку на:
   «**Вопросы — пачкой**, сгруппированные и пронумерованные по темам; жёсткого лимита нет (правило „один вопрос за раз“ отменено, решение создателя 2026-09-23).»
   (нумерация списка §4 не меняется — строка заменяется на месте).
2. **`docs/GD_PROMPT.md` стр. 26** — заменить строку на:
   «**Вопросы — пачкой**, сгруппированные и пронумерованные по темам (без жёсткого лимита).»
3. **`docs/GO_DEV_PROMPT.md` стр. 28** — заменить строку на:
   «**Вопросы — пачкой**, сгруппированные и пронумерованные по темам (без жёсткого лимита).»
4. **`.opencode/agents/manager.md` стр. 52** — убрать оговорку «(штатный механизм, один вопрос за раз)»; смысл строки сохраняется: правка инструкций задаётся **отдельным вопросом** и «ок» на гейте её не покрывает.

**Обоснование замены, а не вычёркивания:** чтобы у агентов не осталось прежней установки «не задавай списками» (в промте геймдизайнера она сформулирована явно), формулируется обратное правило, а не пустое место.

## 3. Маршрут применения

- `AGENTS.md` — **горячий док** → единственный писатель `@dispatcher` (вызывается только создателем).
- `docs/GD_PROMPT.md`, `docs/GO_DEV_PROMPT.md` — владелец в `docs/INDEX.md` **не задан**; предложение — отдать их вместе с `AGENTS.md` через `@dispatcher` (один класс: инструкции/промты) либо назначить владельца.
- `.opencode/agents/manager.md` — зона менеджера, применяет сам (по «го» создателя).

## 4. Разнос по докам

**Применено менеджером 2026-09-23 (по «го» создателя):**
- `docs/GD_PROMPT.md` стр. 26 → «**Вопросы — пачкой**, сгруппированные и пронумерованные по темам (без жёсткого лимита).»
- `docs/GO_DEV_PROMPT.md` стр. 28 → то же.
- `.opencode/agents/manager.md` стр. 52 → оговорка «(штатный механизм, один вопрос за раз)» убрана.

**Дельта для `@dispatcher` (горячий док; менеджер его не пишет):**
- `AGENTS.md` §4.3 п.3: заменить строку «**Один вопрос за раз.**» на
  «**Вопросы — пачкой**, сгруппированные и пронумерованные по темам; жёсткого лимита нет (правило „один вопрос за раз“ отменено, решение создателя 2026-09-23).»
  (нумерация списка §4 не меняется — строка заменяется на месте).

**Открытый вопрос маршрута:** `docs/GD_PROMPT.md` / `docs/GO_DEV_PROMPT.md` владельца в `docs/INDEX.md` не имеют (менеджер применил их как бесхозные по «го» создателя). Если владельцем должен быть `@dispatcher` — внести в таблицу владельцев.
