# 2026-09-23 — Сессия фикса багов из статуса (проверка + починка)

**Создано:** 2026-09-23
**Вектор:** Баги
**Приоритет:** по указанию создателя — «проверяй по одному и чини»
**Статус:** 🔄 в работе (B25 ✅; B27 — следующий; проверка остальных — ниже)
**Эталон:** не применимо (баги)

**Пожелание создателя (дословно 2026-09-23):** «проверь все эти баги по очереди,
возможно что-то уже починили» → «проверяй по одному и чини».

**Связи:**
- `STATUS.md` §Баги (B14, 75a, B16, B25, B27);
- B25: `migrations/000040_ship_equipment_radar.sql`, `internal/handlers/admin_universe.go`
  (`clearPlanets`), `admin_regenerate_planets.go` (`clearPlanetsOf`), `admin_worlds.go`
  (`DeleteWorld`), `admin_pacman.go` (явный DELETE), `admin_universe_test.go`,
  `admin_regenerate_planets_test.go`;
- B27: `internal/travel/intrasystem_manager.go` (`RestoreIntra`), `manager.go` (`Restore`),
  `intrasystem_manager_test.go` (`TestIntraRestoreBrokenTarget`);
- 75a: `internal/repository/economy_repository.go` (коммит `041e972`).

---

## 1. Карта проверки (факт из кода, 2026-09-23)

| Баг | Вердикт разведки | Действие |
|---|---|---|
| **B25** — перегенерация падает на удалении планет | **Реален.** `player_planet_knowledge.planet_id → planets(id)` без `ON DELETE CASCADE` (000040); падают `clearPlanets`/`clearPlanetsOf`/`DeleteWorld` | ✅ **исправлено** (миграция `000073`) |
| **B27** — застревание «в полёте» после рестарта при битой цели | **Реален.** `RestoreIntra` удалял строку полёта, но не чистил `current_position` (`in_flight`); тест `TestIntraRestoreBrokenTarget` закреплял прежнее поведение | ✅ **исправлено** (коммит `52f4b8e`) |
| **75a** — «Люди» вместо расы при первом открытии модалки | **Уже починен** (коммит `041e972 fix(75a): путь «Событие» пересчёта поселения не теряет race_id`) | снять из `STATUS.md` §Баги |
| **B14** — вкладка «Тесты» не работает на проде | **Частично:** на проде исходников нет — вкладка отдаёт внятное сообщение (`findGoModDir` → `errTestSummary`, `admin_tests.go`), а не падает. Тесты там физически не выполнить | вынести создателю (закрыть/уточнить формулировку) |
| **B16** — перебор с декластеризацией (`sparse`) | Значение пресета: `clusterRadius 2500` при `clusterSpacing 5000` (радиус = половина шага → кластеры встык, сливаются). В архиве `46b` — решение создателя «не чиним» | вынести создателю (снять/оставить) |

**Смежное, найдено при разведке B25 (не чинили):** `DeleteWorld`
(`admin_worlds.go:149`, голый `DELETE FROM worlds`) может падать и на других FK без
каскада на мир: `npc_agents.current_world_id/from_world_id/target_world_id` (000026)
и `users.current_world_id` (003; на dev-БД уже `SET NULL` после ранее выполненного
`clearUniverseTx`). Пакман не падает — удаляет `npc_agents` явным шагом раньше миров.
Решение по ним — за создателем.

---

## 2. B25 — исправлено

**Корень:** у «ребёнка» планет `player_planet_knowledge` FK заведён без
`ON DELETE CASCADE` (000040:30), в отличие от прочих детей планет. Знание об
удаляемой планете бессмысленно (`planet_id` новый при перегенерации) — должно
чиститься каскадом.

**Правка:** миграция `migrations/000073_planet_knowledge_cascade.sql` —
`DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT ... FOREIGN KEY (planet_id)
REFERENCES planets(id) ON DELETE CASCADE`. Код путей удаления не менялся: каскад
закрывает все три пути. Явный `DELETE` пакмана и `TRUNCATE` (`clearUniverseTx`,
таблица уже в `truncateTables`) остаются как есть.

**Проверка (@tester, независимо, 2026-09-23):** в транзакции с `ROLLBACK` удаление
планеты со знанием проходит (`DELETE 1`, знание 0, ошибки FK нет); пересчёт мира —
`DELETE 8`, знание 0; контроль старого поведения воспроизводит исходную FK-ошибку;
тесты `TestClearPlanets`/`TestClearPlanetsOf`/`TestTruncateTables`/`Pacman` и
`go test -race -short` — зелёные. Журнал:
`docs/QA/checklists/2026-09-23_B25-каскад-знания-о-планете.md`.

**Живая проверка (после перезапуска dev-сервера, 2026-09-23):**
`player_planet_knowledge_planet_id_fkey` → `confdeltype='c'` (CASCADE);
`schema_migrations` содержит `000073`.

---

## 3. B27 — исправлено

**Корень:** `RestoreIntra` (`internal/travel/intrasystem_manager.go`) в ветке «битая
цель» удалял строку полёта без `onArrival`, но `users.current_position` оставался
`in_flight` — игрок застревал (не мог действовать). Та же ловушка — в ветке
«межзвёздная побеждает», но там позицией владеет межзвёздный полёт (`NULL`).

**Правка:** в `RestoreIntra` добавлен колбэк `resetPosition(userID, worldID)`,
вызываемый **только** в ветке битой цели после удаления строки. В `cmd/server/main.go`
колбэк возвращает игрока на `models.StarOrbitPosition(worldID)` через `userRepo.UpdatePosition`,
**только если** текущая позиция `nil` или `in_flight` (иначе чужое состояние не затираем);
ошибки — в лог. Ветки interstellar-wins / moved-system не тронуты.

**Проверка (@tester, независимо, 2026-09-23):** свой тест-сценарий (не тесты
разработчика) — битая цель → сброс `(userID, worldID)`; межзвёздная → нет сброса;
игрок в другой системе → нет сброса. Контроль не-тавтологичности: с отключённым
`resetPosition` тест краснеет. `go test -race -short` и `go vet` зелёные
(наблюдение: флак `TestWSHubBroadcast` по таймауту — не связан, в изоляции PASS).
Журнал: `docs/QA/checklists/2026-09-23_B27-застревание-in_flight.md`.

---

## 3.1. Разнос по докам

- **`STATUS.md` §Баги** (горячий док, дельта `@dispatcher`): снять строки **B25**
  (исправлен, коммит `411fc53`) и **75a** (исправлен ранее, коммит `041e972`) — ✅ снято
  (`d49bd58`); снять строку **B27** (исправлен, коммит `52f4b8e`); обновить шапку
  `**Обновлено:**` до `2026-09-23`.
- **`CHANGELOG.md`** (горячий док, дельта `@dispatcher`): запись по дате о фиксе B25
  (перегенерация планет/пересчёт мира больше не падают, когда у игроков есть знание
  о планетах), коммит `411fc53` — ✅ внесено (`d49bd58`); добавить запись о фиксе B27
  (после перезапуска сервера игрок с невосстановимым полётом больше не застревает
  «в полёте» — его возвращает на орбиту звезды), коммит `52f4b8e`.
- **`docs/DB.md`** — внесено и закоммичено вместе с фиксом B25 (`411fc53`).
  **`docs/PITFALLS.md`** — строка-ловушка (ребёнок `planets` без `ON DELETE CASCADE`
  рушит пути удаления планет) **осталась в рабочем дереве незакоммиченной**: файл
  замусорен чужими незакоммиченными правками другой сессии (156 строк). Дельта —
  перенести/закоммитить вместе с владельцем файла, не смешивая правки.
- **`migrations/000073`** — номер забронирован менеджером (`000072` — за спекой
  `2026-09-23-орбита-планеты-присутствие-и-снимок`).

---

## 4. Журнал

- 2026-09-23: пожелание принято; разведка всех багов списка §1. B25 исправлен
  (миграция `000073`), проверен @tester, включён на живой БД после перезапуска
  dev-сервера (разрешение создателя). 75a — уже починен, снять из статуса.
  B27 — следующий. B14/B16 — вынести создателю. Смежные FK (`npc_agents`, `users`) —
  отдельным вопросом.
- 2026-09-23: **B25 сдан** (коммит `411fc53`), статус почищен (`d49bd58`).
  **B27 исправлен** (коммит `52f4b8e`) и проверен @tester (6/6 PASS). Дальше —
  обязательная задача «перелив массы в гигантов» и вопросы по B14/B16.
