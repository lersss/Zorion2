# 2026-09-23. Студия: назначение рецептов постройкам — справочник товаров на вкладке «Производители»

**Дата:** 2026-09-23
**Вектор:** Поселения (эпик «Экономика поселения» + студия)
**Приоритет:** высокий (создатель ведёт флоу вручную и упирается в ручной ввод номера рецепта)
**Статус:** ✅ **готово 2026-09-23.** Шаг 1 — коммит `0c240ee` (переключатель справочника «Постройки | Товары» на «Производителях»). Шаг 2 (назначение рецептов постройкам + надпись «производит») + новый вид карточек + фикс бага открытия карточки из дерева — коммит `9d43045`. Живой прогон `@tester` — 10/10 PASS; `@critic` — крупных 0 (см. §5 «Разнос по докам»); приёмка создателя — «ок» 2026-09-23.

## 6. ЧЕКПОИНТ 2026-09-23 (задача закрыта)

**Где стоим:** ✅ готово. Код — коммит `9d43045` (шаг 2 + фикс + карточки), шаг 1 — `0c240ee`. Доки — этим же закрытием (спеки + эта идея + `impact_map.json`). Метки в `docs/COORDINATION.md` сняты.

**Сделано и проверено вживую:**

1. **Шаг 2А — сервер (`BindRecipe` и др.)**: любая запись-подтип держит любой рецепт; тип/класс → 400; сняты два мёртвых 409 (смена категории подтипа/товара с привязками); `unbound_recipe` → «ни к одной постройке»; текст 409 → «рецепт уже назначен этой постройке». Тесты перевёрнуты/добавлены, зелёные. Файлы: `internal/repository/producer_repository.go` (+test), `internal/repository/goods_repository.go` (+test), `internal/handlers/studio_recipes_handlers.go` (+test), `internal/handlers/studio_descriptions_test.go`, `internal/goodsstudio/validate/validator.go` (+test).
2. **Шаг 2Б — клиент (`web/studio.html`)**: drag&drop рецепта из справочника (режим «Товары») на узел постройки; надпись «производит: имя · тN» (дерево, список справа, карточка); список набора с «− отвязать»; «Доступен постройкам»; снята блокировка селекта категории товара; убран «null» у постройки без категории.
3. **Вид карточки**: убран JSON из вида (спрятан в `<details>` «служебные параметры»), читаемые блоки «Производит / Потребляет / Условия работы / Люди»; типографика ≫; крупный попап (720 px). Спека UX: `docs/specs/2026-09-23-студия-карточки-товара-и-постройки-крупный-попап.md`.
4. **Спека шага 2**: `docs/specs/2026-09-23-студия-назначение-рецептов-постройкам.md` — решения гейта в §3.2.1.
5. **Баг открытия карточки из дерева** (из прошлого чекпоинта) — исправлен (см. ниже).
6. **Прогон `@tester`** — 10/10 PASS (отчёт `docs/QA/checklists/2026-09-23_студия-шаг2-назначение-рецептов-и-баг-дерева.md`); **`@critic`** — крупных 0, средних 5 (3 вынесены создателю, см. §5/§7), мелких 5 (правятся дельтами доков).

**🔴 ОТКРЫТЫЙ БАГ — ИСПРАВЛЕН 2026-09-23:** при переключении справочника на «Товары» двойной клик по дереву построек (канвас) открывал/пытался открыть карточку товара → карточка постройки не открывалась. Ожидание создателя: **клик по дереву всегда открывает карточку постройки**, режим справочника влияет только на список справа. Причина: `openPopup` (~1485) — условие `spravMode !== "goods"` ошибочно распространили на клики по дереву (канвас-`dblclick` ~1100 шёл через `openPopup`). Фикс (менеджер, мелкая правка): в `canvas` `dblclick` при `branch === "producers"` вызван `openProdNodePopup(id)` напрямую, минуя `openPopup`; режим справочника на канвас не влияет. Проверено вживую (playwright, оба режима открывают карточку постройки `sub:148`; регресс остальных двойных кликов — ок). Вторая причина из чекпоинта («пересборка `#spravList` на одиночном клике ломает двойной») **не подтвердилась**: двойной клик по карточкам справочника работает (Chrome кидает `dblclick` на пересозданный элемент), правку `renderAll`/дебаунс не вносили (хирургичность).

**Не сделано / осталось на будущее:** распространение «Производит»/«Потребляет» из каталога в игровую карточку поселения (отдельная задача); итерация 2 редактора полей (`Ест: [товар][норма] +добавить`) вместо JSON; связь набора рецептов с ветками поселения в мире; решения создателя по 3 средним звоночкам критика (см. §7).

**Метки в `docs/COORDINATION.md`:** сняты 2026-09-23 (задача закоммичена). Файл `docs/COORDINATION.md` в коммит задачи не входит (там метки параллельных задач других агентов).

**Коммиты задачи:** код — `9d43045` (10 файлов: `web/studio.html` + Go 2А); доки — обе спеки + эта идея + `docs/impact_map.json` + отчёт `@tester` (один коммит вместе с этой идеей).

**Порядок следующей сессии:** (1) ответить на 3 средних звоночка критика (§7); (2) далее — итерация 2 (редактор полей вместо JSON) и связь набора с ветками.
**Эталон:** не задан

**Связи:** спека `docs/specs/2026-09-21-рецепт-сущность-и-граф-фабрики.md` (`recipes`/`recipe_components`/`producer_recipes`, роуты `/studio/api/recipes*` и `/studio/api/producers/{id}/recipes*`); спека `docs/specs/2026-09-22-поселение-ветка-буферы-переработка.md` (§6 — ветки поселения, О2: «выбор рецепта в админ-форме — ввод `recipe_id` руками; ручка списка рецептов — отдельная UI-задача»); спека итерации 4 (тип поселения — подтип строения «Поселение» без товарной категории); `web/studio.html` (ветки «Товары»/«Производители»/«Предметы», `renderSprav`/`renderProdSprav`/`applyBranchUI`); `web/static/js/modal/branches.js` (ветки в карточке планеты); `internal/repository/goods_repository.go` (инвариант `BindRecipe`: только конкретная фабрика `kind=goods` и только рецепт её категории).

---

## 0. Пожелание создателя (дословно)

> «мне нужен такой флоу: на странице товары я генерирую рецепт из товаров и ресурсов, фактически это есть, да?
> далее я иду на страницу производители, и там назначаю рецепты постройкам. Драгндропом из справочника (давай сделаем переключение между справочником производителей и справочником рецептов.
> То есть на "обычное поселение" я должен видеть, какие рецепты ему назначены.»

Уточнения:

> «при этом не читая json, а визуально.»

> «Давай по порядку. Сделай мне возможность на вкладке "производители" переключаться между справочником производителей и товаров.»

> «2. режим товары - точно такой же справочник, как и на вкладке товары должен быть. Но это влияет только на сам справочник, а не перерисовывает канвас производителей.»

> «да» (2026-09-23) — согласие на шаг 1 в объёме, описанном менеджером (переключатель «Постройки | Товары» в правой панели, канвас не трогаем).

## 1. Состояние на 2026-09-23 (проверено менеджером по коду и dev-БД)

**Есть:**
- Шаг «Товары»: рецепт собирается из товаров/ресурсов — слоты, количество, сложность, «Доступен фабрикам» (спека рецептов §5–§6).
- Привязка рецепта к фабрике — `producer_recipes`; в студии: drag&drop карточки товара на канвас при выбранной фабрике (только ветка «Товары»), либо селект «+ привязать» в попапе товара; «− отвязать».
- В попапе постройки показывается **число** рецептов в наборе («набор рецептов: N») + кнопка копирования набора универсальной фабрики.

**Нет (разрывы флоу):**
- На вкладке «Производители» справочник один — постройки; переключения на товары нет. Дерево построек не принимает drag&drop (drop инертен).
- Списка назначенных рецептов не видно: только число; в карточке постройки вход/выход/параметры показаны JSON-полями (создатель: «не читая json, а визуально»).
- «Обычному поселению» (dev-БД: `producer_types.id=148`, родитель «Поселение», `category_id` пуст) рецепты назначить нельзя вовсе: блок «набор рецептов» показывается только у подтипов с товарной категорией, а инвариант `BindRecipe` требует «конкретная фабрика `kind=goods` + рецепт своей категории».
- В dev-БД `producer_recipes` пуст (0 привязок); рецептов три: 69 «Пища», 73 «Вода», 71 `qa_i3_dl_good`.

## 2. Объём шага 1 (согласовано создателем)

В правой панели студии на вкладке «Производители» — переключатель **«Постройки | Товары»**:

1. По умолчанию «Постройки» (текущее поведение — список типов построек); выбор запоминается.
2. Режим «Товары» показывает **ровно тот же справочник каталога**, что на вкладке «Товары» (пилюли тиров, галки «используемые/неиспользуемые», фильтр по категории, поиск, карточки; общий код отрисовки — чтобы списки не разъезжались), вместе с его фильтрами, которые в этом режиме становятся видимыми.
3. Переключение влияет **только на правую панель**: канвас остаётся деревом построек, не перерисовывается.
4. Объём шага — чисто визуальный: серверную часть и БД не трогаем, данные уже приходят в студию.

**Граница шага (названа создателю):** перетаскивание рецепта на постройку в этом шаге ещё ничего не делает — дроп на дерево построек сейчас не работает; это следующий шаг.

## 3. Шаг 2 — назначение рецептов постройкам (решение создателя, 2026-09-23)

### 3.1. Правило создателя (дословно)

> «Ну вот должно быть универсально. Назначил рецепт постройке-дочке, и это и будет то, что она делает, без хардкода. Разрешаем любым постройкам назначать любые рецепты. Остальное - на совести того, кто конфигурирует постройки»

> «например для обычного поселения это еда (т1), поправь если я ошибаюсь»

Уточнение менеджера (принято создателем как факт): «Пища» — товар категории «продовольствие», сложность рецепта 1 → тир 1; но к самому типу «Обычное поселение» сейчас не привязано ничего (0 привязок в dev-БД), а по канону (`13_settlements.md` §13.2) выход поселения — «остаток после потребления», один товар за ним не зашит. Значит «производит еду» — это то, что появится только после назначения рецепта, а не хардкод.

**Следствия правила (канон меняется):**
- Снимается инвариант канона `000055` «рецепт привязывается только к конкретной фабрике `kind=goods` с категорией и только рецепт её категории» (`internal/repository/producer_repository.go`, `BindRecipe`): любая постройка-дочка держит любые рецепты. Ответственность за осмысленность набора — на конфигураторе.
- Что построено/привязано — то и «делает» постройка; надпись на карточке — производная от набора рецептов, без отдельного поля «что производит».
- Живые уточнения правят доки (правило 99a): канон «только рецепт своей категории» обязан быть переписан в доках при закрытии задачи (дельта в идее / `@dispatcher`).

### 3.2. Объём шага 2 (к дизайну)

1. **Модель:** ослабить инвариант привязки (подтип `kind=goods` без категории — можно; рецепт — любой). Решить судьбу связанных мест: проверка «выход рецепта — категория фабрики», предупреждение `unbound_recipe`, копирование набора универсальной фабрики (источник задан категорией), фильтр справочника по семейству, селектор фабрики в шапке «Товары».
2. **UI:** перетаскивание рецепта из справочника на узел постройки в дереве (привязка) и отвязка; видимый список назначенных рецептов текстом (в дереве, в списке справа, в карточке постройки) — вместо чтения JSON.
3. **Надпись «производит»:** на карточке подтипа — конечные товары (выходы назначенных рецептов) с тиром, например «производит: Пища · т1». Пусто — строку не показываем.

### 3.2.1. Решения создателя на гейте шага 2 (2026-09-23)

Создатель подтвердил (гейт спеки): «ок, делайте». Закрыты все открытые вопросы:

- **Привязка только к «дочке»** (запись-подтип, `parent_id NOT NULL`) — к типу/классу нельзя (400). Наследование наборов от типа — отдельная будущая фича.
- **Сырьё:** рецепты с выходом-сырьём — только у добывающих построек (отдельная будущая задача). У остальных построек такие рецепты **в фильтрах/справочнике не показываем**; серверный запрет не держим (лишнее отсекает фильтр).
- **Категории не ограничивают** набор; блокировка смены категории в карточке товара снимается вместе с серверным запретом.
- **`kind` подтипа:** привязка не ограничена (`goods`/`items`/`energy`).
- **Рецепт на ресурс:** серверный 400 снимается (следствие правила «сырьё — только добывающим + фильтр»).
- **Мёртвые 409** при смене категории фабрики/товара с привязками — снять.
- **Терминология:** «Доступен фабрикам» → «Доступен постройкам», warning → «ни к одной постройке».
- **Каскады:** остаются молчаливыми (предупреждение числом — отдельной задачей).

### 3.3. Открытые вопросы (для дизайна/гейта)

1. Могут ли рецепты держать **родительские типы/классы** (не «дочки»)? Менеджер исходит из «постройке-дочке» = привязка только к конкретным записям-подтипам; классы остаются абстрактными (подтвердить на гейте).
2. Как теперь выглядит «Доступен фабрикам» в карточке товара (сейчас перечисляет фабрики категории товара — постройки без категории в этот список не попадают) и селектор фабрики/фильтр семейства на вкладке «Товары».
3. Копирование набора рецептов у постройки без категории: не копировать (нет источника) или искать источник иначе.
4. Связь набора рецептов с ветками в мире (авто-ветки / выбор из набора / «рекомендация») — вне объёма шага 2, отдельно.

## 4. DoD и сдача (шаг 1)

- Go-код не тронут (только `web/studio.html`) → Go-цикл DoD не гонялся.
- `@tester`: живой браузерный прогон студии — **9 PASS / 0 FAIL / 0 SKIP**, отчёт `docs/QA/checklists/2026-09-23_студия-справочник-товаров-на-производителях.md`. Проверено: дефолт «Постройки» (список и скрытые фильтры), режим «Товары» (каталог 134 = 134 с вкладкой «Товары», фильтры/поиск/гейт тир+галка), ⓘ открывает товарный попап, канвас побитово не меняется (хеш `toDataURL`), возврат и сохранение режима после перезагрузки, вкладка «Товары» не сломана (переключатель скрыт), консоль без ошибок.
- Приёмка создателя: «ок» (2026-09-23) → коммит.
- Коммит кода: `0c240ee` — `feat(studio): справочник товаров на вкладке «Производители» (переключатель «Постройки | Товары»)`, 1 файл (`web/studio.html`, +51/−6). Метка в `docs/COORDINATION.md` снята, в коммит не включена.

## 5. Разнос по докам

**Статус:** задача закрыта 2026-09-23. Код — коммит `9d43045`; доки (обе спеки + эта идея + `impact_map.json` + отчёт тестера) — одним коммитом вместе с этой идеей.

**Горячие доки (дельта для `@dispatcher`; применяет в паузу):**

- **`CHANGELOG.md`** (дописать в существующий раздел `## 2026-09-23`):
  - `Реализовано:` «в студии любую постройку-дочку (в том числе поселение без товарной категории) можно научить рецепту перетаскиванием карточки товара на постройку в дереве; карточка показывает «производит: Пища · т1», рецепт убирается кнопкой «− отвязать». Рецепт больше не ограничен категорией постройки.»
  - `Исправлено:` «двойной клик по постройке в дереве больше не открывает карточку товара, когда справочник переключён на «Товары» — всегда открывается карточка постройки.»
  - (запись шага 1 — см. прежнюю дельту: справочник на «Производителях» переключается «Постройки | Товары».)
- **`.opencode/context.md`**: «Студия: назначение рецептов постройкам — перетаскивание рецепта на узел дерева, надпись «производит», отвязка; инвариант «рецепт только своей категории» снят (любая постройка-дочка ↔ любой рецепт каталога). Шаг 2 флоу (идея 2026-09-23).»
- **`docs/specs/README.md`**: строки-аннотации двух спек (обе — ✅ реализовано, коммит `9d43045`): `2026-09-23-студия-назначение-рецептов-постройкам.md`; `2026-09-23-студия-карточки-товара-и-постройки-крупный-попап.md`.
- **`STATUS.md`** — дельт нет (реализованная фича → CHANGELOG, не в §1 статуса).
- **`docs/gamedesign/faq_counter.md`** — дельт нет (эталон «производит» задан создателем).

**`docs/impact_map.json` (правит `@manager`) — применено этой задачей:** правка `note` узла `producer_recipes`; рёбра `producer_types → producer_recipes` (обоснование устарело), новые `producer_recipes → producer_repository.go`/`goods_repository.go`/`web/studio.html`/`validator.go`/GDD; `producer_slots` — `note` + ребро `producer_recipes → producer_slots` (обоснование «рецепты живут в категориях слотов» устарело); `buildings → producer_recipes` (отложенное); уточнение ребра `recipes → producer_recipes`; дельта `recipes → docs/gamedesign/05_economy.md` §5.2.4.

**GDD и прочие доки (дельта-строки владельцам; дословные «было → станет» — в спеке шага 2 §10):**

- `docs/gamedesign/11_production.md` §11.1.1 — @designer: строка «Один тип фабрики — товары одной категории … подмножество рецептов своей категории» → «постройка-дочка держит **любой** набор рецептов; категория — ось студии, не ограничение».
- `docs/gamedesign/13_tiers.md` §13.9.1 — @designer: «Фабрика держит набор рецептов — подмножество своей категории» → «любая постройка-дочка держит любой набор рецептов каталога».
- `docs/gamedesign/13_settlements.md` §13.2 — @designer: добавить «что производит поселение — определяется назначенными рецептами в студии (без хардкода); ветка поселения — отдельная механика, связь набора с ветками — вне задачи».
- `docs/gamedesign/05_economy.md` §5.2.4 — @designer (нашёл `@critic`): та же устаревшая строка «подмножество рецептов своей категории» → новый канон (формулировка как в §11.1.1).
- `docs/DB.md` — @developer: абзац `producer_recipes` → «„запись-подтип постройки ↔ любой рецепт каталога“; инвариант „выход рецепта = категория фабрики“ и запрет смены категории при привязках сняты 2026-09-23; остаются 404/400/409 (нет типа/рецепта; тип/класс вместо подтипа; дубль)».
- `docs/GLOSSARY.md` §5/§6 — @designer: §6 «Фабрика» — «держит любой набор рецептов каталога; фабрика — частный случай»; §5 — ввести термины «набор рецептов постройки» и «производит» (надпись = выводы назначенных рецептов, `имя · т<tier>`).
- `docs/PITFALLS.md` — @developer (файл занят меткой/бэклогом, см. `docs/TAILS.md` хвост 2): новый пункт — «снятие инварианта „выход рецепта = категория фабрики“ требует убрать ДВА мёртвых 409 (смена категории фабрики и товара); у drag&drop в студии ДВА разных смысла — ветка „Товары“ бросает на выбранную фабрику, ветка „Производители“ (режим „Товары“) — на узел под курсором» (полный текст — спека шага 2 §10.7).
- `docs/specs/2026-09-21-рецепт-сущность-и-граф-фабрики.md` §2.4 — пометить инвариант «выход рецепта = категория фабрики» снятым 2026-09-23 (иначе ссылка из шага 2 ведёт на отменённый канон).
- `docs/specs/2026-09-23-студия-карточки-товара-и-постройки-крупный-попап.md` §11 — @dispatcher: UI-термины «блок карточки», «строка-характеристика», «крупный попап», «типографический слой студии» в `GLOSSARY.md` (при согласии создателя).

**Ретроспектива:** не требуется (бюджет итераций не превышен). Правок инструкций не предлагается.

**Открытые вопросы критика (средние, не крупные) — вынесены создателю на гейте 2026-09-23 и закрыты:** (1) клиентский запрет по категории в ветке «Товары» — **чинить** (снят, см. §7); (2) «рецепты-сырьё только у добывающих + фильтр» — **отложить**; (3) «что производит поселение» — **отложить**; направление создателя «мир наследует от студии» зафиксировано в §7.1 (конфликт с доком веток — дельта при закрытии будущей задачи).

---

## 7. Решения создателя по звоночкам критика (2026-09-23)

Приёмка шага 2 — «ок» (2026-09-23). Классификация трёх средних звоночков:

1. **Клиентский запрет по категории в ветке «Товары»** — **ЧИНИТЬ** (решение создателя). Убран: в ветке «Товары» кросс-категорийная привязка рецепта к выбранной постройке разрешена (drop на канвас и пункт контекст-меню) — правило «категория не ограничивает набор» теперь одно на обеих ветках. Заодно снят мёртвый текст «товар другой категории — фабрика производит только…».
2. **Рецепты-сырьё только у добывающих построек (+ фильтр)** — **ОТЛОЖИТЬ** (отдельная будущая задача).
3. **Два источника «что производит поселение» (набор в студии vs ветка в мире)** — **ОТЛОЖИТЬ** (связь набора с ветками — отдельный шаг).

### 7.1. Правило создателя (направление, 2026-09-23) — конфликт с доком веток

> «Вообще непонятно про ветку на конкретном поселении в мире. То, что в мире происходит, должно наследоваться от студии, а не наоборот».

**Направление:** мир **наследует** от студии; набор рецептов, назначенный постройке-подтипу в студии, — источник правды, а «ветка на конкретном поселении в мире» не должна быть независимым источником «что производит».

**Конфликт с принятым в доках (правило 99a):** спека `2026-09-22-поселение-ветка-буферы-переработка.md` §6 (и `13_settlements.md` §13.2) описывают ветку как выбор рецепта **на инстансе** поселения (`settlement_branches.recipe_id`), независимый от `producer_recipes`; реализация (`internal/handlers/admin_settlement_branches.go`, `settlement_branch_processing`) `producer_recipes` не читает. Это расходится с новым направлением.

**Что делаем (не сейчас):** связь «набор рецептов постройки → ветки поселения в мире» — отдельная задача (отложена, п.3). При её проектировании источником правды делаем студию, а ветку — производной/наследующей. **Дельты доков при закрытии той задачи:** `2026-09-22-поселение-ветка-буферы-переработка.md` §6 и `13_settlements.md` §13.2 — переформулировать «ветка выбирается на инстансе» → «ветка наследует набор рецептов постройки из студии». До этого правки не вносим (фича отложена).

---

## 8. ЧЕКПОИНТ 2026-09-23 (второй — закрытие сессии)

**Где стоим:** ✅ всё закоммичено. Ветка `master`.
- Шаг 1 — `0c240ee`.
- Шаг 2 + фикс карточки из дерева — `9d43045` (код, 10 файлов).
- Доки шага 2 — `635ee3d` (спеки, идея, `impact_map.json`, отчёт тестера).
- Звоночек №1 (снятие клиентского запрета по категории в ветке «Товары») — код `web/studio.html` + доки — этим коммитом.

**Проверено живьём:** шаг 2 + карточки + фикс — 10/10 PASS (отчёт `2026-09-23_студия-шаг2-назначение-рецептов-и-баг-дерева.md`); снятие запрета по категории — 8/8 PASS (отчёт `2026-09-23_студия-снятие-запрета-категории-в-товарах.md`). DoD Go зелёный (`9d43045`). `@critic`: крупных 0.

**Открытые хвосты (для следующей сессии):**
1. **Горячие доки — дельта для `@dispatcher`** (не применено; вызывается по прямому указанию создателя): `CHANGELOG.md` (записи шага 1/шага 2/фикса), `.opencode/context.md`, `docs/specs/README.md` (аннотации двух спек). Дословные формулировки — §5.
2. **Дельты GDD/прочих доков владельцам** (не применено): `11_production.md` §11.1.1, `13_tiers.md` §13.9.1, `13_settlements.md` §13.2, `05_economy.md` §5.2.4, `DB.md`, `GLOSSARY.md` §5/§6, `PITFALLS.md` (новая ловушка), спека `2026-09-21-рецепт-сущность-и-граф-фабрики.md` §2.4. Дословно — §5.
3. **Мелкий вопрос** — **ЗАКРЫТ 2026-09-23:** создатель — «все». Селект «+ привязать» расширен на все постройки + имена в подписях (см. §9).
4. **Отложено:** рецепты-сырьё только у добывающих + фильтр (п.2); связь набора рецептов с ветками поселения в мире (п.3, направление — §7.1).
5. **Будущее:** итерация 2 редактора полей (`Ест: [товар][норма] +добавить`) вместо JSON; распространение «Производит»/«Потребляет» в игровую карточку поселения.

**Пуш:** не делался (только через `@dispatcher` / с разрешения создателя).

**Метки в `docs/COORDINATION.md`:** сняты, в коммит не входят (там метки чужих задач).

---

## 9. Закрытие мелкого вопроса — «все постройки» в селекте «+ привязать» (2026-09-23)

**Решение создателя:** «все». Селект «+ привязать» в карточке товара показывает **все постройки-подтипы** (любой `kind`, любая/отсутствующая категория), а не только фабрики категории товара. Прежний фильтр был удобством-подсказкой, не серверным запретом.

**Правка (менеджер; мелкая, только `web/studio.html`, функция `renderBindSection`):**
- фильтр селекта: `p.parent_id && p.kind === "goods" && p.category_id === g.category_id` → `p.parent_id` (все записи-подтипы, кроме уже привязанных);
- подпись пункта селекта: `parent_name — level` → `name — level` (иначе десятки «Фабрика — универсальная» неразличимы);
- строка списка «Доступен постройкам»: имя постройки добавлено первым (`Городок · Поселение · универсальная`) — закрывает наблюдение тестера (три лаборатории выглядели одинаково, «Лаборатория · универсальная» ×3);
- пустое состояние: «нет других фабрик категории …» / «Фабрик нет» → «все постройки уже привязаны» / «Построек нет — раздел «Производители»».

**Прогон `@tester`:** 12/12 PASS + 1 SKIP — отчёт `docs/QA/checklists/2026-09-23_студия-выпадашка-привязать-все-постройки.md`. Привязка/отвязка (селект и drag&drop) — 200; поселение и три лаборатории различимы; консоль без ошибок; БД после прогона — `producer_recipes` = 0. Go-код не тронут → Go-цикл DoD не гонялся (как на шаге 1).

**Коммит кода:** `2e3304c` — `fix(studio): селект «+ привязать» — все постройки, подписи с именем`, 1 файл (`web/studio.html`, +12/−10).

**Разнос по докам (дельта для `@dispatcher`, применяет вместе с §5):**
- `CHANGELOG.md` (раздел `## 2026-09-23`, блок `Исправлено:`): «в карточке товара список «+ привязать» показывает все постройки (в том числе поселения и лаборатории без товарной категории), а не только фабрики категории товара; пункты списка и строки привязок начинаются с имени постройки».
- `.opencode/context.md` — дописать к строке про назначение рецептов: «список «+ привязать» — все постройки; подписи с именем постройки».

**Ретроспектива:** не требуется (бюджет итераций не превышен). Правок инструкций не предлагается.
