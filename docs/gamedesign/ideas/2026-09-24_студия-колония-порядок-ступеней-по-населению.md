# 2026-09-24. Студия: ступени «Колонии» — порядок по населению и настройка порогов в карточке

**Вектор:** Поселения (правка чисто клиентская — студия).
**Приоритет:** обычный (инструмент создателя).
**Статус:** ✅ реализовано 2026-09-24 (коммиты `fbb0997` — код и тест, `5e09566` — идея и QA-прогоны).

## Пожелание создателя (дословно)

1. «Переименовал "поселение" в "колония" (категория построек). Теперь я хочу видеть
   отсортировано по количеству населения форпост - поселение - городок - город -
   мегаполис - метрополия - экуменополис»
2. «в студии в разделе "производители"»
3. «причем желательно чтобы они там самостоятельно формировались по максимуму населения»
4. «ну или по минимуму наверное точнее»
5. «Да, плюс отображать эти параметры в карточках»
6. (уточнение адреса) «а чего не сделал конфигурируемым внутри карточки? и на саму
   карточку можно было это не выносить» → «с карточек убрать. 2. Да.»
7. «Кстати, я вообще представлял, что будет явно указана связка, во что превращается
   колония на следующей стадии.»

**Зачем (цель):** создатель строит ладдеру ступеней поселения. В студии ступени должны
читаться как лестница (порядок — сам, по данным), а пороги — **настраиваться в карточке
постройки полями** (не сырым JSON), с явной связкой «во что превращается на следующей
ступени». На карточках дерева числа не нужны.

## Что сделано

Файл: `web/studio.html` (только клиент; Go/БД/сервер не тронуты — серверная ручка
`PUT /studio/api/producers/{id}` уже принимает типизированное `stage`).

1. **Порядок ступеней** «Колонии» в дереве «Производители» — по нижнему порогу
   населения (`params.stage.enter`) по возрастанию, сам из данных (`prodStageEnter`,
   сортировка в `prodSubtypeNoCatRecords`).
2. **Строка диапазона с карточек дерева убрана** (сортировка осталась).
3. **Блок «Стадия» в карточке постройки** (`stageBlock`) — по спеке
   `2026-09-23-стадии-поселения-и-скорость-производства` §11.1 п.3 / §11.2:
   - поля `[порог входа] → [порог выхода]` в людях, значения из `params.stage`;
   - у низшей ступени (`enter = 0`) — подпись «порог выхода не читается (пол)»;
   - `exit ≥ enter` — красная ошибка, сохранение блока не отправляется (сервер 422);
   - сохранение по изменению поля: `PUT {stage:{enter,exit}}` → `fetchState()`
     (пересчитывает порядок и связку);
   - **связка со следующей ступенью** (новое, дельта к §11.1): «следующая ступень:
     <имя> (при N чел.)» / «дальше ступеней нет» — по данным (`prodNextStage`);
   - блок — только у ступеней класса без слотов (тот же предикат, что у дерева).
4. Сырой JSON «служебные параметры» внизу карточки оставлен (аварийный путь).

**Рамки:** только студия. База/сервер не тронуты.

## Проверка

- DoD: `go build ./...`, `go vet ./...`, `go test -race -short` (без генераторных
  пакетов) — все пакеты `ok`; `go test ./web/...` — ok; синтаксис классического
  скрипта студии (`node --check`) — OK.
- TDD: новый контрактный тест `web/frontend_studio_stage_test.go` (до кода — красный).
- Независимый прогон `@tester` (живой браузер): `docs/QA/checklists/2026-09-24_студия-стадия-блок-в-карточке.md`
  — T1–T6 PASS (блок у ступени, отсутствие у не-ступеней, пол/верх, ошибка `exit ≥ enter`
  без отправки запроса, живой round-trip сохранения с возвратом данных, дерево без
  строки диапазона + порядок сохранён, ошибок в консоли нет).
- Предыдущий прогон показа диапазона: `docs/QA/checklists/2026-09-24_студия-колония-порядок-ступеней.md`
  (итерации 1–2; направление «показ на карточке дерева» отменено п.6 пожелания).

## Разнос по докам

- `CHANGELOG.md` — **ГОТОВО** (коммит `9e525cb`): +2026-09-24, раздел «Прочее» —
  порядок ступеней «Колонии» по населению, блок «Стадия» в карточке (пороги +
  связка «следующая ступень»), переименование имён ступеней (состояние данных).
- **Дельта к спеке** `docs/specs/2026-09-23-стадии-поселения-и-скорость-производства.md`
  §11.1 (связка «следующая ступень») — **ГОТОВО** (коммит `57d3000`, автор `@designer`).
- `impact_map.json`: правка клиентская (отрисовка/редактор в студии) — записи не требует.
- Пуш ветки — **ГОТОВО** (`@dispatcher`, `origin/master` = `c95c776`; в диапазон попали
  чужие коммиты других линий — опубликованы как есть).

## Открытый вопрос: переименование класса живёт только в dev-БД

**Выявлено 2026-09-24 при закрытии.** Создатель переименовал в студии класс
«Поселение» → «Колония» и ступени «Аутпост» → «Форпост», «Посёлок» → «Поселение»
(в dev-БД: `producer_types` id 1 = «Колония», 148 = «Форпост», 155 = «Поселение»).
Но **сидер каталога и доки знают старые имена**:

- `internal/goodsstudio/seed_producers.go` (сид свежей БД / прод) создаёт класс
  «Поселение» и ступени «Аутпост»/«Посёлок»/… (`ON CONFLICT (name_norm) DO NOTHING`
  — на dev-БД с переименованием это **вставит дубли** рядом с «Колонией»);
- там же `producerIDs["Аутпост"]` задаёт `default_settlement_type_id` — при повторном
  сиде ключ уедет на seeded-«Аутпост», а не на создателев «Форпост»;
- тесты-фикстуры и комментарии (`admin_structures_test.go`, `settlement_type_consistency_test.go`,
  `seed_producers_test.go`, `stage.go`, спека §4.3, `GLOSSARY.md` §7, `DB.md`) называют
  класс «Поселением».

Адрес: `@developer` (сидер, тесты, комментарии) + `@dispatcher` (доки) — по слову
создателя; либо откатить переименование в dev-БД. Решение — за создателем.
