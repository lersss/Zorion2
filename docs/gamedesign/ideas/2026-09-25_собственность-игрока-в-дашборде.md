# 2026-09-25. Собственность игрока в дашборде

**Создано:** 2026-09-25
**Статус:** ✅ **ЧК1, ЧК2 и ЧК3 реализованы и приняты создателем 2026-09-26** (вкладка «Собственность»: список + «Посмотреть»; «Перелететь»; ЧК3 — разгрузка вкладки и статус маршрута). Код — коммиты `65d6e8c` (ЧК1+ЧК2) и `be48759` (ЧК3). Решения и ТЗ ЧК3 — §9, разнос дельт — §10. Спека `docs/specs/2026-09-26-собственность-игрока-в-дашборде.md`
**Вектор:** Игрок · Поселения · Строения
**Родительский эпик:** «Постройка структур на планете» (ЧК2)
**Источник:** живое пожелание создателя 2026-09-25

---

## 0. Пожелание (слова создателя)

- В дашборде игрока нужна **отдельная вкладка «Собственность»**.
- В ней перечислить **собственность игрока** — пока это **поселения и строения, где владелец — игрок**.
- По каждой единице собственности нужны два действия:
  - **«Посмотреть»** — открывает попап планеты; показывается **только то, что игрок знает**;
  - **«Перелететь»** — перелёт **к нужной планете из любой точки**, одним действием до планеты.

## 1. Уточнения создателя

1. Тип собственности на сейчас: поселения и строения с владельцем-игроком. «Пока да» — состав
   списка может расшириться позже.
2. «Посмотреть» — попап планеты, но без раскрытия неизвестной информации; игрок видит своё
   имущество в рамках уже известного ему знания о планете.
3. «Перелететь» — одним действием доводит до планеты, откуда бы игрок ни находился.
4. Отдельная вкладка в дашборде, а не блок внутри другой вкладки.
5. **Видеть, что происходит на планете, можно только при личном присутствии или через
   ретранслятор.** Собственность не даёт бесплатного обзора планеты. Сеть ретрансляторов
   — запланированная механика (`99.2.9`), пока не реализована; до неё наблюдение — только
   личным присутствием.
6. **Строку собственности показываем всегда** — игрок знает, чем владеет. Состояние
   планеты внутри строки — только по знанию: присутствие, ретранслятор или уже
   сохранённый снимок; неизвестное не раскрывается.

## 2. Что уже есть (факты для дизайна)

- Дашборд игрока — `web/index.html`, вкладки по паттерну админки (`.tab-btn`/`.tab-pane`).
- Владение: `settlements.owner_type='player'` + `owner_id`, `buildings.owner_type='player'` +
  `owner_id` (миграции `000082`; у поселений — владелец только у новых, легаси без владельца).
- Знание о планете — отдельный слой (`player_planet_knowledge`, присутствие/снимок); попап
  планеты уже умеет показывать урезанную картину по знанию.
- Полёты: межзвёздный перелёт к звезде и внутрисистемный перелёт к планете — существующие
  механики; у игрока один активный полёт. Автоматическая цепочка «межзвёздный → к планете»
  уже реализована (спека `99.2.30`, `users.pending_destination`) — новую не изобретать.
- Ретранслятор как тип строения в дереве есть, но сети покрытия и наблюдения нет: механика
  `99.2.9` запланирована, не реализована. До неё «что происходит на планете» — только
  личным присутствием.
- API собственности игрока отдельным списком пока нет.

## 3. Открытые вопросы для дизайна

1. **Два плеча перелёта.** Если игрок в другой системе, «Перелететь» должен сам выполнить
   межзвёздный перелёт до звезды, а по прибытии — внутрисистемный до планеты. Как это
   оформляется: цепочка полётов, фоновое продолжение после прибытия, статус в списке?
2. **Ограничения.** Что делать, если уже есть активный полёт, нет двигателя, игрок на
   поверхности или уже на месте? Кто и когда отменяет цепочку?
3. **Знание и собственность.** ✅ **Решение создателя:** сведения о состоянии планеты
   доступны только при личном присутствии или через ретранслятор; собственность не
   раскрывает планету. Открытый остаток: показывать ли **саму строку владения** на
   непосещённой планете (игрок знает, что владеет объектом) или скрывать и её до
   присутствия/ретранслятора; что именно доступно в «Посмотреть» без наблюдения.
4. **Живость списка.** Что происходит со строкой, если владелец сменился, поселение вымерло
   или мир очищен, пока игрок смотрит вкладку/летит.
5. **Границы.** Новых сущностей/таблиц не требуется (владение уже есть); нужен только
   список и действия. Подтвердить API-форму: отдельный список собственности игрока.
6. **Нарезка.** Список и «Посмотреть» — одно; «Перелететь» с двумя плечами — отдельный
   чекпоинт из-за сложности маршрута. Подтвердить у создателя.

## 4. Границы

- Не переписывать существующие механизмы полёта и знания — только использовать их.
- Не добавлять новые сущности владения: владелец-игрок уже есть.
- Не раскрывать игроку неизвестную планету через вкладку собственности.
- Вне скоупа: продажа/дарение собственности, управление имуществом из вкладки, рейтинги,
  доходность, уведомления о нападении.

## 5. Маршрут (предложение)

1. Дизайн-спека: данные и API списка, вкладка и действия, поведение «Посмотреть» по знанию.
2. Отдельно — дизайн «Перелететь» с двумя плечами и краевыми случаями.
3. `@critic` после спеки — связь с полётами, знанием, владением.
4. Гейт создателя → разработка → тестер.

## 5а. Критика идеи (2026-09-25)

**Вердикт:** 1 крупное, 4 средних, 3 мелких.

- **Крупное** (раскрытие неизвестной планеты через вкладку) закрыто решением создателя:
  собственность не даёт обзора, сведения — только присутствие или ретранслятор.
- **Средние к учёту в дизайне:** автоматическая цепочка двух плеч уже есть — переиспользовать;
  ветвление «своя/чужая система, орбита, поверхность, уже на месте, активный полёт»; стык
  «дашборд ↔ карта» для открытия модалки и полёта; живость списка при вымирании поселения,
  смене владельца и очистке мира.
- **Мелкие:** терминология (в проекте — модалка системы, термин «собственность» не заведён);
  пока нет игрового пути приобретения собственности (владелец-игрок ставится админом);
  полиморфный владелец без FK — возможны строки-сироты.

## 5б. Критика спеки (2026-09-26)

Прогон `@critic` по спеке `docs/specs/2026-09-26-собственность-игрока-в-дашборде.md`:
**крупных 0, средних 6, мелких 4.** Фундамент не сломан. Создатель: «чини» (делегировал решения).

Закрыто в спеке (2 пакета правок дизайнера):
- **С1** пакетное чтение знания в новом файле репозитория (per-planet резолв не переиспользуется);
- **С2** честное поведение для системы вне знания (карточка звезды — только при наличии кэша карты,
  иначе тост «система вне зоны видимости — данных нет», попап не открываем);
- **С3** детерминированный приоритет deeplink над отложенными авто-открывателями (прибытие/пояс);
- **С4** ветвление «своя/чужая система» — серверным флагом модалки, не косвенным признаком;
- **С5** запуск маршрута — существующей обёрткой (маркер/закрытие модалки/слежение), не низкоуровневым стартом;
- **С6** (координация) — менеджер: спека **не трогает** занятые файлы (`planet_visibility.go`,
  `knowledge_repository.go`) — доступ к знанию живёт в новом `property_repository.go`; номер миграции
  `000090` забронирован в `docs/COORDINATION.md`;
- мелкие М1–М4 (ссылки словаря, fallback имени строения, формулировка индекса, ложный вывод «ничего не добавляет») — исправлены.

Решения-развилки (закрыты менеджером, гейт создателя 2026-09-26): **В1** «Посмотреть» = переход
на карту; **В2** числа в списке не показываем; **В3** система вне знания — честное «данных нет»;
**В4** вымершее поселение остаётся в списке.

## 6. Связь с ЧК2в

Пожелание пришло в эпик «Постройка структур на планете» во время чекпоинта ЧК2в. Отдельный
гейт ЧК2в по росту цены во времени остаётся открытым; эта идея его не заменяет.

## 7. Чекпоинты (нарезка менеджера, 2026-09-26)

- ✅ **ЧК1 — вкладка «Собственность»: список + «Посмотреть».** Реализован 2026-09-26, **принят
  создателем**. Двумя подэтапами, у каждого свой гейт и ревью/тест:
  - ✅ **ЧК1а — сервер:** миграция `000090` (обычный индекс владельца `buildings`), ручка
    `GET /me/property` (свой по JWT, пакетно, без утечки знания; пакетное чтение знания —
    в новом `property_repository.go`, занятые файлы не тронуты), серверные тесты. Ревью без
    блокеров; две мелкие правки менеджера (тай-брейк порядка, чистота доки БД).
  - ✅ **ЧК1б — клиент и стык:** `web/index.html` (+кнопка вкладки и пустой `.tab-pane`) + модуль
    `dashboard/property.js` (список, пустое состояние, плашка знания, кнопка «Посмотреть» → URL);
    стык `map/deeplink.js` + hook в `main.js` (авто-открытие попапа по URL, подавление
    авто-открывателей по флагу, затирание URL через `history.replaceState`), Node-тесты и
    e2e-смоук. Ревью без блокеров; прогон `@tester` **10/10 PASS** (живые данные: своя строка
    видна, чужая не показывается).
- ✅ **ЧК2 — «Перелететь».** Реализован 2026-09-26, **принят создателем**; код — коммит `65d6e8c`.
  Один клик доводит до планеты откуда угодно — межзвёздный + внутрисистемный на существующей
  цепочке полётов. Итог:
  - в строке списка добавлена кнопка **«Перелететь»** (стык `&fly=1`); без двигателя — неактивна с
    тултипом (признак из `/me` — то же правило, что у карты);
  - действие «лететь к планете» вынесено в общий хелпер `flyToPlanet` (`modal/events.js`) — им
    пользуются и меню планеты, и переход с дашборда (один предикат режима `flightModeForSystem()`,
    те же обёртки `startIntraFlight`/`startCompositeFlight`, без низкоуровневого `startFlight`);
  - стык `map/deeplink.js`: ветка `fly=1` ждёт готовности попапа (флаг `modalState.meLoaded` +
    список планет) и запускает маршрут; если целевой планеты в попапе нет — тихо ничего (попап сам
    честно показывает «нет данных»), без вводящего в заблуждение тоста;
  - ревью — без блокирующих; найденный риск гонки выбора режима (межзвёздный полёт ещё грузился из
    `/me`) закрыт доводкой с флагом готовности `meLoaded`; прогон `@tester` — **9/9 PASS** (живой
    браузерный смоук, 0 ошибок консоли);
  - ветки «чужая видимая система», «активный межзвёздный при клике», «уже у планеты» реальным
    игроком закрыты только подстановкой ответов (живых данных в dev-БД нет) — см. хвосты §7б.
- ✅ **ЧК3 — доводка интерфейса/текстов** по итогам ЧК1/ЧК2 (предложение `@uidesigner` + решения создателя — §9). Реализован и принят 2026-09-26, код — коммит `be48759`: двухстрочная строка, отдельный `dashboard.css`, подписи «На карте»/«🚀 Лететь», тексты, статус маршрута («в пути к…»/«прибыли»). Ревью без блокирующих; прогон `@tester` — 8 PASS / 1 N-A (живой полёт в dev-БД отсутствует — хвост §10).

**Дальше:** чекпоинты фичи пройдены (ЧК1–ЧК3). Открытые хвосты — §7б и §10 (живой полёт к своей планете; шапка дашборда с сырыми id миров; дельты доков).

**Ретроспектива:** —

## 7а. Разнос по докам: **ГОТОВО** (дельта менеджера, ЧК1; разнесено @dispatcher 2026-09-26)

Горячие доки правит `@dispatcher` (менеджер не пишет). Дельты — дословно:

- `CHANGELOG.md`: +2026-09-26, `Реализовано: вкладка «Собственность» в дашборде игрока (ЧК1) —
  список своей собственности (поселения/строения) + «Посмотреть» с переходом на карту;
  ручка GET /me/property; индекс idx_buildings_owner (миграция 000090)`.
- `STATUS.md` §1: строку **не добавлять** (это реализованная фича, её место — CHANGELOG, не §1).
- `docs/specs/README.md` — ✅ **уже внесено менеджером** (строка спеки = «✅ ЧК1 реализован 2026-09-26»).
- `docs/EPICS.md` — ✅ **уже внесено менеджером** (сателлит эпика «Постройка структур на планете»).
- `docs/impact_map.json` — менеджеру **внести узлы/связи §12 спеки**: новые узлы `player_property`
  (`internal/handlers/property_handlers.go` + `internal/repository/property_repository.go`),
  `property_ui` (`web/static/js/dashboard/property.js`), `dashboard_map_deeplink`
  (`web/static/js/map/deeplink.js`); **обратные рёбра** у существующих: `buildings` ←
  `player_property` (reads), `player_planet_knowledge`/`planet_knowledge_snapshot` ← `player_property`
  (reads), `modal`/`openSystemModal` ← `dashboard_map_deeplink` (affects), `map/config.js`
  (`state.worlds`) ← `dashboard_map_deeplink` (reads), `users.current_position`/`pending_destination`
  ← `dashboard_map_deeplink` (ЧК2, affects). Файл правит `@manager` (зона менеджера) — внести при
  следующем проходе.
- `docs/DB.md`, `docs/ARCHITECTURE.md` — ✅ **уже внесено developer'ом** (миграция `000090`,
  строка read-модели §2, блок «Дашборд игрока»).
- `docs/pitfalls/front-ui.md` — ✅ **уже внесено developer'ом** (ловушка приоритета deeplink
  над авто-открывателями карты).
- `docs/GLOSSARY.md` — предлагаемые термины спеки §15 («Собственность игрока», «Единица
  собственности» — в §1 к «Владелец (owner_type)»; «Попап системы» — §15): **внесение на
  усмотрение создателя** (терминология — GDD-зона, не блокирует; хвост не заводим, т.к. адрес —
  «по слову создателя»).

### ЧК1+ЧК2 — код закоммичен (дополнение 2026-09-26)

- **Код фичи** (вкладка, стык, «Перелететь», `meLoaded`, тесты, e2e, миграция, ручка) — коммит
  `65d6e8c`, 18 файлов, чужих нет. Состав: `cmd/server/main.go`, `internal/handlers/property_handlers.go`
  (+тест), `internal/repository/property_repository.go`, `internal/integration/property_*` (2 теста),
  `migrations/000090_buildings_owner_index.sql`, `web/index.html`, `web/static/js/main.js`,
  `web/static/js/map/data.js`, `web/static/js/map/deeplink.js`, `web/static/js/dashboard/property.js`,
  `web/static/js/modal/{index,events,state}.js`, `web/frontend_{property,deeplink}_test.go`,
  `tools/e2e/dashboard-property-check.js`.
- **Осталось вне коммита** (файлы смешаны с чужой незакоммиченной линией воды — коммит за
  `@dispatcher`): `docs/DB.md`, `docs/ARCHITECTURE.md`, `docs/pitfalls/front-ui.md`,
  `docs/specs/README.md` (в них наши строки про собственность).
- `CHANGELOG.md` (дельта `@dispatcher`, ЧК1+ЧК2 объединены):
  `+2026-09-26, Реализовано: собственность игрока в дашборде (ЧК1+ЧК2) — вкладка «Собственность» (список поселений/строений + «Посмотреть») и кнопка «Перелететь» (переход на карту с &fly=1, доводит до планеты существующей цепочкой полётов); ручка GET /me/property; индекс idx_buildings_owner (миграция 000090)`.
- `STATUS.md` §1 — строку **не добавлять** (реализованная фича → CHANGELOG, не §1).

## 7б. Хвосты (дельта в `docs/TAILS.md`, адрес у каждого)

**Хвосты ЧК1 разнесены** (хвосты 80–82 в `docs/TAILS.md`, 2026-09-26); прочие пункты — по адресам ниже.

- Не проверено живьём: плашки знания `presence` («свежие данные») и `snapshot` (снимок с датой)
  для строки собственности — в dev-БД не нашлось планеты владения с нужным знанием; закрыто
  только Node-тестом. **Адрес:** `@tester` — при следующем касании вкладки/знания (снимок знания
  игроку о планете владения).
- Ветка «мир не в кэше карты и `/worlds/{id}` → 403» проверена тостом на синтетическом id;
  реального владения вне радиуса в БД нет. **Адрес:** `@tester` — при появлении строения игрока
  в системе вне радара.
- Визуал вкладки глазами игрока (пиксели/подписи) не отсматривался отдельно; скриншоты собраны
  тестером. **Адрес:** `@uidesigner` — при доводке ЧК3.
- `docs/impact_map.json` — узлы/связи ЧК1 и ЧК2 не внесены (см. §7а). **Адрес:** `@manager` —
  при следующем проходе по реестру.
- **ЧК2:** ветки «чужая видимая система», «активный межзвёздный полёт при клике „Перелететь“»,
  «уже у планеты» реальным игроком **не проверены живьём** — закрыты подстановкой ответов (живых
  данных/флоу посадки в dev-БД нет). **Адрес:** `@tester` — при появлении данных (чужой видимый
  мир; игрок в межзвёздном полёте; игрок на орбите/поверхности целевой планеты).
- **ЧК2:** дельта спеки §8.2 про «планета не найдена → тост как у меню» реализована иначе —
  при отсутствии целевой планеты в попапе deeplink молча ничего не делает (попап честно показывает
  «нет данных»). **Адрес:** `@designer` — поправить формулировку §8.2 спеки при следующем касании.
- `docs/DB.md`, `docs/ARCHITECTURE.md`, `docs/pitfalls/front-ui.md` — свойственные (property)
  дельты ЧК1/ЧК2 **не закоммичены**: файлы смешаны с чужой незакоммиченной линией воды.
  **Адрес:** `@dispatcher` — закоммитить после коммита линии воды.
- `docs/specs/README.md` — строка спеки собственности не обновлена до «ЧК2 реализован» (файл
  смешан с чужими строками специй). **Адрес:** `@designer`/`@dispatcher` — при разблокировке файла.

## 8. Вызов на новую сессию (ЧК3 — доводка интерфейса/текстов)

Стартовать в **новой сессии**, задача — **ЧК3**: разгрузка вкладки и формулировки по итогам
ЧК1/ЧК2 (пиксели/подписи — `@uidesigner`), плюс статус маршрута в строке списка («в пути к…»,
«прибыли») — источник `/me`, без новой сущности (спека §8.5). Брать за источник: спека
`docs/specs/2026-09-26-собственность-игрока-в-дашборде.md` §6, §8.5, §16 (ЧК3) и хвосты §7б
(визуал вкладки не отсматривался глазами игрока). Рамки: клиентская половина
(`dashboard/property.js`, при необходимости `map/deeplink.js`); новой механики не изобретать.
При правке дельт учти, что файл вкладки небольшой, а `web/index.html` — ~908 строк (разметку
туда не растить, рисует модуль).

## 9. ЧК3 — решения создателя и ТЗ интерфейса (2026-09-26)

Источник — предложение `@uidesigner` (сессия 2026-09-26) и решения создателя на гейте.

### 9.1. Решения создателя (гейт 2026-09-26)

- **Разгрузка вкладки** — по предложению `@uidesigner`: направление «компактная двухстрочная
  строка». Альтернативы (плитки-карточки, группировка по системам) — кандидаты на будущее, в
  ЧК3 не тащим.
- **Подписи кнопок** — новые: **«На карте»** и **«🚀 Лететь»** (было «Посмотреть»/«Перелететь»).
  Решение создателя: «дизайнер норм предлагает».
- **Иерархия кнопок** — решение создателя «без разницы» → **менеджер**: акцент на безопасном
  «На карте» (основное действие), «Лететь» — тише (защита от случайного полёта).
- **Сигнал прилёта** — **достаточно чипа в строке** (тост не добавляем).
- **Оформление** — **отдельный файл оформления дашборда** `web/static/css/dashboard.css`
  (стили вкладки уходят из модуля; страница не растёт). Остальные вкладки дашборда — позже,
  отдельной задачей.

### 9.2. Строка (компактная двухстрочная)

- **Ярус 1:** `иконка Имя · <тип>` слева; действия справа.
- **Ярус 2:** адрес «система › планета», плашка знания и (если есть) статус маршрута — в одну
  строку.
- Иконки: поселение `🏘` (было `🏠` — дублировало иконку вкладки), строение `🏗` (оставляем);
  `subtitle` («Поселение»/«Строение») оставляем.
- Плашка знания — «пилюля» внутри яруса, не отдельный блок.
- Стили — в `dashboard.css` (классы `property-*`), инлайн-стили из модуля убрать.
- **Не трогать:** строка владения видна всегда; состояние планеты — только по знанию; адрес
  раскрыт; живых чисел планеты (население/стабильность/поверхность) в списке нет (В2).

### 9.3. Статус маршрута (из `/me`, без новой сущности)

- **Сопоставление строки с полётом** (планета одна — однозначно):
  - внутрисистемное плечо: `current_position.status === 'in_flight' && to_type === 'planet' && to_id === item.planet_id`;
  - композит (межзвёздное + второй шаг): `flight.to === item.world_id && pending_destination.object_id === item.planet_id`;
  - чистый межзвёздный к звезде (без намерения на планету) — статуса ни одной строке (цель — звезда).
- **«В пути»:** чип `🚀 В пути к <планета>`, пока полёт активен (+ время прилёта — показывать).
- **«Прибыли»:** чип `🚀 Прибыли` — вспышка ~4 с по прилёте, затем гаснет (как тост карты), НЕ
  постоянная метка «ты здесь». Момент прилёта ловится таймером до `arrive_at` (или
  `start_time + duration`), затем перечитать `/me`.
- **Крайние:** строки, к которым полёт не относится, — статуса нет (пустого чипа не рисуем);
  битый/отсутствующий `arrive_at` — «в пути» без времени; пересчёт на каждом `load`;
  `visibilitychange` догоняет; объект исчез из выдачи — показывать некому, ничего не делаем.

### 9.4. Формулировки

- Плашка `none`: «Нет данных» → **«Нет данных о планете»**.
- Плашка `scan`: добавляем « · устарело» при `fresh === false` (выравнивание с попапом).
- Ошибка загрузки: «❌ Не удалось загрузить собственность.» → **«❌ Не удалось загрузить
  собственность. Обновите вкладку.»**
- Без изменений: `presence`/`snapshot` плашки, адрес, пустое состояние, тултип без двигателя,
  тост «система вне зоны видимости — данных нет».

### 9.5. Файлы и тесты

- Правка: `web/static/js/dashboard/property.js` (разметка строки, статус, тексты), новый
  `web/static/css/dashboard.css`, `web/index.html` (одна строка `<link rel="stylesheet"
  href="/static/css/dashboard.css">` в `<head>`), `web/frontend_property_test.go` (обновить
  текстовые ассерты + новые тесты статуса).
- **Классы `property-view-btn`/`property-fly-btn` сохранить** (тесты на классы остаются
  зелёными); обновить только ассерты на тексты.
- URL-стык `&fly=1`, `escapeHtml`, `gameDate`, гейт двигателя, один in-flight запрос,
  `401/403` → логин, Node-безопасность модуля — **инварианты, не трогать**.
- **Не трогать:** сервер/знание/полёты, `web/static/js/map/deeplink.js`, `property_repository.go`.

### 9.6. Вне ЧК3 (замечено)

- Шапка дашборда «Статус полёта» показывает вместо имён миров их сырые id
  (`web/index.html`, `loadUser`: «🚀 В полёте: <uuid> → <uuid>»). → хвост при сдаче (адрес:
  `@developer` — при следующем касании шапки дашборда).

## 10. Разнос по докам: ЧК3 (дельта `@manager` → `@dispatcher`)

Сдача ЧК3. **Код** — коммит `be48759` (5 файлов: `web/static/js/dashboard/property.js`,
`web/static/css/dashboard.css` (новый), `web/index.html`, `web/frontend_property_test.go`,
`tools/e2e/dashboard-property-check.js`). Артефакты (идея, спека, `EPICS.md`, QA-отчёт) —
коммит менеджера. Метку занятости в `docs/COORDINATION.md` менеджер снял.

**Горячие доки** (пишет `@dispatcher`) — дословно:

- `CHANGELOG.md`: +2026-09-26, `Реализовано: ЧК3 вкладки «Собственность» дашборда —
  компактная двухстрочная строка, отдельный dashboard.css, подписи «На карте»/«🚀 Лететь»,
  тексты (плашка «Нет данных о планете», «устарело» у сканера, текст ошибки), статус маршрута
  в строке («в пути к…»/«прибыли» из /me)`.
- `STATUS.md` §1: строку **не добавлять** (реализованная фича → CHANGELOG, не §1).
- `.opencode/context.md`: в указателе про собственность строку «Далее — ЧК3 (доводка
  интерфейса/текстов)» заменить на «ЧК3 ✅ (коммит `be48759`) — доводка интерфейса/текстов и
  статус маршрута в строке; фича завершена».
- `docs/TAILS.md`:
  - **добавить хвост:** `Живой статус маршрута в строке собственности («в пути к…»/«прибыли»)
    не проверен живьём — в dev-БД нет летящего игрока с владением; закрыт Node-тестом (все
    ветки + прошлое/битое время прибытия)` | прогон @tester 2026-09-26 (ЧК3, коммит `be48759`) |
    `@tester` — при появлении летящего игрока с владением | 2026-09-26.
  - **добавить хвост:** `Шапка дашборда «Статус полёта» показывает сырые id миров вместо имён
    («В полёте: <uuid> → <uuid>»; web/index.html, loadUser)` | замечено при ЧК3 | `@developer` —
    при следующем касании шапки дашборда | 2026-09-26.
  - **закрыть хвост 82** (визуал вкладки глазами игрока не отсматривался) — закрыт доводкой
    ЧК3 и прогоном тестера (браузерный смоук 8/8, скриншоты).

**Зона менеджера** (внести при следующем проходе по реестру):

- `docs/impact_map.json` / `docs/impact_map.md` — в заметке узла `property_ui` дополнить: ЧК3 —
  двухстрочная строка, `dashboard.css`, подписи «На карте»/«🚀 Лететь», статус маршрута из
  `/me` (коммит `be48759`).
- `docs/specs/README.md` (зона `@designer`, файл смешан с чужими правками — хвост 76) — строку
  спеки обновить до «✅ ЧК1–ЧК3 реализованы 2026-09-26».
