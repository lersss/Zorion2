# Студия: рефакторинг монолита по блокам (эпик)

**Дата:** 2026-09-25
**Пожелание создателя:** «а нам не пора рефакторить studio.html по блокам?» → «режь, и го
рефакторить» (2026-09-25).
**Вектор:** операционка / интерфейс студии (инструмент контента, не игровая механика).
**Статус:** 🔄 в работе — нарезка укрупнена до **8 шагов** (решение создателя 2026-09-25).
**ЧК1 ✅** (коммит `8ed5e4e`: пилот — вынос экспорт/импорта в `content_transfer.js` +
тест-помощник; смоук 5/5, ревью пройдено). **ЧК2 ✅** (коммит `342b08c`: ядро →
`core.js`, взаимодействия канваса → `interactions.js`; ревью пройдено, браузерный смоук
пройден). **ЧК3 ✅** (коммит `48ec089`: граф товаров → `graph.js`; ревью «блокирующих нет»,
браузерный смоук 9/9). **ЧК4 ✅** (коммит `6fd7915`: попап товара → `goods_popup.js`,
ИИ-блок → `fill_ai.js`; ревью «блокирующих нет», браузерный смоук 9/9). **ЧК5 ✅** (коммит
`6f8e873`: диалоги/каркас справочника → `dialogs.js`, справочник производителей →
`producers_ref.js`; ревью «блокирующих нет», браузерный смоук 9/9). **ЧК6 ✅** (коммит `ecedcba`:
масштаб единицы и редакторы попапа производителя → `unit_scale_calc.js`, справочник предметов →
`items_ref.js`; ревью «блокирующих нет», браузерный смоук 9/9). **ЧК7 ✅** (коммит `16ed85e`:
дерево и разделы построек → `buildings_tree.js`; ревью «блокирующих нет», браузерный смоук 9/9).
Дальше **ЧК8** (последний) — селекторы + попапы каталога + оверлеи/режимы/сборка/инициализация.

**Связи:** `web/studio.html` (1297 строк; инлайновый `<script>` — остаток после выноса
ЧК1–ЧК7); `web/frontend_studio_*.go` (контрактные тесты читают файл через общий помощник
`studioHTML`); `web/static/js/studio/auth.js` и `web/static/js/unit_scale.js` (уже вынесенные
модули — прецедент); `AGENTS.md` §4.8 («файл >200–300 строк — делить»); `docs/COORDINATION.md`
(мягкий локейт файла); `docs/EPICS.md` (строка эпика).

---

## 0. Цель и зачем

- Студия — **один файл 5120 строк**, из которых ~4620 строк — единый блок кода внутри
  страницы. По проектному правилу «файл длиннее 200–300 строк — делить» превышение ~15×.
- **Цена сегодня:** любая правка студии — правка гигантского файла; обзор диффа теряется,
  легко зацепить соседний блок, параллельные линии конфликтуют на одном файле.
- **Цель:** разнести код студии на связные блоки-файлы. **Поведение не меняется** — это чистый
  перенос строк, без переименований и «улучшений» по пути.

## 1. Жёсткие рамки (почему так, а не иначе)

1. **Классические скрипты, НЕ ES-модули.** Разметка дёргает функции прямо из атрибутов
   (`onchange=`/`onclick=`), т.к. функции и состояние — глобальные. Перевод в `import/export`
   снесёт глобальность → интерфейс отвалится. Выносим как обычные `<script src="...">` **без**
   `type="module"`, подключая их **до** инлайнового скрипта (чтобы объявления существовали
   раньше, чем инлайновая часть их использует).
2. **Поведение и строки — дословно.** Переносим текст функции как есть. Никаких переименований,
   «чисток», смены порядка строк внутри блока.
3. **Один блок — один чекпоинт** со своим гейтом и браузерным смоуком студии.
4. **Контрактные тесты не переписываем по одному.** Все `web/frontend_studio_*.go` читают файл
   через **единственный** помощник `studioHTML()` в `frontend_studio_subtype_test.go`. Учим его
   читать и вынесенные файлы — тогда 6 тест-файлов остаются нетронутыми.
5. **Сторож зацикливания.** Большой файл правим **одной сводной записью**, не серией мелких
   правок (иначе сессия рубится). На один большой файл — не более двух батчей.
6. **Игровой код вне зоны.** Правки только в `web/studio.html`, `web/static/js/studio/*` и
   тест-помощнике.

## 2. Карта блоков: 30 секций → 14 файлов

Границы — по существующим секционным маркерам `// ---------- ... ----------`. Объём примерный.
Колонка «ЧК» — в какой чекпоинт (шаг) входит файл.

| № | Файл | Что несёт | Строки сейчас | ~строк | ЧК |
|---|---|---|---|---|---|
| 1 | `studio/core.js` | глобалы, `state`, `$`, константы канваса, `fetchState`, отчёты, `esc`, `studioFetch`, `api` | 492–737 | ~246 | ЧК2 |
| 2 | `studio/interactions.js` | контекстное меню, drag&drop карточки справочника на канвас | 1368–1648 | ~281 | ЧК2 |
| 3 | `studio/graph.js` | скоп ветки «Товары», отрисовка графа, тултип, клики/пан/зум | 738–1367 | ~630 | ЧК3 |
| 4 | `studio/goods_popup.js` | попап-деталь товара, сложность, объём/вес, дроп в слот | 1649–1901 + 2279–2333 | ~308 | ЧК4 |
| 5 | `studio/fill_ai.js` | «заполнить комплектующие», попап «Описания ИИ», локальный ИИ-помощник | 1902–2142 | ~241 | ЧК4 |
| 6 | `studio/content_transfer.js` | экспорт/импорт снимка контента | 2143–2278 | ~136 | ✅ ЧК1 |
| 7 | `studio/dialogs.js` | модалки, «добавить родителя», каркас справочника | 2335–2511 | ~177 | ЧК5 |
| 8 | `studio/producers_ref.js` | справочник производителей | 2512–2890 | ~379 | ЧК5 |
| 9 | `studio/unit_scale_calc.js` | масштаб единицы темпа | 2891–3439 | ~549 | ЧК6 |
| 10 | `studio/items_ref.js` | справочник предметов | 4179–4328 | ~150 | ЧК6 |
| 11 | `studio/buildings_tree.js` | дерево построек на канвасе + разделы построек | 3440–4178 | ~739 | ЧК7 |
| 12 | `studio/selectors.js` | селекторы скопа ветки «Товары» | 4329–4380 | ~52 | ЧК8 |
| 13 | `studio/catalog_popups.js` | категории/товары, попап категорий, «Эффекты», формы добавления | 4381–4731 | ~351 | ЧК8 |
| 14 | `studio/init.js` | оверлеи состояний, режимы канваса, сборка, разделы студии, инициализация и проводка событий | 4732–5111 | ~380 | ЧК8 |

Итого 14 файлов; в чекпоинты они сгруппированы по 1–3 файла (ниже).

## 3. Чекпоинты (8 шагов)

Решение создателя 2026-09-25: шаг **крупный** (7–8 чекпоинтов на эпик, а не 12). Точные
границы блоков исполнитель подтверждает на каждом шаге; шаг может дать 1–3 файла.

| # | Чекпоинт | Файлы шага | ~строк | Состояние |
|---|---|---|---|---|
| **ЧК1** | Пилот: экспорт/импорт + правка тест-помощника `studioHTML()` | `content_transfer.js` | 137 | ✅ `8ed5e4e` (смоук 5/5, ревью пройдено) |
| **ЧК2** | Ядро + взаимодействия канваса | `core.js`, `interactions.js` | 527 (246+281) | ✅ `342b08c` (ревью пройдено, браузерный смоук пройден) |
| **ЧК3** | Граф товаров | `graph.js` | 629 | ✅ `48ec089` (ревью «блокирующих нет», браузерный смоук 9/9) |
| **ЧК4** | Попап товара + ИИ-блок | `goods_popup.js`, `fill_ai.js` | 552 (291+261) | ✅ `6fd7915` (ревью «блокирующих нет», браузерный смоук 9/9) |
| **ЧК5** | Модалки + справочник производителей | `dialogs.js`, `producers_ref.js` | 558 (178+380) | ✅ `6f8e873` (ревью «блокирующих нет», браузерный смоук 9/9) |
| **ЧК6** | Масштаб единицы темпа + справочник предметов | `unit_scale_calc.js`, `items_ref.js` | 699 (549+150) | ✅ `ecedcba` (ревью «блокирующих нет», браузерный смоук 9/9) |
| **ЧК7** | Дерево и разделы построек | `buildings_tree.js` | 739 | ✅ `16ed85e` (ревью «блокирующих нет», браузерный смоук 9/9) |
| **ЧК8** | Селекторы + попапы каталога + оверлеи/режимы/сборка/инициализация (последним) | `selectors.js`, `catalog_popups.js`, `init.js` | ~783 | ⬜ ждёт |

**Почему такой порядок:** сначала безопасный лист, покрытый тестами (ЧК1), затем ядро (ЧК2),
далее блоки по возрастанию риска, крупные — к концу, проводка событий (`init.js`) — последней
(она ссылается на всех и на этапе выноса подключается последним классическим скриптом).

### ЧК1 — пилот (пройден)

- **Что вынесли:** строки 2143–2278: `exportContent`, `refreshImportStatus`, `importContent`,
  `openImportPopup`, `closeImportPopup`, `applyImportContent`, `sumCounts`, `importDiff`.
- **Куда:** `web/static/js/studio/content_transfer.js`, классический скрипт; подключён
  `<script src="/static/js/studio/content_transfer.js"></script>` **перед** инлайновым скриптом.
- **Правка тест-помощника:** `studioHTML()` читает `web/studio.html` **и** тела вынесенных
  классических скриптов студии, в порядке подключения — отдельные тест-файлы не правятся.
- **Результат:** студия 5120 → 4986 строк; смоук 5/5; ревью пройдено; коммит `8ed5e4e`.

### ЧК2 — ядро + взаимодействия (пройден)

- **Что вынесли (HEAD-строки):** `core.js` ← 494–739 (`"use strict";`, глобалы/`state`,
  `$`, константы канваса, `zoom/pan`, секция «состояние» — `fetchState`, `showReport`,
  `hideReport`, `aiWait*`, `esc`, `codeNote`, малая секция «граф» — `fullTreeMode`,
  `focusSubset`); `interactions.js` ← 1369–1648 (контекстное меню карточки + drag&drop
  карточки справочника на канвас, вкл. `bindRecipeToFactory`).
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` →
  инлайновый `<script>`; каждый классический скрипт и инлайн начинаются с `"use strict";`.
- **Границы:** блок взаимодействий вырезан как 1368–1648 (вместе с пустой строкой-разделителем
  1368), чтобы на стыке не осталось двух пустых строк подряд; иного не тронуто.
- **Результат:** студия 4986 → **4462** строки (−524); `core.js` 246, `interactions.js` 281.
  `node --check` обоих файлов и тела инлайна — OK; сверка множеств объявлений верхнего уровня
  354 → 354 (missing=0, extra=0); DoD (`go build`/`go vet`/`-race -short`) зелёный, `go test ./web/...`
  PASS; ревью — «блокирующих нет» (перенос подтверждён побайтово, посторонних правок нет).
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК2-вынос-ядра.md`):**
  `/studio` без JS-ошибок; все вынесенные имена на месте; живой правый клик по карточке дал меню
  (6 кнопок); живой `dragenter` на канвас дал `dnd-over`; экспорт снимка контента (ЧК1) жив.
  Единственный FAIL — известный хвост A6 (`RECIPE_FREE=71` нет в dev-БД → 404, ожидался 409),
  данные, не регрессия.
- **Коммит:** `342b08c`.

### ЧК3 — граф товаров (пройден)

- **Что вынесли:** `graph.js` ← HEAD-строки `web/studio.html` 497–1124 — секции
  «скоп ветки Товары» (`factoryType`…`visibleGoods`, `depthOf`, `computeLayout`,
  `childrenOf`/`colsOf`/`place`/`orthoEdge`), «отрисовка графа» (`renderGraph`,
  `trunc`/`truncPx`/`wrapPx`/`roundRect`/`catName`), «тултип карточки» (`showTip`/`hideTip`),
  «клики и пан/зум» (`hitTest`, `saveView`, `centerTop`, `centerProdTree`, `centerOn`).
  Top-level `let` блока (`visibleGoodsCache`, `panning`/`downPos`/`moved`, `downOnCard`)
  уехали вместе с блоком.
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` →
  `graph.js` → инлайновый `<script>` (`web/studio.html:492–496`); каждый классический
  скрипт начинается с `"use strict";`.
- **Границы:** блок 497–1124 заменён на одну пустую строку; на стыке получилось
  `"use strict";` (строка 497) / пустая (498) / маркер `// ---------- попап-деталь` (499).
  Иного не тронуто.
- **Результат:** студия 4462 → **3835** строк (−627); `graph.js` 629 строк.
  `node --check` файла и тела инлайна — OK; сверка множеств объявлений верхнего уровня
  372 → 372 (missing=0, extra=0), дублей между скриптами нет; перенос подтверждён
  побайтово (тело `graph.js` == HEAD-строки 497–1124); реконструкция `studio.html` ==
  HEAD + один тег подключения + пустой разделитель. DoD (`go build`/`go vet`/`-race -short`
  без генераторов) и `go test ./web/...` — зелёные; ревью — «блокирующих нет».
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК3-вынос-графа.md`):**
  9/9 PASS — `/studio` без JS-ошибок; 12 имён вынесенного блока — функции; граф реально
  нарисован (19 размещённых карточек, непустых пикселей ~15%); живой правый клик по карточке
  дал меню (6 кнопок); живой `dragenter` дал подсветку канваса; колесо мыши зумит и вид
  сохраняется в `localStorage`; экспорт снимка контента (ЧК1) жив.
- **Наблюдение тестера (не баг продукта):** при **программном** включении галки «всё дерево»
  без события `change` служебный оверлей `#focusEmptyOverlay` (`z-index:5`) остаётся поверх
  канваса и перехватывает мышь — живой флоу (клик по галке) оверлеи прячет. Учесть в
  e2e-скриптах: взводить галку реальным кликом/с событием.
- **Коммит:** `48ec089` (код: `web/studio.html`, `web/static/js/studio/graph.js`).

### ЧК4 — попап товара + ИИ-блок (пройден)

- **Что вынесли (HEAD-строки):** `goods_popup.js` ← 499–751 + 1012–1048 — попап-деталь
  (`openPopup`, `renderPopup`, `renderBindSection`, `bindFactory`/`unbindFactory`, `openParent`,
  `selectGood`, переименование/смена категории, `api`, `popupRecipeId`, слоты `addSlot`/`clearSlot`/
  `delSlot`/`setAllowResource`/`setQuantity`) и «сложность/объём-вес/дроп в слот»
  (`setComplexity`, `setVolumeWeight`, `slotDragOver`/`slotDragLeave`, `dropOnSlot`).
  `fill_ai.js` ← 752–1011 — «заполнить комплектующие» (`fillGood`), попап «Предложения ИИ»,
  описания (`describeGood`, `openDescPopup`…`cancelDesc`), пакетный прогон, локальный
  ИИ-помощник (`updateAIStatus`, `aiAction`, `aiStart`/`aiStop`), помощники грязного попапа.
- **Почему `goods_popup.js` из двух кусков:** карта §2 (§«попап-деталь, сложность, объём/вес,
  дроп в слот») — эти функции лежат в файле раздельно; оба куска принадлежат попапу товара.
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` → `graph.js` →
  `goods_popup.js` → `fill_ai.js` → инлайновый `<script>`; `"use strict";` в каждом.
- **Результат:** студия 3835 → **3286** строк (−549); `goods_popup.js` 291, `fill_ai.js` 261.
  `node --check` (оба + тело инлайна) — OK; объявления верхнего уровня 362 → 362 (missing=0,
  extra=0), дублей нет; оба тела подтверждены побайтово; реконструкция `studio.html` == HEAD +
  два тега + один разделитель. DoD и `go test ./web/...` — зелёные; ревью — «блокирующих нет».
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК4-вынос-попапа-и-ии.md`):**
  9/9 PASS — `/studio` без JS-ошибок; 27 имён двух блоков — функции; живой попап товара
  (двойной клик по карточке → `#detailPopup` flex, тело непустое → закрытие); живая подсветка
  слота при `dragover`/`dragleave`; ИИ-индикатор жив (`aiStatusText` заполнен); граф, правый клик
  и экспорт (ЧК1–ЧК3) не сломаны.
- **Замечание тестера (не баг):** попап открывается двойным кликом (обработчик в `graph.js`,
  поведение не менялось выносом) — учесть в формулировке следующего чек-листа.
- **Коммит:** `6fd7915` (код: `web/studio.html`, `web/static/js/studio/goods_popup.js`,
  `web/static/js/studio/fill_ai.js`).

### ЧК5 — диалоги + справочник производителей (пройден)

- **Что вынесли (HEAD-строки):** `dialogs.js` ← 501–677 — модалки (`openModal`, `closeModal`),
  удаление товара (`askDeleteGood`, `confirmDeleteGood`, `doDeleteGood`), «добавить родителя»
  (`askAddParent`, `addParent`), каркас справочника (`renderSprav`, `renderGoodsSprav`).
  `producers_ref.js` ← 678–1056 — справочник производителей (`renderProdSprav`, `prodClick`/
  `prodDblClick`, `openProdPopup`, `prodSpecRows`/`specRow`/`pblockHtml`, `renderProdPopup`,
  создание подтипа, переименование/смена категории, рецепты (`recipesBlock`, `recipeRate`,
  `saveRecipeRate`)).
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` → `graph.js` →
  `goods_popup.js` → `fill_ai.js` → `dialogs.js` → `producers_ref.js` → инлайновый `<script>`;
  `"use strict";` в каждом.
- **Побочный эффект на загрузке:** единственный top-level `document.addEventListener("keydown", …)`
  (Escape → `closeModal`) уехал в `dialogs.js`; порядок относительно других глобальных
  `keydown`-слушателей сохранён (`interactions.js` грузится раньше, инлайновых `keydown` вне
  диапазона нет) — ревью подтвердило, Escape/Enter работают как раньше.
- **Результат:** студия 3286 → **2732** строки (−554); `dialogs.js` 178, `producers_ref.js` 380.
  `node --check` (оба + тело инлайна) — OK; объявления верхнего уровня 362 → 362 (missing=0,
  extra=0), дублей нет; тела подтверждены побайтово; реконструкция `studio.html` == HEAD + два
  тега + один разделитель. DoD и `go test ./web/...` — зелёные; ревью — «блокирующих нет».
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК5-вынос-диалогов-и-справочника.md`):**
  9/9 PASS — `/studio` без JS-ошибок; 16 имён двух блоков — функции; живая модалка открывается и
  закрывается по `Escape`; каркас справочника рисуется; панель производителей заполняется (8 карточек),
  попап производителя открывается/закрывается; граф, правый клик, попап товара и экспорт
  (ЧК1–ЧК4) живы. Уточнение: попап производителя — общий `#detailPopup` (`#prodPopup` в разметке нет).
- **Коммит:** `6f8e873` (код: `web/studio.html`, `web/static/js/studio/dialogs.js`,
  `web/static/js/studio/producers_ref.js`).

### ЧК6 — масштаб единицы + справочник предметов (пройден)

- **Что вынесли (HEAD-строки):** `unit_scale_calc.js` ← 503–1050 — секция «масштаб единицы
  темпа» (`unitScaleModule`, `scaleDef`/`scaleToStored`/`scaleToDisplay`, `unitScaleSegmentHtml`,
  `setUnitScale`, `fmtNum`, `recipeTakeText`) и редакторы попапа производителя (`prodNorm`/
  `goodByNorm`, `prodPosOptions`/`prodEffOptions`, `fetchEffectTypes`, `prodEat*`/`eatBlock`,
  `stageBlock`/`prodStage*`/`saveStageBlock`, `prodArithmeticBlock`, `unbindFactoryRecipe`,
  `copyRecipes`, `popupRace*`/`saveProdRace`, `prodSlot*`, `saveProdJSON`/`saveProdHidden`,
  `askDeleteProducer`/`doDeleteProducer`, `linkItem`/`unlinkItem`, `prodAdd`).
  `items_ref.js` ← 1791–1939 — справочник предметов (`renderItemSprav`, `itemClick`/`itemDblClick`,
  `openItemPopup`/`renderItemPopup`, переименование/слот, `itemAdd`, тиры
  `toggleTier`/`toggleAllTiers`/`saveTierSel`, `spravClick`/`spravDblClick`, `dragStart`).
- **Кусок не смежен с первым:** между ними «дерево построек» (1052–1790) не тронуто — уходит в ЧК7.
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` → `graph.js` →
  `goods_popup.js` → `fill_ai.js` → `dialogs.js` → `producers_ref.js` → `unit_scale_calc.js` →
  `items_ref.js` → инлайновый `<script>`; `"use strict";` в каждом.
- **Поправка к ТЗ:** в брифе исполнителю `raceFamilyOf` был ошибочно указан в блоке масштаба — по
  факту она лежит ниже маркера «дерево построек» (остаётся в остатке, уходит в ЧК7). Исполнитель
  следовал точному диапазону; загрузку не ломает (вызовы только в рантайме), ревью подтвердило.
- **Результат:** студия 2732 → **2035** строк (−697); `unit_scale_calc.js` 549, `items_ref.js` 150.
  `node --check` (оба + тело инлайна) — OK; объявления верхнего уровня 375 → 375 (missing=0,
  extra=0), дублей нет, top-level `let`/`const` в диапазонах нет; тела подтверждены побайтово;
  реконструкция `studio.html` == HEAD + два тега + вырезание 503–1051 и 1791–1940. DoD и
  `go test ./web/...` — зелёные; ревью — «блокирующих нет».
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК6-вынос-масштаба-и-предметов.md`):**
  9/9 PASS — `/studio` без JS-ошибок; 25 имён двух блоков — функции; живой переключатель масштаба
  (`gs_unitScale` изменился, попап перерисован); справочник предметов рисуется (4 карточки, попап
  предмета открывается/закрывается); блоки «Потребляет»/«Стадия»/«Слоты» попапа производителя
  рендерятся; граф, правый клик и экспорт (ЧК1–ЧК5) живы.
- **Коммит:** `ecedcba` (код: `web/studio.html`, `web/static/js/studio/unit_scale_calc.js`,
  `web/static/js/studio/items_ref.js`).

### ЧК7 — дерево и разделы построек (пройден)

- **Что вынесли (HEAD-строки):** `buildings_tree.js` ← 505–1242 — «дерево построек на канвасе»
  (`buildProdTree`, `prodTreeLayout`, `renderProdTree`, `prodTreeHitTest`, `openProdNodePopup`,
  узлы/видимость/специфичность `prodNode*`/`prodSlot*`/`prodCat*`, `raceName`/`familyLabelById`/
  `raceFamilyOf`, `showProdTip`, расовость `setProdRace*`) и «разделы построек» (`PROD_SECTIONS`,
  `PROD_SECTION_KEYS`, `renderBuildingsTabsUI`, `setProdSection`/`saveProdSection`,
  `prodSectionOf`/`prodSectionRoots`/`prodSectionBlock`). Top-level `const PROD_SECTIONS`/
  `PROD_SECTION_KEYS` уехали вместе с блоком.
- **Порядок подключения:** `core.js` → `content_transfer.js` → `interactions.js` → `graph.js` →
  `goods_popup.js` → `fill_ai.js` → `dialogs.js` → `producers_ref.js` → `unit_scale_calc.js` →
  `items_ref.js` → `buildings_tree.js` → инлайновый `<script>`; `"use strict";` в каждом.
- **Результат:** студия 2035 → **1297** строк (−738); `buildings_tree.js` 739. `node --check`
  (файл + тело инлайна) — OK; объявления верхнего уровня 299 → 299 одним экстрактором
  (missing=0, extra=0), дублей нет, `PROD_SECTIONS`/`PROD_SECTION_KEYS` только в новом файле;
  тело подтверждено побайтово; реконструкция `studio.html` == HEAD + тег + вырезание 505–1243.
  DoD и `go test ./web/...` — зелёные; ревью — «блокирующих нет».
- **Браузерный смоук (отчёт `docs/QA/checklists/2026-09-25_студия-ЧК7-вынос-дерева-построек.md`):**
  9/9 PASS — `/studio` без JS-ошибок; 17 имён — функции; дерево построек рисуется (8 узлов);
  переключение разделов раздела построек работает (28 узлов в «Фабриках»); попап узла открывается
  двойным кликом и закрывается; граф, правый клик, справочник предметов и экспорт (ЧК1–ЧК6) живы.
- **Коммит:** `16ed85e` (код: `web/studio.html`, `web/static/js/studio/buildings_tree.js`).

## 4. Дальше

1. **ЧК8** (последний) — селекторы + попапы каталога + оверлеи/режимы/сборка/инициализация
   (`selectors.js`, `catalog_popups.js`, `init.js`), со своим гейтом (diff + браузерный смоук
   студии). После пройденного чекпоинта следующий **по умолчанию идёт в новой сессии**.
2. Финальный шаг эпика: убедиться, что в инлайновом `<script>` не осталось логики, и обновить
   карту в `docs/ARCHITECTURE.md`.

## 5. Вызов на новую сессию

**Где стоим:** нарезка эпика готова и укрупнена (8 шагов); **ЧК1 ✅** (`8ed5e4e`), **ЧК2 ✅**
(`342b08c`), **ЧК3 ✅** (`48ec089`), **ЧК4 ✅** (`6fd7915`), **ЧК5 ✅** (`6f8e873`), **ЧК6 ✅**
(`ecedcba`), **ЧК7 ✅** (`16ed85e`); в работе **ЧК8** (последний). Файл студии — 1297 строк.
**Ориентир:** эта идея, §2 (карта файлов) и §3 (чекпоинты).
**Задача чекпоинта ЧК8 (последний, шаг даёт 1–3 файла; точные границы исполнитель подтверждает
по секционным маркерам):**
1. `web/static/js/studio/selectors.js` — селекторы скопа ветки «Товары». От маркера
   `// ---------- селекторы скопа ветки «Товары» (ТЗ §3/§4/§8) ----------` (сейчас 506) до маркера
   `// ---------- категории и товары ----------`; сейчас это строки **506–557** (~52 строки).
2. `web/static/js/studio/catalog_popups.js` — категории/товары, попап категорий, «Эффекты», формы
   добавления. От маркера `// ---------- категории и товары ----------` (558) через
   `// ---------- попап категорий ... ----------` (603), `// ---------- попап «Эффекты» ... ----------`
   (682), `// ---------- попапы форм добавления ... ----------` (808) до маркера
   `// ---------- оверлеи состояний ----------`; сейчас это строки **558–908** (~351 строка).
3. `web/static/js/studio/init.js` — оверлеи, режимы канваса, сборка, разделы студии,
   инициализация и проводка событий. От маркера `// ---------- оверлеи состояний ----------` (909)
   через `// ---------- режимы канваса ... ----------` (946), `// ---------- сборка ----------` (967),
   `// ---------- разделы студии ... ----------` (1002) до конца инлайнового скрипта; сейчас это
   строки **909–1296** (~388 строк).
4. **Финал эпика:** после выноса в инлайновом `<script>` не должно остаться логики (ожидается
   только `"use strict";`; если так — пустой инлайновый блок можно убрать); обновить карту студии
   в `docs/ARCHITECTURE.md` §2 (дельта владельцу — `@developer`).
**Рамки:** поведение дословно; один сводный перенос на блок; контрактные тесты — через помощник
`studioHTML()` (он динамический, отдельные тесты править не нужно); гейт — diff + браузерный
смоук студии (переключение разделов, попапы каталога, оверлеи, инициализация при загрузке);
сторожа зацикливания учесть (большой файл — один сводный `write`/скриптовая нарезка, не серия
мелких `edit`).
**Предохранитель (порядок загрузки):** три файла подключаются классическими скриптами после
`buildings_tree.js`, до инлайнового скрипта (инициализацию/проводку событий — последним);
каждый начинается с `"use strict";`. Вынесенные блоки вызывают функции всех предыдущих скриптов
(`state`, `esc`, `api`, `renderGraph`, `renderProdTree` и т.п.) — они уже загружены раньше;
`init.js` содержит проводку событий и вызывается при загрузке — порядок «после остальных» важен.

## 6. Разнос по докам (заполняется при сдаче)

| Док | Дельта |
|---|---|
| `docs/ARCHITECTURE.md` §2 | карта студии: `web/studio.html` + `web/static/js/studio/*.js` (блоки) — финальный шаг эпика |
| `docs/COORDINATION.md` | метка рефакторинга студии ставилась на время ЧК1–ЧК7 и снималась после каждого коммита (в рабочем дереве теперь только чужие незакоммиченные строки). Устаревшая метка `web/studio.html` в линии «И2: экспорт снимка контента» — снять владельцу линии / `@dispatcher` (чужая незакоммиченная строка, менеджер её не трогал) |
| `docs/EPICS.md` | строка эпика обновлена до **ЧК7 ✅** и **закоммичена** (`20bab95`) аккуратным способом: закоммичена только наша строка, чужая незакоммиченная правка («Постройка структур») возвращена в рабочее дерево и не тронута. Далее обновлять при каждом чекпоинте |
| `CHANGELOG.md` | запись о рефакторинге по завершении эпика (владелец — `@dispatcher`) |

## 6а. Хвосты (дельта в `docs/TAILS.md`)

| Хвост | Откуда | Адрес: кто и где закроет | Появился |
|---|---|---|---|
| `tools/e2e/qa-i1-studio.js` ждёт 409 на отсутствующий рецепт (`RECIPE_FREE=71` — в dev-БД его нет, сервер корректно отдаёт 404) — устаревшая предпосылка данных, ложный FAIL | прогон `@tester` 2026-09-25 (смоук ЧК1) | `@tester`/`@developer` — обновить id/данные фикстуры при следующем касании скрипта | 2026-09-25 |

## 7. Ретроспектива

- **Правка инструкций (прямое указание создателя, 2026-09-25):** в `manager.md` (раздел
  «ПРАВИЛА») добавлено правило — **предупреждать создателя о разросшихся файлах**: на приёме
  пожелания и перед постановкой задачи прикинуть размер целевых файлов; файл сверх ориентира
  200–300 строк (`AGENTS.md` §4.8) → сказать создателю до старта (сколько строк, во сколько раз
  превышение) и предложить вариант дробления. Это предупреждение, не вето. Коммит — при сдаче.
