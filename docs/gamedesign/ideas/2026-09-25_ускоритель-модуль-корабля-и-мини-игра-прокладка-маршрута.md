# 2026-09-25. Ускоритель — модуль корабля и мини-игра ускорения межзвёздного перелёта

**Дата:** 2026-09-25
**Вектор:** Игрок · Новые механики
**Статус:** ✅ **готово (2026-09-26)** — линия сдана (Э5): спеки закрыты, дельты доков переданы, реестр влияний обновлён. ЧК1 ✅, ЧК2 ✅, ЧК3-сервер ✅, ЧК3-клиент S1 ✅ (`85bfd7f`/`970d304`/`9b0018e`/`a127e69`), S2 ✅ (`62a28d9`), S3 ✅ (смоук + прогон тестера, 2026-09-25), админ-сброс таймера ✅ (`0c74e7d`). Переделка сложности закрыта: модель **v9 «Планшет»** принята решением создателя **B — внедряем** (2026-09-26). Серверная половина: **Э1-A ✅** (`32073ae`), **Э1-B ✅** (`46453f3`). Клиент-доска: **Э3-C1 ✅** (`bfba4eb`), **Э3-C2 + легенда-попап ✅** (`fe0adcc`), **Э3-R — космический словарь и визуал ✅** (`0f16f62`); игра играбельна в браузере, словарь и визуал в сеттинге; **Э4 — спрайты заморожены ✅** (`39fb31a`); **Э6 — разбор по факторам ✅** (`a8983ff`, `1468f26`, тестер 12/12). **Э5 — сдача ✅** (2026-09-26, см. §16.8). См. §16/§16.1–§16.8
**Приоритет:** по решению создателя — начинать с **самой интересной** механики (не с самой простой)
**Эталон:** не задан

**Связи:** спеки `docs/specs/99.2.27-intrasystem-flight.md`, `docs/specs/99.2.30-composite-route.md`; идея `2026-09-22_трюм-грузоподъёмность-корабля.md` (образец нового типа оборудования и слота), `2026-09-22_один-мир-много-клиентов-мини-игры-как-входы.md`; GDD `10_exploration.md` §10.2.3 (канон мини-игр), `07_ui.md`; спеки 91a (раздел «Корабль»), 77a (модель корабля/слоты/оборудование).

---

## 0. Пожелание создателя (дословно, 2026-09-25)

> «Что насчет мини-игры, ускоряющей продвижение между звездами? вроде "постройки маршрута", логически или в реальном времени. Сложность должна зависеть от дистанции до цели. Ускорять, пока не сменишь направление. Предложи на выбор 5 игровых механик (геймдизайнер в помощь)»

Далее в разговоре:

> «Отбрось такие, которые не будут работать на мобилках» → «точнее, у которых не может быть мобильного интерфейса»
> «так, а можно взять несколько, и случайным образом давать их игроку?»
> «Я думаю знаешь как, будет модуль корабля "ускоритель", и разные модули = разные мини-игры. Пока реализуем одну. А характер маршрута - это надо в каждую игру как-то пронести наверное.»
> «1 - ускорение, причем пользоваться ускорителем можно раз в N минут (зависит от крутости ускорителя); 2 - да, ускорился засчет поиска маршрута и ушел; 3 - А мы так можем - поверх карты? Карта тормозит на большом количестве звезд и большом зуме, а на маленьком непонятно ускорение; 4 +»
> «Выберите, какая попроще»
> «так, а интерес в чем у выбранной игры?»
> «Начать надо с самой интересной игры»

---

## 1. Решения создателя (свод)

1. **Разные ускорители = разные мини-игры.** Случайности нет: игрок выбирает механика тем, какой модуль стоит на корабле.
2. **Пока реализуем одну** мини-игру.
3. **Только ускорение, без штрафов.** Ошибка = нет выигрыша; перелёт не наказывает.
4. **Откат:** пользоваться ускорителем можно раз в N минут; N и «сила» — характеристики модуля (круче модуль → короче откат и/или больше срез времени). Трактовка приёмной (подтвердить на гейте): откат общий на игрока, реальное время, переживает рестарт; отсчёт от момента использования.
5. **Накопление:** «ускорился за счёт поиска маршрута и ушёл» — выигрыш фиксируется, игрок не привязан к экрану.
6. **Характер маршрута** проносится в каждую мини-игру — общий «паспорт перелёта» (§4).
7. **Контракты не трогаем** в первой версии (их сроки считаются от эталонного медленного двигателя; ускорение их лишь облегчает).
8. **Фильтр мобильности:** механика обязана иметь мобильный интерфейс. Отброшена «Гравитационный слалом» (непрерывное многоканальное пилотирование).
9. **Приоритет:** начинаем с **самой интересной** механики, не с самой дешёвой.

---

## 1.1. Решения на гейте дизайна (2026-09-25)

1. Слот модуля — **выделенный** «ускоритель».
2. Экран мини-игры — **отдельная лёгкая страница**.
3. Стартовый ускоритель — **выдаётся** новому игроку (слабый).
4. **Награда — не срез времени, а +10…50 % к СКОРОСТИ полёта** в зависимости от качества прохождения.
5. **Точный прогноз прибытия убран** — перед отправкой игрок видит только качественную оценку; качество заранее неизвестно (это и даёт интригу).
6. Паспорт — массу и возраст звезды **убрать** (открытые данные звезды 77a §5.5 не расширяем).
7. Регион в паспорт — включать только при дешёвом пути чтения (проверяет дизайнер).
8. Контракты — без изменений.
9. **Ускорение не складывается:** пока действует текущее ускорение, ускориться ещё раз нельзя. **Смена цели полёта (разворот) сбрасывает ускорение**, после чего (при истёкшем откате) можно ускориться снова. Одно активное ускорение на сегмент.
10. **Порог с запасом:** игру предлагать при остатке с запасом на её длительность (~3 мин), отказ при отправке — ниже (~1,5 мин), чтобы игрок не тратил силы впустую.
11. **Прогрессия модулей — по силе ускорения (бонус),** а не по откату: на длинных перелётах откат почти не работает. В v1 реализуется только один модуль.

---

## 2. Отбор механик (итог идеации `@designer` + решения создателя)

| Механика | Класс | Мобильность | Итог |
|---|---|---|---|
| **Прокладка маршрута** | логика (путь/граф) | да (рисование пальцем) | **выбрана — самая интересная** |
| Код прыжка | логика (последовательность) | да (тапы) | резерв (дёшево, но интерес узкий — держится на неполных подсказках и риске) |
| Окна тяги | логика (оптимизация таймлайна) | да | резерв (нет напряжения; «поставил и ушёл» не занимает мёртвое время) |
| Резонанс хода | реальное время, одна ось | да | резерв (легко скатывается в «держи кнопку») |
| Гравитационный слалом | реальное время, пилотаж | **нет** | отброшена |

**Почему выбрана «Прокладка маршрута»:** планирование под ограничениями + «ага» изящной связки + прямая связь с темой курса и направления (фраза создателя «ускорять, пока не сменишь направление»). Интерес шире и надёжнее, чем у остальных: не требует тонкой настройки «сколько игроку дают знать».

---

## 3. Что есть сейчас (факты, проверено 2026-09-25)

- **Межзвёздный перелёт — серверный таймер.** `POST /travel` считает `duration = dist * speed_factor` (мин. 3 с, **без потолка**): `internal/handlers/travel_handlers.go` → `calcTravelDuration` → `models.TravelDuration` (`internal/models/contract_travel.go`). `dist` — евклидово расстояние по `CoordX/CoordY` миров. Дефолтный двигатель `speed_factor = 0.3` (`models.EngineSpeedDefault`).
- **Полёт авторитетен серверу и переживает рестарт.** `internal/travel/manager.go`: `StartFlight` (память + таблица `player_flights`, идея 97a), горутина ждёт `arrive_at`; `Restore` при старте сервера дожимает остаток. `StartTime/Duration/StartX/Y` в течение полёта неизменны.
- **Прогресс полёта считается интерполяцией** `timeSince(StartTime)/Duration` (видимость, `visibility.go`) — правка длительности на лету обязана не сломать эту формулу и Restore.
- **«Смена направления» = разворот (61a):** выбор новой цели в полёте, сегмент пересчитывается из текущей точки P, длительность считается заново. Естественная точка сброса накопленного ускорения.
- **Оборудование:** таблица `equipment` (id, type, name, params JSONB), установка — `users.equipment` JSONB по слотам; типы: `radar`, `scanner`, `engine`, `cargo` (`internal/models/ship.go`). Слоты модели корабля: `{radar:1, scanner:1, engine:1, universal:3}` (`ship_models.slots`). Модуль работает по типу в любом слоте (`hasType`), кроме скорости двигателя (читается из легаси-слота `engine`). Образец добавления нового типа — грузовой модуль (трюм): `cargo` + `params.capacity` (`internal/ship/catalog.go`).
- **Внутрисистемный полёт** (`intrasystem_handlers.go`, `CalcIntraDuration`): мин. 3 с, до ~5.5 мин. Основной скоуп — межзвёздный (по слову создателя); применение к внутрисистемному — отдельная развилка.
- **Мини-игры-клиенты существуют как отдельные страницы:** пояс — `web/belt.html` + `web/static/js/belt/*`; поверхность — `web/surface.html`. Канон `10_exploration.md` §10.2.3: мини-игра определяет «сколько и как быстро», но **никогда «что»**; тип мини-игры зависит от места.
- **Контракт на перелёт:** срок выводится как `TravelFlightTimeRef(dist) * 1.5` от **эталонного** (медленного) двигателя — ускорение будет только облегчать дедлайн.

---

## 4. Модель (гипотеза, на дизайн)

**Новая сущность — модуль «ускоритель».**

1. **Природа** — оборудование (капитал), не событие; второй потребитель — «характер мини-игры».
2. **Владелец** — у игрока, в слоте `users.equipment` (как прочие модули).
3. **Расширяемость** — новый тип оборудования `accelerator` с параметрами:
   - `game` — какую мини-игру открывает (первая: «прокладка маршрута»);
   - `cooldown_min` — откат в минутах (крутость модуля);
   - `force`/`max_boost` — предельный срез времени (крутость модуля).
   Второй ускоритель (другая игра) ложится теми же полями.
4. **Эксплуатация** — срез времени перелёта; боты ограничены откатом и серверной валидацией результата.

**Слот:** развилка — выделенный слот «ускоритель» (один активный) **или** универсальный слот. Рекомендация приёмной — выделенный (чище: активная игра одна).

**Серверный поток:** при старте перелёта сервер видит установленный ускоритель и остаток отката; если модуль есть и откат истёк — предлагает мини-игру (её тип — из `params.game`). По итогу валидирует результат и **срезает остаток времени полёта** на вычисленную величину. Нет модуля / откат не истёк — перелёт идёт как сегодня.

**Откат** — серверное состояние на игрока (время последнего использования), переживает рестарт; источник — БД (отдельное поле/таблица).

**«Паспорт перелёта»** — общий набор свойств участка пути, который считает сервер при старте: дальность, типы звёзд на концах, аномалии/пояса на пути, характер пространства. Любая мини-игра читает паспорт и переводит в свою сложность. Смысл завести его сразу: добавление второй мини-игры станет дешёвым (только перевод паспорта, мир не трогаем).

**Мини-игра «Прокладка маршрута» (эскиз):** поле с узлами и препятствиями; игрок ведёт путь от старта к цели; короткий изящный путь даёт больший срез, ломаный/с пропуском обязательного узла — меньший/никакой. Дальность → число обязательных узлов; характер участка → препятствия и ложные узлы. Ограничена каноном: меняет только «как быстро».

---

## 5. Чекпоинты

- **ЧК1 ✅ реализован и принят 2026-09-25** (код `d71d932`). Новый тип `accelerator`, каталожные записи, выделенный слот, состояние отката, `BoostFlight`/`Booster`/`SetBooster`, аддитивные блоки `/travel` и `/me`; до регистрации игры (ЧК3) — `available=false`, `reason=unknown_game`. Ручки `offer`/`boost` и UI перенесены в ЧК3 (нужны поле и `q`). См. §9.
- **ЧК2 ✅ реализован и принят 2026-09-25** (новый пакет `internal/routegame`, отдельного коммита нет — вливается в коммит линии). Паспорт перелёта — производная из открытых данных: дальность, класс/тип/температура концов, тип системы, видимые пояса системы назначения; масса/возраст/регион не входят. Подключение к ручке `offer` — ЧК3. См. §10.
- **ЧК3. Мини-игра «Прокладка маршрута».** 🔄 **серверная часть ✅ (2026-09-25, см. §11)**; клиент (страница `web/route.html`, ввод, HUD, пул спрайтов с рандомом при каждом запуске), UI «Корабль»/панель полёта, тест + e2e-смоук — следующая сессия. Гейт.

---

## 5.1. Визуал и ассеты (требование создателя 2026-09-25)

**Дословно:** «и сделать клёвые спрайты для нее и графику, чтобы по-взрослому было, а не как домашний проект выглядело».

**Что это значит на практике:**

- «По-взрослому» — это в первую очередь **единый стиль интерфейса**: типографика, отступы, глубина, свет, анимация, отклик, а не только отдельные картинки. Поэтому трек идёт через `@uidesigner` (интерфейс) и `@gdesigner` (визуальный стиль), а не только через `@artist`.
- **Что рисуется кодом, а что спрайтами.** Логическое поле/доска лучше рисуется **кодом** (чётко на любом экране, масштабируется, дёшево на мобилке, без мутных картинок). Спрайты — там, где дают ценность: типы звёзд, аномалии, иконки узлов, течения/пыль, корабль, фон/атмосфера.
- **Состояние поля как обратная связь:** подсветка выбранного пути, «живые» течения, предсказание прибытия — это тоже часть «взрослого» вида.

**Порядок:** сначала геймдизайн (что игра показывает) → интерфейс (`@uidesigner`) → визуальный стиль и ТЗ на ассеты (`@gdesigner`) → спрайты (`@artist`, ComfyUI) → разработка.

**Открытая развилка:** эталон/ориентир стиля не задан — предложит `@gdesigner`, утверждает создатель.

---

## 6. Открытые развилки (на гейт)

1. **Слот ускорителя:** выделенный или универсальный.
2. **Где играть:** отдельный лёгкий экран (как добыча в поясе — просто и без тормозов карты) **или** оверлей поверх карты (карту гасим/останавливаем на время игры; рекомендация приёмной — начать с отдельного экрана).
3. **Сила и откат** — числа (гипотезы для `@balancetester`): насколько срезает и как часто; прогрессия «крутости» модулей.
4. **Контракты:** оставляем как есть в первой версии (решено); пересмотр — позже.
5. **Область:** только межзвёздный (решено) или позже внутрисистемный.
6. **Нужна ли модулю польза без игры** (пассивный множитель) — предложение приёмной: нет, модуль только открывает игру.

---

## 7. Маршрут

1. `@designer` — спека (ЧК1–ЧК3, общая архитектура + интерфейсы; описывает, **что показывает сцена** — цель для визуала).
2. `@critic` — прогон по дереву связности (полёт/видимость/оборудование/контракты).
3. Гейт создателю → `@uidesigner` (интерфейс мини-игры, mobile-first) → `@gdesigner` (визуальный стиль + ТЗ на ассеты, ориентир «по-взрослому») → гейт на стиль.
4. `@artist` — спрайты по ТЗ `@gdesigner` (ComfyUI).
5. `@developer` — ЧК1, затем ЧК2, ЧК3 (каждый со своим гейтом; визуал подключается в ЧК3).
6. `@balancetester` — числа силы/отката (после ЧК1).
7. `@tester` + e2e-смоук клиентского флоу (+ визуальный прогон).
8. Закрытие: разнос по докам, `impact_map`, коммит.

---

## 8. Журнал хода работ

- 2026-09-25: создатель наговорил пожелание в приёмную (вектор Игрок). Менеджер поднял контекст (полёт, оборудование, канон мини-игр), запустил `@designer` на 5 механик. Создатель отбраковал «Гравитационный слалом» по мобильности; выбрал модель «модуль-ускоритель = мини-игра», откат раз в N минут, накопление, контракты не трогаем. Из оставшихся четырёх по критерию «самая интересная» выбрана **«Прокладка маршрута»**. Идея записана, дизайн запущен.
- 2026-09-25 (продолжение): создатель добавил требование к визуалу — «клёвые спрайты и графика, по-взрослому, не как домашний проект». В идею добавлен трек визуала (§5.1): `@uidesigner` → `@gdesigner` → `@artist`; шаги внесены в маршрут.
- 2026-09-25 (сдача ЧК1): дизайн (спека, критика ×2, балансировщик ×2), разработка ЧК1, ревью («блокеров нет»), тест (14/14 PASS), приёмка создателем. Ручки `offer`/`boost` и UI перенесены в ЧК3. Код — `d71d932`; артефакты — отдельным коммитом. Создатель: «останавливаемся на чекпоинте».
- 2026-09-25 (сессия «доделать игру», продолжение): визуальный трек (`@uidesigner` — интерфейс mobile-first; `@gdesigner` — стиль и пул вариантов; `@artist` — 22 PNG), ЧК2 (паспорт), ЧК3-сервер (поле от seed + оценка пути, ручки `offer`/`boost`, регистрация игры, фикс double-close `CancelChan`; ревью — без блокеров). Решения создателя: **варианты спрайтов случайны при каждом запуске** (режим наигрыша, только в игре) и **палитра расширена ради красоты**. Сессия закрыта на границе «сервер → клиент».

## 9. ЧК1 — сдача (2026-09-25)

**Статус:** ✅ ЧК1 реализован и принят создателем. Ревью диффа — «блокеров нет»; независимый прогон `@tester` — **14/14 PASS**, дефектов нет; DoD (`build`/`vet`/`-race -short`) зелёный по зоне.

**Коммиты:** код — `d71d932` `feat(travel): ускоритель перелёта — модуль и серверное ускорение (ЧК1)` (18 файлов, +1428/−12); артефакты — эта идея, спека, отчёт тестера (отдельный коммит менеджера).

**Что вошло.** Тип оборудования `accelerator` (+CHECK, запись `accel_1`), выделенный слот `accelerator` у `starter`, выдача стартового ускорителя новичку, состояние отката (`player_accelerator`, переживает рестарт, не в `truncateTables`), серверное ускорение (перебазирование сегмента: `BoostFlight`/`Booster`/`SetBooster`), аддитивные блоки `accelerator` в `POST /travel` (оба пути) и `/me`. Миграция `000089` (номер `000088` заняла параллельная линия). Ручки `offer`/`boost` и UI — в ЧК3.

**Разнос по докам (дельты для `@dispatcher`, применяются по слову создателя):**
- `docs/DB.md`: миграция `000089_accelerator.sql`; `equipment.type` CHECK += `accelerator`; таблица `player_accelerator (user_id PK→users ON DELETE CASCADE, last_boost_at, last_cooldown_min, updated_at)`; `ship_models.slots` стартовой модели += `accelerator:1`.
- `docs/ARCHITECTURE.md` §2/§4: новые файлы `internal/ship/accelerator.go`, `internal/models/player_accelerator.go`, `internal/repository/player_accelerator_repository.go`, `internal/handlers/accelerator_handlers.go`; `travel.Manager` += `BoostFlight`/`Booster`/`SetBooster`; аддитивные блоки `accelerator` в `/travel` и `/me`.
- `CHANGELOG.md`: +2026-09-25 — `Реализовано: ускоритель перелёта (ЧК1) — модуль-ускоритель (тип оборудования, выделенный слот, стартовый модуль), состояние отката, серверное ускорение межзвёздного перелёта перебазированием сегмента; одно активное ускорение на сегмент; до появления мини-игры ускоритель неактивен (unknown_game).`
- `.opencode/context.md` §Указатели: «Ускоритель перелёта (ЧК1 сдан 2026-09-25, коммит `d71d932`): работая с полётом, оборудованием/слотом или `/travel`/`/me` — читай спеку `docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md`; впереди ЧК2 (паспорт) и ЧК3 (мини-игра `web/route.html` + ручки `offer`/`boost`).»
- `docs/EPICS.md`: строка эпика «Ускоритель + мини-игра „Прокладка маршрута"» (ЧК1 ✅ → ЧК2 паспорт → ЧК3 игра/UI/визуал). **Не внесена** — файл занят чужой незакоммиченной правкой.
- `docs/specs/README.md`: строка спеки обновлена дизайнером в рабочем дереве, но файл **смешан** с чужими правками — коммитить нельзя; внести при чистом дереве.

**Хвосты (открытые, с адресом):**
1. **Живой `/travel` на dev не проверен** — миграция накатывается при старте сервера, а он поднимался раньше правки. Адрес: `@tester`/`@developer` при следующем касании (перезапуск сервера + живой запрос → ожидаем `unknown_game`).
2. ✅ **Закрыт (2026-09-25, ЧК3-сервер):** double-close `CancelChan` в `travel.Manager` — запись из `flights` снимается вместе с `close` под одним локом; регресс-тест ловит баг.
3. **Числа** (`bonus_max` стартового 0.50, откат 25 мин, пороги показа 180 с / отправки 90 с; прогрессия `accel_2` 0.60 / `accel_3` 0.70) — гипотезы. Адрес: `@balancetester` по накопленным данным.
4. **`offer`/`boost`, паспорт перелёта, мини-игра `web/route.html`, UI «Корабль»/панель полёта, визуал/спрайты** — ЧК2/ЧК3, следующая сессия.
5. **`docs/impact_map.json`**: узлы `accelerator` / `travel_boost` / `travel_passport` / `route_game` + таблица `player_accelerator`; рёбра на `players_positions.go`, `internal/ship/*`, `/me`-блок, клиентские панели. Адрес: `@manager` (ведёт реестр).
6. **`AcceleratorBonus`** пока в прод-пути не вызывается (потребитель — `boost` в ЧК3) — не удалять.
7. **Несданные дельты доков** (п. «Разнос по докам») — `@dispatcher` по слову создателя.

**Вызов на новую сессию (ЧК3 — клиент игры; исторический, актуальный — в §16.1):**

```text
@manager Продолжаем линию «ускоритель + мини-игра „Прокладка маршрута"», ЧК3 — клиент игры.
Идея — docs/gamedesign/ideas/2026-09-25_ускоритель-модуль-корабля-и-мини-игра-прокладка-маршрута.md.
Спека — docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md;
интерфейс и визуал — docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md.
Сделано: ЧК1 (коммит d71d932), ЧК2 — паспорт, ЧК3-сервер — ручки offer/boost, поле от seed
и оценка пути, регистрация игры, фикс двойного закрытия канала отмены; ревью — без блокеров.
Задача: клиент игры — страница web/route.html + route.css + js/route/* (поле кодом, рисование
пальцем/мышью, HUD и состояния, пул спрайтов со случайным выбором при каждом запуске — режим
наигрыша); кнопка «Ускорить» в панели полёта (map.html/map.css/flight.js); ячейка «Ускоритель»
в разделе «Корабль» (index.html). Визуал — по разделу «Визуальный стиль и ТЗ на ассеты»
интерфейсной спеки; пул ассетов — web/static/sprites/route/.
Дальше: наигрыш создателя (выбор победителей спрайтов), тест + браузерный смоук игры,
сдача (доки/impact_map/хвосты), пуш — по слову создателя.
Хвосты: минимальный шаг точки и rate-limit (анти-бот §6.6); несданные дельты доков.
```

---

## 10. ЧК2 — сдача (2026-09-25)

**Статус:** ✅ ЧК2 реализован и принят создателем («ок»).

**Что вошло.** Новый пакет `internal/routegame`: «паспорт перелёта» — чистая серверная производная из уже открытых данных (`BuildPassport`). Поля: дальность участка; для каждого конца — класс звезды, тип звезды, тип системы, температура (в K, UI конвертирует в °C); пояса системы назначения — только `visible=true`, пустой список → `[]`. Чего нет: масса/возраст звезды, регион, аномалии, «пояса по пути» (решения 15/16 спеки §5.1/§5.2). Паспорт ни к чему пока не подключён — потребитель `offer` появится в ЧК3.

**Зачем.** Общий «характер маршрута»: любая будущая мини-игра (второй ускоритель = своя игра) переводит паспорт в свою сложность; мир не трогаем.

**Проверка.** `go build ./...` + `go vet ./...` — зелёные; `go test -short ./internal/routegame/` — PASS (6 тестов, включая: скрытый пояс не утекает, пустой список поясов = `[]`, запретные поля отсутствуют в JSON, детерминизм). Отдельного ревьюера не гоняли (мелкая задача) — DoD проверен менеджером. Коммита нет (по правилу — после общей приёмки линии).

**Хвосты:**
- Паспорт пока без потребителя (подключается в ЧК3) — не удалять.
- Пуш/коммит линии — по слову создателя, через `@dispatcher`.

---

## 11. ЧК3-сервер — сдача (2026-09-25)

**Статус:** ✅ серверная часть ЧК3 реализована и принята создателем («ок»); независимое ревью диффа — **блокеров нет**; DoD (`build`/`vet`/`-race -short` по затронутым пакетам) зелёный.

**Коммиты:** код — `425a910` `feat(accelerator): мини-игра «Прокладка маршрута» — поле, оценка пути, offer/boost (ЧК3 сервер)` (14 файлов); артефакты — `e894f9e` (эта идея + `docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md`). Пуш не делался (по слову создателя — после готовности игры).

**Что вошло.** `internal/routegame`: генератор поля от seed и оценка пути (`HashSeed`, `GenerateField`, `EvaluatePath`, `L_eff`/`L_ref`/`q`, константы-гипотезы). Ручки `GET /api/accelerator/offer` и `POST /api/accelerator/boost` (`internal/handlers/accelerator_game_handlers.go`), регистрация игры `route` (ускоритель включился). Фикс давнего риска double-close `CancelChan` в `travel.Manager.CancelFlight` + регресс-тест. Роуты в `cmd/server/main.go`.

**Визуал (ЧК3).** Интерфейс — `docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md` (`@uidesigner`, mobile-first); стиль + ТЗ на ассеты + **пулы вариантов** — там же (`@gdesigner`, итерация 2). Пул спрайтов — `web/static/sprites/route/` (22 PNG, `@artist`): `star_core`×5, `black_hole`×3, `beacon`×4, `false_signal`×4, `hazard_cloud`×3, `nebula_bg`×3.

**Решения создателя.** Варианты спрайтов **случайны при каждом запуске** — режим наигрыша (только в игре; после выбора заморозить по одному на тип). Палитра расширена ради «максимума красоты».

**Хвосты:**
1. **Победитель спрайтов не выбран** — режим наигрыша в игре; после выбора `@artist` замораживает по одному варианту на тип. Адрес: `@artist`/создатель (после клиента).
2. **Анти-бот §6.6 не полон:** нет минимального шага точки и per-user rate-limit ручек. Адрес: `@developer` при следующем касании сервера игры.
3. **Код причины `no_flight` при `boost`** (спека резервирует его для `offer`) и код `changed` — согласовать с клиентом/спекой. Адрес: `@developer`/`@designer` при сборке клиента.
4. **Роуты `offer`/`boost` в `cmd/server/main.go` не закоммичены** — файл смешан с параллельной линией (сдача груза ЧК2б: `SetCargo`, `/api/contracts/deliver`), коммитить нельзя. Внести после коммита той линии (или её владельцем). Адрес: `@manager`/`@developer`. До этого в HEAD ручки есть, а роутов нет (в рабочем дереве всё работает).
5. **Ассеты и инструменты не закоммичены:** `web/static/sprites/route/` (пул 22 PNG + `_preview/*`), `tools/{make_route_silhouettes,process_route_glow,process_route_icon,make_route_preview}.py`, `tools/batch_route_{test,pool}.ps1`. Адрес: `@manager` — закоммитить пул (без `_preview`) после выбора победителя `@artist`.
6. **Несданные дельты доков** (ЧК1/ЧК2/ЧК3): `CHANGELOG.md`, `ARCHITECTURE.md` §2, `DB.md`, `docs/specs/README.md`, `docs/EPICS.md`, `.opencode/context.md`, `docs/TAILS.md`, `docs/impact_map.json`. Адрес: `@dispatcher`/`@manager` по слову создателя.
7. **`docs/impact_map.json`:** узлы `accelerator`/`travel_boost`/`travel_passport`/`route_game` + таблица `player_accelerator` — `@manager`.

---

## 12. ЧК3-клиент S1 (страница игры) — чекпоинт сдачи (2026-09-25)

**Статус:** ✅ S1 реализован и принят создателем («ок, только через коммит и чекпоинт»). Независимое ревью диффа — **блокеров нет**; замечания закрыты.

**Коммиты (чекпоинт):**
- `85bfd7f` — роуты `offer`/`boost` в `cmd/server/main.go` (закрыт хвост ЧК3-сервера: файл был смешан с параллельной линией груза, та закоммичена 2026-09-25).
- `970d304` — клиент S1: `web/route.html`, `web/static/css/route.css`, `web/static/js/route/*` (7 файлов, +1413).
- `9b0018e` — пул спрайтов (22 PNG) + инструменты ассетов маршрута.
- `a127e69` — рабочие превью `_preview` исключены из репозитория (остаются на диске).

**Что вошло.** Экран мини-игры: canvas-поле кодом (фон, звёзды/пыль, туманности, виньетка), паспорт-полоса (сворачивается), статус-строка, счёт маяков, рисование пути пальцем/мышью (Pointer Events, `touch-action:none`), захват маяков (вспышка), ложные сигналы (тост, без штрафа), зоны потери времени (подсветка сегмента + чип «дороже» без чисел), гейт «Проложить» (СТАРТ+ФИНИШ+все маяки+остаток ≥ порога отправки), отправка, результат (класс качества словами, прибавка скорости, новый остаток), звук. Спрайты — **пулы со случайным выбором на каждый запуск** (режим наигрыша) + режим заморозки + фолбэк на кодовые фигуры. До отправки нет ETA/чисел/`q`/`bonus` (решение 14).

**Проверка.** Ревью независимым агентом: сверка констант с сервером (радиус СТАРТА/ФИНИША 0.06, радиус маяка из `field.nodes[].r`, потолок 64 точки) — подтверждена; утечки прогноза нет; контракт ручек обработан (все коды отказа, `changed`, 401→логин, сеть→ошибка); хирургичность (`belt`/`surface`/карта не тронуты). Замечания ревью закрыты: человеческие тексты отказов вместо сырых данных, таймер отката, потолок DPR ≤2, заморозка после успеха, круглая маска фактуры зоны, guard пустого паспорта. Живой браузерный прогон разработчика (390×844, мок offer/boost) — PASS, `pageerror` нет.

**Хвосты S1 (открытые):**
1. **S2:** кнопка «Ускорить» в панели полёта + ячейка «Ускоритель» в разделе «Корабль» + возврат на карту с перечитыванием `/me` (идут следом).
2. **S3:** e2e-смоук `tools/e2e/route-check.js` (сценарий осн. спеки §9).
3. **Победитель спрайтов не выбран** — режим наигрыша; после выбора заморозить по одному варианту на тип, лишние убрать.
4. **Временные скрипты** `tools/e2e/tmp-route-*.js` (untracked) — удалить при сдаче.
5. **Обновление доков/`impact_map`/`EPICS`** — сдача.
6. **Закрыты хвосты:** роуты `main.go` (коммит `85bfd7f`); ассеты+инструменты (коммит `9b0018e`); превью (не в репо, `a127e69`). Остаётся «заморозка по выбору создателя».

## 13. Пожелание создателя: админский сброс своего таймера ускорителя (2026-09-25)

**Дословно:** «дай админу возможность сбрасывать таймер» → «и skycomposer само собой» → «себе».

**Статус:** ✅ реализовано (коммит `0c74e7d`, 2026-09-25); независимый прогон `@tester` — **8/8 PASS**, дефектов нет; замечание тестера (ручка отвечала и на не-POST) закрыто до гейта.

**Решение создателя.** Админ-роли `admin` и `skycomposer` получают действие, сбрасывающее **своё** состояние ускорителя — и откат, и признак «ускорение уже действует». Развилка «кому» решена: **себе** (не выбранному игроку, не всем).

**Зачем.** Для наигрыша спрайтов: сыграл → сбросил → сыграл снова, не ожидая откат в 25 минут и не меняя цель полёта ради снятия `active`.

**Механика (гипотеза, на разработку).** Обнуление строки состояния ускорителя вызывающего пользователя (`player_accelerator.last_boost_at = NULL`, `last_cooldown_min = NULL`): откат становится готов, `active` снимается (`BoostActive` = сравнение с `StartTime` → ложь). Роли — штатным `auth.AdminAuth` (admin + skycomposer). Ручка административная, идемпотентная (нет строки → ничего не меняется, ответ успешный).

**Замечание приёмной (осознанный эффект).** Сброс `active` на текущем сегменте позволяет админу ускориться повторно на **уже ускоренном** сегменте — это обход гейта «одно активное ускорение на сегмент» (И-9). Это инструмент наигрыша/отладки для админа; для обычных игроков гейт И-9 не меняется. Правило названо явно, а не «по умолчанию».

**Маршрут:** малая фича → разработка → тестер → один итоговый гейт (коммит после «ок»).

## 14. ЧК3-клиент S2 (вход в игру из интерфейса) — чекпоинт сдачи (2026-09-25)

**Статус:** ✅ реализован и принят создателем («ок»). Независимое ревью диффа — **блокеров нет**; замечания закрыты.

**Коммит:** `62a28d9` — `web/map.html`, `web/static/css/map.css`, `web/static/js/map/{flight,config,data}.js`, `web/index.html`.

**Что вошло.** Кнопка «Ускорить» в панели полёта со состояниями: доступно (ведёт на страницу игры) / «Ускорение уже действует» (смена цели или прибытие сбросят) / «Откат mm:ss» с живым таймером / «Перелёт слишком короткий» / нет модуля или игра неизвестна — кнопки нет. Блок берётся из ответа `/travel` (оба пути — 202); при восстановлении полёта из `/me` (возврат на карту / после игры) состояние выводится из `/me` — best-effort, сервер остаётся последним авторитетом. Панель полёта: кнопка кликабельна (`pointer-events`), крупная тач-цель, без hover-зависимостей. Ячейка «Ускоритель» в разделе «Корабль»: имя модуля + крупный индикатор «Готов» / «Ускорение действует» / «Откат mm:ss»; без модуля ячейка скрыта.

**Проверка.** Ревью независимым агентом: регресс таймера/полосы полёта не допущен, состояния корректны, до отправки нет прогноза/секунд результата (решение 14), соседние экраны и серверный код не тронуты. Замечания закрыты: защита от цикла опроса `/me` при истёкшем откате, формулировка подсказки, мёртвое поле, заполнение данных буста вне ветки полёта. Браузерный мок разработчика — 18/18; отдельный тест «нет цикла запросов» — 6/6.

**Хвосты S2:**
1. Независимый прогон тестера и браузерный смоук игры (реальный полёт → игра → ускорение → новая полоса) — **S3**.
2. Живой сброс `active` при **реальном** полёте (админ-сброс) — не проверялся (см. §13; `active` снимается по `last_boost_at=NULL`).

## 15. ЧК3-клиент S3 (смоук и независимый прогон) — чекпоинт сдачи (2026-09-25)

**Статус:** ✅ S3 пройден и принят создателем («го»). Смоук `tools/e2e/route-check.js` — **ALL PASS (8 PASS / 1 SKIP)**; независимый прогон `@tester` — **С ОГОВОРКАМИ** (по функционалу ПРОЙДЕНО; единственная оговорка — инфраструктурная, D1).

**Коммит (чекпоинт):** артефакты S3 — эта идея, отчёт прогона `docs/QA/checklists/2026-09-25_ускоритель-маршрут-чк3.md`, смоук `tools/e2e/route-check.js`.

**Что проверено.** Полный клиентский флоу живьём: старт перелёта → кнопка «Ускорить» → страница игры → рисование пути Pointer-Events → отправка → остаток 275 с → 192 с (−30 %, в окне −9…−33 %) → `active=true`; повтор запрещён («Ускорение уже действует», кнопка disabled); цикл наигрыша через админ-сброс (`reset-self` → `/me` `ready=true`, `/offer` снова 200); нет утечки прогноза в HUD до отправки; мобильная раскладка 390×844 (поле 1:1 в кадре, кнопки 44 px); регресс карты (полёт/панель/`pageerror`); Go-DoD по затронутым пакетам.

**Замечание D1 (инфраструктура, не дефект).** Dev-сервер крутился на бинаре, собранном до правки защиты метода в `reset-self`, поэтому на живом сервере GET/PUT дали 200 (в коде и юнит-тесте — 405). Живого дефекта нет; закрывается ближайшим перезапуском dev-сервера.

**Хвосты S3 (открытые):**
1. Визуальное качество таймера/полосы панели полёта и поля маршрута «глазами игрока» — вне машинной проверки. Адрес: `@tester`/`@gdesigner` при следующем касании.
2. «Кнопка снова доступна после истечения отката» (25 мин) живьём недостижимо. Адрес: `@tester` (правка таймера/перезапуск с нужным откатом).
3. `route-check.js` STEP 7 (рестарт сервера в полёте) — SKIP: рестарт срединного dev-сервера уронил бы сессии других агентов; покрытие — Go-тесты (хвост 97a).
4. Временные скрипты прогона (`tools/e2e/tmp-route-*.js`) — удалены при коммите чекпоинта.

**Дальше.** Переделка дизайна «Сложность» по требованиям `@gamecritic` (см. следующий раздел): убрать подсказку-оракул; «ленивая» и «осмысленная» линии в одной модели стоимости; «кое-как» для реального ленивого игрока = +10 %; читаемость препятствий на телефоне; замер заново на новой географии.

## 16. Переделка «Сложность» — ход (2026-09-25, в работе)

**Задача создателя.** Игра «нельзя ошибиться», ленивый путь даёт q≈0.94 (+47.6 % из +50 %). Переделать дизайн с чистого листа по binding-требованиям критика, итерации до «ИНТЕРЕСНО», затем разработка. **Правило создателя:** критик геймплея запускается каждый раз свежим контекстом (без прошлых вердиктов).

**Сделано.**
- **Дизайн v1** (пере-якорь `q = (L_lazy − L_eff)/(L_lazy − L_opt)`, много мелких зон) → критик **СКУЧНО**: награда оседает на очевидном «обойти красное», верх шкалы не различает, риска и причины переиграть нет, два нуля в обратной связи, на телефоне — ловкость пальца.
- **Дизайн v2 «Планшет»** (дискретная сетка 10×10, маяки обязательны, опасные клетки, 5 полос уровней, `L_naive` = лучший наивный план, `L_opt` = истинный оптимум) — записан в спеку разделом **§14 «модель v2 (🔄 предложение, не реализовано)»**; старая модель §6.1–6.8 остаётся текущей.
- **Офлайн-замер** (`internal/routegame/gridv2*.go`, untracked) на **450 реальных парах** из dev-БД: наивные стратегии = ровно +10 %; «человек-модель» +40 % в ~78–83 %; вклад порядка 100 %. Но **уровень +50 % для человека недостижим (0 %)**, результат держится на экстремальной калибровке (k=6, h=14, все опасные клетки на коридорах), связь «дальность → число маяков» потеряна.
- **Критик (свежий, с числами)** — снова **СКУЧНО**: решение одноразовое и без риска/провала; награда сатурируется на +40 % после первого «ага»; верх +50 % мёртв; все перелёты — одна и та же задача (фиксация k=6/h=14); обучение невозможно (оценка приходит после необратимой отправки); «человек» измерен числом `1.1·L_opt`, а не реальным путём; гейты отбора поля выданы за критерии приёмки.

**Корень конфликта.** Критик требует, чтобы **ошибка кусала** (провал/штраф). Решение создателя — «**провала нет**, просто не выжать максимум». Без цены ошибки цикл не сходится: любая модель с гарантированным полом +10 % остаётся механикой «нельзя ошибиться».

**Решение создателя (2026-09-25):** «**провал — это отрицательный прирост скорости**». Прежняя рамка «провала нет / пол +10 %» **отменена**: награда может быть ниже нуля (перелёт медленнее), спектр — от отрицательного до +50 %. Создатель: «Работайте дальше, ищите решение и интерес». Открытый под-вопрос дизайну: как согласовать с прежним «кое-как = +10 %» — где на шкале ленивый, где провал (решает дизайнер, несёт на гейт).

**Решение создателя (2026-09-25, позже):** «**Можно вводить в игру новые сущности, если они приводят к улучшению геймплея**». Ограничение «без нового состояния» на переделку **снято** — серверный секрет на сегмент / хранение поля / скрытая информация допустимы, если дают интерес. Автосолвер и «непредсказуемость» теперь можно закрывать по-настоящему (см. v5).

**Циклы переделки (итоги; каждый вердикт — свежий `@gamecritic`):**
- **v3** (непрерывная шкала `[−20 %…+50 %]`, сложность от географии, риск-ставка) → замер на 450 парах → **СКУЧНО**: стандартная эвристика (жадный порядок + 2-opt) упирается в потолок; верх — плато; провал нейтрализуется индикатором; нет неопределённости и причины переиграть.
- **v4** (безопасный пол +10 %, цена поворота `τ`, русла/эффект Штейнера, seed от `start_time`, тиры) → **СКУЧНО** (без замера): детерминизм/автосолвер, нужна настоящая неопределённость.
- **v5** (новая сущность `player_route_puzzle`: серверный секрет + скрытые секторы + разведка импульсами + развилка надёжно/осведомлённо/слепо) → замер → **СКУЧНО**: скрытый слой вышел **−EV** (разведка хуже бездействия на стартовом тире), вершина +50 недостижима (тавтология), мастерство перестаёт платить сразу после стандартной эвристики, «секторов ≤ импульсов» делает верхний тир детерминированным, причина переиграть не подтверждена (харнесс сеял без `start_time`).
- **v6** (18 ошибок, «100 % при везении») → замер → **СКУЧНО**: скрытый слой доминируем (в замере «игнорировать тёмное» выгоднее), везение = пред-ролл сервера, ошибки схлопываются в ~5–6.
- **v7** (два якоря, скрытый слой обязателен для верха, 15 ошибок) → замер на единой шкале → **СКУЧНО**: разведка бесплатна (`I ≥ V` всегда, `ping_wasted` Δ=0), замер «осведомлённого» — оракул, единственный разумный ход «обойти тёмное».
- **v8** (две линии `Safe`/`Risk`, публичные подписи `σ`, разведка с ценой R1–R5, 16 ошибок) → замер → **СКУЧНО** (с конструктивным форматом): **оставить** — пол +10 %, провал кусает, безопасная линия не проваливается, каркас двух линий, публичная `σ` без оракула, поле-пазл, богатая таксономия ошибок; **доработать** — рисковая линия доминируема (равное среднее при большей дисперсии, выплата разовая), разведка бесплатна (5.3 % против ≥25 %), доминирующий скрипт «Safe + зондируй громкое», режимы не меняют развилку, ошибки схлопываются, верх = лотерея, сильная видимая игра наказана.

**Статус:** **9 циклов designer→критик (v1–v9), все «СКУЧНО»**. Замер v9: 12 из 20 подкритериев не сходятся, ключевые — «премия за риск» (факт +0.002 вместо +0.04…+0.08), «зонд не умеет проигрывать» (вредит 3.1 % против ≥25 %), «единый скрипт побеждает» (правило «обойди + зондируй громкое» бьёт условную политику). **Решение создателя (2026-09-26): B — заморозить и разрабатывать** — внедряем лучшую измеренную модель (v9, §14) в игру, принимая, что «интересно» по мерке критика не дотянуто. Харнесс `internal/routegame/gridv9*.go` — untracked (база для внедрения).

**План внедрения (чекпоинты, каждый со своим гейтом):**
- **Э1. Сервер-ядро.** Перенести модель поля/оценки v9 в прод `internal/routegame`: сетка 10×10; объекты (топь/русла/шлюзы/односторонние течения/тупики/разовый мост/узкий проход); скрытые секторы с публичной подписью `σ` из **серверного секрета**; кривая награды (пол +10 %, безопасная линия +30 %, рисковая до +50 %, низ до −20 %); новая таблица `player_route_puzzle` + миграция; ручка `POST /api/accelerator/scan`; сохранить контракт `offer`/`boost`.
- **Э2. Интеграция/анти-бот.** Валидация пути по сетке, `fingerprint`, лимиты; применение бонуса (в т.ч. **отрицательного** — перелёт может удлиниться), сброс при развороте/прибытии, очистка состояния сегмента.
- **Э3. Клиент-доска.** `web/route.html` + `web/static/js/route/*`: рисование с прилипанием к клеткам, захват маяков, разведка импульсами, индикация «дорого/тупик/течение», результат с разбором по факторам; без подсказки-оракула.
- **Э4. Вход/UI + визуал.** Панель полёта, ячейка «Корабль», кнопка сброса (уже есть), визуальный стиль/спрайты (победители пула).
- **Э5. Тест/сдача.** `@tester` + e2e-смоук доски, доки/`impact_map`/хвосты, пуш по слову создателя.

**Важная дельта:** отрицательный бонус означает, что **перелёт может удлиниться** — это меняет инвариант И-6 (§7) и шкалу (§8); §14 уже помечает дельты «вносится при реализации».

**Новое направление создателя (2026-09-25, дословно):** «50 % при везении — это норм. Ну сделайте 100 % при везении. Нужно придумать именно разнообразные ошибки, 10–20 штук, и пронести их в геймплей.» То есть: (1) верх шкалы может достигаться **везением** — нужно, чтобы удача реально выводила на **полный потолок** (+50 %), а не на 2.7 % полей; (2) ядро интереса — **10–20 разных ошибок игрока**, каждая со своей ценой и понятной причиной, вплетённых в геймплей (не один «неверный порядок»). Это снимает требование критика «верх только за счёт мастерства» — везение допустимо.

**Правила создателя (2026-09-25, дословно/по смыслу):** (а) критик геймплея отвечает в формате «**что стало лучше / оставить**» + «**что доработать**» + вердикт, а не только «скучно»; (б) критик — всегда свежим контекстом; (в) при повторном сбое субагента задача **дробится на составляющие**.

### 16.1. Э1-A — сдача (2026-09-26)

**Статус:** ✅ подэтап A принят создателем («ок»). Независимое ревью диффа — **блокирующих нет**; DoD (build/vet/`-race -short` по затронутым пакетам) зелёный.

**Коммит:** код — `32073ae` `feat(accelerator): offer отдаёт доску v9 и scan — разведка сектора (Э1-A)` (7 файлов, +867/−27).

**Что вошло.** `GET /api/accelerator/offer` отдаёт **видимую доску v9** (`board`: n, старт, финиш, маяки, видимые цены/объекты поля, секторы с публичной σ и окружением, режим) + `pings_left` + `revealed` + прежние поля; скрытый слой (`secret`, `realized`, содержимое секторов, якоря `L_*`, `vis_path`, `l0_cells`) клиенту не отдаётся (тест «утечки нет»). Сегментная задача `player_route_puzzle` заводится/пересоздаётся на смене сегмента (secret/layout/pings; импульсы = 2). Новая ручка `POST /api/accelerator/scan` вскрывает сектор серверным secret и **атомарно** списывает импульс (гейт в одном UPDATE); нет импульсов → 409. `boost` сознательно оставлен на старой модели (Э1-B). Старая модель `field.go`/`evaluate.go`/`passport.go` не тронута.

**Доверие к цифрам замера.** Тест-двойник (`grid_twin_test.go`) подтверждает: **публичная геометрия прод-модели совпадает с замерным харнессом** при одном seed/dist/паспорте; таблицы распределения содержимого σ→content и поправок окружения скопированы 1:1. Скрытый слой намеренно строится иначе (раскладка — от публичного seed, содержимое — от серверного secret): это требуемое разделение слоёв, не расхождение.

**Хвосты Э1-A (открытые, с адресом):**
1. **Клиент временно мёртв:** старая страница читает `field`, новый offer отдаёт `board` — мини-игра в браузере неиграбельна до переделки клиента (Э3). Осознанно.
2. **`boost` пока на старой модели** — offer и boost несогласованы; закрывается Э1-B (до живого прогона).
3. Контракт отказа `scan` при `bad_sector`/`no_pings` идёт без `cooldown_remaining_s` — мелкий разнобой с прочими отказами; поправить в Э1-B. Адрес: `@developer`.
4. Повторное вскрытие сектора снова списывает импульс (дедупа нет) — согласуется с §14.4 R6; зафиксировать в спеке. Адрес: `@designer`.
5. Импульсы = 2 константой; параметр тира `accel_N` — позже. Адрес: `@designer`/`@balancetester`.
6. `docs/DB.md`: `player_route_puzzle` (`000091`) и `player_accelerator` (`000089`) не описаны — дельта при разносе. Адрес: `@dispatcher`/`@developer`.
7. `accelerator_grid_handlers.go` — 390 строк (>300, `AGENTS.md` §4.8): кандидат на дробление при следующем касании. Адрес: `@developer`.
8. Разбор результата по 16 факторам (§14.7/§14.8) в модели пока нет — **отдельный чекпоинт**. Адрес: `@manager`/`@designer`.

**Дальше (план внедрения, каждый со своим гейтом):** Э1-B (boost на v9 + бонус включая отрицательный + жизненный цикл задачи + фикс отказа scan) → отдельный чекпоинт «разбор по 16 факторам» → Э3 (клиент-доска) → Э4 (вход/UI + визуал) → Э5 (тест/сдача, доки/`impact_map`/хвосты).

**Вызов на новую сессию (Э1-B — исторический, актуальный — в §16.2):**

```text
@manager Продолжаем линию «ускоритель», внедрение модели v9 «Планшет», подэтап B.
Идея — docs/gamedesign/ideas/2026-09-25_ускоритель-модуль-корабля-и-мини-игра-прокладка-маршрута.md (§16/§16.1).
Спека — docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md §14.
Сделано: Э1-A (коммит 32073ae) — offer отдаёт доску v9, ручка scan вскрывает сектор, задача сегмента; ревью без блокеров.
Задача B: boost на модели v9 (валидация пути по клеткам: старт/финиш/маяки/4-связность), бонус ВКЛЮЧАЯ отрицательный
(перелёт может удлиниться — И-6 отменяется), учёт помех от разведки (§14.4 R2/§14.7), применение через BoostFlight,
жизненный цикл задачи сегмента (сброс при прибытии/развороте), форма отказа scan как у прочих.
Дальше: отдельный чекпоинт «разбор по 16 факторам»; Э3 клиент-доска; Э4 вход/UI/визуал; Э5 тест/сдача.
```

### 16.2. Э1-B — сдача (2026-09-26)

**Статус:** ✅ подэтап B принят создателем («го»). Независимое ревью диффа — **блокирующих нет**; DoD (build/vet, `-race -short` по всей затронутой зоне) зелёный.

**Коммит:** код — `46453f3` `feat(accelerator): boost на доске v9 — оценка пути и бонус (Э1-B)` (9 файлов, +844/−417).

**Что вошло.** `POST /api/accelerator/boost` переведён на модель v9: общая точка сборки сегмента для offer/scan/boost (один seed + серверный secret/layout → поля не разъезжаются); валидация пути по клеткам (границы, старт, финиш, все маяки, 4-связность); бонус моделью, **может быть отрицательным** (перелёт удлиняется; И-6 §7 отменена, низ ≈ −0.20); **помеха разведки** — вскрытые секторы со `unstable` поднимают стоимость своих клеток и 4-соседей до цены ловушки (согласовано с эталонным харнессом, доказано тестом-двойником); `newRem = remaining/(1+bonus)` применяется атомарно; откат тратится только за принятый полный маршрут; ответ — `{bonus, remaining_s}` (старое `quality` убрано). Форма отказа `scan` унифицирована.

**Жизненный цикл задачи.** Ленивая защита по `segment_hash` (новый сегмент → новый secret/layout, `revealed=[]`, импульсы=2) + явная очистка строки в `ArrivalHandler` при прибытии.

**Выведено из прода.** Старый `boost` и непрерывная модель (`field.go`/`evaluate.go`: `GenerateField`/`EvaluatePath`/`Field`/`Point`) — прод-потребителей не осталось, живут только в собственных тестах. Не удалены (решение об удалении — при следующем касании). `passport.go`/`HashSeed` — общие с v9, не легаси. `AcceleratorBonus`/`bonus_max` осиротели в v9-пути (только тест + дефолт каталога).

**Хвосты Э1-B (открытые, с адресом):**
1. **Клиент-доска (Э3):** `web/static/js/route/route_game.js` ещё на старой модели (читает `field`/`quality`, шлёт путь точками `{x,y}`; v9 ждёт `board` и путь **индексами клеток**, умеет отрицательный итог). Адрес: `@uidesigner`→`@developer`.
2. **Гонка `Get+Replace`** задачи сегмента (конкурентные запросы одного игрока на смене сегмента могут дать разные secret). Предсуществующая (Э1-A). Адрес: `@developer` — на чекпоинте клиента (детерминированный secret от seed либо `ON CONFLICT DO NOTHING` + перечитывание).
3. **Разбор по 16 факторам** (§14.7/§14.8) — отдельный чекпоинт (сейчас отдаётся только число). Адрес: `@manager`/`@designer`.
4. Именованная цена `ping_destabilize` (−0.09) пока «спрятана» в подъёме стоимости клеток — выделить при разборе по факторам. Адрес: `@designer`.
5. `docs/ARCHITECTURE.md` §2 не знает новых файлов (A: `accelerator_grid_handlers.go`; B: `accelerator_grid_boost.go`, `grid_model.go`, `grid_place.go`, `grid_search.go`, `grid_disturb.go`) — файл занят чужой правкой. Адрес: `@developer`/`@dispatcher`.
6. Дублирование гейтов (offer — `acceleratorTravelReason`, boost/scan — руками) — кандидат на сведение. Адрес: `@developer`.

**Дальше (Э3 — клиент-доска).** Интерфейс игры переделывается под планшет: рисование по клеткам (прилипание), захват маяков, разведка импульсами по секторам, подсказки «дорого/тупик/течение», результат с учётом **отрицательного** бонуса. Дизайн интерфейса — `@uidesigner` (mobile-first), затем `@developer`. Отдельным чекпоинтом — разбор по 16 факторам. Затем Э4 (вход/UI + визуал, победители спрайтов) и Э5 (тест/сдача).

**Вызов на новую сессию (Э3 — клиент-доска):**

```text
@manager Продолжаем линию «ускоритель», внедрение v9 «Планшет», Э3 — клиент-доска.
Идея — docs/gamedesign/ideas/2026-09-25_ускоритель-модуль-корабля-и-мини-игра-прокладка-маршрута.md (§16.2).
Спека модели — docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md §14 (особенно §14.8 — что видит игрок);
интерфейсная спека — docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md (переделать под доску).
Сделано: серверная половина — Э1-A (32073ae) и Э1-B (46453f3): offer отдаёт доску, scan вскрывает сектор,
boost оценивает путь по клеткам, бонус включая отрицательный, помеха разведки, снятие задачи при прибытии.
Задача Э3: переделать web/route.html + web/static/js/route/* под доску v9 (рисование по клеткам с прилипанием,
разведка импульсами, индикация дорого/тупик/течение, результат включая «медленнее»), обновить вход кнопки и панель полёта.
Побочное: убрать гонку Get+Replace задачи сегмента.
Дальше: разбор по 16 факторам; Э4 вход/UI/визуал (победители спрайтов); Э5 тест/сдача.
```

### 16.3. Э3-C1 — сдача (2026-09-26)

**Статус:** ✅ подэтап C1 принят создателем («го»). Независимое ревью диффа — **блокирующих нет**; браузерный смоук 390×844 — 8/8, `pageerror` нет; сервер/Go/БД не тронуты.

**Коммит:** `bfba4eb` `feat(route): клиент-доска v9 — сетка, путь по клеткам, гейт (Э3-C1)` (7 файлов, +893/−513; новый `web/static/js/route/route_board.js`).

**Что вошло.** Экран игры переведён на доску: `offer` читается как `board` (старого `field` нет); сетка `n×n`, объекты поля, секторы с публичной σ, маяки, старт (корабль игрока) и финиш (звезда/ЧД); путь — 4-связная цепочка клеток (тап/протяжка, `connectStair`, «Шаг назад»/«Заново»); гейт «Проложить» (старт+финиш+все маяки+порог). Новый модуль чистых функций `route_board.js`. Ревью подтвердило: до отправки нет чисел/вердикта/оптимума. Разведка/отправка/результат — C2; полировка входа (§13 п.10) — Э4.

**Хвосты Э3-C1 (открытые, с адресом):**
1. **C2:** разведка импульсом + карточка сектора, «жар»-метки, двухтапная отправка и результат (включая отрицательный), состояния отказа/тосты, обновление `tools/e2e/route-check.js` (сейчас устарел под старую модель). Адрес: `@developer`.
2. Подсветка чипа режима не срабатывает (в CSS селектор `.hud-chip.mode-chip`, а у `#mode-chip` только класс `hud-chip`) — фикс в C2. Адрес: `@developer`.
3. `web/static/js/route/route_render.js` — 521 строка (>300, `AGENTS.md` §4.8) — кандидат на разделение. Адрес: `@developer`.
4. Мёртвые до C2 символы (`turnCount`, словари scan/результата, осиротевший `#toast`) — оживут в C2. Адрес: `@developer`.
5. `docs/ARCHITECTURE.md` §2 не перечисляет `web/static/js/route/*` (файл занят чужой правкой). Адрес: `@developer`/`@dispatcher`.

**Дальше:** Э3-C2 → независимый прогон `@tester` (полный флоу игры) → Э4 (вход/визуал, победители спрайтов) → отдельный чекпоинт «разбор по 16 факторам» → Э5 (test/сдача).

### 16.4. Э3-C2 + легенда-попап — сдача (2026-09-26)

**Статус:** ✅ функционал принят создателем («го»), с доработкой по легенде (см. ниже). Ревью C2 и легенды — **блокирующих нет**; живой e2e `route-check.js` — 11/11; браузер 390×844.

**Коммит:** `fe0adcc` `feat(route): разведка, «жар», отправка и результат + легенда-попап (Э3-C2)` (11 файлов, +1398/−547; новые модули `route_ui`, `route_effects`, `route_figures`, `route_legend`).

**Что вошло.** Разведка импульсом (карточка сектора: σ/окружение/риск, вскрытие, списание импульса, `revealed`; повторно не сканируется), «жар» трассы (локальные метки, включая «по течению»/«выгодно»), двухтапная отправка, результат со знаком (включая «медленнее»), состояния отказа/тосты; легенда — **попап с фигурами, нарисованными тем же кодом** (без цветных свотчей); e2e `route-check.js` под доску. Сервер/БД/миграции не тронуты. **Игра впервые играбельна в браузере на модели v9.**

**Решение создателя (2026-09-26) — переделка словаря и визуала (следующий чекпоинт):**
- **Космический сеттинг** (player-facing; внутренние коды сервера не меняем): стена → **«Помехи»** (решение создателя: в легенде пояснять как «помехи в гиперпространстве»), топь → **«Мгла»** (вместо «Туманность» — коллизия с фоновыми туманностями и именами регионов), русло → «Трасса», шлюз → «Кордон», мост → «Тоннель», тупик → «Обрыв», узкий проход → «Разрыв», финиш → «Цель»; остаются «Маяк», «Течение», «Сектор»; σ — Тихий/Ровный/Гулкий; содержимое сектора — Просвет/Зов/Воронка/Мираж/Сбой/Вакуум; «излом» → «Манёвр».
- **Легенда:** раскрывашки по группам (Ориентиры / Поле / Секторы / Метки курса), все свёрнуты — попап компактный; **убрать** путь/предпросмотр и вскрытое содержимое (оно — в карточке сектора).
- **Визуал:** кодировать **цвет + форма + паттерн** (различие обязано сохраняться в градациях серого); палитра разведена; содержимое сектора и метки «жара» — свои глифы; **новые спрайты не нужны** (всё кодом).
- Физика имён — игровая условность (науку не зовём).

**Хвосты Э3-C2 (открытые, с адресом):**
1. **Синк словаря** во всей интерфейсной спеке и §14.8 модели (критик: С-1/С-2 — средние) + мелочи М-1…М-9 (в т.ч. «Обрыв» в двух группах; «Разведать» vs «Зондировать»; коллизия имён с каноном). Адрес: `@designer` (модель) / `@uidesigner` (интерфейс).
2. **Незакрытые автотестом состояния отказа** (`no_pings`, `cooldown`, `too_short`, `no_field`, boost-`changed`) — закрыть независимым прогоном. Адрес: `@tester`.
3. `route_figures.js` 369 / `route_render.js` 414 строк — кандидаты на разделение. Адрес: `@developer`.
4. `docs/ARCHITECTURE.md` §2 не перечисляет `web/static/js/route/*`. Адрес: `@developer`/`@dispatcher`.
5. `docs/impact_map.json`: нет узлов линии ускорителя (`accelerator`, `player_accelerator`, `player_route_puzzle`, `travel_boost`, `travel_passport`, `route_game`, клиентские модули `route_*`) — критик подтвердил. Адрес: `@manager` (Э5).

**Дальше:** переделка словаря/визуала (синк спек `@designer`+`@uidesigner` → `@developer`) → независимый `@tester` (полный флоу + состояния отказа) → Э4 (полировка входа/визуал, победители спрайтов) → отдельный чекпоинт «разбор по 16 факторам» → Э5 (доки/`impact_map`/хвосты/пуш).

### 16.5. Э3-R — космический словарь и визуальный код (2026-09-26)

**Статус:** ✅ принято создателем («го»). Независимое ревью — **блокирующих нет**; живой e2e — 11/11; различие фигур в градациях серого подтверждено отдельным рендером.

**Коммиты:** дизайн — `3cb5686` `docs(design): ускоритель — космический словарь поля и визуальный код доски (v9)`; код — `0f16f62` `feat(route): космический словарь поля и визуальный код доски (Э3-R)` (12 файлов, +565/−274).

**Что вошло.**
- **Словарь игрока:** `wall`→«Помехи» (легенда: «помехи в гиперпространстве»), `mud`→«Мгла», `lane`→«Трасса», `current`→«Течение», `gate`→«Кордон», `bridge`→«Тоннель», `dead_end`→«Обрыв», `bottleneck`→«Разрыв»; Старт/Маяк/Цель; σ — Тихий/Ровный/Гулкий; находки — Просвет/Зов/Воронка/Мираж/Сбой/Вакуум; действие — «Зондировать»; «излом»→«Манёвр»; метки курса (в т.ч. «Срыв»). Ключи сервера (`mode`/`sig`/`content`/`dir`) не тронуты.
- **Легенда-попап:** 4 свёрнутые группы (Ориентиры/Поле/Секторы/Метки курса), раскрываются по тапу; убраны путь/предпросмотр и вскрытое содержимое (находки — в карточке сектора).
- **Визуал:** цвет **+ форма + паттерн**; «Трасса» = светлые продольные струи, «Помехи» = тёмная перекрёстная прерывистая решётка (±60°); находки — свой глиф+кайма (trap/decoy/empty не близнецы); метки — микро-глифы. Различие держится в градациях серого. Новых спрайтов нет.

**Хвосты Э3-R (открытые, с адресом):**
1. **Незакрытые автотестом состояния отказа** (`no_pings`, `cooldown`, `too_short`, `no_field`, boost-`changed`) — закрыть независимым прогоном. Адрес: `@tester`.
2. `route_figures.js` — 502 строки (>300): вынести `contentGlyph`/`heatGlyph`. Адрес: `@developer`.
3. `heatColor` в `route_figures.js` — лишний `export`. Адрес: `@developer`.
4. В `route_input.js` в комментариях остались «СТАРТ/ФИНИШ». Адрес: `@developer`.
5. Коллизии имён (В-5 «Мираж» ↔ корабль `ship_mirage`; В-6 «Сектор» ↔ имена регионов) — по умолчанию оставлены; альтернативы «Морок»/«Область». Адрес: создатель.
6. Дельта `docs/GLOSSARY.md` (термины линии ускорителя + «Слот корабля» += accelerator) — критик М-6. Адрес: `@dispatcher`/`@manager`.
7. `docs/ARCHITECTURE.md` §2 не перечисляет `web/static/js/route/*`. Адрес: `@developer`/`@dispatcher`.
8. `docs/impact_map.json` — нет узлов линии ускорителя (критик подтвердил). Адрес: `@manager` (Э5).

**Независимый прогон `@tester` (2026-09-26):** отчёт `docs/QA/checklists/2026-09-26_ускоритель-маршрут-v9-планшет.md` — **11/12 PASS** (полный флоу, легенда 4 группы/свёрнута, нет утечки до отправки, `no_pings`/`changed`/`no_flight`/`already_active`, отрицательный результат, мобильность 390×844, регресс карты/панели, `/health` 200). Дефект **D1** (метка курса «Тупиковый ход» — не-космично) **исправлен по решению создателя:** метка `dead_end` → **«Срыв»** (объект остаётся «Обрыв»), спеки модели/интерфейса и код синхронизированы, живой прогон снова 11/11. `NA`: `too_short`/`cooldown` (на dev недостижимы — нужны фикстуры/админ-сброс), баланс кривой — зона `@balancetester`, визуальная приёмка — создатель.

**Дальше:** Э4 (полировка входа `#flight-panel`/`slot-accelerator`, визуал, победители спрайтов) → отдельный чекпоинт «разбор по 16 факторам» → Э5 (доки/`impact_map`/хвосты/пуш).

### 16.6. Э4 — вход и спрайты (2026-09-26)

**Статус:** ✅ вход (кнопка «Ускорить» / ячейка «Корабль») уже соответствует спеке (§3/§7.2) — подтверждено тестером и смоуком; **победители спрайтов выбраны и заморожены**.

**Решение создателя:** «спрайты все хорошие, бери какой больше нравится» — выбор делегирован визуальному дизайнеру.

**Коммит:** `39fb31a` `feat(route): заморозка спрайтов доски — победители пула _01 (Э4)`.

**Победители пула (все `_01`), заморожены в `SPRITE_FREEZE`:** `star_core_01`, `black_hole_01`, `beacon_01`, `false_signal_01`, `hazard_cloud_01`, `nebula_bg_01`. Обоснование — читаемость на 390×844 (звезда ≈59 px, маяк ≈29 px, маркер Обрыва ≈18 px) и совместимость с кодовыми слоями (чёрный круг ЧД, глиф сектора, «спрайт нейтрален — цвет кодом»).

**Хвосты Э4 (открытые, с адресом):**
1. **Риск Р1 (визуальный, нужен глаз создателя):** фоновая туманность может оказаться почти невидимой (multiply-тинт × α≤0.35 + виньетка). Кандидаты фиксов (код, правило α≤0.35 не нарушается): пред-яркость тинта, второй проход «bloom», ослабить виньетку/рисовать туманность после неё, либо признать слой косметикой. Адрес: `@developer`/`@gdesigner` — по слову создателя (сначала посмотреть глазами на 390×844).
2. Лишние 16 PNG оставлены в пуле (перегенерация дорога); перенос проигравших в `web/static/sprites/route/_pool/` и сокращение `POOLS` — по желанию. Адрес: `@artist`/`@developer`.
3. `web/static/sprites/route/_preview/` (~3.67 МБ, untracked) — гигиена рабочей копии, не репозитория. Адрес: `@artist`/`@manager`.

**Дальше:** отдельный чекпоинт «разбор по 16 факторам» (§14.7/§14.8 — сервер + клиент) → Э5 (доки/`impact_map`/хвосты/пуш).

### 16.7. Э6 — разбор по факторам (2026-09-26)

**Статус:** ✅ принято создателем («го»). Независимое ревью — **блокирующих нет**; живой e2e — **13/13**; независимый прогон `@tester` — **12/12 PASS, дефектов нет** (`docs/QA/checklists/2026-09-26_разбор-по-факторам-маршрут-v9.md`).

**Коммиты:** `a8983ff` `feat(route): разбор по факторам — сервер и клиент (Э6)`; `1468f26` `feat(route): значки разбора — Петля, Перелёт Цели, Зонд впустую (Э6)`.

**Что вошло.** `ExplainGridPath` — чистая функция (§14.13.3/§14.13.4): 16 ошибок (10 путь-геометрия + 6 под неопределённостью) + `gain`/`neutral`; **не влияет на `bonus`**; `severity=0` при gain/neutral; порядок `error→gain→neutral`. Ответ `boost` — аддитивное поле `breakdown` (в `offer`/`scan` поля нет — до отправки не утекает; старый клиент не ломается). Клиент: `#result-breakdown` — группы Ошибки/Находки/Не ошибка, строка = глиф тем же кодом + имя (per-code: `mud_cost` «Сопротивление», `wall_cost` «Помехи») + полоса «тяжести» без цифр + «×N»; пусто → «Чистый курс»; нет поля/неизвестные коды → блок скрыт/игнор. Значки `revisit`/`overshoot`/`ping_wasted` — собственные (§12.14). Ловушка `contentGlyph` (kind без ветки → пустой слот) внесена в `docs/pitfalls/front-ui.md`.

**Хвосты Э6 (открытые, с адресом):**
1. **Калибровка полос `severity`** — гипотеза §14.13.7 (у `ping_destabilize` не измерена). Адрес: `@balancetester`.
2. **Тап-подсветка `cells`** на доске — отложена (§4.7.1). Адрес: `@developer`.
3. **Размеры файлов:** `grid_explain.go` 479, `route_config.js` 367, `route_figures.js` ~574 — кандидаты на разделение. Адрес: `@developer`.
4. **§4.7.1 спеки:** строка `ping_wasted` — синк на «свой глиф» (сейчас «содержимое empty»). Адрес: `@uidesigner`.
5. `cells` могут содержать дубликаты (косметика подсветки). Адрес: `@developer`.
6. Тестер предложил кейсы в QA-чек-лист (непустой разбор со всеми неизвестными кодами → блок скрыт; глиф строки различим). Адрес: `@manager` (`docs/QA_CHECKLIST.md`).

**Дальше:** Э5 — сдача линии (доки, реестр влияний, хвосты, пуш) по слову создателя.

### 16.8. Э5 — сдача линии (2026-09-26)

**Статус:** ✅ линия сдана. Все чекпоинты приняты создателем (ЧК1, ЧК2, ЧК3 S1/S2/S3, админ-сброс; v9 — Э1-A/B, Э3-C1/C2/R, Э4, Э6). Э5 закрывает разнос доков, реестр влияний, хвосты; пуш — по слову создателя.

**Что сделано на Э5.**
- Реестр влияний: узлы линии добавлены (`accelerator`, `player_accelerator`, `player_route_puzzle`, `route_game`, `route_breakdown`, `travel_passport`, `travel_boost`) + индекс `docs/impact_map.md` подтянут — коммит `dfa94d4`.
- Спеки линии закрыты (статус «реализовано»): модель — `docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md`; интерфейс — `docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md`. Прежде незакоммиченные обновления обеих спек (модель v9 §14 и интерфейс доски) зафиксированы в репозитории.
- QA-отчёты линии зафиксированы: `docs/QA/checklists/2026-09-26_ускоритель-маршрут-v9-планшет.md`, `docs/QA/checklists/2026-09-26_разбор-по-факторам-маршрут-v9.md`.
- Строка эпика на доске `docs/EPICS.md` — переведена в «✅ реализован».

**Разнос по докам (дельты для `@dispatcher`, применяются по слову создателя).**

`CHANGELOG.md` (компактно, тремя датами):
- +2026-09-25 — `Реализовано: ускоритель перелёта (ЧК1) — модуль-ускоритель (тип оборудования, выделенный слот, стартовый модуль), состояние отката, серверное ускорение межзвёздного перелёта перебазированием сегмента; одно активное ускорение на сегмент; до появления мини-игры ускоритель неактивен (unknown_game).`
- +2026-09-25 — `Реализовано: мини-игра «Прокладка маршрута» — паспорт перелёта, генератор поля от seed, оценка пути, ручки offer/boost, клиентский экран игры (страница доски), вход «Ускорить» из панели полёта и ячейка «Ускоритель» в разделе «Корабль»; админский сброс своего состояния ускорителя.`
- +2026-09-26 — `Реализовано: переделка сложности мини-игры на модель «Планшет» (v9) — серверная доска, разведка импульсами (ручка scan), оценка пути по клеткам, бонус включая отрицательный, разбор по факторам; клиент-доска с космическим словарём и визуальным кодом, заморозка спрайтов.`

`docs/DB.md`:
- миграция `000089_accelerator.sql`: `equipment.type` CHECK += `accelerator`; таблица `player_accelerator (user_id PK→users ON DELETE CASCADE, last_boost_at, last_cooldown_min, updated_at)` — откат, переживает рестарт, не в `truncateTables`; `ship_models.slots` стартовой модели += `accelerator:1`.
- миграция `000091_player_route_puzzle.sql`: таблица `player_route_puzzle` (задача сегмента: `secret`, `layout` JSONB, `revealed`, `pings_left`; владелец — игрок; переживает рестарт; **не** в `truncateTables`).

`docs/ARCHITECTURE.md` §2/§4 (файл занят чужой правкой — вносить при освобождении):
- Новые файлы сервера: `internal/ship/accelerator.go`; `internal/models/player_accelerator.go`, `internal/models/route_puzzle.go`; `internal/repository/player_accelerator_repository.go`, `internal/repository/route_puzzle_repository.go`; `internal/routegame/{passport,grid_model,grid_place,grid_search,grid_disturb,grid_explain}.go`; `internal/handlers/{accelerator_handlers,accelerator_game_handlers,accelerator_grid_handlers,accelerator_grid_boost}.go`.
- `travel.Manager` += `BoostFlight`/`Booster`/`SetBooster`; аддитивные блоки `accelerator` в `POST /travel` (оба пути) и `/me`.
- Клиент: `web/route.html`, `web/static/css/route.css`, `web/static/js/route/*` (доска, рендер, ввод, интерфейс, эффекты, фигуры, легенда, спрайты, игра, конфиг), пул `web/static/sprites/route/*`.

`.opencode/context.md` §Указатели:
- «Ускоритель перелёта (линия сдана 2026-09-26): работая с полётом, оборудованием/слотом, `/travel`/`/me` или мини-игрой маршрута — читай спеки `docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md` (§14 — модель v9) и `docs/specs/2026-09-25-маршрут-мини-игра-интерфейс.md`.»

`docs/GLOSSARY.md`:
- «Ускоритель (модуль корабля)», «Откат ускорителя», «Активное ускорение», «Паспорт перелёта», «Бонус скорости»; «Слот корабля» += `accelerator`.
- Космический словарь доски: Помехи, Мгла, Трасса, Течение, Кордон, Тоннель, Обрыв, Разрыв, Маяк, Сектор; содержимое сектора — Просвет/Зов/Воронка/Мираж/Сбой/Вакуум; действия — «Зондировать», «Манёвр».

`docs/specs/README.md` (файл смешан с чужими правками — зона `@designer`/`@dispatcher`): строки обеих спек — пометить «✅ реализовано 2026-09-26».

`docs/TAILS.md`: снять закрытые хвосты линии, внести открытые (список ниже).

`docs/EPICS.md`: строка эпика «Ускоритель + мини-игра „Прокладка маршрута"» → «✅ реализован (2026-09-26)».

**Хвосты линии (открытые, с адресом).**
1. Анти-бот `boost`: минимальный шаг точки и лимит частоты на игрока — `@developer`.
2. Гонка при смене сегмента (конкурентные запросы могут дать разные секреты) — `@developer` (детерминированный секрет от seed либо `ON CONFLICT DO NOTHING` + перечитывание).
3. Калибровка полос значимости разбора — гипотеза; `ping_destabilize` не измерена — `@balancetester`.
4. Тап-подсветка клеток разбора на доске отложена — `@developer`.
5. Размеры файлов больше ориентира: `grid_explain.go`, `route_figures.js`, `route_render.js`, `route_config.js`, `accelerator_grid_handlers.go` — `@developer`.
6. Синк §4.7.1 интерфейсной спеки (строка `ping_wasted` — «свой глиф») — `@uidesigner`.
7. `docs/ARCHITECTURE.md` §2 — новые файлы линии (файл занят чужой правкой) — `@developer`/`@dispatcher`.
8. Строки обеих спек в `docs/specs/README.md` (файл смешан) — `@designer`/`@dispatcher`.
9. Риск Р1: фоновая туманность может быть почти невидима (multiply-тинт × α≤0.35 + виньетка) — нужен глаз на 390×844 — `@developer`/`@gdesigner`.
10. Гигиена пула: 16 неиспользованных PNG и `web/static/sprites/route/_preview/` (~3.67 МБ, untracked) — `@artist`/`@manager`.
11. Коллизии имён («Мираж» ↔ корабль `ship_mirage`; «Сектор» ↔ имена регионов) — по умолчанию оставлены; решение создателя.
12. Легаси-модель (`internal/routegame/field.go`/`evaluate.go`) без прод-потребителя (живёт в своих тестах) — решение об удалении при следующем касании — `@developer`.
13. Импульсы = 2 константой; параметр тира `accel_N` — `@designer`/`@balancetester`.
14. Числа (стартовый `bonus_max` 0.50, откат 25 мин, пороги показа 180 с / отправки 90 с; прогрессия `accel_2` 0.60 / `accel_3` 0.70) — гипотезы — `@balancetester`.
15. Дублирование гейтов (`offer` — хелпер, `boost`/`scan` — руками) — `@developer`.
16. `AcceleratorBonus`/`bonus_max` осиротели в v9-пути (только тест + дефолт каталога) — решение при следующем касании.
17. Временные e2e-скрипты линии (`tools/e2e/tmp-accel-entry-check.js`, `tmp-qa-accel-reset-btn.js`, `qa-route-v9-probe.js`) — гигиена — `@manager`/`@developer`.
18. Кейсы в `docs/QA_CHECKLIST.md` (разбор: непустой набор всех неизвестных кодов → блок скрыт; глиф строки различим) — `@manager`.

**Пуш:** не делался — по слову создателя, через `@dispatcher`.

**Ретроспектива:** правок инструкций по этой линии применено не было (обе дельты из раздела «Ретроспектива» остались предложениями).

---

## Ретроспектива

- **Корень зависания:** цикл переделки упёрся в противоречие между требованием критика (ошибка должна кусать — провал/штраф) и решением создателя «провала нет». Противоречие не было выявлено на старте: менеджер знал оба факта по отдельности, но не сверил их между собой до запуска цикла.
- **Предлагаемая дельта:** в `.opencode/agents/manager.md` (раздел «ПРОЦЕСС», шаг 5 «Дизайн») — «**В ТЗ `@gamecritic` явно передавай решения создателя, которые считаются неизменными** (напр. «провала нет»), и просьбу: если «интересно» невозможно без их отмены — сказать это в первом же вердикте, не гоняя итерации.»
- **Корень зависания (2):** открытая задача «сделай интересно» без согласованного критерия приёмки и предела итераций → 4 круга designer→критик без сходимости (v1–v5, все «СКУЧНО»), бюджет итераций превышен.
- **Предлагаемая дельта (2):** в `.opencode/agents/manager.md` (раздел «ПРОЦЕСС», шаг 5 «Дизайн») — «**Для открытой творческой задачи («сделай интересно») до старта зафиксируй с создателем критерий приёмки и предел итераций** (напр. «до 3 прогонов критика, дальше — решение: доработать / сменить механику / принять»).»
