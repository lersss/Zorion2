# 46a. Векторы разработки — параллельные команды

**Статус:** Закрыта (2026-09-17, решение создателя: сетка утверждена, `docs/VECTORS.md` создан и работает; планы R1–R5 — справочно)
**Дата:** 2026-09-16
**Ведёт:** @manager (эта идея)
**Создатель запросил:** систематизировать разработку, запуская 4–5 команд ИИ параллельно; выделить векторы, минимально мешающие друг другу в коде; накидать план по каждому на 4–5 релизов вперёд; включить новые механики (игровую петлю).

---

## 0. Откуда взято (не из роадмапа — из кода)

Векторы выделены по **графу зависимостей Go-пакетов и общим точкам правок**, а не по роадмапу. Факты:

- Подсистемы уже разнесены по пакетам и почти не импортируют друг друга:
  - `internal/generator/galaxy` + `internal/astro` → только `models`/`names`/`astro`
  - `internal/npc/*` → только `mapcache`/`models` (самодостаточен)
  - `internal/generator/ship` + `ship_*` → только `models`/`names`
  - `internal/generator/planet` + `audit/planet` → связаны с `settlement` и `resource`
  - `internal/economy/settlement` + `settlement`-генератор + `planet_repo`/`economy_repo`/`galaxy_population`
- **Горячие точки (пересекутся все вектора, неизбежно):**
  1. `internal/models/*` — каждая фича расширяет модели (файлы уже разнесены: `npc_agent.go`, `ship_visual.go`, `economy.go`, `planet.go` — отдельные).
  2. `internal/repository/*` — тоже разнесён по файлам.
  3. `migrations/` — **общая нумерация**, последняя `000032`. Реальные конфликты, если два вектора добавляют миграцию в одном релизе.
  4. `cmd/server/main.go` — регистрация всех маршрутов + инициализация всех менеджеров.
  5. `web/static/js/admin/{tabs.js, main.js, admin.html}` — регистрация вкладок админки.
  6. Стык `planet ↔ settlement`: `planet_data_generate.go`, `descriptions_tags.go`, `properties.go` импортируют settlement — Вектор A и B могут тут столкнуться.
- Каркас игровой петли в коде есть, но это каркас, не механика:
  - Контракты: `Assignment` (open/taken/completed), `GetContracts/TakeContract/CompleteTestContract`; **нет эскроу, поставок, возврата, охраны**.
  - Полёт: `TravelManager` реальный (старт/отмена/прибытие, `onArrival`); дальше — пусто.
  - Добыча / сдача / «мир изменился»: **нет** — нет инвентаря/груза игрока, нет денег (только дизайн `14_money.md`), нет реакции мира.

---

## 1. Сетка векторов

**Решение создателя 2026-09-16:** сетка из **7 векторов** с именами: **Генерация ·
Новые механики · Поселения · Агенты · Визуал · Баги · Игрок** (маппинг: A→Генерация,
B→Поселения, C→Агенты, D→Визуал; F разделён: системные механики → Новые механики,
петля/аккаунт/UI игрока → Игрок; E «Карта/UX» выведен — баги → Баги, UX по месту).
Практическая подсказка по разнесению — `docs/VECTORS.md`. Ниже — исходная буквенная
сетка и планы (справочно).

**5 активных + D тонкий (в паузах).** (Вопрос создания: см. §4.)

| # | Вектор | Зона кода | Общий слой |
|---|--------|-----------|------------|
| A | Генерация вселенной (звёзды, планеты, аномалии, ресурсы, профили регионов) | `internal/astro`, `internal/generator/galaxy`, `internal/generator/planet`, `internal/audit/planet`, `config/`, `internal/resource` | models, миграции |
| B | Поселения и экономика-фон (смертность, рождаемость, тир, заводы, близнецы, балансировщик) | `internal/economy/settlement`, `internal/generator/settlement`, `planet_repo.go`, `economy_repository.go`, `galaxy_population.go`, вкладки «Основное/Гипотезы/Балансировка» | models/economy, миграции |
| C | NPC-агенты (планировщик, масштаб за 100k, фронт агентов) | `internal/npc/*`, `models/npc_agent.go`, `npc_repository.go`, `admin_npc.go`, `npc_agents.js`, `npc.js` | models/npc |
| E | Карта, UX и баги фронта | `web/static/js/map/*`, `web/static/js/modal/*`, `mapcache`, `filter_worlds_handler.go`, `search.js` | mapcache, front |
| **F** | **Игровая петля: контракт → полёт → добыча → сдача → мир изменился** | `internal/travel`, `internal/models/{contracts,economy,assignment,user}.go`, `internal/repository/{assignment,economy}.go`, инвентарь игрока (новое в `users`), эскроу/деньги (новые таблицы), `internal/handlers/{contract,travel}_handlers.go`, фронт `web/static/js/`, `web/contracts.html` | models/contracts+economy, travel, миграции |
| D | Корабли-визуал — **тонкий вектор, в паузах** | `internal/generator/ship`, `models/ship_*`, `ship_repository.go`, `ship_catalog.go`, `ship_render.js`, `ships.js`, `web/static/sprites/` | models/ship |

---

## 2. Планы по релизам (5 активных)

### Вектор A — Генерация вселенной
- **R1:** 99.2.1 Аномалии как контент (Go-чтение `config/anomalies/`, API, детекция, показ).
- **R2:** 99.2.5 Переход на новую ресурсную модель (вещество ≠ месторождение, универсальный слой + генератор полей).
- **R3:** 99.2.11 Генератор: физика → архетип (перевернуть цепочку классификации).
- **R4:** 99.2.10 Профили регионов (классы кризисов/благ по физической причине).
- **R5:** 99.2.2 LLM-генерация описаний (fallback на библиотеки).

### Вектор B — Поселения и экономика-фон
- **R1:** B24 (🔴 срочно) — поселения на планету в гипотезах + удаление старой логики заводов из `GenerateSettlements` (решение 2026-09-13).
- **R2:** Калибровка оставшихся констант (гравитация, радиация, NDead) через близнецов.
- **R3:** Естественный запас «сливки» + прогресс тира в товарах.
- **R4:** Деньги — три потока `14_money.md` (эмиссия от занятого населения, приватные балансы).
- **R5:** Тир с корзиной (после разблокировки номенклатуры / появления потребителей).

### Вектор C — NPC-агенты
- **R1:** 20a.1 NPC v1 — довести по баланс-проверке В3 и одобрить; бенчмарк 26b (10-50-100k).
- **R2:** 26d Масштабирование за 100k — архитектура планировщика.
- **R3:** 26e Фронт — браузер не справляется с большим числом агентов на карте.
- **R4:** 26f Равновесие потока — потолок одновременно летящих ~30k.
- **R5:** Расширение NPC (бюджет поиска, детерминизм, headless-симуляция).

### Вектор E — Карта, UX и баги фронта
- **R1:** B16, B17 (кластеризация/декластеризация, полёт), B25 (редирект на логин).
- **R2:** B20 «скалы»→«горы» по всем вхождениям.
- **R3:** Вкладка «Поселения» первой в модалке + мини-карта в модалке.
- **R4:** Компактность карточки планеты / открытая проблема «модалка разрывает опыт».
- **R5:** Сквозные UX-долги из бэклога 99.3.4.

### Вектор F — Игровая петля (приоритетный вертикальный срез)
- **R1:** Петля-минимум: инвентарь игрока (груз) + эскроу-депозит при взятии + сдача контракта с проверкой груза. «Полёт теперь что-то возит».
- **R2:** Добыча: действие «собрать ресурс» на планете → груз; + простейшие деньги `14_money` (эмиссия от поселений, приватный баланс).
- **R3:** «Мир изменился» — сдача контракта влияет на поселение (рост/товар), территория реагирует; карточка поселения показывает эффект.
- **R4:** 99.2.8 Автозаводы и присутствие (автозавод как постройка, присутствие как контракт) — по спеке.
- **R5:** 99.2.7 полная модель контракта: охрана, поставки, возврат, операции (по спеке) + 99.2.9 Сеть ретрансляторов (до 1.0, отдельная подсистема в том же векторе).

---

## 3. Правила развязки на горячих точках

1. **Миграции — бронь номеров через @manager.** Последняя `000032`. Вектор, которому нужна таблица/колонка, берёт номер в `COORDINATION.md` до начала работы. Два вектора одну миграцию в одном релизе не добавляют.
2. **`cmd/server/main.go` — только свои блок-секции.** Каждый вектор добавляет маршруты + инициализацию своего менеджера отдельным блоком; общую часть не трогает.
3. **Вкладки админки — один JS-файл на фичу** (паттерн AGENTS.md §4.23), в `tabs.js`/`admin.html`/`main.js` — точечные вставки (импорт + кнопка + pane), «добавил в конец, не переписал».
4. **Стык `planet ↔ settlement` (A vs B)** — назначаем владельца общего файла на релиз; второй вектор в этот релиз туда не лезет. (Буфер-интерфейс — отдельный рефакторинг, не сейчас.)
5. **Общие модели/репо** — каждый вектор трогает только свои файлы; нужна правка чужого — через дельту/меня, не напрямую.

---

## 4. Открытые решения создателя (нужны до старта)

1. **Развязка «Поселения» ↔ «Новые механики»** (оба живут в `models/economy.go` + `economy_repository.go` + `planet_repo.go`) — **открыто, не блокирует подсказку**:
   - **Вариант 1 (слой):** Поселения = фон (смертность/тир/заводы), Новые механики = игрок и его петля. Общий `models/economy.go` на релиз отдаём одному вектору.
   - **Вариант 2 (очередь):** Поселения заканчивают R1–R2 до старта Новых механик.
   - Предложение менеджера: **Вариант 1** (поселения почти завершены, петля — свежий приоритет).
2. **Сетка — РЕШЕНО 2026-09-16 (дополнено в тот же день):** 7 векторов с именами создателя: **Генерация, Новые механики, Поселения, Агенты, Визуал, Баги, Игрок** (маппинг на A/B/C/D/F; F разделён на «Новые механики» (системные) и «Игрок» (петля/аккаунт/UI игрока); E выведен — баги → «Баги», UX по месту). Подсказка по разнесению — `docs/VECTORS.md`.
3. **Определение релиза:** итерация (1–2 в день, как в истории) или календарный спринт — от этого зависит глубина нарезки R1–R5. **Открыто, на подсказку не влияет.**

---

## 5. Разнос по докам (дельты для @dispatcher)

> Применяет @dispatcher в паузу. Пока идея в статусе «предложение» — **ничего не разносить**. Дельты активны только после решений создателя и старта векторов.

Разнос по докам: **ГОТОВО** (разнесено @dispatcher 2026-09-16, сетка утверждена). Внесено: docs/INDEX.md — строка про `docs/VECTORS.md` (раздел «Код и эксплуатация») + владелец `docs/VECTORS.md | @manager`; docs/COORDINATION.md — правило брони номеров миграций в «Как пользоваться»; .opencode/CONTEXT.md — сводка про 7 векторов + открытая развязка «Поселения»↔«Новые механики»; STATUS.md §3 — п.0 вектор «Игрок» (петля игрока) как приоритетный вертикальный срез, §4 «Стратегия скоупа» закрыта. Не внесено (условные дельты, активны при старте вектора F): 99_roadmap §99.2 — строки 99.2.7/99.2.8/99.2.9 «в разработке (вектор F)», §99.1 — пометка петли как вектора.
