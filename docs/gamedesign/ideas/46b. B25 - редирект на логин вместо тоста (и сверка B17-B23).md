# 46b. B25 — редирект на логин вместо тоста (и сверка закрытых B17/B18/B20/B23)

**Создано:** 2026-09-16
**Статус:** Готово (2026-09-16). Гейт создателя: «ок». Вердикт @tester: ПРОЙДЕНО
с оговоркой → оговорка закрыта добивкой (takeContract на assignments.html),
повторный `node --check` зелёный.
**Приоритет:** баг (🟡)

---

## Сырое пожелание (создатель, 2026-09-16)

> «25 проверь, если есть — надо закрыть»

(сверка полного списка багов из STATUS.md §2: 16 — не чиним, 17 — вроде закрыт,
18 — можно сделать, 20 — убрать (переделка генерации), 23 — закрыт, 25 — закрыть)

## Результат сверки (менеджер, факты из кода)

| Баг | Вердикт | Основание |
|-----|---------|-----------|
| B16 (декластеризация sparse) | **Открыт, НЕ чиним** (решение создателя) | пресет sparse: `clusterRadius 2500`/`clusterSpacing 5000` встык (`poisson.go:94`), кластеры сливаются; вопрос переделки генерации |
| B17 (кластеризация в полёте) | **Закрыт в коде** | `maybeReloadClusters` + комментарии B17 (`data.js:12,34,58`, `animation.js:46`); 33a/37a подтверждают |
| B18 (дубли при повторной генерации поселений) | **Закрыт в коде** | `admin_settlements.go:79-89` — `clearSettlementsLayer()` чистит settlements/factories/goods_batches до генерации; тест `admin_settlements_test.go:45` |
| B20 (скалы → горы) | **Закрыт** | миграция `000022_rename_scaly_to_gory.sql`, конфиги/пресеты на «горы» |
| B23 (стрелка тренда населения) | **Закрыт** | CHANGELOG 2026-09-14 «закрыл B23»; `settlementTrendArrow` из живого сигнала (спека 99.2.16) |
| B25 (тост вместо редиректа) | **Открыт → закрыт этим фиксом** | остатки в assignments/contracts/index (см. ниже) |

## Диагностика B25 (факты)

`handleUnauthorized` уже использовался в `modal/index.js`, `map/flight.js:81`,
`search.js`, `data.js` (5 мест). Остались три старые страницы с инлайн-JS без
редиректа:
- `assignments.html:35-36` — нет токена → текст «❌ Вы не авторизованы» без
  редиректа; 401 от `/me` и `/api/contracts` — общая ошибка.
- `contracts.html:207-210` — редирект был, но с `alert()`; 401 от API — общая ошибка.
- `index.html:97-100` — `selectShip` без токена → текст «❌ Не авторизован».

## ТЗ (утверждено создателем, «го» 2026-09-16)

На 401/403 от API и на отсутствие токена — редирект на `/login-page`
(паттерн `handleUnauthorized`), с защитой от цикла
(`if (window.location.pathname !== '/login-page') window.location.href = '/login-page'`).
Не-401 ошибки показывают текст/alert как раньше. Go/API не трогаем, рестарт не нужен
(статика с диска, noCache).

## Реализация (2026-09-16, менеджер — быстрый путь)

1. **`web/assignments.html`:** нет токена → редирект (был текст без редиректа);
   401/403 от `/me`, `/api/contracts`, `/api/contracts/take` → редирект.
2. **`web/contracts.html`:** убран `alert` при отсутствии токена; 401/403 от
   `/api/contracts` (loadContracts), `/api/contracts/take`, `/api/contracts/complete-test`
   → редирект.
3. **`web/index.html`:** `selectShip` без токена → редирект (был текст); 401/403
   от PUT `/me/ship-icon` → редирект.

## Проверки

- `node --check` инлайн-скриптов всех трёх HTML — зелёный (два прогона: до и
  после добивки takeContract).
- Вердикт `@tester` (2026-09-16): **ПРОЙДЕНО с оговоркой** — замечание про
  `assignments.html` takeContract без 401/403-ветки (тот же эндпоинт, что в
  contracts.html) → закрыто добивкой; повторный `node --check` зелёный.
  Тестер также подтвердил: сервер отдаёт все три страницы байт-в-байт с диска,
  noCache на месте, защита от цикла во всех 9 местах редиректа, остаточных
  текстов «Не авторизован» в страницах нет.
- Go не менялся → DoD `go build/vet/test` не требуется.

## Известные нюансы (вне объёма, предсуществующее)

- `/` отдаётся `http.ServeFile` без noCache (`cmd/server/main.go:302-308`) —
  правка index.html может закэшироваться браузером (эвристика + If-Modified-Since).
- `loadUser`/`loadShipIcon` (`index.html`) без токена тихо `return` — дефолтный
  рендер без редиректа.
- `npc_agents.js:103` — тихий отказ сокета без токена (осознанно).

## Разнос по докам

Дельта для `@dispatcher` (применить в паузу):

1. **STATUS.md §2** — убрать строки **B17, B18, B20, B23, B25** из таблицы
   открытых багов (все закрыты; B17/B18/B20/B23 — в коде ранее, B25 — этим
   фиксом). Остаются открытыми: **B14**, **B16** (B16 — решение создателя
   2026-09-16 «не чиним», ждёт переделки генерации).
2. **.opencode/CONTEXT.md §Баги** — убрать строки B17, B18, B20, B25 из таблицы;
   в строку «Закрыты …» добавить: «2026-09-16: B17 (полётная подгрузка кластеров
   — сверка), B18 (чистка settlements/factories/goods_batches перед генерацией —
   сверка), B20 (скалы→горы, миграция 000022 — сверка), B25 (401/403 и нет токена
   на assignments/contracts/index → редирект на /login-page, защита от цикла)».
3. **CHANGELOG.md** — `+2026-09-16, Исправлено: B25 — 401/403 и отсутствие токена
   на assignments/contracts/index редиректят на /login-page (вместо тоста/текста,
   защита от цикла); закрыты по сверке с кодом B17, B18, B20, B23`.
4. **QA_CHECKLIST** — предложение @tester «B-AUTH-5» (фронт: 401/403 от игровых
   API → редирект на /login-page без тоста/текста) — владелец @tester, по желанию.
5. **99_roadmap.md / docs/INDEX.md / faq_counter.md** — без изменений.