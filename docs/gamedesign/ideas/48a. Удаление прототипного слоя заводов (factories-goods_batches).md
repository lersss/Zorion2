# 48a. Удаление прототипного слоя заводов (factories/goods_batches)

**Статус:** ✅ Готово (2026-09-16, коммит 98d3bf2); тестер — функционально готова; применён на живой БД (рестарт dev-сервера + проверка схемы) — за создателем
**Дата:** 2026-09-16
**Ведёт:** @manager
**Создатель запросил:** убрать прототипное наслоение заводов/товаров из `GenerateSettlements` целиком — код + таблицы + модели. GDD на заводы есть (`11_production.md`, `13_tiers.md`, `19_robots.md`), но это **будущие постройки игрока** с другой схемой (привязка к поселению, тир, содержание); прототипная таблица `factories` (planet_id, type, input_resource, output_product) под неё не подходит — не переиспользуется.

---

## 0. Решения и контекст

- **Решение создателя 2026-09-13** (STATUS.md §3 п.2): «Заводы будут создаваться отдельно — удалить старую логику генерации заводов из `GenerateSettlements` (прототипное наслоение)».
- **Решение создателя 2026-09-16 (эта идея):** убрать весь слой **включая таблицы** `factories`/`goods_batches` (DROP-миграция). GDD оставляем: «Всё что в ГДД оставляй, потом актуализируем».
- **Правило менеджера 2026-09-16 (коммит 276707e):** при удалении легаси — сначала анализ потребителей, не двигаться пока не разобрано. Анализ выполнен ниже (§1).

## 1. Анализ потребителей (до удаления)

| Потребитель | Что делает | Судьба |
|---|---|---|
| `generator/settlement/generator.go:82-95,117-126` — блоки «2. Заводы»/«3. Товары», `copyInRows` в factories/goods_batches | пишет в таблицы | **удалить** |
| `generator/settlement/generator.go` — хелперы `buildFactories`, `buildGoods`, `buildGoodsRow`, `extractResources` (167–…, 251–…) | строят строки заводов/товаров | **удалить** (осиротеют от правки) |
| `repository/economy_repository.go:198-207` — `CreateFactory`, `CreateGoodsBatch` | INSERT в таблицы | **удалить** — уже мёртвые (0 вызовов) |
| `models/economy.go:34-57` — `Factory`, `GoodsBatch` | модели | **удалить** — используются только мёртвыми методами репо |
| `handlers/admin_settlements.go:142-156` — `clearSettlementsLayer` | DELETE из settlements+factories+goods_batches | **оставить только `settlements`** |
| `handlers/admin_universe.go:52` — `truncateTables` | TRUNCATE списком | **убрать** `factories, goods_batches` |
| `handlers/admin_settlements_test.go:45` | sqlmock ожидает 3 таблицы при очистке | **обновить** (1 таблица) |
| `handlers/admin_hypothesis_test.go:51-52` | комментарий «без заводов и товаров» | обновить комментарий (уже не «неожиданно», а просто нет таблиц) |
| `handlers/admin_universe_test.go` | проверяет согласованность truncateTables с FK | **проверить** — после удаления таблиц список должен остаться согласованным (тест сам это докажет) |
| Миграция `000018` | CREATE TABLE factories/goods_batches | не трогать (историческая); удаление — новой миграцией |
| **Читатели данных** (SELECT/JOIN из factories/goods_batches) | — | **нет ни одного** (проверено grep'ом) |
| Фронтенд / API-хендлеры (factory/GoodsBatch) | — | **нет ни одного** (проверено) |

## 2. ТЗ

1. `internal/generator/settlement/generator.go`: убрать блоки заводов/товаров из `GenerateSettlements` + хелперы `buildFactories`/`buildGoods`/`buildGoodsRow`/`extractResources`. Поселения остаются как есть.
2. `internal/repository/economy_repository.go`: удалить `CreateFactory`/`CreateGoodsBatch` (и осиротевшие импорты).
3. `internal/models/economy.go`: удалить `Factory`/`GoodsBatch`.
4. Миграция **`000034_drop_factories_goods_batches.sql`** (и .down): `DROP TABLE IF EXISTS factories; DROP TABLE IF EXISTS goods_batches;` — по образцу `000016_drop_resource_tables.sql`.
5. `admin_settlements.go`: `clearSettlementsLayer` → только `settlements`; поправить комментарий.
6. `admin_universe.go`: убрать `factories, goods_batches` из `truncateTables`.
7. Тесты: обновить `admin_settlements_test.go` (1 таблица в очистке), комментарий в `admin_hypothesis_test.go`; прогнать все тесты.
8. DoD: `go build ./...`, `go vet ./...`, `go test ./...`.

## 3. Приоритет / маршрут

- Приоритет: средний (расчистка под будущие заводы, из плана вектора B R1, закрывает решение 2026-09-13).
- Маршрут: разработка (дизайн не нужен — решение создателя уже есть, чистое удаление). Миграция + тесты → не «быстрый путь», полный процесс с гейтами.

## 4. Разнос по докам (дельты для @dispatcher)

> Применяет @dispatcher в паузу. Активны после сдачи фичи.

- `STATUS.md` §3 п.2: вычеркнуть «Удалить старую логику генерации заводов» из остатков прототипа поселения (закрыто идеей 48a).
- `.opencode/CONTEXT.md`: +строка: прототипный слой заводов (factories/goods_batches) удалён (48a, 2026-09-16); будущие заводы — постройки по GDD (11_production.md), создаются отдельно.
- `CHANGELOG.md`: +2026-09-16, `Удалено: прототипный слой заводов (factories/goods_batches) — генерация, модели, мёртвый репозиторий, DROP-миграция 000034; заводы по GDD — отдельная будущая фича (постройки игрока)`.