# 49b. Координаты из карточки звезды — в шапку модалки

**Создано:** 2026-09-16
**Статус:** ✅ готово (закрыто 2026-09-16, подтверждено создателем)
**Приоритет:** косметика (визуал)

---

## Суть

Пожелание создателя: «Убери координаты из правой части вкладки, добавь в шапку
модалки после светимости». Уточнение (2026-09-16): переносим **только
координаты**, светимость остаётся в карточке звезды.

## Текущее состояние (из кода)

- `web/static/js/modal/panel.js` — карточка звезды (правая панель модалки):
  строка `Координаты: (x; y)` в списке параметров, сразу после «Светимость».
- `web/static/js/modal/index.js` — шапка модалки: заголовок + строка чипов
  («тип системы», «тип объекта», модификаторы). Светимости в шапке нет —
  поэтому «после светимости» не применимо, переносим только координаты.

## Правка (мелкая, менеджер, 2026-09-16)

1. `panel.js` — убрать строку «Координаты» из карточки звезды и осиротевшие
   переменные `coordX`/`coordY` (использовались только в этой строке).
2. `index.js` — в строку чипов шапки (infoLine) добавить чип
   `координаты: (x; y)`; формат как в старой строке — 2 знака после
   запятой, «—» при отсутствии значения.
3. `state.js` + `index.js` — удалены осиротевшие поля `worldCoordX`/`worldCoordY`
   и их присваивания (после переноса никто не читает).

Итерация по тестеру (B-фикс): в первой версии falsy-проверка `coordX ? …`
трактовала 0 как отсутствие — twin-миры из «Проверки гипотез» живут ровно в
(0,0) и (600,0) и показали бы «—». Исправлено на `typeof === 'number'`:
0 → `0.00`, «—» только при отсутствии поля.

## Проверка

- `node --check` на panel.js, index.js, state.js — ок.
- `go build/vet/test ./...` — зелёные (Go не трогали).
- Тестер (2026-09-16): источник данных подтверждён — `planet_handler.go:41`
  отдаёт `coord_x`/`coord_y` (json без omitempty, поле всегда в ответе);
  осиротевших чтений `worldCoordX`/`worldCoordY` нет.

## Разнос по докам (заполняется при сдаче)

Разнос по докам: **ГОТОВО** (менеджер, коммит `5978e28` сделан менеджером
напрямую — указание создателя «закомить сам, без диспетчера»; дельты разносит
@dispatcher при следующем вызове). Внести:
- `CHANGELOG.md`: +2026-09-16, `Исправлено: координаты мира перенесены из
  карточки звезды в шапку модалки (чип «координаты: (x; y)»); 0 больше не
  показывается как «—» (twin-миры в (0,0)) — идея 49b`.
- `.opencode/CONTEXT.md`: таблица «Активная задача», строка `| 49b Координаты
  из карточки звезды — в шапку модалки | сдача | пройден | ✅ Готово
  (2026-09-16) |`.
- `STATUS.md`, `docs/INDEX.md`, `99_roadmap.md`, `faq_counter.md`, `AGENTS.md`
  — без изменений (косметика, история в CHANGELOG).
- Чек-лист QA: тестер предложил B-MAP-14 (модалка, шапка: чип координат,
  0.00 у twin-миров) — добавить при следующем прогоне тестера.