# 2026-09-19. Перенос студии товаров в игровой сервер (работа с БД напрямую)

**Создано:** 2026-09-19
**Статус:** **✅ ЗАВЕРШЕНО (2026-09-20)** — итерации A ✅ (2026-09-19), B ✅ (2026-09-20), **C ✅ ГОТОВО (2026-09-20, приёмка создателя «ок», коммиты 7fef2d1 + 4d1abb5)**. Перенос студии товаров в игровой сервер завершён полностью.
**Приоритет:** высокий (инструмент наполнения контента экономики; продолжение 99a)
**Вектор:** Новые механики (контент экономики) · Поселения (экономика)
**Автор пожелания:** создатель

**Связи:** идеи `99a` (родительская студия, ✅), `99a.2`, `99a.3`; спеки
`99a.1-goods-studio.md` (модель/API/инварианты), `99a.1-goods-studio-ui.md`,
`99a.2-goods-studio-import-top-catalog.md`, `99a.3-*`; код `cmd/goods-studio/`
(бинарь, state.json вне git), `internal/resource/` (Go-каталог ресурсов: layer 20 +
real 111), `cmd/server/` (игровой сервер, PostgreSQL); 94a (вкладка «Ресурсы»,
«substances vs resources — следующий релиз»); STATUS §3 п.2 (генератор ресурсов —
в блоке до потребителей).

---

## 0. Пожелание создателя (2026-09-19)

> «Как работает удаление и бан?» → разбор (ответил). Далее:
>
> «Я могу как-то добавить товар в категорию "ресурс"?» → разбор: ресурсы — отдельный
> каталог на сервере, read-only в студии. Далее:
>
> «Давай так: 1. ресурсы больше не read-only, 2. У ресурсов может быть разный Tier,
> 3. их так же можно добавлять, удалять, банить и тд — никаких привелегий.»
>
> Уточнения (ответы на 4 вопроса скоупа):
> 1. Переносим ВСЮ студию, можно разбить на итерации и даже сессии.
> 2. Свой URL — шапка админки мешать будет.
> 3. Работаем с базой игры напрямую, а не с JSON.
> 4. Данные удаляем, кроме T0, которые есть в игре уже (ресурсы тира 0 из
>    Go-каталога internal/resource).

## 1. Решения (зафиксированы)

1. **Ресурс — «добываемый» товар, не производимый**: у ресурса НЕ может быть
   дочек (рецептов/слотов). Ресурс всегда лист дерева.
2. **Ресурсы без привилегий**: можно добавлять, удалять, банить, разбанивать,
   переименовывать, менять категорию и тир.
3. **Перенос студии в игровой сервер** (`cmd/server`): данные — в PostgreSQL
   (таблицы каталога), UI — на собственном URL (НЕ админка, без её шапки).
4. **state.json и текущие данные**: удаляем (не переносим), кроме ресурсов T0.
5. Разбиение на итерации/сессии — см. §5.

### Решения по вердикту критика (2026-09-19, создатель)

- **К1 (сидер)**: реальные вещества (`real.go`, 111) **переносим в игровой
  контент** — сидер БД = все 131 ресурс (слой 20 + витрина 111). Витрина
  «Реальные вещества» (94a) **переделывается: смотрит в контент (БД)**, а не в
  `real.go`. Эталон «витрина ≠ контент» (2026-09-18) пересмотрен создателем.
- **С1 (источник правды)**: **БД — источник правды** для каталога (ресурсы +
  товары). Go-каталог `internal/resource` перестаёт быть источником; читатели
  (94a витрина, проверки «все накормлены»/посредственность, forage, валидатор
  студии, будущий генератор полей) переходят на БД.
- **С3 (тир ресурса)**: развилка «тир назначаемый или вычисляемый» сохраняется
  (как у товаров: оверрайд ?? вычисленный; у ресурса вычисленный = 0, т.к.
  рецепта нет). Ресурсу можно **назначить тир** — для удобства разграничения
  «редких» и обычных ресурсов (презентация/сортировка; игровой эффект не
  заявлен).

## 2. Текущее состояние (факты, 2026-09-19)

- Студия — отдельный бинарь `cmd/goods-studio` (порт 8799, конфиг
  `config/goods/studio.json`, состояние `goods_data/state.json` вне git):
  394 товара / 13 категорий / 131 ресурс. UI — `cmd/goods-studio/web/index.html`
  (1556 строк, embed в бинарь).
- Серверные хендлеры: `handlers/state.go` (~1067 строк) — CRUD товаров/категорий/
  слотов/статусов/тир-оверрайдов, ИИ `ai/`, каталоги `catalog/` (импорт
  top-каталога `top_catalog.json` и деревьев Т8 `trees_t8.json`), валидаторы
  `validate/`, экспорт `export/`, граф `graph/` (тир = 1+max детей, ресурс = 0).
- Read-only ресурсов сейчас в 4 местах: PUT/DELETE товара (403), статус
  (ресурс — только banned/unban), тир (403), переименование (403).
- Игровой сервер: PostgreSQL + миграции `migrations/` (автоприменение при старте),
  JWT-роли `admin`/`skycomposer`, админка `web/static/js/admin/`. Таблиц каталога
  товаров/ресурсов в БД НЕТ (`planet_resources` — ресурсы планет, `goods_batches` —
  партии в экономике; каталог номенклатуры — ещё не в БД).
- Go-каталог ресурсов `internal/resource`: LayerCatalog() 20 + RealCatalog() 111 —
  используется студией (импорт на чтение) и сервером (94a, витрина «Реальные
  вещества»).

## 3. Архитектурный фильтр (новая сущность — таблицы в БД)

Новые таблицы в БД игры (миграции): каталог товаров/ресурсов. Связи с impact map:
`cmd/goods-studio` → 05_economy §5.2, 11_production §11.1.1, 13_tiers §13.9.1,
94a/real.go, GLOSSARY, goods_data/*. Перенос меняет сущность (механика студии →
часть игрового сервера) — impact map дополнить при сдаче.

## 4. Вопросы к дизайну (решает @designer, выносит на гейт)

- **Таблицы**: `categories` (id, name), `goods` (id, name, category_id, kind,
  status, source, tier_override, banned_at, created_at, resource_ref?), слоты —
  отдельная таблица `slots` (good_id, pos, component_id, quantity, allow_resource)
  или JSONB-колонка в `goods` (рецепт). Учитывать проверку циклов (граф),
  нормализацию имён, `res:`-id ресурсов.
- **Сидер ресурсов**: первичное наполнение таблиц из `internal/resource`
  (одноразовая миграция/сид при первом старте; при обновлении Go-каталога —
  как обновлять?).
- **Роуты**: свой URL (например `/studio/goods` или `/goods-studio`), статика —
  в `web/` (не embed). Доступ: JWT `admin`/`skycomposer` (студия — инструмент
  наполнения контента, на проде открывать нельзя).
- **Судьба старой студии**: удаление `cmd/goods-studio` + `goods_data/` после
  переноса (отдельным шагом итерации).
- **Экспорт JSON**: нужен ли после переноса (данные уже в БД) или оставить
  как выгрузку для будущих систем?
- **ИИ-заполнение комплектующих** (`ai/`): переносится (opencode HTTP — клиент
  студии переиспользуется).
- **Импорт top-каталога / деревьев Т8**: переносится (кнопки в UI).

## 5. Предлагаемые итерации (разбиение, уточнится на гейте)

- **Итерация A — каркас на БД**: миграции (таблицы каталога: `categories`,
  `goods`, слоты; **сидер = все 131 ресурс** — слой 20 + витрина 111, из
  `internal/resource`), роуты `/studio/*` на игровом сервере (JWT admin/skycomposer,
  свой URL, без шапки админки), перенос серверной логики студии на SQL (CRUD
  товаров/категорий/слотов/статусов/тир-оверрайдов), **ресурсы — без привилегий**
  (создание/удаление/бан/тир-назначение; без рецептов), статика студии в `web/`.
- **Итерация B — UI студии**: подключение перенесённого `index.html` к новому
  API (граф/канвас, фокус-подграф, справочник с фильтрами — OR-семантика
  (используемые OR неиспользуемые OR забаненные) AND тиры, попап-деталь,
  drag&drop, «+ товар»/«+ ресурс»); **витрина «Реальные вещества» (94a) — переезд
  на чтение из БД**.
- **Итерация C — доводка**: ИИ «заполнить комплектующие», импорт top-каталога/
  деревьев Т8, экспорт (по решению гейта), удаление `cmd/goods-studio` +
  `goods_data/state.json`, перевод читателей Go-каталога (проверки 94a, forage,
  валидатор) на БД, обновление доков (ARCHITECTURE, PITFALLS, INDEX, impact map,
  GLOSSARY — «ресурс» остаётся «добываемым»; эталон «витрина ≠ контент»
  пересмотрен).

## 6. Маршрут

Идея записана → @critic (прогон по дереву связности) → гейт плана итераций →
@designer (архитектурная спека итерации A: таблицы, сидер, роуты, API; затем B/C)
→ критик по спекам → гейты по итерациям → @developer (TDD, DoD) → гейты →
@tester (вкл. e2e-смоук студии, перенос `tools/e2e/goods-studio-check.js`) →
сдача. UI-спека — @uidesigner (интерфейсы первичнее стиля, решение создателя
2026-09-19). Числа баланса затрагиваются (тиры ресурсов) — @balancetester на
итерации с тирами по запросу.

## 7. Открытые хвосты прошлой задачи (фильтры, идея 2026-09-19_студия-товаров-фильтры-пусто-пусто)

Фильтры справочника (OR-семантика) реализованы и протестированы в старой студии
(14/14). Код UI переедет в игру в итерации B — фильтры переезжают вместе с ним.
Коммит фильтров в старую студию — отдельным гейтом (зафиксировать до переноса).

## 8. Вопрос на итерацию C (из вердикта критика по спеке A, 2026-09-19)

**Правило экспорта не-approved ресурсов** (среднее критика): валидатор 3 по kind
(пропуск ресурсов) ослабляет защиту «выгрузка без битых ссылок». Решение на C:
не-approved ресурсы (banned/excluded/draft) — выгружаются по факту ссылки или
только approved? От этого зависит корректность ослабления валидатора. В A не
блокер (экспорт — C).
## 9. Итерация A — ГОТОВО (2026-09-19)

Спека: `docs/specs/_archive/перенос-студии-товаров-iterA-каркас-БД.md` (617 строк). Реализация:
миграция `000045` (categories/goods/goods_slots, name_norm Go-нормализация, JSONB props),
сидер 131 ресурс + 19 категорий (маркер goods_catalog_seed; БД = источник правды), роуты
`/studio` + `/studio/api/*` (JWT admin/skycomposer), API на SQL (CRUD, bulk, статусы,
тир-оверрайды, ресурсы без привилегий, DELETE с cleared_links), конкурентность
(pg_advisory_xact_lock + REPEATABLE READ + FOR UPDATE), чистые пакеты > internal/goodsstudio.
DoD зелёный; @reviewer — чисто (bulk-роут исправлен); @tester — 14/14 после фикса BUG-1
(кириллическая нормализация — Go). Журнал: docs/QA/checklists/2026-09-19_перенос-студии-iterA-каркас-БД.md.

## 10. Разнос по докам (итерация A): **ГОТОВО** (разнесено @dispatcher 2026-09-20)

Внесено: CHANGELOG.md — запись 2026-09-19 (iterA); STATUS.md §1 — факты
итерации A (каталог в БД, /studio, ресурсы без привилегий) покрыты заметками
B/C; docs/INDEX.md — строка идеи переноса и спеки iterA; .opencode/context.md —
решение (в составе C: перенос A/B/C завершён); 99_roadmap.md §99.2 — строка
переноса A/B/C ✅. Impact map внёс @manager (e54967d, 2026-09-20).

## 11. Промт на следующую сессию (итерация B)

Перенести UI студии товаров на новый API игрового сервера и перевести витрину «Реальные
вещества» (94a) на чтение из БД.

1. **UI студии > /studio**: `web/studio.html` (копия index.html старой студии) подключить к
   `/studio/api/*`: заменить все вызовы `/api/*` на `/studio/api/*`, авторизация JWT
   (заголовок Bearer; хранение токена — localStorage/логин на /studio, продумать с
   @uidesigner), состояние/контракт по спекам 99a.1/99a.3 и iterA (поля state: категории,
   goods с recipe[SlotView], banned, unused, warnings; тир effective/override).
2. **Фильтры справочника** (OR-семантика «(используемые OR неиспользуемые OR забаненные)
   AND тиры» — уже реализована в index.html старой студии, коммит 5f9c7c2) — переносятся
   вместе с UI, проверить на новом API.
3. **«+ ресурс»**: кнопка создания ресурса (kind=resource, категория ресурсная, без слотов);
   попап ресурса — без слотов, с тир-полем, статусами, удалением (без привилегий).
4. **Удаление**: UI-слой второго подтверждения при `cleared_links > 0` (сервер уже
   возвращает count).
5. **Витрина «Реальные вещества» (94a)**: переезд с internal/resource/real.go на чтение
   каталога из БД (goods kind=resource с props); вкладка «Ресурсы» админки —
   admin_resources.go real_summary — тоже на БД.
6. **e2e-смоук**: tools/e2e/goods-studio-check.js адаптировать под /studio (JWT, новый
   base-url), добавить шаги «+ ресурс», кириллицу, OR-фильтры.
7. Критерии приёмки: спеку iterA §11 + 99a.3-ui §8 (адаптировать под /studio).

Маршрут: @uidesigner (UI-спека /studio: авторизация, компоновка, «+ ресурс», фильтры) >
@critic > гейт > @developer (подключение, витрина 94a) > @tester (e2e-смоук /studio) >
сдача. Средний звоночек из вердикта критика iterA (правило экспорта не-approved ресурсов) —
вопрос на итерацию C (см. §8).

## 12. Сессия B (2026-09-20) — старт

Создатель дал промт §11 в работу: «Подключить UI студии (web/studio.html) к /studio/api/*
(авторизация JWT, поля контракта iterA), перенести OR-фильтры справочника, «+ ресурс»,
UI-слой второго подтверждения удаления при cleared_links > 0, витрина «Реальные вещества»
(94a) → чтение из БД, адаптировать e2e-смоук под /studio. Маршрут: @uidesigner → @critic →
гейт → @developer → @tester». Маршрут подтверждён создателем. UI-спека — @uidesigner
(интерфейсы первичнее стиля, решение 2026-09-19). Факты сессии: `web/studio.html` уже
перенесён (1556 строк, копия index.html студии), но обращается к `/api/*` старого API —
нужно подключение к `/studio/api/*` (iterA §7: префикс, kind, cleared_links, category_id
числом). Витрина 94a сейчас читает `internal/resource/real.go` (admin_resources.go
RealCatalog) — нужен переезд на БД (goods kind=resource с props). e2e-смоук
`tools/e2e/goods-studio-check.js` написан под старый base-url 8799 и `/api/*` без JWT —
адаптировать под /studio (JWT, base-url, + «+ ресурс», кириллица, OR-фильтры).

## 13. Сессия B (2026-09-20) — дизайн готов к гейту

UI-спека `docs/specs/_archive/перенос-студии-товаров-iterB-ui.md` (576 строк) — готова.
Вердикт @critic по спеке: **0 крупных, 5 средних, 3 мелких**. Средние внесены по
предложениям по умолчанию (С1 авто-обновление — клиентский дефолт `auto_refresh_ms ||
3000`, сервер не правим; С2 `good_id` → Number в PUT slots; С3 дельта на правку
старых спек «ресурсы read-only» устаревает; С4 семантика пилюли «0» после С3; С5
рассинхрон layer-подвкладки — известное ограничение B). Мелкие внесены (М1 bootstrap
— env; М2 id real зависят от порядка сида; М3 терминология GLOSSARY).
**Хвосты на сдачу:** (а) impact map — связи iterA §15 не внесены (сущности goods/
categories/goods_slots/internal/goodsstudio/web/studio.html, природа cmd/goods-studio)
— внесёт @manager при сдаче B вместе со связями §13 спеки; (б) faq_counter — 18, а
iterA §16 обещал 19 (кандидат «Промышленный атлас») — дельта A не разнесена
@dispatcher'ом.

## 14. Сессия B (2026-09-20) — разработка и тест ✅

**Разработка (@developer):** 6 файлов — `web/studio.html` (JWT-авторизация инлайн
паттерна auth.js, общий adminToken; префикс /studio/api/*; маппинг category_id/good_id
→ Number; селекты категорий по kind; «+ ресурс» (kind=resource, ресурсная категория,
без слотов); попап ресурса без привилегий (rename/категория/тир-поле/статусы/удаление);
OR-фильтры 1:1; двухступенчатое удаление с cleared_links; скрыты кнопки C
(Экспорт/Импорт/fill, мёртвый код удалён); С1 auto_refresh_ms||3000; С4 тултип пилюли
«0»), `internal/handlers/admin_resources.go` (real/real_summary/families из БД: goods
kind=resource c props ? 'family', маппинг русских осей → DTO, id строка, category code,
t_melt/t_boil K, sublimating производное, DistinctCount на БД-данных), `internal/
repository/goods_repository.go` (+RealResources), тесты (sqlmock), `tools/e2e/
goods-studio-server-check.js` (новый, JWT, 12/12). DoD: build/vet зелёные; полный
`go test -race ./...` имеет падения от ЧУЖИХ незакоммиченных правок параллельных
агентов (99.2.27 внутрисистемный полёт — handlers/main.go; арт-студия — families.json;
ОБА НЕ отметились в COORDINATION.md) — наши тесты зелёные (проверено точечно).

**Ревью (@reviewer): ЧИСТО** (0 замечаний к диффу; хирургичность, инварианты §0,
соответствие спеке — ок).

**Тест (@tester, журнал `docs/QA/checklists/2026-09-20_перенос-студии-товаров-iterB-ui.md`):**
- Основной прогон: 9/10 ПАСС + 4/4 особые (С1/С2/С4/кириллица).
- **BUG-1 (блокер, найден тестом):** «добавить родителя» — TypeError (addParent читал
  recipe из ответа POST /slots, а контракт iterA возвращает {id}). Фикс @developer:
  номер слота из state ДО POST (`parent.recipe.length`), PUT с Number; e2e + шаг 6b.
- Регрессия: **5/5 ПАСС** (ресурс→родитель, товар→родитель, откат при цикле-409,
  e2e 12/12, выборочные регрессии). **Блокер закрыт, РЕГРЕССИЯ ЧИСТАЯ.**

## 15. Сессия B (2026-09-20) — сдача

**Статус итерации B: ✅ ГОТОВО к приёмке создателем.** Коммит — после «ок» создателя
(код коммитит @developer своей зоны; артефакты — @manager; пуш — через @dispatcher).

### Разнос по докам: ГОТОВО (разнесено @dispatcher 2026-09-20)

Внесено: CHANGELOG.md (+2026-09-20, iterB + переходы/пилюли), STATUS.md §1
(заметка iterB), docs/INDEX.md (спека iterB + QA-журнал), .opencode/context.md
(решение 2026-09-20), 99_roadmap.md §99.2 (строка переноса, A/B ✅, C —
следующая сессия), docs/ARCHITECTURE.md §2 (строка iterB: UI /studio + витрина
из БД), docs/DB.md (props JSONB — family как признак real, витрина 94a),
docs/PITFALLS.md (витрина real читает БД, не real.go; ловушка «студия к
старому /api/*» устарела с B), docs/GLOSSARY.md §5 (кандидаты 53a: «студия
товаров», «витрина реальных веществ», оговорка М3), docs/specs/README.md
(строка iterB уже была в дереве — закоммичена вместе с чужой 99.2.27).
Impact map — внёс @manager (e54967d), не трогал. faq_counter: счётчик 0
(сессия закрытия FAQ 2026-09-20 обнулила 18→0; правило — роадмап-вопросы не в
счётчике), кандидат «Промышленный атлас» — роадмап-вопрос, дельта A не применима.
Передано @designer (зона одного писателя): правка старых спек
`99a.1-goods-studio-ui.md` §7 и `99a.3-goods-studio-focus-graph-spravochnik.md`
§11/§10.4 — инвариант «ресурсы read-only» устаревает → «ресурсы без привилегий
(С1/С3, идея §1.2); тир ресурса назначаемый».

### Хвосты на итерацию C (зафиксированы)

- ИИ «заполнить комплектующие» (fill; кнопки скрыты в B), экспорт JSON,
  удаление `cmd/goods-studio` + `goods_data/`.
- **Импорт top-каталога / деревьев Т8 — ОТМЕНЁН (решение создателя 2026-09-20):
  «Импорт каталога товаров — не надо, будем новые собирать. Там мусор сейчас»** —
  старые данные (goods_data/top_catalog.json, trees_t8.json) в игру НЕ переносятся;
  каталог наполняется новыми товарами через студию («+ товар», подгрузка списка).
- Перевод читателей Go-каталога на БД: слой 20 «Базовый слой», покрытие рас, gaps,
  chemotypes, forage, валидаторы, генератор полей (в B остаются в Go — осознанно, С5).
- Правило экспорта не-approved ресурсов (идея §8) — открытый вопрос.
- Схема substances/resources (99.2.5) — отдельный релиз.
- Вынести общий модуль авторизации (auth.js + студия) — предложение (дельта §13 спеки B).

## 16. Сессия B (2026-09-20) — доработки после приёмки (создатель, «мелкие»)

1. **Кнопка «🧰 Студия товаров» в шапке админки** (`web/admin.html`): рядом с заголовком,
   ведёт на `/studio`; общий adminToken — без повторного входа. Правка клиентская, DoD
   build/vet зелёные.
2. **Пилюли тиров справочника — всегда 0..8** (`web/studio.html`): раньше пилюли строились
   от фактических тиров каталога (в БД только ресурсы → одна пилюля «0»); теперь
   фиксированный набор 0..8 (шкала каталога, решение создателя 2026-09-19) + динамические
   тиры выше 8 (оверрайды) — пустое видно сразу (создатель: «пусть будут все пилюли, что
   нету — я увижу сам»). `spravTiers()`: base 0..8 + extra >8; `toggleAllTiers` по нему.
   Проверено e2e-смоук 12/12 + ручная проверка пилюль `["все","0","1",...,"8"]`.
3. **Кнопка «⚙️ Админка» в шапке студии** (`web/studio.html`): обратный переход на `/admin`
   (создатель: «и обратный переход в админку пусть будет»).
   Статус: правки сделаны менеджером (мелкие), e2e 12/12 PASS, пилюли 0..8 и обе кнопки
   подтверждены в браузере. Коммит — после «ок» создателя.

## 17. Итерация C (2026-09-20) — решения создателя

Ответы на 4 вопроса менеджера (старт сессии C):

1. **Экспорт JSON — НЕ НУЖЕН** («не нужен»). Вопрос критика §8 (правило экспорта
   не-approved ресурсов) **закрыт — неактуален**. Пакет `cmd/goods-studio/export/` не
   переносится, удаляется со старой студией.
2. (вопрос §8 отпал вместе с экспортом).
3. **ИИ-fill — вариант (а)**: переносится, кнопка работает; при недоступности локального
   opencode — честная ошибка «ИИ недоступен» (студия — dev-инструмент наполнения контента;
   на проде opencode нет — это нормально).
4. **Общий модуль авторизации — ВЫНОСИМ** («причеши»): общий auth-модуль вместо
   инлайн-паттерна студии + дублирования в админке (предложение дельты спеки B §13).

### Решения гейта дизайна (2026-09-20, спека iterC готова)

- **Удаление — ПОДТВЕРЖДЕНО** («всё что перенесли можно удалять, тут дочиним если что»):
  удаляем вместе со старой студией и 33 e2e-висяка на порт 8799 (`tools/e2e/qa99a3-*.js`,
  `diag-goods*.js`, `diag-subgraph-rebuild.js`, `diag-t0-drag.js`) — средний звоночек
  критика закрыт.
- **Развилка №4 (невалидная категория от ИИ)**: товар создаётся с **фолбеком на категорию
  родителя** + строка в отчёте; **полный отчёт fill показывается в попапе** (все действия
  ИИ: созданные товары, заполненные слоты, пропуски с причинами — бан/цикл/невалидная
  категория). Создатель: «Полный отчёт в попапе» — всё видно, поправки руками.
- Развилки 1–3, 5–7 (env-конфиг opencode, in-memory статус fill, чистый ApplyFill +
  write-back, переиспользование читателей + дискриминатор `props ? 'closes'`, auth
  ядро+адаптеры, объём удаления) — **приняты предложения по умолчанию** (технические,
  обоснованные).
- **Механика fill — двухфазная «предложи → подтверди в попапе»** (создатель: «Полный
  отчёт в попапе, с вариантами что сделать»): ответ ИИ → proposals (НЕ применяется) →
  попап с вариантами по пунктам (Создать/Пропустить/Изменить категорию — выбор из
  существующих, дефолт родитель при невалидной; Заполнить/Пропустить) → «Применить
  выбранное»/«Отмена»; применяется только принятое, пропущенное/дропы — в полном отчёте.
- **Критик 2-й прогон**: 0 крупных, 3 средних (С1–С3: исчезла link-цель / TOCTOU
  категории / сдвиг-заполнение слота между фазами — закрыты дропами с отчётом в
  ApplyProposals под lock), 7 мелких — внесены.
- **Гейт дизайна ПРОЙДЕН (2026-09-20, «ок»)** — итерация C в разработку.

### Скоуп итерации C (по §5/§15 + решения):

- **ИИ «заполнить комплектующие» (fill)**: перенос `cmd/goods-studio/ai/` (client/prompt/
  parse/apply) в `cmd/server` (в `internal/goodsstudio/ai`), хендлер
  `POST /studio/api/goods/{id}/fill` (асинхронно: TryStart + goroutine, UI опрашивает
  state), включение кнопки fill в `web/studio.html` (скрыта в B), конфиг opencode
  (env OPENCODE_URL/MODEL/TIMEOUT_S/MAX_RETRIES, дефолты старых значений), статус
  генерации in-memory (TryStart + отчёт в StateView), ApplyFill чистый + write-back в
  одной транзакции с advisory lock; недоступность opencode → честная ошибка «ИИ
  недоступен»; **полный отчёт fill — в попапе**.
- **Экспорт — НЕ делаем** (решение 2026-09-20).
- **Импорт top-каталога/деревьев Т8 — отменён** (решение 2026-09-20), `catalog/` удаляется
  со старой студией.
- **Удаление старой студии**: `cmd/goods-studio/` целиком + `goods_data/` (state.json,
  top_catalog.json, trees_t8.json, export.json) + `config/goods/` + старый смоук
  `tools/e2e/goods-studio-check.js` + 33 e2e-висяка на 8799 + мёртвый код model
  (LoadState/SaveState/NextCategoryID/ResourceRef).
- **Перевод читателей Go-каталога на БД**: `admin_resources.go` — «Базовый слой» (20),
  покрытие рас, gaps (LayerCatalog/LayerTemplates/CoveringResources/CheckAllFed) → чтение
  из БД (goods kind=resource c props), дискриминатор `props ? 'closes'`; снимается
  ограничение С5. Сидер (seed.go) остаётся на Go-каталоге (одноразовый, маркер
  goods_catalog_seed). forage/генератор полей — будущие потребители, не C.
- **Общий модуль авторизации**: ядро `web/static/js/auth.js` + тонкие адаптеры
  (`admin/auth.js`, `studio/auth.js`), поведение 1:1, граф импортов фронта резолвится.
- **Доки**: ARCHITECTURE, PITFALLS (студия на 8799 больше нет; ловушки ai/client.go и
  studio.json обновляются), INDEX, impact map (сущность cmd/goods-studio удаляется;
  читатели переведены; + связи e2e-скриптов и studio/auth.js), GLOSSARY (если нужно),
  99_roadmap §99.2 (C ✅; строка «Топовые товары» — легаси удалён), CHANGELOG, STATUS §1.

### Маршрут C

@designer (спека C: fill-бэкенд + конфиг opencode, перевод admin_resources на БД, удаление
старой студии, общий auth-модуль) → @critic по спеке → гейт → @developer (TDD, DoD) →
гейт → @tester (e2e-смоук /studio, журнал) → сдача (коммит после приёмки; удаление
cmd/goods-studio — отдельным коммитом в зоне developer).

## 18. Итерация C (2026-09-20) — ГОТОВО

**Приёмка создателя «ок»** (после теста). Коммиты: `7fef2d1` (код C: fill двухфазный,
admin_resources на БД, auth-модуль, конфиг env; main.go — только studio-хунки) +
`4d1abb5` (удаление: cmd/goods-studio, goods_data, config/goods, старый смоук, 33
e2e-висяка на 8799, мёртвый код model; −7120/+3). DoD зелёный; @reviewer — 1 среднее
(e2e шаг 8) исправлено; @tester — 9/9 PASS + e2e 14/14 (журнал
`docs/QA/checklists/2026-09-20_перенос-студии-товаров-iterC-доводка.md`). Impact map
обновлён @manager (cmd/goods-studio/goods_data/* удалены; + internal/goodsstudio/ai,
web/static/js/auth.js; goods/internal.goodsstudio/web.studio.html актуализированы).
Пометки в старые спеки 99a.1/2/3 внесены @designer. COORDINATION — метки студии сняты.

### Разнос по докам: **ГОТОВО** (разнесено @dispatcher 2026-09-20)

Внесено: CHANGELOG.md — запись 2026-09-20 (iterC, сверху); STATUS.md §1 —
заметка про итерацию C + завершение переноса (A/B/C ✅); docs/INDEX.md —
строка идеи переноса (✅ A/B/C), строки спек iterA/iterC и QA-журнала iterC,
строка 99a актуализирована; 99_roadmap.md §99.2 — строки переноса (A/B/C ✅),
99a (перенесено в /studio, cmd/goods-studio удалён), «Топовые товары» (легаси
удалён); .opencode/context.md — решение C + указатель студии актуализирован;
docs/ARCHITECTURE.md §2 — строка «Студия товаров (99a.1)» переписана (студия —
часть cmd/server); docs/PITFALLS.md — opencode-ловушки (env OPENCODE_*,
internal/goodsstudio/ai), embed-ловушка устарела; docs/DB.md — дискриминаторы
props (family/closes), сид 131 = первичный источник. Не трогал: faq_counter.md
(обнулён 2026-09-20, роадмап-вопросы не в счётчике), docs/specs/README.md
(манифест iterC внесён @designer/@manager), старые спеки 99a.1/2/3 (пометки
@designer), docs/QA_CHECKLIST.md и impact map (внёс @manager), docs/GLOSSARY.md
(для C новых терминов нет).

### Хвосты после C (не входили в итерацию)

- Схема substances/resources (99.2.5) — отдельный релиз.
- Генератор ресурсов — в блоке до потребителей (STATUS §3 п.2); forage — будущий потребитель.
- 99a.1 §7.3 «категория составляющей — строго из реестра» — историческая формулировка; актуально: выбор в попапе (пометка внесена).
