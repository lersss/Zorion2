# 24a. B24 — «поселений на планету» в Проверке гипотез (закрыт фактом)

**Создано:** 2026-09-15
**Статус:** Закрыто (2026-09-15) — баг уже исправлен в коде, правок не
требуется; снята только устаревшая строка B24 из STATUS.md §2.
**Приоритет:** баг, срочный (🔴) → снят

---

## Сырое пожелание (создатель, 2026-09-15)

> «Срочный баг: в „Проверке гипотез" нельзя создать больше одного поселения
> на планету — runHypothesisJob делает ровно один INSERT в settlements. Нужна
> настройка „поселений на планету"».

## Факт из кода (менеджер, диагностика)

Настройка **уже реализована полностью** — строка B24 в STATUS.md устарела
(коммит `32caae9`, «параллельная работа…»):

- **Бэкенд** `internal/handlers/admin_hypothesis.go` (строки 153–176):
  читает `group.Settlement.SettlementsPerPlanet`, цикл
  `for n := 0; n < perPlanet; n++` создаёт N поселений на заселённую планету.
- **Валидация** `internal/generator/planet/twin.go` (строки 105–107):
  `settlements_per_planet` не может быть отрицательным.
- **Фронт** `web/static/js/admin/hypothesis.js`: поле «Поселений на планету»
  (`hypSPP_${i}`, строки 59–63), отправка `settlements_per_planet` в
  `buildTwinSpec` (строка 492).
- **Тест** `internal/handlers/admin_hypothesis_test.go:42` —
  `SettlementsPerPlanet: 2` покрыт.

Рабочее дерево чистое (`git status` пуст), код на месте.

## Маршрут

Код не трогали — правок не требовалось. Быстрый путь не нужен (нечего
разрабатывать и тестировать: фича уже в коде и покрыта тестом).

## Сдача (2026-09-15)

Статус идеи — Закрыто. Осталось только убрать устаревшую строку B24 из
STATUS.md §2 (дельта ниже) — вносит `@dispatcher`.

## Разнос по докам

Разнос по докам: **ГОТОВО** (разнесено @dispatcher 2026-09-16). Внесено: STATUS.md §2 — строка B24 убрана (закрыт фактом, реализован в коммите `32caae9`). CHANGELOG — без изменений (по дельте: баг закрыт фактом, фича не вводилась).