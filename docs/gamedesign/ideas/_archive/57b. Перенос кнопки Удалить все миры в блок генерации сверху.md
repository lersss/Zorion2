# 57b. Перенос кнопки «Удалить все миры» в блок генерации (сверху)

**Создано:** 2026-09-16
**Статус:** ✅ Готово (сдано 2026-09-16). Пройдено: правка (менеджер, «Мелкие правки») → тест (@tester, ОК) → гейт создателя → коммит `5d4512b` (вместе с UI 57a, один файл admin.html).
**Вектор:** Визуал / UX (админка)
**Приоритет:** низкий (мелочь)

## Пожелание создателя

Перенести кнопку «Удалить все миры» в блок генерации вселенной (сверху), чтобы
не листать до неё каждый раз. Сейчас она в отдельной карточке «🗑️ Очистка
вселенной» внизу подвкладки «Звёзды».

## Текущее состояние

- `web/admin.html` строки 268–273: карточка «🗑️ Очистка вселенной» с кнопкой
  `onclick="clearUniverse()"` и `#clearResult` — внизу подвкладки `genSub-stars`.
- `web/static/js/admin/generation.js`: `clearUniverse()` (строки 670–685) — уже
  есть, использует `#clearResult`, вызывает `/admin/clear` + loadStats/loadWorlds.
  JS не меняется — переносятся оба элемента (кнопка + result) вместе.

## Правка (менеджер, «Мелкие правки»)

1. В карточку «🌌 Генерация вселенной» (`#genSub-stars`, строки 93–139) — после
   компакт-ряда с кнопками «Сгенерировать»/«Остановить» добавить строку с
   кнопкой «🗑️ Удалить все миры» (`btn danger`, `onclick="clearUniverse()"`) и
   `<div id="clearResult" class="result"></div>`.
2. Удалить старую карточку «🗑️ Очистка вселенной» (строки 269–273).
3. Комментарий строки 268 («Генерация фракций перенесена...») — оставить
   (поясняет структуру подвкладки, не относится к очистке).

## Гейты

1. Правка + DoD → тест (@tester) → один итоговый гейт создателю.

## Разнос по докам

> Заполнено на сдаче 2026-09-16. Вносит `@dispatcher` (горячие доки) — механически, по дельтам ниже.

**Дельта (горячие доки):**
- `CHANGELOG.md`: +2026-09-16, `Исправлено: кнопка «Удалить все миры» перенесена в блок генерации вселенной (сверху, идея 57b).` — **ГОТОВО** (внесено @dispatcher 2026-09-16)
- `STATUS.md` §1: без изменений (мелочь админки).
- `.opencode/CONTEXT.md`: без изменений.