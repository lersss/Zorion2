# 77a. Модель корабля, оборудование и радар — видимость игрока по радиусу радара

**Создано:** 2026-09-17
**Статус:** ✅ Готово (2026-09-18, дозакрытие под новое решение): интервью → дизайн →
баланс → разработка → ревью → тест → принято создателем. Коммиты: `ef6c2e9`
(база 2026-09-17: сущности, миграции 000040-000042, старый код «точка-огонёк»),
`33dbf82` (2026-09-18: дозакрытие — код под НОВОЕ решение «открытые звёзды»).
**Спека:** `docs/specs/77a-ship-equipment-radar.md` (переработана под новое
решение, 2026-09-17; §5.5 «Открытая информация о звёздах», §7.3 полёт, §13 И6/И11).
**Приоритет:** крупная фича (базовый блок + дозакрытие выполнены)
**Векторы:** **Игрок** (корабль игрока, карта, UI, видимость) + **Новые механики**
(оборудование как системная механика)

**Следующие сырые идеи (вне скоупа 77a):** 77b транспондер (анонимность на чужих
радарах), 77c карта региона (торговая сущность «зажигает» звёзды кластера;
привязка к будущим рынкам §11_markets.md).

---

## Пожелание (создатель, 2026-09-17)

1. Ввести сущность «**модель корабля**».
2. Добавить сущность «**оборудование**» (объекты).
3. Добавить сущность «**радар**» как тип оборудования.
4. Ограничить область видимости игрока на карте (не админ и не skycomposer)
   радиусом радара.

---

## Ядро механики (словами создателя, интервью 2026-09-17)

> «Глобально задача — чтобы игрок Не видел "дальше своего радара" (если у него
> нет купленной или ранее добытой информации (с датой актуальности)».
>
> «То есть информация, которую может игрок получить — только "то, что можно
> наблюдать со стартовой планеты игрока" в физическом смысле. Возможно область
> видимости будет изменяться/дополняться.»

**Что скрыто за радаром (если нет купленной/добытой информации):**
- какие поселения есть на планетах;
- какие недра/поверхность/состав атмосферы у планет;
- корабли дальше радиуса видения (агенты + другие игроки).

**Что видно всегда:** где находятся звёзды, их характеристики и модалка с
количеством планет — «информация, которую можно наблюдать со стартовой планеты
в физическом смысле».

---

## Аспекты (решения создателя, интервью 2026-09-17)

### Модель корабля
- **Механические характеристики** («модель корабля — механические характеристики
  пока»), НЕ визуал.
- **Скорость** — «будет зависеть от установленных двигателей и загруженности
  корабля» (замысел; реализация двигателей — вне этого скоупа).
- **Слоты под оборудование**: «количество слотов под оборудование, какое оно
  будет и за что отвечать — отдельная проработка. В этом скоупе — пока только
  радар». По ходу интервью зафиксировано: **три слота с типом «радар», «сканер»,
  «двигатель» — как в космических рейнджерах** (решение создателя).
- **Стартовая модель — одна** («одна стартовая модель»); ассортимент моделей
  прирастёт вместе с рынками.
- **Визуал корабля** (`ship_icon`/`ship_color`, 61b) — «пока остается как есть.
  Модель в будущем станет "пакетом"» (пакет = вид + механика вместе).

### Оборудование
- **Радар** — тип оборудования, ограничивает зону видимости.
- **Сканер** — второй тип оборудования («вторая вскрылась»): «сканирует планеты в
  радиусе действия радара». Работает **автоматически**; «в его радиусе добавляется
  видимость поверхности (имеет дату актуальности) и наличие поселений».
- **Двигатель** — третий тип слота (отвечает за скорость; конкретика — отдельно).
- **Стартовая комплектация**: радар + сканер («сканер и радар» в комплекте).
- **Приобретение** — локальные рынки: «они будут делиться на "ресурсы", "товары",
  "оборудование", может потом еще что-то по вкладкам». ⚠️ Рынков в коде пока нет
  (см. Поправки) — покупка оборудования завязана на будущую реализацию.

### Радар и радиус
- **Стартовый радиус 300–500 px** — «чтобы видно было 3–5 систем вокруг» (слова создателя;
  **факт по плотности боевых пресетов: в круге 400 px ~6–12 систем**, dense/sparse/compact —
  принято создателем как есть, 2026-09-17, см. спеку 77a §4.2).
  Сверка: боевые пресеты галактики `mapSize` 36–50k px (радиус круга!) при 100k
  миров, `MinDist` 150–200 → 400 px даёт ~6–12 систем, ~1% диаметра галактики.
- **Прокачанные радары**: «самые сильные будут почти покрывать сектор примерно.
  Пока так, но дальше будет видно». Мини-радар при отсутствии радара — см.
  Крайности.
- **Улучшаемый** — подразумевается прогрессией уровней (детали не проговорены).

### Видимость
- **Чужие игроки**: «Нужно добавить чужих игроков тоже. Игроки должны быть видны
  друг другу в пределах радара, и за его пределами — невидимы». ⚠️ Сейчас чужих
  игроков на карте НЕТ вообще (см. Поправки) — это добавление новой механики.
- **Агенты (NPC)** — видимы в пределах радара (сейчас видны в viewport по зуму,
  порог `nameDisplayThreshold`).
- **Админ и skycomposer видят всё — навсегда, подтверждено как исключение**
  («да, навсегда»).
- **Визуал границы видимости**: «нужно несколько вариантов показать. Добавить
  тестовую "переключалку" между видами, потом понравившийся оставить» — задача
  для @gdesigner/@developer, варианты за визуальным дизайнером.

### Полёт в невидимое
- Выбран вариант «слепой прыжок» (≈ нельзя лететь «не видя»), **но с развилкой А**:
  **полёт за радар разрешён, если есть знание (отчёт/координаты)** — «Давай пока
  А, но пока дойдем до реализации контрактов может быть что-то поменяется».
  Развилка временная, зафиксировать для контрактной сессии.

### Знание vs видение (связь с рынком отчётов)
- **Скан — личный каталог игрока** («чтобы получить полные данные о планете,
  скана недостаточно, нужно физическое присутствие и покупка свежих данных,
  потом подумаем как именно эта механика будет работать»).
- Не ломает `10_exploration.md` §10.3.3 («никакая сеть не даёт данных свежее
  присутствия») — сканер = генератор личного присутствия, а не сети; зазор между
  личным сканом и чужими отчётами остаётся и держит рынок отчётов (синергия).

### Хранение информации о планетах (на дату) и протухание
- «В задумке было, что у игрока есть информация о каждой планете на какую-то
  дату, но нужно посовещаться, как и где ее хранить, чтобы это не стало узким
  горлышком. Возможно, после истечения какого-то срока она действительно будет
  "протухать" — нужно техническое ревьюирование реализации» — **передано на
  техническое ревью** (@designer/@developer), на интервью НЕ решается.

---

## Поправки (режим 2 — сверка с фактами)

- **Сказано: «в радиусе радара видно другие корабли (агенты, другие игроки)» →
  фактически:** чужих игроков на карте нет вообще: клиент рендерит только своего
  `player` и NPC-агентов (`state.npcPositions`, `web/static/js/map/npc_agents.js`,
  спека 20a.1); `/api/entities/search` (`internal/handlers/search_handler.go`) —
  поиск звезды/планеты/спутника по имени, не кораблей. «Ограничение видимости
  игроков» = **добавление** видимости, а не ограничение существующего.
- **Сказано: «покупка на локальных рынках (вкладка Оборудование)» → фактически:**
  рынков в коде нет — только дизайн (`11_markets.md` §11.2.1, статус «не
  реализовано» в `05_economy.md` §«Не реализовано вовсе: … биржа»; в `internal/`
  нет market/exchange). Привязка к будущей системе, 77a рынки НЕ строит.
- **Уточнено по масштабу:** `MapSize` — **радиус** круга галактики
  (`internal/generator/galaxy/galaxy.go`, «радиус круга»), не диаметр; боевые
  пресеты 36–50k px (`web/static/js/admin/generation.js`), `MinDist` 150–200.
  Стартовый радар 400 px ≈ 6–12 систем (факт по плотности пресетов) ≈ ~1% диаметра.
- **Полёт:** длительность = дистанция × 0.3 сек, мин 3 (66a, 61a); полёт
  переживает рефреш (42a); `current_world_id` на игроке — рамки полёта уже есть.
- **Рынок отчётов:** спроектирован полностью (`10_exploration.md` §10.3.4 —
  предмет с датой/глубиной/точностью, не копируется, быстро устаревает;
  §10.3.5 — лаборатории/сертификаты, вторая ветка информации). Сканер 77a —
  личный слой поверх, не дублирует торгуемые отчёты.
- **Эталон И5 (`21_ships.md` §21.5):** «тех-характеристики — далёкое будущее, не
  проектируются». Решение создателя на интервью «модель корабля — механические
  характеристики» **пересматривает эталон** — зафиксировано как решение (см.
  Заметки для 53a; развилка для дизайна: как именно вводить, не отменяя 61b
  визуал).

---

## Границы (что точно НЕ входит в эту сессию)

- **Слоты под оборудование: количество, что за что отвечает** — отдельная
  проработка (зафиксированы только 3 типа слота).
- **Двигатель как механика скорости** (формулы, загруженность) — замысел
  «скорость = двигатели + загруженность», реализация вне скоупа.
- **Механика «полные данные планеты»** (физическое присутствие + покупка свежих
  данных) — «потом подумаем как именно».
- **Хранение информации о планетах на дату и протухание** — техническое ревью,
  не интервью.
- **Рынок оборудования** — только привязка к будущим локальным рынкам, не
  реализация рынков.
- **Визуал границы** — выбор варианта через «переключалку», не финальный вид.
- **Ассортимент моделей кораблей** — одна стартовая модель; прирастает с рынками.

---

## Белые пятна (не решено / не проговорено на интервью)

- Точная компоновка данных модели корабля/оборудования/радара на игроке и в БД —
  не проговорено (миграция нужна; номер бронировать в `docs/DB.md` через
  @manager при старте разработки).
- Прогрессия радара: уровни, цена, как «почти покрывает сектор» достигается —
  не проговорено.
- Что видно при **радиусе 200 px без радара** (звёзды? корабли? детали?) —
  зафиксирован только факт радиуса (см. Крайности).
- Сканер: глубина скана — «поверхность + наличие поселений» названы, но недра/
  состав атмосферы (из списка скрытого) — кем/чем вскрываются — не проговорено.
- Миникарта (51a): ограничена ли видимость и на ней — не проговорено.
- Поиск (`/api/entities/search`) и тултипы при цели за радаром — поведение не
  проговорено.
- Контракты: полёт за радар по отчёту (развилка А) — временно; пересмотр при
  реализации контрактов.
- Приоритет фичи — интервью не присвоил (крупная, уточнить на гейте).

---

## Крайности (проговорено на интервью)

- **Без радара (снял/потерял):** «игрок без радара — видит на радиус 200» —
  не слепой, есть минимальный радиус обзора.
- **Максимальный радар:** «самые сильные будут почти покрывать сектор примерно.
  Пока так, но дальше будет видно» — потолок локальный, не вся галактика;
  рынок отчётов не убивается (отмечено как желаемая защита `10.3.3`).
- **Полёт к невидимой цели:** «слепой прыжок» + развилка А (легеть можно при
  наличии знания/отчёта/координат).

---

## Развилки для @designer

- **Эталон И5 vs 77a:** в каком виде вводить тех-характеристики корабля, чтобы
  не отменить 61b-визуал (модель = рамка механики; вид пока отдельно, «пакетом»
  станет позже — решение создателя).
- **Три слота с типом «радар/сканер/двигатель»:** по одному слоту каждого типа
  или несколько? (как в Space Rangers — типовая компоновка; конкретика за
  дизайнером).
- **Сканер:** «автоматически в радиусе радара» — постоянный фон или интервальный
  прогон; какие данные даёт по глубине (поверхность+поселения названы; недра/
  атмосфера — открытый вопрос).
- **Радар: 300–500 px старт, ~сектор макс** — кривая прогрессии (линейная/
  ступенчатая), число уровней — за дизайнером.
- **Видимость чужих игроков:** что показывает радар о корабле (иконка, имя,
  статус, маршрут?) — не проговорено.
- **Визуал границы видимости:** 2–4 варианта на выбор + тестовая «переключалка»
  — ТЗ на варианты за @gdesigner.
- **Полёт по отчёту:** отчёт = разрешение на полёт за радар? Как игрок
  «нацеливается» по координатам из отчёта (кнопка в карточке отчёта?) — за
  дизайнером/контрактной сессией.

---

## Против существующего (где цепляется/конфликтует)

- **`/api/worlds/filter`** (`internal/handlers/filter_worlds_handler.go`) —
  серверная точка, где радиус радара технически применим (вырезать кластеры вне
  радиуса); кластеризация из mapcache, лимиты только anti-DoS (`maxCellCols=250`).
  Решение «как применять» — за дизайном, НЕ принято.
- **Карта клиента** (`web/static/js/map/*`, map_render.js): viewport-загрузка
  кластеров, переключалка видов — новый слой поверх.
- **NPC-агенты** (`npc_agents.js`, `npc_positions`, спека 20a.1): сейчас видимость
  по зуму (`nameDisplayThreshold` 0.5), добавится радиус; `/api/entities/search`
  по имени — вопрос поведения за радаром.
- **Чужие игроки** — новой: у игрока есть `current_world_id`, но рендера чужих
  игроков нет — добавить позиции игроков + видимость по радиусу.
- **Рынок отчётов** `10_exploration.md` §10.3 — синергия: сканер = личное
  присутствие, отчёты = торгуемое знание; «полные данные = присутствие + покупка».
- **Полёт 66a/61a/42a** — выбор цели ограничивается знанием/радаром; рамки полёта
  (0.3 сек × дистанция, мин 3, переживает рефреш) сохраняются.
- **Модалка системы** — сейчас показывает детали планет (поселения, недра,
  атмосфера) без ограничений; для `role=player` потребуется скрытие за радаром.
- **Визуал корабля 61b** (`users.ship_icon`, `ship_color`) — остаётся отдельной
  системой от модели корабля.
- **Миграция** — новая сущность/таблицы (модель корабля, оборудование, радар;
  возможно, на игрока) — номер бронировать в `docs/DB.md` через @manager.

---

## Заметки для 53a (устаревшие утверждения в легаси-доках)

- **`21_ships.md` §21.5, эталон И5:** «тех-характеристики — далёкое будущее, не
  проектируются» — устаревает: интервью 77a (2026-09-17) зафиксировало решение
  создателя «модель корабля — механические характеристики» (3 слота радар/сканер/
  двигатель, скорость от двигателей и загруженности). NB: сам эталон «детали не
  влияют на механику» — остаётся предметом подтверждения с создателем; развилка
  документарно открыта.
- **Проверка (не устарело):** «игрок ничего не видит, пока не купит информацию»
  — `10_exploration.md` §10.3 и `22_races.md` (вариант А, 2026-09-16) —
  согласуется с 77a: сканер даёт только локальное личное знание, полные данные —
  присутствие/покупка.

---

## Факты о текущем состоянии (справочно, для сессий)

- **Корабль сейчас — только визуал:** `users.ship_icon` (PNG-имя из реестра
  `internal/models/ship_sprites.go`, 21 PNG в `web/static/sprites/`, спека 61b),
  `users.ship_color` (палитра 9 + «Оригинал»). Тех-характеристик НЕТ. Источник:
  `internal/models/user.go`, `STATUS.md` §1 (61b).
- **Карта:** клиент грузит кластеры по viewport-границам через `/api/worlds/filter`
  (игровой JWT-маршрут), кластеризация в Go по снапшоту mapcache, лимиты только
  anti-DoS (`maxCellCols=250`). Ограничений видимости по ролям НЕТ — игрок видит
  всё в viewport. Источник: `internal/handlers/filter_worlds_handler.go`.
- **Роли:** `admin`/`skycomposer` — FPS-счётчик на карте
  (`web/static/js/map/map_render.js`, идея 28a); ролевая модель —
  `docs/gamedesign/20_admin_roles.md`.
- **Полёт:** длительность = дистанция × 0.3 сек, мин 3 (66a, 61a); полёт
  переживает рефреш (42a). `users.current_world_id`.
- **Информация для игрока (из 56a):** «игрок вообще ничего не видит, пока не
  купит информацию (отчёт с датой актуальности)» — рынок отчётов
  `10_exploration.md` §10.3; «теневая физика» про звёзды даётся на старте. Радар
  пересекается с этой механикой — проговорено (синергия, см. выше).
- **Поиск/агенты:** NPC-агенты видны через `npc_positions`/поиск
  `/api/entities/search`; иконки агентов, порог видимости `nameDisplayThreshold`.
- **Рынки:** `11_markets.md` §11.2 — дизайн; кода нет (вкладка «Оборудование» —
  будущее).
- **Масштаб:** `MapSize` — радиус круга (`galaxy.go`), боевые пресеты 36–50k px,
  `MinDist` 150–200 → радар 400 px ≈ 6–12 систем ≈ ~1% диаметра.

---

## Реализация (2026-09-17, по спеке 77a)

- **Миграция `000040`** (`migrations/000040_ship_equipment_radar.sql`): таблицы
  `ship_models` (starter), `equipment` (radar_1 радиус 400, scanner_1), колонки
  `users.ship_model_id`/`users.equipment`, `player_planet_knowledge`
  (PK user_id+planet_id); бутстрап + бэкфилл 70 существующих игроков
  стартовой комплектации.
- **Гибрид «звёздное поле» (§5.1):** `/api/worlds/filter` — кластеры в радиусе
  / «зажжённые» знанием — полные; за радаром — «точка-огонёк» (только
  координаты, И11). «Зажигание» звезды знанием = сервер переключает кластер
  в полный вид.
- **И11 закрыт на всех точках** (ревью + фикс): legacy-роуты `/worlds` и
  `/worlds/{id}` — фильтр видимости для player (403 вне радиуса, кроме текущего
  мира/цели полёта/зажжённых); `/api/entities/search` — имена и `world_id`
  за-радарного скрыты; `/api/npc/*` — фильтр по радиусу. admin/skycomposer
  видят всё (И7).
- **Сканер (§6.1):** ленивый прогон при открытии модалки ТОЛЬКО в радиусе
  (фикс тестера: зажжённая вне радиуса НЕ обновляет знание — свежее знание
  только из расстояния, И9 держится); вскрывает поверхность + наличие
  поселений; недра/атмосфера/детали — всегда скрыты (заглушка «купить отчёт»).
  Протухание 7 дней на чтении (`IsFresh`, юнит-тест границы).
- **Чужие игроки (§5.3):** `GET /api/players/positions` — иконка+имя+статус
  («в системе X»/«в полёте»), без маршрута; фильтр по радиусу на сервере.
  **Решение создателя (2026-09-17): admin/skycomposer видны как обычные игроки
  (без фильтра роли) — оставлено как есть.**
- **Полёт (§7.3):** `/travel` 403 «цель вне зоны видимости» для player;
  известные звёзды и admin проходят (И6/И7); идемпотентный повтор и редирект
  66a не ломаются (фильтр — только для новых целей).
- **Визуал границы (§9):** круг радиуса вокруг корабля + тестовая переключалка
  4 вариантов (туман/контур/градиент/пунктир, localStorage); точки-огоньки,
  тултип «вне зоны детальной видимости», клик не открывает модалку. Полировка
  вариантов — отдельная задача @gdesigner (§16.6).
- **DoD:** build/vet зелёные, 19 пакетов `internal/` тест зелёные; сервер
  перезапущен, `/health` 200.
- **Артефакты тестера в dev-БД (оставлено по решению создателя):** пароль
  `skycomposer` → `qa77a_admin_pass`, тестовые пользователи `qa77a_*`, записи
  знания.

## Дозакрытие (2026-09-18, коммит `33dbf82`) — код под НОВОЕ решение «открытые звёзды»

Спека переработана (2026-09-17, §5.5/§7.3/§13); код был старый. Приведено к новой спе:

- **`/api/worlds/filter`** — точка-огонёк удалена как концепция: ВСЕ кластеры
  отдаются player как админу — полный вид звезды (sid/sname/sspec/stemp/stype/
  systype/smods/координаты); радиус звёзды НЕ режет (И11 новый: открыта звезда,
  закрыта система). Удалены `applyPlayerVisibility`/`reduceAllClusters`.
- **`/travel`** — 403 «цель вне зоны видимости» убран: полёт к ЛЮБОЙ звезде,
  валидация — только существование цели (404). Топливо/дальность НЕ реализованы
  (задел, решение создателя). Удалены `visibility`/`SetVisibility`.
- **`/api/entities/search`** — за-радарная звезда отдаётся полностью (имя/спектр/
  координаты); планеты/спутники неизвестных систем — без имени звезды/координат/
  world_id (детали системы, И11).
- **Карта/клиент** — все звёзды рисуются как у админа (цвет/размер по спектру),
  тултип «имя+спектр» для любой; `drawAnonymousPoint` удалён.
- **Клик по за-радарной звезде (фикс по багу создателя, живой браузер):**
  раньше — тост-ошибка «Вне зоны видимости» (попап+ошибка); теперь — модалка
  с карточкой звезды (имя/спектр/тип/модификаторы/температура/координаты,
  §5.5) и заглушкой «Система вне зоны видимости: детали (планеты, поселения)
  недоступны — долетите или купите отчёт» (вместо списка планет). 403 больше
  НЕ разлогинивает (был баг: `handleUnauthorized` на 403 → редирект на логин).
- **Legacy-роуты `/worlds` и `/worlds/{id}`** — оставлены как есть (403/фильтр
  для player): `/worlds/{id}` отдаёт world+locations+assignments — это детали
  системы, 403 требуется И11 (вердикт ревьюера; открывать не рекомендовано —
  против И2).
- **Тесты** — `visibility_handlers_test.go` под новую И11 (все звёзды полные,
  полёт к неизвестной → 202, звезда в поиске полная + планета скрыта; удалены
  тесты «зажигания» и «полёт по знанию»).
- **Фильтры по планетам** (`has_planets`/`has_life`/…) — НЕ тронуты (решение
  создателя 2026-09-18: «информация по галактике нужна»; долг после релиза —
  см. «Разнос по докам» п.7).
- **DoD:** build/vet/test зелёные; сервер перезапущен, `/health` 200; живой
  смоук: полёт к далёкой звезде 202, модалка далёкой системы 403 (не менялось).
- **Прогон тестера** (`docs/QA/checklists/2026-09-18_77a-open-stars.md`):
  12/12 чек-лист + ре-прогон клиентского флоу — ✅, блокеров нет.
- **Фикс promise-цепочки (коммит `82d56be`):** баг создателя «Ошибка загрузки
  планет: Cannot read properties of undefined (reading 'star_type')» — `return;`
  в `.then` НЕ прерывал цепочку, второй `.then` выполнялся с `undefined`
  (двойной renderModal затирал restricted-карточку + TypeError в record).
  Фикс: guard `if (!data) return;` в начале второго `.then`
  (`modal/index.js:79`). Проверено по всем веткам (403/200/401/прочие).

---

## Открытые хвосты (зафиксировано, не блокеры фичи)

- **`/api/worlds/filter` на каждый pan делает 2 доп. SQL** (`GetByID` +
  `KnownWorldIDs`) — против «без SQL в хот-пате» (спека §11.3). Решено НЕ
  трогать: межзапросный кеш рискует устаревшей позицией радара; переделка —
  отдельная задача оптимизации.
- **Поиск: клик по планете неизвестной системы** (`world_id=""` в результате
  поиска, спека 77a §11.2) → `openSystemModal("")` → 404 → тост «Ошибка загрузки
  планет». Существовало до дозакрытия, не регрессия; UX-край новой фичи
  (за-радарная планета в поиске). Предложение тестера: при пустом `world_id` не
  открывать модалку или открывать restricted-карточку. Мелкая отдельная правка.
- **Дельты доков 07_ui.md §7.1.3 / 06_factions.md §6.7.7:** противоречат новому
  эталону (видимость игрока = радиус радара 200/800 px, не зум; открытая звезда,
  закрытая система) — обновить при следующей правке этих доков (владельцы
  GDD-доков).
- **Замечания разработчика по пути (не трогаем без «го»):** `run.ps1` битый
  (`go run cmd/server/main.go` → `undefined: bootstrapSkycomposer`; известная
  ловушка AGENTS.md §2 — запускать `go run ./cmd/server`); мусорный файл
  `0.00006` в корне репо (не наш).

---

## Разнос по докам (заявка @dispatcher) — ПЕРЕПИСАН под новое решение «открытые звёзды» (2026-09-18)

Разнос по докам: **ГОТОВО** (разнесено @dispatcher 2026-09-18). Внесено (п.1–6, 8): 99_roadmap.md §99.2 — строка 99.2.25 (✅ Реализовано, база 2026-09-17 + дозакрытие «открытые звёзды» 2026-09-18); STATUS.md §1 — строка про 77a «открытая звезда, закрытая система» + §3 блок «Отложено до 1.0» — пункт про фильтры `/api/worlds/filter` по планетам (после релиза ограничить радиусом радара или убрать); CHANGELOG.md — запись 2026-09-18 (дозакрытие 77a «открытые звёзды»); .opencode/context.md — указатель про 77a (сущности, радиус, сырые идеи 77b/77c); docs/INDEX.md — строка на спеку `77a-ship-equipment-radar.md` (+77b/77c в описании); faq_counter.md — 11 → 13 (+2: транспондер §17, звёздный атлас §17.1). Не внесено (п.7 — мелкие дельты владельцам GDD-доков, не на сдачу): `07_ui.md` §7.1.3 и `06_factions.md` §6.7.7 — видимость игрока = «открытая звезда, закрытая система» — передано @designer.

---

## ✅ Подхват следующей сессии — ВЫПОЛНЕН (2026-09-18)

Все шаги чек-листа дозакрытия пройдены: гейт дизайна (создатель «ок» + решения
по фильтрам) → разработка (код приведён к новой спеке, коммит `33dbf82`) →
тест (независимый прогон + ре-прогон клиентского флоу, ✅) → сдача (идея
обновлена, «Разнос по докам» переписан под новое решение). Код соответствует
НОВОЙ спеке «открытые звёзды». Ниже — история чек-листа для контекста.

**История (что было до дозакрытия):** 77a реализована и закоммичена (`ef6c2e9`),
но после сдачи создатель принял новое решение, которое меняет инварианты спеки.
Спека переработана дизайнером (`docs/specs/77a-ship-equipment-radar.md`, §5.5
«Открытая информация о звёздах», §7.3 полёт, §13 инварианты И6/И11), код был
старый. Разработка отложена создателем: «сначала соберём что есть».

**Текущее состояние кода (на момент до дозакрытия; после — см. «Дозакрытие (2026-09-18)»):**
- `/api/worlds/filter` — гибрид «звёздное поле»: за-радарный кластер = точка-огонёк
  (только координаты, И11 старый). Файл: `internal/handlers/filter_worlds_handler.go`
  (`applyPlayerVisibility`, `reduceAllClusters`), `internal/handlers/visibility.go`.
- `/travel` — 403 «цель вне зоны видимости» для player (И6 старый). Файл:
  `internal/handlers/travel_handlers.go:169-177`.
- `/api/entities/search` — за-радарные звёзды скрыты (И11 старый). Файл:
  `internal/handlers/search_handler.go` (`applySearchVisibility`).
- Фронт: точка-огонёк `drawAnonymousPoint`, тултип только для `cnt===1`. Файл:
  `web/static/js/map/map_render.js` (`clusterScreenRadius`, `drawAnonymousPoint`,
  `drawHoveredStarName`).

**ЧЕК-ЛИСТ ДОЗАКРЫТИЯ (по шагам, каждый — гейт; ВЫПОЛНЕН 2026-09-18):**

1. **Гейт дизайна (от создателя):** «дизайн "открытые звёзды" — ок, в разработку?»
   + вопрос: фильтры `/api/worlds/filter` по планетам (`has_planets`/`has_life`),
   которые косвенно раскрывают наличие планет у за-радарных звёзд, — НЕ трогаем
   (предложение дизайнера §16.10, моё согласие по умолчанию)? ЖДЁМ явного ответа.
   **✅ РЕШЕНО (2026-09-18, создатель):** фильтры по галактике НЕ трогаем —
   «информация по галактике нужна. Мы же видим сейчас звезды с нашей планеты,
   хоть и не были там, можем узнать их спектр и множество физической информации
   по ним, и даже мы им даем названия». Подтверждает открытую информацию о
   звезде (§5.5) и существующее поведение фильтров. Закрытым остаётся только:
   планеты/поселения/число планет вне радиуса/знания.
   **⚠️ ДОЛГ ПОСЛЕ РЕЛИЗА (2026-09-18, создатель):** «фильтры по галактике пока
   пусть будут, но потом надо будет убрать, я уже думал про это» — записать
   в TODO после релиза: фильтры `/api/worlds/filter` по планетам косвенно
   раскрывают наличие планет/жизни у за-радарных звёзд → после релиза
   ограничить радиусом радара или убрать. Дельта — в «Разнос по докам».
2. **Разработка** (@developer) — привести код к НОВОЙ спеке:
   - `filter_worlds_handler.go` — за-радарный кластер отдаёт полный «вид звезды»
     (sid/sname/sspec/temp/stype/systype/smods/координаты), НО без деталей системы
     (планеты/поселения — по-прежнему закрыты, модалка 403 вне радиуса/знания);
     точка-огонёк удаляется как концепция;
   - `travel_handlers.go` — убрать 403 «цель вне зоны видимости» (полёт к любой
     звезде; валидация — только существование цели); топливо НЕ реализовывать
     (задел);
   - `search_handler.go` — за-радарная звезда отдаётся полностью (имя/спектр/
     координаты); планеты/спутники неизвестных систем — без имени звезды/координат;
   - `map_render.js` — рисовать все звёзды как у админа (цвет/размер по спектру),
     тултип «имя + спектр» для любой звезды; убрать drawAnonymousPoint;
   - тесты: обновить `visibility_handlers_test.go` (И11 новый), travel-тесты
     (полёт к неизвестной), search-тесты;
   - DoD build/vet/test, перезапуск, /health.
   **Гейт разработки:** показать diff + вердикт, ждать «ок».
3. **Тест** (@tester, автоспавн): независимый прогон (И11 новый: детали системы
   закрыты; полёт к неизвестной; DoD; живой сценарий). **Гейт:** отчёт, «ок?».
4. **Сдача:** обновить идею 77a (статус ✅, «Реализация» под новое решение),
   коммит кода (developer своей зоны) + артефакты (менеджер), затем команда
   создателя «@dispatcher, разнеси дельты» → раздел «Разнос по докам» ПЕРЕПИСАН
   под новое решение (И11: «открытая звезда, закрытая система»; полёт к любой;
   радиус 800/200; строка 99.2.25 в roadmap переформулирована; FAQ-счётчик: +2 —
   транспондер §17 + звёздный атлас §17.1). ✅ Выполнено 2026-09-18 (коммит
   кода `33dbf82`, артефакты — менеджер).

**Решения создателя, которые уже зафиксированы (не переспрашивать):**
- Открытая информация о звезде: имя, спектральный класс, внешний вид (цвет,
  размер — «как её видит админ»), координаты. «Видно в телескоп».
- Полёт к неизвестной звезде — разрешён: «Романтично»; «дальность полёта по
  топливному баку ещё будет ограничиваться. Пусть летит, если хочет» — топливо
  = будущий задел, сейчас без ограничений.
- Детали системы (планеты, поверхности, поселения, число планет) — закрыты вне
  радиуса/знания (модалка 403). Чужие игроки за радаром — скрыты. Скан — только
  в радиусе. Всё это НЕ меняется.

**Уже закоммичено (ef6c2e9, не терять):** код 77a (старый), миграции 000040
(сущности), 000041 (стартовый мир «к людям» — фикс), 000042 (радар 400→800),
спека 77a (НОВАЯ, с §5.5), идея 77a, QA-чеклисты. Сырые идеи 77b (транспондер)
и 77c (карта региона) — untracked (не закоммичены), лежат в `docs/gamedesign/ideas/`.

---

## Ретроспектива (2026-09-18, превышение бюджета итераций этапа разработки 77a)

**Корень зависания:** статическая проверка JS-флоу (тестер без живого браузера)
не поймала, что `return;` внутри `.then` НЕ прерывает promise-цепочку —
предыдущий фикс устранил лишь видимый симптом (тост), а второй `.then`
продолжал падать на `undefined` (двойной renderModal, TypeError в record).
Каждая итерация закрывала один симптом, а не паттерн: 4 итерации фиксов
по живым багам создателя вместо одной сквозной.

**Предлагаемая дельта (в `developer.md`, применена 2026-09-18 по явному «да»
создателя):** «JS fetch-цепочки: `return;` внутри `.then` НЕ прерывает цепочку —
следующий `.then` выполнится с `undefined` (двойной рендер/TypeError). После
веток обработки ошибок (401/403) обязательно ставь guard `if (!data) return;`
в следующем `.then`. При правке обработки ответа проверяй ВСЮ цепочку промисов,
а не только изменённую ветку.»

**Сопутствующий инструмент (вне скоупа 77a, внедрён 2026-09-18 по «го»
создателя, коммит `bf8a3c4`):** браузерный смоук для @tester —
`tools/e2e/map-check.js` (playwright-core + системный Chrome/Edge, без скачивания
браузеров): регистрация → карта → клик по звезде (нет тост-ошибки/редиректа) →
restricted-карточка за-радарной звезды (starInfo, radar_radius из /me) →
скриншоты. Прогон ~30–60 с, ALL PASS 2/2 (developer) + 1/1 (tester),
QA-чеклист `docs/QA/checklists/2026-09-18_e2e-smoke-tool.md`. Устраняет «слепую
зону» статической проверки клиента — баги promise-цепочек, разлогины, тосты
ловятся до сдачи, а не живым браузером создателя.