# Флаки-тест TestBiosphereNicheDominance — недетерминированная итерация map

**Создано:** 2026-09-14
**Статус:** Бэклог (некритичный баг)
**Приоритет:** низкий (по правилу триажа: некритичный → бэклог, чиним потом)
**Найдено:** @tester при проверке 99.2.12 (прогон 2026-09-14)
**Связано с:** `internal/audit/planet/composition_modifiers.go` (функция `applyRandomJitter`, строка ~10), тест в `internal/audit/planet/`

---

## Симптом

Тест `TestBiosphereNicheDominance` падает только в полном прогоне `go test ./...`,
стабильно зелёный в изоляции (`-count=5`). К 99.2.12 отношения не имеет.

## Причина (предположение тестировщика)

Недетерминированная итерация map в `applyRandomJitter` — порядок обхода map в Go
случаен, результат джиттера зависит от порядка.

## Что нужно при починке (черновик)

1. Воспроизвести: полный прогон `go test ./...` несколько раз.
2. Сделать тест/код детерминированным (фиксированный порядок обхода map /
   стабильный источник случайности).
3. DoD: `go build`, `go vet`, `go test ./...` ×N зелёные.