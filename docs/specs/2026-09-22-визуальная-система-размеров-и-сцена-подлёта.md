# 2026-09-22 — Визуальная система размеров объектов и сцена подлёта к планете

**Статус:** 🔄 дизайн — **скелет + спека + развилки** (визуал @gdesigner). Готова к проверке создателем.
Этап 1 (лестница размеров в схеме) и этап 2 (сцена подлёта) — **наброски**, отдельные гейты.
**Дата:** 2026-09-22
**Вектор:** Визуал · Игрок
**Блок роадмапа:** `docs/gamedesign/99_roadmap.md` §99.2 — номер/строку добавляет @manager
**Миграции:** нет (чистый визуал; новые сущности БД не вводятся)

**Связи (читаются, не переписываются):**
- `99.2.27-intrasystem-flight.md` — внутрисистемная позиция, `my_position`, полёт, маркер-корабль
  (§5.7 полоса полёта, §5.8 анимация, §5.11 «я здесь»), `object_type`/`object_id`;
- `2026-09-21-компаньоны-отдельные-миры.md` — компоненты кратных становятся мирами
  (развилка **Ф1** «маркер системы vs отдельные точки» — **не решена**); визуальный размер
  обязан работать в обеих ветках;
- `2026-09-21-пояса-малых-тел-объект-системы.md` — пояса (этап 2 «показ» ещё не сделан) —
  место в схеме резервируется заранее;
- `2026-09-21-планета-форма-поверхности-блик-и-реальная-атмосфера.md` — картинка планеты **v4**,
  световой вектор `L(seed)`, режимы `stub/honest/full`, кэш `v4` — **переиспользуется без изменений**;
- `2026-09-21-высадка-прогулка-по-планете.md` — `POST /api/surface/land|leave`, пакет прогулки
  с `sky.bodies[].size_hint` (§7.1) — существующий контракт «размер тела в небе», берётся за образец;
- `99.2.20-planet-physics-cascade.md` — `planet.size` (радиус в R⊕), `GasGiantRadius*`
  (`gas_giant_physics.go`) — **источник чисел, не меняется**;
- `2026-09-21-масса-каменистых-и-ледяных-планет.md`, идея «перелив массы в гигантов/мини-нептуны» —
  класс мини-нептуна попадает в лестницу **автоматически** (она от `size`, не от типа);
- `2026-09-21-угол-корабля-в-метаданных.md` — `ShipSprite{Angle,Flip}`, спрайты 200×200;
- код: `web/static/js/modal/{layout.js,utils.js,modal_render.js,events.js,state.js,minimap.js,tabs.js,textures.js}`,
  `web/static/js/map/starfield.js`, `web/static/js/surface/{surface_render.js,surface_config.js}`,
  `internal/handlers/surface_handlers.go` (`sizeHint`), `internal/models/ship_sprites.go`;
- **не трогает:** массу и каскад (`99.2.15`/`99.2.20`), генерацию, картинку планеты v4,
  сдачу этапа 1 облака (`dd37a8c`), полётные механики 99.2.27/99.2.30, баланс.

**Эталон создателя (дословно, 2026-09-21):**
> «у нас где-то было требование, чтобы планеты помещались в экран. Давай его снимем. Плюс я хочу,
> чтобы объекты больше коррелировали в визуальном размере (понятно, что не натуральная величина),
> но сейчас газовые гиганты совсем немного больше остальных планет например, да и звёзды тоже.
> Хочу подлетать к планете на корабле — а она больше тебя в 10 раз.»

**Референса внешнего вида сцены у создателя не запрошено** — поэтому §4 даёт **4 разных
направления** (не вариации одного), выбирает создатель.

---

## 0. Резюме (одним абзацем)

Сейчас масштаб объектов в модалке системы **дважды зажат под кадр** и **дважды рассогласован**:
звезда клампится потолком `maxStarRadius = min(w,h)·0.4·0.25` (при 800×600 O/B/A/F/G рисуются
одинаково — 60 px), планета рисуется по формуле `(10 + size·0.6)`, где `size` — реальный радиус в
R⊕, что сплющивает диапазон 0.3…11.2 R⊕ до **×1.64** (гигант почти как камень), а хит-тест при этом
считает совсем другой размер (`getPlanetSize` 22/12/10/8). Корабль в схеме — маркер, экранно-
константные 16 px, то есть почти с планету (в новой версии — крошечное мировое тело, §4.3). Спека
вводит **одну лестницу размеров от реального `size`** (степенная, `R_px = 10·size^0.6`), **снимает
клампы под кадр** (кадр — окно, не рамка; видимость — камера/зум/пан/миникарта), и выводит
«планета больше корабля в 10 раз» в **отдельную сцену подлёта**, где масштаб физический (корабль —
единица, планета = `10·size^0.6` кораблей),
переиспользуются картинка планеты v4 и спрайты кораблей 200×200, новых ассетов по умолчанию не
требуется.

---

## 1. Цель и скоуп

**Цель.** (1) Дать размеры, которые **читаются**: класс звезды и класс планеты различимы с первого
взгляда, порядок величин узнаваем (это **не** натуральный масштаб). (2) Дать сцену, где подлёт к
планете на корабле читается по масштабу: планета ≈ **×10** корабля (эталон — землеподобная).

**В скоупе:**
1. Лестница размеров: звёзды (классы + экзотика), планеты (от `size`, R⊕), место для поясов,
   спутников, мини-нептунов, кораблей.
2. Политика переполнения кадра: что происходит с орбитами, звёздами, компаньонами, элементами UI;
   согласование с уже принятым «широкая пара за кадром» (идея `51a`, только миникарта).
3. Сцена подлёта: где показывается, как масштабируется, что видно, как соотносится с картинкой v4.
4. ТЗ на поля для @developer, ТЗ на сцену для @uidesigner, ассеты для @artist.

**ВНЕ скоупа:** физика/массы/каскад/генерация (числа `size` только читаются); картинка планеты v4
(переиспользуется как есть); полётные механики 99.2.27/99.2.30; пояса как механика (эта  спека
только резервирует им место в схеме); экран поверхности (используется как источник контракта неба).

---

## 2. Четыре направления внешнего вида (выбирает создатель)

Ось разницы — **где живёт физический масштаб** (одно и то же требование «×10» решается по-разному).

| № | Направление | Что такое | Цена | Оценка |
|---|---|---|---|---|
| **A** | **«Схема + кино» (два масштаба, разведены)** | Схема системы остаётся **картой**: лестница классов, без клампов, корабль — **крошечное мировое тело** (не маркер, §4.3); «×10» живёт **только** в отдельном слое-сцене подлёта (полноэкранный оверлей). Переход — из карточки планеты / по прибытии. | Два визуальных языка + переход между ними; новая сцена — новый модуль. | **Рекомендую.** Честно разделяет «карту» и «вид», не ломает 99.2.27 и zoom/миникарту, требует минимум новых сущностей |
| **B** | **«Один непрерывный масштаб»** | Модалка сама по себе: при приближении (зум) схема плавно становится физической — корабль сжимается, планета растёт до ×10. Отдельной сцены нет. | ZOOM-контроллер со сменой режима, орбиты «плывут», переделка `computeLayout` и follow-камеры; риск сломать миникарту/hit-тест. | **Отложено** (решение создателя 2026-09-22): требует спайка/теста. Дорого, рискованно; «вау» сильное, но масштаб в схеме неестественен |
| **C** | **«Плоский минимализм»** | Размеры — стилизованные значки (класс → размер значка), физики нет; картинка планеты остаётся картинкой. | Дешево, ничего не ломается. | **Не закрывает запрос:** «планета больше меня в 10 раз» не появляется |
| **D** | **«Орбитальный режим картинки»** | Не отдельный слой, а расширение существующей «Вид с орбиты» (`tabs.js`): большая картинка планеты получает передний план с кораблём, лимб/атмосферу; планета = ×10 корабля. | Минимум новых сущностей: переиспользуется место большой картинки и её кэш. Но «картинка» превращается в «сцену» — теряется чистая функция вкладки. | **Хорошая дешёвая альтернатива A**, если создателю важна скорость |

**Рекомендация:** **A** — основной путь (корабль в схеме — мировое тело, вариант **(а)** создателя
2026-09-22), **D** — как быстрый первый шаг, если сцену хочется увидеть раньше. **B — отложена до
спайка**, C — отклонена. Решение за создателем (развилка **Ф1**, §10).

---

## 3. Аудит существующего (факты по коду, 2026-09-22)

### 3.1. Звёзды — кламп убивает различие классов

- `utils.js getStarSize`: O **120**, B **105**, A **90**, F **75**, G **60**, K **48**, M **36**,
  L **30**, T **24**, Y **18** px; экзотика: ЧД/НЗ **7**, WD **11**, протозвезда **20**.
- `layout.js computeLayout`: `maxRadius = min(w,h)·0.4`; `maxStarRadius = maxRadius·0.25`;
  `finalStarRadius = min(starRadius, maxStarRadius)`.
- **Следствие:** при 800×600 (min=600 → `maxStarRadius=60`) **O/B/A/F/G — все 60 px**; при
  1280×800 (`maxStarRadius=80`) O/B/A — все 80. Различие классов пропадает на всех типовых кадрах.
- Кламп прячет **и** орбиты: `availableRadius = maxRadius − finalStarRadius·1.8` — большая звезда
  «съедает» орбитальное пространство (при O и кадре 800×600: 240−216 = 24 px на все орбиты), поэтому
  кламп и появился. Убрать его без пересмотра орбит нельзя — см. §5.3.

### 3.2. Планеты — реальный радиус сплющен, а хит-тест считает другое

- **Отрисовка** (`modal_render.js:220`): `drawRadius = (10 + (p.size||10)·0.6) · sizeMultiplier`,
  где `p.size` — **реальный радиус в R⊕** (`models/planet.go:26`).
  Практический диапазон `size`: гиганты **5.9…11.2 R⊕** (`gas_giant_physics.go:18-19`), камень —
  `computeRadius(mass,density)` ≈ **0.3…2 R⊕**.
  Тогда: size 0.3 → 10.2 px; Земля (1.0) → 10.6 px; гигант (11.2) → 16.7 px. **Отношение
  макс/мин = ×1.64** — гигант «совсем немного больше» именно отсюда (создатель прав).
- **Хит-тест** (`events.js:24` → `layout.js getPlanetSize`): 22 / 20? — по факту **22** газовый,
  **12** землеподобная/океан, **10** пустынная/ледяная/вулканическая, **8** прочие.
  **Тот же объект рисуется одним радиусом, а кликается другим** (Земля: рисуется 10.6, кликается 12;
  гигант: рисуется 16.7, кликается 22) — существующее рассогласование, чинится унификацией (§5.2).
- `sizeMultiplier` **0.85** (9–12 планет) / **0.7** (>12) дополнительно ужимает **тела**, нарушая
  соотношения (это баг лестницы, а не решение плотности).

### 3.3. Кадр и зум — правило «влезать» сидит в четырёх местах

1. `layout.js:43-45` — `maxRadius`/`maxStarRadius`/`finalStarRadius` (кламп размеров под кадр).
2. `layout.js:59-62` — `availableRadius`/`step` (орбиты вписаны в кадр).
3. `events.js:132-138` — `minZoom` (пол 0.02, динамический от дальних звёзд) — не даёт уйти дальше.
4. `modal_render.js:203` — `MIN_STAR_PX=4` (экранный пол звезды — **это хорошо**, оставляем).

Правило в GDD **не записано** — это правило кода (подтверждено менеджером).

### 3.4. Корабль — маркер, а не тело

- В схеме корабль — **экранно-константные 16 px** (`modal_render.js:416,624,719`: `16/zoom`),
  чужие — 0.75·16 = 12 px. То есть при Земле (диаметр ~21 px) корабль **почти с планету**.
- **Сцена, где корабль рядом с планетой в честном масштабе, отсутствует.** Корабль рисуется:
  маркером «я здесь»/в полёте — в схеме модалки; спрайтом на карте — в межзвёздном полёте
  (`map_render.js:915`, `mapCfg.shipSize·scale`). Планет на карте нет.
- Спрайты кораблей: 200×200 PNG, нос вправо, `ShipSprite{Angle,Flip}` (`ship_sprites.go`).

### 3.5. Существующий контракт «размер тела в небе» (образец для подражания и та же болезнь)

- Пакет прогулки отдаёт `sky.bodies[].size_hint` (`surface_handlers.go`), клиент рисует небесные
  тела как `vh·0.03·size_hint·2.4` (`surface_render.js:133`).
- `sizeHint(size) = clamp(size/5, 0.05, 1)`: **линейный** и **клампится** — Земля 0.2, все гиганты
  (≥5.9) → **ровно 1.0** (гиганты в небе неразличимы), мелкий камень → 0.05 (тоже неразличим).
- Это **тот же дефект**, что у звёзд: кламп на краях убивает различие. Лестница §5 закрывает его
  и здесь (одна формула на все три представления).

### 3.6. Что УЖЕ хорошо (не ломаем)

- Звёздная лестница `getStarSize` — **пропорции верные** (O/M = 3.33); лечить надо кламп, а не числа.
- Экранный пол `MIN_STAR_PX` и «мин. размер маркера» — правильный приём, расширяем его.
- Прецедент «широкая пара за кадром, только миникарта» (`51a`) — **уже принятая политика переполнения**;
  спека её **обобщает**, а не отменяет.
- Картинка планеты v4 (свет `L(seed)`, лимб, атмосфера) — готова и годится для сцены как есть.
- `planet.size` **не вырезается** для чужих/неизвестных миров (`stripPlanetDetails` его не трогает) —
  лестница работает и для чужих планет, доступа к скрытым данным не требует.

---

## 4. Единая лестница размеров

Принцип: **одна нормированная величина на объект, три представления (схема / сцена / небо),
никаких клампов, сплющивающих диапазон.**

### 4.1. Звёзды (мировые px в схеме)

Числа `utils.js getStarSize` **сохраняются по пропорциям** (O/M = 3.33), но **масштабируются ×1.5**
(решение создателя 2026-09-22: «звезда чтоб тоже большая была относительно корабля и других планет»):
с новой лестницей планет (§4.2, гигант до 42.6 px) прежние 36 px у M перестали доминировать над
гигантом (42.6 > 36) — отсюда ×1.5 (M → 54); T/Y всё равно остаются ниже гиганта, и это закрывается
**правилом-полом** ниже (Ф9 — РЕШЕНО).
**Кламп `maxStarRadius` снимается**:

| Класс | O | B | A | F | G | K | M | L | T | Y |
|---|---|---|---|---|---|---|---|---|---|---|
| R, px (было) | 120 | 105 | 90 | 75 | 60 | 48 | 36 | 30 | 24 | 18 |
| R, px (станет) | 180 | 158 | 135 | 113 | 90 | 72 | 54 | 45 | 36 | 27 |
| к M | ×3.33 | ×2.92 | ×2.50 | ×2.08 | ×1.67 | ×1.33 | ×1.00 | ×0.83 | ×0.67 | ×0.50 |

Экзотика: WD 11, ЧД/НЗ 7 (компактные + аккреционный ореол) — **без масштабирования** (компактность —
суть класса); протозвезда 20 → 30 (+ протопланетный диск).

**Правило-пол (Ф9 — РЕШЕНО создателем 2026-09-22):**
`R_звезды = max(R_класса, 1.15 · R_самой_крупной_планеты_системы)` — звезда всегда **заметно**
крупнее любой планеты **своей** системы (прецедент «звезда доминирует» над планетами; `1.15` — запас
«заметно крупнее», а не «на волосок»). Пол зависит от класса звезды и крупнейшей планеты системы,
но **не** от кадра, зума и числа планет. Для системы с гигантом (11.2 R⊕ = **42.6 px**, §4.2) пол
`1.15·42.6 ≈ 49`:

| Класс | O | B | A | F | G | K | M | L | T | Y |
|---|---|---|---|---|---|---|---|---|---|---|
| R, px (тихая система, пол не срабатывает) | 180 | 158 | 135 | 113 | 90 | 72 | 54 | 45 | 36 | 27 |
| R, px (система с гигантом 11.2) | 180 | 158 | 135 | 113 | 90 | 72 | 54 | **49** | **49** | **49** |

**Почему пол не ломает порядок классов (проверка):** крупнейшая **реальная** планета — гигант
11.2 R⊕ = 42.6 px, значит пол никогда не превышает `1.15·42.6 ≈ 49`. Тогда **M (54), K (72), G (90),
F (113), A (135), B (158), O (180) — все выше пола**, их размеры не меняются; **L 45 → 49, T 36 → 49,
Y 27 → 49**. Порядок высоких классов сохраняется полностью; поздние (L/T/Y) **сглаживаются** в одну
величину 49 — их различие теряется **только в системах с крупным гигантом**. В системах без гиганта пол
ещё ниже (Земля → 11.5; мини-нептун 19.3 → 22.2; гигант-минимум 29 → 33.4), и лестница L/T/Y
(45/36/27) сохраняется везде, где пол не срабатывает (макс. планета < 23.5 / 31.3 / 39.1 соответственно).
Конфликта с M нет: пол ≤ 49 < 54, то есть даже в системе с самым крупным гигантом M-звезда остаётся
выше любой T/Y/L. Сглаживание L/T/Y в системе с гигантом — **принято** (приоритет создателя: «звезда
не меньше планеты» важнее различия редких поздних классов). Если различие L/T/Y понадобится и там —
это перебалансировка всего низкого конца (M должен остаться верхней), отдельным гейтом.

**Инвариант:** `R_звезды` зависит от класса и **крупнейшей планеты своей системы** (пол выше),
**не** от кадра, зума и числа планет. `R_планеты` — только от `size` (И-В1).

### 4.2. Планеты (от реального `size` в R⊕)

`h = size^0.6` (степенная, показатель 0.6), `R_px = clamp(10·h, 5, 43)` — **мировые px схемы**.
Решение создателя 2026-09-22 (№3): вариант «ещё драматичнее» — планета **`^0.6`** (гигант/Земля ×4.3),
вместо прежнего `√size` (×3.2). Потолок поднят `32 → 43` (обоснование ниже).

| size (R⊕) | Пример | size^0.6 | R_px стало | R_px было (√size) | к Земле (стало) | к кораблю (§4.3/§6.3) |
|---|---|---|---|---|---|---|
| 0.3 | карлик/малый камень | 0.49 | **5** (пол) | 5.5 | ×0.5 | ×5.0 |
| 0.53 | Марс-класс | 0.68 | 6.8 | 7.3 | ×0.68 | ×6.8 |
| **1.0** | **Земля/Венера (эталон)** | 1.00 | **10** | 10 | **×1.00** | **×10** |
| 1.5 | суперземля | 1.28 | 12.8 | 12.2 | ×1.28 | ×12.8 |
| 3.0 | **мини-нептун** (будущий класс) | 1.93 | 19.3 | 17.3 | ×1.93 | ×19.3 |
| 5.9 | гигант (минимум) | 2.90 | 29.0 | 24.3 | ×2.90 | ×29.0 |
| 11.2 | гигант (максимум, Юпитер) | 4.26 | **42.6** | 32 (кламп) | **×4.3** | ×42.6 |

**Диапазон:** гигант/Земля = **×4.3** (было ×3.2 при `√size`, ×2.6 при `^0.4`), гигант (мин.)/малый
камень = **×5.8**, гигант (макс.)/малый камень = **×8.5**, гигант/Марс = **×6.2**. Требование «заметно
больше» выполнено с запасом, при этом порядок величин честный (не натуральные 11.2×).

**Почему потолок `32 → 43` (а не оставлен):** `10·11.2^0.6 = 42.6` — при потолке 32 максимум
прижимается ровно к √size-значению и решение создателя «ещё драматичнее» отменяется (снова ×3.2).
Потолок 43 **не срабатывает** ни на одном реальном размере (максимум гиганта 11.2 → 42.6 < 43) —
это страховка от выбросов, а не рабочий кламп; пол 5 срабатывает лишь при `size < 0.315`.

**Почему `^0.6`, а не ещё круче:** `^0.7` даёт гигант/Земля ×6.0 (гигант 54 px) — пол звезды
`1.15·54 ≈ 62` превысил бы **M (54)** и сломал бы порядок M/K/G (звезда тянулась бы вверх за планетой,
а не наоборот); `^0.6` — предел «ещё драматичнее», при котором пол (≤ 49) остаётся **ниже** M (54) и
порядок классов сохраняется (Ф9 закрыта полом звезды, §4.1).

**Почему от `size`, а не от типа:** (а) числа уже есть и корректны (каскад `99.2.20`); (б) будущий
мини-нептун и «перелив в гигантов» попадают в лестницу **автоматически**, без правки визуала;
(в) работает для чужих миров (size не скрывается). Тип используется только как **цвет**, не как размер.

### 4.3. Пояса, спутники, мини-нептуны, корабли

- **Пояс** (резерв под этап 2 поясов): в схеме — **полоса-кольцо** (`annulus`) в той же орбитальной
  шкале: внутренний/внешний радиус из `radius_au ± width_au/2`, приведённые тем же множителем, что
  орбиты; вид — 6–12 точек/штрихов по кольцу (процедурно, без ассетов). Оорт/Койпера — та же полоса.
- **Спутник:** точная точка R = `clamp(0.35·R_планеты, 2.5, 6)` px рядом с планетой (сейчас спутники
  на канвасе не рисуются — `v1`; точка нужна, чтобы у лестницы было место; помечено как опция).
- **Мини-нептун:** отдельная строка не нужна — `size≈3` даёт R≈15.5 px (между камнем и гигантом).
- **Корабли (свои/чужие/NPC):** в схеме — **крошечное мировое тело**, не маркер (вариант **(а)**
  создателя, 2026-09-22). `R_корабля = 1.0` мировой px (диаметр 2 px) → Земля (R=10) ровно **×10**
  корабля — та же лестница, что в сцене (§6.3), один язык масштаба. Экранный размер = `R·zoom`,
  пол 2 px (И-В4): при стартовом зуме (0.30–1.0) корабль почти не виден, читается на зуме (≥3) —
  дословно «совсем маленький, только на зуме было видно». Свои/чужие/NPC — один радиус тела;
  различия (зелёное кольцо «я здесь», бейдж N≥2, чужие) — **экранные подсветки** (И-В4), не размер
  тела. Навигация — существующие «Найти меня»/миникарта (И-В5).
  **Эффект-замысел для @developer (не числа):** полётные эффекты (трасса, частицы, вспышки
  старта/прибытия) сейчас экранно-константны (`16/zoom`, `55/zoom`, `110/zoom` …) — при переходе
  тела в мировую шкалу их мировые размеры нужно пересчитать так, чтобы эффекты оставались
  читаемыми, но «принадлежали» крошечному телу. Нужна смена структуры — техпроектирование @developer
  (это его зона, не визуальные числа).
- **Компоненты кратных / компаньоны:** по звёздной лестнице, каждый — своя запись класса; пол (§4.1)
  считается по крупнейшей планете системы **этого** компонента (работает в **обеих** ветках развилки
  Ф1: и «маркер системы», и «отдельные точки»).

### 4.4. Небо поверхности (единый контракт, дешёвая унификация)

`sizeHint(size)`: **было** `clamp(size/5, 0.05, 1)` → **станет** `clamp(h/4.26, 0.05, 1)`,
где `h = size^0.6` (4.26 = 11.2^0.6 — нормализатор, гигант → 1.0):

| size | 0.3 | 1.0 | 3.0 | 5.9 | 11.2 |
|---|---|---|---|---|---|
| было | 0.06 | 0.20 | 0.60 | **1.00** | **1.00** |
| станет | 0.11 | 0.23 | 0.45 | 0.68 | 1.00 |

Гиганты перестают сливаться; мелкий камень перестаёт быть невидимым. Клиент (`surface_render.js`)
менять не нужно — он уже читает `size_hint`.

---

## 5. Политика кадра: «кадр — окно, не рамка»

**Правило:** объекты и орбиты живут в **мировых** координатах на единой лестнице; кадр показывает
окно, **не** масштабирует объекты. Видимость обеспечивают **камера, зум, пан, миникарта,
«Найти меня»** — уже существующие инструменты. Прецедент `51a` (широкая пара честно за кадром)
**обобщается на всё**.

### 5.1. Инварианты политики

- **И-В1 (размер от объекта):** `R_планеты` — только от `size`; `R_звезды` — от класса и **крупнейшей
  планеты своей системы** (пол, §4.1). Ни то, ни другое не зависит от кадра, зума и числа планет
  (в мировых px). Следствие: `sizeMultiplier` **не влияет на размеры тел**.
- **И-В2 (переполнение разрешено):** орбиты, компаньоны/компоненты, пояса, внешние планеты **могут
  выходить за кадр**. Обрезка — норма (canvas обрезает, как сегодня для wide-пары).
- **И-В3 (два языка):** схема — **карта** (классы тел, мировые мини-тела), сцена — **масштаб**
  (крупный план, физика). Корабль в схеме — **крошечное мировое тело** `R=1.0` px (§4.3, лестница
  общая со сценой); корабль в сцене — единица масштаба. Экранные подсветки (кольца, бейджи) — UI,
  не размер тела.
- **И-В4 (экранные полы — можно):** звезда не меньше 4 px (`MIN_STAR_PX`, остаётся), планета не
  меньше 3 px на экране, **корабль не меньше 2 px** на экране (пол, как у звезды). Подсветки-маркеры
  (кольцо «я здесь», бейдж скопления) — **экранно-константный UI**, не тела. Это **экранные** полы,
  не клампы размеров.
- **И-В5 (нет обрезанных без навигации):** если объект вне кадра, к нему есть путь: миникарта
  (есть), «Найти меня» (есть), **указатели у краёв кадра** (новое, опция @uidesigner).
- **И-В6 (порядок и слои не меняются):** орбиты → звёзды → планеты → маркер «я здесь» → чужие →
  миникарта. Новые объекты (пояса, спутники-точки) встают слоями, не перекрывая UI.

### 5.2. Что конкретно меняется в схеме (ТЗ по полям)

| Файл : поле | Было | Станет | Почему |
|---|---|---|---|
| `layout.js : maxStarRadius` | `maxRadius·0.25`; `finalStarRadius=min(star,maxStar)` | **удаляется**; `finalStarRadius = starRadius` | И-В1, различие классов (§3.1) |
| `layout.js : availableRadius` | `maxRadius − finalStarRadius·1.8` | **`maxRadius`** (без вычитания звезды) | большая звезда съедала орбиты; клиренс уходит в формулу орбиты |
| `layout.js : getOrbitRadius` | `starR·1.8 + (i+1)·step` | **без изменений** (клиренс `starR·1.8` остаётся) | большая звезда честно отодвигает внутреннюю орбиту, возможно за кадр (И-В2) |
| `layout.js : step` | `availableRadius/(maxOrbit+1)·orbitSpacingMultiplier` | **без изменений** (кроме нового `availableRadius`) | орбиты остаются «картой системы» |
| `layout.js : sizeMultiplier` | 0.85 / 0.7 → **радиусы тел** | **не влияет на радиусы тел** (удалить из `getPlanetSize`/отрисовки; можно оставить только как мелкую поправку начального зума — на гейт) | И-В1 |
| `layout.js : getPlanetSize` | тип-таблица 22/12/10/8 | **`clamp(10·size^0.6, 5, 43)`** (единая функция `planetRadius(size)`) | хит-тест = отрисовка (§3.2) |
| `modal_render.js : drawRadius` | `(10 + size·0.6)·sizeMultiplier` | **`planetRadius(p.size)`** (та же функция) | сплющивание ×1.64 → **×4.3** |
| `modal_render.js : shipSize` | `16/zoom` (маркер всех кораблей) | **`SHIP_WORLD_R = 1.0`** мировой px (тело), экранный пол 2 px | §4.3/И-В4: кораблик — мировое мини-тело, не маркер |
| `events.js : minZoom` | пол 0.02 / динамический | **оставить** (ничего не режет) | запас на будущие широкие системы |
| `state.js : zoom` (старт) | 1 | **`z0 = clamp(0.45·min(w,h)/R_zone, 0.30, 1.0)`**, `R_zone = max(2·R_звезды, мин. орбита планеты)` (§6.3, развилка **Ф4**) | звезда большая, «интересная зона» (звезда + внутренняя орбита) в кадре |
| `surface_handlers.go : sizeHint` | `clamp(size/5,0.05,1)` | **`clamp(size^0.6/4.26, 0.05, 1)`** | §4.4, гиганты различимы |
| **новое:** `planetRadius(size)` | — | одна экспортируемая функция (JS), один источник | «один факт — одно место» |

### 5.3. Почему это не ломает орбиты и кадр (проверка числами)

Кадр 800×600, `maxRadius = 240`, `step = 240/(maxOrbit+1)`, клиренс `starR·1.8`:

| Система | Звезда (R px, §4.1) | Внутр. орбита | Внешняя орбита | В кадре? | Стартовый зум z0 → экранный R звезды |
|---|---|---|---|---|---|
| M, 3 планеты | 54 | 177 | 337 | внешняя за кадром → миникарта | z0=1.0 → **54 px** |
| G, 6 планет | 90 | 202 | 402 | внешняя за кадром | z0=1.0 → **90 px** |
| O, 3 планеты | 180 | 404 | 564 | внешняя за кадром | z0≈0.67 → **120 px** |
| **L, 3 планеты + гигант 11.2** | **49** (пол: 45→49) | 168 | 328 | внешняя за кадром | z0=1.0 → **49 px** |

**Итог:** звезда **никогда** не клампится (O=180 против M=54 — различие ×3.3 сохраняется), внешние
орбиты честно уходят за кадр (как wide-пара сейчас), навигация — зумить/миникарта. Стартовый зум
`z0` подбирается под «интересную зону» (звезда + внутренняя орбита, §6.3), а не под всю систему:
у больших звёзд `z0 < 1`, и звезда всё равно ≥120 px на экране. Кто захочет «система целиком при
открытии» — авто-fit внешней орбиты (развилка **Ф4**), он **сжимает камеру, а не звезду**.

**Пол звезды (§4.1) на орбиты не влияет** — меняет только `R_звезды`; клиренс `starR·1.8` (§5.2)
честно отодвигает внутреннюю орбиту на `1.8·49 ≈ 88` px у L с гигантом (та же логика, что у M: 97 px).
В системе с гигантом пол поднимает только поздние звёзды (L 45→49, T 36→49, Y 27→49); M (54) и выше не
затронуты (пол ≤ 49).

---

## 6. Сцена подлёта (направление A; равно применимо к D)

### 6.1. Что это

**Отдельный полноэкранный слой** поверх модалки (или отдельная страница — на выбор @uidesigner),
показывающий игрока на корабле у планеты. Схема системы при этом **не меняется** и остаётся картой.

### 6.2. Точки входа

1. **Кнопка «Подлететь»** в карточке планеты (рядом с «Лететь»/«Высадиться»).
2. **По прибытии** внутрисистемного полёта к планете — после полосы полёта переход в сцену
   (опция; развилка **Ф2**).
3. Из «Вид с орбиты» (если выбран путь D — прямо вкладкой карточки).

Выход: «Назад к системе» (возврат в модалку), «Высадиться» (существующий `POST /api/surface/land`),
«Лететь» (следующий сегмент).

### 6.3. Масштаб (главное число)

- **Единица = корабль.** `SHIP_PX = clamp(0.06·min(vw,vh), 24, 56)` px.
- **Планета:** `planetDiameter = 10 · size^0.6 · SHIP_PX` — та же лестница, что в схеме (§4.2),
  корабль — единица. У **землеподобной (size=1) планета ровно ×10 корабля** (дословный выполненный
  запрос), Марс-класс ×6.8, гигант-Юпитер ×42.6, малый камень ×5.0. В схеме `R_корабля = 1.0` px —
  соотношения совпадают (один язык масштаба).
- **Переполнение — намеренно:** при `SHIP_PX=36` (800×600) Земля = 360 px диаметром (влезает),
  гигант = 1534 px (заполняет/перекрывает кадр — это и есть «вау, она огромная»). И-В2 действует.
- **Стартовый зум схемы `z0`** (Ф4): `z0 = clamp(0.45·min(w,h)/R_zone, 0.30, 1.0)`, где `R_zone` —
  больший из `2·R_звезды` и радиуса внутренней орбиты («интересная зона» = звезда + внутренняя
  орбита, не вся система). Пример 800×600: O-звезда z0≈0.67 (звезда 120 px), G — z0=1.0 (90 px),
  M — z0=1.0 (54 px); внешние орбиты честно за кадром (И-В2).
- **Атмосфера/лимб** — из картинки v4 (режим `full` в своей системе): свечение и терминатор уже
  нарисованы генератором, сцена их только показывает.
- **Спутники/кольца:** спутники — малые диски, `R = 0.35·R_планеты`; кольца — из текстуры v4
  (уже рисуются генератором), сцена не дорисовывает.

### 6.4. Что видно (композиция v1)

1. **Фон** — звёздное поле `map/starfield.js` (переиспользуется, без новых ассетов);
   градиент неба в цвет звезды системы (`getStarColor`).
2. **Планета** — текстура v4 (`getPlanetTexture(p,'big')`, 512 px) в центре/чуть влево,
   диаметр по §6.3; **не вращается** (свет `L(seed)` вшит в текстуру — вращение сломало бы терминатор).
3. **Корабль** — спрайт игрока 200×200 (`recolorShipSprite`), в переднем плане снизу,
   носом к планете, лёгкое парение и пульс двигателя (существующие приёмы `modal_render.js`).
4. **Свет:** **звезда-диск в сцене v1 не рисуется** (направление `L(seed)` клиенту недоступно,
   а поставить светило на неосвещённую сторону — хуже). Вместо этого — свечение неба в цвет
   звезды и вшитый лимб планеты. Вывести `L(seed)` в API — развилка **Ф5**.
5. **Дымка/частицы** — те же пулы частиц, что в 99.2.27 (переиспользуются).
6. **HUD** — имя планеты, тип, класс размера, дистанция/таймер (если вход из полёта), кнопки.
   Пиксели и формулировки — **@uidesigner**.

### 6.5. Соотношение с картинкой планеты v4

- Сцена **потребитель** картинки v4, а не её замена: те же файлы кэша (`full` в своей системе,
  `honest`/`stub` в чужой), тот же ключ `(planet|mode|size|v4)`.
- Никаких новых миграций/кэш-осей: сцена не вводит нового «вида» генератора, только новую
  **компоновку** существующей текстуры.
- Если планета чужая и картинка `stub` (нейтральный силуэт) — сцена всё равно честно показывает
  масштаб (силуэт ×10 корабля), но без деталей поверхности. Это **правильно** (77a: скрытое не
  раскрывается визуалом).

---

## 7. Инварианты (не ломаем)

- **И-Н1:** генерация, каскад `99.2.20`, масса (`a8a8304`), `PlanetMeans`, пол `0.02` — **не трогаются**;
  `size` только читается.
- **И-Н2:** картинка планеты v4 и её кэш (`imageGenVersion`) — **без изменений**.
- **И-Н3:** полётные механики 99.2.27/99.2.30 (полоса полёта, `my_position`, маркер «я здесь»,
  follow-камера, миникарта, hit-тест) — работают как сейчас; меняются только радиусы тел.
- **И-Н4:** `MIN_STAR_PX`, экранно-константные маркеры, «широкая пара за кадром» (`51a`) — сохраняются.
- **И-Н5:** 77a (видимость) не нарушается: сцена ничего не раскрывает, чего нет в текстуре/API.
- **И-Н6:** порядок слоёв и отсутствие перекрытий UI — как в `modal_render.js` (сцена — отдельный слой).
- **И-Н7:** читаемость: любой объект либо виден, либо доступен через навигацию (И-В5).

---

## 8. Что нужно от других ролей

### 8.1. @uidesigner (после @gdesigner, до @developer)

1. **Сцена подлёта** (направление A/D): композиция, точки входа/выхода, кнопки, HUD, переходы
   «модалка ↔ сцена ↔ поверхность», поведение на мобильном/узком кадре.
2. **Указатели off-frame объектов** (И-В5): вид стрелок/чипов у края кадра для того, что ушло
   за кадр (звезда-компаньон, внешняя орбита, пояс).
3. **Полоса плотных систем** (если `sizeMultiplier` снимется): подсказка «зумьте/миникарта» —
   или подтвердить, что подсказка не нужна.
4. Согласовать формулировки «×10» и класса размера (не показывать игроку сырые R⊕ без нужды).

### 8.2. @artist

1. **По умолчанию ассеты НЕ нужны:** планеты — v4, корабли — 200×200 PNG, фон — процедурный starfield.
2. **На проверку (только после спайка):** годится ли существующий спрайт корабля («3/4 сверху,
   нос вправо») для сцены, где корабль летит **к** планете (вид «вдогонку»). Если нужно новое
   положение — **одна** поза/поворот, а не новый набор. Решение по спайку, не заранее.
3. Новых спрайтов планет/атмосфер/фонов **не требуется**.

### 8.3. @developer (по этапам)

1. **Этап 1:** `layout.js` (`planetRadius(size)`, убрать `maxStarRadius`/вычитание звезды из
   `availableRadius`, убрать `sizeMultiplier` из тел), `modal_render.js` (единый радиус; корабль —
   мировое тело `SHIP_WORLD_R=1.0` + пол 2 px), `events.js` (хит-тест через ту же функцию; стартовый
   зум `z0`), `surface_handlers.go` (`sizeHint`).
   Проверка: hover/клик совпадают с отрисовкой; O/M и гигант/камень различимы; кораблик крошечный.
2. **Этап 2:** модуль сцены (новый файл `web/static/js/modal/approach.js` или отдельная страница),
   переиспользование `textures.js`/`ship_sprites.js`/`starfield.js`, `SHIP_PX`, формула ×10.
3. **При выборе Ф5:** выдать `L(seed)` (азимут/высота) в ответ картинки/планеты — только чтение,
   кэш не ломается (клиентский hint, не ось кэша).
4. DoD — по `AGENTS.md` §2; для JS — `node --check`; для сцены — e2e-смоук на открытие/выход.

### 8.4. @balancetester

Не требуется: числа **визуальные**, не балансовые. Если меняется `sizeHint` — согласовать только
контракт с пакетом прогулки (не числа баланса).

---

## 9. Этапы и гейты

| Этап | Содержание | Размер | Гейт |
|---|---|---|---|
| **0 (этот документ)** | Скелет + лестница + политика кадра + развилки | — | **создатель:** выбор направления (§2) и ответы по развилкам (§10) |
| **1** | Лестница в **схеме**: снять клампы, единый `planetRadius(size)`, пол звезды (`max(класс, 1.15·крупнейшая_планета)`), корабль — мировое мини-тело, стартовый зум `z0`, хит-тест = отрисовка, `sizeHint` | малый | создатель смотрит модалку: O≠M, звезда не меньше крупнейшей планеты, гигант заметно больше камня, кораблик крошечный (виден на зуме), hover совпадает |
| **2** | **Сцена подлёта** (A или D): ×10, переиспользование v4/спрайтов, вход из карточки/по прибытии | средний | создатель смотрит сцену: «планета больше меня в 10 раз» |
| **3** | Единое небо (пояса/спутники/указатели off-frame, seamless-переход полёт→сцена) | малый | по запросу |
| **4** | **Палитра текстур и кольца** (аудит в идее; Ф11): богаче цвета биомов, кольца с полосами/тенью | средний | **очередь — после ближайшего чекпоинта текущей визуальной линии** (решение создателя 2026-09-22, №3: «берем, после чекпоинта»); затем — гейт создателя на крупной картинке |

**Правило остановки:** при разрастании — сдать скелет + этап 1, сцену (этап 2) выделить отдельным
гейтом. **Этот документ останавливается на гейте 0.**

---

## 10. Развилки для создателя (с рекомендациями)

| # | Вопрос | Варианты | Рекомендация |
|---|---|---|---|
| **Ф1** | Направление внешнего вида | A «схема+кино» / B «один масштаб» / C «плоский» / D «орбитальный режим картинки» | **A**; D — дешёвый первый шаг |
| **Ф2** | Открывать сцену автоматически по прибытии полёта? | автоматически / только по кнопке | по кнопке (v1), авто — этап 3 |
| **Ф3** | Лестница планет (степень) | `√size` / **`^0.6`** / таблица классов | **РЕШЕНО создателем 2026-09-22 (№3): `^0.6`** — `R_px = clamp(10·size^0.6, 5, 43)`, гигант/Земля ×4.3 (вариант «ещё драматичнее»). Потолок 43 (не 32) — иначе решение отменяется; прежняя цена (гигант больше T/Y) **снята полом звезды** (§4.1, Ф9 РЕШЕНО) |
| **Ф4** | Стартовый зум | `zoom=1` / **z0 по «интересной зоне»** (`0.45·min(w,h)/R_zone`, 0.30–1.0) / авто-fit всей системы | **z0 по «интересной зоне»** (§6.3): звезда + внутренняя орбита в кадре, внешние — за кадром (И-В2) |
| **Ф5** | Светило в сцене | не рисовать (вшитый лимб) / выдать `L(seed)` и поставить звезду на освещённую сторону | **не рисовать** в v1; `L(seed)` — этап 3 |
| **Ф6** | Формула «×10» | ровно ×10 эталону (Земля), остальные `10·size^0.6` / ×10 всем | **`10·size^0.6`**: дословное «×10» эталону + корреляция |
| **Ф7** | Спутники-точки и пояса-полосы в схеме | резервировать сейчас / отложить | резервировать (место в лестнице), рисовать — по поясам/этапу 3 |
| **Ф8** | Судьба `sizeMultiplier` | удалить / оставить только на начальный зум | **удалить из тел** (И-В1) |
| **Ф9** | Поздние звёзды L/T/Y | гигант (42.6 px) больше T/Y / ограничить гигант / поднять поздние / принять | **РЕШЕНО создателем 2026-09-22 (№3): пол звезды.** `R_звезды = max(R_класса, 1.15·R_крупнейшей_планеты_системы)`: с гигантом 42.6 пол ≈ **49** (L 45→49, T 36→49, Y 27→49), M (54)/K/G/O выше — порядок сохраняется (§4.1) |
| **Ф10** | Направление B «один непрерывный масштаб» | спайк / отложено | **отложено** (создатель 2026-09-22): требует отдельного теста |
| **Ф11** | Палитра текстур и кольца гигантов | правки в текущий генератор / отдельная спека | аудит — в идее (`2026-09-21_визуальные-размеры-объектов-сцена-подлёта.md`, «Аудит @gdesigner»); реализация — отдельным гейтом/спекой (@manager) |

---

## 11. Дельты доков при сдаче (через @dispatcher/@manager)

- `docs/specs/README.md` — строка этой спеки.
- `docs/GLOSSARY.md` §2 — кандидат: **«лестница размеров»** (визуальные размеры от реального `size`),
  **«сцена подлёта»**.
- `docs/PITFALLS.md` (Фронт и UI) — ловушка: «кламп размера под кадр убивает различие классов»
  (звёзды O/B/A/F/G → 60 px; `sizeHint` → все гиганты 1.0) и «хит-тест ≠ отрисовка».
- **Аудит палитры и колец** — в идее `2026-09-21_визуальные-размеры-объектов-сцена-подлёта.md`
  (раздел «Аудит @gdesigner: палитра и кольца»). При выборе вариантов создателем — отдельная спека/ТЗ
  (решает @manager); в этой спеке только развилка **Ф11**.
- `docs/specs/99.2.27-intrasystem-flight.md` §5.8/§5.11 — примечание: корабль в схеме — **крошечное
  мировое тело** `R=1.0` px (не маркер 16 px, §4.3); полётные эффекты привязываются к телу.
- `docs/specs/2026-09-21-высадка-прогулка-по-планете.md` §7.1 — `size_hint` переходит на
  степенную формулу (§4.4).
- `STATUS.md`/`CHANGELOG.md`/`.opencode/context.md` — дельты @dispatcher на сдаче.

---

## 12. Что НЕ входит (границы)

- Никакой правки генератора, масс, каскада, `PlanetMeans`, пола `0.02`.
- Никакой правки картинки планеты v4 и её кэш-версии.
- Никакой новой механики (это визуал): пояса/спутники получают **место** в схеме, но их механика —
  чужие спеки; сцена подлёта не даёт новых действий, только показывает.
- Никаких новых ассетов по умолчанию; при нужде — только через @artist и отдельным гейтом.
- Роадмап `99_roadmap.md` не трогается (@manager).

---

## 13. История итераций (для протокола)

| Итерация | Что | Итог |
|---|---|---|
| 1 | Аудит кода (layout/utils/modal_render/events/surface) + спеки 99.2.27/высадка/v4/пояса/компаньоны | найдены 4 места «влезать в кадр», сплющивание ×1.64, рассогласование хит-теста, `sizeHint` кламп |
| 2 | Скелет: 4 направления, лестница от `size`, политика «кадр — окно», сцена ×10 | **остановка на гейте создателя** |
| 3 | Ответы создателя №2: корабль — вариант **(а)** (мировое тело), планеты **√size** (+«ещё ярче»), звёзды ×1.5, стартовый зум `z0`, аудит палитры и колец | спека обновлена; **остановка на гейте создателя** |
| 4 | Ответы создателя №3: планеты **`^0.6`** (×4.3, вариант «ещё драматичнее»), потолок `32 → 43`, синхронизация §0/§4.1/§4.2/§4.4/§5.2/§6.3/§10; палитра и кольца приняты — **после чекпоинта** | спека обновлена; **остановка на гейте создателя** |
| 5 | Решение создателя: **звезда не меньше планеты** — правило-пол `R_звезды = max(R_класса, 1.15·R_крупнейшей_планеты_системы)`; синхронизация §4.1/§4.2/§4.3/§5.1/§5.3/§9/§10 (Ф9 → РЕШЕНО) | спека обновлена; **остановка на гейте создателя** |
