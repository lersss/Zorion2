# 2026-09-22 — Пояса малых тел, этап 3: UI добычи в поясе (точка входа + мини-игра)

**Статус:** 🔄 дизайн (готова к проверке создателем; ждёт гейт) — рекомендация
по компоновке HUD (§1), точка входа (§2–§3) и зависимости от развилок §10
основной спеки (§11).
**Дата:** 2026-09-22
**Роль:** @uidesigner (ТЗ для @developer, подэтап **3b** спеки)
**Основание (не переписывается):** механика —
`docs/specs/2026-09-22-пояса-малых-тел-этап-3-добыча.md` (далее **«осн. §N»**):
§0 резюме, §5 механика мини-игры, §6 вход/выход/обрыв/видимость (+ §6.1а задел
ПКМ), §7 пакет захода, §9 ТЗ на графику/HUD, §10 развилки, §14 подэтап 3b.
**Код (read-only, сверено 2026-09-22):** `web/static/js/modal/{panel.js,events.js,
state.js,modal_render.js,index.js}`, `web/surface.html`,
`web/static/js/surface/{surface_main.js,surface_ui.js,surface_net.js}`,
`web/static/css/surface.css`, `web/static/js/ui/{toast.js,sound.js}`,
`web/static/js/dashboard/cargo.js`, `web/index.html` (блок «Трюм», строки 96–109,
167–183). `web/belt.html` **ещё не существует** — новая страница.
**Термины:** `GLOSSARY.md` §1 «Пояс»; дельта осн. §15 «Заход в пояс», «Буфер
захода», «Запас пояса» (ведёт @dispatcher).
**Стиль/палитра:** только существующие константы (`surface.css`, `panel.js`
`beltRow`, `index.html`). Новых цветов/шрифтов **не вводим**; `@gdesigner` не
зовём. `AudioContext`/спрайты — переиспользуем (`ui/sound.js`, реестр корабля).
**Объём:** точка входа в модалке системы (кнопка «⛏ Добывать» + задел ПКМ),
страница `web/belt.html` + `web/static/js/belt/*` + `web/static/css/belt.css`
(сцена/HUD/экраны/сети/звук), все состояния экрана. **Графика астероидов —
не здесь** (осн. §9, `@gdesigner`/`@artist`; подэтап 3c) — в 3b плейсхолдеры.
Числа баланса, Go/API/БД — не наша зона (осн. §4–§8).
**История итераций:** 1 (2026-09-22) — первая версия: 3 направления HUD (§1),
точка входа и её состояния (§2), задел ПКМ (§3), полная компоновка HUD (§4),
экраны/оверлеи (§5), реактивность/звук (§6), формулировки (§7), пороги запаса
(§8), ТЗ (§9), инварианты (§10), зависимость от §10 основной спеки (§11),
зависимости данных (§12), impact map (§13), открытые вопросы (§14).
2 (2026-09-22) — синхронизация с новой числовой моделью осн. §8: пороги запаса
согласованы (правка не требуется), гейт знания уровня/выработанности, визуальность
«прогресса жилы/запаса астероида» (§4.3/§5.1/§8/§9.1/§12.1).
3 (2026-09-22) — дельта `@designer`: разведены **две оси** — `remaining_level`
(`полный/истощается/выработан`, меняется) и `belt_class` (`богатый/средний/бедный`,
постоянный); чипы запаса/класса (§4.3), класс в строке пояса (§2.1/§9.1), оба поля
в `BeltView` под гейтом знания (§12.1), `belt_class` в impact map (§13); О7 закрыт (§14).

---

## 0. Что уже есть (факт, чтобы говорить предметно)

| Место | Факт на 2026-09-22 |
|---|---|
| `panel.js` `beltRow(b)` (204–244) | строка пояса полноширинная (`colspan=4`), правая часть — **одна** кнопка: своя система `🚀 Лететь` (`data-belt-fly`), чужая `🚀 Лететь · через систему` (`data-belt-composite-fly`), обе — инлайн-стиль `background:#2a2a4a; color:#fde68a; padding:4px 10px; border-radius:4px; font-size:0.85rem`. Бейдж «● Вы в поясе» (`#4ade80`, `border-radius:10px`, `padding:2px 8px`) — при `myPos.status==='orbit' && object_type==='belt' && object_id===b.id`. Строка состава: «Состав: rock 60%, iron 30%, …» либо «Состав: нет данных — просканируйте систему в радиусе или долетите до пояса». |
| `panel.js` `wireBeltButtons(panel)` (250–297) | блокировка `[data-belt-fly]`: нет двигателя / межзвёздный полёт / уже в этом поясе (`orbit`+belt+id) / цель или отправление активного полёта. Disabled → `opacity:0.4`, `cursor:not-allowed`, `title`. `[data-belt-composite-fly]` — только `hasEngine`. |
| `events.js` `contextmenu` (281–297) | ПКМ: `hit==='star'`→`showStarMenu`, `hit.type==='star'`→`showCompanionMenu`, `hit.type==='planet'`→`showPlanetMenu`. **Хит-теста по кольцу пояса нет** (кольца на канвасе нет — осн. §6.1, Д-Л5). |
| `events.js` `showPlanetMenu` (634–752) | меню `#star-context-menu`, заголовок = имя объекта, `menuItem(html)` — единый стиль строки; пункты «🚀 Лететь», «🚶 Высадиться» / «Вы на поверхности». Готовый образец для меню пояса. |
| `modal_render.js` `drawMyPosition` (455–466) | `mining` **не** обрабатывается: рисуется только `orbit`/`surface`; для `object_type==='belt'` — возврат без маркера (канвас-координат нет). |
| `surface.html` + `surface_ui.js` + `surface.css` | готовый образец страницы-мини-игры: `#loading`, `#briefing`, `#death`, `#pause` (`.overlay`→`.panel`→`.panel-actions`), `#hud` (`.hud-left/.hud-center/.hud-right`, `pointer-events:none`, кнопкам `pointer-events:auto`), `#toast` (низ-центр, 3 с, `notify()`). |
| `surface_main.js` | поток: `boot()` → `showLoading(reason)` → `land()` → скрыть loading → игра; «Вызвать корабль» → `leave()` → `location.href='/map'`; `Esc` → пауза; ошибка входа — `ui.showLoading('Высадка невозможна: '+err)` + через 2.5 с `location.href='/map'`. |
| `ui/toast.js` | глобальные тосты верх-право (`notifyError/Success/Info`), звук через `playToast` (`ui_error`/`ui_success`). `ui/sound.js` играет **только после `activateSound()`** (сейчас её зовёт только карта). |
| `index.html` блок «Трюм» (96–109, 167–183) | полоса `.cargo-bar/.cargo-bar-fill` (#3b82f6) + подпись `used / total т` (`.cargo-bar-label`), строки груза, «Трюм пуст.». `dashboard/cargo.js` — источник данных `GET /api/cargo`. |
| `beltRow` источник данных | `modalState.belts` из `/api/worlds/{id}/planets`. **Полей класса/запаса в строке нет** (осн. §8.4 требует видимый класс/уровень — см. §12). |

**Пробел данных (не UI-зона):** чтобы показать **класс пояса и уровень запаса**
в строке пояса **до** входа, `BeltView` (payload `/api/worlds/{id}/planets`) должен
нести **оба** качественных поля — `belt_class` (`богатый/средний/бедный`) и
`remaining_level` (`полный/истощается/выработан`), как в пакете входа (осн. §7).
Статус этой зависимости — §12. Пока полей нет — кнопка ведёт себя как v1
(активна при позиции в поясе, отказ приходит тостом от сервера, осн. §6.2).

---

## 1. Направления компоновки HUD мини-игры (выбор за создателем)

> Все три направления используют **одну сцену** (осн. §5.1/§9.2) и одни данные
> (осн. §7). Отличаются только **где живут элементы интерфейса**.

### Направление A — «Приборная панель сверху» (рекомендую)

Полоса `#hud` сверху (как `surface.html`): слева — буфер захода + трюм + чип
уровня запаса; по центру — имя пояса и подсказка у цели; справа — кнопка
«Вернуться на корабль»; низ-лево — легенда управления (тяга/тормоз).

- **Плюсы:** визуально и кодово совпадает с уже принятой страницей-мини-игрой
  (`surface.css`: `.overlay/.panel/.hud-*`, `pointer-events`), дешевле всего
  (переиспользуемые классы), все ключевые числа на виду, HUD не перекрывает
  игровое поле у корабля (корабль визуально «в центре»).
- **Цена:** верхняя полоса «прижимается» к верхнему краю и не мешает обзору;
  новые классы — только `#cargo-bar`/чип запаса (по образцу существующих).

### Направление B — «Кокпит снизу»

Трёхсекционная панель снизу по центру: буфер + трюм в «консоли», над ней —
контекстная подсказка; справа сверху — уровень запаса; слева снизу — управление.

- **Плюсы:** «игровое» ощущение, полоса трюма ближе к глазу.
- **Цена:** нижняя треть экрана — зона, куда игрок ведёт корабль (полёт вниз =
  движение к астероидам внизу), панель перекрывает цель; верстка новая,
  отступает от образца surface.

### Направление C — «Минимальный HUD»

Постоянно — только подсказка у цели и маленький индикатор буфера; трюм, запас,
управление — по `Esc`/паузе.

- **Плюсы:** максимум погружения.
- **Цена:** «полный трюм» и «пояс выработан» (осн. §8.4, обязательно видимые)
  прячутся; игрок узнаёт о блокировке постфактум — против осн. §8.4 и §10.1.

**Рекомендация — A.** Ниже полностью спроектировано **A**; при выборе B/C
переносятся только позиции блоков (§4), данные и состояния (§5–§8) не меняются.

---

## 2. Точка входа: кнопка «⛏ Добывать» в строке пояса (`panel.js`)

### 2.1. Компоновка строки (было → станет)

Правая часть строки `beltRow` — сейчас один `flyBtn`. Станет **группа действий**
`display:flex; gap:6px` из двух кнопок: **«⛏ Добывать»** (новая, первой) и
**«🚀 Лететь»** (существующая, без изменения семантики). Стиль новой кнопки —
**тот же инлайн-стиль**, что у `flyBtn` (`background:#2a2a4a; color:#fde68a;
padding:4px 10px; border-radius:4px; font-size:0.85rem`), иконка `⛏`. Новых
цветов нет.

Бейдж «● Вы в поясе» расширяется: показывается при
`myPos.object_type==='belt' && myPos.object_id===b.id && (status==='orbit' ||
status==='mining')`; при `status==='mining'` текст — **«⛏ Добываете»** (чип того
же стиля, `#fde68a`-акцент). Причина: позиция `mining` живёт после ухода со
страницы (осн. §6.3), игрок должен видеть, что заход не потерян.

**Класс пояса в строке.** `beltRow` показывает **чип класса пояса** `belt_class`
(«Пояс: богатый/средний/бедный», стиль/цвет — §4.3) — именно он отвечает на вопрос
«стоит ли лететь» (постоянная характеристика пояса), а не уровень запаса (тот
меняется и виден в HUD при заходе). Чип — под **гейтом знания** (§4.3): без знания
пояса — «Пояс: нет данных», не «выработан».

### 2.2. Матрица состояний кнопки «⛏ Добывать»

Приоритет — сверху вниз (первое совпавшее):

| # | Условие | Кнопка | Подпись / `title` |
|---|---|---|---|
| 1 | `!modalState.hasEngine` | disabled | ⛏ Добывать · `Двигатель не установлен` (**нет**, см. ниже) |
| 2 | активный полёт (`status==='in_flight'` или `modalState.interstellarFlight`) | disabled | `Вы в полёте — дождитесь прибытия` |
| 3 | не в этом поясе (`!(status orbit/mining && belt && id)` ) | disabled | `Сначала долетите до пояса` |
| 4 | пояс выработан (`remaining_level==='выработан'` — при наличии поля, §12) | disabled | `Пояс выработан` |
| 5 | пояс без данных о запасе (старый мир, §8.4/§12) | disabled | `Нет данных о запасе пояса` |
| 6 | свободный трюм 0 (если клиент знает, §12) | disabled | `Трюм полон — освободите место` |
| 7 | `status==='mining'` (сессия уже идёт) | active | **«⛏ Продолжить добычу»** |
| 8 | иначе (в этом поясе, `orbit`) | active | «⛏ Добывать» |

**Пункт 1 — важное решение:** добыча — **не полёт**; двигатель для неё не нужен
(игрок уже в поясе). Кнопка «Добывать» **не** блокируется отсутствием двигателя
(в отличие от `🚀 Лететь`). Двигатель нужен только чтобы добраться — это уже
забота кнопки «Лететь». (Отмечено явно, чтобы разработчик не скопировал
`!hasEngine` из `wireBeltButtons`.)

Если клиент **не знает** запаса/трюма (поля нет — §12), состояния 4–6 не
применяются: кнопка активна, отказ приходит при `enter` (осн. §6.2) — тост из
§2.3. Это допустимый фолбэк v1.

### 2.3. Поведение по клику

1. Клик → кнопка `disabled` + текст `Заход в пояс…` (защита от двойного клика,
   как у композитной кнопки в `wireBeltButtons`).
2. Переход **в той же вкладке**: `location.href = '/belt.html?belt=' + id`
   (образец — `appendLandItem` → `/surface.html?planet=<id>`).
3. `enter` вызывает сама `belt.html` при загрузке (осн. §6.2), а не модалка —
   единая точка валидации; модалке не нужен свой запрос.
4. Серверный отказ (400: «Пояс выработан» / «Трюм полон» / «Сначала долетите до
   пояса») показывает `belt.html` своим экраном ошибки (§5.3) — не тост модалки.
5. Если клиент уже знает причину (состояния 4–6) — кнопка disabled, клика нет.

**Альтернатива (не рекомендую):** дизейблить кнопку в модалке под каждый отказ
сервера — требует полей запаса/трюма в `BeltView`/`/me` (§12) и дублирует
валидацию. Рекомендую: **дешёвый пречек только для выработанного пояса** (когда
поле появится) + единственный источник правды — `enter`.

### 2.4. Реактивность в модалке

- При `refreshPlanets` модалка перечитывает `my_position` (index.js 242): уход
  на `belt.html` и возврат (`orbit`) перерисуют бейдж и кнопку автоматически —
  специальных обработчиков не нужно.
- Пока страница `belt.html` открыта, модалки нет (переход в той же вкладке) —
  рассинхрона нет.

---

## 3. Задел: ПКМ по астероидному кольцу (осн. §6.1а) — **активируется вместе с кольцом**

> Кольцо пояса на канвасе **ещё не нарисовано** (осн. §6.1, Д-Л5; визуальный шаг
> `@gdesigner`). ПКМ по тому, чего нет, невозможен → ветка **не активна** до
> появления кольца (подэтап 3d, осн. §14). Проектирую пункт меню **как заготовку**.

**Где:** `events.js`, обработчик `contextmenu` (281–297) — новая ветка
`hit && hit.type === 'belt'` → `showBeltMenu(e.clientX, e.clientY, hit.index)`
(хит-тест по `annulus` пояса — координаты/радиус из слоя `@gdesigner`, когда
появятся).

**Меню `showBeltMenu`** (копия каркаса `showPlanetMenu`: `#star-context-menu`,
`menuItem(html)` со стилем строки — не изобретать):

- заголовок: **«Пояс <имя>»** (`b.name` в `capitalize`, фолбэк `beltKindLabel(b.kind)`);
- пункт **«🚀 Лететь»** — та же логика/гейты, что у кнопки в строке пояса
  (`startIntraFlight`/`startCompositeFlight`, disabled при уже-в-поясе/полёте);
- пункт **«⛏ Добывать»** — та же матрица состояний §2.2, действие — переход на
  `/belt.html?belt=<id>`;
- **пока кольца нет — ветка не активна**: хит-тест не срабатывает, меню не
  создаётся (кода добавлять не нужно, пока `@gdesigner` не отдал annulus).

**Помечено:** этот раздел — **не входит в 3b** (клиент мини-игры); реализуется
подэтапом **3d** вместе с кольцом. В v1 работает **кнопка** (§2).

---

## 4. HUD страницы `web/belt.html` (направление A) — компоновка и элементы

### 4.1. Каркас страницы (по образцу `surface.html`)

```
canvas#belt-canvas                      (fixed, inset:0)
div#hud (display:none)                  (полоса сверху, pointer-events:none)
div#pause.overlay                       (Esc: «Продолжить» / «Вернуться на корабль»)
div#error.overlay                       (отказ входа/обрыв — §5.3)
div#loading.overlay                     (прелоадер, z-index ниже оверлеев)
div#toast (display:none)                (низ-центр, 3 с — как surface)
script type="module" src="/static/js/belt/belt_main.js"
```

CSS — `web/static/css/belt.css`: копия каркаса `surface.css`
(`html,body`, `.overlay/.panel/.panel-actions`, `#hud/.hud-*`, `#toast`,
`#hud button{pointer-events:auto}`) + классы трюма из `index.html`
(`.cargo-bar/.cargo-bar-fill/.cargo-bar-label`) + новый `.hud-chip` (чип уровня
запаса, стиль бейджа `beltRow`). **Палитра/шрифт — существующие.**

### 4.2. Элементы HUD (слева → справа)

**`.hud-left`** (мин-ширина ~240px, как `surface.css`):

1. **Буфер захода** — `span.hud-label` «Добыто за заход» + `span#hud-mined`
   (значение `mined`, формат `N т`, целое/до 1 знака — как `cargoNum`).
   *Данные:* `mined` из пакета (осн. §7) и из каждого ответа `collect`.
2. **Трюм** — `#cargo-bar > #cargo-fill` + `span#cargo-text` (`used / total т`,
   формат `cargoMassLabel`). Полоса: `#3b82f6` (как дашборд); при `free===0` —
   `#ef4444` (существующий красный).
   *Данные:* `cargo.{used,total,free}` пакета (осн. §7).
3. **Чипы запаса и класса пояса** — `#hud-reserve` («Запас пояса: <уровень>»,
   меняется при добыче; *данные:* `remaining_level` пакета и ответов `collect`) и
   `#hud-belt-class` («Пояс: богатый/средний/бедный», постоянный; *данные:*
   `belt_class` пакета). Цвет/текст — таблицы §4.3.

**`.hud-center`** (текст по центру, `#cbd5e1`):

4. Имя пояса `#hud-belt` — «Пояс <имя>» / `beltKindLabel(b.kind)` (тихая строка,
   как `#hud-env`).
5. **Контекстная подсказка** `#hud-hint` — единственная динамическая строка
   (тексты §4.4). Пусто в покое (или короткая памятка, см. §4.4 п.0).

**`.hud-right`:**

6. **Кнопка «Вернуться на корабль»** `#belt-leave-btn` — стиль
   `#ship-call-btn` (surface.css 75–84). Disabled на время `leave`-запроса
   (`Выходим…`). `title="Вернуться на корабль и выгрузить добычу в трюм"`.

**`.hud-controls`** (низ-лево, `position:fixed; left:14px; bottom:12px`):

7. **Легенда управления**: 4 стрелки тяги (⬆⬇⬅➡ / WASD) + чип **«тормоз»** +
   `Space` — «добыча». Активная клавиша подсвечивается (accent-цвет). Назначение:
   первая погружённость без брифинга (см. §4.5).
   **Зависимость:** конкретная клавиша торможения в осн. §5.1 **не задана**
   («тяга по 4 направлениям, **слабое торможение**») → помечено как открытый
   вопрос (§14 О2); чип «тормоз» подписывается фактической клавишей после
   решения (предложение: `Shift`).

### 4.3. Чипы: уровень запаса и класс пояса (текст и цвет)

**Чип уровня запаса** `#hud-reserve` — **меняется при добыче**:

| `remaining_level` | Текст чипа | Цвет (border/color на rgba-фоне) |
|---|---|---|
| `полный` | «Запас пояса: полный» | `#4ade80` (зелёный, как бейдж «Вы в поясе») |
| `истощается` | «Запас пояса: истощается» | `#facc15` (жёлтый) |
| `выработан` | «Пояс выработан» | `#ef4444` (красный, как `hp-fill`) |
| нет данных | «Запас пояса: нет данных» | `#64748b` (тихий серый) |

**Чип класса пояса** `#hud-belt-class` — **постоянный** (из `belt_class`):

| `belt_class` | Текст чипа | Цвет |
|---|---|---|
| `богатый` | «Пояс: богатый» | `#4ade80` |
| `средний` | «Пояс: средний» | `#facc15` |
| `бедный` | «Пояс: бедный» | `#f97316` (оранжевый, как `hazardRow`) |
| нет данных | «Пояс: нет данных» | `#64748b` |

Все цвета — существующие константы (`surface.css`, `panel.js`), не новые.
Формулировки — из пакета (осн. §7), клиент их **не пересчитывает**.

**Гейт знания (как состав по этапу 2, осн. §6.4/§8.4; решение приёмной 2026-09-22).**
Обе оси — уровень запаса, факт выработанности и класс пояса — показываются **только
при знании пояса** (система в радиусе радара **ИЛИ** присутствие игрока в поясе) —
тем же гейтом, что раскрывает `composition` (`planet_visibility.go`/`stripBeltDetails`).
Без знания полей `remaining_level`/`belt_class` нет → чипы показывают «нет данных»
(оба — `#64748b`), **не** «выработан» (осн. §8.4: `NULL` ≠ «выработан»). На странице
`belt.html` игрок присутствует в поясе → знание есть всегда; гейт важен для строки
пояса в модалке (§2.2 п.4–5, §12.1).

### 4.4. Подсказка `#hud-hint` — состояния (тексты)

| Состояние | Текст `#hud-hint` |
|---|---|
| 0. покой (не у цели, ничего не блокирует) | пусто (легенда управления — отдельно, §4.2 п.7) |
| 1. корабль у крупного астероида в радиусе `R_extract`, идёт удержание | «⛏ Добыча идёт…» (подсказка меняется на прогресс) |
| 2. корабль в радиусе, клавиша не нажата | **«Держите [Space] — добыча»** |
| 3. трюм полон (§5.2) | **«Трюм полон — добыча недоступна. Вернитесь на корабль.»** |
| 4. пояс выработан (§5.1) | **«Пояс выработан — добывать больше нечего.»** |
| 5. нет связи (§5.4) | «Нет связи — добыча приостановлена» |

Согласно решению **§10.4-A** (без таймера) — таймера/счётчика времени в HUD
**нет**. Согласно **§10.2-A** (без последствий) — полосы HP/прочности в HUD
**нет** (§11).

### 4.5. Брифинг входа — **не вводим** (предложение, не факт)

У поверхности брифинг нужен был из-за приговора скафандра и осей опасности
(осн. поверхность §5.2). В поясе нет ни риска, ни осей — брифинг был бы пустым
экраном. Управление объясняет легенда (§4.2 п.7) + подсказка у цели (§4.4 п.2).
**Рекомендую без брифинга.** Если создатель захочет «карточку захода» (имя
пояса, добывается железо, запас, предупреждение «столкновения прерывают
добычу») — это дешёвый `.overlay`-панель после `enter` (помечено как
**предложение**, не в объёме по умолчанию).

---

## 5. Состояния экрана и оверлеи

### 5.1. Выработанный пояс

- **До входа** (`enter` → 400 «Пояс выработан»): экран ошибки (§5.3) с текстом
  **«Пояс выработан»**, кнопка «Вернуться к карте». Сцены нет.
- **Во время захода** (запас ушёл в 0 при `collect`, ответ
  `remaining_level==='выработан'`): чип запаса → красный «Пояс выработан» (§4.3),
  подсказка `#hud-hint` → §4.4 п.4, луч/частицы бурения прекращаются, `Space`
  больше не шлёт `collect`. Один тост (`info`) «Пояс выработан» (§6.1).
  Игрок может продолжать летать и вернуться на корабль — **не принуждаем**.

**«Прогресс жилы» / «запас астероида» на клиенте — чисто визуальный** (осн. §5.2/§8.5,
Б8). Шкала прогресса/запаса отдельного астероида — локальная (от `seed`); сервер знает
только **агрегат** `iron_remaining` пояса и точных чисел клиенту не отдаёт (осн. §7).
Клиент опирается на `remaining_level` из ответов `collect`: при `выработан` жилы
**гаснут**, луч/частицы прекращаются, сцена переходит в состояние «выработан».
Точное число запаса клиент не знает и не показывает.

### 5.2. Полный трюм

- Триггер: `cargo.free === 0` (из пакета) или ответ `collect` с `full:true`
  (осн. §7).
- Чип трюма: полоса 100% `#ef4444`, подпись `total / total т`, рядом бейдж
  **«Полон»** (красный, стиль бейджа).
- `#hud-hint` → §4.4 п.3 (постоянно, пока трюм полон) + **инлайн-кнопка**
  «Вернуться на корабль» в подсказке (дублирует `#belt-leave-btn` — путь «в один
  клик», но **не блокирующий оверлей**: осн. §5.4 «предложение, не принуждение»).
- Сбор блокируется: луч/частицы не рисуются, `Space` не шлёт `collect`,
  подсказка у цели (§4.4 п.2) не показывается.
- Один тост (`info`) в момент заполнения: «Трюм полон — выгрузите груз на
  корабле» (§6.1). Повторно не повторяется.

### 5.3. Экран ошибки `#error` (вход/сеть)

Каркас `.overlay` → `.panel` (как `#death` в surface). Заголовок **«Заход в пояс
невозможен»**, тело — серверное сообщение, кнопки: «Повторить» (только для
сетевой ошибки) + **«Вернуться к карте»** (primary).

| Причина | Тело | Повторить? |
|---|---|---|
| 400 «Пояс выработан» | Пояс выработан — добывать здесь больше нечего. | нет |
| 400 «Трюм полон» | Трюм полон — сначала освободите место на корабле. | нет |
| 400 «Сначала долетите до пояса» | Сначала долетите до пояса. | нет |
| 400 «Пояс вне зоны видимости» (осн. §6.2 п.6) | Пояс вне зоны видимости — подлетите ближе. | нет |
| 401 | — | редирект `/login-page` (как surface) |
| сеть/5xx | Не удалось связаться с сервером. | **да** |

Кнопка «Вернуться к карте» → `location.href = '/map'` (как `surface` при
«Вызвать корабль»). **Пометка:** в осн. спеке явно не сказано, куда возвращать
после выхода — предлагаю `/map` (единообразие с surface); альтернатива —
вернуть в системную модалку того же мира (дороже, отдельный deep-link) —
вынесено в §14 О4.

### 5.4. Обрыв / рефреш / потеря связи

| Событие | Поведение UI |
|---|---|
| **Рефреш/возврат на страницу** (сессия возобновляется, осн. §6.3) | `boot()` → `enter` идемпотентно возвращает пакет с сохранённым `mined`; сцена генерируется заново от `seed` (спавн с начала — осн. §6.3). Если `mined > 0` — один `info`-тост **«Сессия добычи возобновлена: в буфере N т»**; трюм и чип — из пакета. |
| **Потеря соединения при `collect`** | Не спамить тостами: подсказка §4.4 п.5 + ненавязчивый пилл `#offline-pill` (верх-центр, «Нет связи — добыча приостановлена», стиль `#offlinePill` студии). Локальные частицы идут, но буфер **не растёт** (сервер — касса, осн. §5.3). При успешном ответе — пилл скрыть, тостов не слать (восстановление тихое). |
| **Разлогин / сервер перезапущен** (осн. §6.3) | Позиция `mining` восстанавливается — UI не меняется; после перезапуска первый успешный `collect`/повторный `enter` вернёт состояние. |
| **Разрыв связи не наказывает** (осн. §6.3) | Буфер НЕ обнуляется, сцена не сбрасывается дальше спавна; никаких «провал»-экранов (если не выбран §10.2-B, §11). |

---

## 6. Реактивность: тосты, звук, анимации, блокировки

### 6.1. Тосты (низ-центр, `#toast`, 3 с — как `surface_ui.notify`)

| Событие | Тип | Текст |
|---|---|---|
| Сессия возобновлена (`mined>0` при `boot`) | info | «Сессия добычи возобновлена: в буфере N т» |
| Трюм заполнен (переход `free>0` → `0`) | info | «Трюм полон — выгрузите груз на корабле» |
| Пояс стал выработан (переход `remaining>0` → `0`) | info | «Пояс выработан» |
| Успешный `leave` | success | «Возвращаюсь на корабль…» (перед переходом) |
| Ошибка `leave` | error | «Не удалось выйти из пояса: <сообщение>» |
| Сеть пропала | — | **не тост**, пилл (§5.4) |

Формулировки стиля — как у surface (`«Вызываю корабль…»` / `«Не удалось вызвать
корабль: …»`). Тост **не** показывается на: каждую порцию добычи (это HUD),
успешный `collect`, потерю и восстановление связи (пилл).

**Решение по механизму:** на `belt.html` используется **локальный `#toast`**
(как на `surface.html`), а не глобальный `ui/toast.js` — игровые страницы
держат сообщения низ-центром, не перекрывая HUD; глобальные тосты верх-право
конфликтовали бы с `#hud`. Единообразие с surface важнее.

### 6.2. Звук (`ui/sound.js`)

- `belt_main.js` при `boot()` вызывает **`activateSound()`** (как карта) — иначе
  звук на странице не активируется (в поверхность звук не завезён; здесь он
  нужен под роли из задачи).
- Роли (по назначению, `ROLE_FILE`): успешный `leave` → `playSound('ui_success')`;
  ошибка `leave`/сервера → `playSound('ui_error')`; клик «Вернуться на корабль»
  → `playSound('ui_select')`.
- **Постоянного звука бурения НЕТ.** Гул `flight_hum` — это полёт, не бурение
  (не переиспользуем без решения). Если создатель захочет «звук бура» —
  отдельный ассет/роль, **предложение**, не в объёме (§14 О3).
- Звук не должен «спамить»: `ui_select` — только на явный клик, не на каждое
  событие.

### 6.3. Анимации и переходы (UI-уровень; сцена — осн. §9.2)

- **Уход со страницы** («Вернуться на корабль»): кнопка disabled + текст
  `Выходим…`, затем `location.href='/map'` после 200 OK.
- **Чип запаса/трюма**: смена цвета/ширины — CSS-transition ~0.2 s (как
  `#hp-fill`, `.cargo-bar-fill`).
- **Пилл offline**: появление/скрытие — fade (как тост).
- **Всплывающие «+X т»** у астероида — **сцена** (осн. §9.1 п.5), не UI-оверлей;
  проектируется `@gdesigner`.
- Новых анимационных механик не вводим.

### 6.4. Что блокируется и что параллельно

- Во время `enter` (до пакета): показан `#loading`, HUD скрыт, `Esc`/клики по
  сцене инертны (оверлей перехватывает).
- Во время `leave`: кнопка выхода disabled; игра может продолжаться визуально,
  но новые `collect` не шлются (выход закрыт) — простая защита.
- **Esc → пауза** (`#pause`, оверлей; игра и таймер сцены стоят, `collect` не
  шлётся) с кнопками «Продолжить» / «Вернуться на корабль» — как surface.
- Пока открыт `.overlay` (`#error`/`#pause`) — ввод сцены заблокирован.

---

## 7. Формулировки (сводка — единый источник для клиента)

| Место | Текст |
|---|---|
| Кнопка строки пояса (норма) | `⛏ Добывать` |
| Кнопка строки пояса (сессия идёт) | `⛏ Продолжить добычу` |
| Кнопка, disabled — не в поясе | `Сначала долетите до пояса` |
| Кнопка, disabled — полёт | `Вы в полёте — дождитесь прибытия` |
| Кнопка, disabled — выработан | `Пояс выработан` |
| Кнопка, disabled — нет данных | `Нет данных о запасе пояса` |
| Кнопка, disabled — трюм | `Трюм полон — освободите место` |
| Кнопка, переход | `Заход в пояс…` |
| Бейдж строки (orbit) | `● Вы в поясе` |
| Бейдж строки (mining) | `⛏ Добываете` |
| HUD: буфер | `Добыто за заход` → `N т` |
| HUD: трюм | `N / M т` (+`Полон`) |
| HUD: запас | `Запас пояса: полный/истощается` / `Пояс выработан` / `Запас пояса: нет данных` |
| HUD / строка пояса: класс | `Пояс: богатый/средний/бедный` / `Пояс: нет данных` |
| Подсказка у цели (2) | `Держите [Space] — добыча` |
| Подсказка, идёт (1) | `⛏ Добыча идёт…` |
| Подсказка, трюм полон (3) | `Трюм полон — добыча недоступна. Вернитесь на корабль.` |
| Подсказка, выработан (4) | `Пояс выработан — добывать больше нечего.` |
| Подсказка, нет связи (5) | `Нет связи — добыча приостановлена` |
| Кнопка выхода | `Вернуться на корабль` |
| Кнопка выхода, в процессе | `Выходим…` |
| Загрузка | `Заход в пояс…` |
| Экран ошибки, заголовок | `Заход в пояс невозможен` |
| Экран ошибки, кнопки | `Повторить` / `Вернуться к карте` |
| Тост: возобновление | `Сессия добычи возобновлена: в буфере N т` |
| Тост: трюм полон | `Трюм полон — выгрузите груз на корабле` |
| Тост: выработан | `Пояс выработан` |
| Тост: выход | `Возвращаюсь на корабль…` |
| Тост: ошибка выхода | `Не удалось выйти из пояса: …` |
| Пилл offline | `Нет связи — добыча приостановлена` |
| Пауза (заголовок/текст) | `Пауза` / `Добыча продолжается, пока вы в поясе.` |

Серверные сообщения (Сначала долетите / Пояс выработан / Трюм полон) — **те же
строки**, что в осн. §6.2, клиент их не переизобретает (показ «как есть» на
экране ошибки, §5.3).

---

## 8. Две оси: уровень запаса (`remaining_level`) и класс пояса (`belt_class`)

Осн. §7 отдаёт **качественные** метки (анти-метагейминг) — границы считает **сервер**.
Решение `@designer` (2026-09-22) разводит **две независимые оси** (закрывает О7):
меняющийся **уровень запаса** (сколько осталось) и постоянный **класс пояса**
(стоит ли лететь). Оба поля качественные, числа клиенту не отдаются.

### 8.1. Уровень запаса `remaining_level` (меняется при добыче)

Опорная шкала — **доля от начального запаса** `f = iron_remaining / reserve`, где
`reserve = 600 т · k_iron` и `k_iron = composition.iron / 0.30` (осн. §8.2) — это
ровно `f = iron_remaining / (600 т · k_iron)`:

| Доля `f` | `remaining_level` |
|---|---|
| `f = 0` | `выработан` |
| `0 < f ≤ 0.60` | `истощается` |
| `f > 0.60` | `полный` |

### 8.2. Класс пояса `belt_class` (постоянный, от `k_iron`)

Постоянная характеристика пояса (не меняется при добыче) — отвечает на вопрос
«стоит ли лететь»; та же шкала, что классы поясов осн. §8.1 п.4:

| `k_iron` (`= composition.iron / 0.30`) | Железо `composition.iron` | `belt_class` |
|---|---|---|
| `k_iron > 0.60` | > 0.18 | `богатый` |
| `0.25 < k_iron ≤ 0.60` | 0.075–0.18 | `средний` |
| `k_iron ≤ 0.25` | ≤ 0.075 | `бедный` |

**Обоснование:** при `reserve ≈ 6 трюмов` (осн. §8.1 п.3) игрок чувствует
переход «полный→истощается» примерно после 2–2.5 трюмов; это читается без точных
чисел (метагейминг закрыт). Пороги — **числа UI**, калибруются вместе с §8 осн.
спеки (`@balancetester`), не догма. Помечено: значения `reserve`, `iron_remaining`,
`k_iron` клиенту **не** отдаются — только метки (осн. §7).

---

## 9. ТЗ разработчику: «элемент/экран: было → станет» (+почему)

### 9.1. `web/static/js/modal/panel.js`

| Элемент | Было | Станет | Почему |
|---|---|---|---|
| `beltRow` правая часть | один `flyBtn` | группа `.belt-actions` (`display:flex;gap:6px`): `⛏ Добывать` (`data-belt-mine`) + существующий `flyBtn` | вход в мини-игру рядом с полётом (осн. §6.1) |
| `beltRow` бейдж | `orbit`+belt+id | то же **или** `mining`+belt+id; при `mining` текст `⛏ Добываете` | сессия живёт после ухода (осн. §6.3) |
| `beltRow` класс пояса | — | **чип класса** «Пояс: богатый/средний/бедный» из `belt_class` (гейт знания: без знания — «Пояс: нет данных») | отвечает «стоит ли лететь» (осн. §8.4, §2.1) |
| `beltRow` строка состава | «Состав: …» | без изменения; **при наличии поля и знании пояса** (§12) добавить строку «Запас железа: <уровень>»; без знания — «нет данных» (не «выработан») | видимость выработанного пояса (осн. §8.4, гейт знания §6.4) |
| `wireBeltButtons` | обработчики `[data-belt-fly]`/`[data-belt-composite-fly]` | + обработчик `[data-belt-mine]` по матрице §2.2 → `location.href='/belt.html?belt='+id` | единая точка входа |

### 9.2. `web/static/js/modal/events.js` (задел 3d, §3)

| Элемент | Было | Станет | Почему |
|---|---|---|---|
| `contextmenu` (281–297) | 3 ветки (star/companion/planet) | + `hit.type==='belt'` → `showBeltMenu` | осн. §6.1а (ПКМ по кольцу) |
| меню | — | новая `showBeltMenu` (каркас `showPlanetMenu`), пункты «🚀 Лететь» + «⛏ Добывать» | задел, активируется с кольцом |

### 9.3. `web/static/js/modal/modal_render.js`

| Элемент | Было | Станет | Почему |
|---|---|---|---|
| `drawMyPosition` (455–466) | `orbit`/`surface`; для `belt` — return | для `status==='mining' && object_type==='belt'` — **return** (маркера нет, как у belt) | `mining` не должен падать в «орбиту звезды» (осн. §6.4) |

### 9.4. Новые файлы (структура — по образцу surface)

| Файл | Содержание |
|---|---|
| `web/belt.html` | каркас §4.1, `?belt=<id>` |
| `web/static/css/belt.css` | каркас `surface.css` + `cargo-*` + `.hud-chip` (§4.1) |
| `web/static/js/belt/belt_main.js` | поток: `boot()` (читает `?belt`), `enter`, сцена, HUD, `leave`, `Esc`-пауза, `activateSound()` |
| `web/static/js/belt/belt_net.js` | `enter`/`collect`/`leave` (Bearer, как `surface_net.js`); 401 → `/login-page` |
| `web/static/js/belt/belt_ui.js` | HUD (§4), экраны (§5), тосты (§6.1), пилл |
| `web/static/js/belt/belt_world.js` + `belt_render.js` | сцена (осн. §5.1/§9.2) — **плейсхолдеры** вместо ассетов (3c) |

**Не в 3b:** ассеты астероидов, слои/эффекты, замена плейсхолдеров (3c,
`@gdesigner`/`@artist`); ПКМ по кольцу (3d).

### 9.5. Критерий проверки (как создатель увидит результат)

1. Игрок в поясе открывает модалку системы: в строке пояса рядом с «🚀 Лететь»
   есть **«⛏ Добывать»**; не в поясе — кнопка неактивна с подсказкой «Сначала
   долетите до пояса».
2. Клик «⛏ Добывать» → открывается `/belt.html?belt=<id>`, сцена отрисована, HUD
   показывает буфер, трюм и уровень запаса; управление работает, `Space` у
   астероида даёт добычу, буфер растёт.
3. «Вернуться на корабль» → возврат на карту, в модалке пояс помечен
   «● Вы в поясе», в дашборде трюм вырос (осн. §12.2).
4. Рефреш во время захода → сессия возобновляется, буфер не потерян, показан
   тост «Сессия добычи возобновлена…»; сцена с начала (осн. §6.3).
5. Полный трюм → полоса красная + «Полон», подсказка «Трюм полон…» с кнопкой
   выхода, добыча не идёт.
6. Выработанный пояс → до входа экран «Пояс выработан»; во время захода — чип
   красный + подсказка, добыча остановлена.
7. Вне пояса/не в системе → осмысленный экран ошибки с «Вернуться к карте».
8. `Esc` → пауза «Продолжить» / «Вернуться на корабль».
9. ПКМ по кольцу пояса (когда кольцо появится) → меню «Пояс <имя>» с «⛏
   Добывать» — **активируется вместе с кольцом**; сейчас кнопка — рабочий путь.

---

## 10. Что не трогать (инварианты UI)

- **`web/surface.html` + `web/static/js/surface/*` + `surface.css`** — не
  меняем; `belt.css` **копирует** нужные классы, не редактирует общие файлы.
- **Кнопка «🚀 Лететь»** в строке пояса и её гейты (`wireBeltButtons` 253–274) —
  семантика без изменений; меняется только обёртка в группу.
- **Строки планет** (`planetRow`), клик `data-index`, тултип/меню звезды и
  компаньонов, `showPlanetMenu`/`appendLandItem` — как есть.
- **`modal_render.js` `drawMyPosition`** — кроме явной ветки `mining` (return)
  логика `orbit`/`surface`/маркеров не трогается.
- **Сцена мини-игры** (мира, инерции, бурения) — осн. §5; UI её не переписывает,
  только читает числа/HUD.
- **Дашборд, блок «Трюм»**, `dashboard/cargo.js`, тост-система `ui/toast.js`,
  `ui/sound.js` — не меняем (только **вызываем** существующее).
- **Стиль/палитра/шрифты** — существующие; новых цветов нет (только
  rgba-фоны на базе `#4ade80/#facc15/#f97316/#ef4444/#3b82f6/#64748b`).
- **Числа баланса, БД/миграция, Go/API** — не наша зона (осн. §4–§8).
- Температур в интерфейсе нет — правило «только °C» не задействовано.

---

## 11. Зависит от решения создателя (осн. §10) — что перепроектируется

| Развилка осн. | Рекомендация | Что уже спроектировано под неё | Что изменится при другом выборе |
|---|---|---|---|
| **§10.1 истощение** | (A) **конечный** | состояние «выработан» (§5.1), уровни запаса (§4.3, §8), чип в HUD | (B) бесконечный → **убрать** чип запаса, экран/состояние «выработан», поле `remaining_level` в `BeltView`; остаётся только буфер+трюм |
| **§10.2 риск** | (A) **без последствий** | нет HP, нет экрана «провал», столкновение лишь прерывает бурение (текст подсказки не нужен) | **(B) буфер теряется при аварии** → новое состояние: тост/оверлей «Авария — буфер захода потерян» + (нужен видимый триггер: индикатор «прочность/столкновения» в HUD) — **проектируется отдельно**; **(C) HP** → полоса прочности в HUD (`.hud-left`) + экран провала |
| **§10.3 находка** | (B) **позже** | UI для находок **не делаем** | (A) сейчас → новый слой сцены + подсказка/взаимодействие (отдельный UI-шаг) |
| **§10.4 форма захода** | (A) **без таймера** | таймера/энергии в HUD **нет** | (B) таймер/энергия → новый HUD-элемент (полоса времени/энергии) + экран «время вышло» |
| **§10.5 вход** | кнопка сейчас, ПКМ позже | кнопка (§2) + задел ПКМ (§3) | (B) только ПКМ → кнопку в строке убрать, ждать кольца (v1 не запускается) |
| **§10.5 видимость** | приватная (скрыт) | UI других игроков не показывает «добывает» | если видимость введут — бейдж статуса у пояса в модалке/карте (отдельная дельта) |

---

## 12. Зависимости данных (не UI-зона — передать @designer/@developer)

1. **Уровень запаса и класс пояса в строке пояса** (осн. §8.4 требует показ
   «выработан/уровень»): в `BeltView` и пакете осн. §7 нужны **оба** качественных
   поля — `remaining_level` (`полный/истощается/выработан`) и `belt_class`
   (`богатый/средний/бедный`), а не только `remaining_level`.
   **Гейт знания:** оба поля отдаются тем же гейтом, что `composition`
   (осн. §6.4/§8.4, тест M21) — **без знания полей нет**, строка показывает
   «нет данных» (не «выработан»); «выработан» без знания не показываем.
   Без полей §2.2 п.4–5 и чипы класса/запаса в §9.1 недоступны (фолбэк —
   только тост от сервера). **Эффект, который хотим видеть:** заранее видно,
   куда лететь есть смысл (класс пояса), а пояс не предлагает вход «выработан».
2. **Свободный трюм в модалке** (для §2.2 п.6): сейчас клиент трюм в модалке не
   читает (`GET /api/cargo` — только дашборд). Либо `/me` отдаёт `cargo_free`,
   либо состояние «трюм полон» в модалке **не пречекается** (рекомендую —
   оставить отказ сервера + экран ошибки §5.3; дешевле и единый источник).
3. **`seed`, `limits`, `cargo`, `resource.good_id`** — уже в пакете осн. §7;
   клиент их не считает и не хардкодит (осн. §7).
4. **Клавиша торможения** — не задана в осн. §5.1 (см. §14 О2).

---

## 13. Связи механик (impact map) — дельта для `@manager`

| Сущность | Связь | Куда | Почему |
|---|---|---|---|
| `belt_mining` | affects | `web/static/js/modal/panel.js` (`beltRow`/`wireBeltButtons`) | кнопка «⛏ Добывать» + бейдж `mining` |
| `belt_class` (пояс) | affects | `web/static/js/modal/panel.js` (`beltRow`) + `web/static/js/belt/*` (чип `#hud-belt-class`) | чип класса пояса в строке/HUD — «стоит ли лететь» (§2.1/§4.3/§8.2) |
| `belt_mining` | affects | `web/static/js/modal/events.js` (contextmenu) | задел ПКМ по кольцу (3d) |
| `belt_mining` | affects | `web/static/js/modal/modal_render.js` | `mining` в `drawMyPosition` (return, без маркера) |
| `belt_mining` | new | `web/belt.html`, `web/static/js/belt/*`, `web/static/css/belt.css` | страница-мини-игра (3b) |
| `belt_mining` | reads | `web/static/js/ui/sound.js` | `activateSound()` + роли `ui_select/ui_success/ui_error` |
| `belt_mining` | reads | `web/static/js/dashboard/cargo.js`-формат | единый вид трюма (полоса/подпись `used / total т`) |
| `belt` (кольцо, `@gdesigner`) | affects | `belt_mining` | ПКМ-вход активируется вместе с кольцом |
| `belt_mining` | affects | `docs/impact_map.json` | узлы/рёбра (ведёт `@manager`) |

---

## 14. Открытые вопросы (гейт создателя / уточнение у менеджера)

- **О1. Направление компоновки HUD** — рекомендую **A** (верхняя приборная
  панель). Выбор направления = гейт (§1).
- **О2. Клавиша торможения** — в осн. §5.1 «слабое торможение» без клавиши.
  Предлагаю `Shift`; нужен ответ `@designer`/создателя, чтобы подписать чип
  «тормоз» (§4.2 п.7).
- **О3. Звук бурения** — постоянного звука нет (гул `flight_hum` не
  переиспользуем). Нужен ли отдельный «бур» — предложение, вне объёма.
- **О4. Куда возвращать после выхода** — предлагаю `/map` (единообразие с
  surface); альтернатива — системная модалка мира (дороже). Решает создатель.
- **О5. Брифинг входа** — рекомендую **без** брифинга (§4.5); «карточка захода»
  — предложение.
- **О6. «Нет данных о запасе»** (старый мир, осн. §8.4/§4) — формулировка и
  поведение кнопки зависят от выбора инициализации (генератор vs ленивый
  расчёт) — решает `@designer`/разработка.
- **О7. Семантика `remaining_level` — ✅ закрыт** (`@designer`, 2026-09-22): две оси
  разведены. `remaining_level` (`полный/истощается/выработан`) — меняющаяся доля
  остатка `f`; `belt_class` (`богатый/средний/бедный`) — постоянный класс пояса по
  `k_iron` (§8). Тексты чипов обновлены (§4.3), класс показан в строке пояса (§2.1).
  Обе оси — под гейтом знания пояса (§4.3/§12.1).

---

## 15. История итераций

| Итерация | Что | Итог |
|---|---|---|
| 1 | 3 направления HUD (A верх/ B кокпит / C минимал), рекомендовано A; точка входа и матрица состояний; задел ПКМ; полная компоновка A; экраны/состояния; реактивность/звук/тосты; формулировки; пороги запаса; ТЗ; инварианты; зависимость от §10; impact map; открытые вопросы | ждёт гейта создателя |
| 2 | **Синхронизация с новой числовой моделью дизайнера** (осн. §8, пакет 2): пороги уровней запаса выровнены — формула `f = iron_remaining / (600 т · k_iron)` совпадает с моделью, **правка не требуется**, границы 0.25/0.60 привязаны к `k_iron` (§8); зафиксирован **гейт знания** уровня/выработанности — как состав этапа 2, без знания «нет данных», не «выработан» (§4.3/§9.1/§12.1); отмечен **чисто визуальный** характер «прогресса жилы/запаса астероида» — сервер знает только агрегат (§5.1) | пороги согласованы; открыт вопрос О7 (семантика `remaining_level`) |
| 3 | **Дельта `@designer` — две оси** (2026-09-22): `remaining_level` переименован в `полный/истощается/выработан` (границы по доле `f`: `полный` `f>0.60`, `истощается` `0<f≤0.60`, `выработан` `f=0`); добавлена вторая ось — постоянный **`belt_class`** (`богатый` `k_iron>0.60` / `средний` `0.25<k_iron≤0.60` / `бедный` `k_iron≤0.25`; железо >0.18 / 0.075–0.18 / ≤0.075). Чипы запаса+класса (§4.3), класс пояса как чип в строке (§2.1/§9.1), оба поля в `BeltView` под гейтом знания (§12.1), `belt_class` в impact map (§13) | О7 **закрыт** (две оси разведены) |

**Ретроспектива:** бюджет итераций не превышен (3 из 5) — ретроспектива не
требуется.
