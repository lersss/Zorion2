# Трюм — грузоподъёмность корабля

**Статус:** ✅ реализовано 2026-09-22 (коммит `bdc76c7`); **дельта 2026-09-24 реализована** (коммит `86a0412`): модуль `cargo_1` 80 → **30 т** (стартовая ёмкость **50 т**), универсальных слотов **1 → 3**, слоты **видимы** в разделе «Корабль», миграция **`000080`** (обрезка перегруженных трюмов) — см. §20.
**Дата:** 2026-09-22 (дельта 2026-09-24)
**Блок:** Трюм — грузоподъёмность корабля: фундамент петли «полёт → добыча → сдача → мир изменился» (вектор «Игрок»)
**Связи:**
идея-носитель `docs/gamedesign/ideas/2026-09-22_трюм-грузоподъёмность-корабля.md` (пожелание, 4 решения создателя, архитектурный фильтр, ориентир X4, скоуп);
первый потребитель — добыча в поясе `docs/gamedesign/ideas/2026-09-21_добыча-в-поясе-мини-игра.md` (этап 3 поясов), спека поясов `docs/specs/2026-09-21-пояса-малых-тел-объект-системы.md` §5.2 (`data.resources` пояса);
модель корабля/оборудования — `docs/specs/77a-ship-equipment-radar.md` (§2–§4: `ship_models`, `equipment`, `users.equipment`), дашборд «Корабль» — `docs/specs/_archive/91a-ship-section-dashboard.md` (§5.1 — «трюм», «грузоподъёмность — ядро экономики»), ГДД `docs/gamedesign/21_ships.md`;
каталог товаров — `migrations/000045_create_goods_catalog.sql`, `migrations/000048_producer_types_catalog.sql` (`goods.weight`/`goods.volume`), `migrations/000054_catalog_hidden.sql` (`NOT NULL DEFAULT 1`);
будущие потребители — контракты на перевозку (`docs/gamedesign/11_contracts.md` §11.3.4, требование-окно `contract_requirements`), торговля/рынки (`docs/gamedesign/11_markets.md`);
хранение/транспортировка — `docs/gamedesign/09_resources.md` §9.1.2;
миграция **`000068`** — перебронирована на гейте 2026-09-22: `000066` занят (`000066_factions_race.sql`, фракции-расы) и `000067` занят (`000067_settlement_type.sql`) — **отклонение от указания менеджера (`000067`)**, сверено свежим листом `migrations/`; `000060` забронирован спекой компаньонов `2026-09-21-компаньоны-отдельные-миры.md`, `000063` свободен. Строку в `docs/DB.md` и `99_roadmap.md` добавит менеджер/`@dispatcher`.

---

## 0. Резюме (одним абзацем)

Вводится **трюм** — грузоподъёмность корабля игрока: сколько **массы** (тонн) корабль может везти и **что** в нём лежит. Ёмкость — **производная** величина: врождённая ёмкость модели корабля + сумма грузовых модулей (`equipment.type='cargo'`, `params.capacity`) в **универсальных** слотах (решение создателя №1, пока **1** слот). Числа **закрыты гейтом 2026-09-22**: врождённая **20 т** + модуль **80 т** = **100 т** суммарно. Содержимое — **отдельная таблица** `player_cargo` (решение №4: ключ `(user_id, good_id)`, разных товаров много), занятое считается из **веса каталога** (`goods.weight`, `000048`) — новых полей у груза не заводим. Ограничение — **только масса** (решение №2); объём (`goods.volume`) уже в данных и закладывается **второй осью** так, чтобы его добавление было мелкой правкой, а не переделкой. Объём задачи — **необходимый минимум** (решение №3): нет типов груза, торговли, погрузки в порту, NPC-перевозчиков; **новое в скоупе** — «груз за борт» (сброс своего груза, `POST /api/cargo/jettison`). Трюм самостоятелен: первый потребитель — **добыча в поясе** (кладёт добытое), дальше — **контракты на перевозку** и **торговля**, поэтому спека не проектируется «под пояс». Чтение — `GET /api/cargo` + блок в разделе «Корабль» дашборда; запись — **внутренний сервис** для потребителей, **единственная player-facing запись** — сброс груза за борт (только уменьшает). Числа ёмкости — **предположение дизайнера**, проверяет `@balancetester`; формулы не выдаются как требование свыше — сформулировано, что должно быть на выходе (§5).

---

## 1. Контекст и решения создателя (не перепроектируем)

Решения создателя (2026-09-22, приёмная идеи-носителя):

1. **Ёмкость: врождённая (небольшая) + универсальные слоты.** У корабля есть врождённая небольшая грузоподъёмность; сверх неё ёмкость набирают **грузовыми модулями** в **универсальные** слоты. Пока у корабля **один универсальный слот**.
2. **Сейчас лимит по тоннажу (масса).** В будущем масса и объём — **разные** ограничения; сейчас только масса. Данные есть: у товаров/ресурсов заведены и вес, и объём.
3. **Объём — необходимый минимум** (логистику/торговлю/типы груза не тащить).
4. **Содержимое — отдельная таблица** (не JSONB-поле в игроке).

**Решения создателя (гейт 2026-09-22, пакет правок — нумерация создателя):**

- **№1 — числа трюма закрыты.** Врождённая ёмкость **20 т** + стартовый грузовой модуль **80 т** = **100 т** суммарно — подтверждено. Формулировка «без модуля возишь образец, а не петлю / модуль обязателен» — **ослаблена до гипотезы** (замечание `@balancetester`: при 12 т/заход врождённых 20 т хватает на ~1.7 захода — петля работает и без модуля; «обязательность» зависит от чисел мини-игры пояса, которых ещё нет).
- **№3 — «груз за борт» в скоупе.** Игрок может **сбросить свой груз**; это **единственная player-facing запись** (только уменьшает — ценность создать нельзя): `POST /api/cargo/jettison`. Сброшенное просто исчезает; **объект-контейнер в космосе не создаём** (кандидат на будущее: контейнер / пиратская добыча, §16).
- **№5 — тех-характеристики корабля в скоупе (пересмотр эталона).** Грузоподъёмность как тех-характеристика корабля входит в скоуп — «приходим уже к далёкому будущему». Это ревизует «тех-характеристики корабля — далёкое будущее» из `21_ships.md` §21.5 (И5) **применительно к грузу/ёмкости** — дельта §15 (правило 99a, владелец `@dispatcher`).
- **№6 — грузовой модуль.** Существующий «Модуль корабля» в каталоге **предметов** (`items`, `000048`) — **тестовый**; настоящий грузовой модуль позже **производится в лабораториях корабельного оборудования**; сейчас **выдаётся** стартовым набором. Стык двух «домов» модуля (`equipment` v1 / `items` производство) — **не конфликт на v1** (снято как находка критика), будущая связь зафиксирована (§12/§16).
- **№7 — риск CASCADE принят.** `goods → player_cargo` CASCADE (удаление ресурса из студии уносит груз) — **принято создателем**; предупреждение числом в студии — **необязательный бэклог**. Главное (И5): трюм как состояние игрока **вайп переживает** (§8.2/§11).

Архитектурный фильтр (предложение менеджера, подтверждён создателем; уточнён ниже):

- **Природа:** состояние (накопленный груз), не событие и не лог.
- **Владелец:** один на игрока (один корабль = один трюм).
- **Расширяемость:** таблица `player_cargo` `(user_id, good_id, quantity)`, ключ `(user_id, good_id)`.
- **Ёмкость:** врождённая — параметр **модели корабля**; модули — тип оборудования `cargo` с `params.capacity`, ставится в **универсальный** слот.
- **Новых полей у груза не заводить** — вес/объём уже в `goods`.

Ориентир X4 (создатель просил посмотреть): в X4 грузоподъёмность — в **объёме**, у груза есть «тип хранения», отдельно у корабля — масса. Вывод для Zorion: создатель хочет в будущем тоже развести **массу и объём**, сейчас — **только масса**; «тип груза» — кандидат на будущее, в минимум не входит.

---

## 2. Термины (GLOSSARY-кандидаты)

| Термин | Смысл | Где закрепить |
|---|---|---|
| **Трюм** | Грузовое состояние корабля игрока: сколько массы он везёт и что лежит внутри. Состояние (таблица `player_cargo`), не событие. | `GLOSSARY.md` §5 (дельта менеджеру) |
| **Грузоподъёмность** | Лимит трюма по массе (тонны): врождённая ёмкость модели + сумма грузовых модулей. Производная величина, отдельно не хранится. | `GLOSSARY.md` §5 |
| **Грузовой модуль** | Предмет оборудования типа `cargo` (`params.capacity` — тонны); ставится в универсальный слот. | `GLOSSARY.md` **§1 «Слот»** (значение «слот корабля») **и §6** (оборудование); сам термин оборудования уже закреплён в `21_ships.md`/77a — не дублировать, ссылаться |
| **Универсальный слот** | Слот модели корабля, принимающий модули разных типов (`slots.universal`); пока один. | `GLOSSARY.md` §1 «Слот» (дельта менеджеру) |

> **Дельта `GLOSSARY.md` §1 «Слот» (находка `@critic`):** в разделе сейчас **три** значения («слот строения», «слот рецепта», «слот предмета»); добавляются **четвёртое и пятое** — **«Слот корабля»** (позиция под оборудование модели корабля, `ship_models.slots`/`users.equipment`) и **«Универсальный слот»** (принимает модули разных типов, `slots.universal`). «Слот корабля» в глоссарии **ещё не заведён** — 77a/91a его туда не вносили; это отдельная дельта.

---

## 3. Природа сущностей (обязательный разбор)

1. **Природа — состояние.** Трюм — накопленный груз (результат-состояние), не происшествие и не лог. Записей — по одной на пару (игрок, товар); «событие добычи» логом не становится (это дело мини-игры пояса, не трюма).
2. **Владелец — один на игрока.** Один корабль = один трюм (решение создателя). Записи принадлежат игроку (`user_id`), не объекту мира.
3. **Расширяемость — таблица записей.** Разных товаров/ресурсов много (каталог 131+), поэтому **таблица записей** `player_cargo`, а не JSONB-поле в `users` (решение №4). Схема переживает второй тип записи без изменений.
4. **Ёмкость — производная, не сущность.** Врождённая ёмкость — параметр существующей модели корабля (`ship_models`, 77a §2); модуль — запись существующего справочника `equipment` (77a §3, новый тип `cargo`). Отдельной таблицы ёмкости не заводим: лимит = модель + установленные модули.
5. **Грузовой модуль — не новая сущность, а новый тип** существующего справочника `equipment` (как `engine_1` в 91a §7.1): CHECK `equipment.type` расширяется значением `cargo`.

**Правило «сущность с двумя экземплярами» в обе стороны:** груз — поток записей (таблица, не атрибут), ёмкость — атрибут модели и параметр предмета (не таблица). Второй тип груза (объём как лимит) влезает в ту же схему (§4).

---

## 4. Ёмкость: модель лимита и задел под объём

**Что должно быть на выходе (требование, не формула):**

1. У корабля есть **лимит по массе** (тонны) — врождённая ёмкость модели + сумма `params.capacity` установленных грузовых модулей.
2. Занятая масса = Σ (`player_cargo.quantity` × `goods.weight`) по всем строкам игрока.
3. Лимит и занятое отдаются **по осям** (сейчас одна ось — `mass`), чтобы добавление объёма вторым лимитом было аддитивной правкой.

**Задел под объём (решение №2 — «мелкая правка, не переделка»):**

- Лимит в коде и API — **не скаляр**, а **набор осей**: `CargoLimit{Mass float64}` сегодня → `CargoLimit{Mass, Volume float64}` завтра. Сервер отдаёт `limits: {"mass": {...}}`; добавление объёма = новая ось в ответе, существующие потребители не ломаются.
- Хранение: `ship_models.base_capacity` (тонны) — врождённая; модуль — `params.capacity` (тонны). При объёме добавятся `ship_models.base_capacity_volume` и `params.capacity_volume` — **миграция + поле + ось**, без переделки схемы `player_cargo`.
- `goods.volume` уже существует (`000048`, `NOT NULL DEFAULT 1` с `000054`) — данные под второй лимит готовы, сейчас **не читаются**.

**Отклонённый вариант (зафиксировано):** лимит как единый JSONB-объект `{"mass":..,"volume":..}` в `params`/`ship_models`. Цена: разбор чисел из JSONB (риск `float64` vs `int`, ловушка каталога) и неявная типизация; выгода — «совсем без миграции» — не стоит риска. Выбран **типизированный** путь (скаляр сейчас, ось в API).

---

## 5. Числа (предположение дизайнера; `@balancetester` следующий)

**На выходе должно быть:**

- Стартовая **суммарная** ёмкость корабля — **100 т** (согласуется с ранее названным «базовым значением корабля 100», идея поясов §Уточнения п.3).
- Врождённая ёмкость — **меньше** модуля (решение №1: «небольшая»).
- Ровно **1** универсальный слот (решение создателя).

**Предложение (гипотеза, не эталон):**

| Величина | Значение | Обоснование |
|---|---|---|
| Врождённая ёмкость `starter` (`ship_models.base_capacity`) | **20 т** | «небольшая»; **гипотеза**: делает модуль осмысленным шагом, но **не обязательным** — при 12 т/заход врождённых 20 т хватает на ~1.7 захода, петля работает и без модуля (замечание `@balancetester`); «обязательность» модуля зависит от чисел мини-игры пояса, которых ещё нет |
| Стартовый модуль `cargo_1` (`params.capacity`) | **80 т** | 20 + 80 = **100 т** — ровно «стартовое 100» из идеи поясов |
| **Стартовая суммарная ёмкость** | **100 т** | совпадает с «базовым значением корабля 100» |
| Уровни модулей (будущее) | `cargo_2` **200 т**, `cargo_3` **500 т** (шаг **×2.5**) | **не проектируется сейчас** — заимствуется **подход** радара 77a §4.1 (прогрессия уровней вернётся с рынками и ценами), но **не шаг**: у радара шаг **×1.5** (400/600/900/1400/2000), у cargo — **×2.5**; числа разные осознанно |

**Развилка «100 = врождённая или суммарная» — ✅ закрыта создателем (2026-09-22):** принят **вариант A** — 100 = **суммарная стартовая** (20 врождённая + 80 модуль); буквальное «базовое значение корабля 100» как врождённая отклонено (противоречило бы «небольшой врождённой»).

**Числа — предположение.** Кросс-проверка: сколько даёт один заход добычи в поясе — **не спроектировано** (этап 3 поясов, отдельная задача). Когда числа мини-игры появятся, сверить: заход не должен переполнять трюм «с запасом на 20 заходов» и не должен требовать 10 трюмов на заход. Ёмкость **не входит** в текущие числа экономики/населения — это лимит игрока, двойного учёта нет.

---

## 6. Масштаб веса (единица каталога → тонны)

**Соглашение (гипотеза, §17):**

- `goods.weight` — **масса одной единицы товара в тоннах**. Занятая масса = `quantity × weight`.
- `goods.volume` — **объём одной единицы в м³** (зарезервирован под будущий лимит, сейчас не читается).
- Сейчас **все** товары и ресурсы имеют `weight = 1` и `volume = 1` (дефолт миграции `000054`, `NOT NULL DEFAULT 1`) → **1 единица = 1 тонна**. Проверено в dev-БД: `goods` kind=resource, в т.ч. «Железо Fe» (`id 21`) — `weight = 1`.
- Калибровка реальных весов каталога (студия) — **отдельная задача**: трюм от неё не зависит, но числа ёмкости должны быть согласованы с весами, когда они появятся.

Смысл для первого потребителя: «заход добыл 12 единиц железа» → занято **12 т** из 100 т.

**Находки `@critic`/`@balancetester` (зафиксировано, не блокирует):**

- **Плотность при дефолтах.** При `weight = volume = 1` обе оси дают у **всех** товаров **плотность 1 т/м³** — в т.ч. у железа и у газов (у газов в реальности плотность на порядки ниже). Согласование с осью «Плотность» (`09_resources.md` §9.1.2, «плотность — верхний предел для брони и транспорта; масса груза: дороже возить, всегда») — **отдельная калибровка** каталога; трюм читает вес как есть и от неё не зависит.
- **Риск данных: `goods.weight` не имеет CHECK `> 0`.** Нулевой (или крошечный) вес = «бесконечный» трюм по массе. Инвариант держится на данных каталога, не на БД. **Пометка:** при добавлении объёма вторым лимитом — та же дыра; кандидат на CHECK `weight > 0`/`volume > 0` при следующей правке каталога (не входит в миграцию `000068`, каталог не трогаем).

---

## 7. Поведение при перегрузе (варианты + рекомендация)

| Вариант | Как | Цена | Итог |
|---|---|---|---|
| **A. Жёсткий предел (рекомендация)** | Запись клампится по свободной ёмкости: принимается `min(сколько дают, свободно)`, остаток **остаётся на источнике**. `used ≤ total` — инвариант. | Нет штрафов и новой механики; «перегруза» как состояния не существует | **рекомендуется** |
| **B. Мягкий перегруз со штрафом скорости** | Можно взять сверх ёмкости, скорость падает | Требует механики «скорость от загруженности» — замысел 77a §1.3, **не реализован** (двигатель даёт константу 0.3); это отдельная фича | отложено |
| **C. Гибрид** | Жёсткий предел для добычи, штраф для контрактов | Две модели в одной фиче — сложнее объяснить | отклонено |

**Принято: A.** Запись **частичная** (fill-to-capacity) — для добычи это естественно («набрал полный трюм — домой»), а не «отказ целиком». Для транзакций, которым нужна атомарность (контракт/торговля — будущее), потребитель сам решает: взять меньше или отказаться; трюм предоставляет примитив, а не политику.

**Скорость от загруженности** — не входит (§12), кандидат на будущее (замысел 77a §1.3).

### 7.1. Груз за борт — сброс своего груза (решение создателя №3, гейт 2026-09-22)

Игрок может **выбросить груз за борт** — освободить трюм, отказавшись от содержимого.

- **Единственная player-facing запись трюма.** Все прочие записи — внутренний сервис (§9.2). Сброс **только уменьшает** — создать ценность нельзя (нельзя «нарисовать» груз), поэтому это безопасная ручка.
- **Сброшенное просто исчезает.** Объект-контейнер в космосе **не создаём** (нет сущности «груз в пустоте»); кандидат на будущее — контейнер/пиратская добыча (§16).
- **Инварианты держатся:** `used ≤ total` (сброс только уменьшает `used`), строка с нулём **удаляется**, действие — на своём корабле **в любом месте** (трюм не привязан к позиции; порт не нужен — §12).
- **Форма:** `good_id` + количество, либо «всё» (весь трюм / весь этот товар). Точные варианты «всё» — при разработке (кнопка «выбросить всё» в блоке трюма дашборда, §9.3).

---

## 8. Схема БД (миграция `000068`)

**Номер:** **`000068`** — перебронирован на гейте 2026-09-22: указанный менеджером `000067` **занят** (`000067_settlement_type.sql`), `000066` занят (`000066_factions_race.sql`); сверено свежим листом `migrations/`. `000060` забронирован спекой компаньонов, `000063` свободен. Строка в `docs/DB.md` — дельта `@dispatcher`. Миграция **самостоятельная**, чужих ручек не трогает, каталог (`goods`) не меняет (риск `weight > 0` — §6, отдельная правка каталога).

### 8.1. Содержимое миграции

```sql
-- 000068: трюм — грузоподъёмность корабля (спека 2026-09-22-трюм-грузоподъёмность-корабля)

-- 1. Расширить CHECK типа оборудования: + cargo
ALTER TABLE equipment DROP CONSTRAINT equipment_type_check;
ALTER TABLE equipment ADD CONSTRAINT equipment_type_check
    CHECK (type IN ('radar','scanner','engine','cargo'));

-- 2. Врождённая ёмкость модели корабля (тонны)
ALTER TABLE ship_models ADD COLUMN base_capacity DOUBLE PRECISION NOT NULL DEFAULT 0;

-- 3. Стартовая модель: врождённая ёмкость + универсальный слот
UPDATE ship_models
SET base_capacity = 20,
    slots = slots || '{"universal": 1}'::jsonb
WHERE id = 'starter';

-- 4. Справочник: стартовый грузовой модуль
INSERT INTO equipment (id, type, name, params)
VALUES ('cargo_1', 'cargo', 'Грузовой модуль-1', '{"capacity": 80}')
ON CONFLICT (id) DO NOTHING;

-- 5. Содержимое трюма
CREATE TABLE player_cargo (
    user_id    UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    good_id    BIGINT NOT NULL REFERENCES goods (id) ON DELETE CASCADE,
    quantity   DOUBLE PRECISION NOT NULL DEFAULT 0 CHECK (quantity >= 0),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (user_id, good_id)
);

-- 6. Бэкфилл существующих игроков: стартовый модуль в универсальный слот
UPDATE users
SET equipment = jsonb_set(COALESCE(equipment, '{}'::jsonb), '{universal}', '"cargo_1"')
WHERE equipment IS NULL
   OR NOT (equipment ? 'universal')
   OR equipment->>'universal' IS NULL
   OR equipment->>'universal' = '';
```

### 8.2. Решения по схеме

- **`quantity` — `DOUBLE PRECISION`:** вес товара — `double` (`000048`), добыча может давать дробные количества; `CHECK (quantity >= 0)`. Строка с нулём **удаляется** (нулевых строк не держим).
- **Ключ `(user_id, good_id)`** — PK; запрос «весь груз игрока» покрыт префиксом PK, отдельный индекс не нужен.
- **`goods` FK `ON DELETE CASCADE` — ✅ принято создателем (решение №7, гейт 2026-09-22).** Удаление записи каталога в студии сносит её груз (как `deposits`/`settlement_branch_buffers`) — риск принят осознанно. Предупреждение в студии числом (`GET /studio/api/goods/{id}/cargo-count`, по образцу) — **необязательный бэклог**, не в скоупе.
- **`users` FK `ON DELETE CASCADE`:** удаление игрока чистит трюм.
- **`truncateTables` — НЕ включать (И5, главное решение).** `player_cargo` не ссылается ни на `worlds`, ни на `planets` → сторож `admin_universe_test.go` (таблицы с FK на усекаемые обязаны быть в списке) не срабатывает. Трюм — **состояние игрока**, переживает очистку вселенной, как кошелёк (`accounts`) и активный полёт (`player_flights`). **Пометить осознанным решением** (§17.1, закрыто).
- **`updated_at`** пишет приложение при записи (как `buildings`); триггеров в проекте нет.

### 8.3. Код (дельта `@developer`, не код в этой спеке)

- `internal/models/ship.go`: `ShipModel.BaseCapacity`; константа `StarterCargoModuleID = "cargo_1"`; `StarterEquipment` + `"universal": "cargo_1"`.
- `internal/ship/models.go`: `defaultShipModels` — `base_capacity` 20 + `slots.universal` 1; `LoadModels` читает `base_capacity`.
- `internal/ship/catalog.go`: `defaultEquipment` + `cargo_1`; функция `CargoCapacityMass(modelID, equipment)` (по образцу `RadarRadius`/`EngineSpeed`) — врождённая + сумма `params.capacity` установленных `cargo`-модулей; безопасное чтение при битом `params`.
- Репозиторий/сервис трюма (новый, напр. `internal/cargo/`): `GetCargo(userID)`, `TryAddCargo(userID, goodID, qty) float64` (кламп по свободному), `TakeCargo(userID, goodID, qty) float64` (удаляет строку при нуле), `JettisonCargo(userID, goodID, qty|all) float64` (сброс за борт, только уменьшает — §7.1/§9.2), `CargoMass(userID)`.
- Обработчик `POST /api/cargo/jettison` (`internal/handlers/`) — единственная player-facing запись, поверх `JettisonCargo`.

---

## 9. API и интерфейс

### 9.1. Чтение — `GET /api/cargo` (JWT, `player`)

Единственный источник данных о трюме (не дублировать в `/me`: груз меняется часто, `/me` — редко меняющийся профиль). Отдаёт лимиты **по осям** (задел под объём, §4):

```json
{
  "limits": { "mass": { "used": 12.0, "total": 100.0 } },
  "items": [
    { "good_id": 21, "name": "Железо Fe", "kind": "resource",
      "quantity": 12.0, "weight": 1.0, "mass": 12.0 }
  ]
}
```

- `used`/`mass` считает **сервер** (И2 «один факт — одно место»): клиент не перемножает вес сам.
- `used ≤ total` — инвариант по построению (запись клампится).
- `total` = `CargoCapacityMass` (врождённая + модули). Админ/skycomposer — то же чтение (своего трюма; видимости тут нет).

### 9.2. Запись — внутренний сервис; единственная player-facing — сброс

**Ручки записи «положить груз» не вводим** — иначе игрок «нарисует» себе любой груз. Пополнение — только из игровой логики через сервис трюма (§8.3): добыча пояса, позже — контракты/торговля. Это серверно-авторитетное правило (инвариант проекта: клиент не распоряжается состоянием).

**Единственная player-facing запись — «груз за борт»** (решение создателя №3, §7.1): она **только уменьшает** и ценности создать не может.

**`POST /api/cargo/jettison`** (JWT, `player`), тело:

```json
{ "good_id": 21, "quantity": 12.0 }   // либо { "good_id": 21, "all": true } / { "all": true } — весь трюм
```

- Ответ — обновлённый трюм (та же форма, что `GET /api/cargo`, §9.1) или `204` + повторный `GET` (решить при разработке).
- Валидация: `quantity > 0` (или `all`); `good_id` существует в каталоге; списывается **не больше, чем есть** (кламп; строка с нулём удаляется).
- Инвариант `used ≤ total` держится (уменьшение). Действие — на своём корабле, в любом месте (порт не нужен).
- **Сброшенное исчезает** — объект-контейнер не создаётся (§7.1); кандидат на будущее (§16).

### 9.3. Дашборд — раздел «Корабль» (91a)

Блок «Трюм» в существующем разделе (91a §3.1): **занято/всего** (полоса `used/total`) + список строк «товар — количество — масса» + действие **«выбросить за борт»** (по строке и «выбросить всё», §7.1). Данные — `GET /api/cargo` при открытии вкладки «Корабль» и после действий, меняющих груз (добыча, сброс).

**`@uidesigner` — не нужен отдельным шагом для v1:** блок аддитивный, по образцу существующих карточек раздела (полоса + список + кнопка сброса), рисуется при разработке. Если создатель захочет визуально богаче (иконки ресурсов, сортировка, подтверждение сброса с предупреждением) — отдельный шаг `@uidesigner` (`docs/VECTORS.md` §5).

---

## 10. Разворот подходов (история; что отклонено и почему)

| Развилка | Варианты | Принято | Цена принятого |
|---|---|---|---|
| Где хранить ёмкость | (A) колонка `ship_models.base_capacity` + `params.capacity`; (B) JSONB-объект осей; (C) отдельная таблица ёмкости | **(A)** | при объёме — маленькая миграция + поле (не переделка) |
| Природа лимита | (A) скаляр в БД, оси в API/коде; (B) скаляр везде | **(A)** | чуть больше кода на оси; зато объём — аддитивно |
| Содержимое | (A) таблица `player_cargo`; (B) JSONB в `users` | **(A)** — решение создателя №4 | JOIN к `goods` при чтении (дёшево) |
| Перегруз | (A) жёсткий предел; (B) мягкий + штраф скорости; (C) гибрид | **(A)** | «перегруза» как состояния нет; штраф — будущее |
| Запись груза | (A) внутренний сервис; (B) HTTP-ручка | **(A)** + единственная player-facing — сброс за борт (§7.1) | пополнение нельзя «снаружи»; сброс безопасен (только уменьшает) |
| Груз за борт | (A) сброшенное исчезает; (B) контейнер в космосе | **(A)** — решение создателя №3 | контейнер/пиратская добыча — кандидат на будущее (§16) |
| Слот | (A) тип слота `universal`; (B) частный слот `cargo` сейчас | **(A)** — решение №1 «универсальные слоты» | чуть больше обобщения сразу |

---

## 11. Инварианты и режимы отказа

**Инварианты:**

- **И1 (масса — единственный лимит сейчас):** `used ≤ total` по оси `mass`; объём не читается.
- **И2 (сервер — источник чисел):** `used`/`total`/`mass` считает сервер; клиент не перемножает вес.
- **И3 (нет новых полей груза):** масса — из `goods.weight`; новых колонок веса/объёма не заводим.
- **И4 (ёмкость — производная):** лимит = `base_capacity` модели + Σ `params.capacity` установленных `cargo`-модулей; отдельно не хранится.
- **И5 (трюм — состояние игрока):** не в `truncateTables`, переживает очистку вселенной (как кошелёк).
- **И6 (не-регрессия 77a/91a):** существующие типы оборудования, `users.equipment`, слоты радар/сканер/двигатель, `/me` — не меняются; расширение аддитивное.
- **И7 (задел под объём):** лимит — по осям в API и коде; `goods.volume` не удалять и не переиспользовать под массу.
- **И8 (сброс только уменьшает):** «груз за борт» — единственная player-facing запись; ценности создать нельзя, `used` только падает; сброшенное исчезает (объекта в космосе нет).

**Режимы отказа:**

- **Каталог не загрузился** (битый `params`) — `params.capacity` не читается → модуль «пустой», ёмкость = только врождённая (безопасный отказ, не бесконечный трюм).
- **Удаление товара из студии** — CASCADE сносит его груз; риск **принят создателем** (решение №7), предупреждение студии — необязательный бэклог (§8.2).
- **`goods.weight = 0` (нет CHECK `> 0`)** — «бесконечный» трюм; риск данных, не блокирует, помечен (§6).
- **Клиент показывает больше, чем влезает** — невозможно: сервер клампит запись и отдаёт `used ≤ total` (И2).
- **Сброс груза «в никуда»** — штатное поведение (сброшенное исчезает, §7.1); UI-подтверждение — при разработке (косметика).
- **Запрос груза без знания/радиуса** — трюм не под видимостью (это личное состояние, как `/me`), ограничений нет; чужие трюмы не отдаются вообще.

---

## 12. Что решается сейчас / что отложено

**Сейчас (скоуп):** врождённая ёмкость модели; тип `cargo` + стартовый модуль в универсальном слоте (1); таблица `player_cargo`; чтение `GET /api/cargo`; внутренний сервис записи (add/take); **«груз за борт»** — `POST /api/cargo/jettison` + кнопка сброса в блоке трюма (решение №3); блок «Трюм» в разделе «Корабль»; миграция **`000068`**; каскады/тесты. **Грузовой модуль выдаётся стартовым набором** (решение №6) — производство позже.

**Отложено (вне скоупа, явно):**

- **Объём как второй лимит** (`goods.volume`, `base_capacity_volume`, `params.capacity_volume`) — задел заложен (§4).
- **Типы груза** (твёрдое/жидкое/газ, «тип хранения» X4) — кандидат на будущее.
- **Уровни грузовых модулей** (прогрессия, цены) — вернутся с рынками (подход радара 77a §4.1; шаг у cargo ×2.5, §5).
- **Производство грузового модуля** — позже в **лабораториях корабельного оборудования** (решение №6); сейчас модуль только выдаётся.
- **Контейнер / пиратская добыча сброшенного груза** — кандидат на будущее (§16); сейчас сброшенное исчезает.
- **Скорость от загруженности** — замысел 77a §1.3, отдельная фича.
- **Торговля/рынки, погрузка/выгрузка в порту, NPC-перевозчики** — будущие потребители.
- **Сама добыча в поясе** (мини-игра) — следующая задача линии (первый потребитель).
- **Температура хранилища/трюма** (`09_resources.md` §9.1.2, вопрос 99.4) — открыт, не здесь.
- **Предупреждение студии числом о грузе при удалении ресурса** — необязательный бэклог (§8.2).

---

## 13. Связи механик (impact map)

> Дельта `@manager` в `docs/impact_map.json`. Новые сущности-механики — `type: "mechanic"`.

| Сущность | Связь | Куда | Почему |
|---|---|---|---|
| `player_cargo` | schema | `goods` | FK `good_id` CASCADE: удаление записи каталога сносит её груз |
| `player_cargo` | schema | `db:users` | FK `user_id` CASCADE: удаление игрока чистит трюм |
| `cargo_capacity` (новая механика) | reads | `ship_models` | врождённая ёмкость `base_capacity` |
| `cargo_capacity` | reads | `equipment` | `params.capacity` установленных модулей типа `cargo` |
| `cargo_capacity` | reads | `db:users.equipment` | какие модули стоят в слотах (в т.ч. `universal`) |
| `cargo_capacity` | reads | `player_cargo` | занятая масса = Σ `quantity × goods.weight` |
| `cargo_capacity` | affects | `travel` | кандидат: скорость от загруженности (77a §1.3) — **не сейчас** |
| `equipment` | affects | `migrations/000068` | CHECK `type` расширен значением `cargo` |
| `player_cargo` | reads | `goods` | `weight` — масса единицы (масштаб §6); риск `weight = 0` без CHECK (§6) |
| дашборд «Корабль» (91a) | affects | `GET /api/cargo`, `POST /api/cargo/jettison` | новый блок «Трюм» + сброс за борт |
| добыча в поясе (идея) | writes | `player_cargo` | первый потребитель: кладёт добытое. **Стык с поясом:** на этапе 3 — **только железо-заглушка**; `data.resources` пояса (`§5.2` спеки поясов) — **задел**, а **не источник состава трюма** (трюм хранит то, что реально положил потребитель) |
| контракты (`contract_requirements`) | reads | `cargo_capacity` | будущее требование-окно «трюм N т». **Будущий кросс-чек:** числа контрактов/торговли сверить с ёмкостью в **том же порядке**, что 100 т (чтобы требование «трюм N т» не оказалось недостижимым при стартовой ёмкости) |
| торговля/рынки (будущее) | reads/writes | `player_cargo` | будущий потребитель: погрузка/выгрузка через сервис трюма |
| 77a | duplicates | `equipment`, `ship_models` | типы оборудования/модель описаны в 77a; новый тип — расширение |
| 91a | reads | `equipment`, `ship_models` | раздел «Корабль» показывает модули/ёмкость |

---

## 14. DoD и тесты (TDD: красный → зелёный)

- **Миграция `000068`** применяется на старте (`migrations.Apply`); на свежей БД — те же данные, что сид-дефолты кода.
- **Тесты (предложение):**
  1. `CargoCapacityMass` = врождённая + модуль (20 + 80 = 100); без модуля = 20; битый `params` → врождённая.
  2. `TryAddCargo` клампит: при 95/100 и запросе 20 принимает 5, остаток не списывается у источника.
  3. `TakeCargo` до нуля удаляет строку; нулевых строк нет.
  4. **`JettisonCargo`** уменьшает только своё: списывает ≤ имеющегося, `used` падает, `used ≤ total` держится, строка с нулём удаляется; `all` сбрасывает весь товар/весь трюм.
  5. Занятая масса = Σ `quantity × weight` (при `weight = 1` совпадает с суммой quantity).
  6. Каскады: удаление `goods`/`users` чистит `player_cargo` (миграционный тест/интеграция).
  7. `player_cargo` **отсутствует** в `truncateTables` и не ломает сторож `admin_universe_test.go` (нет FK на усекаемые).
  8. `GET /api/cargo` — форма ответа (`limits.mass`, `items`), `used ≤ total`; `POST /api/cargo/jettison` — валидация (`quantity > 0`/`all`, кламп).
  9. Свежая регистрация: `equipment.universal = cargo_1`, стартовая ёмкость 100.
- **Definition of done:** `go build ./...`, `go vet ./...`, `go test -race -short (go list ./... | ? { $_ -notmatch 'generator/(planet|galaxy)$' })`. Генератор не трогаем → тяжёлый прогон не нужен. Перезапуск сервера (миграция, `AGENTS.md` §4.13) + проверка `/health`.
- **QA-прогон** — отдельный чек-лист (`docs/QA/checklists/`), ведёт `@manager`/`@tester`.

---

## 15. Дельта доков (разнос)

| Док | Дельта | Кому |
|---|---|---|
| `docs/specs/README.md` | строка манифеста (сделано автором спеки) | `@designer` |
| `docs/DB.md` | строка миграции **`000068`** + таблица `player_cargo`, колонка `ship_models.base_capacity`, тип `cargo` | `@dispatcher` (дельта менеджеру) |
| `docs/impact_map.json` | узлы/рёбра §13 | `@manager` |
| `docs/GLOSSARY.md` | §1 «Слот» — **4-е и 5-е значения** («Слот корабля», «Универсальный слот»; «слот корабля» в глоссарии ещё не заведён); §5 — Трюм, Грузоподъёмность; §6 — Грузовой модуль (ссылка на 77a/`21_ships.md`) | `@dispatcher` (дельта менеджеру) |
| `99_roadmap.md` | строка блока (по решению менеджера) | `@manager` |
| `docs/gamedesign/21_ships.md` | **дельта (решение создателя №5, правило 99a):** снять/переформулировать «тех-характеристики корабля — далёкое будущее» (§21.5, И5) **применительно к грузу/ёмкости** — грузоподъёмность входит в скоуп («приходим уже к далёкому будущему»); косметика (спрайт/цвет) по-прежнему на механику не влияет | `@dispatcher` (дельта менеджеру) |
| `docs/gamedesign/03_planets.md` | **не требуется** (трюм не трогает планеты) | — |
| `docs/gamedesign/09_resources.md` §9.1.2 | **не требуется**: вопрос температуры трюма остаётся открытым; плотность `1 т/м³` при дефолтах и согласование с осью «Плотность» — отдельная калибровка (§6) | — |
| `docs/gamedesign/faq_counter.md` | **не двигаем**: числа трюма — дизайн игрока, не эталон физики/мира. Если создатель зафиксирует масштаб «1 единица = weight тонн» как эталон — строка в FAQ, дельта менеджеру | — |

---

## 16. Кандидаты новых ГД-сущностей (Вопрос → Ответ; в текущие числа не входят)

**Кандидат 1 — «манифест груза».**
**Вопрос:** нужен ли **«манифест груза»** — состав трюма корабля, видимый другим игрокам в радиусе радара (связка с «чужим кораблём» 77a §5.3 и транспондером 77b)?
**Ответ:** эталон не задан (2026-09-22) — **открытый вопрос**; в текущие числа и в схему **не входит**.
Зачем: даёт трюму вторую жизнь помимо логистики — разведку/пиратство/торговлю «на глаз» (увидел, что везут, — решил, перехватить или предложить сделку).

**Кандидат 2 — «контейнер сброшенного груза» (следствие решения №3).**
**Вопрос:** становится ли сброшенный за борт груз **объектом в космосе** (контейнер, который можно подобрать/отобрать), или исчезает?
**Ответ:** на v1 **исчезает** (решение №3); «контейнер / пиратская добыча» — **кандидат на будущее**, в числа не входит.
Зачем: превращает сброс в игровое событие (брошенный груз = находка/приманка), но требует сущности «груз в пустоте», привязки к позиции и правил подбора.

**Альтернатива по теме:** **«ангар игрока»** — стационарный склад на планете, куда груз выгружается (даёт петле «сдачу», но пересекается с классом построек «склад», `GLOSSARY.md` §5). Кандидат, отсев за создателем.

**«Два дома» грузового модуля — не конфликт на v1 (решение №6, снято как находка критика).**
Существующий «Модуль корабля» в каталоге **предметов** (`items`, `000048`) — **тестовый**; v1 работает через `equipment` (`cargo_1`, стартовый набор). Настоящий грузовой модуль позже **производится в лабораториях корабельного оборудования** (будущая связь `items`/`producer_types` ↔ `equipment`). Сейчас два «дома» сосуществуют как тестовый предмет и рабочий справочник — конфликта нет; сведение — задача будущего производства (§12).

---

## 17. Открытые вопросы к создателю

### 17.1. ✅ Закрыто гейтом 2026-09-22

| # | Вопрос | Решение |
|---|---|---|
| 1 | Число «100» — врождённая или суммарная ёмкость? | **Вариант A:** 100 = **суммарная стартовая** (врождённая 20 + модуль 80) |
| 5 | Судьба груза при очистке вселенной | **Переживает** (как кошелёк `accounts` и активный полёт `player_flights`) — И5, главное решение |

Гейт также **добавил в скоуп** «груз за борт» (решение №3, §7.1/§9.2), подтвердил **тех-характеристики в скоупе** (№5, §15) и **стартовую выдачу модуля** (№6, §16); риск **CASCADE принят** (№7, §8.2).

### 17.2. Остаются открытыми (не блокируют разработку; рекомендации дизайнера)

1. **Уровни грузовых модулей сейчас?** (A, рекомендация) один `cargo_1` (80 т), прогрессия — с рынками (подход 77a, шаг ×2.5, §5); (B) сразу лестница 80/200/500.
2. **Перегруз:** (A, рекомендация) жёсткий предел с частичной погрузкой; (B) мягкий перегруз со штрафом скорости (нужна механика загруженности, 77a §1.3).
3. **Частичная погрузка:** (A, рекомендация) «берём сколько влезет, остаток на источнике»; (B) «отказ целиком, если не влезает».
4. **Универсальный слот:** (A, рекомендация, следует из решения №1) тип слота `universal` (`slots.universal`, ключ `users.equipment`); (B) частный слот `cargo` сейчас, обобщение позже.
5. **Масштаб веса:** (A, рекомендация) `goods.weight` — тонны на единицу (сейчас все веса = 1 → 1 ед. = 1 т); (B) свой масштаб (кг/«единица — не тонна»). Калибровка весов каталога (и ось «Плотность», §6) — отдельная задача?
6. **Дашборд:** достаточно ли «полоса занято/всего + список строк + сброс за борт», или нужен отдельный шаг `@uidesigner` (иконки ресурсов, сортировка, подтверждение сброса)?

---

## 18. Сверка с `@writer`/`@scientist`

**Пакет не передан** — сверка не требуется. Обоснование: фича не вводит чисел физики/мира (ёмкость — игровой лимит, вес — данные каталога) и не влияет на формулы экономики/населения. Единственная «физическая» точка — масштаб «единица → тонна» (§6), это соглашение каталога, не эталон мира; при желании создателя фиксируется как эталон (тогда дельта FAQ, §15).

---

## 19. История итераций (коротко)

- **Принято из решений создателя (2026-09-22):** врождённая ёмкость + универсальные слоты (1 слот); только масса; минимум; отдельная таблица содержимого.
- **Гейт 2026-09-22 (пакет правок):** числа закрыты (20 + 80 = 100); «груз за борт» в скоупе (`POST /api/cargo/jettison`, единственная player-facing запись); тех-характеристики корабля в скоупе (дельта `21_ships.md`); грузовой модуль — стартовый набор, производство позже («два дома» — не конфликт на v1); CASCADE принят; миграция перебронирована `000066`/`000067` → **`000068`** (сверено свежим листом `migrations/`).
- **Принято дизайнером (гипотезы, ждут гейта):** лимит по осям с типизированным хранением (задел под объём); жёсткий предел с частичной погрузкой; запись — внутренний сервис + сброс, чтение — `GET /api/cargo`; `player_cargo` переживает очистку вселенной; `@uidesigner` не нужен для v1.
- **Отклонено:** JSONB-объект осей лимита (разбор чисел, неявная типизация); мягкий перегруз со штрафом скорости (механика не реализована); гибрид перегруза; HTTP-ручка **пополнения** груза (игрок «нарисует» груз); контейнер сброшенного груза на v1; частный слот `cargo` вместо универсального (противоречит решению №1).
- **Отложено:** объём вторым лимитом, типы груза, уровни модулей, производство модуля в лабораториях, контейнер/пиратская добыча, скорость от загруженности, торговля/порты/NPC-перевозчики, сама добыча в поясе.

---

## 20. Дельта 2026-09-24 — модуль +30 т, три универсальных слота, видимость слота

> **Источник:** идея `docs/gamedesign/ideas/2026-09-24_трюм-30т-три-универсальных-слота-видимость.md`
> (решения создателя 2026-09-24). **Правка хирургическая:** основной текст §0–§19 **не переписывается**;
> ниже — три решения и **явные пометки пересмотра** прежних чисел/формулировок. Где §20 противоречит
> §0–§19, **действует §20** (прежние числа остаются как история решения).
> **Статус:** готова к проверке создателем.

### 20.1. Решения создателя (2026-09-24)

1. **Значение существующего модуля `cargo_1`: `params.capacity` 80 → 30 т.** Правка **глобальная** — у всех,
   у кого модуль установлен, ёмкость становится **20 (врождённая `base_capacity`) + 30 = 50 т** (было 100 т).
   **Новый модуль не заводится**, каталог `equipment` расширяется только значением.
2. **Универсальных слотов у модели `starter`: 1 → 3.** Два новых слота — **пустые** (задел под будущие
   модули: интерфейса установки/снятия модулей пока нет).
3. **Универсальные слоты видимы** в разделе «Корабль» (сейчас `renderShipSection` рисует только
   радар/сканер/двигатель; универсальный слот и его содержимое не отображаются — отсюда ощущение
   «модуля нет» при ёмкости 100 т).

**Диагноз (подтверждён):** бага в расчёте нет — `CargoCapacityMass` корректно суммирует врождённую ёмкость
и `params.capacity` всех `cargo`-модулей (§11 И4). «Модуль не установлен» — **дефект отображения**, а не данных.

### 20.2. Конвенция ключей универсальных слотов (решение дизайна)

`users.equipment` — **плоская JSONB-карта** со скалярными ключами
(`{"radar":"radar_1","scanner":"scanner_1","engine":"engine_1","universal":"cargo_1"}`, §8.3/77a §3.3).
Обратная совместимость обязательна: первый слот остаётся **`universal`** (иначе ломается бэкфилл `000068`
и существующие тесты).

**Принято: `universal` / `universal2` / `universal3`.**

- **Почему не массив** (`"universal": ["cargo_1", null, null]`): сломает существующий бэкфилл `000068`
  (`equipment ? 'universal'` / `->>'universal'`), `StarterEquipment` и тесты; потребует разбора
  «скаляр или массив» в `CargoCapacityMass` и в клиенте — цена выше выгоды на v1.
- **Почему суффикс-число, а не `universal_1…3`:** `universal` (без цифры) — уже занятый ключ в БД
  и коде; сохранение его первым даёт **аддитивную** миграцию (добавляются только `universal2`/`universal3`).
- **Нумерация — 1-based по позиции** (`universal` = слот 1, `universal2` = слот 2, `universal3` = слот 3);
  порядок отрисовки — фиксированный (слот 1 → 2 → 3).
- **Расширяемость:** конвенция переживает второй/третий тип модуля в тех же слотах (ключ описывает
  **позицию**, не тип). Когда появится интерфейс установки, ключи не меняются — меняются только значения.
  **Отклонено:** ключ по типу модуля (`cargo`/`cargo2`) — слот универсальный, тип в ключе противоречит §10
  («слот `universal`») и ломается при смене типа модуля в позиции.

**Чтение:** `CargoCapacityMass` уже перебирает **все** значения `equipment` (§8.3) — новые ключи
подхватываются **без правки кода**; достаточно, чтобы значения были строками-id модулей.

### 20.3. Бэкфилл БД — ключи `universal2`/`universal3` не заводим

**Решение: `users.equipment` новых ключей НЕ получает.** Достаточно `ship_models.slots.universal = 3`.

- **Обоснование:** `CargoCapacityMass` итерирует значения карты, отсутствие ключа = пустой слот
  (вклад 0) — **отсутствие ключа и `null` эквивалентны** по эффекту. Пустые слоты — **не данные игрока**,
  а **форма модели** (`ship_models.slots`): хранить «пустоту» в `users.equipment` = дублировать форму модели
  в состоянии игрока (нарушение «один факт — одно место»).
- **Совместимость:** существующие игроки (ключ `universal` = `cargo_1`) и новые (тот же `StarterEquipment`)
  получают 3 слота **через модель** — бэкфилл `users` не нужен.
- **Что бэкфилл всё-таки делает (миграция `000080`):** только `params.capacity` модуля (каталог) и
  `slots` модели — см. §20.5. **Отклонено:** проставлять `"universal2": null` / `"universal3": null` —
  шум в JSONB, лишняя миграция данных, дубль формы модели.

### 20.4. UI — отрисовка универсальных слотов (раздел «Корабль»)

**Данные уже есть в `/me`** (серверную часть трогать не нужно): `ship_model.slots` (форма),
`equipment` (что стоит), `ship_catalog` (имя и `params` модуля).

**Что меняется в `web/index.html` (`renderShipSection`, спека 91a §3–§6):**

1. **Число универсальных ячеек — из `data.ship_model.slots.universal`** (после дельты = 3), а не хардкод.
   Отрисовка — цикл `i = 1..N`; ключ слота `i === 1 ? 'universal' : 'universal' + i` (§20.2).
2. **Судьба заглушек `.ship-slot-future` (`web/index.html:175–178`, 4 декоративные ячейки по бортам):**
   **удаляются** из разметки (вместе со стилем `.ship-slot-future`, строка 39) — иначе получится
   **визуальный дубль** пустых ячеек (3 реальных универсальных + 4 заглушки). Универсальные ячейки
   получают **собственные id** (`slot-universal`, `slot-universal2`, `slot-universal3`) и позиции на силуэте;
   радар/сканер/двигатель остаются на своих местах (`slot-radar`/`slot-scanner`/`slot-engine`).
   Если 3 универсальных ячейки не размещаются на силуэте без наложения на существующие — позиции
   подбираются при разработке (косметика, решение дизайнера; требование — **без наложения и без дублей**).
3. **Содержимое ячейки (по образцу существующих слотов):**
   - **занята** (ключ есть, id найден в `ship_catalog`, `type === 'cargo'`): иконка 📦 + **имя модуля**
     («Грузовой модуль-1») + **бонус** «+30 т» (из `params.capacity`; формат — по образцу
     `slotDetail` для радара/сканера/двигателя);
   - **пуста** (ключа нет / `null` / id не найден / не-`cargo`): иконка 📦 + **«не установлен»**
     (та же формулировка, что у радар/сканер/двигатель).
4. **`slotDetail`** дополняется веткой `cargo`: `+{params.capacity} т` (число — с сервера, И2;
   клиент не перемножает и не суммирует).
5. **Сводка характеристик** (`#ship-section-stats`) — без изменений: строка «Грузоподъёмность» там
   **не заводится** (она уже есть в блоке «Трюм», §9.3) — иначе дубль показателя.

**Инварианты UI:** показываем **только установленное** (И1 91a) + **форму модели** (пустые слоты — как
«не установлен»); каталог «что бывает» не рисуем.

### 20.5. Миграция `000080` (номер забронирован менеджером)

Сверено свежим листом `migrations/`: последняя в дереве — **`000077`** (`000077_settlement_stages_ladder.sql`);
`000078` занят спекой льда, `000079` — спекой потребления. Миграция **самостоятельная**, чужих ручек не трогает.

```sql
-- 000080: трюм — дельта (спека 2026-09-22-трюм-грузоподъёмность-корабля §20)
-- Решения создателя 2026-09-24: модуль cargo_1 80 → 30 т (старт 20 + 30 = 50 т);
-- универсальных слотов 1 → 3 (два новых пустые). Ключи universal2/universal3
-- в users.equipment НЕ заводим (пустой слот = отсутствие ключа, §20.3);
-- перегруженные трюмы обрезаются до новой ёмкости (§20.8 п.4).

-- 1. Значение существующего модуля: 80 → 30 т (глобально, у всех, у кого он стоит).
UPDATE equipment
SET params = jsonb_set(params, '{capacity}', '30')
WHERE id = 'cargo_1';

-- 2. Стартовая модель: три универсальных слота (1 → 3).
UPDATE ship_models
SET slots = slots || '{"universal": 3}'::jsonb
WHERE id = 'starter';

-- 3. Обрезка перегруженных трюмов до новой ёмкости (used ≤ total восстанавливается
--    для всех, а не только для новых записей). Ёмкость игрока = ship_models.base_capacity
--    + Σ params.capacity установленных cargo-модулей — та же формула, что
--    ship.CargoCapacityMass (§8.3, §11 И4). Обрезка детерминированная и идемпотентная:
--    строки идут в порядке good_id, масса набирается кумулятивно; «хвост» сверх
--    ёмкости удаляется, пограничная строка урезается до влезающего остатка
--    (лишнее убирается), строки с нулём удаляются (конвенция «строк с нулём нет», §8).
--    ВАЖНО: шаг идёт ПОСЛЕ шагов 1–2 — ёмкость считается уже по новым числам
--    (cargo_1 = 30 т), иначе обрезка считалась бы по старой ёмкости (100 т).
WITH cap AS (
    SELECT u.id AS user_id,
           COALESCE(sm.base_capacity, 0) + COALESCE((
               SELECT SUM((e.params->>'capacity')::double precision)
                 FROM jsonb_each_text(u.equipment) AS eq(key, module_id)
                 JOIN equipment e ON e.id = eq.module_id
                WHERE e.type = 'cargo'
                  AND (e.params->>'capacity') ~ '^[0-9]+(\.[0-9]+)?$'
           ), 0) AS total
      FROM users u
      LEFT JOIN ship_models sm ON sm.id = u.ship_model_id
),
walk AS (
    SELECT pc.user_id, pc.good_id, pc.quantity, g.weight, c.total,
           COALESCE(SUM(pc.quantity * g.weight) OVER (
               PARTITION BY pc.user_id ORDER BY pc.good_id
               ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING), 0) AS mass_before
      FROM player_cargo pc
      JOIN goods g ON g.id = pc.good_id
      JOIN cap c ON c.user_id = pc.user_id
),
tail AS (
    DELETE FROM player_cargo pc
     USING walk w
     WHERE pc.user_id = w.user_id AND pc.good_id = w.good_id
       AND w.mass_before >= w.total
)
UPDATE player_cargo pc
   SET quantity = (w.total - w.mass_before) / w.weight,
       updated_at = NOW()
  FROM walk w
 WHERE pc.user_id = w.user_id AND pc.good_id = w.good_id
   AND w.mass_before < w.total
   AND w.mass_before + w.quantity * w.weight > w.total;

-- 3b. Строки, усечённые до нуля, удаляются (конвенция «строк с нулём нет», §8/DB.md).
DELETE FROM player_cargo WHERE quantity = 0;
```

- **Все три шага идемпотентны** (повторный прогон не меняет результат): значения `params`/`slots`
  пишутся через `jsonb_set`/`||`, а обрезка после первого прогона не находит ни строк за ёмкостью
  (`mass_before < total` для всех), ни пограничных (`mass_after ≤ total`).
- **Порядок обязателен:** шаги 1–2 (каталог/модель) **до** обрезки (шаг 3) — иначе `total` посчитается
  по старой ёмкости (100 т) и перегруза не найдёт.
- **Точность:** пограничная строка урезается до `(total − mass_before)/weight`; возможный ULP-перелёт
  (~1e-16) игрой не нарушает И1 — runtime-кламп трактует `free ≤ 0` как полный трюм (§7-A):
  `free = total − used; if free <= 0 → отказ`.
- **`player_cargo` — состояние игрока, в `truncateTables` НЕ входит** (И5, §8.2): обрезка меняет только
  перегруз, но таблица по-прежнему переживает очистку вселенной.
- **На практике затронут только тестовый аккаунт** `qa_cargo_124736` (95 т → 50 т, §20.8 п.4): остальные
  игроки держат груз в пределах новой ёмкости.
- Бэкфилл `users.equipment` **не нужен** (§20.3): существующие игроки уже несут `universal` = `cargo_1`
  (миграция `000068`), новые получают `StarterEquipment`; `universal2`/`universal3` — пустые по отсутствию ключа.
- Строка в `docs/DB.md` — дельта `@dispatcher` (§20.9).

**Код (дельта `@developer`, не код в этой спеке):**

- `internal/ship/catalog.go` — `defaultEquipment`, `cargo_1`: `capacity` **80 → 30**.
- `internal/ship/models.go` — `defaultShipModels`, `starter`: `slots.universal` **1 → 3**.
- `internal/models/ship.go` — комментарии `StarterCargoModuleID` (80 т → 30 т) и `StarterEquipment`
  (ключи слотов; **значение** `StarterEquipment` не меняется — `universal` = `cargo_1`).
- `CargoCapacityMass` — **правок не требует** (перебирает все значения `equipment`).

### 20.6. Пометки пересмотра прежних решений (числа и формулировки §0–§19)

| Где | Было | Стало (дельта 2026-09-24) |
|---|---|---|
| §0, §5, §14 | «ровно **1** универсальный слот» (решение №1) | **3** слота (`starter`); решения №1 в части числа слотов **пересмотрено** |
| §0 (стр. ~19 «20 + 80 = 100»), §1 (стр. ~27 «один универсальный слот», стр. ~34 «20 + 80 = 100»), §5, §6, §9.1, §13 (стр. ~343 «100 т»), §14, §17.1 (стр. ~409 «100 = врождённая 20 + модуль 80») | модуль `cargo_1` **80 т**, суммарно **100 т** | модуль **30 т**, суммарно **50 т** (20 + 30) |
| §5 (таблица), §12 (стр. ~315), §17.2 п.1 (стр. ~416) | прогрессия `cargo_2` **200 т** / `cargo_3` **500 т**, шаг **×2.5** | **несогласована** с новым якорем (30 × 2.5 = 75 т — «шаг ради шага»); **не пересчитывается сейчас** — уровни вне скоупа, вернутся с рынками (открытый вопрос §20.8) |
| §5 «100 = суммарная стартовая» | вариант A: 20 + 80 = 100 | **20 + 30 = 50**; принцип (100 = суммарная) сохраняется, число пересмотрено |
| §2 (термин «Универсальный слот») | «пока **один**» | **три** (`universal`/`universal2`/`universal3`); дельта `GLOSSARY.md` §1 — §20.9 |
| §9.3, §12 | UI «Корабль»: блок «Трюм»; слоты не рисуются | + **видимые универсальные слоты** (§20.4); заглушки `.ship-slot-future` удаляются |
| §8.3/§14 п.9 | `StarterEquipment` + `"universal": "cargo_1"` | **без изменений** (ключ и значение те же; новые ключи не добавляются) |
| §15 | дельта доков | + строки §20.9 (DB.md, GLOSSARY, impact_map, belt/contracts/ice — наружу) |
| §7-A, §11 И1 (формулировка «`used ≤ total` — инвариант») | инвариант держался только для **новых** записей (кламп); существующий перегруз не пересчитывался | миграция `000080` (шаг 3) **обрезает** существующий перегруз — `used ≤ total` восстанавливается для **всех** (§20.5, §20.8 п.4) |

**Что НЕ меняется:** жёсткий предел `used ≤ total` (§7-A, §11 И1), таблица `player_cargo` (§8), `GET /api/cargo`
(§9.1), «груз за борт» (§7.1/§9.2), внутренний сервис записи (§9.2), инварианты И1–И8 (§11), задел под объём (§4).

### 20.7. Тесты — что перезашить (находка `@critic` М4)

Числа 80/100/«один слот» зашиты в тестах; после дельты они **красные** — перечислено для `@developer`:

| Файл | Что зашито | Куда |
|---|---|---|
| `internal/ship/cargo_test.go` | `cargo_1` `{"capacity":80}`; кейсы «20 + 80 = 100», дефолты «100 т» | 30 / 50 |
| `internal/ship/models_test.go:25` | `Slots["universal"] == 1` («один универсальный слот») | 3 |
| `internal/cargo/cargo_test.go:32,292` | `view.Limits.Mass.Total == 100` («starter 20 + cargo_1 80») | 50 |
| `internal/cargo/cargo_test.go:321` | `TestViewEmptyHold`: `view.Limits.Mass.Total == 100` | 50 |
| `internal/cargo/cargo_test.go:54–62` | `TestTryAddCargoClamps`: `used = 95`, ожидает `accepted = 5` | при `free = 50 − 95 ≤ 0` станет `accepted = 0` — **пересчитать сценарий** под новую ёмкость |
| `internal/handlers/auth_handlers_test.go:391` | `cargoParams["capacity"] == 80` | 30 |
| `internal/handlers/cargo_handlers_test.go:70` | `Limits.Mass.Total == 100` | 50 |

**Новые тесты (TDD):**

1. `CargoCapacityMass` = 20 + 30 = **50** со стартовым модулем; без модуля = 20; битый `params` → 20 (§14 п.1).
2. `starter.slots.universal == 3` (§14 п.9).
3. **Пустые универсальные слоты:** `equipment` без `universal2`/`universal3` → вклад 0 (ёмкость 50,
   не ошибка) — закрепляет §20.3.
4. **Второй модуль в новом слоте:** `{"universal":"cargo_1","universal2":"cargo_1"}` → 20 + 30 + 30 = **80**
   (суммирование по всем ключам, §20.2).
5. Миграция `000080`: после применения `cargo_1.params.capacity = 30`, `starter.slots.universal = 3`
   (идемпотентность — повторный прогон не меняет).
6. **Перегруз обрезается миграцией** (`000080`, шаг 3, §20.5): игрок с грузом сверх новой ёмкости
   (кейс `qa_cargo_124736`: 95 т при ёмкости 50 т) после миграции имеет `used ≤ total` — «хвост» по
   `good_id` удалён, пограничная строка урезана; повторный прогон миграции результат не меняет
   (идемпотентность обрезки).

### 20.8. Открытые вопросы создателю

1. **«Никому не выдавать, только мне» vs «заменить значение всем».** В идее зафиксировано **глобальное**
   значение (ответы 8/10 идеи), но это **снижает ёмкость у всех** игроков (100 → 50 т). Требует явного
   подтверждения на гейте. *(Рекомендация дизайнера: глобально — иначе два разных `cargo_1` = дубль каталога.)*
2. **Название модуля.** Создатель называет его «трюм-1», в каталоге — «Грузовой модуль-1».
   По умолчанию **не переименовываем** (косметика каталога; переименование — отдельная правка).
3. **Прогрессия уровней** (`cargo_2`/`cargo_3`): прежний шаг ×2.5 даёт 200/500 от якоря 80; от нового
   якоря 30 шаг ×2.5 = 75 т — **несогласован** (§20.6). Пересчёт — когда уровни вернутся в скоуп
   (рынки). Сейчас **не трогаем**.
4. **Тестовый аккаунт `qa_cargo_124736`** держит 95 т груза → после правки масса превышает новую
   ёмкость (50 т). **Решение создателя 2026-09-24:** перегруженные трюмы **обрезаем** до новой ёмкости
   (не оставляем «перегруз» и не чистим в ноль) — миграция `000080` (§20.5, шаг 3) оставляет только то,
   что влезает. Инвариант `used ≤ total` восстанавливается для **всех** записей, а не только для новых
   (кламп при записи, §7-A). На практике обрезка затрагивает только тестовый аккаунт (95 т → 50 т):
   остальные игроки держат груз в пределах новой ёмкости.

### 20.9. Дельта доков (наружу — передаётся менеджеру/`@dispatcher`)

| Док | Дельта | Кому |
|---|---|---|
| `docs/specs/README.md` | строка манифеста: пометка «дельта 2026-09-24» | `@designer` (сделано автором спеки) |
| `docs/DB.md` | миграция **`000080`**: `equipment.cargo_1.params.capacity` 80 → 30; `ship_models.starter.slots.universal` 1 → 3; обрезка перегруженных `player_cargo` до новой ёмкости (шаг 3, `used ≤ total` для всех; `player_cargo` по-прежнему вне `truncateTables`, И5) | `@dispatcher` (дельта менеджеру) |
| `docs/GLOSSARY.md` | §5 «Грузоподъёмность» (80 т → 30 т, старт 100 → 50 т); §5 «Грузовой модуль» (`cargo_1` 30 т); §1 «Универсальный слот» («пока один» → **три**, ключи `universal`/`universal2`/`universal3`) | `@dispatcher` (дельта менеджеру) |
| `docs/impact_map.json` | формулировки в **единственном числе** («один универсальный слот», «100 т») → множественное/50 т; **новое ребро** `web/index.html → ship_models`/`users.equipment` (UI читает форму слотов и содержимое) и ребро `player_cargo → migrations/000080` (обрезка перегруза) | `@manager` |
| `docs/ARCHITECTURE.md:170` | строка про трюм в **единственном числе** («грузовые модули … в универсальном слоте») → множественное (форма слотов — 3) | `@dispatcher` (дельта менеджеру) |
| спека контрактов `2026-09-23-контракт-ленивая-доска-пакет-и-снабжение.md` | держит «один универсальный слот», «20/100»; **сверх того** — `cargo_2` **200 т** / `cargo_3` **500 т** (несогласованная прогрессия, §20.6/§20.8) — **документарная дельта** (код не ломается) | `@dispatcher` (дельта менеджеру) |
| спека поясов `2026-09-22-пояса-…-этап-3-…md` | «жёсткий предел **100 т**» → **50 т**: формулировки §5.3/§13; **также** шапка-связи (стр. ~33 «жёсткий предел 100 т (20 врождённая + 80 модуль)»), пример пакета §7 (стр. ~385 `"cargo": {"total": 100.0}`), §8.1 (стр. ~403–405 «Полный трюм (100 т)») | `@dispatcher` (дельта менеджеру) |
| архивная спека `_archive/91a-ship-section-dashboard.md` | описывает раздел «Корабль» («3 активных слота + пустые ячейки будущих слотов»), **не помечена** дельтой 2026-09-24 (заглушки `.ship-slot-future` удаляются; число универсальных слотов — из `slots.universal`) | `@dispatcher` (дельта менеджеру) |
| идея ледяных астероидов `2026-09-24_ледяные-астероиды-вода.md` | «трюм (100 тонн)» → 50 т; завязаны **не только формулировки**, но и **производные оценки** темы: в идее — «полный трюм … за 2–3 мин»; в одноимённой спеке `docs/specs/2026-09-24-ледяные-астероиды-вода.md` §7.2 — «20 т/астероид», «трюм 100 т за 2–3 мин», «запас 20 000 т = 1000 астероидов». **Величины оставляем `@balancetester`** — здесь фиксируется только **факт зависимости** от ёмкости | `@manager` |
| `99_roadmap.md` | строка блока (по решению менеджера) | `@manager` |
| `CHANGELOG.md` | по реализации | `@dispatcher` |

### 20.10. Связи механик (impact map, дельта)

| Сущность | Связь | Куда | Почему |
|---|---|---|---|
| `cargo_capacity` | reads | `ship_models` | врождённая ёмкость (`base_capacity`); число универсальных слотов (`slots.universal` = 3) ёмкость **не читает** — его читает UI (§20.4, `web/index.html`) |
| `cargo_capacity` | reads | `db:users.equipment` | ключи `universal`/`universal2`/`universal3`; пустой слот = **отсутствие ключа** |
| `equipment` | affects | `migrations/000080` | значение `cargo_1.params.capacity` **80 → 30** (глобально) |
| `ship_models` | affects | `migrations/000080` | `starter.slots.universal` **1 → 3** |
| `player_cargo` | affects | `migrations/000080` | шаг 3: обрезка перегруженных трюмов до новой ёмкости (`used ≤ total` для всех; ёмкость = `ship_models.base_capacity` + Σ cargo-модулей) |
| `web/index.html` (раздел «Корабль») | reads | `ship_models` | форма слотов: число универсальных ячеек из `slots.universal` |
| `web/index.html` (раздел «Корабль») | reads | `db:users.equipment`, `equipment` | содержимое слотов + имя/бонус модуля (`ship_catalog`) |
| `belt_mining` | reads | `cargo_capacity` | жёсткий предел трюма: **100 → 50 т** (формулировки) |
| контракты (`contract_requirements`) | reads | `cargo_capacity` | будущее требование «трюм N т» сверять с **50 т** |

### 20.11. Кандидат новой ГД-сущности (Вопрос → Ответ; в текущие числа не входит)

**Кандидат — «модуль-заглушка пустого слота» (слот как объект).**
**Вопрос:** нужен ли **интерфейс установки/снятия модулей** (перетаскивание модуля в универсальный слот,
снятие в инвентарь) — то есть превращаются ли **пустые слоты** из «задела» в игровую механику
(сборка корабля под задачу: трюм/радар/двигатель в одном универсальном слоте)?
**Ответ:** эталон не задан (2026-09-24) — **открытый вопрос**; в текущие числа и схему **не входит**
(два новых слота остаются пустыми).
Зачем: даёт кораблю **конфигурируемость** — игрок выбирает, что везти/как летать, а не получает фиксированный
набор; прямо опирается на решение №2 (три слота) и делает «задел» осмысленным.

### 20.12. История итераций (дельта)

- **Принято из решений создателя (2026-09-24):** `cargo_1` 80 → 30 т (глобально, старт 50 т);
  `starter.slots.universal` 1 → 3 (два пустых); универсальные слоты видимы в разделе «Корабль».
- **Принято из решений создателя (2026-09-24, обрезка):** перегруженные трюмы **обрезаем** до новой
  ёмкости (не оставляем «перегруз» и не чистим в ноль) — миграция `000080`, шаг 3, восстанавливает
  `used ≤ total` для всех записей; на практике затронут только `qa_cargo_124736` (§20.5, §20.8 п.4).
  *(Прежняя формулировка §20.8 п.4 «не чистим/не переносим» — снята 2026-09-24.)*
- **Принято дизайнером:** конвенция ключей **`universal`/`universal2`/`universal3`** (обратно совместима,
  аддитивна); бэкфилл `users.equipment` **не нужен** (пустой слот = отсутствие ключа, форма — в `ship_models`);
  заглушки `.ship-slot-future` **удаляются** (иначе дубль пустых ячеек); миграция **`000080`**.
- **Отклонено:** массив в ключе `universal` (ломает `000068`/тесты, разбор «скаляр или массив»);
  ключ по типу модуля (`cargo`/`cargo2`); бэкфилл `"universal2": null` (шум/дубль формы модели);
  строка «Грузоподъёмность» в сводке раздела (дубль блока «Трюм»).
- **Отложено:** пересчёт прогрессии `cargo_2`/`cargo_3` (несогласована, §20.6); интерфейс установки/снятия
  модулей (кандидат §20.11); переименование модуля (§20.8 п.2).
