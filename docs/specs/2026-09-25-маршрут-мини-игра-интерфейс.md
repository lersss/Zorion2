# 2026-09-25 — Мини-игра «Прокладка маршрута»: интерфейс экрана доски v9 (mobile-first)

**Статус:** ✅ **решения создателя приняты (2026-09-26)**: развилки О1–О6, О8 и **О9
(космический словарь)** зафиксированы; замечания критика **С-1/С-2** и мелочи **М-1/М-3/М-5**
внесены. Легенда-попап — четыре свёрнутые группы. Термины сведены с §14.12 осн. спеки:
действие разведки — **«Зондировать»**, метка обрыва — **«Срыв»** (объект
`dead_end_lane` — «Обрыв»), режимы/гулкость/содержимое — по словарю. Спека готова к передаче
в разработку (`@developer`) по команде менеджера.
**Дата:** 2026-09-25 (файл); редакция 2026-09-26 (итерация 7).
**Вектор:** Игрок
**Роль:** `@uidesigner` (ТЗ для `@developer`; экран Э3)
**Основание (не переписывается):**
- главная спека `docs/specs/2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md`,
  раздел **§14 (модель v9 «Планшет»)** — особенно **§14.8 «Что и когда видит игрок»**
  (ключевое требование), §14.3–14.4 (секторы/зондирование), §14.7 (16 ошибок), §14.10
  (критерии);
- идея-трекер `docs/gamedesign/ideas/2026-09-25_ускоритель-модуль-корабля-и-мини-игра-прокладка-маршрута.md`
  §16/§16.1/§16.2 (сделано по серверу: Э1-A `32073ae`, Э1-B `46453f3`).
**Серверный контракт (фиксирован, не меняем):** ручки `GET /api/accelerator/offer`,
`POST /api/accelerator/scan`, `POST /api/accelerator/boost` — см. §8. Источник —
`internal/handlers/accelerator_grid_handlers.go`, `accelerator_grid_boost.go`,
`accelerator_game_handlers.go` (read-only, сверено 2026-09-26).
**Код клиента (read-only, сверено 2026-09-26):** `web/route.html`,
`web/static/js/route/*` (`route_game.js`, `route_render.js`, `route_input.js`,
`route_config.js`, `route_sprites.js`), `web/static/css/route.css`. **Клиент сейчас
читает старую модель (`field`/`nodes`/`zones`, путь точками `{x,y}`) и мини-игра в
браузере неиграбельна** (Э1-B, хвост 1) — эта спека переводит его на `board`.
**Термины:** `GLOSSARY.md` — «Ускоритель (модуль корабля)», «Откат ускорителя»,
«Активное ускорение», «Паспорт перелёта», «Бонус скорости», «Слот корабля»
(`accelerator`). Новые термины модели v9 — в §1.
**Стиль/палитра:** базовые константы проекта (`belt.css`/`surface.css`) и атмосферные
тона — раздел «Визуальный стиль и ТЗ на ассеты» в конце (`@gdesigner`); он **сохранён**.
Роли спрайтов под объекты доски согласованы с `@gdesigner` и **подтверждены создателем**
(2026-09-26). Проектные решения итерации 1–2 не отменяются (направление A, максимум
красоты, пулы-наигрыш).
**Объём:** `web/route.html` + `web/static/js/route/*` + `web/static/css/route.css`;
точка входа — кнопка «Ускорить» в `#flight-panel` и ячейка `slot-accelerator` раздела
«Корабль» (`web/index.html`) — **уже сделаны** (Э4 их лишь полирует, см. §7.2).
Числа/механика/API/БД — не здесь (осн. §14).
**История итераций:**
1 (2026-09-25) — первая версия интерфейса под старую модель (полилиния, `field`/`zones`).
2 (2026-09-25) — визуальный раздел `@gdesigner`: направление A выбрано создателем, палитра
расширена, пулы вариантов/наигрыш.
3 (2026-09-26) — **вся UI-часть переработана под доску v9**: сетка по клеткам,
секторы с публичной σ, разведка импульсами, локальные метки курса, результат с
отрицательным `bonus`, зарезервировано место под разбор по 16 факторам. Визуальный
раздел сохранён и обновлён под объекты доски.
4 (2026-09-26) — **зафиксированы решения создателя по развилкам О1–О6** и внесены
замечания критика: коды отказов `scan`/`boost` приведены к реальным; убрана
несуществующая ветка «`offer` 409 при развороте»; «жар» (ныне метки курса) дополнен
положительными метками «Попутный поток»/«Находка»; убраны остатки старой модели
(`qualityBonusWord` → `bonusWord`, роль `false_signal` под Обрыв/Мираж).
5 (2026-09-26) — **решение создателя: легенда — попап** (§4.9) со **всей азбукой доски**
(«всё») **мини-фигурами тем же кодом, что доска** (не цветные свотчи); свёрнутого списка
в HUD больше нет. Приведены в соответствие §2/§3/§7.1/§7.3/§7.4/§7.5/§7.6/§7.7/§7.9/§9/§11/§13/§14.
6 (2026-09-26) — **синхронизация с космическим словарём создателя** (осн. спека §14.12):
объекты, гулкость, содержимое, метки курса и режимы переведены в сеттинг — **Помехи**
(`wall`), **Мгла** (`mud`), **Трасса** (`lane`), **Течение** (`current_oneway`),
**Кордон** (`gate`), **Тоннель** (`bridge`), **Обрыв** (`dead_end_lane`), **Разрыв**
(`bottleneck`), **Маяк**, **Старт**, **Цель**, **Сектор**; σ — Тихий/Ровный/Гулкий;
содержимое — Просвет/Зов/Воронка/Мираж/Сбой/Вакуум; «излом» — **Манёвр**; метки курса.
**Легенда-попап перестроена**: четыре **свёрнутые** группы (Ориентиры · Поле · Секторы ·
Метки курса); убраны путь/предпросмотр и вскрытое содержимое (оно — в карточке сектора).
Старый словарь (стена/топь/русло/шлюз/мост/тупик/излом/жар/джекпот/…) удалён из
player-facing текстов.
7 (2026-09-26) — **сведение терминов с §14.12 осн. спеки**: действие разведки — **«Зондировать»**
(кнопки, карточка сектора, статусы; «разведать/разведка» в значении действия убраны), метка
`dead_end` — **«Срыв»** (объект `dead_end_lane` остаётся «Обрыв»), режим «топь» —
**«Мгла»** (было «Мглы»), «риск помехи» по σ — **«риск Сбоя»** (ед. ч. «Помеха» в значении
`hazard`/`unstable` запрещена). В §4.9/§7.5/§7.9/§12.10/§14 подставлены итоговые имена вместо
заготовок «термин уточнит `@designer`». Визуальные решения `@gdesigner` (§12.1/§12.2,
§12.9–§12.12) не менялись — только подписи ролей и терминов.
8 (2026-09-26) — **М-5: мотивы паттернов Трассы/Помех разведены** (§12.13): Трасса —
**«струи потока»** (продольные непрерывные сужающиеся линии + ядро), Помехи — **«шумовая
решётка»** (перекрещенные штрихи ±60° с разрывами + крапины). Обновлены §12.2 (матрица +
п.(4а)), §12.7 п.8, §12.12 (`figLane`/`figWall`). Новых спрайтов нет.

---

## 0. Что изменилось против итерации 1 (дельта, чтобы не сверять построчно)

| Было (v1, итерация 1) | Стало (v9 «Планшет») | Почему |
|---|---|---|
| Непрерывное поле `[0,1]²`, путь — полилиния точками `{x,y}` | **Дискретная сетка n×n** (сейчас n=10), путь — **цепочка индексов клеток** `[cell,…]` | §14: доска-пазл, валидация по клеткам (4-связность) |
| Узлы `nodes[]` (маяки/ложные), зоны `zones[]` (потеря времени) | **Маяки** `beacons[]`, **Секторы** `sectors[]` с публичной σ, **объекты поля** (Мгла/Трассы/Помехи/Кордоны/Тоннели/Течения/Обрывы/Разрывы) | §14.1/§14.3: новый набор объектов; ложных сигналов как отдельных узлов нет |
| Захват маяка «радиусом линии» | Захват = **маяк лежит на пути** (его клетка в цепочке) | путь дискретный; захват точный |
| Глобальная шкала «короче/как/длиннее эталона» (§3.5 итер. 1) | **Убрана.** Только **локальные метки курса** | §14.8: «без тотала, без вердикта плюс/минус, без оптимума» |
| Результат: `quality` + «Скорость +X %» (только плюс) | Результат: **только `bonus` (может быть отрицательным)** + новый остаток + качественные слова | §14.2/§14.8: перелёт может **удлиниться** (И-6 отменена) |
| Одна ручка `offer`/`boost` | + **`scan`** — зондирование импульсом, счётчик импульсов, вскрытые секторы | §14.4 R1–R6 |
| — | **Режим задачи** (`board.mode`) чипом; **риск Сбоя** по σ; **легенда-попап** поля | §14.5; обучаемость — объекты новые |
| — | Зарезервирован блок **разбора по 16 факторам** (скрыт) | §14.8: разбор — **отдельный чекпоинт** |

**Что сохраняется без изменений:** каркас страницы-мини-игры (canvas + HUD + оверлеи +
тосты по образцу `belt.html`), палитра и приёмы (код рисует поле/путь, спрайты — ядро
сцены, фолбэк без ассетов, `preloadSprites` до кадра, DPR-`resize`), пулы спрайтов и
режим наигрыша, состояния отказа/отката/«ускорение действует», мобильные требования.

---

## 1. Модель и что видит игрок (сводка §14.8 — ключевое требование)

**Поле.** Дискретная сетка `n×n` (сейчас 10). Клетка — целое `0…n·n−1`, где
`i = cell % n`, `j = cell / n` (в JSON — `board.start`, `board.finish`, `beacons[]` и
списки объектов). Курс — **4-связная цепочка клеток** от **Старта** через **все** маяки к
**Цели**; иначе `boost` вернёт 400 (см. §8). **Имена игроку** (космический словарь, осн.
спека §14.12): `wall` — **Помехи**, `mud` — **Мгла**, `lane` — **Трасса**,
`current_oneway` — **Течение**, `gate` — **Кордон**, `bridge` — **Тоннель**,
`dead_end_lane` — **Обрыв**, `bottleneck` — **Разрыв**, `beacon` — **Маяк**, `start` —
**Старт**, `finish` — **Цель**, `sector` — **Сектор**.

**Объекты поля (публичные, кроме содержимого секторов):**

| Объект (`board.*`) | Что это | Как влияет (механика — осн. §14) |
|---|---|---|
| `start` / `finish` | клетки Старта/Цели | концы курса; Цель — звезда из паспорта (цвет/спрайт) |
| `beacons[]` | обязательные маяки | все должны быть на курсе |
| `mud[]` + `visible[]` | Мгла (клетка → цена в `visible`) | дорого пересекать; цена видна публично |
| `visible[]` | видимая цена **каждой** клетки | база 1.0; Трасса 0.2; Мгла — своя; Помехи 9.0 |
| `lane[]` | Трассы | дешёвые клетки (0.2) |
| `wall[]` | Помехи (помехи в гиперпространстве) | очень дорогие (9.0), но **проходимы** |
| `gate[]` | Кордоны (платные) | + toll за **каждый вход** |
| `bridge[]` | разовый Тоннель | первый проход дешёвый, повторный — дорогой |
| `current[{cell,dir}]` | одностороннее Течение | по течению дешевле, **против** — дорого |
| `dead_end[]` | Обрывы | заход в Обрыв — ошибка (штраф) |
| `bottleneck[]` | Разрывы | узкая щель между Помехами |
| `sectors[]` | скрытые Секторы | публичная гулкость σ + окружение; содержимое — только после `scan` |
| `mode` | режим задачи | Трассы / Течения / Кордоны / Обрывы и миражи / Мгла |

**Сектор** (`board.sectors[]`): `cells[]`, `sig`+`sig_name` (0 **Тихий** / 1 **Ровный** /
2 **Гулкий**), `surround`+`surround_name` (0 **Ничего** / 1 **Кордон** / 2 **Обрыв** /
3 **Мгла** / 4 **Течение**). Обобщающее слово для σ — **«гулкость»**.
**Содержимое сектора клиенту НЕ отдаётся до `scan`** — только гулкость (подпись) и окружение.

**Зондирование (§14.4).** `pings_left` импульсов (сейчас 2). `scan` **вскрывает** содержимое
сектора: `jackpot | lure | trap | decoy | unstable | empty` — игроку **Просвет | Зов |
Воронка | Мираж | Сбой | Вакуум**, списывает импульс. **Любой зонд несёт риск Сбоя**
`P(unstable|σ)` = Гулкий ~0.30 / Ровный ~0.15 / Тихий ~0.05; при `unstable` — **Сбой**
(клетки и подход дорожают, §14.7 `ping_destabilize`). Вскрытые Секторы **не сбрасываются**
(переживают перерисовку и заход) — `revealed[]`.

**Что видит игрок (§14.8).**
- **До отправки:** видимое поле (Маяки, Мгла, Трассы, Манёвры/τ, Тоннели, Кордоны,
  Течения, Обрывы, Разрывы), **подписи гулкости Секторов**, счётчик импульсов, вскрытые
  Секторы. Показываются **локальные метки курса** (дорогие клетки, Манёвры, Обрывы,
  Встречный поток, повторный Тоннель/Кордон) — **без тотала, без вердикта «плюс/минус»,
  без оптимума, без бесплатного призрака** и без чисел результата.
- **После «Проложить» (необратимо, раз на сегмент):** точный `bonus` (в т. ч.
  **отрицательный**) и — в будущем чекпоинте — **именованный разбор по 16 факторам**
  (§14.7). В этой итерации закладывается **только место** под разбор (§7.3, §10).

---

## 2. Компоновка экрана (рекомендованная — «Навигационный планшет»)

Направление A (выбрано создателем, итер. 2) сохраняется: **полноэкранный canvas +
верхняя паспорт-полоса + нижняя панель действий**. Портрет, mobile-first (390×844 —
базовый кадр).

```
┌─ safe-area top ─────────────────────────────────────┐
│ [Паспорт ⏷] [Режим: Кордоны]            [← На карту]│  ← чипы (паспорт сворачивается)
│              «Не хватает 2 маяков»                   │  ← статус-строка (центр)
│  ┌────────────────────────────────────────────────┐  │
│  │                                                │  │
│  │   ДОСКА n×n (canvas, квадрат 1:1)              │  │
│  │   ⚓ Старт(корабль) ──курс── ✦ Цель             │  │
│  │   маяки ●;  Секторы ▒ с подписью σ;            │  │
│  │   Мгла/Трассы/Помехи/Кордоны/Тоннели/Течения/  │  │
│  │   Обрывы/Разрывы                               │  │
│  └────────────────────────────────────────────────┘  │
│  [Маяки 2/4] [Импульсы ●●] [Легенда]                  │  ← строка обратной связи
│  ┌──────────────────────┐ ┌───────────────────────┐  │
│  │ ↶ Шаг назад           │ │        Заново         │  │  ← сервисные, тач-цели
│  └──────────────────────┘ └───────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │                 Проложить (primary)            │  │  ← главная кнопка, на всю ширину
│  └────────────────────────────────────────────────┘  │
│  (при выборе сектора — карточка сектора над кнопками) │
└─ safe-area bottom ──────────────────────────────────┘
```

**Элементы (сверху вниз):**

1. **Паспорт-полоса** (сворачивается по тапу, по умолчанию свёрнута — не мешать
   доске). Чипы:
   - **дальность** качественно: «близкий / средний / дальний» (без чисел и ETA;
     перевод `dist` в минуты запрещён — решение 14, осн. §6.4);
   - **класс звёзд концов** `G → K` + тип системы («одиночная/двойная/кратная»);
   - **температура концов — только °C** (перевод из K; решение 2026-09-18):
     «+5 400 °C → +3 100 °C»;
   - **состав поля** из `board`: «Маяки 4 · Секторы 4 · Мгла 5 · Трассы 2» (без цен!);
   - пояс системы назначения — **только если есть** (`destination_belts` непуст):
     чип «Пояс в системе».
2. **Чип режима** — `board.mode` («Режим: Кордоны») рядом с паспортом (виден всегда —
   задаёт ожидание задачи, §14.5).
3. **Выход «← На карту»** — верх-право, тач-цель ≥44 px. Доступна **всегда**, в любом
   состоянии (уход до отправки бесплатен).
4. **Статус-строка** (верх-центр) — единственная динамическая подсказка: «Ведите курс
   от корабля к Цели», «Не хватает N маяков», «Курс не доходит до Цели»,
   «Перелёт уже завершается — ускорить не получится», «Курс готов» (формулировки §4.5).
5. **Доска** — `<canvas id="route-canvas">` на весь экран; сетка рисуется в
   **квадратную область 1:1** (letterbox), отцентрованную в свободном месте; фон
   (звёздное поле/туманность) заполняет весь экран. Квадрат 1:1 — **сознательно**
   (нормализация ломает восприятие длины и близости; серверная оценка от вёрстки не
   зависит). Клетка ≈ `size / n`; при 390 px ширине поле ≈ 358 px → клетка ≈ 36 px
   (это **поверхность рисования**, не тач-цель; см. §6).
6. **Строка обратной связи** (над кнопками): счётчик **«Маяки k/N»** (task),
   счётчик **«Импульсы ●●»** (точки, потрачено/осталось), кнопка **«Легенда»**
   (открывает **попап-легенду** — модальная панель поверх игры, §4.9).
7. **Панель действий** (safe-area bottom):
   - **«↶ Шаг назад»** и **«Заново»** — в ряд (сервисные);
   - **«Проложить»** (primary, **на всю ширину** ниже) — единственная главная кнопка;
     отправка **двухтапная** (решение создателя 2026-09-26, §4.4).
   Все тач-цели ≥44–52 px.
8. **Карточка сектора** — появляется над панелью действий при выборе сектора (§4.3):
   гулкость σ, окружение, риск Сбоя, кнопка **«Зондировать»** (или «Уже вскрыт» +
   название содержимого — Просвет / Зов / Воронка / Мираж / Сбой / Вакуум).

**Альтернативы (коротко).**
- **B — «кокпит снизу»**: паспорт/обратная связь — нижней карточкой над кнопками.
  Минус: нижняя треть — зона рисования пальцем, карточка перекрывает доску.
- **C — «минимализм»**: только доска + кнопки; паспорт/режим по тапу из чипа. Минус:
  обязательный «характер маршрута» и режим спрятаны; хуже для обучаемости.

Полностью спроектировано **A**. При выборе B/C переносятся позиции блоков; данные,
состояния и поведение (§3–§5) не меняются.

---

## 3. Все состояния экрана

| Состояние | Что показываем | Управление |
|---|---|---|
| **Загрузка игры** | оверлей `#loading`: «Готовим маршрут…». HUD/кнопки скрыты, доска пустая | вход невозможен |
| **Ошибка сети / 5xx** | оверлей `#error`: «Не удалось связаться с сервером» + «Повторить» (`boot()`) и «На карту» (primary) | повтор / выход |
| **Не авторизован (401)** | `window.location.href='/login-page'` (как `belt_main.js`) | — |
| **`no_flight`** (только `offer`) | «Сейчас нет активного перелёта» + «Откройте карту и начните перелёт» | «На карту» |
| **`no_module`** | «Ускоритель не установлен» | «На карту» |
| **`unknown_game`** | «Мини-игра недоступна» + «Этот ускоритель пока не поддерживает игру» | «На карту» |
| **`already_active`** | «Ускорение уже действует» + «Смена цели или прибытие сбросит его — тогда ускориться можно снова» (решение 18) | «На карту» |
| **`too_short`** | «Перелёт слишком короткий» + «Осталось мало пути для партии и ускорения» | «На карту» |
| **`cooldown`** | «Ускорение перезаряжается» + таймер `mm:ss` (`cooldown_remaining_s`, локальный досчёт) + «Можно не ждать — вернитесь на карту» | «На карту» |
| **`no_field`** | «Доска не собралась» + «Поле этого перелёта не удалось построить — попробуйте другой перелёт» (редкий отказ сервера) | «На карту» |
| **Готово к игре** | доска + HUD; статус «Ведите курс от корабля к Цели»; «Проложить» **disabled** | ввод |
| **Путь неполный** | «Проложить» **disabled**; статус объясняет первую причину (§4.5) | ввод / «Заново» |
| **Сектор выбран** | карточка сектора (§4.3) с гулкостью σ/окружением/риском Сбоя; клетки сектора подсвечены | «Зондировать» / закрыть |
| **Зондирование** (кнопка занята) | «Зондирую…»; повторные тапы игнорируются; поле заморожено для ввода в этой карточке | — |
| **Сектор вскрыт** | название содержимого (Просвет/Зов/Воронка/Мираж/Сбой/Вакуум), «Уже вскрыт»; повторный `scan` **не предлагается** (§4.3) | закрыть |
| **Легенда открыта** | попап поверх игры: четыре **свёрнутые** группы азбуки (§4.9: Ориентиры · Поле · Секторы · Метки курса); доска и HUD под ним **не интерактивны**, счётчики не меняются | закрыть: «✕» / тап по фону / Esc |
| **Остаток < порога** | «Проложить» disabled; статус «Перелёт уже завершается…». Локальный досчёт от `remaining_s` с порогом `min_remaining_boost_s` | «На карту» / «Заново» |
| **Отправка** | «Проложить» disabled, текст «Прокладываю…» + индикатор; поле/HUD заморожены | — |
| **Успех (в т. ч. отрицательный)** | результат (§4.7): `bonus` со знаком, новый остаток, качественные слова; за оверлеем — импульс вдоль пути | «Вернуться на карту» |
| **Отказ после отправки** | тост (низ-центр, 3 с) + возврат в **готовое** (путь сохраняем). Точные коды — по §8: 400 (невалидный путь: `too_few_cells`/`cell_out_of_bounds`/`not_adjacent`/`start_mismatch`/`finish_mismatch`/`beacon_missing`) → тост; 409 `changed`/`too_short` → тост; 409 `already_active`/`no_flight` → **оверлей-причина** (таблица §3); 409 `no_module`/`unknown_game`/`no_field` → оверлей-причина; 401 → `/login-page`. Несуществующих веток не закладывать | «На карту» |

**Разворот/прибытие — `offer` НЕ даёт 409.** `offer` строится от **текущего** перелёта,
поэтому при развороте 61a/перебазировании/прибытии/в другой вкладке между тапом
«Ускорить» и загрузкой доски **`offer` вернёт 200 с доской нового сегмента** (другой
`fingerprint`), и игрок сразу играет новый сегмент. `409` у `offer` бывает **только**
по причинам `no_flight | no_module | unknown_game | already_active | too_short |
cooldown | no_field` (таблица выше) — ветку «409 при развороте» **не закладывать**.
Если сегмент сменился **после** загрузки доски, отправка старого пути отвергается
сервером: `boost` → `409 changed` → тост «Маршрут изменился» + возврат на карту.

---

## 4. Реактивность и ввод

### 4.1. Модель ввода — курс по клеткам (тап/протяжка с прилипанием)

Ввод — `Pointer Events` (`pointerdown/move/up`), `touch-action:none` на канвасе; тач и
мышь равнозначны. Курс — **цепочка клеток** `[cell,…]`.

- **Начало.** Первый тап обязан попасть в **клетку Старта** (или её радиус ≥0.5 клетки).
  Попал — курс инициализируется клеткой Старта (точная привязка). Не попал — статус
  «Начните курс от Старта», курс не начат.
- **Точка под пальцем → клетка.** Экранная точка переводится в клетку
  (`i = floor((x−x0)/cell)`, `j = floor((y−y0)/cell)`), за пределами квадрата — клип.
- **Жест (решение создателя 2026-09-26).** Тап/протяжка задаёт **путевые клетки
  (waypoints)**; клиент соединяет соседние waypoints **4-связной цепочкой с минимумом
  манёвров** («лестница»: продолжает текущее направление, поворот ставит как можно
  **позже**). Итог — единственный предсказуемый курс; **игрок видит ровно те клетки, что
  уйдут на сервер** (предпросмотр совпадает с отправкой). Строгая «4-смежность под
  пальцем» отклонена.
- **Продолжение.** Палец ведёт по клеткам: вход в новую клетку добавляет её waypoint'ом
  (при 4-связной достройке промежуточные клетки добавляются автоматически).
- **Конец.** Курс завершается на клетке Цели (Цель достигнута — статус «Курс готов»
  при выполненных условиях). Курс можно вести и дальше, но гейт требует, чтобы
  **последняя** клетка была Целью.
- **Свободный сегмент.** Пока палец на экране — от конца курса к пальцу тянется
  **бледная «резинка»/предпросмотр** (бледные клетки-кандидаты); зафиксированные
  сегменты — **ярче**.
- **Отмена шага.** «↶ Шаг назад» убирает последний waypoint (бесплатно). «Заново»
  очищает курс целиком (бесплатно).
- **Ограничения** (осн. §14): курс — 4-связная цепочка; клетки проходимы все (Помехи —
  дорогие, но не запретные); заход в Обрыв/против Течения — допустим и стоит дорого
  (это **решение игрока**, UI не запрещает; §4.6 показывает локальные метки курса).

### 4.2. Рисование (что происходит на каждом шаге)

- **Захват маяка.** Как только клетка маяка попадает в цепочку — маяк **вспыхивает**
  (пульсация), счётчик «Маяки k/N» растёт, звук захвата.
- **Манёвр (τ).** Смена направления на клетке рисуется **засечкой-уголком** на курсе и
  краткой вспышкой; метки курса помечают Манёвр (§4.6). Числа манёвров **не
  показываем** — только засечки и качественный чип «Манёвр» (решение создателя
  2026-09-26: без счётчика «Манёвров: N»).
- **Цена клетки.** Клетка с `visible[c] > 1` (Мгла/Помехи) рисуется тёплой/плотной
  фактурой; `visible[c] < 1` (Трасса) — холодной светлой. **Численных цен в UI нет.**
- **Течения.** Стрелка-шеврон по `dir`; когда курс идёт **против** Течения — на этой
  клетке локальная метка «Встречный поток»; когда **по** Течению — светлая метка
  «Попутный поток» (§4.6).
- **Кордон/Тоннель.** Заход в Кордон — метка «Кордон»; **повторный** заход/проход —
  «Повторный кордон»/«Повторный тоннель» (метки курса).
- **Обрыв.** Заход в Обрыв — метка **«Срыв»** (§4.6; объект — «Обрыв», метка — «Срыв», чтобы имя не стояло в двух группах легенды).
- **Сектор.** Клетки сектора под слоем «облака σ»; пока не вскрыт — содержимое
  неизвестно. Если курс идёт через невскрытый сектор — это осознанный риск игрока.

### 4.3. Секторы и зондирование импульсом (§14.4)

- На доске **каждый Сектор** — облако по своим клеткам с **публичной подписью**
  (`sig_name`) и окружением (`surround_name`) — **без содержимого**.
- **Тап по Сектору** → подсветка клеток + **карточка сектора** (над панелью действий):
  - «Сектор N · σ: Гулкий · рядом: Кордон»;
  - **риск Сбоя** по σ: Тихий → «низкий», Ровный → «средний», Гулкий → «высокий»
    (это правило §14.4 R2, не секрет);
  - счётчик «Импульсов: k» (общий) и подпись «Зондирование стоит 1 импульс»;
  - кнопка **«Зондировать»** (primary) — активна, если `pings_left > 0` и сектор не вскрыт.
- **`scan`** `{fingerprint, sector}` → 200: сектор вскрыт, содержимое показано,
  импульс списан, `revealed[]` обновлён. Отказы:
  - `no_pings` (409) → тост «Импульсы закончились»;
  - `bad_sector` (400) → тост «Сектор не найден» (не должно случаться);
  - `changed` (409) → тост «Маршрут изменился — вернитесь на карту»;
  - `no_flight`/`already_active`/`no_module`/`unknown_game`/`no_field` (409) →
    оверлей-причина по таблице §3;
  - 401 → `/login-page`; сеть/5xx → тост «Не удалось зондировать».
  - **Гейты `cooldown` и `too_short` к зондированию не применяются** — сервер их для
    `scan` не возвращает; импульсы доступны и во время отката, и «на исходе» перелёта.
    Ветку `cooldown`/`too_short` для `scan` **не закладывать**.
- **Вскрытое содержимое** — название + **одна строка смысла** (без точных множителей);
  строка смысла — как в осн. §14.12.4:
  - `jackpot` — **«Просвет»** · чистый короткий ход в секторе — лучшая находка;
  - `lure` — **«Зов»** · дешёвый срез, который легко прозевать: не свернёшь — потеряешь время;
  - `trap` — **«Воронка»** · гравитационная ловушка: проход через неё очень дорог;
  - `decoy` — **«Мираж»** · выглядит выгодно, а на деле дороже обычного пути;
  - `unstable` — **«Сбой»** · зонд разворошил поле: клетка и подходы к ней дорожают
    (+ клетки сектора и подходы помечаются «Сбой»; точную надбавку не показываем);
  - `empty` — **«Вакуум»** · внутри ничего: зонд потрачен зря, но и вреда нет.
- **Вскрытые Секторы сохраняются** (`revealed[]`): рисуются фигурой содержимого и не
  сбрасываются при перерисовке/заходе. Повторный `scan` того же сектора **не
  предлагаем** (клиент не даёт случайно потратить импульс на уже вскрытый сектор;
  сервер дедупа не имеет — §14.4 R6).
- **Сбой** влияет на итог при `boost` (осн. §14.4 R2/§14.7). UI лишь
  **честно помечает** вскрытый `unstable`; величину штрафа не показывает.

### 4.4. Гейт «Проложить»

Кнопка активна **только** при одновременном: (1) курс начинается от Старта, (2) последняя
клетка — Цель, (3) **все** маяки на курсе, (4) остаток ≥ `min_remaining_boost_s`,
(5) не идёт отправка/партия завершена. Иначе — disabled + объяснение.

**Двухтапное подтверждение (решение создателя 2026-09-26).** Отправка необратима, поэтому:
первый тап переводит кнопку в состояние **«Проложить?»** на 3 с, второй тап отправляет;
истечение 3 с без второго тапа возвращает «Проложить». **Финальное слово — сервер**
(осн. §14.2).

### 4.5. Статус-строка (приоритет причин)

Первая невыполненная причина, по порядку:
1. курс пуст → «Ведите курс от корабля к Цели»;
2. курс не начинается от Старта → «Курс должен начинаться от Старта»;
3. не хватает маяков `N` → «Не хватает N маяков»;
4. последняя клетка не Цель → «Курс не доходит до Цели»;
5. остаток < порога → «Перелёт уже завершается — ускорить не получится»;
6. всё выполнено → «Курс готов».

Знаменатель `N` — `board.beacons.length`.

### 4.6. Метки курса (локальные подсказки до отправки, §14.8)

Показываем **только локальные** метки на самом курсе — **без тотала, без вердикта
«плюс/минус», без оптимума, без бесплатного призрака, без чисел результата**:

| Метка курса | Когда | Как рисуется |
|---|---|---|
| «Сопротивление» | клетка с `visible[c] > 1` (Мгла/Помехи) на курсе | тёплый маркер на клетке + плотная фактура |
| «Манёвр» | смена направления | засечка-уголок на повороте (без счётчика) |
| «Срыв» | курс вошёл в `dead_end` | метка у входа + «стоп»-кайма (М-1: **не** дублировать объект «Обрыв») |
| «Встречный поток» | клетка `current` пройдена против `dir` | метка-стрелка с перечёркиванием |
| «Попутный поток» | клетка `current` пройдена по `dir` | светлая стрелка (положительная метка) |
| «Находка» | вскрытая клетка `jackpot`/`lure` на курсе | мягкий золотой маркер (только после `scan`) |
| «Повторный кордон» | повторный вход в `gate` | двойной маркер у Кордона |
| «Повторный тоннель» | повторный проход `bridge` | двойной маркер у Тоннеля |
| «Сбой» | вскрытый `unstable` сектор | агитированная кайма сектора/подхода |

**Положительные метки (решение создателя 2026-09-26).** Показываем и хорошее, не только
дорогое: **«Попутный поток»** и **«Находка»**. Оговорки сохранены: тотала/вердикта/оптимума
по-прежнему нет; **«Находка» появляется только после зондирования** (`scan` вскрыл
`jackpot`/`lure`) — до вскрытия содержимое сектора неизвестно и хорошей метки нет.

Правила: метки **локальны** (одна клетка/один объект), не суммируются в число/шкалу;
перерисовываются на каждое изменение курса (throttle по кадру — без «дёрганья»);
`prefers-reduced-motion` → метки статичны.

### 4.7. Результат после «Проложить» (§14.8)

`POST /boost {fingerprint, path}` → 200 `{bonus, remaining_s}`. Отправка **необратима,
раз на сегмент**, подтверждается **двухтапно** (§4.4).

1. **Анимация:** световой фронт пробегает вдоль курса Старт → Цель, вспышка на Цели.
   Тон: `bonus ≥ 0` — «ускорение» (зелёный, как `#4ade80`); `bonus < 0` — «торможение»
   (тёплый/янтарный, **не** зелёный).
2. **Оверлей-результат** (карточка поверх доски; доска остаётся видна):
   - заголовок: `bonus ≥ 0` → **«Ускорение принято»**; `bonus < 0` → **«Перелёт стал
     длиннее»**;
   - **итог `bonus` со знаком**: «Скорость перелёта **+34 %**» / «Скорость перелёта
     **−12 %** (перелёт удлинился)» — число **разрешено** после отправки (решение 14
     ограничивает только до);
   - **качественные слова** (UI-слова, не механика; границы — предложение, §11):
     `≥ +0.42` «Блестящий маршрут», `+0.30…+0.42` «Отличный маршрут»,
     `+0.20…+0.30` «Хороший маршрут», `+0.10…+0.20` «Ровный маршрут»,
     `0…+0.10` «Осторожный маршрут», `< 0` «Неудачный маршрут»;
   - **новый остаток**: «Осталось ~X» (из `remaining_s`, `remainWord`);
   - **место под разбор по 16 факторам — зарезервировано** (§10): в макете divider и
     **скрытый** контейнер `#result-breakdown` (в этой итерации пуст, с комментарием
     «разбор по 16 факторам — отдельный чекпоинт»);
   - кнопка **«Вернуться на карту»** (primary).
3. **Идемпотентность:** повторный тап после успеха игнорируется (`submitting/finished`).

### 4.8. Бесплатные действия

Пока путь не отправлен: «Заново», «↶ Шаг назад», «На карту», зондирование (кроме импульса),
перерисовки — **бесплатны**, откат не тратится. Откат тратится **только** за успешно
применённый полный маршрут.

### 4.9. Легенда (попап) и звук

> **Решения создателя 2026-09-26 (итерации 5 и 6):** легенда — **попап** (модальная
> панель поверх игры), каждая позиция — **мини-фигура тем же кодом, что доска** (не
> цветной свотч). **Итерация 6:** попап не влезал на телефон — **лишнее убрано**
> (путь/предпросмотр и вскрытое содержимое конкретной партии), содержимое сведено в
> **четыре группы-раскрывашки**, все **свёрнуты** по умолчанию. Креатив/структура —
> `@uidesigner`; визуальный язык (цвет + форма + паттерн) — `@gdesigner` (§12).

**Легенда — попап.** Открывается кнопкой **«Легенда»** из строки обратной связи HUD
(§2, п.6). Показывается **поверх игры**, модально: доска и HUD под попапом **не
интерактивны** (ввод курса/сектора заблокирован), при этом партия идёт (таймер остатка
не сбрасывается).

- **Закрытие — явное и однозначное:** кнопка **«✕»** в шапке попапа; **тап по фону**
  (затемнению) вокруг панели; на десктопе — **Esc**. Закрытие не тратит откат и ничего
  не сбрасывает (§4.8).
- **Компактность (ключевое требование).** Попап открывается **списком из четырёх
  свёрнутых строк** и влезает на телефон без скролла; разворачивание — тапом по строке
  группы (тач-цель ≥44 px). Внутри развёрнутой группы — **скролл**.
- **Содержимое — азбука доски, четыре группы** (порядок — от «зачем летим» к «что на
  курсе»; **все свёрнуты**):

  1. **Ориентиры** — **Старт** (корабль), **Маяк**, **Цель** (звезда перелёта).
  2. **Поле** — **Трасса**, **Мгла**, **Помехи** (в легенде поясняем: *помехи в
     гиперпространстве*), **Разрыв**, **Течение**, **Кордон**, **Тоннель**, **Обрыв**.
  3. **Секторы** — **Сектор** и **гулкость** σ: **Тихий / Ровный / Гулкий** (один ряд
     тремя фигурами, не три отдельные строки).
  4. **Метки курса** — **Сопротивление**, **Манёвр**, **Срыв** (метка `dead_end`,
     чтобы не дублировать объект «Обрыв»; М-1), **Встречный поток**, **Попутный поток**,
     **Сбой**, **Находка**, **Повторный кордон**, **Повторный тоннель** (9 позиций).

- **Что из легенды убрано (решение создателя 2026-09-26):**
  - **путь и предпросмотр** («резинка») — это сам интерфейс, объяснять нечего;
  - **вскрытое содержимое** (Просвет / Зов / Воронка / Мираж / Сбой / Вакуум) — это
    разбор **конкретной партии**, а не азбука поля; игрок читает название и строку
    смысла **в карточке сектора** (§4.3). Группы «Находки в секторе» в легенде **нет**;
  - **числа** (`bonus`, цены, τ, остаток) — запрет §14.8.
- **Как показана каждая позиция:** **мини-изображение самой фигуры — как она рисуется
  на доске, тем же кодом отрисовки** (не спрайт и не цветная плашка-свотч) + **название**
  + **одна строка смысла** человеческим языком. Цвет как самостоятельная легенда убран;
  цвет — лишь часть самой фигуры.
- **Не утекает игровая истина.** Легенда — **статичная общая справка об азбуке**, а **не
  подсказка по текущему полю**: в ней нет ни одной клетки/сектора/объекта конкретной
  партии, нет цен, τ, содержимого невскрытых секторов, `bonus`/тотала. Значения —
  качественные слова (§7.5), без чисел механики. Мини-фигуры **обезличены** (не
  подсвечены как «ваши» и не отражают состояние партии).
- **Мобильность.** Тач-цели внутри ≥44 px (кнопка закрытия, строки-раскрывашки);
  соблюдаются **safe-area** (`env(safe-area-inset-*)`) — шапка/закрытие не под «вырезом»
  и не за жестами; кнопка закрытия **не перекрывается** контентом (закреплена в шапке
  попапа).
- **Доступность:** закрытие — `:focus-visible`; заголовки групп — кнопки с
  `aria-expanded`; при открытии фокус переходит в попап, при закрытии возвращается на
  кнопку «Легенда»; `prefers-reduced-motion` → без анимации появления/раскрытия.

**Звук** — существующие события `web/static/js/ui/sound.js`: постановка шага, захват
маяка (`ui_select`), Сопротивление/Воронка (`ui_error` тише), вскрытие сектора (`ui_open`),
успех (`ui_success`), неудача/отрицательный результат (`ui_error`). Открытие/закрытие
легенды звуком не сопровождается (справка). Расширение — пожелание `@gdesigner`.

---

## 5. Навигация

- **Вход — из панели полёта** при `available=true` (осн. §4.4): кнопка «Ускорить»
  открывает `web/route.html` **без параметров** (сервер выводит `offer` из текущего
  полёта). Прямой заход без полёта → `offer` = `no_flight` → оверлей-причина.
- **Выход — «← На карту» / «Вернуться на карту»**: `window.location.href='/map'`.
  Метку `sessionStorage` **не ставим** — попап системы не нужен.
- **Ожидаемый эффект после возврата** (зона разработчика): карта при загрузке заново
  читает `/me`, панель полёта показывает **новый** `start_time`/`duration` — полоса
  короче (или длиннее при отрицательном `bonus`), кнопка «Ускорить» — в состоянии
  «Ускорение уже действует» до прибытия/смены цели.
- **Разворот/прибытие/другая вкладка до загрузки доски:** `offer` отдаёт доску
  **нового** сегмента (200, другой `fingerprint`) — ветки «409 при развороте» **нет**.
  `409` у `offer` — только по причинам из §3.
- **Смена сегмента ПОСЛЕ загрузки доски:** `boost` отвергается `409 changed` → тост
  «Маршрут изменился» + возврат на карту (ускорение не применяется).
- **Прибытие во время партии:** по истечении остатка (локальный таймер) «Проложить»
  гаснет; при явном submit сервер вернёт `too_short`/`no_flight` → оверлей-причина.

---

## 6. Мобильные требования (обязательно)

- **Портрет, полноэкранный canvas** (`position:fixed; inset:0`), DPR-`resize()` как в
  `belt_main.js`; базовый кадр **390×844**.
- **Safe-area**: `viewport-fit=cover` в `<meta viewport>`; отступы HUD/панели через
  `env(safe-area-inset-*)` — кнопки и паспорт не режутся «вырезом»/жестами.
- **Тач-цели ≥44 px**: «Проложить», «Заново», «↶ Шаг назад», «На карту», «Зондировать»,
  чипы, «Легенда», карточка сектора, элементы попапа-легенды (в т.ч. «✕»).
  `touch-action: manipulation` на кнопках, `user-select:none`.
- **Клетка доски — поверхность рисования, а не кнопка.** При 390 px клетка ≈36 px;
  это допустимо, т.к. палец **ведёт** по клеткам, а не «попадает» в них точечно:
  попадание снимается по клетке под пальцем + достройка 4-связной цепочки (§4.1).
  Все **кнопки** без исключения ≥44 px.
- **Без hover**: активные состояния — `:active`/`:focus-visible`; никакая информация
  не доступна **только** по наведению.
- **Рисование тачем и мышью равнозначно** (Pointer Events), `touch-action:none` на
  канвасе — палец не скроллит страницу.
- **Доска 1:1** letterbox внутри свободной области; фон заполняет весь экран.
- **Производительность:** доска/сетка/объекты/курс рисуются **кодом**; спрайты — только
  ядро сцены (звезда-Цель/ЧД, маяки, облака-Секторы, фон-туманность); `preloadSprites()`
  до первого кадра; фолбэк на кодовые фигуры при ошибке загрузки.
- **Паспорт свёрнут по умолчанию** — на узких экранах не съедает доску.
- **`prefers-reduced-motion: reduce`** — без пульсов/дрейфа/разлёта (§визуальный раздел).

---

## 7. ТЗ разработчику (элемент/экран: было → станет)

> Формат: «было → станет (+почему)». Всё новое опирается на существующие образцы
> (`belt.html`/`belt.css`/`surface.html`). Структура кода, API, данные — зона
> `@developer`; ниже — интерфейсный контракт. **Проверяй себя по коду:** каждый
> изменяемый элемент реально существует.

### 7.1. Файлы

| Файл | Действие |
|---|---|
| `web/route.html` | переработать разметку HUD под доску: чипы режима, строка обратной связи (маяки/импульсы/кнопка «Легенда»), **попап-легенда**, карточка сектора, панель действий, результат с **зарезервированным** `#result-breakdown`; оверлеи/тосты — как есть |
| `web/static/css/route.css` | дополнить: чип режима, карточка сектора, **попап-легенда**, строка меток курса, safe-area, тач-цели |
| `web/static/js/route/route_config.js` | **переписать словари**: σ (`SIG_LABELS`), окружение, содержимое, режим, риски, **`MARK_LABELS`** (§4.6), **`LEGEND_ITEMS`** (§4.9); маппинги объектов доски; константы цвета доски; `REASON_OVERLAY`/`REASON_TOAST` по §8 |
| `web/static/js/route/route_game.js` | `boot`/`offer`/`scan`/`boost`, состояние, гейт, статус, карточка сектора, попап-легенда (открыта/закрыта), результат (вкл. отрицательный) |
| `web/static/js/route/route_render.js` | **переписать отрисовку**: сетка, `visible`-цена, объекты, Секторы-облака, Маяки, Старт/Цель, курс-цепочка, метки курса; **общие примитивы отрисовки объекта** (доска + легенда) |
| `web/static/js/route/route_input.js` | **переписать**: тап/протяжка по клеткам → waypoints → 4-связная цепочка; отмена шага |
| `web/static/js/route/route_board.js` | **новый** модуль: чистые функции доски (координаты cell↔экран, соседи, достройка цепочки, локальные метки курса) — чтобы `route_game.js` не рос >300 строк |
| `web/static/js/route/route_legend.js` | **новый** модуль: попап-легенда (§4.9) — открытие/закрытие (кнопка/фон/Esc/фокус), скролл, отрисовка мини-фигур **тем же кодом**, что доска. `route_render.js` уже 565 строк — общие примитивы отрисовки объекта **переиспользуем**, попап его не раздувает |
| `web/static/js/route/route_sprites.js` | без смены механики: обновить маппинг типов под объекты доски (§7.6) |
| `tools/e2e/route-check.js` | обновить смоук под доску (сценарий §9) |

### 7.2. Панель полёта и раздел «Корабль» (точка входа — уже сделана)

| Элемент | Было → Станет |
|---|---|
| `web/map.html` `#flight-panel` | кнопка «Ускорить» / строка состояния — **есть** (Э1-вход). Полировка Э4: тексты причин — по §8 |
| `web/static/js/map/flight.js` (`startFlight`/`updateFlightPanel`) | читает блок `accelerator` — **есть**. Полировка: `available → «Ускорить»` (→ `/route`); `already_active`, `cooldown` (`mm:ss`), `too_short`, `no_module`/`unknown_game` — по §3 |
| `web/index.html` ячейка `slot-accelerator`, `slotDetail()` | ячейка/ветка — **есть**. Полировка: индикатор «Готов» / «Ускорение действует» / «Откат mm:ss» (М-5) |

**Э3 не трогает механику этих блоков** — только тексты/состояния при полировке.

### 7.3. Страница игры `web/route.html`

| Экран/элемент | Было → Станет (+почему) |
|---|---|
| заход | `boot()`: `activateSound()`, `#loading`, `preloadSprites()`, `GET offer` c JWT; 200 → доска; 409 → оверлей-причина (причины — §3); 401 → `/login-page`; сеть/5xx → `#error` |
| доска | полноэкранный canvas; `board.n×n` в квадрат 1:1; фон-звёздное поле; **все объекты и курс — кодом** (осн. §14.8) |
| `visible`-цена | — → тёплая/плотная фактура клеток `visible[c] > 1` (Мгла/Помехи), холодная светлая при `visible[c] < 1` (Трасса); **чисел нет** |
| Старт | — → спрайт корабля игрока (`recolorShipSprite`/`shipDrawTransform`, как в `belt`) + кодовая фигура при фолбэке; первая клетка курса привязана к Старту |
| Цель | — → `star_core`/`black_hole` пул + цвет `map.starColors`/`getStarShade`; последняя клетка курса — Цель |
| маяки | — → спрайт `beacon_*` + код: пульс, вспышка захвата, счёт «Маяки k/N» |
| Секторы | — → облако по клеткам (`hazard_cloud_*`) + **подпись гулкости σ** и окружение чипом; содержимое — только после `scan` |
| объекты поля | — → код: Мгла, Трассы, Помехи, Кордоны, Тоннели, Течения (шевроны), Обрывы, Разрывы (детали — §7.4) |
| курс | — → яркая цепочка клеток (по центрам) + бледный предпросмотр к пальцу; засечки Манёвров; отмена шага |
| карточка сектора | — → гулкость σ, окружение, риск Сбоя, «Зондировать» / «Уже вскрыт» + название содержимого (§4.3) |
| строка обратной связи | — → «Маяки k/N», «Импульсы ●●», кнопка **«Легенда»** (открывает попап-легенду, §4.9) |
| легенда-попап | — → модальная панель поверх игры (§4.9): **четыре свёрнутые группы** — Ориентиры / Поле / Секторы / Метки курса; мини-фигура **тем же кодом отрисовки, что доска** (не спрайт, не цветной свотч) + название + строка смысла; **без** пути/предпросмотра и **без** вскрытого содержимого; закрытие — «✕» / тап по фону / Esc; раскрывашки, safe-area, тач-цели ≥44; **не раскрывает состояние текущей партии** |
| статус-строка | — → первая невыполненная причина (§4.5) |
| «Проложить» | — → disabled, пока не (Старт + Цель + все маяки + остаток ≥ `min_remaining_boost_s`); **двухтапное подтверждение** («Проложить?» 3 с); при отправке — «Прокладываю…», блокировка |
| «↶ Шаг назад» / «Заново» | — → бесплатны до отправки |
| «На карту» / «Вернуться на карту» | — → `location.href='/map'`; доступно всегда |
| результат | — → импульс вдоль курса + оверлей: `bonus` **со знаком** (в т. ч. отрицательный), новый остаток, качественные слова; **`#result-breakdown` скрыт** (§10) |
| паспорт-полоса | — → чипы: дальность качественно, `класс A → класс B` + тип системы, температура **только °C**, «состав поля», пояс (если есть); **+ чип режима** |

### 7.4. `route_render.js` — что кодом, что спрайтом

| Элемент | Код | Спрайт (пул) |
|---|---|---|
| Фон: звёздное поле, пыль, параллакс, виньетка, блики | **да** | — |
| Фон-глубина: туманности | позиция/масштаб/тинт/дрейф/alpha | `nebula_bg_01…03` |
| Доска 1:1: рамка, сетка n×n | **да** | — |
| `visible`-цена клеток (Мгла/Помехи/Трасса) | **да** (тинт + фактура) | — |
| Трассы (`lane`) | **да** (светлая полоса) | — |
| Помехи (`wall`) | **да** (плотный блок + штриховка) | — |
| Кордоны (`gate`) | **да** (створ-барьер + маркер оплаты) | — |
| Тоннели (`bridge`) | **да** (настил + маркер «разовый») | — |
| Течения (`current`) | **да** (шевроны по `dir`) | — |
| Обрывы (`dead_end`) | **да** (кайма + «стоп»-маркер) | `false_signal_*` — **решение создателя 2026-09-26**: маркер сломанного маяка у конца Обрыва/Миража (§12.5) |
| Разрывы (`bottleneck`) | **да** (светлая щель в Помехах) | — |
| Секторы: клетки/граница/σ-подпись/риск | **да** | облако `hazard_cloud_01…03` внутри |
| Курс, Манёвры, предпросмотр, метки курса | **да** | — |
| Легенда-попап: мини-фигуры азбуки | **да** (те же примитивы отрисовки, что объекты доски; **не** спрайт, **не** цветной свотч) | — |
| Маяки: кольцо/пульс/вспышка/счёт | позиционирование, пульс, захват | `beacon_01…04` |
| Цель: звезда / ЧД | оттенок+температура (`map.starColors`), размер, glow, ореол | `star_core_01…05` / `black_hole_01…03` |
| Старт: корабль игрока | позиция, поворот, фолбэк | **переиспользуется** реестр корабля (`ship_sprites.js`) |

**Инвариант:** спрайт **не несёт игровой истины** (цена, τ, состав сектора, награда —
только код). Кольца захвата/радиусы в PNG **не рисуются**.

### 7.5. `route_config.js` — словари и константы

- Словари (тексты UI, без чисел механики; **без цветов** — цвет живёт в фигуре §12.2):
  - `SIG_LABELS = {0:'Тихий',1:'Ровный',2:'Гулкий'}`;
  - `SURROUND_LABELS = {0:'Ничего',1:'Кордон',2:'Обрыв',3:'Мгла',4:'Течение'}`;
  - `CONTENT_LABELS = {jackpot:'Просвет', lure:'Зов', trap:'Воронка',
    decoy:'Мираж', unstable:'Сбой', empty:'Вакуум'}` (строка смысла — §4.3/§14.12.4);
  - `RISK_BY_SIG = {0:'низкий',1:'средний',2:'высокий'}`;
  - `MODE_LABELS` (ключи — сырые значения `board.mode` с сервера, **не менять**; игроку
    показывается **метка чипа**, осн. §14.12.6): «русла» → **«Трассы»**, «течения» →
    **«Течения»**, «шлюзы» → **«Кордоны»**, «тупики/обманки» → **«Обрывы и миражи»**,
    «топь» → **«Мгла»**;
  - `MARK_LABELS` — **метки курса** (§4.6), тексты без чисел: Сопротивление, Манёвр,
    Срыв, Встречный поток, Попутный поток, Сбой, Находка, Повторный кордон,
    Повторный тоннель (9 позиций; метка `dead_end` — «Срыв», объект
    `dead_end_lane` — «Обрыв»);
  - `LEGEND_ITEMS` — **азбука доски для попапа (§4.9)**: для каждой позиции
    `{id, group, name, meaning}` — `name` (название) и `meaning` (**одна строка смысла**
    человеческим языком, без чисел механики). **Группы:** Ориентиры / Поле / Секторы
    (Сектор + гулкость σ) / Метки курса; **путь, предпросмотр и вскрытое содержимое в
    легенду не входят** (§4.9). Мини-фигура позиции рисуется **тем же кодом**, что на
    доске (общая функция отрисовки объекта), — **не спрайт и не цветной свотч**;
    **цвета в `LEGEND_ITEMS` нет** (цвет — внутри мини-фигуры, §12.2);
  - `bonusWord(bonus)` — **нейтральные** качественные слова результата по `bonus`
    **со знаком** (§4.7); имя не должно напоминать старую (непрерывную) модель;
  - `REASON_OVERLAY`, `REASON_TOAST` — §8.
- Константы доски: `n` берётся из `board.n` (не хардкодить 10); направление
  `dir` → вектор шеврона (`0:+i`, `1:−i`, `2:+j`, `3:−j`).
- Палитра доски — **истина в §12.2** (роли: Трасса, Мгла, Помехи, Кордон, Тоннель,
  Течение, Обрыв, Разрыв, Сектор, `unstable`, `jackpot/lure`). **Зарезервированные
  цвета интерактива** (путь `#60a5fa`, захват `#fde047`/`#fde68a`, успех `#4ade80`) не
  подменяются; §7.5 визуальные значения не дублирует.

### 7.6. `route_input.js` + `route_board.js` — ввод по клеткам

- **`route_board.js` (чистые функции):**
  - `cellAtPoint(view, x, y) → cell` (и `cellCenter(view, cell)`);
  - `neighbors4(n, cell)`, `adjacent4(n, a, b)`, `dirOf(n, a, b)`;
  - `connectStair(from, to, heading) → [cells…]` — 4-связная достройка между waypoints
    с минимумом манёвров (поворот **позже**; **решение создателя 2026-09-26**);
  - `stepHeat(board, path, revealed) → [{cell, kind}]` — локальные **метки курса** шага
    (`mud/wall/gate/bridge/current_against/current_along/dead_end/turn/hazard/jackpot`;
    тексты — `MARK_LABELS`, §7.5);
  - `beaconsOnPath(board, path)`, `turnCount(path)` (для меток, **не** для числа в UI).
- **`route_input.js`:** `pointerdown` → если пусто и не Старт → статус «Начните курс от
  Старта»; иначе начать курс. `pointermove` → waypoints по клеткам под пальцем
  (через `connectStair`, продолжение направления). `pointerup` → фиксация. `frozen()` —
  блокировка при отправке/завершении и при **открытом попапе-легенде**. `touch-action:none`
  (CSS).
- **Отмена:** «↶ Шаг назад» — удалить последний waypoint и пересобрать цепочку;
  «Заново» — очистить всё.

### 7.7. `route_game.js` — цикл

- `boot()` → `offer`; сохранить `board`, `passport`, `fingerprint`, `pings_left`,
  `revealed`, `remaining_s`/`remainingAt`, `min_remaining_boost_s`. **Помнить:** `offer`
  при смене сегмента отдаёт 200 с новой доской (не 409) — §3/§5.
- `refresh()` — пересчёт `beaconsOnPath`, `stepHeat`, счётчиков, гейта, статуса.
- `scan(sector)` — `POST /scan`; busy-состояние карточки; merge `revealed`/`pings_left`;
  отказы — §4.3.
- `openLegend()`/`closeLegend()` — попап-легенда (§4.9): состояние «открыта», блокировка
  ввода/секторов, управление фокусом; закрытие по «✕»/тапу по фону/Esc.
- `submit()` — двухтапное подтверждение → `POST /boost {fingerprint, path}`; при 200 —
  анимация + результат (в т.ч. отрицательный); при 400/409 — тост/оверлей по §3; при
  401 — `/login-page`.
- Таймер: `setInterval` для досчёта `remaining_s` и показа отката (если пришёл
  `cooldown_remaining_s`).

### 7.8. `route_sprites.js` — пулы (механика без изменений)

- `POOLS`/`SPRITE_FREEZE` сохраняются (режим наигрыша; заморозка — после выбора
  создателя, §10). Прелоад **только выбранных** вариантов (≤6 PNG); ошибка → фолбэк.
- `finishStarType === 'black_hole'` → пул `black_hole_*`, иначе `star_core_*`.
- Маппинг косметики под доску — §7.4; `false_signal_*` — маркер Обрыва/Миража
  (решение создателя 2026-09-26); победители пулов — не здесь.

### 7.9. Критерий проверки (как создатель увидит результат)

1. **Мобильный флоу (390×844):** `/map` → перелёт → «Ускорить» → `/route.html` на весь
   экран; чипы/доска в кадре, кнопки не под вырезом; доска 1:1.
2. **Курс по клеткам:** ведётся тапом/протяжкой, прилипает к клеткам, цепочка
   4-связная; при захвате маяка — вспышка и рост счётчика; «↶ Шаг назад»/«Заново»
   работают и **не** тратят откат.
3. **Метки курса:** на курсе видны метки «Сопротивление / Манёвр / Срыв /
   Встречный поток / Попутный поток / Находка / Повторный кордон / Повторный тоннель /
   Сбой»; **нет** тотала, вердикта «плюс/минус», оптимума и чисел результата до отправки.
4. **Секторы:** подпись гулкости σ видна; тап → карточка с риском Сбоя; «Зондировать» тратит
   импульс и вскрывает содержимое; вскрытые Секторы сохраняются; повторно не сканируются.
5. **Гейт «Проложить»:** активна только при Старт+Цель+все маяки+остатке; **двухтапное
   подтверждение** работает; иначе — объяснение.
6. **Результат:** после «Проложить» виден `bonus` со знаком (в т. ч. **отрицательный**
   — «Перелёт стал длиннее»), новый остаток, качественные слова; `#result-breakdown`
   скрыт.
7. **Состояния:** воспроизвести `already_active`, `too_short`, `cooldown` (таймер),
   `no_flight` (прямой заход), `no_field` (если достижимо), смену сегмента (offer 200 с
   новой доской / boost `changed`), `no_pings`.
8. **Легенда-попап:** кнопка «Легенда» открывает попап: **четыре свёрнутые группы**
   (Ориентиры · Поле · Секторы · Метки курса), разворачиваются тапом; мини-фигуры тем же
   кодом, что доска (не цветные свотчи), с названием и строкой смысла; **нет** пути,
   предпросмотра и вскрытого содержимого; закрывается «✕»/тапом по фону/Esc; влезает на
   телефон, уважает safe-area; **не раскрывает** состояние текущей партии.
9. **e2e-смоук** `tools/e2e/route-check.js` проходит.

### 7.10. Что не трогать (инварианты)

- **Карта, модалка системы, панель полёта** — как есть (кроме текстов/состояний
  полировки §7.2). Таймер/полоса/`updateFlightPanel`-логику **не менять**.
- **Механику и числа не меняем:** `bonus` (в т.ч. отрицательный), пороги
  `min_remaining_offer_s`/`min_remaining_boost_s`, откат, распределение секторов,
  16 ошибок — на сервере; клиент **не считает награду** и не влияет на неё.
- **Скрытые данные не показываем:** содержимое невскрытых секторов, `secret`,
  реальный слой, якоря — не запрашиваем и не рисуем; σ — публична.
- **Серверный контракт ручек не меняем.** Если замысел требует изменения структуры
  кода/API/данных — это не UI-зона: сообщаем «какой эффект нужен» и передаём
  `@designer`/`@developer` (§8, §11).
- **Температура — только °C** (никаких K в UI; решение 2026-09-18).
- **До отправки нет** ETA/секунд результата, `q`, `bonus`, тотала, оптимума,
  «призрака» (решение 14; §14.8).
- **Одно активное ускорение на сегмент** (решение 18/И-9): UI не предлагает «ускорить
  ещё раз» на том же сегменте.
- **Старая модель (`field`/`nodes`/`zones`)** — не читается и не рисуется.
- **Страницы `belt`/`surface`** и их JS/CSS — не трогаем.

---

## 8. Данные, которые нужны UI (контракт)

**`GET /api/accelerator/offer` → 200**
```
{
  fingerprint, game,
  passport: { dist, from:{spectral_class,star_type,system_type,temperature},
              to:{…}, destination_belts:[{kind,name,radius_au,width_au}] },
  board: {
    n, start, finish, beacons:[cell],
    visible:[float×n·n], lane:[cell], wall:[cell], mud:[cell],
    gate:[cell], bridge:[cell], current:[{cell,dir}], dead_end:[cell], bottleneck:[cell],
    sectors:[{cells:[cell], sig:0|1|2, sig_name, surround:0..4, surround_name}],
    mode
  },
  pings_left, revealed:[{sector,content}],
  remaining_s, min_remaining_boost_s, cooldown_remaining_s|null
}
```
Отказ → `409 {available:false, reason, cooldown_remaining_s}`; причины `offer`:
`no_flight | no_module | unknown_game | already_active | too_short | cooldown | no_field`.
**Смена сегмента (разворот/прибытие/чужая вкладка) — не отказ:** `offer` собирается от
текущего перелёта и вернёт **200 с доской нового сегмента**.

**`POST /api/accelerator/scan`** body `{fingerprint, sector}` → 200
`{fingerprint, sector, content (jackpot|lure|trap|decoy|unstable|empty), pings_left, revealed[]}`;
409 (`changed | no_flight | no_module | unknown_game | already_active | no_field | no_pings`),
400 `bad_sector`.
**Гейтов `cooldown`/`too_short` у `scan` нет** — не закладывать.

**`POST /api/accelerator/boost`** body `{fingerprint, path:[cell,…]}` → 200
`{bonus, remaining_s}` (**`bonus` может быть отрицательным**);
400 (`too_few_cells | cell_out_of_bounds | not_adjacent | start_mismatch | finish_mismatch | beacon_missing`),
409 (`changed | too_short | already_active | no_flight | no_module | unknown_game | no_field`).
**Гейта `cooldown` у `boost` нет** (приходит только как `cooldown_remaining_s` в теле
отказа/offer) — не закладывать отдельную ветку.

**Словари причин UI (в `route_config.js`).** `REASON_OVERLAY`: `no_flight`, `no_module`,
`unknown_game`, `already_active`, `too_short`, `cooldown`, `no_field` (§3).
`REASON_TOAST` (400/409): сообщения по коду причины (§3, §4.3) — сырой текст ответа в
UI не подставляем.

**UI использует:** `fingerprint`, `passport`, `board.*`, `pings_left`, `revealed`,
`remaining_s`, `min_remaining_boost_s`, `cooldown_remaining_s`; из `scan`/`boost` —
`content`/`bonus`/`remaining_s`. **Скрытое не запрашиваем.**

---

## 9. Сценарий e2e-смоука (для `tools/e2e/route-check.js`)

1. Открыть `/route.html` под моком `offer` с доской малого `n`.
2. Провести курс по клеткам от Старта; убедиться, что цепочка 4-связная и маяк
   засчитан.
3. Тап по Сектору → карточка; «Зондировать» → `scan` вызван, импульс −1, название
   содержимого показано.
4. Пройти все маяки и Цель → «Проложить» активна → **подтвердить вторым тапом** →
   `boost` вызван; при `bonus < 0` в результате «Перелёт стал длиннее» и отрицательный
   процент.
5. Повторить для состояний `already_active`/`cooldown`/`no_flight`/`no_pings`.
6. Открыть «Легенду»: попап появляется поверх доски **четырьмя свёрнутыми группами**;
   группа раскрывается тапом; закрывается «✕»/тапом по фону; после закрытия ввод курса
   работает как прежде.

---

## 10. Что не входит (явно)

- **Разбор результата по 16 факторам (§14.7/§14.8) — отдельный будущий чекпоинт.**
  В этой итерации: только **место** в макете — divider и **скрытый** `#result-breakdown`
  с комментарием. Никаких строк факторов сейчас не рисуем.
- **Победители пулов спрайтов и заморозка (§12.4 визуального раздела) — не здесь:**
  режим наигрыша сохраняется; выбор победителя и `SPRITE_FREEZE` — после решения
  создателя (Э4).
- **Изменения серверного контракта/механики** — не в этой спеке (контракт фиксирован).
- **Старая (v1) модель** — не поддерживается в UI.
- **Точные множители/числа объектов и содержимого секторов** — не показываем (§14.8).

---

## 11. Решения создателя и открытые вопросы

**Зафиксировано создателем (2026-09-26, все рекомендации приняты):**

- **О1. Жест прилипания** — **waypoints + автодостройка 4-связной цепочки с минимумом
  манёвров** (поворот — позже). Строгая «4-смежность под пальцем» отклонена.
- **О2. Вид меток курса** — **точечные локальные метки** на клетках (не тепловая карта).
  Глобальной шкалы нет (§14.8).
- **О3. Подтверждение «Проложить»** — **двухтапное** («Проложить?» 3 с).
- **О4. Манёвры** — **засечки на углах + чип**, без счётчика «Манёвров: N».
- **О6. Палитра доски и роли спрайтов** — **по предложению визуального дизайнера**,
  подтверждены создателем (в т.ч. `false_signal` → маркер Обрыва/Миража,
  `hazard_cloud` → фактура Сектора-облака).
- **О8. Легенда** — **попап** из **четырёх свёрнутых групп** (Ориентиры · Поле · Секторы ·
  Метки курса), каждая позиция: мини-фигура **тем же кодом, что доска** + название + строка
  смысла; **цветные свотчи убраны**; **путь/предпросмотр и вскрытое содержимое в легенду
  не входят** (содержимое — в карточке сектора) (решение создателя 2026-09-26, итерация 6).
- **О9. Космический словарь (итерация 6)** — объекты/σ/содержимое/метки/режимы переведены
  в сеттинг (осн. спека §14.12): **Помехи, Мгла, Трасса, Течение, Кордон, Тоннель, Обрыв,
  Разрыв, Маяк, Старт, Цель, Сектор**; σ — Тихий/Ровный/Гулкий; содержимое — Просвет, Зов,
  Воронка, Мираж, Сбой, Вакуум; «излом» → **Манёвр**; метки курса — §4.6. Старый словарь
  удалён из player-facing текстов.

**Остаются открытыми (не блокируют Э3):**

- **О5 (пороги качественных слов §4.7).** Числа — **UI-слова**, не механика; при желании
  `@balancetester` уточнит. Без чисел в интерфейсе игру не ломает.
- **О7 (размер клетки).** При 390 px клетка ≈36 px (<44): это **поверхность рисования**,
  не тач-цель; при необходимости — увеличить доску и/или добавить «лупу» (не в v1).

---

## 12. Визуальный стиль и ТЗ на ассеты (`@gdesigner`)

> **Роль:** `@gdesigner` (стиль + ТЗ `@artist`), `@developer` — интеграция.
> **Итерация 2 (2026-09-25)** сохранена: направление **A «Навигационный планшет»**
> выбрано создателем; палитра расширена («максимум красоты»); пулы + режим наигрыша.
> **Итерация 3 (2026-09-26)** — обновлены маппинги под объекты доски; **итерация 4**
> — роли спрайтов (`false_signal`, `hazard_cloud`) и палитра доски **согласованы с
> `@gdesigner` и подтверждены создателем**. Стиль, бюджет и режим наигрыша — без смены.
> **Итерация 6 (2026-09-26) — решение создателя по читаемости доски и легенды.**
> Дословно: «не влезает в попап. Надо лишнее убрать, например вскрытые сектора, пути.
> А также очень похожие цвета, надо что-то с этим делать. Лучше кодировать цвет + форма.
> Плюс сделайте в легенде раскрывашки, сгруппируйте по типам. И что такое «излом», и
> метки — непонятно. Сделайте паттерны какие-то, не только цвет. А ещё приведите в
> космический сеттинг, ну какая стена, какая топь и так далее.»
> **Зона итерации 6 — только визуальный язык: цвет + форма + паттерн/текстура**
> (§12.1–12.2, §12.9–12.12). Названия/термины ведёт `@designer`; структуру попапа
> (раскрывашки, группы, что убрать) — `@uidesigner` (§12.11). **Новых спрайтов не
> требуется** — паттерны рисуются кодом (§12.12).
> **Синхронизация словаря (2026-09-26, итерация 6):** `@uidesigner` привёл подписи ролей
> к космическому словарю создателя (осн. §14.12) — `wall` — **Помехи**, `mud` — **Мгла**,
> `lane` — **Трасса**, `current_oneway` — **Течение**, `gate` — **Кордон**, `bridge` —
> **Тоннель**, `dead_end_lane` — **Обрыв**, `bottleneck` — **Разрыв**; «излом» —
> **Манёвр**. Визуальные решения ниже (`@gdesigner`) не менялись (итерация 8 — только
> §12.13: разведены мотивы Трассы/Помех, М-5).
> **Решения создателя (дословно, ранее):** «поставь все рандомно при каждом запуске, нужен
> наигрыш, чтобы понять, что лучше»; «Делаем по максимуму красоты»; на вопрос о палитре —
> «разрешаю».

### 12.0. Аудит (факт, чтобы говорить предметно)

| Что | Как сделано | Вывод для route |
|---|---|---|
| Каркас мини-страницы | `belt.css`/`surface.css`: фон `#05070f`, `.overlay>.panel`, `.hud*`, `#toast`, primary `#2563eb`/`#3b82f6`, `'Segoe UI', Roboto, system-ui` | **переиспользуем как есть**; `route.css` — отдельный файл |
| «Взрослая» сцена-образец | `belt`: canvas fullscreen, **кодом** звёздное поле/пыль, **спрайты** камня, `preloadSprites()`, фолбэк | тот же приём: фон/доска/курс — кодом, спрайты — ядро сцены |
| Цвет звезды | `map/utils.js` `getStarColor`/`getStarShade`, палитра `config.js map.starColors` | Цель — тот же источник (единая карта/игра) |
| Спрайт корабля | реестр `RaceShipSprites`, `ship_sprites.js` | Старт — тот же спрайт игрока |
| Тестовая пачка спрайтов | `web/static/sprites/route/` (`_preview/`, `pool.html`) | основа пулов; варианты — режим наигрыша |
| Палитра доски v4/v5 (факт, итерация 6) | Трасса `#38bdf8` **=** Течение `#38bdf8`; Тоннель `#22d3ee` в том же сине-циановом поясе; Мгла `#8b5cf6` ≈ Сектор `#6366f1`; Обрыв `#ef4444` = Сбой `#ef4444`; метки курса — один кружок, отличались только цветом (у `*_twice` — кольцо); `trap`/`decoy`/`empty` в мини-фигурах неотличимы | **развести цвет + дать каждой роли свою форму и паттерн** (§12.2, §12.9–12.10) |
| Легенда-попап v1 | одна строка списка на позицию, фигура 48 px; σ и вскрытое содержимое — отдельными строками | **форма+паттерн должны читаться на ≈44 px**; группировка/раскрывашки и «что убрать» — `@uidesigner` (§12.11) |

**Что хорошо и не ломаем:** палитра/каркас belt/surface, кодовая отрисовка, фолбэк без
ассетов, детерминизм фона от seed, спрайты ядра сцены, режим наигрыша. **Риск:** без
спрайтов сцена скатилась бы в «кружки»; **без формы/паттерна** — в «пятна одного тона».

### 12.1. Направление A («Навигационный планшет») + космический словарь форм (решение создателя 2026-09-26)

Инструмент курса: глубокая тьма, тонкие светящиеся линии, точная сетка, сдержанный неон;
спрайты дают «настоящую» звезду, маяк, ЧД, туманность. Красота — светом и глубиной
(туманность, blend, bloom, параллакс), а не пестротой игрового слоя.

**Космический сеттинг (визуальный язык).** Доска — **сектор обзора навигационного
прибора**, клетка — ячейка сканирования; объекты читаются небесными, «наземных»
силуэтов (настил, болото, кирпичная кладка) нет. Названия — космический словарь
создателя (осн. §14.12); визуал обязан поддерживать космическую трактовку:

| Игровое имя (код) | Космический образ (итерация 6) | Форма/паттерн |
|---|---|---|
| Трасса (`lane`) | **гравитационный коридор** | светлый поток + стримеры |
| Мгла (`mud`) | **вязкая туманность / гравитационная яма** | тёмное ядро + крупинки + свечение |
| Помехи (`wall`) | **плотный обломочный монолит / непрозрачное облако** | тёмный блок + косая штриховка |
| Разрыв (`bottleneck`) | **щель между монолитами** | две скобки-«ворота» |
| Кордон (`gate`) | **контрольный буй / таможенный створ** | створка + ромб оплаты |
| Тоннель (`bridge`) | **разовый транзитный тоннель/переход** | перила + пунктирная настилка |
| Течение (`current_oneway`) | **гравитационный поток** | шевроны по `dir` |
| Обрыв (`dead_end_lane`) | **оборванный коридор** | кайма + сброшенный маяк (`false_signal`) |
| Сектор (`sector`) | **область аномалии/туманности** | облако + σ-текстура |
| Манёвр (`turn`) | **резкая смена курса** | засечка-уголок |

- **Палитра:** база — существующие константы; **атмосферные тона** — только фон/
  туманность/свечение; зарезервированные цвета интерактива не подменяются.
- **Типографика:** `'Segoe UI', Roboto, system-ui`; панель `1.3rem/600`, тело `0.95rem`,
  чипы `0.75rem`, подписи `0.8rem`; числа — `tabular-nums`; метки чипов — верхний регистр
  с `letter-spacing:0.04em`. Новых шрифтов нет.
- **Глубина/свет:** 5 слоёв — (0) база+виньетка; (1) дальняя туманность (α≤0.35,
  параллакс 0.1); (2) звёздное поле (0.15–0.2); (3) ближняя пыль/туманность (0.4);
  (4) доска 1.0 (объекты/Секторы/курс) + HUD. Свет от звезды-Цели (rim-light, аддитивное
  свечение, bloom двойной отрисовкой); у ЧД — тёмный ореол.
- **Анимация:** «прибор + дыхание космоса»: дрейф туманности 60–120 с, дыхание короны
  звезды 3.5–4.5 с, пульс маяка 1.6 с, вспышка захвата 180 мс, вращение фактуры сектора
  20–30 с, световой фронт успеха 0.8–1.2 с. Всё — transform/alpha, без шейдеров по кадру.
- **Ассеты:** пулы (звезда, ЧД, маяк, маркер Обрыва/Миража, фактура Сектора/облака,
  фоновая туманность), спрайты нейтральные (luminance/grayscale), цвет/температура —
  кодом.

### 12.2. Палитра и матрица «роль → форма + цвет + паттерн»

**База — зарезервированные роли (существующие константы):**

| Роль | Значение |
|---|---|
| Фон сцены | `#05070f` |
| Панель/оверлей | `#0f172a` |
| Граница панели | `#334155` |
| Хайрлайн/сетка | `rgba(51,65,85,0.4)` |
| Текст: основной/вторичный/тихий/слабый | `#e2e8f0` / `#cbd5e1` / `#94a3b8` / `#64748b` |
| Действие (primary) | `#2563eb` / `#3b82f6` |
| **Курс** (фикс./свободный) | `#60a5fa` / `rgba(96,165,250,0.35)` |
| Остаток/акцент | `#fde047` |
| **Захват маяка / блик** | `#fde047` / `#fde68a` |
| Успех | `#4ade80` |
| Предупреждение | `#f97316` |
| **Опасность** | `#ef4444` |
| Холодный акцент | `#38bdf8` |
| Звезда O…Y + экзотика | `map.starColors` |

**Атмосферные тона (только фон/туманность/свечение):** эмиссия роза `#e879a9`, пурпур
`#c026d3`, бирюза `#22d3ee`, индиго `#6366f1`, фиолет `#8b5cf6`; ореол ЧД `#3b0764`;
тёплое гало `#fff7ed`; холодный ореол `#7dd3fc`.

**Правило итерации 6.** Каждая роль кодируется **тремя независимыми каналами**:
**форма/силуэт** + **цвет** + **паттерн/текстура**. Различие обязано сохраняться
**в градациях серого** (без цвета): тогда и дальтоник, и мелкий размер (клетка ≈36 px,
легенда ≈44 px) видят то же, что все.

**Изменения палитры объектов (было → станет, +почему):**

| Ключ (`COLORS`) | Было | Станет | Почему |
|---|---|---|---|
| `current` | `#38bdf8` | **`#2dd4bf`** | был тем же тоном, что Трасса |
| `bridge` | `#22d3ee` | **`#f0abfc`** | выйти из сине-цианового пояса Трассы/Течения |
| `mud` | `#8b5cf6` | **`#7e22ce`** (+крупинки `#c084fc`) | отделить от Сектора, «тёмная яма» |
| `sector` | `#6366f1` | **`#7c8db5`** | нейтральнее и холоднее Мглы; σ — текстурой |
| `wall` / `wallDark` | `#334155` / `#1e293b` | без смены (+штрих `#475569`) | Помехи читаются плотностью/штриховкой |
| `bottleneck` | `#e2e8f0` | **`#cbd5e1`** | не сливаться с основным текстом |
| `gate` | `#f97316` | **`#fb923c`** | отделить от метки «Сопротивление» |
| `deadEnd` | `#ef4444` | **`#f43f5e`** | отделить от Сбоя |
| `unstable` | `#ef4444` | без смены | единый канал «опасность» |
| `jackpot` / `lure` | `#fde047` | **`#facc15`** | отделить от мига «захвата» `#fde047` |
| `decoy` | — | **`#94a3b8`** | Мираж — «пустышка», не опасность |
| `empty` | — | **`#64748b`** | «ничего нет» |
| `heatCost` (нов.) | — | **`#f59e0b`** | метка «Сопротивление» |
| reserved: `path`/`capture`/`captureSoft`/`success` | — | **без смены** | интерактив |

> §7.5 (перечень тонов объектов) — **ссылка сюда**: значения обновлены в §12.2
> (итерация 6); при расхождении истина — эта таблица.

**Матрица «роль → форма + цвет + паттерн» (объекты доски):**

| Роль (id) | Цвет | Форма (силуэт) | Паттерн/текстура (код) |
|---|---|---|---|
| Трасса `lane` | `#38bdf8` α0.10–0.12 | широкая спокойная заливка + светлое ядро | **«струи потока»** — 3 продольные непрерывные сужающиеся линии (без рамки/пунктира/диагонали), §12.13 |
| Течение `current` | `#2dd4bf` α0.55–0.95 | **шевроны по `dir`** | **пунктирные хвосты** вдоль `dir` |
| Тоннель `bridge` | `#f0abfc` (заливка α0.30, штрих 0.9) | **перила + настил** | **пунктирная настилка** (3 разрыва) |
| Мгла `mud` | `#7e22ce` α0.30, крупинки `#c084fc` | заливка + мягкое свечение | **крупинки 4–6/клетка** + «дыхание» 4 с |
| Помехи `wall` | `#1e293b` плотно, штрих `#475569` | сплошной блок | **«шумовая решётка»** — две семьи перекрещенных штрихов ±60° с разрывами + крапины, §12.13 |
| Разрыв `bottleneck` | `#cbd5e1` α0.9 | **две скобки-«ворота»** | **вертикальная щель** + короткие засечки |
| Кордон `gate` | `#fb923c` α0.95 | **створка + ромб оплаты** | **двойная поперечная полоса** (турникет) |
| Обрыв `dead_end` | `#f43f5e` α0.9 (кайма) | **кайма + сброшенный маяк** (`false_signal`) | **«стоп»-бар** поперёк входа |
| Сектор `sector` (невскрыт) | `#7c8db5` α по σ (0.20/0.32/0.46) | облако по клеткам | **σ-текстура**: Тихий — 3 дуги; Ровный — 5; Гулкий — 8 хаотичных штрихов + дрейф |
| Курс `path` (reserved) | `#60a5fa` | цепочка по центрам | сплошная линия |
| Предпросмотр `preview` | `pathFree` | бледная «резинка» | пунктир |
| Манёвр `turn` | `captureSoft` | **засечка-уголок** | одиночная ломаная |

**Правила читаемости:** (1) зарезервированные цвета интерактива неизменны; (2)
туманность — аддитивно α≤0.35, тусклее игрового слоя; (3) **обязателен тест «в градациях
серого»** — каждая пара соседних ролей различается формой или паттерном, не только тоном;
(4) **тоновые семейства осознанны**: «поток» (lane/current/bridge — разные тона),
«вязкость/аномалия» (mud/sector), «цена» (gate/heatCost), «опасность»
(unstable/trap/dead_end), «награда» (jackpot/lure/Находка) — внутри семейства форму
разводят §12.9–12.10; **(4а, М-5 — РЕШЕНО, §12.13) Трасса ↔ Помехи различаются мотивом
паттерна, а не плотностью**: Трасса — **продольные «струи потока»** (3 непрерывные
сужающиеся линии + ядро), Помехи — **«шумовая решётка»** (перекрещенные штрихи ±60° с
разрывами + крапины). Мотивы, числа и тест «в градациях серого» — §12.13; (5) цвет звезды —
из `map.starColors`.

### 12.3. Мобильный бюджет «максимума красоты»

| Делаем (дёшево) | Не делаем (дорого) |
|---|---|
| ≤2 туманности на кадр ≤1024², дрейф transform-ом | полноэкранный convolution-bloom/gaussian, DOF, motion-blur |
| bloom = двойная отрисовка (α0.25, scale 1.4) | пост-обработка через shaders по кадру |
| аддитивный blend туманностей | 5+ слоёв туманностей |
| прелоад **только выбранных** вариантов (≤6 PNG) | прелоад всего пула (22 PNG) |
| DPR ≤2; canvas = CSS-px × DPR | текстуры 2048/4096, per-pixel шум |
| пулы 3–5 вариантов | десятки вариантов на тип |

При просадке — первым убираем вторую туманность и крестовые блики; красоту звезды/маяка
не трогаем.

### 12.4. Пулы и наигрыш

| Тип | Вариантов | Чем различаются |
|---|---|---|
| `star_core` (ядро звезды-Цели) | 5 | рисунок лучей (4 длинных / 6 коротких / 8-луч / 2 широких / мягкая) |
| `black_hole` (ЧД-Цели) | 3 | кольцо анфас / наклонный диск / диск с джетом |
| `beacon` (маяк) | 4 | силуэт (тарелка+огонь / мачта / ферма / куб) |
| `false_signal` (маркер Обрыва/Миража) | 4 | характер поломки |
| `hazard_cloud` (фактура сектора/облака) | 3 | клубы / пылевые полосы / волокна-вихрь |
| `nebula_bg` (фоновая туманность) | 3 | облако / вуаль / нити |

**Взаимозаменяемость:** один холст, один якорь, прозрачный фон, одинаковая визуальная
масса; кольца захвата/радиусы в PNG **не рисуются**. **Заморозка:** после выбора
создателя — одно значение на тип, случайность отключается (§10).

### 12.5. ТЗ на ассеты (`@artist`, `web/static/sprites/route/`)

> Пайплайн — `docs/gamedesign/art/art_principles.md`. Отличие: звезда/ЧД/туманность/
> фактура — светящиеся/полупрозрачные, альфа **по яркости** (black → alpha), не по
> хромакею. Имена: `<тип>_NN.png`.

| # | Файлы | Назначение | Формат |
|---|---|---|---|
| 1 | `star_core_01…05.png` | ядро+лучи звезды-Цели (luminance, тинт кодом) | RGBA 512² |
| 2 | `black_hole_01…03.png` | ЧД-Цели | RGBA 512² |
| 3 | `beacon_01…04.png` | маяк (ядро) | RGBA 128² |
| 4 | `false_signal_01…04.png` | сломанный маяк — **маркер Обрыва/Миража (решение создателя 2026-09-26)** | RGBA 128² |
| 5 | `hazard_cloud_01…03.png` | фактура сектора/облака (grayscale-alpha) | RGBA 512² |
| 6 | `nebula_bg_01…03.png` | фоновая туманность (grayscale-alpha) | RGBA 1024² |

Корабль-СТАРТ **не ассет** — спрайт игрока из реестра. Итого **6 типов / 22 PNG**;
звёздное поле/пыль/объекты доски — код.

**Промпт-ядра (английские).** База — для типа, различающая фраза — после `;`.

- *star_core*: `a single bright star core with radiating spikes, volumetric glow, soft bloom, perfectly centered, pure white light, grayscale, on pure black background, game sprite, high contrast`; негатив: `planet, comet, spacecraft, ring, letters, text, watermark, lens flare streak, color, multiple stars`. `_01` `four long thin diffraction spikes, compact bright core`; `_02` `six short diffraction spikes, dense compact corona`; `_03` `eight-point starburst spikes, tight brilliant core`; `_04` `two wide soft flares, broad gentle halo, minimal spikes`; `_05` `no hard spikes, soft diffuse glow, hazy corona`.
- *black_hole*: `a black hole accretion disc, thin bright glowing ring around a dark void, gravitational lensing, centered, on pure black background, sci-fi game sprite`; негатив: `star, sun, letters, text, watermark, spacecraft, multiple objects`. `_01` `face-on thin glowing ring, strong lensing arcs, dark center`; `_02` `tilted elliptical accretion disc, bright upper lensing arc`; `_03` `accretion disc with a narrow bright polar jet`.
- *beacon*: `a single small navigation beacon satellite, glowing signal mast, antenna dish, emissive light, neutral grey metal, centered, crisp silhouette, on pure black background, sci-fi game icon`; негатив: `ship, planet, letters, text, watermark, multiple objects, cartoon, flat vector`. `_01` `dish antenna with a single beacon lamp`; `_02` `tall mast with a projected lamp and small solar fins`; `_03` `lattice truss station with rotating dish`; `_04` `compact cubic module with twin antennae`.
- *false_signal*: `a single damaged decoy distress beacon, broken dish, flickering signal, same family as a nav beacon but wrong, neutral grey metal, centered, crisp silhouette, on pure black background, sci-fi game icon`; негатив: как у `beacon`. `_01` `cracked and partially missing dish`; `_02` `bent mast, dead unlit lamp, scorched housing`; `_03` `fractured module with a missing panel, exposed frame`; `_04` `derelict fragment with sparking damage, dead antenna`.
- *hazard_cloud*: `a wispy dark nebula cloud, soft-edged, turbulent wisps, grayscale, no stars, centered, on pure black background, texture, high detail`; негатив: `stars, planets, spacecraft, letters, text, watermark, hard edges, frames`. `_01` `swirling turbulent wisps`; `_02` `diagonal dust lanes, layered bands`; `_03` `fibrous ionized filaments, spiral vortex core`.
- *nebula_bg*: `a large soft nebula cloud filling the frame, very soft gradient, grayscale luminance, no stars, no hard edges, on pure black background, background texture`; негатив: `stars, planets, spacecraft, letters, text, watermark, hard edges, frames, bright core point`. `_01` `broad billowing cloud, gentle inner glow`; `_02` `thin veiled curtain, vertical folds`; `_03` `long tendrils and filaments, wispy outskirts`.

**Приёмка `@artist`:** пачка превью (10–20 на тип) → создатель отбирает нужное число
вариантов; стиль согласовать на 2–3 тестовых (звезда/маяк/сломанный маяк уже согласованы).
Инструменты превью — `tools/make_route_preview.py`, `_preview/pool.html`.

### 12.6. Интеграция (что увидеть в коде; структура — `@developer`)

- Выбор варианта пула — **до** прелоада; прелоад **только выбранных**; при ошибке —
  фолбэк-фигура (сцена не падает).
- Случайность — только косметика; позиции/фон — от seed.
- Цвет звезды — `getStarColor`/`getStarShade` из `map/utils.js` (формулу не дублировать);
  `black_hole` → пул `black_hole_*` без яркого тинта; экзотика — `star_core_*` + тинт.
- `beacon_*` — спрайт-ядро + код (пульс/кольцо/счёт); радиус кольца = игровому.
- `hazard_cloud_*` — фактура сектора: тинт нейтральный, маска по клеткам сектора,
  плотность/вращение по σ; содержимое (после `scan`) — код.
- `false_signal_*` — маркер Обрыва/Миража (**решение создателя 2026-09-26**).
- `nebula_bg_*` — 1–2 слоя аддитивно α≤0.35, дрейф transform-ом.
- Старт — `recolorShipSprite` + `shipDrawTransform`.
- **Заморозка:** конфиг ассетов умеет оба режима (случайный/фиксированный).

### 12.7. Критерий проверки визуала

1. **390×844:** фон читается «прибором» (глубина), доска 1:1 в кадре, HUD не перекрывает
   спрайты.
2. **Наигрыш:** перезагрузка показывает разные варианты пулов при **идентичной**
   композиции/якорях/доске.
3. **Звезда-Цель:** цвет совпадает с картой (`map.starColors`); спрайт — нейтральный
   luminance (красится кодом).
4. **ЧД:** тёмное ядро + яркий диск + тёмный ореол, **не** «яркая звезда».
5. **Старт:** виден спрайт корабля игрока; при отсутствии PNG — кодовая фигура, `pageerror`
   нет.
6. **Маяки:** пульс, при захвате — вспышка и рост счётчика.
7. **Секторы:** облако читается; σ-подпись видна; **содержимое не видно до `scan`**;
   после вскрытия — фигура+название; σ читается плотностью и текстурой (§12.2).
8. **Объекты доски (§12.2, §12.13):** Трасса/Течение/Тоннель/Мгла/Помехи/Разрыв/Кордон/Обрыв
   различаются **цветом + формой + паттерном**, не только тоном; тест «в градациях
   серого» проходит для пар **Трасса↔Помехи** (полярность + геометрия + непрерывность,
   §12.13), Трасса↔Течение↔Тоннель, Мгла↔Сектор, Кордон↔метка «Сопротивление»,
   Обрыв↔Сбой, Помехи↔Разрыв.
9. **Вскрытое содержимое (§12.9, только на доске):** Просвет/Зов/Воронка/Мираж/Сбой/Вакуум
   различимы **до чтения подписи** — формой центра и каймой.
10. **Метки курса (§12.10):** каждая пара (Срыв/Встречный поток/Сбой, Повторный кордон/
    Повторный тоннель, Попутный поток/Находка) различается **силуэтом**, а не только
    цветом; метки видны (в т.ч. положительные), тотала/шкалы/чисел результата нет.
11. **Легенда (§12.11):** четыре свёрнутые группы (Ориентиры · Поле · Секторы · Метки
    курса); мини-фигура ≈44 px читается; форма+паттерн совпадают с доской; **нет** пути,
    предпросмотра и вскрытого содержимого.
12. **Прелоад:** выбранные ассеты готовы к первому кадру; при удалении PNG — фолбэк.
13. **Бюджет (§12.3):** без просадок на среднем телефоне.
14. **`prefers-reduced-motion`:** нет непрерывных пульсаций/дрейфа.
15. **Заморозка:** с фиксированным значением на тип — один и тот же вариант (§10).

### 12.8. Что не трогать (визуальные инварианты)

- Палитры и страницы `belt`/`surface`/карты — как есть; `route.css` — отдельный файл.
  Новые тона — только фон/туманность/свечение/объекты доски; зарезервированные цвета
  интерактива (`path`/`capture`/`captureSoft`/`success`) не подменяются.
- **Кодирование — цвет + форма + паттерн.** Роли, различающиеся **только цветом**,
  запрещены (итерация 6).
- **Случайный выбор спрайта — сознательный режим наигрыша**, не прод; замораживается
  после выбора создателя.
- **Детерминизм доски (от seed) не трогаем:** поле/объекты/секторы — чистая функция
  seed+secret; выбор спрайта и паттерн на неё не влияют.
- **Взаимозаменяемость пулов:** один холст/якорь/фон/масса; вёрстка не зависит от
  варианта.
- **Доска/объекты/курс/захват — код.** Спрайты не несут игровую истину.
- **Скрытые данные не рисуем** (содержимое невскрытых секторов, secret, реальный слой);
  визуал содержимого (§12.9) включается **только после `scan`**.
- **Температура — только °C**; в спрайтах текста нет.
- **До отправки нет** ETA/секунд/`q`/`bonus`/тотала/оптимума/призрака.
- **Спрайт не выходит за канвас и не перекрывает HUD/тач-цели**; порядок слоёв
  фон → доска → курс → эффекты → HUD сохраняется.
- **Реестр корабля не дублируем.**

---

### 12.9. Вскрытое содержимое сектора — форма + паттерн + кайма (решение создателя 2026-09-26)

Проблема: `trap`/`decoy`/`empty` в мини-фигурах были неотличимы, `jackpot`/`lure`
различались только подписью. Теперь у каждого содержимого — **свой центральный глиф**
и **своя кайма**; различимо без чтения подписи.

| Содержимое | Цвет | Центральный глиф (код) | Кайма | Паттерн/текстура |
|---|---|---|---|---|
| `jackpot` | `#facc15` | **звезда-искра** (4 луча, сплошная) | сплошная, 1.6 px | чистая заливка клеток |
| `lure` | `#facc15` | **ромб-«наживка»** (двойной контур) | **пунктирная** | чистая заливка |
| `trap` | `#ef4444` | **крест «Х»** | сплошная, 1.6 px | **плотная косая штриховка** по клеткам |
| `decoy` | `#94a3b8` | **разомкнутое кольцо**; сброшенный маяк `false_signal` **сохраняется** (решение итер. 4) — если спрайт не загрузился, кольцо различает Мираж кодом | пунктирная | **редкие точки** (2–3/клетка) |
| `unstable` | `#ef4444` | **зигзаг-молния** | **пульсирующая зигзаг-кайма** | искры-лучи к краям сектора |
| `empty` | `#64748b` | **без глифа** | тихая тонкая (α0.5) | без заливки (только фон) |

Правила: глиф рисуется в центре сектора (для многосоставного — в центре масс);
`jackpot`/`lure` — положительные (золото), `trap`/`unstable` — опасность (красный),
`decoy`/`empty` — нейтральные (сталь). До `scan` — только облако σ (§12.2).
**Визуал содержимого — только на доске** (после `scan`); **в легенду содержимое не входит**
(§4.9: легенда показывает Сектор и гулкость σ; название содержимого игрок читает в
карточке сектора, §4.3).

### 12.10. Метки курса — микро-глифы (решение создателя 2026-09-26)

Проблема: все метки были одним кружком и различались только цветом; `dead_end`/
`current_against`/`hazard` имели **один и тот же** `#ef4444`. Теперь у каждой — **свой
силуэт** (глиф из 1–2 штрихов), цвет — вспомогательный канал. `kind` — из
`route_board.stepHeat` (§7.6); тексты — `MARK_LABELS` (§7.5).

| `kind` | Метка курса (§4.6) | Цвет | Микро-глиф (код) |
|---|---|---|---|
| `mud` | «Сопротивление» (Мгла) | `#f59e0b` | **двойная волна** (≈) |
| `wall` | «Сопротивление» (Помехи) | `#f59e0b` | **двойная волна** (≈) |
| `gate` | «Кордон» (вход) | `#fb923c` | **ромб оплаты** |
| `gate_twice` | «Повторный кордон» | `#fb923c` | **Ⅱ** — двойная вертикальная черта |
| `bridge` | «Тоннель» (разовый) | `#f0abfc` | **одинарный пунктирный сегмент** |
| `bridge_twice` | «Повторный тоннель» | `#f0abfc` | **двойной пунктирный сегмент** |
| `current_against` | «Встречный поток» | `#ef4444` | **перечёркнутый шеврон** |
| `current_along` | «Попутный поток» | `#2dd4bf` | **открытый шеврон** |
| `dead_end` | «Срыв» (объект — «Обрыв») | `#f43f5e` | **«стоп»-бар** (▬) |
| `turn` | «Манёвр» | `#fde68a` | **засечка-уголок** (`turnNotch`) |
| `hazard` | «Сбой» | `#ef4444` | **зигзаг-молния** |
| `jackpot` | «Находка» | `#facc15` | **звезда-искра** |

Правила отрисовки: якорь — **верх-левый угол клетки** (0.22·cell), несколько меток
стекаются вдоль верхнего края; размер ≈0.22·cell; минимальная толщина штриха 1.4 px;
`prefers-reduced-motion` → статично; метки курса локальны, в число/шкалу не суммируются.

### 12.11. Легенда: мини-canvas, палитра, группы (решение создателя 2026-09-26)

- **Фигура.** Мини-фигура — тот же код, что доска (`route_figures.drawLegendFigure`).
  Холст `TILE=48` (внутренняя клетка ≈44), DPR ≤2. **Фон фигуры** — `#0b1220` (как клетка
  доски), чтобы паттерн в легенде читался идентично доске.
- **Мелкий размер.** Минимальная толщина штриха 1.2 px @1x, узор не мельче 4 px;
  направленные элементы (Трасса/Течение/Тоннель) показывать **лентой на 2 клетки**, если
  формат строки позволяет (иначе направление теряется) — рекомендация `@uidesigner`.
- **Палитра попапа:** панель `#0f172a`, граница/шапка `#334155`, текст `#e2e8f0` /
  смысл `#94a3b8`, подложка фигуры `#0b1220`; цвет свотчем **не** показывается — только
  готовая фигура (§4.9).
- **Группы и раскрывашки — `@uidesigner`** (§4.9; структура попапа, что убрать/свернуть).
  Со стороны визуала требования: (1) заголовок группы не крупнее строки-позиции;
  (2) **все четыре группы свёрнуты** — попап открывается списком из 4 строк и влезает на
  телефон (ведущие фигуры в свёрнутом виде **не** показываем); (3) группа «Метки курса» —
  под раскрывашкой (9 позиций — она и раздувала попап); **вскрытое содержимое в легенде
  нет** (только карточка сектора, §4.9); (4) σ — **один ряд «Тихий / Ровный / Гулкий»**
  тремя фигурами, а не три отдельные строки.
- **Не утекает истина:** легенда статична, не отражает текущую партию (без клеток,
  цен, τ, содержимого, `bonus`).

### 12.12. ТЗ на изменения (значения — здесь; код — `@developer`)

| Где | Было → Станет |
|---|---|
| `route_config.js` `COLORS` | значения из таблицы §12.2; добавить `heatCost` `#f59e0b`, `decoy` `#94a3b8`, `empty` `#64748b`; `jackpot`/`lure` → `#facc15`; `current` → `#2dd4bf`; `bridge` → `#f0abfc`; `mud` → `#7e22ce`; `sector` → `#7c8db5`; `deadEnd` → `#f43f5e`; `gate` → `#fb923c`; `bottleneck` → `#cbd5e1` |
| `route_figures.js` `figLane` | плоская заливка → заливка + **«струи потока»** (3 продольные непрерывные сужающиеся линии + ядро; §12.13) |
| `route_figures.js` `figWall` | косая штриховка 45° (шаг 6) → **«шумовая решётка»**: перекрещенные штрихи ±60° с разрывами (фаза от координаты клетки) + 3–4 крапины (§12.13) |
| `figMud` | редкие точки → **крупинки 4–6** + мягкое свечение (тёмное ядро `#7e22ce`) |
| `figGate` | створка+точка → створка + **ромб оплаты** + **двойная поперечная полоса** |
| `figBridge` | настил-прямоугольник → **перила + пунктирная настилка** |
| `figChevron` | шевроны → шевроны + **пунктирные хвосты** вдоль `dir` |
| `figDeadEnd` | `#ef4444` → `#f43f5e`; добавить **«стоп»-бар** |
| `figBottleneck` | светлая щель → **две скобки-«ворота»** + вертикальная щель |
| `figSector` | заливка+кайма → + **σ-текстура** (3/5/8 дуг) и **центральный глиф содержимого** (§12.9; глиф — **только на доске**, в легенде нет) |
| `sectorRim(content)` | `unstable`/`jackpot/lure` → полный набор: `trap` красный, `decoy` `#94a3b8`, `empty` `#64748b` |
| `heatMark(...)` | заменить на `heatGlyph(ctx,x,y,cell,kind,pulse)` по таблице §12.10 (сохранить `pulse`/`reduced-motion`) |
| `drawLegendFigure(id)` | обновить/добить id под группы легенды (Ориентиры/Поле/Секторы/Метки курса; **без** вскрытого содержимого и пути); мини-фигура = доска |

**Ассеты.** Новых **не требуется**: все паттерны/глифы рисуются кодом. Существующие
22 PNG (§12.5) остаются; `false_signal_*` — по-прежнему маркер Обрыва, `hazard_cloud_*`
— фактура Сектора. Порядок слоёв, детерминизм, бюджеты §12.3 — без смены.

### 12.13. Мотивы паттернов «Трасса» и «Помехи» (М-5, итерация 8)

> **Зона:** `@gdesigner` (мотив + числа), `@developer` (код). **Новых спрайтов нет.**
> Закрывает замечание критика **М-5** и правило §12.2 п.(4а): Трасса (`lane`) и Помехи
> (`wall`) различаются **мотивом паттерна**, а не плотностью одного и того же угла 45°.

**Факт по коду (сверено read-only, `web/static/js/route/route_figures.js`).** `figWall`
рисует диагональ 45° с шагом 6 px; `figLane` — **плоская заливка** (стримеры-45° из
§12.2 в код не попали). Новое ТЗ ниже **заменяет оба мотива** и для матрицы (§12.2), и
для кода — один источник истины.

**Решения (было → станет):**

| Роль | Было (мотив 45°) | Станет — свой мотив |
|---|---|---|
| Трасса `lane` | 2 диагонали 45°, шаг 8 px | **«струи потока»** — **продольные** непрерывные сужающиеся линии + светлое ядро |
| Помехи `wall` | косая штриховка 45°, шаг 6 px | **«шумовая решётка»** — две семьи перекрещенных штрихов **±60°** с разрывами + зернистые крапины |

#### Трасса `lane` — «струи потока» (гравитационный коридор)

- **Заливка:** `#38bdf8` α **0.10–0.12** (было 0.16 — тише, чтобы мотив читался, а не спорил с тоном).
- **Мотив:** **3 продольные** (вдоль клетки) линии, `y ≈ r.y + s·{0.28, 0.50, 0.72}`,
  `s = r.w`; каждая **непрерывная**, **сужается к концам** (линза: 0 → максимум в
  центре → 0), максимум **1.6–2.0 px**, от `x = r.x + s·0.10` до `r.x + s·0.90`.
- **Ядро потока:** по центральной линии — тонкая светлая нить **1 px**, α0.5–0.6.
- **Чего нет:** рамки, пунктира, перекрестий, диагонали. Направление у Трассы **не задано**
  (`lane` без `dir`), поэтому мотив — **«поток»**, а не «стрелка»: параллельные линии, не
  угол. Смысл совпадает с §12.1 («светлый поток + стримеры»), но мотив — **продольный**.
- **Как читается:** спокойный светлый коридор, по которому «течёт» курс; дешёвая клетка.

#### Помехи `wall` — «шумовая решётка» (барьер помех гиперпространства)

- **Подложка:** плотная заливка `#1e293b` (без смены) — непрозрачный барьер.
- **Мотив:** **две перекрещенные семьи штрихов** под **±60°**, шаг ≈**7 px**, толщина
  **1 px**, цвет `#475569` α0.85–0.95. Штрихи **разорваны** (пунктир `3/3` и `2/5`), а
  сдвиг/фаза берётся **детерминированно от координаты клетки** (как фон от seed) — рисунок
  «дрожит» от клетки к клетке и читается как **шум/интерференция**, а не кладка.
- **Зерно:** **3–4 крапины** 1.5 px (α0.6) в промежутках решётки — «пыль помех».
- **Чего нет:** непрерывных длинных рядов (признак кирпича) и **одиночной** диагонали
  (мотив Трассы). Перекрестие — **обе** семьи штрихов, а не одна; это и есть «шумовая
  сетка», а не «косая штриховка».
- **Как читается:** плотный **шумовой барьер**, сквозь который «пробивается» луч сканера;
  проходим, но очень дорого.

#### Различимость в градациях серого (обязательный тест §12.2 п.3)

Обесцветить фигуру до нулевой насыщенности, клетка ≈36 px (легенда ≈44 px):

| Роль | Что видно в градациях серого |
|---|---|
| Трасса | **светлая** плитка; поперёк — **3 непрерывные** тонкие светлые нити, ровные и параллельные |
| Помехи | **тёмная** плитка; поверх — **прерывистая** косо-перекрещенная решётка (±60°) с крапинами |

Каналы различия — **три независимых**: (1) **полярность** (светлое ↔ тёмное),
(2) **геометрия** (параллельные продольные ↔ перекрещенные косые), (3) **непрерывность**
(сплошные линии ↔ разорванный пунктир). Различие не опирается на плотность/`visible`-цену:
держится и в паре «Трасса 0.2 ↔ Помехи 9.0». ✔

#### §12.7 (критерий визуала) — уточнённый список пар

Проверены все соседние роли §12.7 п.8; к списку добавлены **Трасса↔Помехи** (М-5,
закрыта здесь) и **Трасса↔Тоннель** (новый риск: у обоих мотив горизонтальный):

- **Трасса↔Помехи** — полярность + геометрия + непрерывность (таблица выше). ✔
- **Трасса↔Тоннель** — у Трассы **нет рамки** (открытые сужающиеся нити), у Тоннеля —
  **замкнутый настил**: два сплошных перила + **пунктирная** настилка. ✔
- **Трасса↔Течение** — нити без направления ↔ направленные шевроны с пунктирными хвостами. ✔
- **Помехи↔Разрыв** — сплошной тёмный блок + решётка ↔ две скобки-«ворота» с вертикальной щелью. ✔
- Остальные пары §12.7 п.8 (Мгла↔Сектор, Кордон↔Сопротивление, Обрыв↔Сбой) — без смены.

**Инварианты (не трогать):** зарезервированные цвета интерактива (§12.2/§12.8), порядок
слоёв «фон → доска → курс → эффекты → HUD», детерминизм доски, отсутствие перекрытий и
выхода за канвас, бюджет §12.3. Обе фигуры рисуются **кодом**, новых PNG нет.

---

## 13. Что делать `@developer` (готовый к реализации список, без кода)

1. **`web/route.html`** — разметка: чип режима; строка обратной связи (маяки/импульсы/
   кнопка «Легенда»); **попап-легенда** (§4.9: шапка с «✕», четыре свёрнутые группы —
   Ориентиры · Поле · Секторы · Метки курса); **карточка сектора**; панель действий
   («↶ Шаг назад», «Заново», «Проложить»); результат с **скрытым** `#result-breakdown`
   (пометка «разбор по 16 факторам — отдельный чекпоинт»). Оверлеи
   `#loading`/`#error`/`#reason`/`#result` и `#toast` — по образцу.
2. **`web/static/css/route.css`** — стили новых блоков (чип режима, карточка сектора,
   **попап-легенда**: затемнение, панель, свёрнутые группы-раскрывашки, закреплённая
   кнопка закрытия, safe-area, тач-цели ≥44), метки курса, без hover-зависимостей.
3. **`web/static/js/route/route_config.js`** — словари (`SIG_LABELS`, `SURROUND_LABELS`,
   `CONTENT_LABELS`, `RISK_BY_SIG`, `MODE_LABELS`, **`MARK_LABELS`** (§4.6),
   **`LEGEND_ITEMS`** (§4.9), `bonusWord`), `REASON_OVERLAY`/`REASON_TOAST` по §8,
   палитра доски / `dir`-векторы; `n` — из `board.n`.
4. **`web/static/js/route/route_board.js` (новый)** — `cellAtPoint`/`cellCenter`,
   `neighbors4`/`adjacent4`/`dirOf`, `connectStair`, `stepHeat`, `beaconsOnPath`,
   `turnCount` (порядок логики — §4.1/§4.6).
5. **`web/static/js/route/route_legend.js` (новый)** — попап-легенда (§4.9): открытие/
   закрытие (кнопка/тап по фону/Esc/фокус), свёрнутые группы, отрисовка мини-фигур азбуки
   **тем же кодом**, что доска (**без** вскрытого содержимого и пути). Общие примитивы
   отрисовки объекта **не дублировать** — вынести из `route_render.js` (**уже 565 строк**,
   попап его не раздувает).
6. **`web/static/js/route/route_render.js`** — сетка; `visible`-цена; объекты доски
   (Мгла/Трассы/Помехи/Кордоны/Тоннели/Течения/Обрывы/Разрывы); Секторы-облака + σ-подпись
   + вскрытое содержимое; маяки/Старт/Цель; курс-цепочка + засечки Манёвров +
   предпросмотр; локальные метки курса (включая положительные); результат-анимация
   (тон по знаку `bonus`); **общие примитивы отрисовки объекта для доски и легенды**.
7. **`web/static/js/route/route_input.js`** — Pointer Events по клеткам → waypoints →
   4-связная цепочка; отмена шага/очистка; блокировка при отправке и **при открытом
   попапе-легенде**.
8. **`web/static/js/route/route_game.js`** — `boot`/`offer`, состояние (`board`, `revealed`,
   `pings_left`, таймеры, **открыта ли легенда**), `refresh`, `scan(sector)`, двухтапный
   `submit()`, гейт, статус, карточка сектора, результат (в т.ч. отрицательный).
9. **`web/static/js/route/route_sprites.js`** — маппинг косметики под доску (в т.ч.
   `false_signal` → маркер Обрыва/Миража); пулы/прелоад/фолбэк — без смены механики.
10. **`tools/e2e/route-check.js`** — смоук по §9 (включая открытие/закрытие легенды).
11. **Полировка входа (без смены механики):** тексты/состояния `#flight-panel` и
    `slot-accelerator` по §8 при необходимости.
12. **Не требуется:** изменения серверных ручек/БД/миграций; разбор по 16 факторам;
    заморозка пулов спрайтов; несуществующие ветки (`scan` `cooldown`, `offer` 409 при
    развороте).

---

## 14. Гейт

Решения создателя зафиксированы (2026-09-26): развилки О1–О6, **О8 — легенда попапом**
и **О9 — космический словарь** (§14.12 осн. спеки). Замечания критика внесены: **С-1**
(старый словарь удалён из player-facing текстов), **С-2** (легенда — четыре свёрнутые
группы; убраны путь/предпросмотр и вскрытое содержимое), **М-1** («Обрыв» не в двух
группах: метка `dead_end` — **«Срыв»**, объект — «Обрыв»), **М-3** (единый термин
действия разведки — **«Зондировать»**, сведено с `@designer`/осн. §14.12.9), **М-5**
(Трасса↔Помехи — разными мотивами паттерна, **§12.13**; `@gdesigner`). Ранее внесены: коды
отказов `scan`/`boost`, поведение `offer` при смене сегмента, положительные метки,
остатки старой модели.
Список §13 «Что делать `@developer`» **актуален** (в т.ч. новый модуль `route_legend.js`).
Спека готова к передаче в разработку по команде менеджера; сам в разработку не иду.
