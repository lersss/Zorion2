# 2026-09-25 — Мир прогулки: скульптурный объём (Э5): приложение (справочное)

**Статус:** 🔄 дизайн — **готова к проверке создателем** (v3, правки @critic 2-го раунда внесены)
**Дата:** 2026-09-25
**Родитель:** `docs/specs/2026-09-25-мир-прогулки-скульптурный-объём.md`
**Зачем вынесено:** основная спека — компактный исполнимый документ для `@developer`/`@tester`; здесь справочное (§A.1 разворот 5 классов, §A.2 таблица науки, §A.3 ощущение) **и вынесенные из основной спеки §B.1 план по чекпоинтам (бывш. §4), §B.2 пометки на смежное (бывш. §7), §B.3 журнал (бывш. §10)**. Правила, инварианты и решения — в родительской спеке; этот файл их не дублирует, только детализирует и хранит историю.

---

## A.1. Разворот подходов: 5 классов реализации 2D-поля (справочно)

Пять **разных классов** (не вариации одного). Рычаг у каждого — свой: как представляется и вычисляется `solidAt(x,y)`.

### A — «Столбцы-интервалы» (column spans / run-length)

Твёрдое множество столбца `x` — **объединение вертикальных интервалов** `[{yTop,yBottom}, …]`, вычисляемое функцией `columnSpans(x)`. Физика: `solidAt(x,y)` = поиск в интервалах (двоичный). Растр: заливка интервалов. Это прямое обобщение текущей модели (поверхность + полоса `float` = два интервала).

- **Цена:** низкая — O(1) на столбец для аналитических форм, O(log k) на запрос; нет плотного хранилища. **Риск:** низкий — контракт «один список интервалов» естественно даёт согласие физики и растра.
- **Даёт:** объём (нависание = лишний интервал, грот = разрыв между интервалами), дёшево. **Слабость:** пещеры (2D-шум) не сводятся к интервалам чисто — остаются отдельной маской (как сейчас).

### B — «Плотное поле чанка» (dense chunk bitmap)

На чанк один раз растеризуется булев/байтовый грид `solid[CHUNK+1][CHUNK_HEIGHT]`; и физика, и растр читают **этот грид**.

- **Цена:** высокая — `(CHUNK+1)·CHUNK_HEIGHT ≈ 437 000` сэмплов на чанк (при `SS=2` — 1.75 млн); память +437 КБ (byte) … +1.75 МБ на чанк, при `CHUNK_RADIUS=3` — +3…12 МБ на мир.
- **Риск:** низкий по согласию (одно хранилище), но высокий по **производительности и памяти**. **Даёт:** максимальную надёжность «одно поле» — но это ровно тот «наивный» путь, о котором предупреждает задание.

### C — «1D-база + локальные 2D-наросты» (гибрид)

База остаётся `terrainHeight(x)` (земля + пещеры + полоса `float`); 2D-формы — **локальные операторы в ограниченной окрестности** (на регион/фичу): `overhang` добавляет материал, `void` вычитает, `crater` меняет рельеф кольцом, `crack2d` — косметика (`viewOnly`, Э5.2). `solidAt(x,y)` вычисляется аналитически, без плотного хранилища.

- **Цена:** низкая — пропорциональна числу фич и их площади, а не `CHUNK_HEIGHT` на каждый столбец. **Риск:** средний — нужно свести «база ⊕ формы» в одну функцию и не завести второй путь растеризации.
- **Даёт:** объём дёшево; столбцы без форм сохраняют текущий быстрый путь (нулевая регрессия ЧК0–ЧК5).

### D — «SDF-скульптура» (CSG-операторы над базой)

База — знаковое расстояние `d(x,y) = y − terrainHeight(x)` (положительно под поверхностью); формы — конструктивные операторы: вычесть капсулу (арка), вычесть сферу (грот), добавить эллипс (шляпа худу). `solidAt = (d_итог < 0)`.

- **Цена:** средняя — на запрос несколько операторов; локальность через индекс фич. **Риск:** средний — композиция может давать «тонкие» стенки и артефакты при грубой выборке.
- **Даёт:** самую естественную композицию форм (арка = вычитание из перемычки), но это **реализация** класса C, а не отдельный класс хранения.

### E — «Поле плотности/твёрдости материала» (density field)

Твёрдость = `density(x,y) > порог`, где `density` — шум, а формы «выращиваются» эрозией по твёрдости слоёв.

- **Цена:** высокая (много шума на пиксель). **Риск:** высокий — форма **неуправляема** (нельзя гарантировать арку/свод заданной формы), детерминизм формы хрупок.
- **Даёт:** органичность, но противоречит требованию «достоверно и по рецепту». **Отклоняется.**

### A.1.1. Сводная таблица сравнения

| # | Класс | Цена | Риск | Объём | Согласие физики/растра | Регрессия ЧК0–ЧК5 |
|---|---|---|---|---|---|---|
| A | столбцы-интервалы | низкая | низкий | да | по построению | нет |
| B | плотное поле чанка | высокая | средний (перф/память) | да | максимальное | нет |
| C | 1D-база + локальные наросты | низкая | средний | да | требует дисциплины | нет |
| D | SDF-скульптура | средняя | средний | да | требует дисциплины | нет |
| E | поле плотности | высокая | высокий | да | слабое | нет |

**Почему не B:** при `SS=2` канвас чанка уже ≈7 МБ (257·1700·4·4); плотный грид добавит ещё 3…12 МБ на мир, а выигрыш по согласию даёт и A/C при меньшей цене. **Почему не E:** форма перестаёт быть рецептом.

---

## A.2. Сверка с наукой §32.1 — полная таблица (справочно)

| Форма | Наука §32.1 (кратко) | Дизайн Э5 | Вердикт |
|---|---|---|---|
| Арка | пролёт 10–90 м, свод 5–15 м, стрела 1/3–1/2, толще у пят, тоньше в замке | `overhang.arch=1`: пролёт 80–320 px, свод 20–60 px, `taper` (тоньше в замке), `dir` | ✅ пропорции взяты |
| Козырёк | вынос м–десятки м, наклон 10–40°, тоньше к краю | `overhang.arch=0`: `reach` 30–120 px, `taper` | ✅ |
| Худу/гриб | шляпа 2–3× ножки, высота 5–30 м, ножка 1/4–1/2 | `spike` (ножка) + `overhang` (шляпа), cap 2–3× stem | ✅ композиция |
| Грот | глубина < ширины, высота 2–10 м, ширина 5–30 м | `void.shape=grotto`: w 60–220, h 50–180, depth 40–200 | ✅ |
| Лавовый тоннель | диаметр 1–20 м, кровля 1–10 м, уклон малый, длина км | `void.shape=tube`: w/h 60–180 | ⚠️ длина/уклон не моделируем (бесконечный мир) |
| Свод со сталактитами | высота 20–100 м, купол, сталактиты 0.01–1 мм/год | потолок биома `пещерный_мир_с_потолком` даёт **сплошная полоса `float`** (остаётся, §3.4/§3.6); `overhang arch=1` — **объёмная деталь**; `void` — неглубокие выходимые ниши (`grotto`), полость под плитой — частный случай; сталактиты — **декор, не форма** | ⚠️ сталактиты отложены |
| Кратер/кальдера | глубина ≈ `d/5`, стенки 30–40°, вал, осыпь | `crater`: `depth` + `wallDeg` задают чашу, `wallDeg` — **фактический** уклон; научные `30–40°`/`d ≈ 5·depth` — **ориентир**; **фактические** диапазоны витрины `кратеры` — **`wallDeg [26,34]`, `d [250,500]`, `depth [40,80]`** (осознанное смягчение ради выходимости); `caves` дельты **ослаблен `0.7 → 0.3`** — дно чаши не вскрывается пещерами, чаша остаётся выходимой (§3.6); `rim`, `talus` | ✅ пропорции взяты; уклон/абсолют смягчены осознанно |
| Трещина | ширина 0.1–10 м, глубина десятки–сотни м, клин шире вверху | `crack2d`: w 6–40, depth 80–400, `taper`, `tilt` 0–30; **Э5.2 — косметика** (`viewOnly`, физику не трогает) | ✅ |

**Осознанное расхождение (названо, подтверждено создателем 2026-09-25):** абсолютный масштаб **стилизованный** (умолчание дизайнера принято). `PPM=40` — визуальный масштаб; арка 10–90 м = 400–3600 px, но регион 1400 px, видимая ширина ~1067 px. Настоящая арка заняла бы почти весь экран. Поэтому берём **отношения и углы внутри формы** (стрела/пролёт, `d/5`, 30–40°, клин), а абсолютные метры — нет. То же, что §4.4 спеки рецептов («масштаб нефизический»), чтобы числа §32.1 не применялись дважды (метры → px).

**⚠️ Warning (сильное расхождение), сохраняется:** по абсолютным размерам дизайн **на порядок меньше** науки (арка 80–320 px против 400–3600 px). Это осознанно (стилизованный вид сбоку, регион 1400 px), но помечено как warning — менеджер покажет создателю.

---

## A.3. Ощущение (§32.2 @writer) — что берём (справочно)

- «Камень медленный» → формы **статичны**, обвалов нет (детерминизм, кэш).
- Холод/тишина/эхо под сводом → атмосфера грота (затемнение под сводом — уже есть глубинный градиент; звук — вне объёма).
- «Свод толстый, он гора, а не крыша» → `void.shape=dome` с толстым сводом, не тонкой полкой.
- «Сосульки почти сходятся, просвет — кто-то не доделал» → сталактиты/сталагмиты как **декор** (отложено, кандидат).
- «Заглянешь в трещину — тянет холодом» → `crack2d` — **косметический тёмный разлом** (Э5.2, `viewOnly`): затемнение-клин даёт «сквозной» вид, физику (твёрдость/якоря) **не трогает**.

---

## B.1. План по чекпоинтам (вынесено из §4 основной спеки)

### B.1.1. Э5.1 — фундамент (новых форм нет)

**Что делаем:** земля переезжает на `solidAt(x,y)`; физика и растр читают **одно** поле; `columnSpans` (гипотеза); замер перфа; вытеснение кэша чанков; расширение серверного бюджета вертикали под будущие формы; **пещерная маска приводится к консервативному растру** (граница — шаг 1 px); **угловая коллизия** (бокс 12×28).

**Критерии готовности:** (1) `solidAt` — единственный источник, второй реализации коллизии/растеризации нет; (2) юнит согласия **по твёрдому телу** `solidAt(x,y) ⇄ rasterPainted(x,y)` на сетке seed×биомы (±1 px; косметика — вне сравнения, §2.1), столбцы с формой **и границы пещер** — шаг 1 px, под-покраска твёрдого отсутствует; (3) все биомы ЧК0–ЧК5 сохранили **твёрдое поле/силуэт** (`solidAt`/`terrainHeight`; замороженный кадр) — **косметическая дельта на границе пещер** (консервативный растр, шаг 1 px) **допустима**, замеряется и предъявляется числом (сколько px, только ли кромка); (4) **угловая коллизия не создаёт новых застреваний и стен** на существующих биомах — стресс-прогон узких мест (узкие щели, полоса `float`, кромки пещер); (5) e2e-смоук: 0 `pageerror`, нет застреваний/невидимых стен; (6) перф (генерация чанка, память) в бюджете, baseline зафиксирован; (7) кэш чанков вытесняется.

**Что видит создатель:** видимого изменения **нет** (скромный результат, как решено §32.1); доступен **debug-оверлей** (поле твёрдости vs растр) и стресс-прогулка. Гейт технический: «физика и растр — одно поле, скорость приемлема».

### B.1.2. Э5.2 — формы

**Что делаем:** `overhang`, `crack2d` (косметика); дельты `горы` (арки, нависания, грибные башни), `каменные_пустоши` (нависания, тёмные разломы-косметика); **конкретные `forms`-дельты — §3.5** (готовы к вставке 1:1 в обе копии справочника); серверная валидация `relief.forms`; расширение `declaredReliefTop` (**суммой**: `arch=1` → `opening + h`, `arch=0` → `h`); **регистрация `crater` в `view_primitives`** (обе копии справочника, счёт `4 → 5`) + правка теста `32 → 33` — **согласовано с §3.1/§3.4** (стадия разведена: регистрация/тест — здесь, применение `crater` на `кратеры` — Э5.3).

**Два применения `arch=1` разведены:** на **Э5.2** `overhang arch=1` — **природные арки** на `горах`; на **Э5.3** — **плита-свод** `пещерного_мира_с_потолком` (кровля полости `void dome`). Механика одна, различают биом-дельта и наличие `void` под плитой.

**Критерии готовности:** (1) на `горах` видны арки/нависания/грибные башни, на `каменных_пустошах` — тёмные разломы-косметика (контактный лист); (2) под аркой есть проход — просвет `opening` по диапазону `[50,160]` гарантированно > роста игрока (28), игрок проходит; под козырьком `arch=0` прохода по `opening` не требуется (§3.5); (3) согласие растр/физика на столбцах с формами; (4) прочие биомы не изменились; (5) битый `forms` уводит **один** биом в фолбэк (нефатально); (6) бюджет вертикали (**сумма форм**, `relief.scale` к формам не применяется) и перф в норме; (7) `crater` зарегистрирован в обеих копиях, тест `33` — зелёный; (8) `crack2d` — **косметический (`viewOnly`)**: `solidAt`/`floorY`/`skyTop`/`columnSpans` не меняет, провалов/ловушек нет (перепрогон @tester на реальном рельефе — зелёный). **Потребителей якорей в Э5.2 не переключаем** (`floorY`/`skyTop`) — это критерий Э5.3.

**Что видит создатель:** первая настоящая арка/нависание на `горах` — витрина «офигеть».

### B.1.3. Э5.3 — гроты и своды

**Что делаем (безопасный план, создатель 2026-09-25):** `void`; **применение `crater`** на биоме `кратеры` (регистрация `crater` — **Э5.2**, §3.1/§B.1.2; здесь только запись в дельте биома); `пещерный_мир_с_потолком` — **сплошной потолок + объёмная деталь**: `relief.float: true` **остаётся** (сплошная полоса = потолок биома), **добавляются** `overhang arch=1` (арки/своды) и неглубокие `void`-ниши (§3.4/§3.6); лавовые трубки (`void tube`, сквозные); кольцевые кратеры/кальдеры на `кратерах`. **Конкретные `forms`-дельты — §3.6** (готовы к вставке 1:1). **Механизм «выходимость» — отдельный пункт код-работы** (детерминированная симуляция игрока, а не flood-fill; откат или косметика; §3.6).

**Критерии готовности:** (1) `пещерный_мир_с_потолком` — **сплошной потолок сохранён** (`float: true`), поверх — **арки/своды** `overhang arch=1`, под пологом — неглубокие выходимые ниши (`void`); (2) лавовые трубки на `лавовых_полях`/`магмовом_океане` (сквозные, два открытых конца), кольцевые кратеры/кальдеры на `кратерах` (фактические `wallDeg [26,34]`/`d [250,500]`/`depth [40,80]`, `caves` дельты **`0.3`** — дно чаши не вскрывается пещерами, чаша выходима); (3) согласие **по твёрдому телу**, нет застреваний в потолке (угловая коллизия); (4) прочие биомы не изменились; бюджет **двух сторон** в норме (`declaredReliefTop`/`declaredReliefBottom`); **(5) якоря переключены:** корабль/фауна/спавн → `floorY`, приземная погода → `skyTop` (§5 п.12) — после `void` `floorY ≠ terrainHeight` (`crack2d` — косметика, `floorY` не меняет); оговорка `skyTop` ≤0.983 px учтена (§3.6); **(6) выходимость (§5.1 п.5) подтверждена симуляцией:** из наихудших точек каждой вычитающей формы игрок выходит на поверхность — под пологом, в трубе, в кратере; отказ → форма откатывается; e2e — нет ловушек.

**Что видит создатель:** витрина «офигеть» — `пещерный_мир_с_потолком` (сплошной потолок + арки/ниши), гроты и лавовые трубки.

---

## B.2. Пометки на смежное (вынесено из §7 основной спеки)

### B.2.1. Связи механик (impact map) — для `docs/impact_map.json` (@manager переносит)

**Новая сущность-механика `surface_solid_field`** (`type: "mechanic"`, `path: web/static/js/surface/surface_world.js`) — рёбра (каждая пара «цель + тип» уникальна; цель адресуется по пути, если файла нет в реестре как сущности):

- `affects` → `web/static/js/surface/surface_player.js` — коллизия читает `solidAt`; **спавн игрока** читает `floorY(x)`, не `terrainHeight` (S4).
- `affects` → `web/static/js/surface/surface_render.js` — растр читает то же поле; **корабль на высадке** `drawShip` и точки **фауны** — якоря `floorY(x)` (S4).
- `affects` → `web/static/js/surface/surface_world.js` — **точки фауны**: `terrainHeight(x)` → `floorY(x)`; `terrainHeight`/`isSolid` → единое `solidAt` + `relief.forms`.
- `affects` → `surface_weather` — приземные эффекты/точка земли читают **верх** поля `skyTop(x)` («поверхность неба»), не `terrainHeight`.
- `reads` ← `surface_view_recipe` (формы — из рецепта `relief.forms`).
- `affects` → `biome_catalog.json` — новые данные `relief.forms`, **регистрация `crater`** (4→5), расширенная валидация.
- `affects` → `biome_catalog_seed.json` — та же регистрация `crater`; синхронность копий справочника (`TestBiomeViewSeedSynced`).
- `affects` → `web/static/js/surface/surface_config.js` — константы: минимальный просвет, потолки форм.
- **`planet_image` — связи НЕТ** (2D-формы не видны с орбиты, бамп `ImageGenVersion` не нужен) — явная пометка, чтобы не завели ложное ребро.

**Обновление существующего ребра** `surface_view_recipe` → `web/static/js/surface/surface_world.js`: причина дополняется — `terrainHeight`/`isSolid` → единое `solidAt` + `relief.forms`.

**Якоря на поле (S4) — отдельная пометка.** Приземные эффекты погоды (сегодня `terrainHeight`, `surface_weather.js`) → `skyTop`; корабль на высадке (`surface_render.js` `drawShip`), фауна (`surface_world.js`, `surface_render.js`) и **спавн игрока** (`surface_player.js`) → `floorY` (§5 п.12): под нависанием якорь на голом `terrainHeight`/`skyTop` вмуровывается в материал или встаёт на потолок.

### B.2.2. Прочие дельты

- **`docs/pitfalls/design.md`** (раздел «Дизайн и числа»; владелец @dispatcher) — **пять** ловушек: (1) «растр обязан идти от того же поля, что физика; второй быстрый путь для основного тела при 2D-формах даёт невидимые стены; **пещерная маска — часть твёрдого тела, грубый блок 3×3 допустим только для полностью воздушного блока**»; (2) «бюджет вертикали обязан включать 2D-формы (`declaredReliefTop` = **сумма** по аддитивным несущим: `overhang` `arch=1` → `overhang.opening + h`, `arch=0` → `h`; `crater.rim`; **`relief.scale` к формам не применяется** — формы абсолютные px), иначе твёрдая невидимая земля»; (3) «`relief.forms` — список: дельта биома **затирает** пресетный список целиком (как `horizon`)»; (4) «вычитающая 2D-форма (`void`, впадина `crater`) обязана быть **выходимой пешком**: проверка — **детерминированная симуляция физики игрока** (не flood-fill), домен — регион/чанк формы; глубина ограничена относительно ширины, стенки ≤ `wallDeg 40`, `open` ≥ `PLAYER_W+2`; диапазоны данных проходимость **не гарантируют** — форма, из которой выход не подтверждён, **не применяется** (откат) либо переводится в косметику. Опыт Э5.2 `crack2d` → косметика; механизм — §3.6»; (5) «одиночная проверка выходимости формы **недостаточна**: ловушку создаёт **комбинация соприкасающихся вычитающих форм** (`void`/`crater`) — проверка обязана идти по **полному полю** (`baseSolid ⊕ пещеры ⊕ ВСЕ формы`), а не по одной форме; форма, выходимая по отдельности, запирает в паре (§3.6 п.1, §5.1 п.5)».
- **`docs/GLOSSARY.md`** (кандидаты): «поле твёрдости (`solidAt`)», «поверхность неба (`skyTop`) / пол-опора (`floorY`)», «скульптурная форма (`relief2d`)», «пролёт/замок/пята» (по нужде).
- **`docs/specs/2026-09-21-высадка-прогулка-по-планете.md` §7.2** — «ландшафт: fbm-высоты + пещеры + формации» → «ландшафт: 1D-профиль + 2D-поле твёрдости (`solidAt`), формы `relief2d`»; §7.4 — коллизия читает `solidAt` (угловая выборка, числа не меняются); **якоря (корабль/фауна/спавн) — `floorY`, приземная погода — `skyTop`** (S4).
- **`docs/specs/2026-09-23-мир-прогулки-рецепты-биомов.md`** (владелец — @designer; дельты, не правлю чужие разделы без гейта):
  - §3.1 — подраздел «relief2d» (`relief.forms`, **5** примитивов: `overhang`/`void`/`crack2d`/`crater`/`floatband`, валидация); регистрация `crater` в обеих копиях справочника (**счёт `relief2d` `4 → 5`**).
  - **§2.2/§3.5 — счёт «4 поздних 2D» → «5 поздних 2D»** и арифметика реестра: «27 записей + 4 поздних 2D» → «**28 записей + 5 поздних 2D**»; «27 кусков + 4 поздних 2D» → «**28 кусков** (8 `relief1d` + 16 `decor` + лиана `hang` + 3 `placement`) + 5 `relief2d` = **33 записи**».
  - **§6 п.3 («спавн на корке»)** — уточнение: спавн игрока и прочие якоря читают **`floorY`** (пол/опора), приземная погода — **`skyTop`**; «спавн на корке» сохраняется как поведение по умолчанию (на обычной колонке `floorY = terrainHeight`).
  - §5 — строка Э5 уточняется (три чекпоинта).
  - **§3.1.1 — механизм «выходимость»** для вычитающих `relief2d` (`void`, впадина `crater`): **симуляция физики игрока** (не flood-fill), домен регион/чанк, глубина ≤ ширины, стенки ≤ `wallDeg 40`, `open` ≥ `PLAYER_W+2`; при отказе — откат формы или косметика (решение §3.6 Э5; опыт Э5.2 `crack2d`).
  - **§3.6 Э5 (новая)** — конкретные `forms`-дельты Э5.3 (`пещерный_мир_с_потолком` — **сплошной `float:true` не снимается** + объёмная деталь, лавовые трубки, `crater` с фактическими `wallDeg [26,34]`/`d [250,500]`/`depth [40,80]`, `caves 0.3` ослаблен) + критерий **«выходимость»** (полное поле, комбинация соприкасающихся вычитающих) + таблица якорей.
  - **M2 (внесено):** §6 п.6 рецептов приведён к нижней границе `terrainHeight(x) ≤ baseY + CHUNK_HEIGHT − CHUNK_TOP_MARGIN − 24` = **`baseY + 476`** (низ растра `baseY + 500`); §10 п.7 — тоже.
  - **M3 (внесено):** §6 п.8 рецептов несёт **`CHUNK_TOP_MARGIN = 1200`** (в коде `surface_render.js`, поднято в Э3); дрейф закрыт.
- **`docs/gamedesign/99_roadmap.md` §99.2** — ведёт @manager (строку добавит он).
- **`docs/TAILS.md`** — закрыть хвост «вытеснение кэша кусков земли» (входит в Э5.1); **добавить хвост «дрейф `CHUNK_TOP_MARGIN` 700 vs 1200 в спеке рецептов §6 п.8»** (M3); **добавить кандидат-хвост «перевод декора/`where` на `floorY`»** (M11); хвост «LOD/кап декора» (58) — **не закрывается** здесь.

---

## B.3. Журнал (вынесено из §10 основной спеки)

- 2026-09-25: спека Э5 создана (@designer). Разворот 5 классов реализации 2D-поля; рекомендация C+A (1D-база + локальные наросты, контракт `columnSpans`); архитектура единого `solidAt` для физики и растра; данные `relief.forms` (4 примитива `relief2d`, параметры, нефатальная валидация); план Э5.1/Э5.2/Э5.3; инварианты (двустороннее «что твёрдо — то видно», угловая коллизия, бюджет вертикали с формами, вытеснение кэша); сверка с §32.1/§32.2 (warning по абсолютному масштабу); impact map; кандидат ГД-сущности «Укрытие»; 13 вопросов. Ждёт @critic → гейт создателя.
- 2026-09-25: **гейт создателя — «ок»** (направление принято в разработку). Решения: **масштаб стилизованный** (умолчание дизайнера); кандидат **«Укрытие» принят как кандидат** (в числа Э5 не входит).
- 2026-09-25: **1-й раунд @critic — правки внесены одним сводным батчем.** Средние: **S1** — инвариант сужен до **твёрдого тела**, косметика (viewOnly-рябь, кромка, снег, градиент, пещерная маска 3 px) объявлена **вне равенства**; **S2** — свод `пещерного_мира_с_потолком` собирается из **несущей аддитивной** плиты-свода (`overhang arch=1`) + `void dome`; **S3** — `crater` **регистрируется** в `view_primitives` (`kind=relief2d`, обе копии, счёт `4→5`) + разведён с 1D `carve`; **S4** — якоря читают `surfaceTop`. Мелкие: **M1** — диапазоны `void` сведены к единому источнику; **M2** — нижняя граница бюджета = `baseY + 476`; **M3** — дрейф `CHUNK_TOP_MARGIN` 700 vs 1200 помечен; **M4** — запасной ход «понижение `SS`» снят; **M5** — `surfaceTop(x)` введён в контракт; **M6** — в приёмку Э5.1 добавлен критерий «угловая коллизия не создаёт новых застреваний/стен». Оформление: справочное вынесено в **приложение**.
- 2026-09-25: **3-й (финальный) раунд @critic — долг добран точечными формулировками (не блокирующие замечания, `@designer`).** (1) **§2.1** — глубинный градиент: снята формулировка «вне канваса чанка» → рисуется **и внутри** канваса чанка (`source-atop` в `getChunkCanvas`), **и** отдельным экранным проходом `drawDepthGradient`; на равенство по твёрдому телу не влияет (лишь тонирует нарисованное). (2) **критерий Э5.1 п.(3) (§B.1.1) и регрессионный тест §5.1 п.(4)** — «0 изменённых строк» переформулировано в **«неизменность твёрдого поля/силуэта»** (`solidAt`/`terrainHeight`); **косметическая дельта на границе пещер** (консервативный растр, шаг 1 px) **допустима**, замеряется и предъявляется числом (сколько px, только ли кромка) — решение менеджера 2026-09-25 (идея §32.4–§32.6). Дельта к спеке рецептов (§5/§2.2/§3.5/§6) и регистрация `crater` — остаются на чекпоинт Э5.2.
- 2026-09-25: **2-й раунд @critic — правки внесены; спека сведена к консистентному виду.** Средние: **S1-bis** — пещерная маска объявлена **частью твёрдого тела** (не косметикой), растр **консервативен**, граница пещеры — шаг 1 px (§2.1/§2.3/§5 п.1/§5.1.1); **S2-bis** — введена формула вертикального размещения плиты-свода (`низ = terrainHeight + opening`, `верх = terrainHeight + opening + h`), в бюджет входит `opening + h` (§3.2/§3.3/§5 п.6); **S2-ter** — полость свода разведена с плитой (плита — кровля; полость вырезается из базы вверх до нижней грани плиты; высота — от дна; §2.2/§3.2/§3.4); **S4-bis** — якоря разведены: **«поверхность неба» `skyTop` (погода)** против **«пол/опора» `floorY` (корабль/фауна/спавн)**, дельта к §6 п.3 рецептов (§2.1/§5 п.12/§5.1.6/§9 Д.12). Мелкие: **M5-bis** — единый список якорей во всех местах; **M6** — правка теста реестра `32→33` и «5 поздних 2D» (§3.1); **M7** — факт серверной проверки без `+24`, Э5 приводит к `+24` (§3.3); **M8** — `opening` не «обязан», а гарантирован диапазоном; warning — сетка безопасности (§3.3); **M9** — `crater` — одна запись, два знака (вал аддитив + впадина) (§2.2/§3.2); **M10** — `arch` — фиксированный флаг `1`/`0`, не диапазон (§3.2); **M11** — декор/`where` на `terrainHeight` — принятое косметическое ограничение + кандидат-хвост (§2.1/§B.2.2). Реестр: **§B.2.1 приведён к валидному виду** — висячий `surface_player` заменён путём `web/static/js/surface/surface_player.js`; дубли пары «цель+тип» слиты; составной `biome_catalog.json / biome_catalog_seed.json` разбит на два ребра. Оформление: §4/§7/§10 вынесены в приложение (§B.1/§B.2/§B.3), основная спека сжата до ≤~40 КБ, обновлены строка `docs/specs/README.md` и строка приложения. Готово к финальной проверке создателя.
- 2026-09-25: **Э5.2 — конкретика форм и исправление знака (по заданию менеджера).** (1) **§3.2** — формула вертикального размещения плиты исправлена: было `низ = terrainHeight + opening`, `верх = terrainHeight + opening + h` (плита уходила **в породу**, противоречие §2.1/§2.2/§5 п.12); стало **`низ = terrainHeight − opening`, `верх = terrainHeight − opening − h`** (плита висит **над** поверхностью; в мире `y` растёт вниз), в бюджет вверх — `opening + h`. Добавлено правило `arch=0` (консоль/козырёк/шляпа крепится к опорной колонке `x₀`, подошва = `terrainHeight(x₀)`, вынос `reach`, `taper` к краю) и явное «`opening` гарантирован диапазоном `[50,160]` > 28, warning — сетка безопасности». (2) **§3.5 (новая)** — конкретные `forms`-дельты Э5.2 для `горы`/`каменные_пустоши` (готовы к вставке 1:1), правило «формы — только в дельтах биомов, пресеты не трогаем», композиция грибных башен и **названная неоднозначность** (нет якорного ключа шляпа←`spike`; учёт `arch=0` в `declaredReliefTop`). (3) **§3.4/§B.1.2** — `arch=1` разведён на два применения: природные арки `горы` (Э5.2) против плиты-свода `пещерного_мира_с_потолком` (Э5.3). (4) Формула исправлена во всех местах (§3.2/§3.4/§6, §A.2). Дельты к спеке рецептов (счёт `4→5`, `crater`, `§6 п.3/п.6/п.8`, §5) — в §B.2.2.
- 2026-09-25: **Решения менеджера по неоднозначностям Э5.2 (добор).** (1) Шляпа `overhang arch=0` ↔ ножка 1D-`spike` — **вероятностное совпадение** (вариант «а»), ключ-якорь `from` **не вводим**, вынесен в кандидаты-хвосты (§3.5/§9). (2) `declaredReliefTop` для `overhang`: `arch=1` → `opening + h`, `arch=0` → `h`; берётся **максимум** по аддитивным несущим формам (§3.3). (3) **Стадия `crater` разведена:** регистрация в `view_primitives` (обе копии, счёт `4→5`) и правка теста `32→33` — **Э5.2**; применение на биоме `кратеры` — **Э5.3** (§3.1/§3.4/§9 Г.10). (4) В спеке рецептов §10 п.7 нижняя граница бюджета исправлена на **`baseY + 476`**. (5) §3.5 основной спеки — пометка «файл > 40 КБ → при следующей правке одна полная запись файла».
- 2026-09-25: **@critic (3 средних + 4 мелких) — решения менеджера внесены.** (1) **План чекпоинтов:** регистрация `crater` (обе копии, `4→5`) + тест `32→33` перенесены в **§B.1.2 (Э5.2)**; из **§B.1.3 (Э5.3)** убрана регистрация, осталось применение `crater` на `кратеры` — согласовано с §3.1/§3.4. (2) **Бюджет форм — СУММА, не максимум** (заменяет «максимум» из предыдущей записи): `declaredReliefTop` = `Σ` аддитивных несущих (`overhang` `arch=1` → `opening + h`, `arch=0` → `h`; `crater.rim`), единая методология с `rise` 1D-слоёв; **`relief.scale` к формам НЕ применяется** (абсолютные px). Поправлено в §3.3/§3.5/§5 п.6 Э5 и §3.1.1/§6 п.6 рецептов, §B.2.2 приложения. (3) **Э5.3 — замена заглушки `float`:** в дельте `пещерного_мира_с_потолком` задать **`relief.float: false`** (пресет `экзотика` — не трогаем) — §3.4/§B.1.3. (4) **§3.3 — факт кода:** «проверка без `+24`» устарела, Э5.1 (`5a4a505`) уже ввёл `viewTopSafety = 24`; §B.2.2 M2/M3 приведены к факту (внесено). (5) **Счётчики реестра:** арифметика §2.2/§3.5 рецептов исправлена на «**28 записей/кусков + 5 поздних 2D = 33**» (учтена лиана `hang`). (6) **§3.1.1 рецептов:** в `declaredReliefTop` добавлен `arch=0` → `h`. (7) **Якоря `floorY`/`skyTop`** — критерий **Э5.3** (§B.1.3); в Э5.2 потребителей не переключаем.
- 2026-09-25: **Э5.3 — конкретные `forms`-дельты (§3.6 спеки Э5).** Добавлены готовые JSON: `пещерный_мир_с_потолком` (дельта `relief.float: false` + плита-свод `overhang arch=1` + полость `void dome` под плитой, `open`), `лавовые_поля`/`магмовый_океан` (`void tube`, `open` — сквозная, без жидкости), `кратеры` (`crater` — вал + впадина). Зафиксирован **жёсткий критерий «не запирает»** (§5.1 п.5) с **механизмом** (связность/flood-fill, проходимый `open`, наклонные стенки, режим отказа → откат/косметика; опыт Э5.2 `crack2d`). Якоря-потребители таблицей (корабль/фауна/спавн → `floorY`, приземная погода → `skyTop`). Открытые неоднозначности названы: якорь `void`←`overhang` (вероятностная композиция), непрерывность свода (дискретные плиты vs глобальная полоса — вопрос к гейту), семантика «два конца» `open` для `tube`, уклон входов (рампы) без нового параметра.
- 2026-09-25: **Э5.3 — переписан под безопасный план (создатель «ок»); исправление крупного @critic.** (1) **Свод `пещерный_мир_с_потолком`** — не «замена заглушки», а **сплошной потолок + объёмная деталь**: `relief.float: true` **остаётся** (сплошная полоса = потолок биома), **добавляются** `overhang arch=1` + неглубокие `void`-ниши; дельта `float: false` **снята** (§3.4/§3.6). (2) **Критерий «не запирает» → «выходимость»:** проверка — детерминированная **симуляция физики игрока** (не flood-fill), домен регион/чанк; отказ → откат формы либо косметика (§3.6/§B.1.3/§5.1 п.5). (3) **Лавовые трубки** — выбран **вычитающий `void tube`** (косметика потребовала бы нового флага `viewOnly` на `void`). (4) **`crater`** — свободные `depth` + `wallDeg` задают фактическую геометрию (уклон = `wallDeg`); `d` клампится вверх до `2·(rim + depth/tan(wallDeg))`. (5) **`declaredReliefBottom`** введён: сумма глубин вычитающих (`void.depth`, впадина `crater.depth`), низ `≤ baseY + 476`. Мелкие: `void.h` — один смысл (свободная высота выреза; в дельтах не задаётся); `void.w` — полуширина, `crater.d` — полная ширина; спавн/коридор — **код-правило** (не `where`); оговорка `skyTop` ≤0.983 px перенесена в §3.6 (в Э5.3 `skyTop` подключён к якорям погоды). Приложение §B.1.3/§B.2.2, рецепты §3.1.1 — синхронизированы.
- 2026-09-25: **`crack2d` — переведён в КОСМЕТИКУ (Э5.2, по итогам @tester; заменяет предыдущее решение «кламп физического просвета»).** @tester (High): вычитающая `crack2d` на реальном рельефе `каменные_пустоши` запирает игрока (seed 1, x=892.5 — провал, макушка на 87 px ниже поверхности, выхода нет) — нарушение жёсткого §5.1 п.5; кламп ширины не помог (наклон/рельеф → воронка). Решение менеджера: на Э5.2 `crack2d` — **косметический (`viewOnly`) тёмный разлом** (та же геометрия, рисуется затемнением; `solidAt`/`floorY`/`skyTop`/`columnSpans` не меняет). Зафиксировано в §3.2/§3.5/§5 п.1 спеки Э5, §A.3/§B.1.2 приложения, §3.1.1 рецептов. Кандидат-хвост (Э5.3/далее): глубокие вычитающие разломы с механикой (провал/выбирание) — только отдельным решением создателя.
- 2026-09-25: **`crack2d` — кламп физического просвета (Э5.2, по итогам @developer/@reviewer; снято решением выше).** Реализация клампит физическую ширину щели у поверхности до **`PLAYER_W − 2` = 10 px** (`PLAYER_W = 12`), иначе щель шире коллизионного бокса запирает игрока на дне без выхода (§5.1 п.5). `w` дельты (`[10,34]`) — **визуальный** размах; форма **выглядит сквозной**. Зафиксировано в §3.2/§3.5 спеки Э5; широкие разломы/провалы — `void`/`crater` (Э5.3). Помета: при следующем касании — при необходимости отдельный дизайн-вопрос (глубокие чимнеи с падением/выбиранием).
- 2026-09-25: **Точечная правка по итогам прогона @tester (D2/D3) и фикса D1.** (1) **D2** — в дельте `кратеры` (§3.6) явно закреплён `relief.caves: 0.3` (пресет `скальные пустоши` — `0.7`): ослаблено, чтобы дно чаши не вскрывалось пещерами; чаша остаётся выходимой. Приложение синхронизировано (§A.2 кратер, §B.1.3 п.2). (2) **D3** — зафиксировано осознанное смягчение `crater` ради выходимости: **фактические** диапазоны `wallDeg [26,34]`, `d [250,500]`, `depth [40,80]` у `crater` (§3.2) и у дельты `кратеры` (§3.6); научный `30–40°` — **ориентир**, фактический уклон = объявленному `wallDeg`; §A.2 синхронизирован. (3) **D1 (новое дизайн-правило)** — одиночная проверка выходимости формы **недостаточна**: ловушку создаёт **комбинация соприкасающихся вычитающих форм** (`void`/`crater`); проверка обязана идти по **полному полю** `baseSolid ⊕ пещеры ⊕ ВСЕ формы` (критерий «не запирает» §3.6 п.1, §5.1 п.5). (4) Дельта @dispatcher: `docs/pitfalls/design.md` — пятая ловушка «полное поле, а не одна форма» (§B.2.2).
