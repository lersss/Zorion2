# docs/specs — спеки блоков разработки

> **Роль:** каталог детальных спецификаций по блокам роадмапа. Это «рабочие
> документы»: сюда выносятся дизайн-брифы из `99_roadmap.md` §99.2, чтобы роадмап
> оставался сжатым планом, а детали были в одном месте.
> Правило: **один блок — один файл** + строка в манифесте. Новый блок — см.
> `99_roadmap.md` §99.6.

## Манифест

| Файл | Тема | Статус | Дата | Приоритет |
|---|---|---|---|---|
| `99.2.1-anomalies-as-content.md` | Аномалии как контент | 🔄 следующий блок | 2026-09-12 | высокий |
| `99.2.2-llm-descriptions.md` | LLM-генерация описаний | 💡 идея | 2026-09-12 | средний |
| `99.2.3-generation-tuning.md` | Тонкая настройка генерации (генераторы админки: подвкладки, веса, средние) | ✅ выполнено (частично, 29a) | 2026-09-15 | средний |
| `99.2.4-exotic-stars.md` | Экзотические типы звёзд и единая модель систем (29a) | ✅ Выполнено (закрыта 2026-09-15, 29a) | 2026-09-15 | средний |
| `99.2.5-resource-model.md` | Переход на новую ресурсную модель | 📅 разблокировано | 2026-09-12 | высокий |
| `99.2.6-settlements-labor.md` | Поселения и труд | 📅 планируется | 2026-09-12 | средний |
| `99.2.7-contracts-travel.md` | Контракты и перелёт | 📅 планируется | 2026-09-12 | средний |
| `99.2.8-auto-factories-presence.md` | Автозаводы и присутствие | 📅 планируется | 2026-09-12 | средний |
| `99.2.9-relay-network.md` | Сеть ретрансляторов | 📅 планируется · **до 1.0** | 2026-09-12 | высокий |
| `99.2.10-region-profiles.md` | Профили регионов | 📅 планируется | 2026-09-12 | средний |
| `99.2.11-planet-physics-archetype.md` | Генератор: физика → архетип | ⚠️ важно | 2026-09-12 | высокий |
| `99.2.12-temperature-mortality-calibration.md` | Калибровка температурной смертности | ✅ Сделано · перекалибр. 14b (жара 450 K + холод 100 K, ждёт одобрения) | 2026-09-14 | высокий |
| `99.2.13-other-factors-r-model.md` | Холод, гравитация, радиоактивность → R-модель (17a) | 🔄 дизайн (готова к проверке создателя) | 2026-09-14 | высокий |
| `99.2.14-role-model-admin-users.md` | Ролевая модель и раздел «Пользователи» в админке (18a) | 🔄 дизайн (готова к проверке создателя) | 2026-09-14 | высокий |
| `20a.1-npc-agents-v1.md` | NPC-агенты: первое приближение (20a) | 🔄 дизайн (доработка по баланс-проверке В3, ждёт одобрения) | 2026-09-14 | средний |
| `26a.1-bulk-npc-generation.md` | Массовая генерация NPC-агентов в админке (26a) | 🔄 дизайн (готова к проверке создателем) | 2026-09-15 | средний |
| `99.2.15-ship-visual-generator.md` | Генератор визуала кораблей (24a) | ~~🔄 дизайн~~ → **отменена 2026-09-16 (61b)**: заменена статичными спрайтами | 2026-09-16 | — |
| `99.2.15-honest-gas-giants.md` | Честные газовые гиганты и рамки полей близнецов (23a) | 🔄 дизайн (решена создателем 2026-09-15; обновлена по баланс-проверке В3 — готова к повторной проверке) | 2026-09-15 | высокий |
| `99.2.16-natality-life-expectancy.md` | Естественная рождаемость + крутилки СПЖ/коэффициента рождаемости (29a) | ✅ реализовано 2026-09-15 | 2026-09-15 | высокий |
| `99.2.17-balancer-ui.md` | UI-инструмент балансировки компонент изменения населения (16e) | ✅ Готово (2026-09-15) | 2026-09-15 | средний |
| `99.2.18-binary-systems.md` | Двойные системы — полная модель планет, орбит и модалки (35b) | ✅ реализовано 2026-09-15 | 2026-09-15 | средний |
| `41a-exotic-star-card.md` | Экзотика в карточке звезды — честные значения + возраст системы (41a) | 🔄 дизайн (готова к проверке создателя; 2 гейт-вопроса §8, ожидается прогон @balancetester) | 2026-09-15 | средний |
| `61b-static-ship-sprites.md` | Статичные спрайты кораблей вместо генератора (61b; отменяет 99.2.15) | ✅ реализовано 2026-09-16 | 2026-09-16 | высокий |
| `99.2.20-planet-physics-cascade.md` | Генератор планет — физический каскад, планета из объектов, флаг жидкой воды (45a) | ✅ реализовано 2026-09-16 | 2026-09-16 | высокий |
| `99.2.21-races-v1.md` | Расы: карточка, 50 рас, семейства, кластерная привязка (55a/56a) | 🔄 дизайн (принят создателем 2026-09-16; ждёт доделок 45a — интеграция) | 2026-09-16 | высокий |
| `99.2.22-race-generator-tuning.md` | Подкрутка генератора планет под расу-дома кластера (конфигуратор §7.2) | 🔄 дизайн (итерация 4, решение создателя «Вариант Б», реализована в рабочем дереве, готова к проверке создателем) | 2026-09-17 | высокий |
| `99.2.23-race-r-curves-twins.md` | Расовые R-компоненты + «близнецы» с выбором расы | 🔄 дизайн (итерация 4, «Вариант Б» — кривые материализуемые, не вычисляемые; готова к проверке создателем; реализована: формула §3.2, спец-случай humans) | 2026-09-17 | высокий |
| `67a.1-art-studio-go.md` | Арт-студия на Go: спека продукта + конфиги config/art/ (замена Python-прототипа, 67a) | 🔄 дизайн (готова к проверке создателем; конфиги forms/families/humans созданы, 46 рас F2–F9) | 2026-09-17 | высокий |
| `99.2.24-robots-ai.md` | Роботы и AI — новое семейство рас: 10 карточек 51–60, слой robotic, территория по условиям (73a) | 🔄 дизайн (готова к проверке создателем; числа — предположение дизайнера, прогон @balancetester следующий) | 2026-09-17 | высокий |
| `76a.1-art-studio-morphs-f10.md` | Арт-студия: морф-наборы F2–F9 + семейство F10 «Роботы и AI» (контент конфигов, 76a) | 🔄 дизайн (готова к проверке создателем; конфиги обновлены: 56 рас, морфы у всех F2–F10) | 2026-09-17 | высокий |
| `78a.1-art-studio-forms-pilot.md` | Арт-студия: словарь рас — аудит и расширение forms до 30–50 (все 56 рас: пилот 3 + чанк 1: 18 + чанк 2: 35, 78a) | 🔄 дизайн (готова к проверке формата создателем: генерация в студии F10/51, F2/5, F9/38; все 56 рас расширены до forms=40) | 2026-09-17 | высокий |
| `MVP-0.md` | Стенд проверки гипотез | 🔄 в работе | — | высокий |
| `80a-salt-races-rework.md` | Переработка соль-рас: Кшарры (23, F5) и Маргулы (49, F5) — смысловое разведение близнецов (80a) | 🔄 дизайн (итерация 6; готова к проверке создателем; числа — предположение дизайнера, прогон @balancetester следующий) | 2026-09-17 | высокий |
| `86a-player-dashboard-encyclopedia.md` | Дашборд игрока + Энциклопедия: разделы с заготовками (86a) | 🔄 дизайн (готова к проверке создателем; решения создателя 2026-09-17 внесены) | 2026-09-17 | высокий |
| `88a.1-art-studio-water-f1.md` | Арт-студия: семейство F1 «Водные» (расы 2–4, контент конфигов) + границы переноса людей на F0 (88a) | 🔄 дизайн (готова к проверке создателем; конфиги обновлены: 59 рас, морфы у всех F1–F10) | 2026-09-17 | высокий |
| `91a-ship-section-dashboard.md` | Раздел «Корабль» в дашборде игрока: слоты, характеристики, задел будущих модулей (91a) | 🔄 дизайн (готова к проверке создателем) | 2026-09-18 | средний |
| `94a-consumption-catalog.md` | Потребление рас + базовый слой ресурсов: таблица потребления, каталог 25, развод реестров окон, теневой товар T1 (94a, блок 99.2.5) | 🔄 дизайн (готова к проверке создателем) | 2026-09-18 | высокий |
| `98a-внешность-рас-арт-фильтр.md` | Внешность рас + фильтрация шума в генераторе картинок: раздел «Внешний вид» в races/*.md, поля appearance/blocked в families.json, фильтрация словарей в арт-студии (пилот 6 рас, 98a) | 🔄 дизайн (готова к проверке создателем) | 2026-09-18 | средний |
| `99a.1-goods-studio.md` | Студия товаров: категории, генерация товаров и рецептов, граф рецептов (99a) — отдельный локальный инструмент (как арт-студия), цепочки сверху вниз, тир = уровень графа = сложность рецепта, «заполнить комплектующие» через opencode API, статусы согласовано/исключено/бан, валидатор «каких ресурсов нет» (слой 20 + витрина 111), экспорт JSON для игры | ✅ реализовано 2026-09-19 | 2026-09-18 | высокий |
| `99a.2-goods-studio-import-top-catalog.md` | Импорт top-каталога в студию товаров + количество в слотах (99a.2): кнопка «Импорт», полная замена товаров (ресурсы не тронуты), 100 approved из `goods_data/top_catalog.json`, 8 внешних компонентов draft, `Slot.quantity` (дефолт 1, тир/валидаторы не зависят) | ✅ реализовано 2026-09-19 | 2026-09-19 | средний |
| `99a.3-goods-studio-focus-graph-spravochnik.md` | Студия товаров 99a.3: фокус-подграф на канвасе (выбранный + прямые родители/дети) + режим «всё дерево», тир-оверрайд (ручной тир, полная свобода, экспорт complexity), справочник (поиск, фильтры категория/используемые/неиспользуемые/тиры/забаненные, «+ товар», подгрузка «название \| категория», drag&drop), попап-деталь товара, попап категорий | ✅ реализовано 2026-09-19 (хвост: контекстное меню ПКМ в списке справочника) | 2026-09-19 | высокий |
| `99.2.26-race-card-player-facing.md` | Новая карточка расы — игровое восприятие (Лор·Вид·Атрибуты·Ниша·Размер·Дом) | ✅ реализована (2026-09-19; реконструкция по манифесту DELTAS_2026-09-19: данные 60/60 в race_lore.json, валидация §9, хендлер, JS, e2e PASS) | 2026-09-19 | высокий |
| `перенос-студии-товаров-iterA-каркас-БД.md` | Перенос студии товаров в игровой сервер — итерация A: каркас на БД (таблицы categories/goods/goods_slots, сидер 131 ресурс + 19 категорий, роуты /studio/*, API на SQL, ресурсы без привилегий) | ✅ реализовано 2026-09-19 | 2026-09-19 | высокий |
| `перенос-студии-товаров-iterB-ui.md` | Перенос студии товаров в игровой сервер — итерация B: UI на /studio (JWT-авторизация, /studio/api/*, «+ ресурс» без привилегий, второе подтверждение удаления, OR-фильтры) + витрина «Реальные вещества» (94a) на чтение из БД + e2e-смоук /studio | ✅ реализовано 2026-09-20 (e2e 12/12) | 2026-09-20 | высокий |
| `перенос-студии-товаров-iterC-доводка.md` | Перенос студии товаров в игровой сервер — итерация C: доводка (двухфазный fill «предложи → подтверди в попапе» — proposals/apply/cancel, перевод читателей Go-каталога на БД — слой 20/покрытие/gaps, общий модуль авторизации auth.js, удаление легаси cmd/goods-studio + goods_data) | ✅ реализовано 2026-09-20 (приёмка создателя «ок», e2e 14/14) | 2026-09-20 | высокий |
| `99.2.27-intrasystem-flight.md` | Внутрисистемный полёт: перемещение игрока между объектами системы в модалке (99.2.27) | 🔄 дизайн (числа сверены @balancetester Н1–Н3, UX @uidesigner внесён, @critic С-1/С-2/М-1…М-7 внесены; решения создателя 2026-09-20: позиция отражает полёт, компаньоны — цели v1 — внесены; готова к проверке создателем) | 2026-09-20 | высокий |
| `99.2.28-biomes-planet-surface.md` | Генерация биомов планеты: поверхность и недра объектами, биомы слоем после недр (доделка 45а п.2/99.2.20) | ✅ реализовано 2026-09-20 (коммит 07a5b12) | 2026-09-20 | высокий |
| `99.2.30-composite-route.md` | Композитный маршрут: полёт к планете/спутнику чужой системы из модалки (межзвёздный + внутрисистемный сегменты, серверное намерение `users.pending_destination`, автостарт по onArrival, разворот 61a, починка «Найти меня» при межзвёздном) | ✅ реализовано 2026-09-21 (коммит 910c067) | 2026-09-20 | высокий |
| `2026-09-20-ships-races-template.md` | Корабли рас: шаблон описания (внешний вид + кабина — место аватара) + промпт-задел под генератор + чек-лист артефактов расы + дельты impact map | 🔄 дизайн (готова к проверке создателем) | 2026-09-20 | средний |
| `2026-09-20-pacman-galaxy-wipe.md` | Пакман — вайп галактики: асинхронный джоб `pacman`, порционное поедание миров по полярной спирали (~100к за минуту), WS-события всем игрокам (рендер пакмана, исчезновение миров, счётчик), игроки остаются (current_world_id NULL + уведомления), конфликты с джобами и ClearUniverse | 🔄 дизайн (готова к проверке создателем) | 2026-09-20 | средний |
| `2026-09-20-ships-races-generator.md` | Генератор спрайтов кораблей рас в арт-студии: вкладка «Корабли рас», силуэты скриптом из `silhouette` (парсер ТЗ + библиотека форм), `texture` → промпт этапа 2, `blocked` → фильтрация словарей (аналог 98a), ships.json (проекция 60 рас) + ship_dict.json, панель промпта 98b, пилот 7 рас → пачки по 10 | 🔄 дизайн (готова к проверке создателем) | 2026-09-20 | средний |
| `2026-09-20-фабрики-сущность-производства.md` | Фабрика — сущность производства: строения (производители/другие), владельцы и население строений, предметы и предмет-рецепты, разблокировка через снабжение (тир не гейт), география через логистику, студия (ветки «Производители»/«Предметы», расовые типы), GDD-правки 11/13/10/05/04/GLOSSARY, термин «завод»→«фабрика» | 🔄 дизайн (готова к проверке создателем) | 2026-09-20 | высокий |
| `2026-09-20-визуализация-атмосферы-и-поверхности.md` | Визуализация атмосферы и поверхности: честная картинка планеты из биомов + атмосферы (стиль А), авторизованный /api/planet-image (planet_id, режим честная/заглушка на сервере), заслонение из видимых данных (И1), большая «вид с орбиты» при orbit {planet} (99.2.27), кэш (планета, режим, размер) + диск для большой, иконки биомов (пачка 1) | ✅ реализовано 2026-09-20 (коммит f579edf, приёмка создателя) | 2026-09-20 | высокий |
| `2026-09-20-реализм-распределения-гиганты-горячие-юпитеры-число-планет.md` | Реализм распределения: газовые гиганты по классам звёзд (G/K/F ~10%, M ~3%, O/B/A вниз), горячие юпитеры (миграция, ~0.5–1%), число планет у звезды (mean G/F 6.5, K/M 5.5, потолок 8) | ✅ реализовано 2026-09-21 (коммит 0fef206, приёмка создателя) | 2026-09-20 | высокий |
| `2026-09-21-планета-форма-поверхности-блик-и-реальная-атмосфера.md` | Доработка картинки планеты: форма «поле высот + регионы» (без кругов, доли ±30%), блик от L(seed) + освещённые кольца, реальная атмосфера отдельной осью режима `full` (присутствие/админ; состав→дымка, давление→лимб, τ_IR→свечение, cloudFraction→облака; K1 — отдельный кэш-ключ), версия генератора в кэше (S1), заглушка без блика (M4) | ✅ реализовано 2026-09-21 (приёмка создателя) | 2026-09-21 | высокий |
| `2026-09-21-студия-дерево-построек-канвас.md` | Студия: дерево построек на канвасе в ветке «Производители» (Строения → Классы → Типы → Подтипы), базовый тип `parent_id` у producer_types, расовость два уровня (семейство + раса), автоматика категорий с узлами-приглашениями «создать», переименование лабораторий, переключатель уровня расовости | 🔄 дизайн (готова к проверке создателем) | 2026-09-21 | высокий |
| `2026-09-21-люди-f0-отдельное-семейство.md` | Люди → F0 — отдельное семейство во всей системе: метка family (лор/корабли), валидация F0, список семейств студии (переключатель расовости), канон 22_races (10 биосемейств F0–F9), подписи, доки/тесты (идея 2026-09-21) | ✅ реализовано 2026-09-21 (приёмка создателя) | 2026-09-21 | высокий |
| `2026-09-21-рецепт-сущность-и-граф-фабрики.md` | Рецепт как сущность: `recipes`/`recipe_components`/`producer_recipes` (фабрика держит набор рецептов — подмножество категории), сложность на рецепт (снос `goods.tier_override`), миграция `goods_slots` → рецепты (000055), селектор фабрики и фильтр по семейству рас на ветке «Товары», перетаскивание карточки на канвас = привязка рецепта (новая механика), роут копирования набора рецептов из универсальных фабрик категории, дельты спеки фабрик §4.1/§4.3/§6.1 | ✅ реализовано 2026-09-21 (коммит a35e3bf) | 2026-09-21 | высокий |
| `2026-09-21-студия-товары-рецепты-ui.md` | Студия «Товары» под рецепты (UI-ТЗ): селекторы «Фабрика»/«Семейство» в шапке, граф замыкания выбранной фабрики (несвязный допустим) + «в наборе N», маркер «др. фабрика», попап «Состав рецепта»/«Сложность рецепта»/«Доступен фабрикам», drag&drop привязки рецепта на канвас (без фабрики — тост-подсказка), пункт контекст-меню «привязать к фабрике», кнопка копирования набора рецептов в «Производителях» | ✅ реализовано 2026-09-21 (коммит a35e3bf) | 2026-09-21 | высокий |
| `2026-09-21-студия-производители-лестница-предложения.md` | Студия «Производители» и каталог: снятие согласования как класса (`status` → обратимый флаг `hidden` **только у записей-производителей** `producer_types` + скрытость категории на слоте `producer_slots`, миграция `000054`), «убрать лишнее — удалением», инвариант «скрытое занимает имя» (по таблице); канон «самая конкретная побеждает» без статусного фильтра (скрытая запись скрывает категорию — вариант 1), единая применяемость уровня семейства; дефолт веса/объёма 1/1 (`NOT NULL DEFAULT 1`) вместо требования «участия в графе»; снятие `non_approved_ref`/`incomplete_chain`; переоткрытие решения спеки скрытых «одна ось» (К1, дельты §12.1) | ✅ реализовано 2026-09-21 (коммит `d5c932d`; редакция v4 — решения Р1–Р6 + вердикт @critic по v2, §8/§17) | 2026-09-21 | средний |
| `2026-09-21-ships-quality-rework.md` | Генератор спрайтов кораблей рас (арт-студия), редакция v2: победивший подход — чистый txt2img (Juggernaut XL v9, ракурс 3/4 сверху, нос вправо; расовость — материал/текстура, морские/авиационные слова в негатив), frame-check (край <8 px / вытянутость <1.3 → другой seed), вырез hyst(12/40)+fill_holes, нормализация 200×200 нос вправо, Hi-Res 4x-UltraSharp→1536→img2img 0.40, обязательная ручная приёмка в вкладке «Корабли рас»; отвергнутые подходы (силуэт+ControlNet, процедурный рендер, Blender) — кратко, с измерениями; реестр игры расширяется расовыми кораблями (21+3 «люди», `spriteForAgent` — следствие роста пула) | 🔄 дизайн (готова к проверке создателем) | 2026-09-21 | средний |
| `2026-09-21-угол-корабля-в-метаданных.md` | Поворот корабля углом в метаданных: пиксели не поворачиваются нигде (вырез с `--no-orient`, отчёт `profile_orientation` остаётся начальным значением пары), действия ориентации приёмки (`rotate`/`rot90`/`rot180`/`flipH`) пишут пару `(A, F)` в мету и не трогают файл, показ — CSS в студии (`scale(k) rotate(A) scaleX(F)`, `k` постоянный, без клиппинга в рамке 360) и canvas в игре (`rotate(H + V·A)`, `scale(F ? −1 : 1, V)`, `V = cos H < 0 ? −1 : 1`, единый клиентский хелпер, все точки рендера + дашборд), реестр `ShipSprite` несёт `Angle`/`Flip` (`/me.ship_options`, легаси-21 — нули без регрессий), импорт принятого — `tools/import_ship_sprites.ps1` (копия байт-в-байт + печать строк реестра), удаление `/ships/preview` и пиксельных поворотов | 🔄 дизайн (готова к проверке создателем) | 2026-09-21 | средний |
| `2026-09-21-высадка-прогулка-по-планете.md` | Высадка на планету — мини-игра «прогулка по поверхности»: жребий биома по доле `share`, позиция `{status:"surface", biome}` в `users.current_position` (без миграций), ручки `POST /api/surface/land`/`leave` (кнопка вызова = смерть, инвариант 9), пакет прогулки, клиентский процедурный мир одного биома (Canvas 2D, вид сбоку, бесконечный), жизнь/флора/фауна, скафандр и урон среды (числа — предположение дизайнера), таблица «эмоция → фишка», фолбэки «нет данных» | ✅ реализовано 2026-09-21 (коммит fcf0626; QA 14/14 PASS) | 2026-09-21 | высокий |
| `2026-09-21-масса-каменистых-и-ледяных-планет.md` | Масса каменистых/ледяных планет: аннулярная масса диска `M = clamp(B·M₀·(a_норм/a_⊕)^0.5·10^(0.5[Fe/H])·ζ, 0.02, 8)` — светимость из массы исключена (каскад самоподобен по √L), бюджет системы `B ~ logN(0, 0.5)` (один ролл на систему, образец `giantOrbit`), пол `0.02` как страховка реестра, потолок `8` **без сглаживания** (метка обязательной задачи перелива), подкрутка рас — единый источник без ζ и B; калибровка орбит 1–8 для G и M, TDD-тесты T1–T16 (включая МЕЖсистемный разброс); второй потребитель (`race_tuning.go`) и третий производитель (`exotic.go`) помечены; побочные сдвиги давления/температуры/биомов и обитаемости (§7.2); V2 и др. альтернативы — в отклонённых (§3.2) | ✅ реализовано 2026-09-21, коммит a8a8304 | 2026-09-21 | высокий |
| `2026-09-21-фабрики-релиз-2-столицы-фракций.md` | Фабрики, релиз 2 — итерация 1 (фундамент): таблица `buildings` (миграция `000056`, колонки первой итерации, отложенные колонки §3.2 спеки фабрик), столица фракции (одна на фракцию, на `factions.homeworld_id`, тип «другое», ничего не производит), идемпотентный проход `EnsureCapitals()` в джобе генерации фракций, владелец-фракция, показ во вкладке «Фракции» карточки планеты (список + строка столицы + клик-раскрытие), видимость «админ / знание планеты» (+ дельта глубины скана 77a §6.2), дельты спеки фабрик (3b.6.9, поселения без владельца) | ✅ реализовано 2026-09-21 (коммит dfba218) | 2026-09-21 | высокий |
| `2026-09-21-каталог-описание-товаров-и-ресурсов.md` | Каталог студии: поле «описание» у товаров и ресурсов (миграция `000057`, одно поле для игрока и студии, три ИИ-потока: описание новых составляющих в fill, одиночный прогон после создания/по кнопке, разовый пакетный прогон по записям без описания порциями по 10, попап «Описания ИИ» с принять/пропустить/отредактировать, третья колонка массовой подгрузки, предел 2000 символов, `NULL` = нет описания; показ игроку — будущая задача) | 🔄 дизайн (готова к проверке создателем) | 2026-09-21 | средний |
| `2026-09-21-разброс-массы-планет-обрезка-гигантом.md` | Разброс массы каменистых/ледяных: `σ_ζ` 0.4 → 0.6 + детерминированный множитель **обрезки гигантом** `f_обр = 0.08` при `abs(orbitIndex − giantOrbit) ≤ 2` (Гранд-Так/stranded embryo; без новых роллов, самоподобие по √L сохранено); ярдстик-проверка — Солнечная последовательность Венера/Земля/Марс воспроизводится медианами в пределах 6%; ревизия σ-«НЕ меняется» спеки `a8a8304`, пересчёт порогов T1/T2/T14/T15 + новые T17–T22, развилки F1/F2/F3 (зависимость масштаба от перелива) | 🔄 дизайн (готова к проверке создателем; 7 развилок §12); **впитана** в `2026-09-21-протопылевое-облако-архитектура-и-масса.md` (этап 1), отдельно не выпускается | 2026-09-21 | высокий |
| `2026-09-21-протопылевое-облако-архитектура-и-масса.md` | Протопылевое облако (диск) — **пред-слой системы** (одно на систему, до цикла орбит) + этап 1 «облако и масса»: `M_диск ~ logN(0, 0.5)` (заменяет `B`, тот же ролл — поток RNG не сдвигается), обрезка гигантом `f_обр = 0.08` при `abs(orbitIndex − giantOrbit) ≤ 2`, `σ_ζ` 0.4 → 0.6 (впитывает черновик F1), нормированная координата `x = r/√L` (самоподобие сохранено), ярдстик Венера/Земля/Марс ±6%; архитектурный фильтр облака и поясов (объект системы), этапы 2 (состав/история) и 3 (пояса/Плутон) — наброски; 11 развилок §7 (Р1 — принцип √L); тесты C1–C5 + T17–T22, дельты `99.2.20`/`99.2.22`/`03_planets.md`/`PITFALLS`/`impact_map` (`disk_cloud`, `belt`) | ✅ этап 1 реализован 2026-09-21 (коммит `dd37a8c`); этапы 2–3 — дизайн | 2026-09-21 | высокий |
| `2026-09-22-поселение-добыча-сырья-биома-ленивый-буфер.md` | Залежи на поверхности — итерация 1 эпика «Экономика поселения: залежи + производственная ветка»: генерация случайного числа залежей из трёх пилотных ресурсов каталога (Растения 358, Мясо 359, вода-ресурс 1; резолв по name_norm), объект deposits (планета + ресурс + слой surface + богатство + конечный запас, FK goods ON DELETE CASCADE, миграция 000058), видимость только при знании планеты, инструмент админа «добавить залежь»; биомы в генерации не участвуют (правило «площадь → жирность» снято), ветка/буферы/петля/добыча — целевая модель (итерации 2–4, не входят) | ✅ реализовано 2026-09-22 (коммит `3208cad`, миграция `000058`) | 2026-09-22 | высокий |
| `2026-09-22-погода-поверхности-вид-и-физика.md` | Погода прогулки: вид и связка с физикой планеты (шаг B идеи «поверхность — баги и вид»): модель выбора явления из осей планеты (жёсткие гейты по давлению/радиации + мягкие веса по температуре/токсичности/жидкости/биому; пороги — из `suit` пакета; цикловой `mulberry32`, анти-повтор), визуал 6 явлений (дымка глубины по слоям, частицы в мировых колонках с модульной обёрткой, 3 пояса параллакса 0.4/0.7/1.0, общий ветер с порывами, приземные эффекты по `terrainHeight`), три прохода отрисовки между слоями мира, параметры в `surface_config.js`; админский ручной выбор сохраняется, 2–4 мин авто-цикл; только клиент — без сервера/БД/миграций; зум/края/текстуры неба — не эта спека | 🔄 дизайн (готова к разработке; спеку принял создатель 2026-09-22, развилки закрыты §13, правки @critic внесены) | 2026-09-22 | высокий |
| `2026-09-21-пояса-малых-тел-объект-системы.md` | Пояса малых тел — объект системы (протопылевое облако, «пояса»; идёт раньше состава/истории по решению создателя №2; **итерация 2 — решения создателя №3**): **бюджет облака настоящий** (путь (б), §4.0): планеты берут нормированные доли профиля (`w_i = c_i/S₀`, Σ ≤ 1), приор `M_диск` пере-калиброван (`median ≈ S₀ ≈ 9`) — нормировка и пере-калибровка сокращаются, медианы (Венера/Земля/Марс 0.767/1.000/0.104) сохранены; инвариант `Σ планет + Σ поясов ≤ M_диск` **буквально** (нужен F1 — нормировка шума `ζ`); «остаток = пояса» нефизичен (≈ 7–8 M⊕) → удержание `κ_kind` + сток `M_сброс`; **орбиты поясов выведены** из резонансного окна гиганта 3:1–2:1 (`beltOrbit = g − 1`, `width = 0.1493·r_g`, `P(пояс) ≈ 4.5%` всех / ≈9.4% F/G/K); **пояс — агрегат** (Плутон-класс `bodies` снят); таблица `system_belts` (миграция `000059`, открытый `kind`, в `truncateTables`), `debris` — материализация `disk_state` (WD); этап 3 — мини-игра добычи (`ideas/2026-09-21_добыча-в-поясе-мини-игра.md`, вектор «Игрок»); тесты A1–A7/B1–B14, дельты `DB.md`/`03_planets.md`/`GLOSSARY`/`PITFALLS`/`impact_map` (`belt` — снять candidate) | 🔄 дизайн (итерация 2: бюджет настоящий + орбиты из модели + агрегат; готова к проверке создателем) | 2026-09-22 | высокий |
| `2026-09-22-контракт-модель-сущности.md` | Контракт как сущность — модель хранения (ядро): таблицы `contracts`/`contract_requirements`/`contract_log`, поля/типы, полиморфный автор (`author_type` шире `buildings.owner_type` — добавлен `building`), `type`+`payload` (открытый список), признак финансирования `funding` (подряд), `escrow_withdrawable` (выводимая доля залога), жизненный цикл `draft→open→taken→completed/cancelled/expired` (провал = истечение взятых), место — планета, индексы под оборот (частичные), ленивое истечение без тика, **инвариант возврата залога при любом удалении контрактов** (§6.5: атомарный статусный flip одним оператором, все пути в одной транзакции: очистка вселенной, генерация/гипотезы, генерация/перегенерация планет, удаление мира, пакман), окно свойств и состыковка с рецептами, миграция `000062`, impact map (идея 2026-09-22; правки порций 1–3) | 🔄 дизайн (итерация 1; правки @critic/@balancetester внесены, ждёт гейта создателя) | 2026-09-22 | высокий |
| `2026-09-22-деньги-и-эскроу.md` | Деньги и эскроу — счёт/казна/залог: `accounts` (игрок/фракция/агент, PK полиморфный) + `withdrawable` ⊆ `balance` (корзина «заработанное», две корзины подряда), `money_operations` (журнал), залог на контракте (lock при публикации / release при выполнении / return при провале) с **сохранением метки** (`escrow_withdrawable` — метку двигают только трата и оплата труда), резолв плательщика (автор→владелец), создание счёта при регистрации/первом входе и **у агента — при первом участии** в контракте, политика кошельков при очистке вселенной (кошелёк игрока жив, счета фракций удаляются), казна фракций — заглушка `10^15` (warning о конфликте с эмиссией), связь `npc_agents.owner_faction_id` (агент только исполнитель), миграция `000061` (идея 2026-09-22; правки порций 1–3) | 🔄 дизайн (итерация 1; правки @critic/@balancetester внесены, ждёт гейта создателя) | 2026-09-22 | высокий |
| `2026-09-22-контракт-перелёт-и-доска.md` | Контракт-перелёт (тип 1) + доска планеты + замена легаси: опора на `travel.Manager`/`ArrivalHandler`/`IntrasystemManager` (не дублировать полёты), **две точки закрытия по типу цели** (планета → внутрисистемный сегмент 99.2.30; только система → звезда), путь агента-исполнителя через `NPCManager.processArrivals` (**только цель-система** — у агентов нет внутрисистемного движения), `payload` A→B, требование к двигателю (`subject='speed_factor'`), срок перебазируется от взятия, доска на карточке планеты (публикация только с планеты, агрегация всех источников, видимость по знанию), публикация игроком в игровом UI + админ-инструмент для прочих авторов, полный список потребителей `assignments` → `contracts` (включая статик-роут `/contracts`, конструкторы `main.go`, `ARCHITECTURE`/`QA_CHECKLIST`/`VECTORS`/`AGENTS` §5, спеку пакмана), форма возврата залога по статусу (open→отмена, taken→провал), числа цены/срока — заглушки «калибровать позже» с исправлениями (`min_reward` убран, масштаб карты, независимость от `PlayerBalanceSeed`), расхождения канона, открытые вопросы (идея 2026-09-22; правки порций 1–3) | 🔄 дизайн (итерация 1; правки @critic/@balancetester внесены, ждёт гейта создателя) | 2026-09-22 | высокий |
| `2026-09-22-второй-пакет-погоды-поверхности.md` | Второй пакет погоды поверхности (три части): новые явления на существующих телах (5 новых приёмов — гроза/молния, ледяные иглы, ледяной град, пепельный дождь, свечение; смог/аммиак/радиоактивность/магнитная буря/сухая стеклянно-кристаллическая буря — вариантами; горячие — отложены решением), доработка (метель без «сосулек» → снежинки, «сосульки» = отдельное явление; туман без «блобов»; HUD-подписи «Дождь»/11 явлений), слой среды (детерминированный суточный цикл день/ночь на клиенте, без сервера; ночью сияние ярче, днём не видно; сезоны отложены); разбор факта: холодные миры до 20–60 K (`temperature_min 50` — это K, не °C), криогеника помещается на существующие биомы; спутники/газогиганты не носители; фазовые реперы (реальные тройные точки, sourced) рядом с `suit`-полосами; `life` влияет на выбор (свечение) — дельта к пакету 1; только клиент | 🔄 дизайн (готова к проверке создателем; решения создателя через менеджера 2026-09-22 + правки @critic С1–С16 (итерации 1–5) и @balancetester Б1–Б7 внесены) | 2026-09-22 | средний |
| `2026-09-22-второй-пакет-погоды-поверхности-appendix.md` | Приложение к спеке второго пакета погоды (справочное): §A.1 — аудит фактической модели холодных/ледяных тел (температуры, биомы, `temperature_min 50` = K, спутники/газогиганты не landable); §A.2 — полный список «отложено/отклонено» с носителями и причинами (горячие явления отложены решением №6, «метеорный дождь», терминатор, гейзеры, N₂-иней, соляная буря, фантастика) | 🔄 дизайн (приложение к спеке; готова к проверке создателем) | 2026-09-22 | низкий |
| `2026-09-22-поселение-ветка-буферы-переработка.md` | Ветка поселения — итерация 2 эпика «Экономика поселения»: таблицы `settlement_branches` (поселение ↔ рецепт + своя чек-точка `processed_at`) и `settlement_branch_buffers` (вход/выход, ресурс → количество, `double`, FK `goods` CASCADE), ленивая переработка вход → выход по Δt (скорость — **линейная, вариант A**: `k·population/complexity`, `k = 2.78·10⁻⁸`, разлёт `T` ≈10⁴× принят создателем осознанно; числа не калиброваны), админ-ручки «создать ветку» и «добавить ресурсы во вход», вход видит только админ, выход — вместе с деталями поселения, выход — «пол» (низкое качество), ветка работает всем населением без найма (самохозяйство песочницы); добыча/потребление/потолки/качество/склад — итерации 3–4 (не входят); миграция `000064` | ✅ реализовано 2026-09-22 (коммит `85f6539`) | 2026-09-22 | высокий |
| `2026-09-22-поселение-добыча-из-залежи-итерация-3.md` | Добыча ветки из залежи поверхности — итерация 3 эпика «Экономика поселения»: ветка черпает из залежей **своей планеты** (поселение → `planet_id`) по ресурсам состава рецепта, ровно под переработку (п.24 — та же `k·population/complexity`, `k = 2.78·10⁻⁸`, второй ручки-скорости нет), порядок расходования «вход первым → залежь добором», внутри ресурса залежь берётся от крупной к мелкой (п.25) с локом `deposits … ORDER BY id FOR UPDATE` (порядок ветка → залежи, без дедлоков), запас убывает с клампом ≥ 0 и без двойного учёта, выработанная залежь (`amount = 0`) скрыта от игрока и видна админу (п.26, фильтр в `stripPlanetDetails`); память-путь залежь не трогает; **миграции нет**; новые тесты T1–T16; числа не калиброваны (прогон @balancetester не обязателен) | ✅ реализовано 2026-09-22 (коммит `6a563fa`) | 2026-09-22 | высокий |
| `2026-09-22-поселение-потребление-населением-итерация-4.md` | Потребление населением выхода ветки + тип поселения — итерация 4 эпика «Экономика поселения»: тип поселения — подтип справочника строений «Поселение» (класс строения; **без привязки к категории товаров** — сужение С4 для типов без слотов), связь `settlements.settlement_type_id` (FK RESTRICT, миграция `000067`) + бэкфилл, нормы еды в `producer_types.params.eat` — **гибкая структура на товар-выход ветки** (п.45: нормы изолированы, не суммируются), потребление `eaten = min(eat_k(товар ветки)·население·Δt, output)` в хвосте того же ленивого прохода (после переработки), кламп ≥ 0, идемпотентность, путь «в памяти» без записи; карточка — вход (админ) / выход-«осадок» / сделано / съедено / скорость ед-в-сек; «остаток = осадок» (п.33, слово разведено с «осадком подножного корма» `13_tiers` §13.12 — механизм канона **не отменяется**), потолка буфера нет (п.36), голод — отдельная задача (п.35); пилот — **две ветки** «Вода» (рецепт 73 → товар 381) и «Пища» (рецепт 69 = Мясо → Пища); нормы `2.5·10⁻⁸` на товар — провизорно, не калибровано (@balancetester), коридор В4 сохранён | ✅ реализовано 2026-09-22 (коммит `f702359`, миграция `000067`) | 2026-09-22 | высокий |
| `2026-09-22-эффекты-снабжения-задержка-голод.md` | Эффекты от **дефицита потребности** (позиция корзины; решение создателя 2026-09-23 «производство и потребности — разные слои») — универсальный каркас + первый эффект «голод» (задача линии «Поселения» после чекпоинта эпика): **три слоя** — потребность / производство / каркас эффектов; **позиция корзины = существующая категория** `categories` (**любой `kind`**) (новой сущности нет), закрывается любым товаром категории, в т.ч. привезённым; покрытие = **сумма источников** (выход веток — реализуем сейчас; местное покрытие/запас/завоз/склад — точки подключения); спрос = `население × норма(позиция)` (`params.eat` по позиции), `w = 1 − покрытие/спрос` (та же формула); две сущности уровнем выше — справочник **`effect_types`** («что бывает»: `impact`, `params.curve` — ссылка на кривую силы; **`params.recovery` больше НЕ в модели** — `recovery` это скаляр глобальной компоненты `hunger` в «Балансировке» (заводское значение `0.25`), не свойство типа) и **`active_effects`** («что наложено сейчас»: полиморфный владелец, источник — **позиция** (`source_position`), **`load`/`load_at`** — нагрузка/базис; состояние = `f(load ≥ порог)`, производное), миграция **`000070`** (таблиц потребности/позиции **нет** — производны); **универсальная модель нагрузки** (решения 8–9): условие даёт силу **`w∈[0,1]`** (для голода — доля непокрытого спроса), `load += w·Δt/3600` (`load` — сило-часы, интервалы — секунды) / медленное **восстановление** `recovery`; сила — **кривая `R(load)`** (ось — нагрузка; при `w=1` = время; в «Балансировке» как 5-я **глобальная** компонента `hunger`, **не в студии**; расовый балансировщик её **не принимает**, `IsRaceComponent`); порог включения = нулевой префикс кривой (порог кривой `hunger` — 24 ч); **анти-абьюз из математики** (крошка → `w≈1` → нагрузка растёт; обнуление только долгим реальным покрытием; для голода нагрузка — **текущая нехватка, не вечная память**: реальное покрытие уводит её ровно в 0 — решение 10); **память без зависимости от частоты чтения** — базис `(load, load_at)` + аналитическая дельта, запись на персистентном пути, без третьей чек-точки; связь «**позиция → эффект**» — `producer_types.params.effects.<позиция>` (симметрично `params.eat`; **привязка по товару-выходу убрана**); просадка — **через единственную точку сборки** `ChangeComponents` (`PlanetInput.Effects` кусочной силой `EffectForcePoint`, `Recompute` = `EnvComponents` + сила, без двойного учёта) — `DeathTime`/`DeathCause`/`Projection` видят голод, в `18b`/`settlement_log` добавлен код `hunger`; **порядок «производство → потребность → население»** (хвост `eaten` вынесен из `ProcessBranch` в слой потребности); `active_effects` в `truncateTables`; стабильность не трогается; форма кривой/порог 24 ч — гипотезы (@balancetester, §8), `recovery` `0.25` подтверждён (@balancetester, В2), О5 (число/место `recovery`) закрыт решением создателя 2026-09-23; разобраны находки @critic (батчи 1–2) и уточнения создателя 5–9 (§10); **разворот ядра — батч 6 (§10.9, решение 2026-09-23)** | ✅ реализовано 2026-09-23 (коммит `6aaa406`) | 2026-09-22 | высокий |
| `2026-09-21-компаньоны-отдельные-миры.md` | Компаньоны кратных — **отдельные миры** (миры-компоненты системы; решение создателя 2026-09-21, №4, ревизует «компаньоны — цели, но не миры»): компонент кратной = запись `worlds` со своим классом/массой/диском/планетами/поясами; система = группа миров через `system_id` (self-FK, NULL = система-запись), `separation_au` + `component_index`, миграция `000060` (номер забронирован), бэкфилл запрещён (41a) — старые миры с компаньонами в `stellar_mods` живут легаси; параметры `companion*`/`extra_companions` уходят из `stellar_mods` новых миров (один факт — одно место); тесная пара — циркумбинарный диск и P-планета у системы-записи (компоненты без планет); счёт планет компонента по его классу (×0.9 wide/multiple — ревизия §5.2); диск `M_диск` — **на мир-компонент** (закрыт §12 п.8 спеки облака); синтетические id `companion:`/`extra:` — легаси (снятие — этап 3); этапы 1 (модель+генерация) / 2 (карта/полёты/знание/API) / 3 (снятие синтетики); тесты G1–G13, дельты `02_worlds.md`/`03_planets.md`/`DB.md`/`ARCHITECTURE.md`/`PITFALLS.md`/`GLOSSARY.md`/`99.2.27`/`99.2.30`/`77a`/облако/пояса/impact_map; 8 развилок + О-6 | 🔄 дизайн (этап 1 — модель данных + генерация; готова к проверке создателем; этапы 2–3 — наброски) | 2026-09-21 | высокий |
| `2026-09-22-облако-этап-2-состав-и-история-формирования.md` | Протопылевое облако, этап 2 — состав и история формирования: снятие жёсткой снеговой линии (`compositionByZone` ревизуется **в части планет**, для поясов сохраняется), резервуары + плавная конденсация `a_ice(x_form, x_ice(t_form))`, миграция как ролл положения формирования (`x_form` от `x_now`, гейт мира + per-планета), потеря ледяной мантии (только состав, масса заморожена этапом 1), эпоха формирования (ролл `t_form`); **снеговая линия — по физике, внутрь** (`5–6` рано → `2.7` к 3 млн лет; `x_ice(t)` **не** функция `sp.AgeGyr` — диск рассеян), нормализованный `x = r/√L`, профиль `p` — входной параметр; маркер `formation_history` в `planet.data` (типизированный список `formed_early`/`formed_late`/`migrated`/`ice_lost`/`stripped_embryo`, без миграции) + потребители (описания-теги, аудит, API-whitelist + **значок в карточке**, отключаемый одним флагом); названный сдвиг потока RNG `+2` ролла/планету и `+1`/мир; тесты H1–H20, сверка с научной шкалой `@scientist` (§8), дельты `03_planets.md`/`PITFALLS`/`GLOSSARY`/impact map/спека-носитель (§7‑Р4 закрыта), **рамки реестра `size 0.23`/`gravity 0.12`**; решения гейта 2026-09-22 (Ф3 рамки — расширить, Ф4 показ значок + текст, Ф6 `stripped_embryo` — сейчас; Ф1 решена ранее, Ф2/Ф5 — допущения) | 🔄 дизайн (готова к разработке; решения гейта внесены) | 2026-09-22 | высокий |
| `2026-09-22-пояса-малых-тел-этап-2-показ-знание-полёт.md` | Пояса малых тел — этап 2 (показ/знание/полёт; продолжает `2026-09-21-пояса-малых-тел-объект-системы`): секция «Пояса» в модалке системы (`belts` аддитивно в `/api/worlds/{id}/planets`, DTO `BeltView` + `stripBeltDetails` — фильтрация полей, новая читающая ручка `GetBeltsByWorldID`); знание — базовые поля (тип/имя/геометрия/типичное тело/масса) открыты как объект системы, **состав** — за сканом системы в радиусе или присутствием, без новой таблицы (рекомендация; альтернатива — персистентная `player_belt_knowledge`); `disk_state='debris'` вытесняется показом пояса **для видимых игроку поясов** (РБ3 исполнена); полёт — `object_type='belt'` (`objectRadiusAU`/`targetInSystem`/`normalizeMyPosition`/`arrivalTargetValid`/`StartIntraFlight`, статус «в поясе X», композитный `destination`), миграция `000065` **только** на CHECK `player_intrasystem_flights` (`from_type`/`to_type` += `belt`; `current_position`/`pending_destination` — JSONB без CHECK); кольцо на канвасе не рисуется (резерв спеки размеров §4.3); ТЗ `@uidesigner` + e2e-смоук; тесты V1–V7/F1–F17; дельты `77a`/`99.2.27`/`99.2.30`/`DB.md`/`ARCHITECTURE.md`/`03_planets.md`/`PITFALLS`/`impact_map` | ✅ реализовано 2026-09-22 (коммит `d36f5ce`; довесок Restore — после контрактов) | 2026-09-22 | высокий |
| `2026-09-22-трюм-грузоподъёмность-корабля.md` | Трюм — грузоподъёмность корабля: фундамент петли «полёт → добыча → сдача → мир изменился». **⚠️ дельта 2026-09-24 (§20):** модуль `cargo_1` 80 → **30 т** (старт **50 т**), универсальных слотов **1 → 3** (`universal`/`universal2`/`universal3`, два пустых), слоты **видимы** в разделе «Корабль» (заглушки `.ship-slot-future` удаляются), миграция **`000080`**; бэкфилл `users.equipment` не нужен (пустой слот = отсутствие ключа). Ёмкость — производная (врождённая `ship_models.base_capacity` 20 т + грузовые модули `equipment.type='cargo'` `params.capacity` 80 т в **универсальном** слоте, 1 шт), стартовая суммарная **100 т** (числа закрыты гейтом); содержимое — отдельная таблица `player_cargo` `(user_id, good_id, quantity double)` (решение создателя №4); лимит — только **масса** (решение №2), задел под объём вторым лимитом (оси в API/коде, `goods.volume` не читается); масштаб `goods.weight` = тонны на единицу (сейчас все = 1 → 1 ед. = 1 т; при дефолтах плотность 1 т/м³ у всех — отдельная калибровка); перегруз — жёсткий предел с частичной погрузкой (рекомендация); запись — внутренний сервис, **единственная player-facing** — «груз за борт» `POST /api/cargo/jettison` (сброшенное исчезает, контейнер — кандидат на будущее); чтение — `GET /api/cargo` + блок «Трюм» в разделе «Корабль» (91a), `@uidesigner` для v1 не нужен; миграция **`000068`** (перебронирована с `000066`/`000067` — оба заняты: CHECK типа `cargo`, `base_capacity`, `player_cargo`, сид `cargo_1` + бэкфилл слота), `player_cargo` **не** в `truncateTables` (состояние игрока, переживает очистку вселенной); грузовой модуль выдаётся стартовым набором, производство — позже в лабораториях кораб. оборудования; дельта `21_ships.md` (тех-характеристики в скоупе, решение №5); числа — предположение дизайнера (`@balancetester` следующий); 6 открытых вопросов остаются | ✅ реализовано 2026-09-22 (коммит `bdc76c7`) | 2026-09-22 | высокий |
| `2026-09-22-пояса-малых-тел-этап-3-добыча-ui.md` | UI добычи в поясе (этап 3, подэтап 3b): точка входа «⛏ Добывать» в строке пояса модалки (рядом с «🚀 Лететь», матрица состояний: не в поясе/полёт/выработан/нет данных/полный трюм/сессия идёт), задел ПКМ по кольцу (активируется вместе с кольцом), 3 направления HUD страницы `web/belt.html` (рекомендовано A — верхняя приборная панель), полная компоновка HUD (буфер захода, трюм `used/total`, чип уровня запаса, подсказка у цели, «Вернуться на корабль», легенда управления), экраны/состояния (загрузка, ошибки входа, полный трюм, выработан, обрыв/рефреш, потеря связи), реактивность (локальный тост низ-центр, звук `ui_select/ui_success/ui_error` через `activateSound()`, анимации), формулировки, пороги уровней запаса (предложение), ТЗ разработчику, инварианты, зависимость от развилок §10, impact map, открытые вопросы; **ревизия 4 (2026-09-23, подэтап 3e):** легенда «двух стиков» (`W/S`/`A/D`/мышь-нос/`ЛКМ`/`Shift`/`Esc`), подсказки по наведению носа, прицел/подсветка цели | 🔄 дизайн (ревизия 4 — готова к проверке создателем) | 2026-09-23 | высокий |
| `2026-09-22-пояса-малых-тел-этап-3-добыча.md` | Пояса малых тел — этап 3: **добыча в поясе (мини-игра)**. Механика — «полёт и сбор в невесомости» (решение создателя №1, вариант Б): Canvas 2D сверху, инерционный полёт, удержание у целевого астероида = бурение (класс B; контактный сбор A / трос C / разведка D / survival E — отклонены); добывается **только железо** (заглушка, резолв `good_id` по `name_norm`), выход ∝ **уже существующему** `composition.iron` («место решает что», ГДД §10.2.3); авторитет — **класс II** (сервер-клампы `rate_cap·Δt`/запас/трюм, клиент репортит события сбора; чистое время — отклонено, убивает навык); состояние захода — **позиция-уровень** `{status:"mining", …}` в `users.current_position` (без новой таблицы, как `surface`); запас пояса — **конечный, на пояс** — одна колонка `system_belts.iron_remaining` (миграция **`000069`**, номер подтверждён; **NULL = «нет данных», 0 = «выработан»**), абстракция-метроном (ГДД §10.1), а не масса пояса; вход — кнопка «⛏ Добывать» в строке пояса (ПКМ по кольцу — **после** визуального шага `@gdesigner`, кольца на канвасе ещё нет — конфликт назван); выход — `POST /api/belt/mine/leave`, буфер переливается в трюм через **внутренний сервис** `TryAddCargo` (жёсткий предел 100 т, полный трюм блокирует сбор); обрыв/разлогин — сессия не теряется (офлайн-добычи нет, `Δt_max`); заход скрыт от других (как поверхность); ТЗ на графику астероидов (§9, ассеты `@artist`/`@gdesigner`); числа §8 — предположение (богатый пояс: 100 т за 4–8 мин, ~5–8 трюмов; `R_base 0.25` т/с, `R_cap = 2·R_base·k_iron`) на повторный прогон `@balancetester`; декомпозиция 3a (сервер) → 3b (клиент) → 3c (графика+e2e) → 3d (ПКМ по кольцу); развилки §10 закрыты решениями создателя 2026-09-22 (истощение A, риск A, находка B, форма A, вход — кнопка сейчас/ПКМ потом); кандидат ГД-сущности «плотные сгустки» §16; **ревизия 4 (2026-09-23, подэтап 3e):** управление «два стика» (§5.1), выбор цели бурения по наведению носа (§5.2), «пятно» → бесконечный вдоль кольца пояс со стримингом ячеек (§8.2), критерий глазами (§12.4), сервер не меняется; второй кандидат ГД-сущности «вешка пояса» | ✅ принята создателем, готова к разработке; пакеты правок 1–3 внесены (решения §10; числа §8 — вердикт В2 @balancetester без блокирующих; полировка Н1–Н4: единые диапазоны + разведены класс пояса `belt_class` и уровень запаса `remaining_level`) | 2026-09-22 | высокий |
| `2026-09-23-контракт-ленивая-доска-пакет-и-снабжение.md` | Контракт как состояние: **ленивая материализация доски планеты** (своя чек-точка `contract_board_state`, общий кадэнс `MinPersistInterval` 30 мин, идемпотентность/детерминизм, `INSERT ... ON CONFLICT DO NOTHING` + `SELECT ... FOR UPDATE` (точка сериализации) + единый порядок захвата `contract_board_state → contracts → accounts`, «взятое твёрдо / открытое — производная дефицита», no-op без нужд), **«пакет контрактов»** (поле-группа `contracts.package_key`/`share_index`, а не сущность; правило «один игрок — одна **взятая** доля пакета» — частичным уникальным индексом; идемпотентность — вторым; объём доли в `contract_requirements`), **снабжение постройки** (дефицит по буферам постройки, нарезка долей по максимальной вместимости трюма — каркас, точная формула отложена создателем; нелинейная награда — каркас; срок — выводимая функция от пресета владельца (без перебазирования; §4.3 предшественника «перебазирование — специфика перелёта» сохраняется буквально, «дорога» поставки в v1 не выводима — F4), окно открытой доли — выводимое (не плоские 24 ч), кадэнс окно не продлевает; пол — биржа/NPC, семантика есть, реализации нет; точка сдачи `POST /api/contracts/deliver`; автор-постройка и планета постройки закрывают С5; зависимость от релиза 2 фабрик и порядок 3 поставок; единый порядок захвата блокировок `contract_board_state → contracts → accounts`), миграция-предложение `000071` (`000070` зарезервирован), вилки F1–F8, T1–T16, дельты impact map (+долг С8), числа для `@balancetester` (13 пунктов), кандидат ГД-сущности «наряд снабжения» | 🔄 дизайн (итерация 2: правки @critic/@balancetester внесены; готова к проверке создателем; на гейт — F1 носитель чек-точки, F3 политика витрины, F4 срок/«дорога» поставки, F6 нарезка: приоритет критериев, F8 порядок работ) | 2026-09-23 | высокий |
| `2026-09-23-корабли-рас-раса-агентов-и-игрока.md` | Корабли рас — финал связки: раса у агента (`npc_agents.race_id`, при генерации рандомно) и у игрока (`users.race_id`, дефолт `humans`), старт агента в мире расы (`factions.homeworld_id` через `race_id`, миграция `000066`), расовый реестр кораблей (`RaceShipSprites`, 118 + нейтральный — решение создателя, раунд 3) вместо 21 легаси, корабль агента детерминированно от `(race_id, id)` по суффиксу (`SHIP_VARIANT_SLOTS = 9` — гипотеза), корабль игрока в `users.ship_icon` (при создании — случайный файл пула расы), нейтральный корабль-фолбэк для 3 рас без корабля, доставка `race_id` до `/api/npc/positions`, `ship_options` — транспорт реестра (не выпил, S5), ленивый прелоад (решение создателя, раунд 3), выпил легаси последним подэтапом; миграция `000074` (бронь @manager), дельта эталонов `21_ships.md` §21.7 В4/В5, решения гейта Q1–Q9 приняты | 🔄 дизайн (готова к проверке создателем; правки @critic по спеке внесены полностью — порции 1–2, N1–N12; итерация 3) | 2026-09-23 | высокий |
| `2026-09-23-корабли-рас-раса-агентов-и-игрока-appendix.md` | Приложение к спеке кораблей рас (справочное): §A.1 — чек-лист потребителей легаси-блока (сервер, БД игроков, тесты — ≈45 мест в 12 файлах, e2e/инструменты, админ-UI); §A.2 — рёбра impact map (новые узлы — заводить: `npc_agents`/`npc_agents.race_id`/`users.race_id`/`users.ship_icon`/`ship_options`/`race_ship_registry`/`agent_origin`; клиентский индекс «раса → файлы» — `setShipOptions`; обратная зависимость генерации агентов от фракций) | 🔄 дизайн (приложение к спеке) | 2026-09-23 | низкий |
| `2026-09-23-мир-прогулки-вид-направление.md` | Мир прогулки — направление вида (@gdesigner, ЧК0): аудит текущего визуала (форма — одна fbm-функция на 57 биомов, цвет трёх образцов в пределах 3 %, декор — 4 фигуры, плотность на пиксельную колонку, горизонт один, поле `color` каталога пусто), 4 разных подхода (профиль-грамматика / 2D-скульптура / ярусный горизонт / цвет и жизнь) с ценой и риском, рецепты трёх образцов (пески, джунгли, горы), библиотека ~26 примитивов + 8–9 семейств вида, «что не трогать» | визуале прогулки, рельефе/декоре/палитре | 🔄 направление (код не писан) | 2026-09-23 | средний |
| `2026-09-23-мир-прогулки-рецепты-биомов.md` | Мир прогулки — рецепты биомов (единая грамматика вида, ЧК0): рецепт вида как **данные справочника биомов** (`BiomeDef.view` + секции `view_families`/`view_primitives`), резолв на сервере, аддитивные поля пакета `biome_view`/`view_source`/`view_version`, правило слияния «скаляр/карта/список», библиотека 8 примитивов рельефа + 15 декора + 3 правила размещения + 4 поздних 2D, 9 семейств вида на 57 биомов, палитра и ярусный горизонт, рецепты трёх образцов **сверенные с наукой §11** (правки: `skew` дюн 0.68→0.85 + `leeMaxDeg 34`; амплитуда холмов джунглей ×2.5; `relief.scale` гор; `dune`/`ripple`/`wave` → один `wave`), план этапов Э0–Э5 (цвет/жизнь → горизонт → профили → раскатка ЧК1–ЧК5 → скульптурный объём), новый инвариант «бюджет вертикали чанка», политика «рецепт читается в момент `land` → без бэкфилла», каскад «заполнение `color` меняет картинку планеты с орбиты, кэш `v4`→`v5`»; кандидат ГД-сущности «примета биома»; **§4.5 — рецепты ЧК1 (литосфера, 12 биомов): 10 дельт + 2 дополнения пресетов горизонтом, готовые JSON, бюджет проверен** | рельефе/декоре/палитре прогулки, справочнике биомов, пакете высадки | 🔄 ЧК2 реализован (ЧК1 — литосфера, ЧК2 — биосфера; далее ЧК3 вулканизм) | 2026-09-25 | высокий |
| `2026-09-25-мир-прогулки-скульптурный-объём.md` | Мир прогулки — скульптурный объём (Э5, 2D-поле твёрдости): переход от `y=f(x)` к единому `solidAt(x,y)`, которое **одновременно** читают физика и растеризатор (гарантия «нет невидимых стен»); разворот 5 классов реализации (столбцы-интервалы / плотное поле чанка / 1D-база + локальные 2D-наросты / SDF-скульптура / поле плотности), рекомендация C+A; данные `relief.forms[]` (примитивы `relief2d`: `overhang`, `void`, `crack2d`, `crater` — параметры, нефатальная валидация, расширение `declaredReliefTop`); три чекпоинта **Э5.1 фундамент → Э5.2 формы → Э5.3 гроты и своды** (свои гейты); инварианты (двустороннее «что твёрдо — то видно»; пещерная маска — часть твёрдого тела, растр консервативен, граница пещеры — шаг 1 px; угловая коллизия без смены чисел; бюджет вертикали с формами `overhang opening + h`/`crater.rim`; вытеснение кэша чанков; `planet_image` не затрагивается); якоря разведены — **«поверхность неба» `skyTop`** (погода) против **«пол/опора» `floorY`** (корабль/фауна/спавн); регистрация `crater` требует правки теста реестра (32→33); сверка с наукой §32.1 и ощущением §32.2 (warning по абсолютному масштабу); кандидат ГД-сущности «Укрытие»; план по чекпоинтам §B.1, impact map + дельты §B.2, журнал §B.3 — вынесены в приложение | рельефе прогулки, физике/растре, справочнике биомов | 🔄 дизайн (готова к проверке создателем; гейт создателя «ок» 2026-09-25 — масштаб стилизованный, «Укрытие» принято кандидатом; 2-й раунд @critic закрыт — средние/мелкие внесены; спека сжата до ≤40 КБ, план §4/дельты §7/журнал §10 — в приложении §B) | 2026-09-25 | высокий |
| `2026-09-25-мир-прогулки-скульптурный-объём-приложение.md` | Приложение к спеке Э5 (справочное): §A.1 — разворот 5 классов реализации 2D-поля (столбцы-интервалы / плотное поле чанка / 1D-база + локальные наросты / SDF-скульптура / поле плотности) с ценой, риском и таблицей сравнения; §A.2 — полная таблица сверки с наукой §32.1 (арка/козырёк/худу/грот/тоннель/свод/кратер/трещина) + расчёт стилизованного масштаба и warning; §A.3 — ощущение §32.2; **§B.1 — план по чекпоинтам Э5.1/Э5.2/Э5.3 (бывш. §4 основной спеки); §B.2 — пометки на смежное (impact map §B.2.1 + дельты §B.2.2, бывш. §7); §B.3 — журнал (бывш. §10)** | рельефе прогулки, физике/растре | 🔄 дизайн (приложение к спеке; готова к проверке создателем — v3) | 2026-09-25 | низкий |
| `2026-09-25-мир-прогулки-вода.md` | Мир прогулки — вода (ЧК6, последний чекпоинт): жидкость как новый слой мира (`liquidAt`/`liquidLevel`/`liquidDepth`), **отдельный от поля твёрдости** Э5 (не твёрдость — «ходьба по воде» невозможна), три режима уровня (`global` плоское зеркало / `basin` заполнение впадин / `underIce` корка + вода); признак **по категории биома** + **явная пометка** `view.liquid` (`магмовый_океан` — лава), `liquid_medium` сам не включает; разворот 5 классов (плоское зеркало / заполнение впадин / поле-атрибут / 2D-симуляция / визуальная плёнка) — принято A+B+C; данные `view.liquid` (medium/color/level/surface/fog/glow, семейство ⊕ дельта), 10 дельт водных биомов ( `liquid` — только в дельтах биомов, K1; пресет `водные/берег` несёт только горизонт), `color = palette.base`, **бамп `ImageGenVersion` `v10`→`v11`**; корка `underIce` — твёрдая несущая плита в `solidAt`, `skyTop` = верх корки; отрисовка (`drawLiquidBack/Front`, зеркало/волны/кромка/блик/день-ночь, подкраска под водой, свечение лавы, лёд); плавание/ныряние (`down`, обзор, опора на `floorY`, без урона — 2026-09-21); лодка (источник — развилка); **дробление ЧК6.1 отрисовка → ЧК6.2 плавание → ЧК6.3 лодка**; кандидат ГД-сущности «Прилив/отлив»; готовые JSON — в приложении | воде прогулки, физике/растре, справочнике биомов, пакете высадки | 🔄 дизайн (готова к проверке создателем; решения создателя 2026-09-25 внесены; ждёт @critic) | 2026-09-25 | высокий |
| `2026-09-25-мир-прогулки-вода-приложение.md` | Приложение к спеке воды (ЧК6, справочное): §A.1 — разворот 5 классов реализации жидкости (плоское зеркало / заполнение впадин / поле-атрибут колонки / 2D-симуляция / визуальная плёнка) с ценой/риском/вердиктом; §A.2 — готовые JSON-дельты (10 водных биомов + `магмовый_океан` только `liquid` + горизонт/`liquid`-дефолт пресета `водные/берег` + сводка `color`/бамп `v11`); §A.3 — научные аналоги (волнение/прибой, метан/аммиак/CO₂, подлёдный океан, лава, террасы) и ощущение; §A.4 — критерии приёмки ЧК6.1/6.2/6.3 | воде прогулки, физике/растре | 🔄 дизайн (приложение к спеке; готова к проверке создателем) | 2026-09-25 | низкий |
| `2026-09-23-орбита-планеты-присутствие-и-снимок.md` | Присутствие на орбите планеты: полные данные (поверхность, поселения — раса/население/стабильность/ветки, эффекты, лог вымирания, фракции, строения) + **снимок знания** на момент отлёта (поле `data.snapshot` в `player_planet_knowledge`, merge-запись `\|\|`, без миграции) + общеигровой календарь **+1000** во всём интерфейсе; режимы видимости `presence`/`snapshot`/`scan`/`none`, player-safe DTO эффектов без `load`/`load_at`, снимок хранит результат (округлённое население, без чек-точек), F5/рестарт — не отлёт; вердикт @critic K1 + C1–C7 + M1–M5 разобран; этапы Э1–Э5, тесты T1–T15; дельты 77a §6.2/§8.1/§11.2, 10.3.4, 22_races §127/§416, 18a «Время», 18b, 18_needs §18.2.1, GLOSSARY, impact_map (узел `planet_knowledge_snapshot`, `game_calendar`) канал покупки отчёта: флаг `can_buy_report`, управление только во вкладке «Поселения», покупка — заглушка; даты в UTC; плашки знания | 🔄 дизайн (утверждена создателем 2026-09-23; готова к разработке) | 2026-09-23 | высокий |
| `2026-09-23-стадии-поселения-и-скорость-производства.md` | Стадии поселения и скорость производства: число на паре «постройка-подтип × рецепт» (`producer_recipes.rate`, единица «ед/сутки/млрд», NULL = не производит — **заменяет** `k·population/complexity`), расход входа из состава рецепта, единая точка конверсии + три места нормы (600), тип поселения как **стадия роста** (ладдера из `params.stage` по данным — не по имени/дереву; гистерезис «вход/выход» с зазором, низшая — пол; переход только на персистентном пути, оценка ДО пересчёта населения), при переходе — пересборка веток из набора новой стадии + очистка складов + сброс `load` и очистка сирот `active_effects`, advisory-лок в `AddBranch`, массовой волны нет (ленивость прохода), видимая арифметика «забираем / производим / сверх потребления» (по позиции) в студии и админ-карточке поселения, настройка полями вместо JSON, миграция `000076`; 14 находок @critic второго прогона адресованы; итерации И1 (число+единица) → И2 (стадии) | 🔄 дизайн (пакет 1 правок внесён: решения гейта Г1–Г7 + находки @critic S1–S6/M1; готова к проверке создателем; пакет 2 ожидается) | 2026-09-23 | высокий |
| `2026-09-23-перелив-массы-в-гигантов-и-мини-нептуны.md` | Перелив массы в гигантов + зона мини-нептунов (физичный путь, решения создателя 1–5; **батчи 1–7 правок `@critic`/`@balancetester` внесены** (батч 4 — 12 находок финальной критики; батч 6 — рычаг региона на `f_rich_eff`; батч 7 — синхронизация по прогону `@tester`): полка на `M_crit` убрана — тела выше порога уходят в ветку оболочки; **двухпроходный пред-слой мира** разрывает цикл «масса ↔ позиция гиганта» (K1): проход 1 — сырые ядра `M_i⁰ = M_диск·w_i·ζ_i` без гиганта/`f_обр`/клампа, проход 2 — `g₀ = min{i : M_i⁰ > M_crit}` (порог убегающей аккреции; `M_crit` — производная константа, коридор `[5,10]`, канон ≈8), проход 3 — класс первичного тела по `M_body` и миграция `P_mig ≈ 0.15` (калибровка под §13, замер §17) → `g ∈ {1,2}` (горячие юпитеры, K2; первичный мини-нептун → `g = 0`, пояса нет), проход 5 — пересчёт масс с `f_обр` после фиксации `g` (исключены `g`, `g−1`, **покинутая `g₀` — пуста**); **масса гиганта — честная аккреция** (K3): `M_body = M_core + κ_rich·M_gas·w_{g₀}` (газ — по орбите **рождения**), где **`M_gas` — отдельный газовый резервуар** (газ ≠ твёрдый бюджет `M_диск`; инвариант `Σ твёрдых ≤ M_диск` цел), класс — **единственный критерий `M_body`** (режим `u_gas < f_rich` задаёт только формулу оболочки, §7.1): `(M_crit,16]` мини-нептун (`M_env = f_env·(16−M_core)`, `f_env ∈ [0.01,0.10]`) / `>16` гигант (верх — пересэмплинг, не кламп); **класс мини-нептун** — своя кривая `R = 2.61·(M/8.6)^0.577` (анкер K2-18 b; Нептун — проверка стыка, не опора класса; `α ≈ 0.577`), H/He-оболочка, `TypeMiniNeptune`, флаг `is_mini_neptune`, реестр `fields.go`, аудит (`checkEmptyBiomes` — исключение мини-нептуна), отдельный путь `runCascadeMiniNeptune`; **пояса и регионы** (K4): `beltOrbit = g−1` сохранён, `gas_giant_shift` → **доля газового режима** `f_rich_eff = clamp(f_rich·k_region, 0, 1)` (`k_region(s) = clamp(1 + s/0.3, 1/3, 5/3)`); `M_crit` и граница `16` — глобальны (`M_gas_eff`/`M_crit_eff`/`M_crit_min` — история); снят `rollGiantOrbit` (+`hotGiantChance`), **P-ветка двойных сохранена** (решение создателя п. 6: `gasGiantChanceShifted` + `gasGiantMass()`, `TestCircumbinaryGiantAllowed`, QA A10); сняты `GasGiantMassMin=15.9`/`GasGiantRadiusMin=5.9`; **кривая `M→R` гигантов переанкерена** (`16→3.7`, `95→9.4`, `317.8→11.2`, §7.5), вторичные гиганты разрешены («один мигрирующий»), ядро гиганта тратит твёрдый бюджет `w_{g₀}` (оболочка — `M_gas`), `σ_gas≈0.9` — параметр (эмпирическая σ≈0.62), пара `(M_crit, f_rich)` — единая связка (`P_giant = P_ov·f_eff`, `f_eff ≈ 0.5`); `317.8` разведён (анкер кривой `GasGiantMassRef` / целевая медиана `GasGiantTargetMedian`); **T16 переписан** (ядро зависит от `M_диск`, оболочка — нет); счёт роллов пред-слоя `n + 5` (учтён `migrationMode`); металличность — один множитель (`coreMass`); **частоты (согласованный расчёт, веса `99.2.4` §4.1):** `P_ov ≈ 9.2%` (G/F ≈20%, K/M ≈11%), `P_giant = P_ov·f_eff ≈ 4.6%`, горячие ≈0.7%; полка планет `3.31%` (`σ_ζ 0.6`); классовая таблица частот (10/3/2/0.5%) снята — ориентир **переанкерен замером** (G/F ≈10%, K/M ≈5.7%, окно `[8,12]%` только для G/F; §16.4 закрыт); тесты O1–O23 (переписаны `TestBeltShareDistribution`, `TestGiantRateByClass`, `TestHotJupiterRateAndOrbit`, `TestGiantOnePerSystem`), замер батча 3 (`P_ov` 9.45%, G/F 20.3%, `P_giant` G/F 10.1%, K/M 5.7%, горячие 0.94%), кандидат ГД-сущности «подлёдный океан мини-нептуна» | 🔄 дизайн (батчи 1–7 правок внесены; готова к проверке создателем) | 2026-09-23 | высокий |
| `2026-09-24-студия-управление-локальным-ии.md` | Студия: управление локальным ИИ-помощником (`opencode serve`) — индикатор (не запущен/запускается/работает/занят чужим процессом) по ФАКТУ пробы `OPENCODE_URL` (TCP + `GET /global/health`, фолбэк `/config`), кнопки «запустить»/«остановить», ручки `GET /studio/api/ai/status` и `POST /studio/api/ai/start|stop` (admin/skycomposer), запуск detached `cmd /c npx -y opencode-ai serve --port <порт из URL>` (single-flight, таймаут готовности `OPENCODE_START_TIMEOUT_S`=300), остановка по владельцу порта (`netstat` → PID → командная строка содержит `opencode`+`serve` → `taskkill /T /F`; чужое не гасим), гейт `managed` (Windows + loopback + `npx`; прод честно «недоступно»), `start-all.cmd` — сервис 3456 + переключатель `AI` (cp866), без таблиц/миграций; 7 развилок на гейте создателя | ✅ реализовано 2026-09-24 (коммит `0a5464f`) | 2026-09-24 | средний |
| `2026-09-24-ледяные-астероиды-вода.md` | Ледяные астероиды в поясе — **второй ресурс (лёд → «Вода неочищенная»)**, сырьё T0 цепочки «неочищенная вода → очищенная вода» (рецепт); расширение закрытой спеки `2026-09-22-пояса-малых-тел-этап-3-добыча`. Запас льда — **вторая колонка** `system_belts.ice_remaining` (миграция `000078`, nullable + CHECK, ленивая инициализация при `enter`); ресурс **уже в каталоге** (`goods.id 426`, `kind='resource'`, `source='manual'`) — резолв по точному `name_norm = "вода неочищенная"`, id не хардкод, код каталога не трогается (на проде ресурс нужно завести — данные создателя/студии); пакет захода отдаёт оба ресурса/запаса/класса/предела (`composition_ice`, `resource_ice`, `ice_class`, `remaining_level_ice`, `mined_ice`, `limits.rate_ice`) аддитивно; `collect{amount, resource}` с тройным клампом по выбранному ресурсу и **общим трюмом** (`mined + mined_ice + granted ≤ free`); `enter` — вход разрешён, если жив **хотя бы один** ресурс; `leave`/`FlushTx` кладут оба ресурса в трюм одной транзакцией; тип жилы в клиенте — **F2** `P(лёд) = ice/(ice+iron)` (порода — матрица), визуальное отличие + ледяной арт-ТЗ; фолбэк при отсутствии ресурса — ледяных жил нет, железо работает; железо (числа/классы/клампы) **не меняется**; числа создателя 2026-09-24: **20 т/астероид**, трюм 100 т за **2–3 мин** (`beltIceRate = 0.667·k_ice` — реально накапливаемая; `beltIceRateCap = 2×` — серверный потолок/анти-абьюз, в пакет не выводится), запас **20 000 т плоско** (1000 астероидов); подэтапы 4a–4d; принятый в бэклог кандидат ГД-сущности «Ледяное ядро-странник» (не в v1) | ✅ реализовано 2026-09-24 (коммит `96d71fb`) | 2026-09-24 | высокий |
| `2026-09-24-магазин-модулей-локальный-рынок.md` | Магазин модулей — витрина локального рынка (раздел «Модули», каркас «витрина с разделами»): справочник `market_offers` (4 базовых модуля `cargo_1`/`engine_1`/`radar_1`/`scanner_1`, бесконечный запас, миграция `000081`), `GET /api/planets/{id}/market` + `POST .../buy` (серверно-авторитетно: присутствие на орбите, живое поселение, слот `universal*`, атомарное списание «прочей тратой»), **трейд-ин 50 % зачёт в оплату** (печь кредитов без эмиссии, `money_operations.kind='purchase'`), дельта чтения модулей «эффект по типу из любого слота» (`RadarRadius`/`HasScanner`/`HasEngine`), вкладка «Магазин» в карточке планеты (видимость по знанию о поселениях, паттерн `can_buy_report`); продвинутые модули — вне v1 (лабораторное производство); цены 3 000 Cr — гипотеза к `@balancetester` | ✅ реализовано 2026-09-24 (коммит `a333827`) | 2026-09-24 | высокий |
| `2026-09-24-потребление-по-товарам-и-альтернативы.md` | Потребление по **конкретным товарам** (развилка Б, эпик «Экономика поселения»): ключ позиции потребления (`params.eat`/`params.effects`) — **только товар** `goods.name_norm` (категория как ключ **снята**, решение создателя О2 2026-09-24); нужда = позиция с типом эффекта, формула покрытия/дефицита **прежняя** (`w = clamp01(1 − покрытие/спрос)`) — ничего не обобщается; **альтернативы и Σ долей — будущее расширение** (кратко, без проектирования); миграция категорийных ключей: `продовольствие` → товар **«пища»** (378), мёртвый ключ «вода» удалён, «пища» сохранена как живой (дельта канона решения 11 — §15); **каскад стадий**: нужда на **всей** ладдере, резолв ступеней **по данным** (`params.stage` + `parent_id` типа-родителя «Поселение», 7 ступеней — добавлен пропущенный «Мегаполис» 152), иначе форпост «перестаёт пить» на пороге 1000 чел; **принятый риск** массовой жажды (заглушка `148×98 rate=10000` 0.01 против 20 т/сут, пар у верхних ступеней нет — настроит создатель; гейт нужды не вводим); `active_effects.source_position` — **побеждает спека стадий** (остаётся первой позицией группы, гварды по `effect_type_id`); новый тип эффекта **«Жажда»** на **общей кривой** `hunger`, `DeathCause` различает причины (`жажда` → код `thirst`, идентичность типа в `PlanetInput`); словарь **`goods.name_norm`** в owner-проходе (один запрос на пачку — иначе фича no-op); миграция **`000079`** (data-миграция `params` + строка `effect_types`, схема не меняется); норма «очищенная вода» **20 000 000** ед/сутки/млрд = 20 т/сут на 1000 чел (Р7, гипотеза к `@balancetester`); форма нужды для доски поставки + точка подключения «завоз»; студия (селект = товары, 422/предупреждения), витрина (строка нужды), impact map, кандидат ГД-сущности «Регенерация»; 6 открытых вопросов создателю (В3/В7/В9/В12/В13/В14) | 🔄 дизайн (итерация 2: ответы создателя О1–О6 + находки @critic внесены, **порция 2 внесена** 2026-09-24; готова к проверке создателем) | 2026-09-24 | высокий |
| `2026-09-24-постройка-структур-на-планете.md` | Постройка структур на планете из игрового интерфейса (кнопка «Построить», админ), ред. 3: инструмент песочницы — админ ставит на планету любую структуру дерева производителей. Модель — два существующих носителя: поселение → `settlements` (+`owner_type`/`owner_id`, nullable, пара или NULL), остальное → `buildings` (+`producer_type_id` FK RESTRICT), миграция `000082` (**без бэкфилла владельцев** — Г1); класс типа резолвится по корню дерева **по якорю** `default_settlement_type_id` (fallback по имени снят); владелец **обязателен** (Р13); **владелец только у новых** (Р8/Г1) — фракция расы, проставляет `EnsureSettlementOwners(since)` в генераторе фракций (только поселения текущей генерации; существующие остаются без владельца осознанно); **раса — по региону** планеты (Р9); класс-корень «Поселение» исключён из выбора; сразу потребляет (Р4, ленивый owner-проход); **вкладка «Строения»** (Р11, все постройки — Р14), кнопка в шапке + панель (Р12), строка владельца; **владелец виден игроку** при `presence`/`snapshot` (Г2) — `owner_name`/`type_name` внутри блоков, в `none` не отдаются; ручки `POST /admin/planets/{id}/structures` + `GET .../build-options` + `owner-candidates` (`q` ≥ 2); канон `13_settlements` §13.2/GLOSSARY §7 — уже верен (спека реализует, не меняет), правится §5.2 спеки контрактов; **ЧК2** — контракты «владелец-поселение платит» (отдельная спека, Г3); интерфейс — `@uidesigner` (§11); кандидат ГД-сущности «Стройка» | ✅ ЧК1 (инструмент «Построить» + владелец у поселений + вкладка «Строения») реализован 2026-09-24 (коммит `a217171`), принят создателем; ЧК2 (контракты «владелец-поселение платит») — отдельная спека | 2026-09-24 | высокий |
| `2026-09-24-каталог-экспорт-импорт-контента-на-прод.md` | Каталог: экспорт/импорт контента dev → прод (идея `2026-09-22_каталог-стабильный-код-экспорт-на-прод`): граница контента (таблицы студии — `categories`/`goods`/`recipes`/`recipe_components`/`producer_types`/`producer_slots`/`producer_recipes`/`producer_items`/`items`/`effect_types` + ссылка `generation_config.default_settlement_type_id`; настройки/объекты мира — нет), разворот 5 классов (принят файл-снимок JSON; SQL-дамп отклонён — `id` не переносимы), идентичность — **номерная метка `code`** (`g_0001`/`p_0001`; счётчик-`SEQUENCE`; неизменна; у `categories` метки нет — `(kind, name_norm)`, у листьев — метка родителя), полная замена (удаление того, чего нет в файле — по метке; защита от сноса живого: предсчёт мировых ссылок, в т.ч. против молчаливого `ON DELETE CASCADE` у `deposits`/`settlement_branch_buffers`), **первичная накатка** по имени для выравнивания dev↔прод, сухой прогон/diff перед применением, формат `content/catalog.json` (секции топологически по FK, метки обязательны; `items.unlocks` — по метке/натуральному ключу, не `id`), экспорт `GET /studio/api/content/export`, импорт `POST /studio/api/content/import` (атомарно, upsert без смены `id`), data-миграция канон-имён для баз с маркером (**номер `000083`**), обязательная правка `Dockerfile` (`content/` в образе) + бэкап `pg_dump`, UI студии (двухшаговая кнопка), impact map, итерации **И1 (метка + ренейм) / И2 (экспорт) / И3 (импорт с полной заменой)**, решения гейта О1–О5 закрыты; **итерация 5 (2026-09-25):** решения 2-го раунда **В1–В4 записаны** (первичная накатка на непустую цель разрешена — В1; ренейм-миграция остаётся; у `categories` метки нет; бэкап обязателен), закрыто противоречие F2 прогона И3 — метки `code` не переносимы между базами (бэкфилл по `id` на dev ↔ `DEFAULT`-счётчик сида на свежей БД) → режим `bootstrap` при **расхождении тождества** (сопоставление по `(kind,name_norm)` + переназначение метки, `id` сохранены), тест T16; открыты развилки **A (разовая накатка = В1, рекомендация) / B (стабильная метка «по смыслу», пересматривает О4) / C (перенос по имени — тупик)** и «прод — клон dev или свежая миграция» (§12.2) | 🔄 дизайн (готова к проверке создателем; итерация 5) | 2026-09-25 | высокий |
| `2026-09-25-студия-разделы-построек-по-видам.md` | Студия: разделы построек по видам — привязка через поле каталога `producer_types.section` (`colony`/`factory`/`lab`/`mining`/`energy`; носитель — тип-корень, подтипы наследуют; `NULL`/неизвестное → служебный «Прочее», не хранится; без `CHECK`), миграция `000085` (колонка + бэкфилл 6 канон-корней, идемпотентна) + сид, ручки POST/PUT `/studio/api/producers*` (+`section`, валидация на ручке) и `StateView`, интерфейс: ряд из 5 разделов вместо «Производители», снятые «Строения»/классы, селект «Раздел» в карточке корня и в «+ тип», пустые состояния, общий zoom/pan, дельта экспорта/импорта контента, 8 e2e на правку; кандидат ГД-сущности «отрасль постройки»; 4 открытых вопроса создателю | ✅ реализовано 2026-09-25 (коммит `2b32533`) | 2026-09-25 | высокий |
| `2026-09-25-внутреннее-хранилище-и-рождение-заказов.md` | Внутреннее хранилище и рождение заказов (ЧК2а эпика «Постройка структур на планете»): новая сущность-хранилище (ячейка = товар, изолированы, порог-доля, Σ=100 %; размер — свойство экземпляра `settlements.storage_size`, растёт со ступенью/населением), **замена** `settlement_branch_buffers` (ячейки — единственный запас; вход/выход ветки и питание населения — через ячейки), два вида потребности (населения с эффектом / производства без эффекта; производственные ячейки-вход показываются игроку под настройкой арифметики — D1), автор контракта `settlement` (плательщик — владелец; агент → счёт фракции — D4; `ErrPayerUnresolved` для легаси-поселения), авто-публикация заказов из складского дефицита, бесплатная публикация при нехватке денег (ослабление CHECK `contracts_live_has_escrow` только для supply-0; обход код-гейта `ErrInvalidReward` — D5, развилка режима ДО INSERT), «пока доля в работе — новых долей нет» (P1→P2; при `taken` цель = 0), базис покрытия = ячейка товара один раз при нескольких производителях (F6), замена буферов учтена у всех потребителей (снимок, контент-импорт, стадии, `truncateTables` — D2), минимальный UI (ячейки + доска), `code` в DTO ячейки (иконки); миграция `000086` (бронь), ревизия 3 (находки @critic N1–N7: ownerless не блокирует доску, единая формула комбинированного товара, порядок локов админ-ручки, честный второй писатель), **ревизия 4 (вторая порция решений F1–F4: доска пересчитывает хранилище поселений перед показом — свежесть вместо 30-мин лага, запись при изменении, блокировки разведены; входная ячейка делится между потребителями пропорционально потребности; доля ячейки ∝ вхождениям товара во входы рецептов; цепочка «выход → вход» — вариант A, со следующего пересчёта)**, все 30 вопросов гейта решены, гейт создателя пройден 2026-09-25 (ревизия 5); впереди разработка; кандидат ГД-сущности «Брошенное хранилище» — отложен | ✅ дизайн (ЧК2а) | 2026-09-25 | высокий |
| `2026-09-25-арт-студия-удаление-кораблей-из-игры.md` | Арт-студия: удаление кораблей из игры (вкладка «В игре»): реестр кораблей игры переезжает из Go-констант в статичный файл данных `config/ships_registry.json` (читается игрой при старте; для удаления достаточно перезапуска без пересборки; аварийный минимум `DefaultHumanShip`+`neutral` при битом файле), `POST /ships/ingame/delete` (по одному: запись реестра + PNG `web/static/sprites/` + PNG `ai_drafts/final_accepted/ships/` + запись `ships_meta.json` — **В2: accepted PNG удаляем**, мета/accepted связываются по sha256 (не по имени), канал восстановления закрыт полностью), защита нейтрального/людского дефолта (**последний корабль расы удалять можно — В1**; пустой пул расы — известное поведение), логика фолбэков игрок (людской дефолт) vs агент (нейтральный) — C2, принятая деградация выбора варианта при удалении + тест — C3, окно рассинхрона до перезапуска как штатный фолбэк И4 — C4, импортёр пишет JSON, сохраняет неизвестные поля, смоук на JSON — C5, тесты реестра data-agnostic — M1/M2, порядок «реестр → мета → accepted → PNG игры» — M5, пути-поля `Server` — M6; открытых вопросов нет (В1–В4 решены 2026-09-25) | 🔄 дизайн (готова к проверке создателем; итерация 2 — решения В1/В2, правки @critic) | 2026-09-25 | средний |
| `2026-09-25-сдача-груза-и-зачёт-ЧК2б.md` | Сдача груза и зачёт (ЧК2б эпика «Постройка структур на планете»): точка сдачи `POST /api/contracts/deliver {contract_id}` — игрок количество не выбирает, сервер берёт `min(⌊трюм⌋, остаток требования)`; **частичная сдача разрешена** (СМЕНА КАНОНА §0.11/§5.7/§7 канона-2026-09-23: пропорциональная выплата `⌊escrow_amount × сдано / остаток⌋`, остаток требования уменьшается, статус `taken`; полная — существующим `completeTx`, выпуск остатка залога); привезённое ложится **в ячейку внутреннего хранилища** поселения-автора (settlement/building), не в буфер; гейты — `type='supply'`, `status='taken'`, исполнитель-игрок, не просрочен (`ExpireDue`), **орбита** планеты заказа (surface нет; спутник → родитель); «не то»/«лишнее» — адресация по `good_id` требования и кламп по остатку; **одна транзакция** (contracts → requirement → `player_cargo` → ячейка → accounts, `TakeCargoTx` в `internal/cargo`); `escrow_amount` = **остаток залога** при частичных (без миграции, канон-дельта); новый тип `contract_log` `delivered`; миграция **не нужна** (иначе F2-B → `000090`); минимальный UI «Мои заказы (в работе)» + «Сдать»; кандидат ГД-сущности «Обратный рейс»; 4 открытых вопроса создателю (UI, семантика эскроу, переполнение ячейки, частичная сдача + истечение); T1–T19 | ✅ принята создателем 2026-09-25 (готова к разработке) | 2026-09-25 | высокий |
| `2026-09-26-собственность-игрока-в-дашборде.md` | Вкладка «Собственность» в дашборде игрока (ЧК1: список + «Посмотреть»; ЧК2-задел: «Перелететь»): новая read-модель `GET /me/property` (владелец из JWT; поселения + строения `owner_type='player'`; единый `items[]` с `kind` — расширяемо; новых сущностей владения нет; миграция `000090` — индекс `idx_buildings_owner`), модуль `web/static/js/dashboard/property.js` (тонкий HTML: одна строка-плейсхолдер в 900-строчном `index.html`), пустое состояние; строка владения видна всегда, состояние — по знанию (плашка `presence`/`snapshot`/`scan`/`none`, без утечек); стык «дашборд ↔ карта» — вариант A: переход `/map?system=<world_id>&planet=<planet_id>` + новый `map/deeplink.js` (авто-открытие `openSystemModal` + затирание URL); ЧК2 — `&fly=1` на существующей цепочке полётов (99.2.30/99.2.27), гейты двигатель/активный полёт/уже на месте; кандидат ГД-сущности «Заказ на создание объекта» (путь приобретения собственности); зависимости и стыки для @critic — §11; вопросы В1–В4 закрыты | ✅ ЧК1+ЧК2 реализованы 2026-09-26 (приняты создателем) | 2026-09-26 | высокий |
| `2026-09-25-ускоритель-и-мини-игра-прокладка-маршрута.md` | Модуль корабля «ускоритель» (`equipment.type='accelerator'`, новый тип в CHECK, **выделенный слот `accelerator`**, стартовый модуль `accel_1` выдаётся) + мини-игра «Прокладка маршрута» для ускорения межзвёздного перелёта: **серверное ускорение = перебазирование сегмента** (механизм 61a: `P` из интерполяции + новый `StartTime`/`Duration`, `BoostFlight` под мьютексом) — формула `timeSince(StartTime)/Duration` и `Restore` **не меняются**; награда — **бонус скорости +10 %…+50 %** (`bonus = 0.10 + 0.40·q`, `newRem = remaining/(1+bonus)`), **одно активное ускорение на сегмент** (повтор → 409 `already_active`; сброс при смене цели/прибытии — складывания/компаунда нет); **два порога остатка** (показ `min_remaining_offer_s`, отправка `min_remaining_boost_s`); валидация `0.10 ≤ bonus_max ≤ 1.0`; прогрессия — по силе ускорения (`bonus_max`; **в v9 ось не читается — развилка §14.14**), откат — константа/гейт; откат — **атрибут игрока** (таблица `player_accelerator`, реальное время, переживает рестарт, НЕ в `truncateTables`); **только ускорение, без штрафов**; «паспорт перелёта» — **производная** из существующих данных (дальность, спектр/тип/температура концов, пояс системы назначения; аномалий на пути нет; масса/возраст и регион **не входят**); поле мини-игры — детерминированно от seed `hash(from,to)` (не от «живого» остатка), оценка на сервере (`L_eff`/`q`); точный прогноз прибытия до отправки убран (только грубая обратная связь); экран — отдельная страница `web/route.html` (mobile-first); сброс при развороте — структурный (`fingerprint` + новый сегмент); кандидат ГД-сущности «Навигационные маяки»; миграция `000089`; **граница чекпоинтов:** ЧК1 — тип `accelerator`, слот, состояние отката, `BoostFlight`/`Booster`/`SetBooster` и аддитивные блоки `/travel`/`/me` (до ЧК3 игра не зарегистрирована → `available=false`, `reason=unknown_game`), а `offer`/`boost` и UI — ЧК3; развилки О4/О6/О8/О10 решены; **разбор по факторам после `boost`** (§14.13: аддитивное поле `breakdown` — 16 кодов §14.7 + «Находка»/«Попутный поток»/«Помехи», полосы значимости 1–4, итог остаётся кривой §14.2, арифметика не обещается; подача — `@uidesigner`, полосы — калибровка) | ✅ реализовано 2026-09-26 | 2026-09-26 | высокий |
| `2026-09-25-маршрут-мини-игра-интерфейс.md` | Мини-игра «Прокладка маршрута» — интерфейс экрана доски v9 (mobile-first, `@uidesigner`): сетка и путь по клеткам (4-связная цепочка), разведка импульсами (`scan`), локальные метки курса/«жар», легенда-попап (четыре свёрнутые группы), космический словарь создателя (Помехи/Мгла/Трасса/Течение/Кордон/Тоннель/Обрыв/Разрыв), результат с отрицательным `bonus`, разбор по факторам `#result-breakdown` (§4.7.1: группы ошибки→находки→не ошибка, строка с глифом/полосой без цифр/`×N`, «Чистый курс») и три новых глифа «Петля»/«Перелёт Цели»/«Зонд впустую» (§12.14); серверный контракт `offer`/`scan`/`boost` фиксирован (осн. спека §14) | ✅ реализовано 2026-09-26 | 2026-09-26 | высокий |
| `2026-09-26-мир-прогулки-лодка.md` | Мир прогулки: лодка (ЧК6.3) — задел N4 спеки воды §7 | ✅ реализовано 2026-09-26 (коммит 3f2020c) | 2026-09-26 | средний |
