// Package audit — обобщённый движок аудита Run[T]: прогоняет набор правил по
// представлениям сущностей и агрегирует результат. Что именно проверяется —
// планеты, звёзды, фракции — задаётся типом T и правилами.
package audit