// Package aiserve — управление локальным ИИ-помощником студии товаров
// (`opencode serve`, OPENCODE_URL): вычисление состояния по факту проб,
// запуск/гашение процесса (спека 2026-09-24-студия-управление-локальным-ии).
//
// Управление возможно только на Windows + loopback + npx в PATH (managed);
// на проде (не Windows / нет npx) ручки честно отвечают «управление недоступно».
// Состояние выводится по живой пробе порта/health и опознанию процесса, а не по
// памяти: кэшируется только вердикт по процессам (5 с), под sync.RWMutex
// (AGENTS.md §0). Клиент ИИ (internal/goodsstudio/ai) не затрагивается.
package aiserve
