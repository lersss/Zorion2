// Package npc — фоновый планировщик NPC-агентов (спека 20a.1).
//
// NPCManager — одна горутина, тик каждые npcTickInterval: batch-обработка
// агентов по курсору (приоритет прибытий над стартами), выбор маршрута в
// радиусе 1000, длительность полёта dist × npcSpeedFactor (мин. 3с, без
// потолка). Позиции для карты — in-memory snapshot (atomic.Pointer).
// Единственный писатель состояния агентов (спека §9 И1).
package npc