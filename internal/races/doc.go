// internal/races — каталог рас (спека 99.2.21).
//
// Раса — справочная сущность-каталог (config/races.json, 60 записей), как
// архетипы планет. Проявление в мире — поселения (settlements.race_id,
// NULL = легаси/люди). Пакет нейтрален: его импортируют galaxy (раздача
// территорий) и settlement (пригодность + генерация поселений рас).
package races