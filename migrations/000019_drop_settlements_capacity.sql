-- settlements.capacity — рудимент (B19): «ёмкость = population × 2» нигде не
-- читалась, а аграрная ёмкость по модели не хранится вовсе — она выводится
-- формулой прироста населения из среды планеты на лету (13_tiers.md §13.8).
-- Решение игрока 2026-09-13: колонку удалить.
ALTER TABLE settlements DROP COLUMN IF EXISTS capacity;