-- regions.race_id — доминантная раса территории (спека 99.2.21 §7.1, идея 56a).
-- NULL = территория без назначенной расы (до раздачи; легаси-вселенные).
-- Заполняется генератором галактики (раздача 50 территорий по 50 расам).
ALTER TABLE regions ADD COLUMN IF NOT EXISTS race_id TEXT;